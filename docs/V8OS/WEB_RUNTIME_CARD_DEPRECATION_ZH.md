# Web Runtime 状态卡退役记录

- 状态：产品面已退役；运行摘要统一进入 Web Workbench，Phone 保留轻量摘要抽屉。
- 消息气泡：继续承载思考、工具调用和协作过程，不复用侧栏摘要数组覆盖气泡明细。
- 兼容窗口：`RuntimeDock` 与 `RuntimeTimelinePanel` 仅保留两个完整迭代供旧调用迁移，不再有可见入口。
- 删除门禁：连续两个迭代确认调用量为零后物理删除；期间新增调用应在代码评审中拒绝。
- 验收：Web 主路径不得渲染 Runtime 大卡片或逐条事件时间线；侧栏只保留阶段、审批/阻断、交接、关键产物和最终状态。
