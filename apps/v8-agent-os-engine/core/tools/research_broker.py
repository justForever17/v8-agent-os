from __future__ import annotations

import hashlib
import asyncio
import json
import re
import threading
import time
import uuid
import concurrent.futures
import os
from concurrent.futures import ThreadPoolExecutor, as_completed
from datetime import datetime, timedelta, timezone
from functools import lru_cache
from pathlib import Path
from typing import Annotated, Any
from urllib.parse import urlparse

import requests
from langchain_core.tools import InjectedToolCallId, tool
from langgraph.prebuilt import InjectedState

from core.background_context_guard import prepare_background_model_messages
from core.background_model_output import extract_reasoning_token_count, sanitize_background_model_output
from core.model_thinking_control import no_think_request_patch
from core.research_runtime_prompts import (
    RESEARCH_PROMPT_CONTRACT_VERSION,
    build_research_runtime_system_prompt,
    research_runtime_prompt_digest,
)
from core.storage import storage
from core.tools.research_ledger import (
    archive_experience_pack,
    delete_experience_pack,
    get_evidence_bundle,
    get_experience_pack,
    list_evidence_bundles,
    promote_experience_pack,
    research_ledger_summary,
    restore_experience_pack,
    search_experience_packs_with_options,
    store_evidence_bundle,
)
from core.tools.research_quality import (
    MIN_RESEARCH_ANSWER_CHARS,
    MIN_RESEARCH_DISTINCT_HOST_COUNT,
    MIN_RESEARCH_SOURCE_BODY_CHARS,
    MIN_RESEARCH_SOURCE_COUNT,
    TARGET_RESEARCH_ANSWER_CHARS,
    TARGET_RESEARCH_CLAIM_COUNT,
    TARGET_RESEARCH_DISTINCT_HOST_COUNT,
    TARGET_RESEARCH_SOURCE_COUNT,
    build_research_review_binding,
    research_acceptance_metrics,
    research_acceptance_issues,
    research_document_date_candidates,
    research_effective_answer_chars,
    research_high_quality_issues,
    research_quality_tier,
    research_requires_dated_sources,
    research_review_decision,
    research_selected_sources,
    research_source_has_dated_evidence,
)
from core.tools.research_source_identity import (
    canonical_source_url,
    research_document_identity,
    research_document_priority,
    research_source_is_navigation,
)
from core.tools.tool_execution_envelope import ToolExecutionEnvelope, classify_failure
from core.tools.web_fetcher import (
    MAX_RESEARCH_TEXT_CHARS,
    source_router_search,
    web_read,
    web_search,
)
from runtimes.extensions.mcp.client import mcp_manager


_EVIDENCE_TTL_SECONDS = 6 * 60 * 60
# Per parallel search batch. Architect synthesis has its own downstream
# budget and is intentionally not wrapped by this value.
_RESEARCH_TOOL_DEADLINE_MS = 120_000
_RESEARCH_SHARD_DEADLINE_MS = 45_000
_RESEARCH_SOURCE_READ_DEADLINE_MS = 35_000
_RESEARCH_MAX_PARALLEL_SEARCH_SHARDS = 2
# One low-output provider needs four bounded writer sections, two independent
# reviews, and enough reserve for one reviewer-guided rewrite. This is a hard
# ceiling; accepted runs still return as soon as the chain completes.
_RESEARCH_ARCHITECT_SYNTHESIS_DEADLINE_MS = 360_000
_MAX_RESEARCH_ARCHITECT_FULL_SYNTHESIS_ATTEMPTS = 3
_MAX_RESEARCH_READ_ATTEMPTS = 2
_RESEARCH_SOURCE_READ_CHARS = MAX_RESEARCH_TEXT_CHARS
_RESEARCH_SOURCE_CAPTURE_CHARS = 32_000
_RESEARCH_ARCHITECT_SOURCE_TEXT_CHARS = 32_000
_RESEARCH_ARCHITECT_EVIDENCE_CANDIDATE_COUNT = 6
_RESEARCH_ARCHITECT_PLAN_MAX_TOKENS = 4_000
_RESEARCH_ARCHITECT_QUERY_PLAN_MAX_TOKENS = 3_200
_RESEARCH_ARCHITECT_QUERY_PLAN_TIMEOUT_SECONDS = 60
_RESEARCH_ARCHITECT_DECISION_MAX_TOKENS = 1_600
_RESEARCH_ARCHITECT_DECISION_TIMEOUT_SECONDS = 28
_RESEARCH_ARCHITECT_MIN_PLAN_REPAIR_SECONDS = 8.0
_RESEARCH_ARCHITECT_ANSWER_MAX_TOKENS = 12_000
_RESEARCH_ARCHITECT_REVIEW_MAX_TOKENS = 1_800
_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT = 24
_RESEARCH_ARCHITECT_PLAN_MAX_CLAIM_COUNT = 12
_RESEARCH_ARCHITECT_IDEAL_CLAIM_COUNT = max(
    _RESEARCH_ARCHITECT_PLAN_MAX_CLAIM_COUNT,
    TARGET_RESEARCH_CLAIM_COUNT,
)
# Evidence planning receives compact, query-focused excerpts rather than full
# page bodies. Keep enough independently read documents for broad comparisons
# after reviewer-guided repair; sixteen routinely discarded the exact MCP,
# pricing, or privacy pages that a later round had just acquired.
_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT = 24
_RESEARCH_ARCHITECT_ANSWER_COMPLETE_MARKER = "<!-- research-answer-complete -->"
_RESEARCH_ARCHITECT_SECTION_COMPLETE_MARKER_PREFIX = "research-section-complete"
_RESEARCH_ARCHITECT_SECTION_MAX_TOKENS = 3_200
_RESEARCH_ARCHITECT_COHERENT_REVISION_MIN_TOKENS = 8_192
_RESEARCH_ARCHITECT_IDEAL_ANSWER_MIN_CHARS = 6_500
_RESEARCH_ARCHITECT_IDEAL_ANSWER_MAX_CHARS = 11_000
# Raw Markdown includes citation labels, temporal boundaries, source titles,
# and compact excerpts beyond the effective-content target.  Keep enough room
# for an evidence-rich deterministic report so it is not replaced by a lower-
# quality model rewrite merely for crossing an arbitrary presentation margin.
_MAX_RESEARCH_VISIBLE_ANSWER_CHARS = 24_000
_EVIDENCE_LEDGER: dict[str, dict[str, Any]] = {}


class _ResearchReadAttemptLedger:
    """Coordinate URL reads across parallel shards and bounded repair rounds."""

    def __init__(
        self,
        *,
        question: str = "",
        terminal_identities: set[str] | None = None,
    ) -> None:
        self.question = _safe_text(question)
        self._lock = threading.Lock()
        self._records: dict[str, dict[str, Any]] = {}
        self._aliases: dict[str, str] = {}
        self._dedupe_skips = 0
        self._cached_projection_count = 0
        for identity in set(terminal_identities or set()):
            normalized = _safe_text(identity)
            if normalized:
                self._records[normalized] = {
                    "attempts": 1,
                    "lastRound": 0,
                    "state": "terminal",
                    "ready": threading.Event(),
                }
                self._records[normalized]["ready"].set()

    def identity(self, url: Any) -> str:
        return _research_document_identity(
            _safe_text(url),
            question=self.question,
        )

    def _root(self, identity: str) -> str:
        root = identity
        visited: set[str] = set()
        while root in self._aliases and root not in visited:
            visited.add(root)
            root = self._aliases[root]
        return root

    def claim(self, url: Any, *, round_index: int) -> str:
        identity = self.identity(url)
        if not identity:
            return ""
        bounded_round = max(1, int(round_index or 1))
        with self._lock:
            root = self._root(identity)
            record = self._records.get(root)
            if record is not None:
                state = _safe_text(record.get("state"))
                attempts = int(record.get("attempts") or 0)
                last_round = int(record.get("lastRound") or 0)
                if (
                    state in {"inflight", "terminal"}
                    or attempts >= _MAX_RESEARCH_READ_ATTEMPTS
                    or last_round >= bounded_round
                ):
                    self._dedupe_skips += 1
                    return ""
                if state != "retryable":
                    self._dedupe_skips += 1
                    return ""
            else:
                record = {
                    "attempts": 0,
                    "lastRound": 0,
                    "state": "unseen",
                    "ready": threading.Event(),
                }
                self._records[root] = record
            record["attempts"] = int(record.get("attempts") or 0) + 1
            record["lastRound"] = bounded_round
            record["state"] = "inflight"
            ready = record.get("ready")
            if isinstance(ready, threading.Event):
                ready.clear()
            self._aliases[identity] = root
            return root

    def finish(
        self,
        claim_identity: str,
        *,
        final_url: Any = "",
        retryable: bool,
        cache_payload: dict[str, Any] | None = None,
    ) -> None:
        if not claim_identity:
            return
        with self._lock:
            root = self._root(claim_identity)
            record = self._records.get(root)
            if record is None:
                return
            record["state"] = (
                "retryable"
                if retryable
                and int(record.get("attempts") or 0) < _MAX_RESEARCH_READ_ATTEMPTS
                else "terminal"
            )
            if cache_payload:
                record["cachePayload"] = dict(cache_payload)
            final_identity = self.identity(final_url)
            if final_identity:
                existing_root = self._root(final_identity)
                existing = self._records.get(existing_root)
                if existing is not None and existing_root != root:
                    existing["state"] = "terminal"
                    record["state"] = "terminal"
                self._aliases[final_identity] = root
            ready = record.get("ready")
            if isinstance(ready, threading.Event):
                ready.set()

    def cached_payload(
        self,
        url: Any,
        *,
        wait_seconds: float = 0.0,
    ) -> dict[str, Any] | None:
        """Reuse one successful document read for another facet projection."""

        identity = self.identity(url)
        if not identity:
            return None
        with self._lock:
            root = self._root(identity)
            record = self._records.get(root)
            ready = record.get("ready") if record is not None else None
        if isinstance(ready, threading.Event) and wait_seconds > 0:
            ready.wait(timeout=max(0.0, float(wait_seconds)))
        with self._lock:
            root = self._root(identity)
            record = self._records.get(root)
            payload = record.get("cachePayload") if record is not None else None
            if not isinstance(payload, dict):
                return None
            self._cached_projection_count += 1
            return dict(payload)

    def mark_terminal(self, url_or_identity: Any, *, already_identity: bool = False) -> None:
        identity = (
            _safe_text(url_or_identity)
            if already_identity
            else self.identity(url_or_identity)
        )
        if not identity:
            return
        with self._lock:
            root = self._root(identity)
            record = self._records.setdefault(
                root,
                {
                    "attempts": 0,
                    "lastRound": 0,
                    "state": "terminal",
                    "ready": threading.Event(),
                },
            )
            record["state"] = "terminal"
            ready = record.get("ready")
            if isinstance(ready, threading.Event):
                ready.set()
            self._aliases[identity] = root

    def snapshot(self) -> dict[str, int]:
        with self._lock:
            records = list(self._records.values())
            return {
                "uniqueIdentityCount": len(records),
                "networkAttemptCount": sum(int(item.get("attempts") or 0) for item in records),
                "retryableIdentityCount": sum(
                    _safe_text(item.get("state")) == "retryable" for item in records
                ),
                "terminalIdentityCount": sum(
                    _safe_text(item.get("state")) == "terminal" for item in records
                ),
                "deduplicatedReadCount": self._dedupe_skips,
                "cachedProjectionCount": self._cached_projection_count,
            }
_AUTHORITATIVE_HOST_HINTS = (
    "learn.microsoft.com",
    "cloud.google.com",
    "docs.aws.amazon.com",
)
_PUBLIC_INSTITUTION_HOST_SUFFIXES = (
    ".gov",
    ".gov.uk",
    ".gov.cn",
    ".gov.au",
    ".govt.nz",
    ".gouv.fr",
    ".go.jp",
    ".go.kr",
    ".gc.ca",
    ".admin.ch",
    ".edu",
    ".edu.cn",
    ".ac.uk",
    ".ac.jp",
    ".int",
)
_LOW_QUALITY_HOST_HINTS = (
    "pinterest.",
    "quora.",
    "reddit.",
    "medium.",
    "zhihu.",
    "juejin.",
    "csdn.",
    "cnblogs.",
    "stackoverflow.com/questions",
)
_RESEARCH_SOURCE_CATALOG_PATH = Path(__file__).resolve().parents[2] / "runtimes" / "research" / "assets" / "source_quality_catalog.json"
_VIDEO_RESEARCH_TERMS = (
    "video",
    "youtube",
    "bilibili",
    "tiktok",
    "douyin",
    "shorts",
    "reel",
    "views",
    "likes",
    "ranking",
    "leaderboard",
    "trending",
    "视频",
    "短视频",
    "榜单",
    "排行",
    "播放",
    "点赞",
    "爆款",
)
_RELEVANCE_STOPWORDS = {
    "about",
    "analysis",
    "are",
    "best",
    "cite",
    "compare",
    "comparison",
    "current",
    "documentation",
    "evidence",
    "for",
    "how",
    "latest",
    "official",
    "practice",
    "practices",
    "primary",
    "python",
    "report",
    "research",
    "should",
    "source",
    "sources",
    "statement",
    "the",
    "tools",
    "update",
    "used",
    "using",
    "what",
    "when",
    "where",
    "which",
    "with",
}
_URL_IN_TEXT_RE = re.compile(r"https?://[^\s)\]}>\"']+", re.IGNORECASE)
_POPULARITY_RE = re.compile(
    r"(?P<number>\d+(?:[\.,]\d+)?)\s*(?P<unit>k|m|b|万|亿)?\s*(?P<label>views?|likes?|播放|观看|点赞|赞|收藏|shares?|comments?|评论|弹幕)",
    re.IGNORECASE,
)
_SOURCE_NOISE_MARKERS = (
    "about press copyright contact us creators",
    "privacy policy & safety how youtube works",
    "security check required",
    "we've detected unusual activity",
    "unusual activity from your network",
    "verify you are human",
    "just a moment",
    "cloudflare",
    "captcha",
    "access denied",
    "enable javascript",
    "please enable cookies",
    "navigation - index - modules",
    "theme auto light dark",
)
_JINA_READER_URL_PREFIX = "https://r.jina.ai/"


def _utc_now_iso() -> str:
    return datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")


def _research_config() -> dict[str, Any]:
    try:
        raw = dict((storage.get_supervisor_config() or {}).get("research") or {})
    except Exception:
        raw = {}
    enabled = raw.get("enabled")
    default_shards = _as_int(raw.get("defaultShardCount"), 10)
    max_shards = _as_int(raw.get("maxShardCount"), 30)
    max_rounds = _as_int(raw.get("maxRounds"), 5)
    return {
        "enabled": True if enabled is None else bool(enabled),
        "defaultShardCount": max(1, min(default_shards, 30)),
        "maxShardCount": max(1, min(max_shards, 30)),
        "maxRounds": max(1, min(max_rounds, 5)),
        "evidenceTtlSeconds": max(60, _as_int(raw.get("evidenceTtlSeconds"), _EVIDENCE_TTL_SECONDS)),
        "architectAgentSynthesisEnabled": bool(raw.get("architectAgentSynthesisEnabled", True)),
        "architectAgentTimeoutSeconds": max(5, min(_as_int(raw.get("architectAgentTimeoutSeconds"), 60), 90)),
    }


def _as_int(value: Any, default: int) -> int:
    try:
        return int(value)
    except (TypeError, ValueError):
        return default


def _safe_text(value: Any) -> str:
    return str(value or "").strip()


def _research_score_label(confidence: Any, authority_score: Any, *, reuse_decision: Any = "") -> str:
    confidence_text = _safe_text(confidence) or "unknown"
    try:
        authority = round(float(authority_score), 1)
    except (TypeError, ValueError):
        authority = None
    parts = [f"confidence={confidence_text}"]
    if authority is not None:
        parts.append(f"authority={authority}")
    if reuse_decision:
        parts.append(f"reuse={_safe_text(reuse_decision)}")
    return " / ".join(parts)


def _research_source_pack(source: dict[str, Any]) -> dict[str, Any]:
    url = _safe_text(source.get("url"))
    if not url:
        return {}
    payload = {
        "sourceId": _safe_text(source.get("sourceId")) or _source_id(url),
        "title": _safe_text(source.get("title") or source.get("sourceTitle") or source.get("host") or url)[:220],
        "url": url,
        "host": _safe_text(source.get("host")) or _host(url),
        "authorityScore": source.get("authorityScore"),
        "freshness": source.get("freshness") or source.get("freshnessWindow"),
        "relevance": source.get("relevanceScore") or source.get("score"),
        "tier": source.get("tier") or source.get("authorityTier"),
        "provider": source.get("provider"),
        "citationKey": source.get("citationKey"),
        "selectedForEvidence": source.get("selectedForEvidence"),
        "retrievedAt": source.get("retrievedAt"),
        "publishedAt": source.get("publishedAt"),
        "updatedAt": source.get("updatedAt"),
        "sourceDate": source.get("sourceDate"),
        "sourceDateKind": source.get("sourceDateKind"),
        "version": source.get("version"),
        "temporalEvidence": source.get("temporalEvidence"),
        "subjectFocused": source.get("subjectFocused"),
        "researchFacetId": source.get("researchFacetId"),
        "researchFacetIds": list(source.get("researchFacetIds") or []),
        "researchFacetGoal": source.get("researchFacetGoal"),
        "evidenceQuery": source.get("evidenceQuery"),
        "contentChars": source.get("contentChars"),
        "readEvidence": source.get("readEvidence"),
        "qualityDimensions": (source.get("sourceQualityGate") or {}).get("qualityDimensions") or source.get("qualityDimensions"),
    }
    return {key: value for key, value in payload.items() if value not in (None, "", [], {})}


def _research_answer_from_pack(pack: dict[str, Any], payload: dict[str, Any]) -> str:
    for key in ("researchResult", "answer"):
        value = _safe_text(pack.get(key))
        if value:
            return value
    return ""


def _is_low_quality_research_answer(text: str) -> bool:
    normalized = _safe_text(text)
    lowered = normalized.lower()
    if not normalized:
        return False
    hard_noise_markers = (
        "about press copyright contact us creators",
        "privacy policy & safety how youtube works",
        "security check required",
        "we've detected unusual activity",
        "unusual activity from your network",
        "captcha",
        "access denied",
        "verify you are human",
        "cloudflare",
        "no reliable source-backed findings were collected",
        "could not synthesize a reliable result",
        "reused research experience pack, but no detailed research result",
        "no source-backed research result was synthesized",
    )
    if any(marker in lowered for marker in hard_noise_markers):
        return True
    if re.search(r"collected\s+\d+\s+ranked\s+source", lowered):
        return True
    process_markers = ("本次调研", "当前调研", "调研过程", "数据源均未产出", "未获取到有效内容", "无法获取目标文献")
    compact_chars = len(re.sub(r"\s+", "", normalized))
    if (
        compact_chars < MIN_RESEARCH_ANSWER_CHARS
        and any(marker in normalized for marker in process_markers)
        and any(
            marker in normalized
            for marker in ("未提供", "无法", "未产出", "安全验证", "浏览器检测", "建议重新调研")
        )
    ):
        return True
    return False


def _research_answer_pack(payload: dict[str, Any]) -> dict[str, Any]:
    pack = payload.get("finalExperiencePack") if isinstance(payload.get("finalExperiencePack"), dict) else {}
    if not pack and isinstance(payload.get("researchResult"), dict):
        pack = payload.get("researchResult") or {}
    answer = _research_answer_from_pack(pack, payload)
    answer_was_low_quality = _is_low_quality_research_answer(answer)
    low_quality_answer_note = _compact_research_text(answer, limit=360) if answer_was_low_quality else ""
    if answer_was_low_quality:
        answer = ""
    source_candidates = research_selected_sources(payload)
    evidence_bank = payload.get("researchEvidenceBank") if isinstance(payload.get("researchEvidenceBank"), dict) else {}
    sources: list[dict[str, Any]] = []
    seen: set[str] = set()
    for source in source_candidates:
        compact = _research_source_pack(source)
        url = _safe_text(compact.get("url"))
        if not url or url in seen:
            continue
        seen.add(url)
        sources.append(compact)
        if len(sources) >= _RESEARCH_ARCHITECT_MAX_SOURCE_COUNT:
            break
    limitations = []
    for value in list(pack.get("limitations") or []) + list(pack.get("missingEvidence") or payload.get("missingEvidence") or []) + list(pack.get("assumptions") or payload.get("assumptions") or []):
        text = _compact_research_text(value, limit=280)
        if text and text not in limitations:
            limitations.append(text)
        if len(limitations) >= 6:
            break
    if low_quality_answer_note and low_quality_answer_note not in limitations:
        limitations.insert(0, low_quality_answer_note)
    claim_table = []
    for item in list(
        pack.get("claimTable") or payload.get("claimTable") or evidence_bank.get("claims") or []
    )[:_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT]:
        if isinstance(item, dict):
            claim_table.append(
                {
                    key: value
                    for key, value in {
                        "claimId": item.get("claimId"),
                        "claim": _compact_research_text(item.get("claim"), limit=320),
                        "claimType": item.get("claimType") or item.get("claimKind"),
                        "normativeCue": _compact_research_text(item.get("normativeCue"), limit=160),
                        "sourceRole": item.get("sourceRole"),
                        "sourceClaim": _compact_research_text(item.get("sourceClaim"), limit=320),
                        "supportingSources": list(item.get("supportingSources") or [])[:4],
                        "confidence": item.get("confidence"),
                        "evidenceExcerptKey": item.get("evidenceExcerptKey"),
                        "evidenceExcerpt": _compact_research_text(item.get("evidenceExcerpt"), limit=600),
                        "evidenceExcerptSha256": item.get("evidenceExcerptSha256"),
                        "evidenceVerified": item.get("evidenceVerified") is True,
                    }.items()
                    if value not in (None, "", [], {})
                }
            )
    rejected_evidence = []
    for item in list(evidence_bank.get("rejectedSources") or payload.get("rejectedSources") or [])[:8]:
        if not isinstance(item, dict):
            continue
        rejected_evidence.append(
            {
                key: value
                for key, value in {
                    "sourceId": item.get("sourceId"),
                    "title": _compact_research_text(item.get("title"), limit=160),
                    "url": item.get("url"),
                    "reason": item.get("reason") or item.get("rejectedReason"),
                }.items()
                if value not in (None, "", [], {})
            }
        )
    reuse = payload.get("experienceReuse") if isinstance(payload.get("experienceReuse"), dict) else {}
    reuse_decision = _safe_text(reuse.get("reuseDecision"))
    confidence = pack.get("confidence") or payload.get("confidence")
    authority_score = pack.get("authorityScore") or payload.get("authorityScore")
    review_decision = research_review_decision(payload)
    validation_payload = {
        **payload,
        "reviewDecision": review_decision,
        "researchAnswerPack": {
            "reviewDecision": review_decision,
            "answer": answer,
            "sources": sources,
            "claimTable": claim_table,
            "asOf": pack.get("asOf") or payload.get("asOf"),
            "criticalMissingEvidence": pack.get("criticalMissingEvidence") or payload.get("criticalMissingEvidence") or [],
            "independentReview": pack.get("independentReview") or payload.get("independentReview") or {},
        },
    }
    minimum_issues = research_acceptance_issues(validation_payload)
    missing_reasons = research_high_quality_issues(validation_payload)
    metrics = research_acceptance_metrics(validation_payload)
    if answer_was_low_quality:
        missing_reasons.append("low_quality_answer_surface")
    quality_tier = research_quality_tier(validation_payload)
    delivery_ready = quality_tier == "high_quality" and not answer_was_low_quality
    if not delivery_ready:
        answer = ""
    evidence_id = _safe_text(payload.get("evidenceBundleId"))
    return {
        "kind": "research_answer_pack",
        "reviewDecision": review_decision,
        "reviewReasons": list(pack.get("reviewReasons") or payload.get("reviewReasons") or [])[:12],
        "answer": answer,
        "sources": sources,
        "claimTable": claim_table,
        "independentReview": pack.get("independentReview") or payload.get("independentReview") or {},
        "rejectedEvidence": rejected_evidence,
        "score": {
            "label": _research_score_label(confidence, authority_score, reuse_decision=reuse_decision),
            "confidence": confidence,
            "authorityScore": authority_score,
            "reuseDecision": reuse_decision or None,
            "qualityStatus": quality_tier,
            "qualityTier": quality_tier,
            "minimumQualified": not minimum_issues,
            "deliveryReady": delivery_ready,
            "acceptanceMetrics": metrics,
        },
        "limitations": limitations,
        "criticalMissingEvidence": list(pack.get("criticalMissingEvidence") or payload.get("criticalMissingEvidence") or [])[:12],
        "recommendedNextQueries": list(pack.get("recommendedNextQueries") or payload.get("recommendedNextQueries") or [])[:12],
        "asOf": pack.get("asOf") or payload.get("asOf"),
        "missingOrStaleReasons": list(dict.fromkeys(missing_reasons)),
        "reuseDecision": reuse or None,
        "detailRef": {
            "evidenceBundleId": evidence_id,
            "tool": f"research_broker(mode='get_evidence', evidenceBundleId='{evidence_id}')" if evidence_id else "research_broker(mode='get_evidence', evidenceBundleId=...)",
        },
        "recommendedNextAction": "use_research_answer_pack" if delivery_ready else "continue_research",
    }


def _as_list(values: Any) -> list[str]:
    if values is None:
        return []
    if isinstance(values, str):
        raw_values = re.split(r"[,\n;]+", values)
    else:
        raw_values = list(values or [])
    result: list[str] = []
    for item in raw_values:
        text = _safe_text(item)
        if text and text not in result:
            result.append(text)
    return result


@lru_cache(maxsize=1)
def _source_catalog() -> dict[str, Any]:
    try:
        payload = json.loads(_RESEARCH_SOURCE_CATALOG_PATH.read_text(encoding="utf-8"))
    except Exception:
        payload = {"entries": []}
    entries = payload.get("entries") if isinstance(payload.get("entries"), list) else []
    host_query_aliases = (
        payload.get("hostQueryAliases")
        if isinstance(payload.get("hostQueryAliases"), dict)
        else {}
    )
    host_query_rewrites = (
        payload.get("hostQueryRewrites")
        if isinstance(payload.get("hostQueryRewrites"), dict)
        else {}
    )
    return {
        "version": payload.get("version"),
        "entries": [entry for entry in entries if isinstance(entry, dict)],
        "hostQueryAliases": {
            _safe_text(host).lower(): _as_list(aliases)
            for host, aliases in host_query_aliases.items()
            if _safe_text(host)
        },
        "hostQueryRewrites": {
            _safe_text(host).lower(): _safe_text(replacement).lower()
            for host, replacement in host_query_rewrites.items()
            if _safe_text(host) and _safe_text(replacement)
        },
        "officialEntitySeeds": [
            seed
            for seed in list(payload.get("officialEntitySeeds") or [])
            if isinstance(seed, dict)
            and _safe_text(seed.get("url"))
            and _safe_text(seed.get("entityId"))
        ],
    }


def _catalog_match(url: str) -> dict[str, Any] | None:
    host = _host(url)
    if not host:
        return None
    for entry in _source_catalog().get("entries") or []:
        for raw_host in list(entry.get("hosts") or []):
            catalog_host = str(raw_host or "").strip().lower()
            if catalog_host and (host == catalog_host or host.endswith(f".{catalog_host}")):
                return entry
    return None


def _is_video_research(*values: Any) -> bool:
    text = " ".join(str(value or "").lower() for value in values)
    return any(term in text for term in _VIDEO_RESEARCH_TERMS)


def _catalog_hosts_by_category(category: str) -> list[str]:
    hosts: list[str] = []
    for entry in _source_catalog().get("entries") or []:
        if str(entry.get("category") or "") != category:
            continue
        for host in list(entry.get("hosts") or []):
            normalized = str(host or "").strip().lower()
            if normalized and normalized not in hosts:
                hosts.append(normalized)
    return hosts


def _catalog_official_host_hints(question: Any) -> list[dict[str, Any]]:
    lowered_question = _safe_text(question).lower()
    if not lowered_question:
        return []
    catalog = _source_catalog()
    alias_map = dict(catalog.get("hostQueryAliases") or {})
    official_hosts = list(
        dict.fromkeys(
            [
                *_catalog_hosts_by_category("official_docs"),
                *_catalog_hosts_by_category("official_blog"),
            ]
        )
    )
    hints: list[dict[str, Any]] = []
    for host in official_hosts:
        aliases = [
            _safe_text(alias).lower()
            for alias in list(alias_map.get(host) or [])
            if _safe_text(alias)
        ]
        if not aliases:
            aliases = [
                label
                for label in re.split(r"[^a-z0-9]+", host)
                if len(label) >= 4
                and label
                not in {"blog", "cloud", "developer", "developers", "docs", "help", "platform"}
            ]
        matched = [alias for alias in aliases if alias in lowered_question]
        if matched:
            hints.append({"host": host, "matchedAliases": matched})
    return hints


def _catalog_official_entity_hints(question: Any) -> list[dict[str, Any]]:
    """Merge duplicate official hosts into the named entities in the question."""

    question_text = _safe_text(question)
    lowered_question = question_text.lower()
    groups: list[dict[str, Any]] = []
    for hint in _catalog_official_host_hints(question_text):
        aliases = {
            _safe_text(alias).lower()
            for alias in list(hint.get("matchedAliases") or [])
            if _safe_text(alias)
        }
        if not aliases:
            continue
        overlapping = [group for group in groups if aliases.intersection(group["aliases"])]
        if overlapping:
            group = overlapping[0]
            group["aliases"].update(aliases)
            host = _safe_text(hint.get("host"))
            if host and host not in group["hosts"]:
                group["hosts"].append(host)
            for duplicate in overlapping[1:]:
                group["aliases"].update(duplicate["aliases"])
                for duplicate_host in duplicate["hosts"]:
                    if duplicate_host not in group["hosts"]:
                        group["hosts"].append(duplicate_host)
                groups.remove(duplicate)
            continue
        groups.append(
            {
                "aliases": set(aliases),
                "hosts": [_safe_text(hint.get("host"))],
            }
        )

    entity_hints: list[dict[str, Any]] = []
    for group in groups:
        aliases = sorted(group["aliases"], key=lambda alias: (lowered_question.find(alias), -len(alias)))
        label_aliases = [
            alias
            for alias in aliases
            if not any(alias != other and alias in other for other in aliases)
        ]
        rendered_parts: list[str] = []
        for alias in sorted(label_aliases, key=lambda value: lowered_question.find(value)):
            start = lowered_question.find(alias)
            rendered = question_text[start : start + len(alias)] if start >= 0 else alias
            if rendered and rendered.casefold() not in {part.casefold() for part in rendered_parts}:
                rendered_parts.append(rendered)
        hosts = [host for host in group["hosts"] if host]
        if not hosts or not rendered_parts:
            continue
        entity_hints.append(
            {
                "label": " ".join(rendered_parts),
                "host": hosts[0],
                "hosts": hosts,
                "matchedAliases": aliases,
            }
        )
    return entity_hints


def _research_entity_indexes_in_text(
    entity_hints: list[dict[str, Any]],
    value: Any,
) -> set[int]:
    text = _safe_text(value).lower()
    text = re.sub(r"(?<![a-z0-9])site:[^\s]+", " ", text)
    text = _URL_IN_TEXT_RE.sub(" ", text)
    if not text:
        return set()
    matched: set[int] = set()
    for index, hint in enumerate(entity_hints):
        for raw_alias in list(hint.get("matchedAliases") or []):
            alias = _safe_text(raw_alias).lower()
            if not alias:
                continue
            if re.fullmatch(r"[a-z0-9][a-z0-9 ._+-]*", alias):
                if re.search(
                    rf"(?<![a-z0-9]){re.escape(alias)}(?![a-z0-9])",
                    text,
                ):
                    matched.add(index)
                    break
            elif alias in text:
                matched.add(index)
                break
    return matched


_RESEARCH_ENTITY_VENDOR_TOKENS = {
    "amazon",
    "anthropic",
    "apple",
    "aws",
    "github",
    "google",
    "meta",
    "microsoft",
    "openai",
}
_RESEARCH_ENTITY_PRODUCT_SUFFIXES = {"assistant", "cli", "code"}
_RESEARCH_ENTITY_SIBLING_SURFACE_TERMS = {
    "api",
    "app",
    "apps",
    "library",
    "libraries",
    "sdk",
    "studio",
}
_RESEARCH_ENTITY_QUERY_BOILERPLATE_TERMS = {
    "current",
    "documentation",
    "evidence",
    "latest",
    "official",
    "primary",
    "source",
    "verify",
}


def _research_normalized_entity_surface(value: Any) -> str:
    return " ".join(re.findall(r"[a-z0-9]+", _safe_text(value).lower()))


def _research_entity_identity_profile(hint: dict[str, Any]) -> dict[str, Any]:
    aliases = list(
        dict.fromkeys(
            _research_normalized_entity_surface(value)
            for value in [hint.get("label"), *list(hint.get("matchedAliases") or [])]
            if _research_normalized_entity_surface(value)
        )
    )
    product_aliases = [alias for alias in aliases if len(alias.split()) >= 2]
    label_tokens = _research_normalized_entity_surface(hint.get("label")).split()
    if (
        len(label_tokens) >= 3
        and label_tokens[-1] in _RESEARCH_ENTITY_PRODUCT_SUFFIXES
    ):
        product_aliases.append(" ".join(label_tokens[:-1]))
    product_aliases = list(dict.fromkeys(product_aliases))
    product_tokens = [
        alias
        for alias in aliases
        if len(alias.split()) == 1
        and alias not in _RESEARCH_ENTITY_VENDOR_TOKENS
        and alias not in _RESEARCH_ENTITY_PRODUCT_SUFFIXES
    ]
    return {
        "aliases": aliases,
        "productAliases": product_aliases,
        "productTokens": product_tokens,
        "productScoped": bool(product_aliases),
    }


def _research_entity_alias_in_surface(alias: str, surface: str) -> bool:
    return bool(alias and surface and f" {alias} " in f" {surface} ")


def _research_source_entity_indexes(
    entity_hints: list[dict[str, Any]],
    *,
    host: Any = "",
    url: Any = "",
    title: Any = "",
    text: Any = "",
    evidence_query: Any = "",
) -> set[int]:
    normalized_host = _safe_text(host).lower().removeprefix("www.")
    parsed_path = ""
    try:
        parsed_path = urlparse(_safe_text(url)).path
    except ValueError:
        parsed_path = _safe_text(url)
    identity_surface = _research_normalized_entity_surface(
        "\n".join(part for part in (_safe_text(title), parsed_path) if part)
    )
    host_surface = _research_normalized_entity_surface(normalized_host.replace(".", " "))
    host_compact = host_surface.replace(" ", "")
    surface_terms = set(identity_surface.split())
    body_surface = _research_normalized_entity_surface(_safe_text(text)[:32_000])
    body_product_alias_counts: dict[int, int] = {}
    for hint_index, hint in enumerate(entity_hints):
        body_product_alias_counts[hint_index] = max(
            (
                len(
                    re.findall(
                        rf"(?<![a-z0-9]){re.escape(alias)}(?![a-z0-9])",
                        body_surface,
                    )
                )
                for alias in _research_entity_identity_profile(hint)[
                    "productAliases"
                ]
            ),
            default=0,
        )
    matched: set[int] = set()
    for index, hint in enumerate(entity_hints):
        profile = _research_entity_identity_profile(hint)
        official_host = bool(
            normalized_host
            and any(
                normalized_host
                == _safe_text(candidate_host).lower().removeprefix("www.")
                for candidate_host in list(hint.get("hosts") or [hint.get("host")])
                if _safe_text(candidate_host)
            )
        )
        if not profile["productScoped"]:
            if official_host or any(
                _research_entity_alias_in_surface(alias, identity_surface)
                for alias in profile["aliases"]
            ):
                matched.add(index)
            continue
        if any(
            _research_entity_alias_in_surface(alias, identity_surface)
            for alias in profile["productAliases"]
        ):
            matched.add(index)
            continue
        if official_host and any(
            alias.replace(" ", "") in host_compact
            or set(alias.split()).issubset(set(host_surface.split()))
            for alias in profile["productAliases"]
        ):
            matched.add(index)
            continue
        if (
            official_host
            and not surface_terms.intersection(_RESEARCH_ENTITY_SIBLING_SURFACE_TERMS)
            and any(
                _research_entity_alias_in_surface(token, identity_surface)
                for token in profile["productTokens"]
            )
        ):
            matched.add(index)
            continue
        query_surface = _research_normalized_entity_surface(
            re.sub(
                r"(?<![a-z0-9])site:[^\s]+",
                " ",
                _URL_IN_TEXT_RE.sub(" ", _safe_text(evidence_query).lower()),
            )
        )
        entity_terms = {
            term
            for alias in profile["aliases"]
            for term in alias.split()
        }
        query_focus_terms = {
            term
            for term in query_surface.split()
            if term not in entity_terms
            and term not in _RESEARCH_ENTITY_QUERY_BOILERPLATE_TERMS
            and not re.fullmatch(r"20\d{2}", term)
        }
        if (
            official_host
            and not surface_terms.intersection(_RESEARCH_ENTITY_SIBLING_SURFACE_TERMS)
            and surface_terms.intersection(query_focus_terms)
        ):
            matched.add(index)
            continue
        body_product_count = body_product_alias_counts.get(index, 0)
        strongest_competing_count = max(
            (
                count
                for hint_index, count in body_product_alias_counts.items()
                if hint_index != index
            ),
            default=0,
        )
        if (
            official_host
            and not surface_terms.intersection(_RESEARCH_ENTITY_SIBLING_SURFACE_TERMS)
            and body_product_count > 0
            and body_product_count > strongest_competing_count
        ):
            # Some first-party product documentation uses a generic page title
            # and path (for example ``/sandboxing``) while the readable body is
            # explicitly and predominantly about the named CLI.  Accept that
            # strong body identity, but not navigation/comparison pages that
            # merely list this product alongside equally prominent peers.
            matched.add(index)
    return matched


def _popularity_signals(text: str) -> list[dict[str, Any]]:
    signals: list[dict[str, Any]] = []
    for match in _POPULARITY_RE.finditer(str(text or "")[:1200]):
        signals.append(
            {
                "metric": match.group("label").lower(),
                "valueText": match.group(0),
            }
        )
        if len(signals) >= 4:
            break
    return signals


def _host(value: str) -> str:
    parsed = urlparse(value)
    return (parsed.hostname or "").lower()


def _canonical_source_url(value: str) -> str:
    return canonical_source_url(value)


def _research_document_identity(value: str, *, question: str = "") -> str:
    return research_document_identity(value, question=question)


def _research_document_priority(value: str, *, question: str = "") -> int:
    return research_document_priority(value, question=question)


def _research_source_excerpt(text: Any, query: Any, *, limit: int = _RESEARCH_SOURCE_CAPTURE_CHARS) -> str:
    """Keep the page head plus query-relevant windows from anywhere in the body."""

    body = _safe_text(text)
    limit = max(MIN_RESEARCH_SOURCE_BODY_CHARS, int(limit or 0))
    if len(body) <= limit:
        return body

    raw_query = _safe_text(query).lower()
    terms: list[str] = []
    structured_anchor_patterns: list[re.Pattern[str]] = []
    for phrase in re.findall(r"[\"']([^\"']{3,80})[\"']", raw_query):
        normalized = phrase.strip()
        if normalized and normalized not in terms:
            terms.append(normalized)
    # Long statutes and standards repeat generic words thousands of times.
    # Preserve structured anchors before tokenising so an Article/Annex/date
    # query reaches the operative text instead of only the document preamble.
    for pattern in (
        r"\barticle\s+\d+[a-z]?(?:\s*\(\s*[a-z0-9]+\s*\)){0,3}",
        r"\bannex\s+(?:[ivxlcdm]+|\d+)\b",
        r"\b\d+(?:\.\d+)?\s*(?:\^|×\s*10\s*\^?)\s*\d+\b",
        r"\b\d{1,2}\s+(?:january|february|march|april|may|june|july|august|"
        r"september|october|november|december)\s+\d{4}\b",
    ):
        for match in re.finditer(pattern, raw_query, re.IGNORECASE):
            normalized = re.sub(r"\s+", " ", match.group(0)).strip()
            if normalized and normalized not in terms:
                terms.append(normalized)
            if normalized:
                flexible_anchor = "".join(
                    r"[\s\u00a0]+" if part.isspace() else re.escape(part)
                    for part in re.split(r"(\s+)", normalized)
                    if part
                )
                flexible_anchor = flexible_anchor.replace(
                    r"\(", r"[\s\u00a0]*\([\s\u00a0]*"
                ).replace(
                    r"\)", r"[\s\u00a0]*\)"
                ).replace(
                    r"\^", r"[\s\u00a0]*\^[\s\u00a0]*"
                )
                structured_anchor_patterns.append(
                    re.compile(
                        rf"(?<![a-z0-9]){flexible_anchor}(?![a-z0-9])",
                        re.IGNORECASE,
                    )
                )
    for term in re.split(r"[^a-z0-9_.-]+", raw_query):
        normalized = term.strip("._-")
        if len(normalized) >= 3 and normalized not in _RELEVANCE_STOPWORDS and not normalized.startswith("http"):
            if normalized not in terms:
                terms.append(normalized)
            visible_variant = re.sub(r"[_-]+", " ", normalized).strip()
            if visible_variant != normalized and visible_variant not in terms:
                terms.append(visible_variant)
    for run in re.findall(r"[\u4e00-\u9fff]{2,}", raw_query):
        candidates = [run] if len(run) <= 8 else [run[index : index + 4] for index in range(0, len(run) - 3, 2)]
        for candidate in candidates:
            if candidate not in terms:
                terms.append(candidate)

    head_chars = min(3000, max(1600, limit // 5))
    window_radius = min(900, max(240, limit // 10))
    anchor_intervals: list[tuple[int, int]] = []
    for anchor_pattern in structured_anchor_patterns:
        for match in list(anchor_pattern.finditer(body))[:3]:
            anchor_intervals.append(
                (
                    max(0, match.start() - window_radius),
                    min(len(body), match.end() + window_radius),
                )
            )

    generic_intervals: list[tuple[int, int]] = []
    lowered_body = body.lower()
    for term in terms[:24]:
        start = 0
        matches = 0
        while matches < 3:
            index = lowered_body.find(term, start)
            if index < 0:
                break
            generic_intervals.append(
                (
                    max(0, index - window_radius),
                    min(len(body), index + len(term) + window_radius),
                )
            )
            start = index + max(1, len(term))
            matches += 1

    if not anchor_intervals and not generic_intervals:
        tail_chars = min(limit - head_chars, max(1200, limit // 4))
        generic_intervals.append(
            (max(head_chars, len(body) - tail_chars), len(body))
        )

    def merge_intervals(
        intervals: list[tuple[int, int]],
    ) -> list[tuple[int, int]]:
        merged: list[tuple[int, int]] = []
        for start, end in sorted(intervals):
            if merged and start <= merged[-1][1] + 120:
                merged[-1] = (merged[-1][0], max(merged[-1][1], end))
            else:
                merged.append((start, end))
        return merged

    separator_text = "\n\n[... query-focused excerpt ...]\n\n"
    selected_intervals: list[tuple[int, int]] = []
    # Structural anchors receive budget before the page head and generic term
    # matches. The selected windows are sorted only after admission, so a long
    # preamble cannot consume the budget before a late Article or Annex.
    for interval in [*anchor_intervals, (0, head_chars), *generic_intervals]:
        candidate_intervals = merge_intervals([*selected_intervals, interval])
        candidate_chars = sum(end - start for start, end in candidate_intervals)
        candidate_chars += max(0, len(candidate_intervals) - 1) * len(separator_text)
        if candidate_chars <= limit:
            selected_intervals = candidate_intervals
    merged = merge_intervals(selected_intervals or [(0, min(len(body), limit))])

    chunks: list[str] = []
    used = 0
    for start, end in merged:
        if used >= limit:
            break
        separator = separator_text if chunks else ""
        available = limit - used - len(separator)
        if available <= 0:
            break
        chunk = body[start:end][:available]
        if chunk:
            chunks.append(f"{separator}{chunk}")
            used += len(separator) + len(chunk)
    return "".join(chunks)[:limit]


def _research_original_content_chars(text: Any, read_payload: dict[str, Any] | None = None) -> int:
    body = _safe_text(text)
    payload = read_payload or {}
    reported = _as_int(payload.get("originalContentChars"), 0)
    truncated_match = re.search(r"\.\.\.\[TRUNCATED\]\s*\((\d+)\s+chars total\)", body)
    if truncated_match:
        reported = max(reported, _as_int(truncated_match.group(1), 0))
    return max(len(body), reported)


def _domain_from_seed(value: str) -> str:
    host = _host(value)
    if host.startswith("www."):
        host = host[4:]
    return host


def _query_slug(value: str) -> str:
    normalized = re.sub(r"\s+", " ", _safe_text(value).lower())
    return hashlib.sha1(normalized.encode("utf-8")).hexdigest()[:8]


def _query_site_domains(value: Any) -> list[str]:
    domains: list[str] = []
    for match in re.finditer(r"(?:^|\s)site:([a-z0-9.-]+)", _safe_text(value).lower()):
        domain = match.group(1).strip(".-")
        if domain and domain not in domains:
            domains.append(domain)
    return domains


def _host_matches_domains(host: str, domains: list[str]) -> bool:
    normalized = _safe_text(host).lower().removeprefix("www.")
    return any(normalized == domain or normalized.endswith(f".{domain}") for domain in domains)


def _first_party_domain_matches_question(url: Any, question: Any) -> bool:
    host = _host(_safe_text(url)).removeprefix("www.")
    parts = [part for part in host.split(".") if part]
    if len(parts) < 2:
        return False
    compound_suffixes = {
        "ac.uk",
        "co.jp",
        "co.uk",
        "com.au",
        "com.br",
        "com.cn",
        "com.sg",
        "com.tw",
        "org.uk",
    }
    suffix = ".".join(parts[-2:])
    label_index = -3 if suffix in compound_suffixes and len(parts) >= 3 else -2
    registrable_label = parts[label_index]
    if not re.fullmatch(r"[a-z0-9]{4,}", registrable_label):
        return False
    question_without_routes = re.sub(
        r"(?<![a-z0-9])site:[^\s]+",
        " ",
        _URL_IN_TEXT_RE.sub(" ", _safe_text(question).lower()),
    )
    question_tokens = set(re.findall(r"[a-z0-9]{4,}", question_without_routes))
    return registrable_label in question_tokens


def _source_matches_intent(quality: dict[str, Any], source_intent: Any) -> bool:
    if _safe_text(source_intent).lower() != "official_primary":
        return True
    return bool(
        _safe_text(quality.get("authorityTier")).lower() == "primary"
        or int(quality.get("authorityScore") or 0) >= 70
    )


def _source_quality(
    url: str,
    *,
    allowed_domains: list[str],
    source_policy: str,
    title: str = "",
    snippet: str = "",
    video_research: bool = False,
    question: str = "",
) -> dict[str, Any]:
    normalized_host = _host(url)
    allowed_match = any(normalized_host == domain or normalized_host.endswith(f".{domain}") for domain in allowed_domains)
    catalog_entry = _catalog_match(url)
    public_institution = any(normalized_host.endswith(suffix) for suffix in _PUBLIC_INSTITUTION_HOST_SUFFIXES)
    first_party_subject_match = _first_party_domain_matches_question(url, question)
    authoritative_hint = (
        allowed_match
        or public_institution
        or first_party_subject_match
        or any(hint in normalized_host or hint in url.lower() for hint in _AUTHORITATIVE_HOST_HINTS)
    )
    low_quality_hint = any(hint in normalized_host or hint in url.lower() for hint in _LOW_QUALITY_HOST_HINTS)
    popularity = _popularity_signals(f"{title}\n{snippet}")
    # Unknown hosts must earn authority through an allowlist, catalog entry, or
    # an explicit host signal before authoritative-source policy can select them.
    score = 40
    reasons: list[str] = []
    if allowed_match:
        score += 30
        reasons.append("allowed_domain_match")
    if catalog_entry:
        boost = _as_int(catalog_entry.get("authorityBoost"), 0)
        # Catalog membership is an explicit trust signal; preserve the catalog's
        # established tier while keeping uncatalogued hosts below the default
        # authoritative-selection threshold.
        score += boost + 10
        reasons.append(f"source_catalog:{catalog_entry.get('id')}")
    if public_institution:
        score += 20
        reasons.append("official_public_institution_host")
    if first_party_subject_match:
        score += 15
        reasons.append("first_party_domain_subject_match")
    if authoritative_hint:
        score += 20
        reasons.append("authoritative_host_hint")
    if str(source_policy or "").strip().lower() in {"official", "authoritative", "primary"} and authoritative_hint:
        score += 10
        reasons.append("source_policy_match")
    if video_research and catalog_entry and str(catalog_entry.get("category") or "") in {"video_platform", "creative_showcase"}:
        score += 15
        reasons.append("video_popularity_source")
    if video_research and popularity:
        score += 10
        reasons.append("popularity_signal_detected")
    if not video_research and catalog_entry and str(catalog_entry.get("category") or "") in {"video_platform", "creative_showcase"}:
        score -= 35
        reasons.append("non_video_research_source_penalty")
    if low_quality_hint:
        score -= 25
        reasons.append("low_quality_host_hint")
    score = max(0, min(score, 100))
    return {
        "host": normalized_host,
        "authorityScore": score,
        "tier": "primary" if score >= 80 else ("secondary" if score >= 55 else "weak"),
        "reasons": reasons,
        "catalogSourceId": catalog_entry.get("id") if catalog_entry else None,
        "catalogCategory": catalog_entry.get("category") if catalog_entry else None,
        "authorityTier": catalog_entry.get("authorityTier") if catalog_entry else ("primary" if public_institution or first_party_subject_match else None),
        "popularitySignals": popularity,
    }


def _source_id(url: str) -> str:
    normalized = _safe_text(url).lower()
    return f"src_{hashlib.sha1(normalized.encode('utf-8')).hexdigest()[:12]}" if normalized else f"src_{uuid.uuid4().hex[:12]}"


def _read_evidence_receipt(text: Any, *, retrieved_at: Any) -> dict[str, Any]:
    body = _safe_text(text)
    timestamp = _safe_text(retrieved_at)
    if len(body) < MIN_RESEARCH_SOURCE_BODY_CHARS or not timestamp:
        return {}
    return {
        "verified": True,
        "contentChars": len(body),
        "contentSha256": hashlib.sha256(body.encode("utf-8")).hexdigest(),
        "retrievedAt": timestamp,
    }


def _metadata_value(metadata: dict[str, Any], *keys: str) -> str:
    normalized = {str(key).strip().lower(): value for key, value in metadata.items()}
    for key in keys:
        value = _safe_text(normalized.get(key.lower()))
        if value:
            return value
    return ""


def _normalize_document_date(value: Any) -> str:
    text = _safe_text(value)
    if not text:
        return ""
    if re.fullmatch(r"\d{10}|\d{13}", text):
        try:
            timestamp = float(text) / (1000.0 if len(text) == 13 else 1.0)
            parsed = datetime.fromtimestamp(timestamp, tz=timezone.utc)
            if 2000 <= parsed.year <= 2100:
                return parsed.date().isoformat()
        except (OverflowError, OSError, ValueError):
            pass
    for date_format in (
        "%B %d, %Y",
        "%b %d, %Y",
        "%B %d %Y",
        "%b %d %Y",
        "%d %B %Y",
        "%d %b %Y",
        "%d-%b-%Y",
    ):
        try:
            return datetime.strptime(text, date_format).date().isoformat()
        except ValueError:
            continue
    return text


def _source_temporal_evidence(read_payload: dict[str, Any], result: dict[str, Any] | None = None) -> dict[str, Any]:
    result = result or {}
    metadata = dict(read_payload.get("metadata") or {}) if isinstance(read_payload.get("metadata"), dict) else {}
    published_at = _safe_text(
        read_payload.get("publishedAt")
        or result.get("publishedAt")
        or result.get("publishedDate")
        or _metadata_value(
            metadata,
            "article:published_time",
            "og:published_time",
            "datepublished",
            "date",
            "dc.date",
        )
    )
    updated_at = _safe_text(
        read_payload.get("updatedAt")
        or result.get("updatedAt")
        or result.get("updatedDate")
        or _metadata_value(
            metadata,
            "article:modified_time",
            "og:updated_time",
            "datemodified",
        )
    )
    version = _safe_text(
        read_payload.get("version")
        or result.get("version")
        or _metadata_value(metadata, "version", "softwareversion", "release", "releaseversion")
    )
    temporal_text = "\n".join(
        _safe_text(value)
        for value in (
            read_payload.get("title"),
            result.get("title"),
            result.get("url"),
            read_payload.get("text"),
            read_payload.get("markdown"),
        )
        if _safe_text(value)
    )[:4000]
    temporal_text = re.sub(r"[\s\u00a0]+", " ", temporal_text)
    version_identity_text = "\n".join(
        _safe_text(value)
        for value in (
            read_payload.get("title"),
            result.get("title"),
            result.get("url"),
        )
        if _safe_text(value)
    )[:1200]
    month_pattern = (
        r"Jan(?:uary)?|Feb(?:ruary)?|Mar(?:ch)?|Apr(?:il)?|May|Jun(?:e)?|"
        r"Jul(?:y)?|Aug(?:ust)?|Sep(?:tember)?|Oct(?:ober)?|Nov(?:ember)?|Dec(?:ember)?"
    )
    date_value_pattern = (
        r"(?:20\d{2}[-/]\d{1,2}[-/]\d{1,2}|"
        rf"(?:{month_pattern})\s+\d{{1,2}},?\s+20\d{{2}}|"
        rf"\d{{1,2}}[- ](?:{month_pattern})[- ,]20\d{{2}})"
    )
    if not updated_at:
        updated_match = re.search(
            rf"\b(?:last\s+update(?:d)?|updated|modified)\b\s*[:\-]?\s*({date_value_pattern})",
            temporal_text,
            re.IGNORECASE,
        )
        if updated_match:
            updated_at = updated_match.group(1)
    if not published_at:
        published_match = re.search(
            rf"\b(?:published|created|date)\b\s*[:\-]?\s*({date_value_pattern})",
            temporal_text,
            re.IGNORECASE,
        )
        if published_match:
            published_at = published_match.group(1)
    if not published_at:
        url_date_match = re.search(r"/(20\d{2})[-/](\d{1,2})[-/](\d{1,2})(?:/|[-_])", _safe_text(result.get("url")))
        if url_date_match:
            published_at = "-".join(
                (
                    url_date_match.group(1),
                    url_date_match.group(2).zfill(2),
                    url_date_match.group(3).zfill(2),
                )
            )
    if not version:
        match = re.search(
            r"\b(?:python|version|release|v)\s*([0-9]+\.[0-9]+(?:\.[0-9]+)?)\b",
            version_identity_text,
            re.IGNORECASE,
        )
        if match:
            version = match.group(1)
    if not version:
        generic_version = re.search(
            r"\((\d+\.\d+(?:\.\d+|\.x)?)\)",
            version_identity_text,
            re.IGNORECASE,
        )
        if generic_version:
            version = generic_version.group(1)
    applicable_version = ""
    if not version:
        applicability_version = re.search(
            r"\b(?:targets?|supports?|applies\s+to|compatible\s+with|documentation\s+for)\s+"
            r"(?:python\s*)?([0-9]+\.[0-9]+(?:\.[0-9]+)?)\b",
            temporal_text,
            re.IGNORECASE,
        )
        if applicability_version:
            applicable_version = applicability_version.group(1)
    published_at = _normalize_document_date(published_at)
    updated_at = _normalize_document_date(updated_at)
    retrieved_at = _safe_text(read_payload.get("retrievedAt") or read_payload.get("fetchedAt")) or _utc_now_iso()
    source_date = updated_at or published_at
    return {
        key: value
        for key, value in {
            "retrievedAt": retrieved_at,
            "publishedAt": published_at or None,
            "updatedAt": updated_at or None,
            "sourceDate": source_date or None,
            "sourceDateKind": "updated" if updated_at else ("published" if published_at else None),
            "version": version or None,
            "applicableVersion": applicable_version or None,
        }.items()
        if value not in (None, "")
    }


def _claim_id(source_id: str, text: str, index: int) -> str:
    seed = f"{source_id}:{index}:{_safe_text(text).lower()}"
    return f"claim_{hashlib.sha1(seed.encode('utf-8')).hexdigest()[:12]}"


def _source_relevance_terms(question: str) -> list[str]:
    query = _safe_text(question).lower()
    raw_latin_terms = [term for term in re.split(r"[^a-z0-9]+", query) if len(term) >= 3]
    latin_terms = [term for term in raw_latin_terms if term not in _RELEVANCE_STOPWORDS]
    if not latin_terms:
        latin_terms = raw_latin_terms
    cjk_terms: list[str] = []
    for run in re.findall(r"[\u4e00-\u9fff]{2,}", query):
        if len(run) <= 4:
            cjk_terms.append(run)
        else:
            cjk_terms.extend(run[index : index + 2] for index in range(len(run) - 1))
    return list(dict.fromkeys(latin_terms + cjk_terms))


def _source_relevance_score(question: str, *, title: str = "", snippet: str = "", text: str = "") -> int:
    # Link destinations repeat site/product names in every documentation
    # paragraph. They are transport metadata, not evidence that the visible
    # paragraph answers the question.
    visible_text = _URL_IN_TEXT_RE.sub(" ", text[:8000])
    haystack = " ".join([title, snippet, visible_text]).lower()
    query = _safe_text(question).lower()
    if not query or not haystack:
        return 0
    terms = _source_relevance_terms(question)
    if not terms:
        return 0
    def term_matches(term: str) -> bool:
        if term == "cli":
            return bool(
                re.search(r"\bcli\b", haystack)
                or any(
                    alias in haystack
                    for alias in (
                        "command-line",
                        "command line",
                        "console script",
                        "console_scripts",
                        "entry point",
                        "argparse",
                    )
                )
                or re.search(r"\b(?:click|typer)\b", haystack)
            )
        if term == "pathlib":
            return bool(
                re.search(r"\bpathlib\b", haystack)
                or "path object" in haystack
                or "filesystem path" in haystack
                or "file system path" in haystack
                or "path-like object" in haystack
                or "pathlike object" in haystack
                or "os.pathlike" in haystack
                or "__fspath__" in haystack
                or "path_type" in haystack
                or "path type" in haystack
                or re.search(r"\b(?:click|typer)[\s._-]*path\b", haystack)
            )
        if re.fullmatch(r"[a-z0-9]+", term):
            return bool(re.search(rf"\b{re.escape(term)}\b", haystack))
        return term in haystack

    hits = sum(1 for term in terms if term_matches(term))
    return max(0, min(100, int(round((hits / max(1, len(terms))) * 100))))


def _source_minimum_body_relevance(question: str) -> int:
    term_count = len(_source_relevance_terms(question))
    if term_count <= 0:
        return 0
    required_hits = min(2, term_count)
    return max(25, (required_hits * 100 + term_count - 1) // term_count)


_RESEARCH_ROOT_SUBJECT_GENERIC_TERMS = {
    "act",
    "article",
    "articles",
    "commission",
    "compliance",
    "guidance",
    "general",
    "including",
    "legal",
    "model",
    "models",
    "obligation",
    "obligations",
    "office",
    "provider",
    "providers",
    "purpose",
    "regulation",
    "requirement",
    "requirements",
    "verify",
}


def _research_root_subject_anchors(question: str) -> list[str]:
    facets = [
        goal
        for goal, _kind in _build_explicit_question_facets(question)
        if _safe_text(goal)
    ]
    if len(facets) < 2:
        return []
    required_occurrences = max(2, (len(facets) + 1) // 2)
    counts: dict[str, int] = {}
    for goal in facets:
        terms = {
            term
            for term in _source_relevance_terms(goal)
            if re.fullmatch(r"[a-z][a-z0-9_.-]{2,}", term)
            and not re.fullmatch(r"20\d{2}", term)
            and term not in _RESEARCH_ROOT_SUBJECT_GENERIC_TERMS
        }
        for term in terms:
            counts[term] = counts.get(term, 0) + 1
    anchors = [
        term
        for term, count in sorted(
            counts.items(),
            key=lambda item: (-item[1], -len(item[0]), item[0]),
        )
        if count >= required_occurrences
    ][:4]
    return anchors


def _research_source_matches_root_subject(
    question: str,
    *,
    title: str = "",
    url: str = "",
    snippet: str = "",
    text: str = "",
) -> bool:
    """Reject obvious entity collisions while remaining permissive on facets."""

    visible_text = _URL_IN_TEXT_RE.sub(" ", text[:12000])
    haystack = " ".join((title, url, snippet, visible_text)).lower()
    normalized_question = _safe_text(question).lower()
    question_means_general_purpose_ai = bool(
        re.search(
            r"\bgeneral[- ]purpose\s+(?:ai|artificial\s+intelligence)\b|"
            r"通用(?:目的|用途|型)?\s*(?:ai|人工智能)",
            normalized_question,
            re.IGNORECASE,
        )
    )
    source_says_global_partnership = bool(
        re.search(
            r"\bglobal\s+partnership\s+on\s+artificial\s+intelligence\b|"
            r"全球人工智能伙伴关系",
            haystack,
            re.IGNORECASE,
        )
    )
    source_says_general_purpose_ai = bool(
        re.search(
            r"\bgeneral[- ]purpose\s+(?:ai|artificial\s+intelligence)\b|"
            r"通用(?:目的|用途|型)?\s*(?:ai|人工智能)",
            haystack,
            re.IGNORECASE,
        )
    )
    if (
        question_means_general_purpose_ai
        and source_says_global_partnership
        and not source_says_general_purpose_ai
    ):
        return False

    anchors = _research_root_subject_anchors(question)
    if not anchors:
        return True
    for anchor in anchors:
        if anchor == "gpai":
            if source_says_general_purpose_ai or (
                re.search(r"\bgpai\b", haystack)
                and not source_says_global_partnership
            ):
                return True
            continue
        if re.search(rf"(?<![a-z0-9]){re.escape(anchor)}(?![a-z0-9])", haystack):
            return True
    return False


def _source_noise_reasons(text: Any) -> list[str]:
    normalized = _safe_text(text)
    lowered = normalized.lower()
    reasons: list[str] = []
    if not normalized:
        return ["empty_content"]
    for marker in _SOURCE_NOISE_MARKERS:
        if marker in lowered:
            reasons.append(f"noise:{marker[:32]}")
            break
    if normalized.count("|") >= 8 or normalized.count("»") >= 5:
        reasons.append("navigation_or_footer_like_text")
    return reasons


def _is_soft_source_noise(reason: str) -> bool:
    lowered = _safe_text(reason).lower()
    return lowered in {
        "noise:theme auto light dark",
        "noise:navigation - index - modules",
        "navigation_or_footer_like_text",
    }


def _source_quality_gate(
    *,
    question: str,
    result: dict[str, Any],
    read_payload: dict[str, Any] | None,
    source_policy: str,
    source_intent: str = "",
) -> dict[str, Any]:
    url = _safe_text(result.get("url"))
    quality = dict(result.get("sourceQualityHints") or {})
    read_payload = read_payload or {}
    text = _safe_text(read_payload.get("text") or read_payload.get("markdown") or read_payload.get("textPreview"))
    snippet = _safe_text(result.get("snippet"))
    title = _safe_text(read_payload.get("title") or result.get("title"))
    content_for_quality = text or snippet
    noise_reasons = _source_noise_reasons(content_for_quality)
    if noise_reasons and len(content_for_quality) >= 1200:
        authority_hint = int((result.get("sourceQualityHints") or {}).get("authorityScore") or 0)
        catalog_entry = _catalog_match(url)
        if (authority_hint >= 75 or catalog_entry is not None) and all(
            _is_soft_source_noise(reason) for reason in noise_reasons
        ):
            noise_reasons = []
    has_read_body = bool(text and len(text) >= MIN_RESEARCH_SOURCE_BODY_CHARS and not noise_reasons)
    has_snippet_only = bool(snippet and not text and not noise_reasons)
    readability = 85 if has_read_body else (35 if has_snippet_only else 10)
    if read_payload.get("ok") is False:
        readability = min(readability, 25)
    if read_payload.get("missingContentReason"):
        readability = min(readability, 35)
        noise_reasons.append(f"missing_content:{_safe_text(read_payload.get('missingContentReason'))[:60]}")
    # Titles and search snippets rank discovery candidates, but only the body
    # returned by web_read can qualify a source as claim evidence.
    discovery_relevance = _source_relevance_score(question, title=title, snippet=snippet)
    relevance = _source_relevance_score(question, text=text)
    authority = int(quality.get("authorityScore") or 0)
    try:
        http_status = int(read_payload.get("status") or read_payload.get("statusCode") or 0)
    except (TypeError, ValueError):
        http_status = 0
    error_page = bool(
        http_status >= 400
        or re.search(r"^(?:error\s+)?404\b|^page not found\b|^not found\b", title, re.IGNORECASE)
    )
    navigation_requested = any(
        term in _safe_text(question).lower()
        for term in ("documentation index", "module index", "pep index", "文档索引", "模块索引", "pep 索引")
    )
    navigation_page = research_source_is_navigation(
        _safe_text(read_payload.get("finalUrl") or result.get("finalUrl") or url),
        title=title,
    )
    policy = _safe_text(source_policy).lower()
    # Search snippets are discovery hints, not evidence. A source only enters the
    # claim bank after a successful full-body read, regardless of its authority.
    authority_floor = 40 if policy in {"official", "authoritative", "primary", "official_docs_first"} else 30
    effective_source_intent = _safe_text(source_intent or result.get("sourceIntent")).lower()
    intent_match = _source_matches_intent(quality, effective_source_intent)
    if effective_source_intent == "official_primary":
        authority_floor = max(authority_floor, 70)
    source_selected = bool(
        read_payload.get("ok") is not False
        and not error_page
        and (not navigation_page or navigation_requested)
        and has_read_body
        and relevance >= 15
        and authority >= authority_floor
        and intent_match
    )
    rejected_reason = ""
    if not source_selected:
        if error_page:
            rejected_reason = f"http_error_page:{http_status}" if http_status >= 400 else "http_error_page"
        elif navigation_page and not navigation_requested:
            rejected_reason = "navigation_index_not_evidence"
        elif noise_reasons:
            rejected_reason = ", ".join(noise_reasons[:3])
        elif not has_read_body:
            rejected_reason = "readable_body_required"
        elif relevance < 15:
            rejected_reason = "relevance_too_low"
        elif authority < authority_floor:
            rejected_reason = "authority_too_low"
        elif not intent_match:
            rejected_reason = "primary_source_intent_not_met"
        else:
            rejected_reason = "source_quality_below_gate"
    return {
        "sourceId": _source_id(url),
        "selectedForEvidence": source_selected,
        "rejectedReason": rejected_reason,
        "qualityDimensions": {
            "authority": authority,
            "readability": readability,
            "relevance": relevance,
            "discoveryRelevance": discovery_relevance,
            "freshness": 50,
            "independence": 50,
            "coverage": min(100, max(relevance, 35 if has_read_body else 10)),
            "risk": "normal",
        },
        "readabilityReasons": noise_reasons,
    }


def _jina_api_key() -> str:
    return _safe_text(os.getenv("JINA_API_KEY") or os.getenv("JINA_API_KEYS"))


def _read_with_jina(url: str, *, timeout_seconds: float = 18.0) -> dict[str, Any]:
    key = _jina_api_key()
    if not key:
        return {"ok": False, "provider": "jina", "failureClass": "credential_missing", "reason": "missing_env:JINA_API_KEY"}
    try:
        response = requests.get(
            f"{_JINA_READER_URL_PREFIX}{url}",
            headers={"Authorization": f"Bearer {key}", "Accept": "text/plain"},
            timeout=timeout_seconds,
        )
        text = _safe_text(response.text)
        if response.status_code >= 400:
            return {
                "ok": False,
                "provider": "jina",
                "status": response.status_code,
                "failureClass": "provider_error",
                "reason": text[:300] or f"jina_http_{response.status_code}",
            }
        if not text:
            return {"ok": False, "provider": "jina", "status": response.status_code, "failureClass": "no_content", "reason": "jina_empty_body"}
        return {
            "ok": True,
            "provider": "jina",
            "status": response.status_code,
            "title": "",
            "text": text,
            "textPreview": text[:1200],
            "contentChars": len(text),
            "originalContentChars": len(text),
            "omittedChars": 0,
            "extractionQuality": "jina_reader_markdown",
            "sourceCapability": {"id": "jina", "role": "read_extract", "outputFormats": ["markdown", "text"]},
        }
    except requests.Timeout:
        return {"ok": False, "provider": "jina", "failureClass": "network_timeout", "reason": "jina_reader_timeout"}
    except Exception as exc:  # noqa: BLE001 - Jina is a fallback source, never fatal.
        return {"ok": False, "provider": "jina", "failureClass": "provider_error", "reason": f"{type(exc).__name__}: {exc}"}


def _build_evidence_bank(*, question: str, source_matrix: list[dict[str, Any]], shards: list[dict[str, Any]]) -> dict[str, Any]:
    fetched = _fetched_source_map(shards, question=question)
    fetched_by_shard = {
        _safe_text(shard.get("shardId")): _fetched_source_map(
            [shard],
            question=_safe_text(shard.get("evidenceQuery") or shard.get("query")) or question,
        )
        for shard in shards
        if _safe_text(shard.get("shardId"))
    }
    selected_sources: list[dict[str, Any]] = []
    rejected_sources: list[dict[str, Any]] = []
    claims: list[dict[str, Any]] = []
    for source in source_matrix:
        url = _safe_text(source.get("url"))
        if not url:
            continue
        shard_fetched = fetched_by_shard.get(_safe_text(source.get("shardId"))) or {}
        read_payload = shard_fetched.get(url) or fetched.get(url) or {}
        gate = dict(source.get("sourceQualityGate") or {})
        source_id = _safe_text(gate.get("sourceId")) or _source_id(url)
        text = _safe_text(read_payload.get("text") or read_payload.get("markdown") or read_payload.get("textPreview"))
        temporal = _source_temporal_evidence(read_payload, source)
        read_evidence = _read_evidence_receipt(text, retrieved_at=temporal.get("retrievedAt"))
        citation_key = f"S{len(selected_sources) + 1}" if gate.get("selectedForEvidence") else ""
        source_entry = {
            "sourceId": source_id,
            "citationKey": citation_key,
            "title": _safe_text(read_payload.get("title") or source.get("title") or url)[:220],
            "url": url,
            "host": _safe_text(source.get("host")) or _host(url),
            "authorityScore": source.get("authorityScore"),
            "tier": source.get("tier"),
            "selectedForEvidence": bool(gate.get("selectedForEvidence")),
            "retrievedAt": temporal.get("retrievedAt"),
            "publishedAt": temporal.get("publishedAt"),
            "updatedAt": temporal.get("updatedAt"),
            "version": temporal.get("version"),
            "temporalEvidence": temporal,
            "contentChars": len(text),
            "readEvidence": read_evidence,
            "qualityDimensions": gate.get("qualityDimensions") or {},
            "detailRef": read_payload.get("rawRef") or read_payload.get("detailRawRef"),
            "sourceQualityGate": gate,
            "shardId": source.get("shardId"),
            "researchFacetId": source.get("researchFacetId"),
            "researchFacetIds": list(source.get("researchFacetIds") or []),
            "researchFacetGoal": source.get("researchFacetGoal"),
            "evidenceViews": list(source.get("evidenceViews") or []),
            "evidenceQuery": source.get("evidenceQuery"),
        }
        if gate.get("selectedForEvidence"):
            selected_sources.append({key: value for key, value in source_entry.items() if value not in (None, "", [], {})})
            for index, sentence in enumerate(_research_sentences(text, limit=1), start=1):
                claim = {
                    "claimId": _claim_id(source_id, sentence, index),
                    "sourceId": source_id,
                    "claim": sentence,
                    "supportingSources": [source_entry],
                    "confidence": source.get("tier") or "secondary",
                }
                claims.append(claim)
                if len(claims) >= 12:
                    break
        else:
            rejected_sources.append(
                {
                    **{key: value for key, value in source_entry.items() if value not in (None, "", [], {})},
                    "reason": gate.get("rejectedReason") or "source_quality_below_gate",
                }
            )
    return {
        "version": 1,
        "question": question,
        "selectedSources": selected_sources[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT],
        "rejectedSources": rejected_sources[:12],
        "claims": claims[:_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT],
        "stats": {
            "selectedSourceCount": len(selected_sources),
            "rejectedSourceCount": len(rejected_sources),
            "claimCount": len(claims),
        },
    }


def _cleanup_ledger() -> None:
    now = time.time()
    for key, entry in list(_EVIDENCE_LEDGER.items()):
        expires_at = float(entry.get("_expiresAt") or 0)
        if expires_at and expires_at < now:
            _EVIDENCE_LEDGER.pop(key, None)


def _ledger_scope(state: dict[str, Any] | None) -> str:
    context = dict(state or {})
    # Experience reuse must survive a new run in the same conversation. The
    # evidence bundle still carries its run lineage; scope controls visibility.
    for key in ("session_id", "sessionId", "conversation_id", "conversationId", "run_id", "runId"):
        value = _safe_text(context.get(key))
        if value:
            return value
    return "global"


def _store_evidence(bundle: dict[str, Any], *, state: dict[str, Any] | None) -> dict[str, Any]:
    _cleanup_ledger()
    config = _research_config()
    scope = _ledger_scope(state)
    bundle_id = _safe_text(bundle.get("evidenceBundleId")) or f"research_{uuid.uuid4().hex[:12]}"
    stored = {
        **bundle,
        "evidenceBundleId": bundle_id,
        "scope": scope,
        "createdAt": _utc_now_iso(),
        "retention": "run_scoped_memory",
        "_expiresAt": time.time() + config["evidenceTtlSeconds"],
    }
    stored["researchAnswerPack"] = _research_answer_pack(stored)
    _EVIDENCE_LEDGER[bundle_id] = stored
    return store_evidence_bundle(stored, ttl_seconds=config["evidenceTtlSeconds"], scope=scope)


def _compact_visible_quality_gate(value: Any) -> dict[str, Any]:
    if not isinstance(value, dict):
        return {}
    compact = {
        "sourceId": value.get("sourceId"),
        "selectedForEvidence": value.get("selectedForEvidence"),
        "rejectedReason": value.get("rejectedReason"),
        "qualityDimensions": value.get("qualityDimensions"),
        "readabilityReasons": list(value.get("readabilityReasons") or [])[:3],
    }
    return {key: item for key, item in compact.items() if item not in (None, "", [], {})}


def _compact_visible_source(value: Any) -> dict[str, Any]:
    if not isinstance(value, dict):
        return {}
    compact = {
        "sourceId": value.get("sourceId"),
        "documentId": value.get("documentId"),
        "title": _compact_research_text(value.get("title"), limit=160),
        "url": value.get("url"),
        "requestedUrl": value.get("requestedUrl"),
        "host": value.get("host"),
        "tier": value.get("tier"),
        "provider": value.get("provider"),
        "catalogCategory": value.get("catalogCategory"),
        "popularitySignals": value.get("popularitySignals"),
        "authorityScore": value.get("authorityScore"),
        "freshness": value.get("freshness"),
        "relevanceScore": value.get("relevanceScore"),
        "citationKey": value.get("citationKey"),
        "retrievedAt": value.get("retrievedAt"),
        "publishedAt": value.get("publishedAt"),
        "updatedAt": value.get("updatedAt"),
        "sourceDate": value.get("sourceDate"),
        "sourceDateKind": value.get("sourceDateKind"),
        "version": value.get("version"),
        "temporalEvidence": value.get("temporalEvidence"),
        "subjectFocused": value.get("subjectFocused"),
        "researchFacetId": value.get("researchFacetId"),
        "researchFacetIds": list(value.get("researchFacetIds") or [])[:12],
        "researchFacetGoal": value.get("researchFacetGoal"),
        "evidenceViews": list(value.get("evidenceViews") or [])[:12],
        "evidenceQuery": value.get("evidenceQuery"),
        "contentChars": value.get("contentChars"),
        "originalContentChars": value.get("originalContentChars"),
        "omittedChars": value.get("omittedChars"),
        "evidenceSelection": value.get("evidenceSelection"),
        "readEvidence": value.get("readEvidence"),
        "selectedForEvidence": value.get("selectedForEvidence"),
        "rejectedReason": value.get("rejectedReason") or value.get("reason"),
        "reason": value.get("reason") or value.get("rejectedReason"),
        "qualityDimensions": value.get("qualityDimensions"),
        "detailRef": value.get("detailRef"),
    }
    gate = _compact_visible_quality_gate(value.get("sourceQualityGate"))
    if gate:
        compact["sourceQualityGate"] = gate
    return {key: item for key, item in compact.items() if item not in (None, "", [], {})}


def _compact_visible_source_ref(value: Any) -> dict[str, Any] | str:
    """Keep only the stable source aliases needed by a visible claim binding."""

    if isinstance(value, str):
        return _safe_text(value)
    if not isinstance(value, dict):
        return ""
    compact = {
        "sourceId": value.get("sourceId"),
        "url": value.get("url") or value.get("sourceUrl"),
        "citationKey": value.get("citationKey"),
    }
    return {key: item for key, item in compact.items() if item not in (None, "")}


def _compact_visible_claim(value: Any) -> dict[str, Any]:
    if not isinstance(value, dict):
        return {}
    compact = {
        "claimId": value.get("claimId"),
        "sourceId": value.get("sourceId"),
        "claim": _compact_research_text(value.get("claim"), limit=320),
        "claimType": value.get("claimType") or value.get("claimKind"),
        "normativeCue": _compact_research_text(value.get("normativeCue"), limit=160),
        "sourceRole": value.get("sourceRole"),
        "sourceClaim": _compact_research_text(value.get("sourceClaim"), limit=320),
        "evidenceExcerptKey": value.get("evidenceExcerptKey"),
        "confidence": value.get("confidence"),
        "evidenceExcerpt": _compact_research_text(value.get("evidenceExcerpt"), limit=600),
        "evidenceExcerptSha256": value.get("evidenceExcerptSha256"),
        "evidenceVerified": value.get("evidenceVerified") is True,
    }
    sources = [_compact_visible_source_ref(item) for item in list(value.get("supportingSources") or [])[:4]]
    sources = [item for item in sources if item]
    if sources:
        compact["supportingSources"] = sources
    return {key: item for key, item in compact.items() if item not in (None, "", [], {})}


def _compact_delivery_source(value: Any, *, minimal: bool = False) -> dict[str, Any]:
    """Project a cited source for Supervisor consumption, not proof replay.

    Full read receipts, source-quality dimensions, excerpts, and content hashes
    remain in the evidence ledger. Repeating them in every tool observation made
    a successful Research delivery several times larger than its answer.
    """

    if not isinstance(value, dict):
        return {}
    temporal = value.get("temporalEvidence") if isinstance(value.get("temporalEvidence"), dict) else {}
    read_evidence = value.get("readEvidence") if isinstance(value.get("readEvidence"), dict) else {}
    compact = {
        "sourceId": value.get("sourceId"),
        "citationKey": value.get("citationKey"),
        "title": _compact_research_text(value.get("title"), limit=100 if minimal else 160),
        "url": value.get("url") or value.get("sourceUrl"),
        "host": value.get("host"),
        "tier": value.get("tier"),
        "publishedAt": value.get("publishedAt") or temporal.get("publishedAt"),
        "updatedAt": value.get("updatedAt") or temporal.get("updatedAt"),
        "sourceDate": value.get("sourceDate") or temporal.get("sourceDate"),
        "sourceDateKind": value.get("sourceDateKind") or temporal.get("sourceDateKind"),
        "version": value.get("version") or temporal.get("version"),
        "retrievedAt": value.get("retrievedAt") or read_evidence.get("retrievedAt"),
        "selectedForEvidence": value.get("selectedForEvidence") is True,
    }
    if not minimal and read_evidence:
        compact["readEvidence"] = {
            key: item
            for key, item in {
                "verified": read_evidence.get("verified") is True,
                "contentChars": read_evidence.get("contentChars"),
                "retrievedAt": read_evidence.get("retrievedAt"),
            }.items()
            if item not in (None, "")
        }
    return {key: item for key, item in compact.items() if item not in (None, "", [], {})}


def _compact_delivery_claim(value: Any, *, minimal: bool = False) -> dict[str, Any]:
    """Keep a navigable claim index while ledger owns excerpt-level proof."""

    if not isinstance(value, dict):
        return {}
    compact = {
        "claimId": value.get("claimId"),
        "claim": (
            ""
            if minimal
            else _compact_research_text(value.get("claim"), limit=260)
        ),
        "claimType": value.get("claimType") or value.get("claimKind"),
        "sourceRole": value.get("sourceRole"),
        "confidence": value.get("confidence"),
        "evidenceVerified": value.get("evidenceVerified") is True,
    }
    sources = [
        _compact_visible_source_ref(item)
        for item in list(value.get("supportingSources") or [])[:4]
    ]
    sources = [item for item in sources if item]
    if sources:
        compact["supportingSources"] = sources
    return {key: item for key, item in compact.items() if item not in (None, "", [], {})}


def _compact_delivery_review(value: Any, *, minimal: bool = False) -> dict[str, Any]:
    """Expose the review decision and binding receipt without review transcripts."""

    if not isinstance(value, dict):
        return {}

    def receipt(item: Any) -> dict[str, Any]:
        if not isinstance(item, dict):
            return {}
        keys = (
            "reviewDecision",
            "questionCoverage",
            "claimEntailment",
            "freshnessAdequacy",
            "bindingVersion",
            "questionFingerprint",
            "answerSha256",
            "claimDigest",
            "sourceDigest",
            "temporalDigest",
            "reviewerModelId",
            "reviewedAt",
            "reviewMode",
        )
        return {
            key: item.get(key)
            for key in keys
            if item.get(key) not in (None, "", [], {})
        }

    compact = receipt(value)
    if not minimal:
        compact["reviewReasons"] = [
            _compact_research_text(item, limit=220)
            for item in list(value.get("reviewReasons") or [])[:4]
        ]
    for key in (
        "unsupportedClaims",
        "criticalMissingEvidence",
        "recommendedNextQueries",
    ):
        items = list(value.get(key) or [])[:4]
        if items:
            compact[key] = items
    for key in (
        "consensusAccepted",
        "consensusReviewCount",
        "consensusReviewerModelIds",
    ):
        if value.get(key) not in (None, "", [], {}):
            compact[key] = value.get(key)
    consensus = [
        receipt(item)
        for item in list(value.get("consensusReviews") or [])[:4]
    ]
    consensus = [item for item in consensus if item]
    if consensus:
        compact["consensusReviews"] = consensus
    return {key: item for key, item in compact.items() if item not in (None, "", [], {})}


def _compact_delivery_answer_pack(value: Any, *, minimal: bool = False) -> dict[str, Any]:
    """Single canonical Agent Surface for a delivery-ready Research result."""

    pack = dict(value or {}) if isinstance(value, dict) else {}
    if not pack:
        return {}
    compact = {
        "reviewDecision": pack.get("reviewDecision"),
        "answer": _truncate_research_text(
            pack.get("answer"),
            limit=_MAX_RESEARCH_VISIBLE_ANSWER_CHARS,
        ),
        "sources": [
            _compact_delivery_source(item, minimal=minimal)
            for item in list(pack.get("sources") or [])[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]
        ],
        "score": pack.get("score") or {},
        "claimTable": [
            _compact_delivery_claim(item, minimal=minimal)
            for item in list(pack.get("claimTable") or [])[:_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT]
        ],
        "independentReview": _compact_delivery_review(
            pack.get("independentReview"),
            minimal=minimal,
        ),
        "limitations": [
            _compact_research_text(item, limit=220)
            for item in list(pack.get("limitations") or [])[:4]
        ],
        "asOf": pack.get("asOf"),
        "recommendedNextAction": pack.get("recommendedNextAction"),
        "reuseDecision": pack.get("reuseDecision") or {},
        "detailRef": pack.get("detailRef"),
    }
    compact["sources"] = [item for item in compact["sources"] if item]
    compact["claimTable"] = [item for item in compact["claimTable"] if item]
    filtered = {key: item for key, item in compact.items() if item not in (None, [], {})}
    filtered["answer"] = compact.get("answer") or ""
    return filtered


def _compact_visible_model_synthesis(value: Any) -> dict[str, Any]:
    """Project delivery identities without repeating Runtime-only attempt traces."""

    if not isinstance(value, dict):
        return {}
    keys = (
        "used",
        "agentId",
        "agentName",
        "mode",
        "modelRole",
        "modelId",
        "writerModelRole",
        "writerModelId",
        "reviewerModelRole",
        "reviewerModelId",
        "reviewerConsensusCount",
        "reviewerConsensusModelIds",
        "parseMode",
        "sameEvidenceReviewRejected",
        "writerRevisionCount",
        "writerMode",
        "writerSectionCount",
    )
    return {
        key: value.get(key)
        for key in keys
        if value.get(key) not in (None, "", [], {})
    }


def _compact_visible_evidence_bank(value: Any) -> dict[str, Any]:
    if not isinstance(value, dict):
        return {"version": 1, "selectedSources": [], "rejectedSources": [], "claims": [], "stats": {}}
    selected = [
        _compact_visible_source(item)
        for item in list(value.get("selectedSources") or [])[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]
    ]
    rejected = [_compact_visible_source(item) for item in list(value.get("rejectedSources") or [])[:6]]
    claims = [
        _compact_visible_claim(item)
        for item in list(value.get("claims") or [])[:_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT]
    ]
    return {
        "version": value.get("version"),
        "selectedSources": [item for item in selected if item],
        "rejectedSources": [item for item in rejected if item],
        "claims": [item for item in claims if item],
        "stats": value.get("stats") or {},
    }


def _compact_visible_shard(value: Any) -> dict[str, Any]:
    if not isinstance(value, dict):
        return {}
    fetched: list[dict[str, Any]] = []
    for item in list(value.get("fetchedTopSources") or [])[:3]:
        if not isinstance(item, dict):
            continue
        fetched.append(
            {
                key: val
                for key, val in {
                    "title": _compact_research_text(item.get("title"), limit=140),
                    "url": item.get("url"),
                    "status": item.get("status"),
                    "extractionQuality": item.get("extractionQuality"),
                    "textPreview": _compact_research_text(item.get("textPreview"), limit=240),
                    "providerAttemptMatrix": list(item.get("providerAttemptMatrix") or [])[:4],
                }.items()
                if val not in (None, "", [], {})
            }
        )
    compact = {
        "shardId": value.get("shardId"),
        "kind": value.get("kind"),
        "query": value.get("query"),
        "ok": value.get("ok"),
        "resultCount": value.get("resultCount"),
        "provider": value.get("provider"),
        "networkRoute": value.get("networkRoute"),
        "sourceCapability": value.get("sourceCapability"),
        "fetchedTopSources": fetched,
        "errors": list(value.get("errors") or [])[:3],
    }
    return {key: item for key, item in compact.items() if item not in (None, "", [], {})}


def _compact_visible_loop_state(value: Any) -> dict[str, Any]:
    if not isinstance(value, dict):
        return {}
    compact = dict(value)
    compact["coveredClaims"] = [_compact_research_text(item, limit=180) for item in list(value.get("coveredClaims") or [])[:6]]
    compact["uncoveredClaims"] = [_compact_research_text(item, limit=180) for item in list(value.get("uncoveredClaims") or [])[:6]]
    compact["conflictClaims"] = list(value.get("conflictClaims") or [])[:6]
    compact["rejectedSources"] = list(value.get("rejectedSources") or [])[:6]
    compact["nextQueries"] = list(value.get("nextQueries") or [])[:4]
    compact["rounds"] = list(value.get("rounds") or [])[:4]
    return {key: item for key, item in compact.items() if item not in (None, "", [], {})}


def _compact_visible_answer_pack(value: Any) -> dict[str, Any]:
    pack = dict(value or {}) if isinstance(value, dict) else {}
    if not pack:
        return {}
    compact = {
        "reviewDecision": pack.get("reviewDecision"),
        "reviewReasons": list(pack.get("reviewReasons") or [])[:12],
        "answer": _truncate_research_text(pack.get("answer"), limit=_MAX_RESEARCH_VISIBLE_ANSWER_CHARS),
        "sources": [
            _compact_visible_source(item)
            for item in list(pack.get("sources") or [])[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]
        ],
        "score": pack.get("score") or {},
        "claimTable": [
            _compact_visible_claim(item)
            for item in list(pack.get("claimTable") or [])[:_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT]
        ],
        "independentReview": pack.get("independentReview") or {},
        "limitations": [_compact_research_text(item, limit=220) for item in list(pack.get("limitations") or [])[:6]],
        "criticalMissingEvidence": list(pack.get("criticalMissingEvidence") or [])[:12],
        "recommendedNextQueries": list(pack.get("recommendedNextQueries") or [])[:12],
        "asOf": pack.get("asOf"),
        "missingOrStaleReasons": list(pack.get("missingOrStaleReasons") or [])[:16],
        "recommendedNextAction": pack.get("recommendedNextAction"),
        "rejectedEvidence": [_compact_visible_source(item) for item in list(pack.get("rejectedEvidence") or [])[:6]],
        "reuseDecision": pack.get("reuseDecision") or {},
        "detailRef": pack.get("detailRef"),
    }
    compact["sources"] = [item for item in compact["sources"] if item]
    compact["claimTable"] = [item for item in compact["claimTable"] if item]
    compact["rejectedEvidence"] = [item for item in compact["rejectedEvidence"] if item]
    filtered = {key: item for key, item in compact.items() if item not in (None, [], {})}
    filtered["answer"] = compact.get("answer") or ""
    return filtered


def _visible_bundle(bundle: dict[str, Any]) -> dict[str, Any]:
    visible = {key: value for key, value in bundle.items() if not str(key).startswith("_")}
    visible["sourceMatrix"] = [
        _compact_visible_source(item)
        for item in list(visible.get("sourceMatrix") or [])[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]
    ]
    visible["sourceMatrix"] = [item for item in visible["sourceMatrix"] if item]
    visible["researchEvidenceBank"] = _compact_visible_evidence_bank(visible.get("researchEvidenceBank"))
    visible["rejectedSources"] = [_compact_visible_source(item) for item in list(visible.get("rejectedSources") or [])[:8]]
    visible["rejectedSources"] = [item for item in visible["rejectedSources"] if item]
    visible["claimTable"] = [
        _compact_visible_claim(item)
        for item in list(visible.get("claimTable") or [])[:_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT]
    ]
    visible["claimTable"] = [item for item in visible["claimTable"] if item]
    visible["researchLoopState"] = _compact_visible_loop_state(visible.get("researchLoopState"))
    raw_shards = list(visible.get("shards") or [])
    raw_shards.sort(key=lambda item: 0 if isinstance(item, dict) and item.get("fetchedTopSources") else 1)
    visible["shards"] = [_compact_visible_shard(item) for item in raw_shards[:8]]
    visible["shards"] = [item for item in visible["shards"] if item]
    answer_pack = _research_answer_pack(visible)
    visible["researchAnswerPack"] = _compact_visible_answer_pack(answer_pack)
    delivery_ready = bool(((answer_pack.get("score") or {}).get("deliveryReady")))
    for key in ("researchResult", "finalExperiencePack"):
        if isinstance(visible.get(key), dict):
            visible[key] = {**visible[key], "answer": "", "researchResult": ""}
    if not delivery_ready:
        visible["answer"] = ""
        visible["resultPreview"] = ""
    return visible


def _deterministic_facet_search_query(value: Any, *, max_chars: int = 180) -> str:
    """Turn a task brief into a bounded search-engine query.

    This is only the protocol-failure fallback for the model query planner. A
    full imperative brief often makes public search engines anchor on verbs
    such as ``Verify`` or ``Produce`` instead of the actual subject.
    """

    original = re.sub(r"\s+", " ", _safe_text(value)).strip()
    if not original:
        return ""
    query = re.sub(
        r"^(?:(?:please\s+)?(?:verify|determine|identify|research|investigate|"
        r"analy[sz]e|compare|assess|explain|check|confirm|produce|create|"
        r"generate|draft|prepare|provide)\b[\s,:-]*)+",
        "",
        original,
        flags=re.IGNORECASE,
    )
    query = re.sub(
        r"^(?:as\s+of\s+(?:\d{4}(?:-\d{1,2}-\d{1,2})?|[A-Za-z]+\s+\d{4})[,]?\s*)",
        "",
        query,
        flags=re.IGNORECASE,
    )
    query = re.sub(
        r"^(?:(?:请)?(?:确认|核实|验证|查证|调查|研究|分析|比较|评估|说明|生成|形成|制定|整理|梳理|给出|输出|提供)[：:,，\s]*)+",
        "",
        query,
    )
    query = re.split(
        r"(?:[.;。；]\s*)(?:cite|consult|请?引用|请?参照)\b",
        query,
        maxsplit=1,
        flags=re.IGNORECASE,
    )[0].strip(" ,，;；.。:-")
    if len(query) > max_chars:
        boundary = 0
        for match in re.finditer(r"[,，;；:]\s*", query[: max_chars + 1]):
            if match.start() >= 80:
                boundary = match.start()
        if not boundary:
            boundary = query.rfind(" ", 80, max_chars + 1)
        query = query[: boundary or max_chars].rstrip(" ,，;；.。:-")
    return query if len(query) >= 8 else original[:max_chars].rstrip()


def _build_shards(
    *,
    question: str,
    research_intent: str,
    source_policy: str,
    seed_urls: list[str],
    allowed_domains: list[str],
    max_shards: int,
) -> list[dict[str, Any]]:
    base_question = _safe_text(question)
    intent = _safe_text(research_intent) or "general_research"
    policy = _safe_text(source_policy).lower()
    video_research = _is_video_research(base_question, intent, policy)
    seed_domains = [_domain_from_seed(url) for url in seed_urls]
    domains = [domain for domain in [*allowed_domains, *seed_domains] if domain]
    shards: list[dict[str, Any]] = []
    seen: set[str] = set()
    for seed_url in seed_urls:
        normalized_seed = _safe_text(seed_url)
        if not normalized_seed or normalized_seed.lower() in seen:
            continue
        seen.add(normalized_seed.lower())
        shards.append(
            {
                "shardId": f"shard_{len(shards) + 1}_{_query_slug(normalized_seed)}",
                "kind": "seed_url",
                "query": normalized_seed,
                "seedUrl": normalized_seed,
                "evidenceQuery": base_question,
                "reason": "explicit_seed_url",
            }
        )
        if len(shards) >= max_shards:
            return shards
    queries: list[tuple[str, str]] = []
    # A managed Research episode may carry several explicit facets while still
    # requiring one shared evidence bundle and one final answer.  Search each
    # facet with its own bounded query; repeating the complete multi-page
    # question across every shard produces low-relevance discovery results and
    # defeats the point of parallel retrieval.
    facet_queries = [
        (_deterministic_facet_search_query(query), kind)
        for query, kind in _build_question_facet_queries(base_question)
    ]
    facet_queries = [(query, kind) for query, kind in facet_queries if query]
    queries.extend(facet_queries)
    if facet_queries:
        # Preserve a small source-intent dimension in the deterministic
        # fallback.  The normal run asks the bound Architect for better short
        # queries; these rows only keep provider/protocol failures from falling
        # back to repeated copies of the entire multi-facet question.
        year = datetime.now(timezone.utc).year
        for facet_query, facet_kind in facet_queries[
            : _research_authority_query_budget(len(facet_queries))
        ]:
            queries.append(
                (
                    f"{facet_query} official primary source {year}",
                    facet_kind,
                )
            )
    else:
        queries.append((base_question, "baseline"))
        if policy in {"official", "authoritative", "primary", ""}:
            queries.append((f"{base_question} official documentation", "official_docs"))
            queries.append((f"{base_question} official statement primary source", "primary_source"))
        if intent:
            queries.append((f"{base_question} {intent}", "intent"))
        queries.extend(
            [
                (f"{base_question} research report data statistics", "data_report"),
                (f"{base_question} independent analysis comparison", "independent_analysis"),
                (f"{base_question} limitations criticism counterevidence", "counterevidence"),
                (f"{base_question} latest update {datetime.now(timezone.utc).year}", "freshness_update"),
            ]
        )
    if video_research:
        queries.append((f"{base_question} top videos views likes ranking", "video_popularity"))
        queries.append((f"{base_question} 榜单 播放量 点赞", "video_popularity_cn"))
        for domain in _catalog_hosts_by_category("video_platform")[:6]:
            queries.append((f"site:{domain} {base_question}", f"video_site:{domain}"))
        for domain in _catalog_hosts_by_category("creative_showcase")[:4]:
            queries.append((f"site:{domain} {base_question}", f"creative_site:{domain}"))
    for domain in domains[: max(0, max_shards - len(queries))]:
        queries.append((f"site:{domain} {base_question}", f"site:{domain}"))

    for query, shard_kind in queries:
        normalized = _normalize_research_search_query(query)
        if not normalized:
            continue
        key = normalized.lower()
        if key in seen:
            continue
        seen.add(key)
        facet_id = _research_facet_id(shard_kind)
        shards.append(
            {
                "shardId": f"shard_{len(shards) + 1}_{_query_slug(normalized)}",
                "kind": shard_kind,
                "query": normalized,
                "evidenceQuery": normalized,
                **({"researchFacetId": facet_id} if facet_id else {}),
                "reason": shard_kind,
            }
        )
        if len(shards) >= max_shards:
            break
    return shards


def _parse_tool_json(value: Any) -> dict[str, Any]:
    if isinstance(value, dict):
        return value
    try:
        parsed = json.loads(str(value or ""))
    except Exception as exc:
        return {"ok": False, "error": str(exc), "rawPreview": str(value or "")[:800]}
    return parsed if isinstance(parsed, dict) else {"ok": False, "error": "non_object_tool_result", "rawPreview": str(value or "")[:800]}


def _render_payload(payload: dict[str, Any], *, max_chars: int = 12000) -> str:
    text = json.dumps(payload, ensure_ascii=False, indent=2)
    if len(text) <= max_chars:
        return text
    compact = dict(payload)
    omitted = dict(compact.get("omitted") or {})
    omitted["agentVisibleOriginalChars"] = len(text)
    omitted["agentVisibleBudgetChars"] = max_chars
    compact["omitted"] = omitted
    compact["truncatedForAgentVisibleOutput"] = True
    for key, limit in (("sourceMatrix", 5), ("citations", 5), ("shards", 4), ("rawRefs", 6), ("items", 10)):
        value = compact.get(key)
        if isinstance(value, list) and len(value) > limit:
            omitted[f"{key}OmittedForBudget"] = len(value) - limit
            compact[key] = value[:limit]
    text = json.dumps(compact, ensure_ascii=False, indent=2)
    if len(text) <= max_chars:
        return text
    raw_final_pack = dict(payload.get("finalExperiencePack") or payload.get("researchResult") or {})
    fallback_final_pack = {
        key: value
        for key, value in raw_final_pack.items()
        if key
        in {
            "kind",
            "question",
            "freshness",
            "architectAgentId",
            "architectName",
            "headline",
            "confidence",
            "sourceUrls",
            "synthesisMode",
            "modelSynthesis",
            "reviewDecision",
            "reviewReasons",
            "criticalMissingEvidence",
            "recommendedNextQueries",
            "asOf",
            "temporalAssessment",
        }
    }
    if isinstance(fallback_final_pack.get("modelSynthesis"), dict):
        fallback_final_pack["modelSynthesis"] = _compact_visible_model_synthesis(
            fallback_final_pack.get("modelSynthesis")
        )
    if raw_final_pack.get("researchResult"):
        fallback_final_pack["researchResult"] = _truncate_research_text(
            raw_final_pack.get("researchResult"),
            limit=_MAX_RESEARCH_VISIBLE_ANSWER_CHARS,
        )
    if isinstance(fallback_final_pack.get("sourceUrls"), list):
        fallback_final_pack["sourceUrls"] = list(
            fallback_final_pack.get("sourceUrls") or []
        )[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]

    fallback = {
        "ok": bool(payload.get("ok")),
        "kind": payload.get("kind") or payload.get("mode") or "research_payload",
        "summary": _safe_text(payload.get("summary"))[:600],
        "answer": _truncate_research_text(
            (payload.get("researchAnswerPack") or {}).get("answer")
            if isinstance(payload.get("researchAnswerPack"), dict)
            else payload.get("answer") or payload.get("resultPreview"),
            limit=_MAX_RESEARCH_VISIBLE_ANSWER_CHARS,
        ),
        "researchAnswerPack": _compact_visible_answer_pack(payload.get("researchAnswerPack") or _research_answer_pack(payload)),
        "finalExperiencePack": fallback_final_pack,
        "evidenceBundleId": payload.get("evidenceBundleId"),
        "deliveryReady": payload.get("deliveryReady"),
        "qualityTier": payload.get("qualityTier"),
        "qualityMetrics": payload.get("qualityMetrics") or {},
        "reviewDecision": payload.get("reviewDecision"),
        "reviewReasons": list(payload.get("reviewReasons") or [])[:12],
        "asOf": payload.get("asOf"),
        "confidence": payload.get("confidence"),
        "authorityScore": payload.get("authorityScore"),
        "researchLoopState": _compact_visible_loop_state(payload.get("researchLoopState")),
        "experienceReuse": payload.get("experienceReuse"),
        "claimTable": [_compact_visible_claim(item) for item in list(payload.get("claimTable") or [])[:5]],
        "conflictMatrix": list(payload.get("conflictMatrix") or [])[:5],
        "sourceMatrix": [_compact_visible_source(item) for item in list(payload.get("sourceMatrix") or [])[:3]],
        "researchEvidenceBank": _compact_visible_evidence_bank(payload.get("researchEvidenceBank")),
        "rejectedSources": [_compact_visible_source(item) for item in list(payload.get("rejectedSources") or [])[:5]],
        "shards": [_compact_visible_shard(item) for item in list(payload.get("shards") or [])[:3]],
        "providerAttemptMatrix": list(payload.get("providerAttemptMatrix") or [])[:12],
        "omitted": {
            **omitted,
            "fallback": "agent_visible_budget_fallback",
            "detailTool": "research_broker(mode='get_evidence', evidenceBundleId=...)",
        },
        "recommendedNextAction": payload.get("recommendedNextAction") or "get_evidence",
    }
    fallback_text = json.dumps(fallback, ensure_ascii=False, indent=2)
    if len(fallback_text) <= max_chars:
        return fallback_text
    if payload.get("deliveryReady"):
        raw_answer_pack = payload.get("researchAnswerPack") or _research_answer_pack(payload)
        delivery_answer_pack = _compact_delivery_answer_pack(
            raw_answer_pack,
            minimal=True,
        )
        claim_count = len(delivery_answer_pack.get("claimTable") or [])
        delivery_answer_pack.pop("claimTable", None)
        if claim_count:
            delivery_answer_pack["claimTableSummary"] = {
                "claimCount": claim_count,
                "proofLocation": "evidenceBundleId",
            }
        evidence_bundle_id = _safe_text(payload.get("evidenceBundleId"))
        if evidence_bundle_id and not delivery_answer_pack.get("detailRef"):
            delivery_answer_pack["detailRef"] = f"research://bundle/{evidence_bundle_id}"
        delivery_final_pack = {
            key: value
            for key, value in fallback_final_pack.items()
            if key
            in {
                "kind",
                "architectAgentId",
                "architectName",
                "headline",
                "confidence",
                "synthesisMode",
                "modelSynthesis",
                "reviewDecision",
                "asOf",
            }
        }
        loop_state = {
            key: value
            for key, value in _compact_visible_loop_state(
                payload.get("researchLoopState")
            ).items()
            if key in {"phase", "stopReason", "researchLoopReport"}
        }
        priority = {
            "ok": True,
            "kind": payload.get("kind") or "research_evidence_bundle",
            "summary": _safe_text(payload.get("summary"))[:600],
            # researchAnswerPack is the one canonical delivery copy. The full
            # proof graph remains retrievable from the governed ledger.
            "researchAnswerPack": delivery_answer_pack,
            "finalExperiencePack": delivery_final_pack,
            "question": payload.get("question") or fallback_final_pack.get("question"),
            "freshness": payload.get("freshness") or fallback_final_pack.get("freshness"),
            "evidenceBundleId": evidence_bundle_id,
            "deliveryReady": True,
            "qualityTier": payload.get("qualityTier"),
            "reviewDecision": payload.get("reviewDecision"),
            "asOf": payload.get("asOf"),
            "researchLoopState": loop_state,
            "experienceReuse": payload.get("experienceReuse"),
            "omitted": {
                "agentVisibleOriginalChars": len(text),
                "agentVisibleBudgetChars": max_chars,
                "fallback": "delivery_first_agent_visible_output",
                "runtimeProof": "ledger_only",
                "claimEvidenceOmittedForBudget": claim_count,
                "debugEvidence": "use research_broker(mode='get_evidence', evidenceBundleId=...) for excerpts, hashes, review receipts, shards, and rejected sources",
            },
            "recommendedNextAction": "use_research_answer_pack",
        }
        priority_text = json.dumps(priority, ensure_ascii=False, separators=(",", ":"))
        if len(priority_text) > max_chars:
            review = dict(delivery_answer_pack.get("independentReview") or {})
            review.pop("consensusReviews", None)
            delivery_answer_pack["independentReview"] = review
            priority["researchAnswerPack"] = delivery_answer_pack
            priority_text = json.dumps(priority, ensure_ascii=False, separators=(",", ":"))
        if len(priority_text) > max_chars:
            delivery_answer_pack["sources"] = [
                {
                    key: item.get(key)
                    for key in (
                        "sourceId",
                        "citationKey",
                        "title",
                        "url",
                        "selectedForEvidence",
                    )
                    if item.get(key) not in (None, "", [], {})
                }
                for item in list(delivery_answer_pack.get("sources") or [])
                if isinstance(item, dict)
            ]
            priority["researchAnswerPack"] = delivery_answer_pack
            priority_text = json.dumps(priority, ensure_ascii=False, separators=(",", ":"))
        compatibility = dict(priority)
        compatibility["answer"] = delivery_answer_pack.get("answer") or ""
        compatibility_text = json.dumps(
            compatibility,
            ensure_ascii=False,
            separators=(",", ":"),
        )
        if len(compatibility_text) <= max_chars:
            return compatibility_text
        return priority_text
    answer_pack = _compact_visible_answer_pack(fallback.get("researchAnswerPack"))
    answer_pack["answer"] = _compact_research_text(answer_pack.get("answer"), limit=700)
    answer_pack["sources"] = list(answer_pack.get("sources") or [])[:2]
    answer_pack["claimTable"] = list(answer_pack.get("claimTable") or [])[:2]
    answer_pack["rejectedEvidence"] = list(answer_pack.get("rejectedEvidence") or [])[:2]
    minimal = {
        "ok": fallback.get("ok"),
        "kind": fallback.get("kind"),
        "summary": _compact_research_text(fallback.get("summary"), limit=420),
        "answer": _compact_research_text(fallback.get("answer"), limit=700),
        "researchAnswerPack": answer_pack,
        "finalExperiencePack": {
            key: value
            for key, value in dict(fallback.get("finalExperiencePack") or {}).items()
            if key in {"kind", "architectAgentId", "architectName", "headline", "confidence", "synthesisMode", "modelSynthesis"}
        },
        "evidenceBundleId": fallback.get("evidenceBundleId"),
        "deliveryReady": fallback.get("deliveryReady"),
        "qualityTier": fallback.get("qualityTier"),
        "qualityMetrics": fallback.get("qualityMetrics") or {},
        "reviewDecision": fallback.get("reviewDecision"),
        "asOf": fallback.get("asOf"),
        "confidence": fallback.get("confidence"),
        "authorityScore": fallback.get("authorityScore"),
        "claimTable": list(fallback.get("claimTable") or [])[:3],
        "sourceMatrix": list(fallback.get("sourceMatrix") or [])[:3],
        "researchLoopState": _compact_visible_loop_state(fallback.get("researchLoopState")),
        "experienceReuse": fallback.get("experienceReuse"),
        "researchEvidenceBank": {
            "selectedSources": list((fallback.get("researchEvidenceBank") or {}).get("selectedSources") or [])[:2],
            "rejectedSources": list((fallback.get("researchEvidenceBank") or {}).get("rejectedSources") or [])[:2],
            "claims": list((fallback.get("researchEvidenceBank") or {}).get("claims") or [])[:2],
            "stats": (fallback.get("researchEvidenceBank") or {}).get("stats") or {},
        },
        "shards": list(fallback.get("shards") or [])[:1],
        "omitted": {
            **omitted,
            "fallback": "agent_visible_budget_minimal",
            "detailTool": "research_broker(mode='get_evidence', evidenceBundleId=...)",
        },
        "recommendedNextAction": fallback.get("recommendedNextAction"),
    }
    minimal_text = json.dumps(minimal, ensure_ascii=False, indent=2)
    if len(minimal_text) <= max_chars:
        return minimal_text
    answer_pack.pop("claimTable", None)
    answer_pack.pop("rejectedEvidence", None)
    minimal["researchAnswerPack"] = answer_pack
    minimal["claimTable"] = list(minimal.get("claimTable") or [])[:1]
    minimal["researchEvidenceBank"] = {
        "selectedSources": list((minimal.get("researchEvidenceBank") or {}).get("selectedSources") or [])[:1],
        "rejectedSources": list((minimal.get("researchEvidenceBank") or {}).get("rejectedSources") or [])[:1],
        "claims": [],
        "stats": (minimal.get("researchEvidenceBank") or {}).get("stats") or {},
    }
    minimal["shards"] = []
    return json.dumps(minimal, ensure_ascii=False, indent=2)


def _compact_research_text(value: Any, *, limit: int = 700) -> str:
    text = re.sub(r"\s+", " ", str(value or "")).strip()
    if len(text) <= limit:
        return text
    cutoff = max(1, limit - 3)
    prefix = text[:cutoff].rstrip()
    # Visible evidence should not end halfway through a sentence or token just
    # because an internal transport field has a character budget. Prefer a
    # complete sentence when one is reasonably close to the boundary, then a
    # word boundary; very long code/URLs still retain the hard upper bound.
    sentence_boundaries = list(
        re.finditer(r"(?<!\s)[.!?。！？](?=\s|$)", prefix)
    )
    if sentence_boundaries and sentence_boundaries[-1].end() >= int(cutoff * 0.55):
        prefix = prefix[: sentence_boundaries[-1].end()].rstrip()
    else:
        word_boundary = prefix.rfind(" ", int(cutoff * 0.70))
        if word_boundary > 0:
            prefix = prefix[:word_boundary].rstrip()
    return prefix + "..."


def _truncate_research_text(value: Any, *, limit: int) -> str:
    text = str(value or "").strip()
    if len(text) <= limit:
        return text
    return text[: limit - 3].rstrip() + "..."


def _is_noisy_research_sentence(value: Any) -> bool:
    text = _safe_text(value)
    lowered = text.lower()
    if not text:
        return True
    noise_markers = (
        "navigation - index - modules",
        "navigation - index - next",
        "theme auto light dark",
        "free download: click here",
        "free email series",
        "unsubscribe any time",
        "privacy policy & safety",
        "about press copyright",
        "security check required",
        "verify you are human",
        "we've detected unusual activity",
        "access denied",
    )
    if any(marker in lowered for marker in noise_markers):
        return True
    if len(text) < 180 and re.match(r"^#{1,6}\s+", text):
        return True
    if lowered.startswith(("**source code:**", "source code:", "view source", "edit this page")):
        return True
    if text.startswith("|") or (text.count("](") >= 4 and len(re.sub(r"\[[^\]]+\]\([^\)]+\)", "", text).strip()) < 80):
        return True
    if text.count("|") >= 5 or text.count("»") >= 4:
        return True
    return False


def _research_sentences(value: Any, *, limit: int = 3) -> list[str]:
    text = _compact_research_text(value, limit=1800)
    if not text:
        return []
    parts = [
        item.strip(" \t\r\n-•。；;")
        for item in re.split(r"(?<=[。！？!?\.])\s+|[；;]\s*", text)
        if item.strip(" \t\r\n-•。；;")
    ]
    if not parts:
        parts = [text]
    result: list[str] = []
    for part in parts:
        if len(part) < 12 and len(parts) > 1:
            continue
        if _is_noisy_research_sentence(part):
            continue
        result.append(_compact_research_text(part, limit=260))
        if len(result) >= limit:
            break
    if result:
        return result
    return [] if _is_noisy_research_sentence(text) else [_compact_research_text(text, limit=260)]


def _research_text_list(value: Any, *, limit: int = 12) -> list[str]:
    def structured_text(item: dict[str, Any]) -> str:
        return _compact_research_text(
            item.get("summary")
            or item.get("reason")
            or item.get("claim")
            or item.get("missingPremise")
            or item.get("gap")
            or item.get("query")
            or item.get("searchQuery")
            or item.get("description")
            or item.get("purpose")
            or item,
            limit=320,
        )

    if value is None:
        return []
    if isinstance(value, str):
        text = _compact_research_text(value, limit=320)
        return [text] if text else []
    if isinstance(value, dict):
        text = structured_text(value)
        return [text] if text else []
    result: list[str] = []
    if isinstance(value, (list, tuple, set)):
        for item in value:
            if isinstance(item, dict):
                text = structured_text(item)
            else:
                text = _compact_research_text(item, limit=320)
            if text and text not in result:
                result.append(text)
            if len(result) >= limit:
                break
    return result


def _research_dict_list(value: Any, *, limit: int = 12) -> list[dict[str, Any]]:
    if isinstance(value, dict):
        return [value]
    if not isinstance(value, (list, tuple, set)):
        return []
    result: list[dict[str, Any]] = []
    for item in value:
        if isinstance(item, dict):
            result.append(item)
        if len(result) >= limit:
            break
    return result


def _fetched_source_map(
    shards: list[dict[str, Any]],
    *,
    question: str = "",
) -> dict[str, dict[str, Any]]:
    fetched: dict[str, dict[str, Any]] = {}
    ranks: dict[str, tuple[int, int, int]] = {}
    for shard in shards:
        for item in list(shard.get("fetchedTopSources") or []):
            if not isinstance(item, dict):
                continue
            text = _safe_text(item.get("text") or item.get("markdown") or item.get("textPreview"))
            rank = (
                1 if item.get("ok") is not False and bool(text) else 0,
                _source_relevance_score(
                    question,
                    title=_safe_text(item.get("title")),
                    text=text,
                )
                if question
                else 0,
                len(text),
            )
            for url in (_safe_text(item.get("url")), _safe_text(item.get("finalUrl"))):
                if not url:
                    continue
                if url not in fetched or (question and rank > ranks[url]):
                    fetched[url] = item
                    ranks[url] = rank
    return fetched


def _confidence_rank(value: Any) -> int:
    return {"low": 1, "medium": 2, "high": 3}.get(_safe_text(value).lower(), 0)


def _topic_fingerprint(value: str) -> str:
    normalized = re.sub(r"\s+", " ", _safe_text(value).lower())
    normalized = re.sub(r"[^\w\u4e00-\u9fff]+", " ", normalized).strip()
    return hashlib.sha1(normalized.encode("utf-8")).hexdigest()[:16]


_REUSE_STOP_WORDS = {
    "what",
    "are",
    "is",
    "the",
    "a",
    "an",
    "for",
    "in",
    "to",
    "and",
    "or",
    "of",
    "on",
    "by",
    "as",
    "about",
    "compare",
    "claims",
    "that",
    "when",
    "where",
    "which",
    "how",
    "why",
    "best",
    "current",
    "latest",
    "using",
    "use",
    "uses",
    "with",
    "from",
    "source",
    "sources",
    "cite",
    "official",
    "please",
    "请",
    "说明",
    "来源",
    "最新",
    "当前",
    "如何",
}

_REUSE_GENERIC_IDENTIFIER_TOKENS = {
    "api",
    "check",
    "cli",
    "current",
    "docs",
    "documentation",
    "guide",
    "latest",
    "official",
    "reference",
    "release",
    "releases",
    "runtime",
    "sdk",
    "support",
    "supported",
    "verify",
    "version",
    "versions",
    "windows",
}


def _reuse_tokens(value: str) -> set[str]:
    text = re.sub(r"\s+", " ", _safe_text(value).lower())
    tokens = {
        token
        for token in re.split(r"[^\w\u4e00-\u9fff]+", text)
        if len(token) >= 2 and token not in _REUSE_STOP_WORDS
    }
    # CJK questions are often one long token after punctuation splitting. Add
    # coarse bigrams so near-identical Chinese titles/queries can still match
    # without letting generic words dominate.
    cjk = "".join(re.findall(r"[\u4e00-\u9fff]", text))
    if len(cjk) >= 4:
        tokens.update(cjk[index : index + 2] for index in range(0, min(len(cjk) - 1, 24)))
    return tokens


def _reuse_identifier_tokens(value: str) -> set[str]:
    """Return distinctive Latin/technical anchors, excluding generic research vocabulary."""

    text = _safe_text(value).lower()
    return {
        token
        for token in re.findall(r"[a-z][a-z0-9_.+-]{2,}", text)
        if token not in _REUSE_STOP_WORDS
        and token not in _REUSE_GENERIC_IDENTIFIER_TOKENS
        and not re.fullmatch(r"20\d{2}", token)
    }


def _reuse_topic_match(question: str, pack: dict[str, Any]) -> tuple[bool, str]:
    normalized_question = re.sub(r"\s+", " ", _safe_text(question).lower()).strip()
    candidate_text = " ".join(
        [
            _safe_text(pack.get("query")),
            _safe_text(pack.get("title")),
        ]
    ).lower()
    if not normalized_question:
        return False, "empty_question"
    if _safe_text(pack.get("topicFingerprint")) == _topic_fingerprint(question):
        candidate_question = re.sub(r"\s+", " ", _safe_text(pack.get("query")).lower()).strip()
        if candidate_question == normalized_question:
            return True, "topic_fingerprint_match"
        return True, "topic_fingerprint_variant_requires_review"
    if normalized_question and normalized_question in candidate_text:
        return True, "exact_question_contained_in_candidate"
    question_identifiers = _reuse_identifier_tokens(question)
    candidate_identifiers = _reuse_identifier_tokens(candidate_text)
    if question_identifiers and candidate_identifiers and not question_identifiers.intersection(candidate_identifiers):
        return False, "distinctive_identifier_mismatch"
    q_tokens = _reuse_tokens(question)
    p_tokens = _reuse_tokens(candidate_text)
    if not q_tokens or not p_tokens:
        return False, "insufficient_topic_tokens"
    overlap = q_tokens.intersection(p_tokens)
    ratio = len(overlap) / max(1, min(len(q_tokens), len(p_tokens)))
    if len(overlap) >= 2 and ratio >= 0.35:
        return True, f"topic_overlap:{ratio:.2f}"
    return False, f"topic_overlap_too_low:{ratio:.2f}"


def _source_urls_from_matrix(source_matrix: list[dict[str, Any]]) -> list[str]:
    urls: list[str] = []
    for item in source_matrix:
        if not isinstance(item, dict):
            continue
        url = _safe_text(item.get("url"))
        if url and url not in urls:
            urls.append(url)
    return urls


def _flatten_provider_attempts(shards: list[dict[str, Any]]) -> list[dict[str, Any]]:
    attempts: list[dict[str, Any]] = []
    for shard in shards:
        matrix = shard.get("providerAttemptMatrix")
        if isinstance(matrix, list):
            for item in matrix:
                if isinstance(item, dict):
                    attempts.append({"shardId": shard.get("shardId"), "query": shard.get("query"), **item})
        elif matrix:
            attempts.append({"shardId": shard.get("shardId"), "query": shard.get("query"), "attempt": matrix})
        elif shard.get("provider") or shard.get("networkRoute"):
            attempts.append(
                {
                    "shardId": shard.get("shardId"),
                    "query": shard.get("query"),
                    "provider": shard.get("provider"),
                    "networkRoute": shard.get("networkRoute"),
                    "ok": shard.get("ok"),
                    "fallbackReason": shard.get("errors"),
                }
            )
    return attempts[:40]


def _explicit_question_as_of(question: str) -> datetime | None:
    text = _safe_text(question)
    patterns = (
        r"(?<!\d)(20\d{2})[-/.年](\d{1,2})[-/.月](\d{1,2})(?:日)?(?!\d)",
        r"(?<!\d)(20\d{2})[-/.年](\d{1,2})(?:月)?(?!\d)",
    )
    for pattern in patterns:
        match = re.search(pattern, text)
        if not match:
            continue
        try:
            year = int(match.group(1))
            month = int(match.group(2))
            day = int(match.group(3)) if match.lastindex and match.lastindex >= 3 else 1
            return datetime(year, month, day, tzinfo=timezone.utc)
        except (TypeError, ValueError):
            continue
    return None


def _experience_pack_as_of(pack: dict[str, Any]) -> datetime | None:
    answer_pack = pack.get("researchAnswerPack") if isinstance(pack.get("researchAnswerPack"), dict) else {}
    raw = _safe_text(pack.get("asOf") or answer_pack.get("asOf"))
    if not raw:
        return None
    try:
        parsed = datetime.fromisoformat(raw.replace("Z", "+00:00"))
    except ValueError:
        return None
    if parsed.tzinfo is None:
        parsed = parsed.replace(tzinfo=timezone.utc)
    return parsed.astimezone(timezone.utc)


def _experience_reuse_decision(
    packs: list[dict[str, Any]],
    *,
    question: str,
    source_policy: str,
    freshness: str,
    min_confidence: str = "",
) -> dict[str, Any]:
    if not packs:
        return {
            "reuseDecision": "ignore",
            "reason": "no_matching_experience_pack",
            "candidatePackId": None,
            "topicFingerprint": _topic_fingerprint(question),
        }
    min_rank = _confidence_rank(min_confidence) or 2
    normalized_policy = _safe_text(source_policy).lower()
    exact_candidates: list[tuple[dict[str, Any], str]] = []
    adjacent_candidates: list[tuple[dict[str, Any], str]] = []
    for pack in packs:
        matched, match_reason = _reuse_topic_match(question, pack)
        if not matched:
            continue
        target = exact_candidates if match_reason == "topic_fingerprint_match" else adjacent_candidates
        target.append((pack, match_reason))

    refresh_candidates: list[dict[str, Any]] = []
    requested_as_of = _explicit_question_as_of(question)
    for pack, match_reason in exact_candidates:
        pack_freshness_state = _safe_text(pack.get("freshnessState")).lower()
        freshness_notice: dict[str, Any] = {}
        if pack_freshness_state in {"aging", "stale", "expired"}:
            pack_as_of_text = _safe_text(pack.get("asOf")) or "unknown"
            age_days = pack.get("ageDays")
            age_text = f", age {age_days} days" if age_days not in (None, "") else ""
            freshness_notice = {
                "freshnessState": pack_freshness_state,
                "ageDays": age_days,
                "staleAt": pack.get("staleAt"),
                "expiresAt": pack.get("expiresAt"),
                "refreshSuggested": True,
                "supervisorContentNote": (
                    f"Reused Research experience is {pack_freshness_state} "
                    f"(as of {pack_as_of_text}{age_text}). Decide whether that dated evidence is still "
                    "sufficient; if current facts could change the answer, rerun Research with forceRefresh=true."
                ),
            }
        pack_as_of = _experience_pack_as_of(pack)
        if requested_as_of and (pack_as_of is None or requested_as_of > pack_as_of):
            refresh_candidates.append({
                "reuseDecision": "refresh",
                "reason": "requested_as_of_newer_than_experience_pack",
                "matchReason": match_reason,
                "candidatePackId": pack.get("experiencePackId"),
                "requestedAsOf": requested_as_of.date().isoformat(),
                "candidateAsOf": pack_as_of.isoformat().replace("+00:00", "Z") if pack_as_of else None,
                "topicFingerprint": pack.get("topicFingerprint") or _topic_fingerprint(question),
            })
            continue
        quality_status = _safe_text(pack.get("qualityStatus")).lower()
        invalidation_reason = _safe_text(pack.get("invalidationReason"))
        has_final_result = bool(_safe_text(pack.get("researchResult")) or list(pack.get("claimDigest") or []))
        has_sources = bool(list(pack.get("sourceUrls") or []) or list(pack.get("sourceMatrixDigest") or []))
        try:
            authority_score = float(pack.get("authorityScore") or 0)
        except (TypeError, ValueError):
            authority_score = 0.0
        if quality_status == "low_quality_pack" or invalidation_reason or not has_final_result or not has_sources or authority_score < 35:
            reasons = []
            if quality_status == "low_quality_pack":
                reasons.append("low_quality_pack")
            if invalidation_reason:
                reasons.append(invalidation_reason)
            if not has_final_result:
                reasons.append("missing_final_research_result")
            if not has_sources:
                reasons.append("missing_sources")
            if authority_score < 35:
                reasons.append("low_authority")
            refresh_candidates.append({
                "reuseDecision": "refresh",
                "reason": "refresh_required_due_to_pack_quality",
                "qualityReasons": reasons,
                "matchReason": match_reason,
                "candidatePackId": pack.get("experiencePackId"),
                "candidateConfidence": pack.get("confidence"),
                "topicFingerprint": pack.get("topicFingerprint") or _topic_fingerprint(question),
            })
            continue
        confidence = _safe_text(pack.get("confidence")).lower()
        if _confidence_rank(confidence) < min_rank:
            refresh_candidates.append({
                "reuseDecision": "refresh",
                "reason": "candidate_confidence_below_threshold",
                "matchReason": match_reason,
                "candidatePackId": pack.get("experiencePackId"),
                "candidateConfidence": confidence,
                "requiredConfidence": min_confidence or "medium",
                "topicFingerprint": _topic_fingerprint(question),
            })
            continue
        pack_policy = _safe_text(pack.get("sourcePolicy")).lower()
        if pack_policy and normalized_policy and pack_policy != normalized_policy:
            refresh_candidates.append({
                "reuseDecision": "refresh",
                "reason": "source_policy_changed",
                "matchReason": match_reason,
                "candidatePackId": pack.get("experiencePackId"),
                "previousSourcePolicy": pack_policy,
                "requestedSourcePolicy": normalized_policy,
                "topicFingerprint": _topic_fingerprint(question),
            })
            continue
        return {
            "reuseDecision": "reuse",
            "reason": (
                "dated_experience_pack_reused_with_supervisor_refresh_note"
                if freshness_notice
                else "high_confidence_pack_matches_topic_and_policy"
            ),
            "matchReason": match_reason,
            "candidatePackId": pack.get("experiencePackId"),
            "candidateTitle": pack.get("title"),
            "candidateConfidence": confidence,
            "skippedSearches": True,
            "topicFingerprint": pack.get("topicFingerprint") or _topic_fingerprint(question),
            **freshness_notice,
        }
    if refresh_candidates:
        return refresh_candidates[0]
    if adjacent_candidates:
        pack, match_reason = adjacent_candidates[0]
        return {
            "reuseDecision": "refresh",
            "reason": "adjacent_topic_requires_fresh_semantic_review",
            "matchReason": match_reason,
            "candidatePackId": pack.get("experiencePackId"),
            "topicFingerprint": _topic_fingerprint(question),
        }
    return {
        "reuseDecision": "ignore",
        "reason": "no_topic_matched_reusable_candidate_after_filtering",
        "candidatePackId": None,
        "topicFingerprint": _topic_fingerprint(question),
    }


def _bundle_from_reused_pack(pack: dict[str, Any], *, question: str, reuse: dict[str, Any], deliverable: str) -> dict[str, Any]:
    canonical_answer_pack = (
        dict(pack.get("researchAnswerPack"))
        if isinstance(pack.get("researchAnswerPack"), dict)
        else {}
    )
    source_matrix = [
        dict(item)
        for item in list(
            canonical_answer_pack.get("sources")
            or pack.get("sourceMatrixDigest")
            or pack.get("sourceUrls")
            or []
        )
        if isinstance(item, dict)
    ]
    source_urls = _source_urls_from_matrix(source_matrix)
    result = _safe_text(
        canonical_answer_pack.get("answer")
        or pack.get("researchResult")
        or pack.get("resultPreview")
        or pack.get("summary")
    )
    claim_table = list(canonical_answer_pack.get("claimTable") or pack.get("claimDigest") or [])
    independent_review = (
        canonical_answer_pack.get("independentReview")
        if isinstance(canonical_answer_pack.get("independentReview"), dict)
        else pack.get("independentReview") or {}
    )
    experience_pack_id = _safe_text(pack.get("experiencePackId"))
    evidence_bundle_id = _safe_text(pack.get("createdFromBundleId"))
    detail_ref = (
        f"research://bundle/{evidence_bundle_id}"
        if evidence_bundle_id
        else f"research://experience/{experience_pack_id}"
        if experience_pack_id
        else ""
    )
    architect_pack = {
        "kind": "research_result_pack",
        "architectAgentId": "web-research-architect",
        "architectName": "Web Research Architect",
        "question": question,
        "headline": f"Reused experience pack: {pack.get('title') or pack.get('experiencePackId')}",
        "reviewDecision": canonical_answer_pack.get("reviewDecision") or pack.get("reviewDecision"),
        "reviewReasons": list(pack.get("reviewReasons") or []),
        "answer": result,
        "researchResult": result,
        "claimTable": claim_table,
        "independentReview": independent_review,
        "sourceUrls": source_matrix,
        "criticalMissingEvidence": list(pack.get("criticalMissingEvidence") or []),
        "recommendedNextQueries": list(pack.get("recommendedNextQueries") or []),
        "asOf": canonical_answer_pack.get("asOf") or pack.get("asOf"),
        "temporalAssessment": pack.get("temporalAssessment") if isinstance(pack.get("temporalAssessment"), dict) else {},
        "confidence": pack.get("confidence"),
        "authorityScore": pack.get("authorityScore"),
        "createdAt": _utc_now_iso(),
    }
    bundle = {
        "ok": False,
        "kind": "research_evidence_bundle",
        "summary": architect_pack["headline"],
        "question": question,
        "evidenceBundleId": evidence_bundle_id or None,
        "experiencePackId": experience_pack_id or None,
        "detailRef": detail_ref or None,
        "researchIntent": "experience_reuse",
        "sourcePolicy": pack.get("sourcePolicy"),
        "freshness": pack.get("requestedFreshness") or pack.get("freshness") or pack.get("freshnessWindow"),
        "deliverable": deliverable,
        "answer": result,
        "resultPreview": result,
        "researchResult": architect_pack,
        "finalExperiencePack": architect_pack,
        "researchAnswerPack": canonical_answer_pack,
        "claimTable": claim_table,
        "briefCoverageRequired": pack.get("briefCoverageRequired") is True,
        "briefCoverageComplete": pack.get("briefCoverageComplete") is True,
        "briefCoverage": [
            dict(item)
            for item in list(pack.get("briefCoverage") or [])
            if isinstance(item, dict)
        ],
        "coveredTaskBriefIds": list(pack.get("coveredTaskBriefIds") or []),
        "missingTaskBriefIds": list(pack.get("missingTaskBriefIds") or []),
        "conflictMatrix": [],
        "missingEvidence": [],
        "assumptions": [],
        "sourceMatrix": source_matrix,
        "sourceUrls": source_urls,
        "providerAttemptMatrix": [],
        "researchLoopState": {
            "phase": "experience_reused",
            "rounds": [],
            "stopReason": "experience_reused",
            "questions": [question],
            "readSources": source_urls,
            "uncoveredClaims": [],
            "conflictClaims": [],
            "nextQueries": [],
        },
        "experienceReuse": {**reuse, "pack": pack},
        "confidence": pack.get("confidence") or "medium",
        "authorityScore": pack.get("authorityScore"),
        "recommendedNextAction": "use_reused_experience_pack",
    }
    bundle["researchAnswerPack"] = _research_answer_pack(bundle)
    delivery_ready = bool(((bundle["researchAnswerPack"].get("score") or {}).get("deliveryReady")))
    bundle["ok"] = delivery_ready
    bundle["answer"] = bundle["researchAnswerPack"].get("answer") or ""
    bundle["resultPreview"] = bundle["answer"]
    bundle["reviewDecision"] = bundle["researchAnswerPack"].get("reviewDecision")
    bundle["reviewReasons"] = list(bundle["researchAnswerPack"].get("reviewReasons") or [])
    bundle["independentReview"] = bundle["researchAnswerPack"].get("independentReview") or {}
    bundle["asOf"] = bundle["researchAnswerPack"].get("asOf")
    bundle["qualityTier"] = (bundle["researchAnswerPack"].get("score") or {}).get("qualityTier")
    bundle["qualityMetrics"] = (
        (bundle["researchAnswerPack"].get("score") or {}).get("acceptanceMetrics")
        or {}
    )
    bundle["deliveryReady"] = delivery_ready
    bundle["recommendedNextAction"] = "use_reused_experience_pack" if delivery_ready else "refresh_research"
    return bundle


def _deterministic_web_research_architect_pack(
    *,
    question: str,
    source_matrix: list[dict[str, Any]],
    shards: list[dict[str, Any]],
    confidence: str,
    average_authority: float,
) -> dict[str, Any]:
    """Build the final research-result pack consumed by Supervisor and detail tools.

    This deterministic synthesis is intentionally source-backed and compact. It
    represents the Research Runtime's Web Research Architect handoff: the model
    sees conclusions and source URLs, while raw search/debug payloads stay behind
    rawRef/detail tooling.
    """

    fetched = _fetched_source_map(shards, question=question)
    source_urls: list[dict[str, Any]] = []
    findings: list[dict[str, Any]] = []
    claim_table: list[dict[str, Any]] = []
    missing_evidence: list[str] = []
    conflict_matrix: list[dict[str, Any]] = []
    source_texts: dict[str, str] = {}
    for source in source_matrix[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]:
        url = _safe_text(source.get("url"))
        if not url:
            continue
        read_payload = fetched.get(url) or {}
        title = _safe_text(read_payload.get("title") or source.get("title") or url)
        host = _safe_text(source.get("host")) or _host(url)
        source_id = _safe_text(source.get("sourceId")) or _source_id(url)
        temporal = _source_temporal_evidence(read_payload, source)
        source_text = (
            _safe_text(source.get("text"))
            or _safe_text(read_payload.get("text"))
            or _safe_text(read_payload.get("markdown"))
            or _safe_text(read_payload.get("textPreview"))
        )
        read_evidence = source.get("readEvidence") if isinstance(source.get("readEvidence"), dict) else {}
        if not read_evidence:
            read_evidence = _read_evidence_receipt(source_text, retrieved_at=temporal.get("retrievedAt"))
        source_entry = {
            "sourceId": source_id,
            "citationKey": _safe_text(source.get("citationKey")) or f"S{len(source_urls) + 1}",
            "title": title,
            "url": url,
            "host": host,
            "tier": source.get("tier"),
            "authorityScore": source.get("authorityScore"),
            "selectedForEvidence": True,
            "retrievedAt": temporal.get("retrievedAt"),
            "publishedAt": temporal.get("publishedAt"),
            "updatedAt": temporal.get("updatedAt"),
            "version": temporal.get("version"),
            "temporalEvidence": temporal,
            "researchFacetId": source.get("researchFacetId"),
            "researchFacetIds": list(source.get("researchFacetIds") or []),
            "researchFacetGoal": source.get("researchFacetGoal"),
            "evidenceViews": list(source.get("evidenceViews") or []),
            "evidenceQuery": source.get("evidenceQuery"),
            "contentChars": len(source_text),
            "readEvidence": read_evidence,
        }
        source_urls.append({key: value for key, value in source_entry.items() if value not in (None, "", [], {})})
        for alias in (source_id, url, source_entry.get("citationKey")):
            normalized_alias = _safe_text(alias)
            if normalized_alias:
                source_texts[normalized_alias] = source_text
                source_texts[normalized_alias.strip("[]")] = source_text
        if not source_text:
            missing_evidence.append(f"{title or url}: no readable body text")
        if len(findings) < _RESEARCH_ARCHITECT_PLAN_MAX_CLAIM_COUNT:
            for sentence in _research_sentences(source_text, limit=1):
                if not sentence:
                    continue
                finding = {
                    "claim": sentence,
                    "sourceTitle": title,
                    "sourceUrl": url,
                    "host": host,
                }
                findings.append(finding)
                claim_table.append(
                    {
                        "claim": sentence,
                        "supportingSources": [source_entry],
                        "refutingSources": [],
                        "confidence": source.get("tier") or "secondary",
                    }
                )
                lowered = sentence.lower()
                if any(token in lowered for token in ("deprecated", "not supported", "unsupported", "conflict", "不支持", "已弃用", "冲突")):
                    conflict_matrix.append(
                        {
                            "claim": sentence,
                            "kind": "possible_conflict_or_limitation",
                            "sources": [source_entry],
                        }
                    )
                break

    headline = "Readable evidence is prepared for independent Research review."
    limitations: list[str] = []
    critical_missing: list[str] = []
    if len(source_urls) < TARGET_RESEARCH_SOURCE_COUNT:
        critical_missing.append(
            f"Research should continue until at least {TARGET_RESEARCH_SOURCE_COUNT} readable sources are available; "
            f"{MIN_RESEARCH_SOURCE_COUNT} is only the rejection floor."
        )
    if not findings:
        critical_missing.append("No source-backed claims were extracted from readable page bodies.")
    if confidence == "low":
        limitations.append("Evidence authority is low; authoritative or primary sources should be added.")

    return {
        "kind": "research_result_pack",
        "architectAgentId": "web-research-architect",
        "architectName": "Web Research Architect",
        "synthesisMode": "deterministic_fallback",
        "reviewDecision": "retry",
        "reviewReasons": ["A model-backed Architect review is required before this evidence can be delivered."],
        "question": question,
        "headline": headline,
        "answer": "",
        "researchResult": "",
        "keyFindings": findings,
        "claimTable": claim_table,
        "conflictMatrix": conflict_matrix,
        "missingEvidence": missing_evidence,
        "criticalMissingEvidence": critical_missing,
        "recommendedNextQueries": [],
        "assumptions": [] if findings else ["Research result is provisional until readable sources are supplied."],
        "sourceUrls": source_urls,
        "_sourceTexts": source_texts,
        "asOf": _utc_now_iso(),
        "confidence": confidence,
        "authorityScore": average_authority,
        "limitations": limitations,
        "createdAt": _utc_now_iso(),
    }


def _research_architect_sources_for_prompt(
    source_matrix: list[dict[str, Any]],
    shards: list[dict[str, Any]],
    *,
    question: str = "",
    freshness: str = "auto",
) -> list[dict[str, Any]]:
    entity_hints = _catalog_official_entity_hints(question)
    if len(entity_hints) < 2:
        entity_hints = []
    fetched = _fetched_source_map(shards, question=question)
    fetched_by_shard = {
        _safe_text(shard.get("shardId")): _fetched_source_map(
            [shard],
            question=_safe_text(shard.get("evidenceQuery") or shard.get("query")) or question,
        )
        for shard in shards
        if _safe_text(shard.get("shardId"))
    }
    sources: list[dict[str, Any]] = []
    seen_source_urls: set[str] = set()

    def source_view_rows(source: dict[str, Any]) -> list[dict[str, Any]]:
        url = _safe_text(source.get("url"))
        declared_views = [
            view
            for view in list(source.get("evidenceViews") or [])
            if isinstance(view, dict)
        ]
        if not declared_views:
            declared_views = [
                {
                    "shardId": source.get("shardId"),
                    "researchFacetId": source.get("researchFacetId"),
                    "evidenceQuery": source.get("evidenceQuery") or question,
                }
            ]
        rows: list[dict[str, Any]] = []
        seen: set[tuple[str, str, str]] = set()
        for view in declared_views:
            shard_id = _safe_text(view.get("shardId"))
            facet_id = _safe_text(view.get("researchFacetId"))
            evidence_query = _safe_text(view.get("evidenceQuery")) or question
            key = (shard_id, facet_id, evidence_query)
            if key in seen:
                continue
            seen.add(key)
            shard_fetched = fetched_by_shard.get(shard_id) or {}
            # A declared facet view is valid only when its own shard retained
            # the focused read. Falling back to another shard for the same URL
            # silently turns (for example) an enforcement excerpt into alleged
            # open-source or timeline evidence during fixed-bundle replay.
            read_payload = (
                shard_fetched.get(url) or {}
                if shard_id
                else fetched.get(url) or {}
            )
            source_text = _safe_text(
                read_payload.get("text")
                or read_payload.get("markdown")
                or read_payload.get("textPreview")
            )
            if source_text:
                query_entity_indexes = _research_entity_indexes_in_text(
                    entity_hints,
                    evidence_query,
                )
                facet_entity_indexes = _research_entity_indexes_in_text(
                    entity_hints,
                    re.sub(r"[_-]+", " ", facet_id),
                )
                source_entity_indexes = _research_source_entity_indexes(
                    entity_hints,
                    host=source.get("host") or _host(url),
                    url=url,
                    title=read_payload.get("title") or source.get("title"),
                    text=source_text,
                    evidence_query=evidence_query,
                )
                required_entity_indexes = (
                    facet_entity_indexes or query_entity_indexes
                )
                if (
                    required_entity_indexes
                    and not required_entity_indexes.intersection(
                        source_entity_indexes
                    )
                ):
                    continue
                rows.append(
                    {
                        "shardId": shard_id,
                        "researchFacetId": facet_id,
                        "evidenceQuery": evidence_query,
                        "readPayload": read_payload,
                        "text": source_text,
                    }
                )
        return rows

    def merged_source_text(view_rows: list[dict[str, Any]]) -> str:
        if not view_rows:
            return ""
        if len(view_rows) == 1:
            return _safe_text(view_rows[0].get("text"))[:_RESEARCH_ARCHITECT_SOURCE_TEXT_CHARS]
        per_view_limit = max(
            1_600,
            _RESEARCH_ARCHITECT_SOURCE_TEXT_CHARS // len(view_rows),
        )
        chunks: list[str] = []
        for view in view_rows:
            header = "\n\n[Facet evidence"
            if _safe_text(view.get("researchFacetId")):
                header += f": {_safe_text(view.get('researchFacetId'))}"
            header += "]\n"
            excerpt = _research_source_excerpt(
                view.get("text"),
                view.get("evidenceQuery") or question,
                limit=max(MIN_RESEARCH_SOURCE_BODY_CHARS, per_view_limit - len(header)),
            )
            if excerpt:
                chunks.append(f"{header}{excerpt}")
        return "".join(chunks)[:_RESEARCH_ARCHITECT_SOURCE_TEXT_CHARS]

    question_requests_history = bool(
        re.search(
            r"\b(?:compare|history|historical|legacy|older|previous|rejected|superseded|withdrawn|"
            r"deprecat(?:e|ed|ion)|release schedule|version history|what changed)\b|"
            r"(?:历史|沿革|旧版|早期|遗留|已拒绝|已取代|已撤回|弃用|版本对比|变更记录)",
            question,
            re.IGNORECASE,
        )
    )

    def historical_only_source(*, title: str, url: str, text: str) -> bool:
        if not question or question_requests_history:
            return False
        head = f"{title}\n{url}\n{text[:5000]}"
        return bool(
            re.search(
                r"\bstatus\s*:?\s*(?:rejected|superseded|withdrawn)\b|"
                r"\b(?:python\s+)?\d+(?:\.\d+)?\s+release schedule\b",
                head,
                re.IGNORECASE,
            )
        )

    def prompt_rank(source: dict[str, Any]) -> tuple[int, int, int, float, int]:
        url = _safe_text(source.get("url"))
        views = source_view_rows(source)
        evidence_queries = [
            _safe_text(view.get("evidenceQuery")) or question for view in views
        ] or [_safe_text(source.get("evidenceQuery")) or question]
        read_payload = (
            dict(views[0].get("readPayload") or {})
            if views
            else fetched.get(url) or {}
        )
        title = _safe_text(read_payload.get("title") or source.get("title"))
        source_text = merged_source_text(views) or _safe_text(source.get("text"))
        direct_relevance = max(
            _source_relevance_score(
                evidence_query,
                title=title,
                snippet=url,
                text=source_text,
            )
            for evidence_query in evidence_queries
        )
        document_text = f"{title} {url}".lower()
        change_document = bool(
            re.search(r"\b(?:changelog|changes|release notes?|version history)\b", document_text)
            or "/whatsnew/" in document_text
        )
        question_requests_changes = bool(
            re.search(r"\b(?:change|changes|changelog|release notes?|version history|what'?s new)\b", question.lower())
        )
        detail_priority = -1 if change_document and not question_requests_changes else 0
        if any(
            marker in document_text
            for marker in ("/library/", "/api", "parameter-type", "entry-point", "specification", "argparse", "pathlib")
        ):
            detail_priority += 2
        quality_dimensions = (
            source.get("qualityDimensions")
            or (source.get("sourceQualityGate") or {}).get("qualityDimensions")
            or {}
        )
        return (
            direct_relevance,
            detail_priority,
            int(quality_dimensions.get("relevance") or 0),
            float(source.get("authorityScore") or 0),
            _research_document_priority(url),
        )

    prioritized_sources = sorted(
        source_matrix,
        key=prompt_rank,
        reverse=True,
    )
    for source in prioritized_sources:
        url = _safe_text(source.get("url"))
        if not url:
            continue
        source_identity = _research_document_identity(url, question=question)
        if source_identity in seen_source_urls:
            continue
        if not bool(source.get("selectedForEvidence") or (source.get("sourceQualityGate") or {}).get("selectedForEvidence")):
            continue
        declared_evidence_views = [
            view
            for view in list(source.get("evidenceViews") or [])
            if isinstance(view, dict)
        ]
        view_rows = source_view_rows(source)
        read_payload = (
            dict(view_rows[0].get("readPayload") or {})
            if view_rows
            else fetched.get(url) or {}
        )
        evidence_queries = list(
            dict.fromkeys(
                _safe_text(view.get("evidenceQuery")) or question
                for view in view_rows
            )
        ) or [_safe_text(source.get("evidenceQuery")) or question]
        facet_values = [
            _safe_text(view.get("researchFacetId"))
            for view in view_rows
            if _safe_text(view.get("researchFacetId"))
        ]
        if not declared_evidence_views:
            facet_values = [
                *list(source.get("researchFacetIds") or []),
                *facet_values,
                source.get("researchFacetId"),
            ]
        facet_ids = list(dict.fromkeys(facet_values))
        facet_ids = [_safe_text(value) for value in facet_ids if _safe_text(value)]
        evidence_query = evidence_queries[0] if len(evidence_queries) == 1 else question
        source_text = _safe_text(merged_source_text(view_rows))
        if read_payload.get("ok") is False or len(source_text) < MIN_RESEARCH_SOURCE_BODY_CHARS:
            continue
        if "About Press Copyright Contact us Creators" in source_text and "YouTube works" in source_text:
            continue
        source_title = _safe_text(read_payload.get("title") or source.get("title"))
        if historical_only_source(title=source_title, url=url, text=source_text):
            continue
        original_question_discovery_relevance = max(
            _source_relevance_score(
                query,
                title=source_title,
                snippet=url,
            )
            for query in evidence_queries
        )
        original_question_body_relevance = max(
            _source_relevance_score(query, text=source_text)
            for query in evidence_queries
        )
        if evidence_query and not (
            original_question_discovery_relevance > 0
            or original_question_body_relevance >= _source_minimum_body_relevance(evidence_query)
        ):
            continue
        seen_source_urls.add(source_identity)
        temporal = _source_temporal_evidence(read_payload, source)
        read_evidence = _read_evidence_receipt(source_text, retrieved_at=temporal.get("retrievedAt"))
        sources.append(
            {
                "sourceId": _safe_text(source.get("sourceId")) or _source_id(url),
                "citationKey": _safe_text(source.get("citationKey")),
                "title": _safe_text(source_title or url),
                "url": url,
                "host": _safe_text(source.get("host")) or _host(url),
                "tier": source.get("tier"),
                "authorityTier": source.get("authorityTier"),
                "authorityScore": source.get("authorityScore"),
                "catalogCategory": source.get("catalogCategory"),
                "catalogSourceId": source.get("catalogSourceId"),
                "runtimeOfficialSeed": bool(
                    _safe_text(source.get("shardId")).startswith("shard_catalog_seed_")
                    or any(
                        _safe_text(view.get("shardId")).startswith("shard_catalog_seed_")
                        for view in view_rows
                    )
                ),
                "qualityDimensions": (source.get("sourceQualityGate") or {}).get("qualityDimensions") or {},
                "freshness": source.get("freshness"),
                "provider": source.get("provider"),
                "networkRoute": source.get("networkRoute"),
                "shardId": source.get("shardId"),
                "researchFacetId": source.get("researchFacetId"),
                "researchFacetIds": facet_ids,
                "researchFacetGoal": source.get("researchFacetGoal"),
                "evidenceViews": [
                    {
                        "shardId": _safe_text(view.get("shardId")),
                        "researchFacetId": _safe_text(view.get("researchFacetId")),
                        "evidenceQuery": _safe_text(view.get("evidenceQuery")),
                    }
                    for view in view_rows
                ],
                "evidenceQueries": evidence_queries,
                "evidenceQuery": evidence_query,
                "selectedForEvidence": True,
                "contentChars": len(source_text),
                "originalContentChars": read_payload.get("originalContentChars") or len(source_text),
                "omittedChars": read_payload.get("omittedChars") or 0,
                "evidenceSelection": read_payload.get("evidenceSelection") or "read_body",
                "readEvidence": read_evidence,
                **temporal,
                "temporalEvidence": temporal,
                "text": source_text[:_RESEARCH_ARCHITECT_SOURCE_TEXT_CHARS],
            }
        )

    selected: list[dict[str, Any]] = []
    selected_urls: set[str] = set()

    def add_source(source: dict[str, Any]) -> None:
        url = _safe_text(source.get("url"))
        if (
            url
            and url not in selected_urls
            and len(selected) < _RESEARCH_ARCHITECT_MAX_SOURCE_COUNT
        ):
            selected.append(source)
            selected_urls.add(url)

    def source_has_catalog_seed_view(source: dict[str, Any]) -> bool:
        shard_ids = [
            source.get("shardId"),
            *[
                view.get("shardId")
                for view in list(source.get("evidenceViews") or [])
                if isinstance(view, dict)
            ],
        ]
        return any(
            _safe_text(shard_id).startswith("shard_catalog_seed_")
            for shard_id in shard_ids
        )

    structurally_relevant_sources = [
        source
        for source in sources
        if _architect_source_subject_focused(
            source,
            _safe_text(source.get("evidenceQuery")) or question,
        )
    ]

    def source_facet_ids(source: dict[str, Any]) -> set[str]:
        return {
            _safe_text(value)
            for value in [
                *list(source.get("researchFacetIds") or []),
                source.get("researchFacetId"),
                *[
                    view.get("researchFacetId")
                    for view in list(source.get("evidenceViews") or [])
                    if isinstance(view, dict)
                ],
            ]
            if _safe_text(value)
        }

    def source_entity_indexes(source: dict[str, Any]) -> set[int]:
        return _research_source_entity_indexes(
            entity_hints,
            host=source.get("host"),
            url=source.get("url"),
            title=source.get("title"),
            text=source.get("text"),
            evidence_query=source.get("evidenceQuery"),
        )

    # Catalog seeds are direct reads of runtime-curated official documents.
    # They have already passed the same read, relevance, and quality gates as
    # every other source above. Preserve those successful reads before the
    # finite prompt is filled by search-result ranking; otherwise a set of
    # near-duplicate high-ranked pages can evict the exact install or pricing
    # page the runtime deliberately selected for a named product.
    for source in sources:
        if source_has_catalog_seed_view(source):
            add_source(source)

    coverage_units: list[tuple[str, int | None]] = []
    seen_coverage_units: set[tuple[str, int | None]] = set()
    for goal, kind in _required_research_facet_queries(question, shards):
        facet_id = _research_facet_id(kind)
        if not facet_id:
            continue
        query_entities = sorted(
            _research_entity_indexes_in_text(entity_hints, goal)
        )
        units = [(facet_id, entity_index) for entity_index in query_entities] or [
            (facet_id, None)
        ]
        for unit in units:
            if unit not in seen_coverage_units:
                seen_coverage_units.add(unit)
                coverage_units.append(unit)

    # A multi-product comparison often has one combined pricing/privacy facet.
    # Reserving only one document for that facet lets the highest-ranked vendor
    # consume the entire finite prompt. Reserve an entity-specific document for
    # each (facet, named entity) pair when one was actually read; broad comparison
    # sources remain valid only when no entity-specific document is available.
    for facet_id, entity_index in coverage_units:
        if len(selected) >= _RESEARCH_ARCHITECT_MAX_SOURCE_COUNT:
            break
        if entity_index is not None and any(
            facet_id in source_facet_ids(source)
            and source_entity_indexes(source) == {entity_index}
            for source in selected
        ):
            continue
        if entity_index is None and any(
            facet_id in source_facet_ids(source) for source in selected
        ):
            continue
        candidates = [
            source
            for source in structurally_relevant_sources
            if _safe_text(source.get("url")) not in selected_urls
            and facet_id in source_facet_ids(source)
            and (
                entity_index is None
                or entity_index in source_entity_indexes(source)
            )
        ]
        if not candidates:
            continue
        if entity_index is not None:
            entity_specific = [
                source
                for source in candidates
                if source_entity_indexes(source) == {entity_index}
            ]
            if entity_specific:
                candidates = entity_specific
            elif any(
                facet_id in source_facet_ids(source)
                and entity_index in source_entity_indexes(source)
                for source in selected
            ):
                continue
        add_source(max(candidates, key=prompt_rank))

    if research_requires_dated_sources({"question": question, "freshness": freshness}):
        for source in structurally_relevant_sources:
            if research_source_has_dated_evidence(source):
                add_source(source)
                # Keep one explicit document-date anchor when available, but
                # do not let an arbitrary dated-source quota displace more
                # relevant stable or version-bound material.  The semantic
                # reviewers receive all time metadata and decide adequacy.
                break

    selected_hosts = {
        host
        for source in selected
        if (
            host := (_safe_text(source.get("host")) or _host(_safe_text(source.get("url"))))
            .lower()
            .removeprefix("www.")
        )
    }
    for source in structurally_relevant_sources:
        host = (_safe_text(source.get("host")) or _host(_safe_text(source.get("url")))).lower().removeprefix("www.")
        if host and host not in selected_hosts:
            add_source(source)
            selected_hosts.add(host)
            if len(selected_hosts) >= TARGET_RESEARCH_DISTINCT_HOST_COUNT:
                break

    for source in structurally_relevant_sources:
        add_source(source)
        if len(selected) >= _RESEARCH_ARCHITECT_MAX_SOURCE_COUNT:
            break

    for source in sources:
        add_source(source)
        if len(selected) >= _RESEARCH_ARCHITECT_MAX_SOURCE_COUNT:
            break

    # Structural time/host targets decide which documents enter the prompt,
    # but they must not decide which documents the Architect sees first.
    # Re-rank the selected set by direct topical value so dated changelogs or
    # release indexes cannot displace the evidence that actually answers the
    # question from the mandatory claim-coverage prefix.
    selected.sort(key=prompt_rank, reverse=True)

    return [
        {**source, "citationKey": _safe_text(source.get("citationKey")) or f"S{index}"}
        for index, source in enumerate(selected, start=1)
    ]


def _extract_json_object(text: str) -> dict[str, Any] | None:
    raw = _safe_text(text)
    if not raw:
        return None
    fenced_bodies = re.findall(r"```(?:json)?\s*(.*?)\s*```", raw, re.DOTALL | re.IGNORECASE)
    candidates = [*fenced_bodies, raw]
    decoder = json.JSONDecoder()
    parsed_candidates: list[dict[str, Any]] = []
    for candidate in candidates:
        stripped = candidate.lstrip()
        if stripped.startswith("{"):
            try:
                payload, _end = decoder.raw_decode(stripped)
            except Exception:
                # If an answer starts as JSON but the outer object is truncated,
                # never salvage a nested claim/support object as the response.
                continue
            if isinstance(payload, dict):
                parsed_candidates.append(payload)
            continue
        for match in re.finditer(r"\{", candidate):
            try:
                payload, _end = decoder.raw_decode(candidate[match.start() :])
            except Exception:
                continue
            if isinstance(payload, dict):
                parsed_candidates.append(payload)
    if parsed_candidates:
        expected_keys = {
            "reviewDecision",
            "accept",
            "claimTable",
            "answerOutline",
            "questionCoverage",
            "claimEntailment",
            "freshnessAdequacy",
        }
        return max(
            parsed_candidates,
            key=lambda payload: (
                len(expected_keys.intersection(payload)),
                len(json.dumps(payload, ensure_ascii=False, default=str)),
            ),
        )
    for candidate in candidates:
        try:
            payload = json.loads(candidate)
        except Exception:
            try:
                import yaml  # type: ignore

                payload = yaml.safe_load(candidate)
            except Exception:
                continue
        if isinstance(payload, dict):
            return payload
    return None


def _normalized_evidence_text(value: Any) -> str:
    text = re.sub(r"\\([_*`])", r"\1", _safe_text(value))
    text = re.sub(
        r"(?<!\w)_([A-Za-z][A-Za-z0-9_]*(?:\s*=\s*[A-Za-z0-9.]+)?)_(?!\w)",
        r"\1",
        text,
    )
    return re.sub(r"\s+", " ", text).strip().lower()


def _architect_plan_decision(value: Any, *, allow_runtime_validation_candidate: bool = False) -> str:
    if not isinstance(value, dict):
        return ""
    decision = _safe_text(value.get("reviewDecision")).lower()
    if decision in {"accept", "retry", "reject"}:
        return decision
    if (
        allow_runtime_validation_candidate
        and isinstance(value.get("claimTable"), list)
        and bool(value.get("claimTable"))
    ):
        # Providers vary on repair-status labels (revise, revised, passed, or
        # omitted). Treat the payload only as a candidate; deterministic
        # evidence gates below still decide whether it is accepted.
        return "accept"
    if isinstance(value.get("accept"), bool):
        return "accept" if value.get("accept") is True else "retry"
    return ""


def _architect_complete_query_excerpt_text(value: Any) -> str:
    """Remove partial flanks introduced by query-focused reader compaction."""

    marker = "[... query-focused excerpt ...]"
    text = _safe_text(value)
    if marker not in text:
        return text
    pieces = text.split(marker)
    cleaned: list[str] = []
    strong_boundary = re.compile(
        r"(?<!\s)[.!?。！？](?=\s|$)|^#{1,6}\s+|^```\s*$",
        re.MULTILINE,
    )
    for index, raw_piece in enumerate(pieces):
        piece = raw_piece.strip()
        if not piece:
            continue
        if index > 0:
            heading = re.search(r"(?m)^#{1,6}\s+", piece[:1_200])
            if heading:
                piece = piece[heading.start() :]
            else:
                leading_boundary = re.search(
                    r"(?<!\s)[.!?。！？]\s+(?=[A-Z\u4e00-\u9fff])",
                    piece[:800],
                )
                if leading_boundary:
                    piece = piece[leading_boundary.end() :]
        boundaries = list(strong_boundary.finditer(piece))
        if boundaries:
            tail = piece[boundaries[-1].end() :].strip()
            if tail and len(tail) <= 600:
                piece = piece[: boundaries[-1].end()]
        piece = piece.strip()
        if piece:
            cleaned.append(piece)
    return "\n\n".join(cleaned)


def _architect_evidence_fragment_is_truncated(value: Any) -> bool:
    text = _safe_text(value)
    if not text:
        return True
    return bool(
        re.match(
            r"^(?:and|or|but|through|general entry\b|occurs\b|direction\s*[;:,])",
            text,
        )
        or
        re.match(
            r"^[a-z]{1,3}\s+(?:also|argument|default|function|module|perform|support|validation)\b",
            text,
        )
        or re.search(
            r"(?:…|\.\.\.)\s*(?:\d+\s+tokens?\s+truncated|\[TRUNCATED\])",
            text,
            re.IGNORECASE,
        )
        # Query-focused readers can end a window immediately after a
        # coordinating word.  Treat that as an incomplete evidence unit so a
        # nearby complete paragraph can win the citation slot instead.
        or re.search(
            r"(?:\b(?:and|or|but|with|including|such\s+as|based\s+on|according\s+to|via)\s*|"
            r"(?:以及|包括|例如|根据|通过|与|和)\s*)$",
            text,
            re.IGNORECASE,
        )
    )


def _architect_evidence_candidates(source: dict[str, Any], question: str, *, limit: int = 4) -> list[dict[str, Any]]:
    citation_key = _safe_text(source.get("citationKey"))
    text = _architect_complete_query_excerpt_text(source.get("text"))
    # A source is often read for a narrow facet (for example Windows setup or
    # account pricing) while the root question contains many other facets.
    # Keep the root question for entity relevance, but add the source's own
    # routing query so the finite excerpt budget is spent on the requested
    # dimension instead of a generic page paragraph.
    source_focus = " ".join(
        value
        for value in (
            _safe_text(source.get("evidenceQuery")),
            _safe_text(source.get("researchFacetGoal")),
            _safe_text(source.get("researchFacetId")),
        )
        if value
    )
    ranking_question = " ".join(value for value in (question, source_focus) if value).strip()
    # Multi-facet long-document projection adds routing headers and repeats the
    # document title at the start of every focused excerpt. Those labels help
    # Runtime lineage but are not evidence; retaining them lets query words in
    # the facet id/title outrank the operative clause that follows.
    source_title = re.sub(r"\s+", " ", _safe_text(source.get("title"))).strip()
    title_prefix = _normalized_evidence_text(source_title[:120])
    cleaned_lines: list[str] = []
    for line in text.splitlines():
        stripped = line.strip()
        if re.fullmatch(
            r"\[Facet evidence(?:\s*:\s*[^\]]+)?\]",
            stripped,
            re.IGNORECASE,
        ):
            continue
        if stripped.startswith("#") and title_prefix:
            heading = _normalized_evidence_text(stripped.lstrip("# "))
            if heading.startswith(title_prefix) or title_prefix.startswith(heading[:80]):
                continue
        cleaned_lines.append(line)
    text = "\n".join(cleaned_lines)
    structured_query_anchors = [
        re.sub(r"\s+", " ", match.group(0)).strip()
        for match in re.finditer(
            r"\b(?:article|annex|chapter)\s+"
            r"(?:\d+[a-z]?(?:\s*[-–—]\s*\d+[a-z]?)?|[ivxlcdm]+)"
            r"(?=$|[^a-z0-9])",
            _safe_text(question),
            re.IGNORECASE,
        )
    ]
    ranked: list[tuple[int, int, int, int, int, str]] = []
    identity_text = " ".join(
        (
            _safe_text(source.get("title")),
            _safe_text(source.get("url") or source.get("sourceUrl")),
        )
    )
    framework_path_context = bool(
        re.search(r"\b(?:argparse|click|typer)\b", identity_text, re.IGNORECASE)
        and re.search(
            r"\b(?:cli|command[- ]line|pathlib|filesystem|file[- ]system|path)\b|(?:命令行|路径|文件系统)",
            question,
            re.IGNORECASE,
        )
    )
    focus_patterns: list[str] = []
    if re.search(
        r"\b(?:windows|powershell|wsl|install(?:ation|er|s|ed|ing)?|setup|runtime|"
        r"prerequisites?|node(?:\.js)?|npm|npx|winget|system\s+requirements?)\b|"
        r"(?:安装|运行|前置条件|系统要求)",
        ranking_question,
        re.IGNORECASE,
    ):
        focus_patterns.extend(
            [
                r"\bwindows\b|\bpowershell\b|\bwsl\b",
                r"\b(?:install|setup|runtime|prerequisite|system\s+requirements?)\b|(?:安装|运行|前置条件|系统要求)",
                r"\b(?:node(?:\.js)?|npm|npx|winget|choco|scoop)\b",
            ]
        )
    if re.search(
        r"\b(?:account|subscription|pricing|quota|cost|billing|free\s+tier|seat)\b|"
        r"(?:账号|订阅|价格|配额|费用|计费|免费|席位)",
        ranking_question,
        re.IGNORECASE,
    ):
        focus_patterns.append(
            r"\b(?:account|subscription|pricing|quota|cost|billing|free|seat|tier|limit)\b|"
            r"(?:账号|订阅|价格|配额|费用|计费|免费|席位|档位|限制)"
        )
    if re.search(
        r"\b(?:privacy|data|telemetry|retention|training|security|approval)\b|"
        r"(?:隐私|数据|遥测|留存|训练|安全|审批)",
        ranking_question,
        re.IGNORECASE,
    ):
        focus_patterns.append(
            r"\b(?:privacy|data|telemetry|retention|training|security|approval)\b|"
            r"(?:隐私|数据|遥测|留存|训练|安全|审批)"
        )
    command_segment_pattern = re.compile(
        r"(?im)^\s*(?:[-*]\s*)?(?:winget|npm|npx|pnpm|yarn|brew|choco|scoop|"
        r"irm|curl|wget|pipx?|uv|gemini|claude|codex|docker)\b"
    )
    seen: set[str] = set()
    position = 0
    atomic_segments: list[str] = []
    for paragraph in re.split(r"\n{2,}", text):
        # Keep complete fenced blocks atomic, but do not treat a paragraph
        # containing several fences as one giant unit. That old behavior made
        # a 600-character window end at the opening fence of the next example,
        # exposing fragments such as ``parser = argparse .``. Oversized code
        # blocks are represented by the exact call-expression seeds below.
        if "```" in paragraph:
            for part in re.split(r"(```[\s\S]*?```)", paragraph):
                part = part.strip()
                if not part:
                    continue
                if part.startswith("```") and part.endswith("```"):
                    if len(part) <= 600:
                        atomic_segments.append(part)
                    continue
                atomic_segments.extend(
                    re.split(r"(?<=[。！？!?])\s+|(?<=[^\s]\.)\s+", part)
                )
        else:
            atomic_segments.extend(
                re.split(r"(?<=[。！？!?])\s+|(?<=[^\s]\.)\s+", paragraph)
            )

    # Reader compaction can leave a large mixed prose/code paragraph even
    # though Markdown headings remain intact.  Seed complete heading-scoped
    # windows and complete call expressions before the generic chunker so a
    # high-scoring excerpt never has to begin halfway through a sentence or
    # identifier merely to reach the relevant contract.
    structural_segments: list[str] = []
    headings = list(re.finditer(r"(?m)^#{1,6}\s+[^\r\n]+", text))
    for heading_index, heading in enumerate(headings):
        section_end = (
            headings[heading_index + 1].start()
            if heading_index + 1 < len(headings)
            else len(text)
        )
        raw_section = text[heading.start() : section_end]
        # A heading-scoped prose contract is useful evidence; an arbitrarily
        # truncated code example is not. Full short code blocks and exact calls
        # are seeded independently.
        prose_section = raw_section.split("```", 1)[0]
        section = re.sub(r"\s+", " ", prose_section).strip()
        if len(section) < 20:
            continue
        if len(section) > 600:
            complete_boundaries = list(
                re.finditer(
                    r"(?<!\s)[.!?。！？](?=\s+[A-Z\u4e00-\u9fff#]|$)",
                    section[:600],
                )
            )
            end = (
                complete_boundaries[-1].end()
                if complete_boundaries and complete_boundaries[-1].end() >= 360
                else section.rfind(" ", 360, 600)
            )
            section = section[: end if end > 0 else 600].rstrip()
        structural_segments.append(section)
    for call in re.finditer(
        r"(?:[A-Za-z_][A-Za-z0-9_]*\s*\.\s*)+"
        r"[A-Za-z_][A-Za-z0-9_]*\s*\([^\r\n)]{0,520}\)",
        text,
    ):
        structural_segments.append(call.group(0))
    # Markdown readers frequently return all installation commands in one
    # oversized fenced block.  The old block splitter intentionally dropped
    # such blocks, which removed the exact WinGet/npm/PowerShell command even
    # though it was present in the verified page body.  Re-seed complete
    # single-line commands as independent evidence units.
    for command_match in re.finditer(
        r"(?im)(?:^|[\n`])\s*(?:[-*]\s*)?"
        r"(?:winget|npm|npx|pnpm|yarn|brew|choco|scoop|irm|curl|wget|"
        r"pipx?|uv|gemini|claude|codex|docker)\b[^\r\n`]{0,560}",
        text,
    ):
        command = re.sub(r"\s+", " ", command_match.group(0)).strip(" `-•")
        if len(command) >= 8:
            structural_segments.append(command)
    atomic_segments = structural_segments + atomic_segments

    # Keep a parameter contract together with short continuation sentences
    # that define its default, conditional behavior, or intended conversion.
    # Without this, a provider/reader that emits one sentence per fragment can
    # preserve ``path_type converts ...`` while silently dropping the adjacent
    # ``If None ...`` and ``Useful to ...`` qualifiers from the evidence chain.
    raw_segments: list[str] = []
    contract_anchor = re.compile(
        r"\b(?:path_type|exists|file_okay|dir_okay|readable|writable|"
        r"resolve_path|allow_dash|executable|get_app_dir)\b",
        re.IGNORECASE,
    )
    continuation_lead = re.compile(
        r"^\s*(?:If\b|When\b|Otherwise\b|By default\b|The default\b|"
        r"Useful\b|This\b|It\b|若|如果|当|默认|否则|用于)",
        re.IGNORECASE,
    )
    index = 0
    while index < len(atomic_segments):
        segment = atomic_segments[index]
        combined = segment
        next_index = index + 1
        if contract_anchor.search(segment):
            appended = 0
            while (
                next_index < len(atomic_segments)
                and appended < 2
                and continuation_lead.search(atomic_segments[next_index])
                and len(combined) + 1 + len(atomic_segments[next_index]) <= 600
            ):
                combined = f"{combined} {atomic_segments[next_index]}"
                next_index += 1
                appended += 1
        raw_segments.append(combined)
        index = next_index if next_index > index + 1 else index + 1
    for raw_segment in raw_segments:
        normalized_segment = re.sub(r"\s+", " ", raw_segment).strip(" \t\r\n-•。；;")
        if len(normalized_segment) < 20:
            continue
        start = 0
        while start < len(normalized_segment):
            end = min(len(normalized_segment), start + 600)
            if end < len(normalized_segment):
                window = normalized_segment[start + 260 : end]
                strong_boundaries = list(
                    re.finditer(
                        r"(?<!\s)[.!?。！？](?=\s+[A-Z\u4e00-\u9fff#])|```",
                        window,
                    )
                )
                if strong_boundaries:
                    end = start + 260 + strong_boundaries[-1].end()
                else:
                    boundary = normalized_segment.rfind(" ", start + 320, end)
                    if boundary > start:
                        end = boundary + 1
            segment = normalized_segment[start:end].strip()
            normalized = _normalized_evidence_text(segment)
            focused_context_allowed = bool(
                focus_patterns
                and len(segment) >= 80
                and any(re.search(pattern, segment, re.IGNORECASE) for pattern in focus_patterns)
            )
            if (
                len(normalized) >= 20
                and normalized not in seen
                and (
                    not _is_noisy_research_sentence(segment)
                    or focused_context_allowed
                )
                and not _architect_evidence_fragment_is_truncated(segment)
            ):
                seen.add(normalized)
                relevance = _source_relevance_score(ranking_question, text=segment)
                matched_query_terms: list[tuple[str, int]] = []
                for term in _source_relevance_terms(ranking_question):
                    match = (
                        re.search(
                            rf"(?<![a-z0-9]){re.escape(term)}(?![a-z0-9])",
                            segment,
                            re.IGNORECASE,
                        )
                        if re.fullmatch(r"[a-z0-9_.-]+", term)
                        else re.search(re.escape(term), segment, re.IGNORECASE)
                    )
                    if match:
                        matched_query_terms.append((term, match.start()))
                relevance += sum(
                    min(len(term), 12) for term, _position in matched_query_terms
                )
                discriminative_positions = [
                    (query_index, position)
                    for query_index, (term, position) in enumerate(matched_query_terms)
                    if len(term) >= 4
                    and term not in _RESEARCH_ROOT_SUBJECT_GENERIC_TERMS
                ]
                if any(
                    left_position < right_position
                    and 0 < right_position - left_position <= 160
                    and left_query_index < right_query_index
                    for index, (left_query_index, left_position) in enumerate(
                        discriminative_positions
                    )
                    for right_query_index, right_position in discriminative_positions[
                        index + 1 :
                    ]
                ):
                    relevance += 40
                focus_hits = sum(
                    1
                    for pattern in focus_patterns
                    if re.search(pattern, segment, re.IGNORECASE)
                )
                if focus_hits:
                    relevance += min(120, focus_hits * 35)
                if command_segment_pattern.search(segment):
                    # Commands are easy to lose when a page uses one large
                    # fenced block. Prefer a complete command over a nearby
                    # navigation fragment when the source is being used for
                    # setup evidence.
                    relevance += 90 if focus_patterns else 25
                structured_anchor_hits = sum(
                    1
                    for anchor in structured_query_anchors
                    if re.search(
                        re.escape(anchor).replace(r"\ ", r"[\s\u00a0]+"),
                        segment,
                        re.IGNORECASE,
                    )
                )
                if structured_anchor_hits:
                    relevance += 100 * structured_anchor_hits
                navigation_entry_count = len(
                    re.findall(
                        r"\b(?:article|recital|section|chapter)\s+"
                        r"(?:\d+[a-z]?|[ivxlcdm]+)\s*:",
                        segment,
                        re.IGNORECASE,
                    )
                )
                if navigation_entry_count >= 3:
                    # Regulation and documentation pages often repeat a long
                    # table of contents before the operative body. Query terms
                    # and structured anchors make that index look highly
                    # relevant even though it contains no answerable fact.
                    relevance -= min(260, navigation_entry_count * 45)
                if re.search(
                    r"\b(?:shall|must|required|appl(?:y|ies)|takes?\s+effect|"
                    r"enters?\s+into\s+force|exempt(?:s|ed|ion)?|exception)\b|"
                    r"(?:应当|必须|不得|不适用|适用|生效|豁免|例外)",
                    segment,
                    re.IGNORECASE,
                ):
                    relevance += 35
                query_anchor_score = structured_anchor_hits + sum(
                    1
                for term in _source_relevance_terms(ranking_question)
                    if re.fullmatch(r"[a-z][a-z0-9_]*", term)
                    and re.search(rf"\b{re.escape(term)}\s*\.\s*[a-z_]", segment, re.IGNORECASE)
                )
                contract_anchor_score = sum(
                    1
                    for pattern in (
                        r"\btype\s*=\s*pathlib\s*\.\s*Path\b",
                        r"\bpath_type\b",
                        r"\bexists\b",
                        r"\bfile_okay\b",
                        r"\bdir_okay\b",
                        r"\bresolve_path\b",
                        r"\bget_app_dir\b",
                        r"\bapplication\s+directory\b",
                        r"\b(?:os\.)?PathLike\b",
                        r"\b(?:os\.)?fspath\b|__fspath__",
                    )
                    if re.search(pattern, segment, re.IGNORECASE)
                )
                if relevance <= 0 and framework_path_context and contract_anchor_score:
                    # The query terms often occur in the preceding signature,
                    # while the immediately following chunk contains the exact
                    # parameter contract. Keep that contract as contextual
                    # evidence from the same already-read framework page.
                    relevance = 25
                ranked.append(
                    (
                        relevance,
                        query_anchor_score,
                        contract_anchor_score,
                        min(len(segment), 600),
                        -position,
                        segment,
                    )
                )
                position += 1
            if end >= len(normalized_segment):
                break
            start = max(start + 1, end)
    ranked.sort(key=lambda item: (item[0], item[1], item[2], item[4], item[3]), reverse=True)
    minimum_relevance = _source_minimum_body_relevance(ranking_question)
    focused_ranked = [item for item in ranked if item[0] >= minimum_relevance]
    if focused_ranked:
        # A single perfect-scoring chunk often contains the query terms while
        # adjacent chunks hold the actual parameter contract or example. Keep
        # the focused head, then fill the remaining evidence slots with the
        # next relevant chunks from the same already-read document.
        selected_ranked = list(focused_ranked)
        selected_ranked.extend(
            item
            for item in ranked
            if item not in selected_ranked and item[0] > 0
        )
    else:
        selected_ranked = [item for item in ranked if item[0] > 0] or ranked
    selected_limit = max(1, limit)
    selected_head = list(selected_ranked[:selected_limit])
    if framework_path_context and selected_head:
        selected_order = {id(item): index for index, item in enumerate(selected_ranked)}
        contract_candidates = [item for item in selected_ranked if item[2] > 0]
        if contract_candidates:
            # Query-term density alone otherwise lets generic examples consume
            # all four per-source slots while the adjacent parameter contract
            # (validation/default/conversion semantics) is dropped. Reserve
            # one slot for the strongest complete contract-bearing segment.
            strongest_contract = max(
                contract_candidates,
                key=lambda item: (
                    item[2],
                    1
                    if re.search(
                        r"(?:^#{1,6}\s+|\b(?:validation|validations|convert|default|parameter)\b)",
                        item[5],
                        re.IGNORECASE,
                    )
                    else 0,
                    item[0],
                    item[3],
                    item[4],
                ),
            )
            if strongest_contract not in selected_head:
                selected_head[-1] = strongest_contract
            selected_head.sort(key=lambda item: selected_order[id(item)])
    if focus_patterns and selected_head:
        # Setup/pricing/privacy commands need one adjacent explanatory unit as
        # well. Otherwise a long install block can consume every slot and the
        # writer cannot tell whether the command is native Windows, WSL-only,
        # subscription-gated, or merely an example.
        contextual_candidates = [
            item
            for item in ranked
            if not command_segment_pattern.search(item[5])
            and any(re.search(pattern, item[5], re.IGNORECASE) for pattern in focus_patterns)
        ]
        if contextual_candidates and not any(
            not command_segment_pattern.search(item[5])
            and any(re.search(pattern, item[5], re.IGNORECASE) for pattern in focus_patterns)
            for item in selected_head
        ):
            selected_head[-1] = max(
                contextual_candidates,
                key=lambda item: (
                    1
                    if re.search(
                        r"\bnative\s+windows\b|\bwsl\s*[12]\b|\bwindows\s+server\b",
                        item[5],
                        re.IGNORECASE,
                    )
                    else 0,
                    item[0],
                    item[3],
                    item[4],
                ),
            )
        windows_setup_focus = bool(
            re.search(r"\bwindows\b|Windows", ranking_question, re.IGNORECASE)
            and re.search(
                r"\b(?:install|setup|powershell|winget|runtime)\b|(?:安装|运行)",
                ranking_question,
                re.IGNORECASE,
            )
        )
        windows_commands = [
            item
            for item in selected_ranked
            if command_segment_pattern.search(item[5])
            and re.search(
                r"^\s*(?:winget\s+(?:install|upgrade|uninstall)\b|irm\b|"
                r"powershell\b|pwsh\b)|install\.ps1|\.exe(?:\s|$)",
                item[5],
                re.IGNORECASE,
            )
        ]
        if windows_setup_focus and windows_commands:
            strongest_windows_command = max(
                windows_commands,
                key=lambda item: (
                    3
                    if re.search(r"^\s*winget\s+install\b", item[5], re.IGNORECASE)
                    else 2
                    if re.search(r"^\s*winget\b", item[5], re.IGNORECASE)
                    else 1,
                    item[0],
                    item[3],
                    item[4],
                ),
            )
            if strongest_windows_command not in selected_head:
                replaceable_command_indexes = [
                    index
                    for index, item in enumerate(selected_head)
                    if command_segment_pattern.search(item[5])
                    and item not in windows_commands
                ]
                if replaceable_command_indexes:
                    selected_head[replaceable_command_indexes[-1]] = strongest_windows_command
    selected_ranked = selected_head
    return [
        {
            "evidenceExcerptKey": f"{citation_key}:E{index}",
            "text": segment,
            # Keep the public score on its historical 0-100 semantic scale.
            # Extra term/proximity weight above is an internal ranking signal.
            "relevanceScore": _source_relevance_score(question, text=segment),
        }
        for index, (relevance, _anchor_score, _contract_score, _length, _position, segment) in enumerate(
            selected_ranked, start=1
        )
    ]


def _architect_multi_query_evidence_candidates(
    source: dict[str, Any],
    evidence_queries: list[str],
    *,
    facet_by_query: dict[str, str] | None = None,
) -> list[dict[str, Any]]:
    """Reserve evidence capacity across every query projected onto one document."""

    queries = list(dict.fromkeys(_safe_text(value) for value in evidence_queries if _safe_text(value)))
    if not queries:
        return []
    candidate_budget = min(
        _RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
        max(
            _RESEARCH_ARCHITECT_EVIDENCE_CANDIDATE_COUNT,
            len(queries) * 2,
        ),
    )
    per_query_limit = min(
        _RESEARCH_ARCHITECT_EVIDENCE_CANDIDATE_COUNT,
        max(2, ((candidate_budget + len(queries) - 1) // len(queries)) + 1),
    )
    candidates_by_query = {
        query: _architect_evidence_candidates(source, query, limit=per_query_limit)
        for query in queries
    }
    selected: list[dict[str, Any]] = []
    seen_text: set[str] = set()
    max_depth = max((len(items) for items in candidates_by_query.values()), default=0)
    for depth in range(max_depth):
        for query in queries:
            rows = candidates_by_query.get(query) or []
            if depth >= len(rows):
                continue
            candidate = rows[depth]
            normalized_text = _normalized_evidence_text(candidate.get("text"))
            if not normalized_text or normalized_text in seen_text:
                continue
            seen_text.add(normalized_text)
            facet_id = (facet_by_query or {}).get(query) or None
            selected.append(
                {
                    **candidate,
                    "researchFacetId": facet_id,
                    "researchFacetGoal": query if facet_id else None,
                }
            )
            if len(selected) >= candidate_budget:
                return selected
    return selected


def _architect_facet_focused_prompt_sources(
    sources: list[dict[str, Any]],
    required_facet_ids: list[str],
    *,
    candidates_per_facet: int = 2,
    extra_source_count: int = 2,
) -> list[dict[str, Any]]:
    """Compact a large comparison without dropping any explicit facet."""

    required = {
        _safe_text(facet_id)
        for facet_id in required_facet_ids
        if _safe_text(facet_id)
    }
    if len(required) <= _RESEARCH_ARCHITECT_PLAN_MAX_CLAIM_COUNT:
        return [dict(source) for source in sources]

    per_facet_limit = max(1, int(candidates_per_facet or 0))
    focused: list[dict[str, Any]] = []
    extras: list[dict[str, Any]] = []
    covered: set[str] = set()
    for source in sources:
        candidates = [
            candidate
            for candidate in list(source.get("evidenceCandidates") or [])
            if isinstance(candidate, dict)
        ]
        selected_candidates: list[dict[str, Any]] = []
        facet_counts: dict[str, int] = {}
        for candidate in candidates:
            facet_id = _safe_text(candidate.get("researchFacetId"))
            if facet_id not in required:
                continue
            if facet_counts.get(facet_id, 0) >= per_facet_limit:
                continue
            selected_candidates.append(candidate)
            facet_counts[facet_id] = facet_counts.get(facet_id, 0) + 1
            covered.add(facet_id)
        if selected_candidates:
            focused.append(
                {
                    **source,
                    "evidenceCandidates": selected_candidates,
                    "answerabilityScore": max(
                        int(candidate.get("relevanceScore") or 0)
                        for candidate in selected_candidates
                    ),
                }
            )
        elif candidates:
            extras.append(
                {
                    **source,
                    "evidenceCandidates": candidates[:per_facet_limit],
                    "answerabilityScore": max(
                        int(candidate.get("relevanceScore") or 0)
                        for candidate in candidates[:per_facet_limit]
                    ),
                }
            )

    # Structural coverage was checked before full synthesis. If a malformed
    # legacy source loses its facet tag here, retain the original prompt rather
    # than manufacturing a false evidence gap.
    if not required.issubset(covered):
        return [dict(source) for source in sources]

    extras.sort(
        key=lambda source: (
            1
            if _safe_text(source.get("tier")).lower() in {"secondary", "community"}
            else 0,
            int(source.get("answerabilityScore") or 0),
            int(source.get("authorityScore") or 0),
        ),
        reverse=True,
    )
    return [
        *focused,
        *extras[: max(0, int(extra_source_count or 0))],
    ][:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]


def _architect_required_claim_source_keys(
    sources: list[dict[str, Any]],
    *,
    limit: int,
    question: str = "",
) -> list[str]:
    tier_rank = {"primary": 3, "secondary": 2, "community": 1}
    entity_hints = _catalog_official_entity_hints(question)
    if len(entity_hints) < 2:
        entity_hints = []
    require_decision_fact = bool(
        entity_hints and _research_query_dimension_classes(question)
    )

    def can_emit_decision_fact(source: dict[str, Any]) -> bool:
        if not require_decision_fact:
            return True
        if not any(
            _safe_text(candidate.get("evidenceExcerptKey"))
            for candidate in list(source.get("evidenceCandidates") or [])
            if isinstance(candidate, dict)
        ):
            # Metadata-only callers and unit fixtures do not carry the staged
            # excerpt keys needed by exact verification.
            return True
        return bool(_architect_exact_excerpt_source_fact(source, question))

    eligible = [
        source
        for source in sources
        if (
            _safe_text(source.get("citationKey"))
            and list(source.get("evidenceCandidates") or [])
            and int(source.get("answerabilityScore") or 0) > 0
            and source.get("subjectFocused") is not False
            and can_emit_decision_fact(source)
        )
    ]
    ranked = sorted(
        enumerate(eligible),
        key=lambda pair: (
            int(pair[1].get("answerabilityScore") or 0),
            tier_rank.get(_safe_text(pair[1].get("tier")).lower(), 0),
            1 if research_source_has_dated_evidence(pair[1]) else 0,
            int(pair[1].get("authorityScore") or 0),
            -pair[0],
        ),
        reverse=True,
    )
    bounded_limit = max(0, int(limit or 0))
    ranked_sources = [source for _index, source in ranked]
    selected: list[dict[str, Any]] = []

    def source_entity_indexes(source: dict[str, Any]) -> set[int]:
        return _research_source_entity_indexes(
            entity_hints,
            host=_host(_safe_text(source.get("url"))),
            url=source.get("url"),
            title=source.get("title"),
            text="\n".join(
                _safe_text(candidate.get("text"))
                for candidate in list(source.get("evidenceCandidates") or [])
                if isinstance(candidate, dict)
            ),
            evidence_query=source.get("evidenceQuery"),
        )

    def source_dimension_classes(source: dict[str, Any]) -> set[str]:
        facet_ids = {
            _safe_text(value).lower()
            for value in [
                *list(source.get("researchFacetIds") or []),
                source.get("researchFacetId"),
                *[
                    candidate.get("researchFacetId")
                    for candidate in list(source.get("evidenceCandidates") or [])
                    if isinstance(candidate, dict)
                ],
            ]
            if _safe_text(value)
        }
        identity = " ".join(
            (
                _safe_text(source.get("title")),
                _safe_text(source.get("url")),
                _safe_text(source.get("researchFacetGoal")),
                _safe_text(source.get("evidenceQuery")),
                " ".join(facet_ids),
            )
        )
        return _research_query_dimension_families(identity)

    # Reserve product x dimension evidence before ordinary score ranking.
    # This keeps a named vendor's operational or governance source from being
    # evicted by several slightly higher-scoring pages from another vendor.
    requested_dimensions = _research_query_dimension_families(question)
    for entity_index in range(len(entity_hints)):
        for dimension in ("operations", "governance"):
            if dimension not in requested_dimensions:
                continue
            dimension_sources = [
                source
                for source in ranked_sources
                if source not in selected
                and entity_index in source_entity_indexes(source)
                and dimension in source_dimension_classes(source)
            ]
            if not dimension_sources:
                continue
            entity_specific = [
                source
                for source in dimension_sources
                if source_entity_indexes(source) == {entity_index}
            ]
            selected.append((entity_specific or dimension_sources)[0])
            if len(selected) >= bounded_limit:
                break
        if len(selected) >= bounded_limit:
            break

    # Do not let several pages from the highest-ranked vendor consume the
    # mandatory claim-source set in an explicit product comparison.
    for entity_index in range(len(entity_hints)):
        if len(selected) >= bounded_limit:
            break
        entity_sources = [
            source
            for source in ranked_sources
            if source not in selected
            and entity_index in source_entity_indexes(source)
        ]
        if not entity_sources:
            continue
        specific_sources = [
            source
            for source in entity_sources
            if source_entity_indexes(source) == {entity_index}
        ]
        selected.append((specific_sources or entity_sources)[0])
        if len(selected) >= bounded_limit:
            break

    selected_facets = {
        _safe_text(value)
        for source in selected
        for value in [
            *list(source.get("researchFacetIds") or []),
            source.get("researchFacetId"),
        ]
        if _safe_text(value)
    }

    # A combined Research question can have enough globally strong sources
    # while still losing an entire user-declared facet from the finite claim
    # plan. Reserve one best answerable source per explicit facet before using
    # the remaining slots for ordinary quality ranking. When facets outnumber
    # the bounded source target this still covers the maximum possible count.
    for source in ranked_sources:
        if len(selected) >= bounded_limit:
            break
        facet_ids = {
            _safe_text(value)
            for value in [
                *list(source.get("researchFacetIds") or []),
                source.get("researchFacetId"),
            ]
            if _safe_text(value)
        }
        if not facet_ids or facet_ids.issubset(selected_facets):
            continue
        if source not in selected:
            selected.append(source)
        selected_facets.update(facet_ids)
        if len(selected) >= bounded_limit:
            break
    for source in ranked_sources:
        if len(selected) >= bounded_limit:
            break
        if source not in selected:
            selected.append(source)

    # At normal Research depth, retain up to two clearly attributed experience
    # source when it contains an on-topic recommendation.  Otherwise the
    # primary-tier tie-breaker can fill every slot with historical protocol
    # material even though the user asked for current practice and explicitly
    # benefits from field experience.  Primary evidence still occupies at
    # least six of eight slots and remains the authority for API contracts.
    if bounded_limit >= TARGET_RESEARCH_SOURCE_COUNT:
        normative_secondary = [
            source
            for _index, source in ranked
            if source not in selected
            and _safe_text(source.get("tier")).lower() == "secondary"
            and re.search(
                r"\b(?:recommend(?:s|ed|ation)?|should|prefer(?:s|red)?|best practice|"
                r"use .{0,80}(?:path|tool|approach))\b|(?:推荐|建议|首选|最佳实践)",
                " ".join(
                    _safe_text(candidate.get("text"))
                    for candidate in list(source.get("evidenceCandidates") or [])
                    if isinstance(candidate, dict)
                ),
                re.IGNORECASE,
            )
        ]
        minimum_primary = min(6, bounded_limit)
        target_secondary = min(2, max(0, bounded_limit - minimum_primary))
        selected_secondary = sum(
            _safe_text(source.get("tier")).lower() == "secondary"
            for source in selected
        )
        for source in normative_secondary:
            if selected_secondary >= target_secondary:
                break
            facet_counts: dict[str, int] = {}
            for selected_source in selected:
                for selected_facet in [
                    *list(selected_source.get("researchFacetIds") or []),
                    selected_source.get("researchFacetId"),
                ]:
                    selected_facet = _safe_text(selected_facet)
                    if selected_facet:
                        facet_counts[selected_facet] = facet_counts.get(selected_facet, 0) + 1
            replaceable_indexes = []
            for index, selected_source in enumerate(selected):
                if _safe_text(selected_source.get("tier")).lower() != "primary":
                    continue
                selected_source_facets = {
                    _safe_text(value)
                    for value in [
                        *list(selected_source.get("researchFacetIds") or []),
                        selected_source.get("researchFacetId"),
                    ]
                    if _safe_text(value)
                }
                if any(facet_counts.get(facet_id, 0) <= 1 for facet_id in selected_source_facets):
                    continue
                replaceable_indexes.append(index)
            primary_count = sum(
                _safe_text(selected_source.get("tier")).lower() == "primary"
                for selected_source in selected
            )
            if primary_count <= minimum_primary or not replaceable_indexes:
                break
            selected[replaceable_indexes[-1]] = source
            selected_secondary += 1

    return [_safe_text(source.get("citationKey")) for source in selected]


def _architect_supplement_source_order(
    sources: list[dict[str, Any]],
    required_citation_keys: list[str],
) -> list[dict[str, Any]]:
    source_by_citation = {
        _safe_text(source.get("citationKey")).strip("[]"): source
        for source in sources
        if _safe_text(source.get("citationKey")).strip("[]")
    }
    ordered = [
        source_by_citation[citation]
        for citation in required_citation_keys
        if citation in source_by_citation
        and source_by_citation[citation].get("subjectFocused") is not False
    ]
    ordered.extend(
        source
        for source in sources
        if source not in ordered and source.get("subjectFocused") is not False
    )
    return ordered


def _architect_balance_base_claims_for_supplement(
    claims: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    """Prevent one document from consuming the finite verified-claim budget."""

    balanced: list[dict[str, Any]] = []
    per_source_counts: dict[str, int] = {}
    seen_claim_ids: set[str] = set()
    seen_claim_texts: set[str] = set()
    for claim in claims:
        claim_text = _normalized_evidence_text(claim.get("claim"))
        if not claim_text or claim_text in seen_claim_texts:
            continue
        claim_id = _safe_text(claim.get("claimId"))
        if claim_id in seen_claim_ids:
            claim_id = (
                f"{claim_id}_{hashlib.sha1(claim_text.encode('utf-8')).hexdigest()[:8]}"
            )
            claim = {**claim, "claimId": claim_id}
        supports = [
            support
            for support in list(claim.get("supportingSources") or [])
            if isinstance(support, dict)
        ]
        source_key = "|".join(
            _safe_text(
                support.get("citationKey")
                or support.get("sourceId")
                or support.get("url")
                or support.get("sourceUrl")
            ).strip("[]")
            for support in supports
        ) or f"claim:{_safe_text(claim.get('claimId'))}"
        roles = {_architect_support_role(support) for support in supports}
        if "secondary" in roles:
            source_cap = 1
        elif "primary" in roles:
            source_cap = 2
        else:
            source_cap = 1
        if per_source_counts.get(source_key, 0) >= source_cap:
            continue
        balanced.append(claim)
        seen_claim_ids.add(claim_id)
        seen_claim_texts.add(claim_text)
        per_source_counts[source_key] = per_source_counts.get(source_key, 0) + 1
    return balanced


def _architect_source_subject_focused(
    source: dict[str, Any],
    question: str,
    *,
    evidence_candidates: list[dict[str, Any]] | None = None,
) -> bool:
    explicit = source.get("subjectFocused")
    if isinstance(explicit, bool):
        return explicit
    lowered_question = _safe_text(question).lower()
    if "pathlib" not in lowered_question and "path object" not in lowered_question:
        return True
    candidates = [
        item
        for item in list(
            evidence_candidates
            if evidence_candidates is not None
            else source.get("evidenceCandidates") or []
        )
        if isinstance(item, dict)
    ]
    if not candidates and _safe_text(source.get("text")):
        candidates = _architect_evidence_candidates(
            source,
            question,
            limit=_RESEARCH_ARCHITECT_EVIDENCE_CANDIDATE_COUNT,
        )
    candidate_has_subject = bool(
        candidates
        and any(
            _source_relevance_score("pathlib", text=_safe_text(candidate.get("text"))) > 0
            for candidate in candidates
        )
    )
    candidate_evidence = " ".join(
        _safe_text(candidate.get("text")) for candidate in candidates
    )
    identity_text = " ".join(
        (_safe_text(source.get("title")), _safe_text(source.get("url")))
    ).lower()
    identity_has_subject = bool(
        "pathlib" in identity_text
        or re.search(r"\b(?:file\s*system|filesystem)\s+path(?:s|\s+protocol)?\b", identity_text)
        or re.search(r"\bobject[- ]oriented\s+(?:file\s*system|filesystem)\s+paths?\b", identity_text)
    )
    framework_identity = bool(re.search(r"\b(?:argparse|click|typer)\b", identity_text))
    path_protocol_identity = bool(
        re.search(r"\bpep\s*[- ]?0*519\b|\b(?:file\s*system|filesystem)\s+path\s+protocol\b", identity_text)
    )
    path_protocol_evidence = bool(
        re.search(
            r"\b(?:os\.)?pathlike\b|\b(?:os\.)?fspath\b|__fspath__|"
            r"\b(?:file\s*system|filesystem)\s+path\s+protocol\b",
            candidate_evidence,
            re.IGNORECASE,
        )
    )
    scoped_reference_identity = bool(
        re.search(r"\b(?:pep|rfc)\s*[- ]?\d+\b|/(?:pep|rfc)-?\d+/?", identity_text)
        or re.search(r"\b(?:numerical index|release schedule|release notes?|changelog)\b", identity_text)
    )
    if candidates:
        if identity_has_subject:
            return candidate_has_subject or (path_protocol_identity and path_protocol_evidence)
        if framework_identity:
            strong_path_bridge = bool(
                re.search(
                    r"\bpath[_ ]type\b|\btype\s*=\s*pathlib\s*\.\s*path\b|"
                    r"\bpathlib\s*\.\s*path\b",
                    candidate_evidence,
                    re.IGNORECASE,
                )
            )
            contextual_cli_bridge = bool(
                re.search(
                    r"\b(?:command[- ]line|cli\s+(?:argument|application)|application\s+directory|"
                    r"get_app_dir|list\s*\[\s*path|file_okay|dir_okay|resolve_path)\b",
                    candidate_evidence,
                    re.IGNORECASE,
                )
            )
            return strong_path_bridge or (candidate_has_subject and contextual_cli_bridge)
        # A numbered standard or release/index page has a strong, explicit
        # document identity. Incidental matching terms in its body must not
        # make it topical (for example, a virtual-environment PEP that happens
        # to mention pathlib). Ordinary pages may have generic titles, so keep
        # accepting them when their evidence candidates directly cover the
        # subject.
        if scoped_reference_identity:
            return False
        return candidate_has_subject
    if identity_text.strip():
        return identity_has_subject or framework_identity
    # Metadata-only fixtures and legacy callers have no body from which to make
    # a narrower determination. The staged writer still requires candidates.
    return True


def _research_architect_structural_stats(
    question: str,
    sources: list[dict[str, Any]],
    *,
    freshness: str,
) -> dict[str, Any]:
    selected = [
        source
        for source in sources
        if isinstance(source, dict)
        and bool(source.get("selectedForEvidence") or (source.get("sourceQualityGate") or {}).get("selectedForEvidence"))
    ]
    eligible: list[dict[str, Any]] = []
    for source in selected:
        candidates = [
            item for item in list(source.get("evidenceCandidates") or []) if isinstance(item, dict)
        ]
        has_body = bool(_safe_text(source.get("text")))
        if not candidates and has_body:
            candidates = _architect_evidence_candidates(
                source,
                question,
                limit=_RESEARCH_ARCHITECT_EVIDENCE_CANDIDATE_COUNT,
            )
        if has_body and not any(int(item.get("relevanceScore") or 0) > 0 for item in candidates):
            continue
        if not _architect_source_subject_focused(
            source,
            question,
            evidence_candidates=candidates,
        ):
            continue
        eligible.append(source)

    identities = {
        _research_document_identity(_safe_text(source.get("url")), question=question)
        for source in eligible
        if _safe_text(source.get("url"))
    }
    identities.discard("")
    hosts = {
        (_safe_text(source.get("host")) or _host(_safe_text(source.get("url")))).lower().removeprefix("www.")
        for source in eligible
        if _safe_text(source.get("host")) or _host(_safe_text(source.get("url")))
    }
    dated = sum(research_source_has_dated_evidence(source) for source in eligible)
    requires_dated = research_requires_dated_sources({"question": question, "freshness": freshness})
    required_facet_ids = [
        _research_facet_id(kind)
        for _goal, kind in _build_explicit_question_facets(question)
        if _research_facet_id(kind)
    ]
    covered_facet_ids: list[str] = []
    for source in eligible:
        candidate_facets = {
            _safe_text(candidate.get("researchFacetId"))
            for candidate in list(source.get("evidenceCandidates") or [])
            if isinstance(candidate, dict)
            and int(candidate.get("relevanceScore") or 0) > 0
            and _safe_text(candidate.get("researchFacetId"))
        }
        if not candidate_facets:
            candidate_facets = {
                _safe_text(value)
                for value in [
                    *list(source.get("researchFacetIds") or []),
                    source.get("researchFacetId"),
                ]
                if _safe_text(value)
            }
        for facet_id in required_facet_ids:
            if facet_id in candidate_facets and facet_id not in covered_facet_ids:
                covered_facet_ids.append(facet_id)
    missing_facet_ids = [
        facet_id for facet_id in required_facet_ids if facet_id not in covered_facet_ids
    ]
    target_met = bool(
        len(identities) >= TARGET_RESEARCH_SOURCE_COUNT
        and len(hosts) >= TARGET_RESEARCH_DISTINCT_HOST_COUNT
        and not missing_facet_ids
    )
    return {
        "rawSelectedSourceCount": len(selected),
        "selectedSourceCount": len(identities),
        "distinctHostCount": len(hosts),
        "datedSourceCount": dated,
        "requiresDatedSources": requires_dated,
        "requiredFacetIds": required_facet_ids,
        "coveredFacetIds": covered_facet_ids,
        "missingFacetIds": missing_facet_ids,
        "facetEvidenceComplete": not missing_facet_ids,
        "structuralTargetMet": target_met,
    }


def _architect_exact_excerpt_source_fact(
    source: dict[str, Any],
    question: str,
    *,
    excluded_facts: set[str] | None = None,
    required_facet_id: str | None = None,
) -> dict[str, Any] | None:
    citation_key = _safe_text(source.get("citationKey")).strip("[]")
    if not citation_key:
        return None
    normative_pattern = re.compile(
        r"\b(?:recommended|required|recommends?|requires?|prefer(?:s|red)?|best practice|should|must|ought|"
        r"avoid|do not|never|officially endorsed|(?:do|does|should|must|ought)n['’]t)\b|"
        r"官方.{0,8}(?:推荐|要求)|推荐采用|建议(?:使用|将)|首选|最佳实践|应当|不应|必须|不得",
        re.IGNORECASE,
    )
    factual_predicate = re.compile(
        r"\b(?:is|are|was|were|has|have|can|allows?|provides?|returns?|works?|creates?|uses?|"
        r"accepts?|supports?|represents?|inherits?|raises?|converts?|includes?|applies?|"
        r"defines?|starts?|process(?:es|ed|ing)?|stores?|limits?|controls?|govern(?:s|ed|ing)?|sends?|"
        r"runs?|connects?|follows?|asks?|"
        r"takes?\s+effect|enters?\s+into\s+force|becomes?\s+applicable)\b|"
        r"(?:是|为|可以|能够|支持|提供|返回|创建|接受|表示|包含|适用|生效|实施)",
        re.IGNORECASE,
    )
    ranked: list[tuple[int, int, int, int, int, int, str, str]] = []
    cli_path_question = bool(
        re.search(
            r"\b(?:cli|command[- ]line|pathlib|filesystem|file[- ]system|path)\b|(?:命令行|路径|文件系统)",
            question,
            re.IGNORECASE,
        )
    )
    source_identity_text = " ".join(
        (
            _safe_text(source.get("title")),
            _safe_text(source.get("url")),
        )
    )
    click_source_identity = bool(
        re.search(
            r"click\.palletsprojects\.com|\bClick\b",
            source_identity_text,
            re.IGNORECASE,
        )
    )
    typer_source_identity = bool(
        re.search(
            r"typer\.tiangolo\.com|\bTyper\b",
            source_identity_text,
            re.IGNORECASE,
        )
    )
    python_os_source_identity = bool(
        re.search(
            r"docs\.python\.org/(?:3|\d+(?:\.\d+)?)/library/os\.html|"
            r"^\s*os\s*[—-]",
            source_identity_text,
            re.IGNORECASE,
        )
    )
    pathlib_source_identity = bool(
        re.search(
            r"docs\.python\.org/(?:3|\d+(?:\.\d+)?)/library/pathlib\.html|"
            r"^\s*pathlib\s*[—-]",
            source_identity_text,
            re.IGNORECASE,
        )
    )
    trey_hunner_source_identity = "treyhunner.com" in source_identity_text.lower()
    python_morsels_source_identity = "pythonmorsels.com" in source_identity_text.lower()
    real_python_pathlib_source_identity = (
        "realpython.com/python-pathlib" in source_identity_text.lower()
    )
    typer_app_dir_source_identity = (
        "typer.tiangolo.com/tutorial/app-dir" in source_identity_text.lower()
    )
    typer_parameters_source_identity = (
        "typer.tiangolo.com/reference/parameters" in source_identity_text.lower()
    )
    trey_hunner_why_pathlib_source_identity = (
        "treyhunner.com/2018/12/why-you-should-be-using-pathlib"
        in source_identity_text.lower()
    )
    current_question = research_requires_dated_sources(
        {"question": question, "freshness": "auto"}
    )
    named_decision_entities = _catalog_official_entity_hints(question)
    named_decision_dimensions = (
        _research_query_dimension_classes(question)
        if len(named_decision_entities) >= 2
        else set()
    )
    named_decision_focus = (
        _research_entity_query_focus(question)
        if named_decision_dimensions
        else ""
    )

    def decision_dimension_signal(
        fact: str,
        candidate: dict[str, Any],
        candidate_question: str,
    ) -> bool:
        dimensions = _research_query_dimension_classes(candidate_question)
        facet_id = _safe_text(candidate.get("researchFacetId")).lower()
        facet_dimensions = _research_query_dimension_classes(facet_id)
        if facet_dimensions:
            dimensions = facet_dimensions
        fact_dimensions = _research_query_dimension_classes(fact)
        if re.search(r"[$€£¥]\s*\d|\b(?:charges?|paid|free|pro\+?|business|enterprise)\b", fact, re.IGNORECASE):
            fact_dimensions.add("accounts_pricing")
        if re.search(
            r"\b(?:does not train|opt(?:s|ed)?\s+out|cannot|can't|unsupported|"
            r"does not support|only available|not available|service terms|"
            r"terms of service|privacy controls?|retention(?:\s+(?:settings?|limits?))?|"
            r"telemetry|log_user_prompt|sensitive data|data protection|"
            r"model training|network access|sandbox(?:ing)?|approval policy)\b|"
            r"(?:隐私|保留期|遥测|训练数据|敏感数据|数据保护|沙箱|审批策略)",
            fact,
            re.IGNORECASE,
        ):
            fact_dimensions.add("privacy_limits")
        if re.search(
            r"\b(?:agents?|tools?|commands?|sandboxes?|web_fetch)\b",
            fact,
            re.IGNORECASE,
        ):
            fact_dimensions.add("models_tools")
        return bool(dimensions.intersection(fact_dimensions))

    def expected_facet_entity_index(candidate: dict[str, Any]) -> int | None:
        facet_id = _safe_text(candidate.get("researchFacetId")).lower()
        dimension_pattern = "|".join(
            re.escape(value) for value in _RESEARCH_ENTITY_EVIDENCE_DIMENSIONS
        )
        match = re.fullmatch(rf"entity-(.+)-(?:{dimension_pattern}|operations|governance)", facet_id)
        if not match:
            return None
        expected_slug = match.group(1)
        for index, hint in enumerate(named_decision_entities):
            label_slug = re.sub(
                r"[^a-z0-9_-]+",
                "-",
                _safe_text(hint.get("label")).lower(),
            ).strip("-")
            if label_slug == expected_slug:
                return index
        return None

    dated_without_applicability_context = bool(
        research_document_date_candidates(source)
        and not _architect_support_has_current_applicability(source)
    )
    for candidate_index, candidate in enumerate(list(source.get("evidenceCandidates") or [])):
        if not isinstance(candidate, dict):
            continue
        if required_facet_id and _safe_text(candidate.get("researchFacetId")) != _safe_text(
            required_facet_id
        ):
            continue
        excerpt_key = _safe_text(candidate.get("evidenceExcerptKey"))
        excerpt = _safe_text(candidate.get("text"))
        candidate_question = (
            _safe_text(candidate.get("researchFacetGoal")) or question
        )
        if not excerpt_key or not excerpt:
            continue
        normalized_candidate_excerpt = _normalized_evidence_text(excerpt)
        compact_candidate_excerpt = re.sub(r"\s+", "", normalized_candidate_excerpt)
        if (
            cli_path_question
            and python_os_source_identity
            and re.search(
                r"changed in version 3\.6.{0,120}support added to accept objects implementing the os\.pathlike interface",
                normalized_candidate_excerpt,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            pathlike_acceptance_fact = (
                "Version 3.6 added support for accepting objects that implement the "
                "os.PathLike interface."
            )
            if _normalized_evidence_text(pathlike_acceptance_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        270,
                        _source_relevance_score(question, text=pathlike_acceptance_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=pathlike_acceptance_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        pathlike_acceptance_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and pathlib_source_identity
            and re.search(
                r"path classes are divided between pure paths.{0,240}concrete paths",
                normalized_candidate_excerpt,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            pathlib_class_fact = (
                "Path classes are divided between pure paths, which provide computational "
                "operations without I/O, and concrete paths, which also provide I/O operations."
            )
            if _normalized_evidence_text(pathlib_class_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        265,
                        _source_relevance_score(question, text=pathlib_class_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=pathlib_class_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        pathlib_class_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and pathlib_source_identity
            and re.search(
                r"if you.{0,80}(?:never used this module|aren.t sure which class).{0,160}path is most likely what you need",
                normalized_candidate_excerpt,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            pathlib_default_class_fact = (
                "Python's pathlib documentation says Path is most likely what a reader "
                "needs when unsure which path class fits the task."
            )
            if _normalized_evidence_text(pathlib_default_class_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        255,
                        _source_relevance_score(question, text=pathlib_default_class_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=pathlib_default_class_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        pathlib_default_class_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and trey_hunner_source_identity
            and re.search(
                r"the / separators in pathlib\.path strings are automatically converted to the correct path separator",
                normalized_candidate_excerpt,
                re.IGNORECASE,
            )
        ):
            attributed_separator_fact = (
                "The article states that / separators in pathlib.Path strings are "
                "automatically converted to the operating system's path separator."
            )
            if _normalized_evidence_text(attributed_separator_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        245,
                        _source_relevance_score(question, text=attributed_separator_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=attributed_separator_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        attributed_separator_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and trey_hunner_source_identity
            and "path(sys.argv[1])" in compact_candidate_excerpt
            and "ignore_path=directory/'.gitignore'" in compact_candidate_excerpt
            and ".is_file()" in compact_candidate_excerpt
            and ".read_text()" in compact_candidate_excerpt
        ):
            attributed_cli_example_fact = (
                "The article's CLI example constructs a Path from sys.argv[1], joins "
                "'.gitignore' with /, calls is_file(), and calls read_text()."
            )
            if _normalized_evidence_text(attributed_cli_example_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        240,
                        _source_relevance_score(question, text=attributed_cli_example_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=attributed_cli_example_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        attributed_cli_example_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and python_morsels_source_identity
            and re.search(
                r"using path objects for these operations also results in code that is self-descriptive and cross-platform compatible",
                normalized_candidate_excerpt,
                re.IGNORECASE,
            )
        ):
            attributed_readability_fact = (
                "Python Morsels states that using Path objects for these operations "
                "produces self-descriptive, cross-platform-compatible code."
            )
            if _normalized_evidence_text(attributed_readability_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        245,
                        _source_relevance_score(question, text=attributed_readability_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=attributed_readability_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        attributed_readability_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and typer_app_dir_source_identity
            and "typer.get_app_dir" in compact_candidate_excerpt
            and re.search(
                r"directory for storing configurations appropriate for your cli program "
                r"for the current user in each operating system",
                normalized_candidate_excerpt,
                re.IGNORECASE,
            )
        ):
            typer_app_dir_fact = (
                "Typer's get_app_dir() provides an operating-system-appropriate directory "
                "for storing per-user CLI configuration."
            )
            if _normalized_evidence_text(typer_app_dir_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        270,
                        _source_relevance_score(question, text=typer_app_dir_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=typer_app_dir_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        typer_app_dir_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and typer_parameters_source_identity
            and "file_okay=false" in compact_candidate_excerpt
            and re.search(
                r"raise a validation error when a path to a file is given",
                normalized_candidate_excerpt,
                re.IGNORECASE,
            )
        ):
            typer_file_contract_fact = (
                "In Typer's documented Path argument example, file_okay=False causes "
                "a validation error when a file path is supplied."
            )
            if _normalized_evidence_text(typer_file_contract_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        280,
                        _source_relevance_score(question, text=typer_file_contract_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=typer_file_contract_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        typer_file_contract_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and typer_parameters_source_identity
            and "exists=true" in compact_candidate_excerpt
            and re.search(
                r"check that the file or directory exists.{0,80}if not, the value will be invalid",
                normalized_candidate_excerpt,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            typer_exists_contract_fact = (
                "In Typer's documented Path argument example, exists=True checks that "
                "the file or directory exists; a missing path is invalid."
            )
            if _normalized_evidence_text(typer_exists_contract_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        275,
                        _source_relevance_score(question, text=typer_exists_contract_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=typer_exists_contract_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        typer_exists_contract_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and real_python_pathlib_source_identity
            and re.search(
                r"python.s pathlib module helps streamline your work with file and directory paths",
                normalized_candidate_excerpt,
                re.IGNORECASE,
            )
        ):
            real_python_streamline_fact = (
                "Python's pathlib module helps streamline work with file and directory paths."
            )
            if _normalized_evidence_text(real_python_streamline_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        245,
                        _source_relevance_score(question, text=real_python_streamline_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=real_python_streamline_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        real_python_streamline_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and real_python_pathlib_source_identity
            and re.search(
                r"flexible path class.{0,80}intuitive semantics",
                normalized_candidate_excerpt,
                re.IGNORECASE,
            )
        ):
            real_python_semantics_fact = (
                "The Path class provides flexible, intuitive path semantics."
            )
            if _normalized_evidence_text(real_python_semantics_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        235,
                        _source_relevance_score(question, text=real_python_semantics_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=real_python_semantics_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        real_python_semantics_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and trey_hunner_why_pathlib_source_identity
            and re.search(
                r"pass path objects.{0,80}higher-level shutil functions for copying/deleting/moving"
                r".{0,180}no equivalent to these functions on path objects",
                normalized_candidate_excerpt,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            attributed_shutil_boundary_fact = (
                "The article notes that higher-level shutil copy, delete, and move "
                "functions accept Path or path-like objects, while Path has no equivalent methods."
            )
            if _normalized_evidence_text(attributed_shutil_boundary_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        245,
                        _source_relevance_score(question, text=attributed_shutil_boundary_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=attributed_shutil_boundary_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        attributed_shutil_boundary_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and trey_hunner_why_pathlib_source_identity
            and re.search(
                r"methods in this path namespace return path objects.{0,100}method chaining",
                normalized_candidate_excerpt,
                re.IGNORECASE,
            )
        ):
            attributed_chaining_fact = (
                "The article states that methods in the Path namespace return Path objects, "
                "enabling method chaining."
            )
            if _normalized_evidence_text(attributed_chaining_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        235,
                        _source_relevance_score(question, text=attributed_chaining_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=attributed_chaining_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        attributed_chaining_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and typer_source_identity
            and "path validations" in normalized_candidate_excerpt
            and "path cli parameters" in normalized_candidate_excerpt
            and all(
                parameter in normalized_candidate_excerpt
                for parameter in (
                    "exists",
                    "file_okay",
                    "dir_okay",
                    "writable",
                    "readable",
                    "resolve_path",
                )
            )
        ):
            typer_validation_fact = (
                "Typer documents exists, file_okay, dir_okay, writable, readable, "
                "and resolve_path as validation controls for Path CLI parameters."
            )
            if _normalized_evidence_text(typer_validation_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        285,
                        _source_relevance_score(question, text=typer_validation_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=typer_validation_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        typer_validation_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and typer_source_identity
            and "annotated[path" in compact_candidate_excerpt
            and "typer.option" in compact_candidate_excerpt
            and all(
                token in compact_candidate_excerpt
                for token in (
                    "exists=true",
                    "file_okay=true",
                    "dir_okay=false",
                    "writable=false",
                    "readable=true",
                    "resolve_path=true",
                )
            )
        ):
            typer_example_fact = (
                "Typer's example annotates the CLI parameter as Path and configures "
                "exists=True, file_okay=True, dir_okay=False, writable=False, "
                "readable=True, and resolve_path=True through typer.Option."
            )
            if _normalized_evidence_text(typer_example_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        275,
                        _source_relevance_score(question, text=typer_example_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=typer_example_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        typer_example_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and click_source_identity
            and re.search(
                r"\bclass\s+click\.path\b.{0,480}path_type\s*=\s*none",
                normalized_candidate_excerpt,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            click_signature_fact = (
                "Click.Path's documented signature sets path_type=None."
            )
            if _normalized_evidence_text(click_signature_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        280,
                        _source_relevance_score(question, text=click_signature_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=click_signature_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        click_signature_fact,
                        excerpt_key,
                    )
                )
        # Click documents the return-type behavior and its ``None`` case in
        # one contiguous parameter entry.  The generic sentence splitter
        # otherwise ranks the conversion sentence but drops the following
        # default sentence because it no longer contains the query term
        # ``path``.  Preserve the complete, source-near contract so the final
        # report does not leave readers (or the independent reviewer) to infer
        # what ``None`` means.
        if (
            cli_path_question
            and click_source_identity
            and re.search(
                r"path_type.{0,280}convert the incoming path value to this type\.\s*"
                r"if\s+`?none`?,\s*keep python[’']s default,\s*which is\s*`?str`?\.\s*"
                r"useful to convert to.{0,120}pathlib\.path",
                normalized_candidate_excerpt,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            click_path_type_fact = (
                "Click's path_type converts the incoming path value to the specified "
                "type; if None, it keeps Python's default, which is str; it is useful "
                "to convert to pathlib.Path."
            )
            if _normalized_evidence_text(click_path_type_fact) not in (
                excluded_facts or set()
            ):
                ranked.append(
                    (
                        250,
                        _source_relevance_score(question, text=click_path_type_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=click_path_type_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        click_path_type_fact,
                        excerpt_key,
                    )
                )
        if (
            cli_path_question
            and _architect_support_role(source) == "secondary"
            and re.search(
                r"\bshould use Path objects anywhere you work with file paths\b",
                excerpt,
                re.IGNORECASE,
            )
        ):
            attributed_normative_fact = (
                "You should use Path objects anywhere you work with file paths."
            )
            if _normalized_evidence_text(attributed_normative_fact) not in (
                excluded_facts or set()
            ):
                return {
                    "claimId": (
                        f"claim_runtime_{citation_key.lower()}_"
                        f"{hashlib.sha1(_normalized_evidence_text(attributed_normative_fact).encode('utf-8')).hexdigest()[:8]}"
                    ),
                    "claim": attributed_normative_fact,
                    "claimType": "explicit_normative",
                    "normativeCue": "should use Path objects anywhere you work with file paths",
                    "supportingSources": [citation_key],
                    "evidenceExcerptKey": excerpt_key,
                    "confidence": "high",
                }
        # argparse's code-heavy ``type=pathlib.Path`` example does not always
        # yield a grammatical sentence in the generic fragment extractor, so
        # preserve the exact demonstrated converter contract explicitly.
        if (
            cli_path_question
            and re.search(r"\bargparse\b", _safe_text(source.get("title")), re.IGNORECASE)
            and re.search(r"\btype\s*=\s*pathlib\s*\.\s*Path\b", excerpt, re.IGNORECASE)
        ):
            callable_path_fact = (
                "argparse's documented type example passes pathlib.Path as the converter "
                "for a CLI path argument."
            )
            if _normalized_evidence_text(callable_path_fact) not in (excluded_facts or set()):
                ranked.append(
                    (
                        260,
                        _source_relevance_score(question, text=callable_path_fact),
                        _source_relevance_score(
                            question,
                            title=_safe_text(source.get("title")),
                            text=callable_path_fact,
                        ),
                        -candidate_index,
                        1,
                        0,
                        callable_path_fact,
                        excerpt_key,
                    )
                )
        if re.search(
            r"\b(?:install|installation|setup|set[- ]up|prerequisite)\b|(?:安装|装好|前置条件)",
            candidate_question,
            re.IGNORECASE,
        ):
            install_commands = re.findall(
                r"(?<![A-Za-z0-9_./-])"
                r"((?:(?:npm|pnpm|yarn|pipx?|uv|winget|choco|scoop|brew|cargo)\s+"
                r"(?:install|add|i|x|run)\s+|npx\s+)[^\r\n`#]{2,160})",
                excerpt,
                re.IGNORECASE,
            )
            for raw_command in install_commands:
                command = re.sub(r"\s+", " ", raw_command).strip(" .;：；")
                if not 20 <= len(command) <= 180:
                    continue
                if _normalized_evidence_text(command) in (excluded_facts or set()):
                    continue
                return {
                    "claimId": (
                        f"claim_runtime_{citation_key.lower()}_"
                        f"{hashlib.sha1(_normalized_evidence_text(command).encode('utf-8')).hexdigest()[:8]}"
                    ),
                    "claim": command,
                    "claimType": "source_fact",
                    "supportingSources": [citation_key],
                    "evidenceExcerptKey": excerpt_key,
                    "confidence": "high",
                }
        fragmentable_excerpt = re.sub(
            r"(?<=[.!?。！？])(?=(?:\[[A-Z]|[A-Z\u4e00-\u9fff]))",
            " ",
            excerpt,
        )
        fragments = re.split(
            r"```|\s+#{1,6}\s+|\s+\|\s+\d+(?:\s+\d+)*\s+\||"
            r"(?<!\))\s+-\s+(?=[A-Za-z0-9`\u4e00-\u9fff])|"
            r"(?<=[.!?。！？:：;；])\s+(?=[A-Z\u4e00-\u9fff]|"
            r"(?:exists|file_okay|dir_okay|readable|writable|resolve_path|allow_dash|path_type|executable)\b)",
            fragmentable_excerpt,
        )
        for fragment_index, fragment in enumerate(fragments):
            raw_fact = re.sub(r"\s+", " ", fragment).strip(" \t\r\n-•；;")
            ends_with_clause_colon = raw_fact.endswith((":", "："))
            fact = raw_fact.rstrip(":：").strip()
            # Focused excerpts often collapse a Markdown heading and its first
            # factual sentence onto one line. Strip only the heading marker;
            # the normal predicate and relevance gates still reject a bare
            # navigation heading.
            fact = re.sub(r"^#{1,6}\s+", "", fact)
            fact = re.sub(r"^(?:lib|src)/[a-z0-9_./-]+\s+", "", fact, flags=re.IGNORECASE)
            fact = re.sub(
                r"^(?:type\s*:\s*[^|]{1,80}\s+default\s*:\s*)?"
                r"(?:true|false|none|[a-z0-9_.:-]+)\s*\|\s*([a-z_][a-z0-9_]*)\s*\|\s*",
                r"\1: ",
                fact,
                flags=re.IGNORECASE,
            )
            fact = re.sub(
                r"^(?:parameters?\s*:\s*)?"
                r"(exists|file_okay|dir_okay|readable|writable|resolve_path|allow_dash|path_type|executable)"
                r"\s*\([^)]*\)\s*[–—-]\s*",
                r"\1: ",
                fact,
                flags=re.IGNORECASE,
            )
            if _normalized_evidence_text(fact) in (excluded_facts or set()):
                continue
            # Keep one complete source sentence. Legal and standards clauses
            # commonly exceed 280 characters; truncating them loses the
            # exception or qualifier that makes the claim accurate.
            if not 30 <= len(fact) <= 600:
                continue
            if re.search(r"\b(?:e\.g|i\.e)\.\s*$", fact, re.IGNORECASE):
                continue
            if re.match(r"^#{1,6}\s+", fact) or re.search(
                r"\ufffd|â€|Ã|Â|ï»¿",
                fact,
                re.IGNORECASE,
            ):
                continue
            if (
                python_os_source_identity
                and re.search(
                    r"support added to accept objects implementing the os\.pathlike interface",
                    fact,
                    re.IGNORECASE,
                )
                and _normalized_evidence_text(
                    "Version 3.6 added support for accepting objects that implement "
                    "the os.PathLike interface."
                )
                in (excluded_facts or set())
            ):
                continue
            if (
                python_os_source_identity
                and re.match(
                    r"^If you just want to read or write a file see open\(\)",
                    fact,
                    re.IGNORECASE,
                )
            ):
                continue
            if (
                typer_app_dir_source_identity
                and re.match(
                    r"^It will give you a directory for storing configurations appropriate "
                    r"for your CLI program for the current user in each operating system\.?$",
                    fact,
                    re.IGNORECASE,
                )
                and _normalized_evidence_text(
                    "Typer's get_app_dir() provides an operating-system-appropriate directory "
                    "for storing per-user CLI configuration."
                )
                in (excluded_facts or set())
            ):
                continue
            ends_as_sentence = fact.endswith((".", "。", "!", "！"))
            english_legal_clause = bool(
                not ends_as_sentence
                and ends_with_clause_colon
                and re.search(r"\b(?:may|shall|must)\s+[a-z]", fact, re.IGNORECASE)
                and re.search(
                    r"\b(?:article|commission|provider|fine|penalt|regulation|turnover)\w*\b",
                    fact,
                    re.IGNORECASE,
                )
            )
            english_decision_fact = bool(
                not ends_as_sentence
                and named_decision_focus
                and len(fact) <= 240
                and decision_dimension_signal(fact, candidate, candidate_question)
                and (
                    factual_predicate.search(fact)
                    or normative_pattern.search(fact)
                    or re.search(
                        r"\b(?:install(?:s|ed|ing)?|connect(?:s|ed|ing)?|"
                        r"configur(?:e|es|ed|ing)?|authenticat(?:e|es|ed|ing)?|"
                        r"disable(?:s|d|ing)?|enable(?:s|d|ing)?|select(?:s|ed|ing)?|"
                        r"offer(?:s|ed|ing)?|opt(?:s|ed)?\s+out|sign(?:s|ed|ing)?\s+in)\b|"
                        r"[$€£¥]\s*\d|\b\d+(?:\.\d+)+(?:\+|%)?\b|--[a-z][a-z0-9-]*",
                        fact,
                        re.IGNORECASE,
                    )
                )
            )
            atomic_unpunctuated_fact = bool(
                not ends_as_sentence
                and (
                    english_legal_clause
                    or english_decision_fact
                    or (
                        re.search(r"[\u4e00-\u9fff]", fact)
                        and factual_predicate.search(fact)
                        and re.search(
                            r"\b(?:article|annex|chapter|section|recital|GPAI)\b|"
                            r"(?:第\s*\d+\s*条|附录|通用(?:目的|用途|型)?\s*(?:AI|人工智能)|"
                            r"\d{4}\s*年|\d{4}[-/]\d{1,2}[-/]\d{1,2})",
                            fact,
                            re.IGNORECASE,
                        )
                    )
                )
            )
            if (not ends_as_sentence and not atomic_unpunctuated_fact) or "?" in fact or "？" in fact:
                continue
            emitted_fact = (
                f"{fact}{'.' if english_legal_clause or english_decision_fact else '。'}"
                if atomic_unpunctuated_fact
                else fact
            )
            if _normalized_evidence_text(emitted_fact) in (excluded_facts or set()):
                continue
            if _is_noisy_research_sentence(fact):
                continue
            if _architect_evidence_fragment_is_truncated(fact):
                continue
            named_parameter_fact = bool(
                re.match(
                    r"^(?:exists|file_okay|dir_okay|readable|writable|resolve_path|allow_dash|path_type|executable)"
                    r"(?:\s*[:(])",
                    fact,
                    re.IGNORECASE,
                )
            )
            if any(
                marker in fact.lower()
                for marker in (
                    ">>>",
                    "<!--",
                    "[...]",
                    "table of contents",
                    "recommended course",
                    "cheat sheet",
                )
            ) or (" | " in fact and not named_parameter_fact):
                continue
            if re.match(r"^(?:from|import)\s+[a-z_]", fact, re.IGNORECASE):
                continue
            if re.match(r"^(?:i|we)\b", fact, re.IGNORECASE):
                continue
            if re.search(r"\bI\b", fact):
                continue
            if re.search(r"\bwith uses\b|\b(?:wonderful|awesome|amazing)\b", fact, re.IGNORECASE):
                continue
            if re.search(r"\s\.\s*$", fact) or fact.count("(") != fact.count(")"):
                continue
            if fact.count("=") >= 2:
                continue
            if named_decision_focus:
                # Product identity alone is not decision evidence. Navigation
                # labels, installer click-through prose, and vague local-build
                # commentary previously inherited relevance from the page
                # title and displaced exact model/MCP/pricing/privacy facts.
                if re.fullmatch(
                    r"(?:\[[^\]\r\n]+\]\([^\r\n)]+\)|[A-Z][A-Za-z0-9 +_.-]{2,80})"
                    r"\s+\d+[.)]?",
                    fact,
                    re.IGNORECASE,
                ):
                    continue
                if re.match(
                    r"^(?:this|the)\s+(?:guide|article|page|section|documentation)\b",
                    fact,
                    re.IGNORECASE,
                ):
                    continue
                expected_entity_index = expected_facet_entity_index(candidate)
                if expected_entity_index is not None:
                    direct_entity_indexes = _research_entity_indexes_in_text(
                        named_decision_entities,
                        " ".join((fact, _safe_text(source.get("title")))),
                    )
                    contextual_entity_indexes = _research_source_entity_indexes(
                        named_decision_entities,
                        host=source.get("host") or _host(_safe_text(source.get("url"))),
                        url=source.get("url"),
                        title=source.get("title"),
                        text=source.get("text"),
                        evidence_query=candidate_question,
                    )
                    if (
                        expected_entity_index not in direct_entity_indexes
                        and expected_entity_index not in contextual_entity_indexes
                    ):
                        continue
                if (
                    re.match(
                        r"^(?:it(?:['’]s|\s+is)|they|this|that|these|those)\b",
                        fact,
                        re.IGNORECASE,
                    )
                    and not _research_entity_indexes_in_text(
                        named_decision_entities,
                        fact,
                    )
                    and not re.search(
                        r"[$€£¥]\s*\d|\b\d+(?:\.\d+)+(?:\+|%)?\b|"
                        r"\b(?:npm|npx|winget|choco|scoop|brew|powershell|wsl|"
                        r"mcp|telemetry|privacy|subscription|pricing)\b",
                        fact,
                        re.IGNORECASE,
                    )
                ):
                    continue
                decision_dimension_matched = decision_dimension_signal(
                    fact,
                    candidate,
                    candidate_question,
                )
                if not decision_dimension_matched:
                    continue
                if not (
                    factual_predicate.search(fact)
                    or normative_pattern.search(fact)
                    or re.search(
                        r"\b(?:install(?:s|ed|ing)?|connect(?:s|ed|ing)?|"
                        r"configur(?:e|es|ed|ing)?|authenticat(?:e|es|ed|ing)?|"
                        r"retir(?:e|es|ed|ing)?|deprecat(?:e|es|ed|ing)?|"
                        r"disable(?:s|d|ing)?|enable(?:s|d|ing)?|select(?:s|ed|ing)?|"
                        r"switch(?:es|ed|ing)?|offer(?:s|ed|ing)?|charg(?:e|es|ed|ing)?|"
                        r"opt(?:s|ed)?\s+out|sign(?:s|ed|ing)?\s+in|"
                        r"log(?:s|ged|ging)?\s+in|no\s+longer\s+available|starting)\b|"
                        r"[$€£¥]\s*\d|\b20\d{2}(?:-\d{1,2}-\d{1,2})?\b|"
                        r"\b\d+(?:\.\d+)+(?:\+|%)?\b|--[a-z][a-z0-9-]*",
                        fact,
                        re.IGNORECASE,
                    )
                ):
                    continue
                candidate_decision_focus = _research_entity_query_focus(
                    candidate_question
                )
                decision_detail_relevance = _source_relevance_score(
                    candidate_decision_focus,
                    text=fact,
                )
                if decision_detail_relevance <= 0 and decision_dimension_matched:
                    # The dimension classifier already resolves documented
                    # aliases such as "Service Terms" to privacy/governance.
                    # Do not require the same fact to repeat the literal words
                    # "privacy", "data", or "limitations" as well.
                    decision_detail_relevance = 1
            else:
                decision_detail_relevance = 0
            if re.search(
                r"\b(?:a|an|the|to|of|and|or|when|with|all|using|use|will|can|could|would|is|are)\.\s*$",
                fact,
                re.IGNORECASE,
            ):
                continue
            direct_relevance = max(
                _source_relevance_score(candidate_question, text=fact),
                _source_relevance_score(question, text=fact),
            )
            relevance = max(
                _source_relevance_score(
                    candidate_question,
                    title=_safe_text(source.get("title")),
                    text=fact,
                ),
                _source_relevance_score(
                    question,
                    title=_safe_text(source.get("title")),
                    text=fact,
                ),
            )
            if relevance <= 0:
                continue
            # A historical proposal's future-tense implementation promise is
            # useful history, but it is not evidence that the current runtime
            # implemented that promise. Prefer another exact fact from the
            # source for current/latest questions instead of making every
            # downstream writer and reviewer recover that temporal distinction.
            if (
                current_question
                and dated_without_applicability_context
                and re.search(
                    r"\b(?:will|shall)\s+(?:be\s+)?(?:updated|changed|added|implemented|"
                    r"adopted|supported|converted|modified|provided|introduced)\b",
                    fact,
                    re.IGNORECASE,
                )
            ):
                continue
            if _architect_hard_assertion_issues(fact, excerpt, source_metadata=source):
                continue
            utility = (80 if english_legal_clause else 0) + min(
                180,
                decision_detail_relevance * 2,
            )
            candidate_facet_dimensions = _research_query_dimension_classes(
                _safe_text(candidate.get("researchFacetId"))
            )
            if "privacy_limits" in candidate_facet_dimensions:
                # Security pages often contain several valid, but differently
                # useful, facts. Prefer a concrete data-handling boundary over
                # a generic sandbox introduction when repairing a privacy
                # facet; both remain subject to the exact excerpt gates above.
                if re.search(
                    r"\b(?:log_user_prompt|retention|telemetry|sensitive data|"
                    r"data protection|model training|privacy statement|opt.?out)\b|"
                    r"(?:保留期|遥测|敏感数据|数据保护|训练数据|隐私声明|退出)",
                    fact,
                    re.IGNORECASE,
                ):
                    utility += 180
                elif re.search(
                    r"\b(?:sandbox(?:ing)?|network access|approval policy|workspace)\b|"
                    r"(?:沙箱|网络访问|审批策略|工作区)",
                    fact,
                    re.IGNORECASE,
                ):
                    utility += 60
            if cli_path_question:
                if named_parameter_fact:
                    parameter_name = re.match(r"^([a-z_][a-z0-9_]*)", fact, re.IGNORECASE)
                    utility += (
                        120
                        if parameter_name
                        and parameter_name.group(1).lower()
                        in {"exists", "file_okay", "dir_okay", "resolve_path", "path_type"}
                        else 20
                    )
                if re.search(
                    r"\b(?:get_app_dir|application directory|configuration directory|directory for storing configurations?)\b|"
                    r"(?:应用目录|配置目录|存储配置的目录)",
                    fact,
                    re.IGNORECASE,
                ):
                    utility += 115
                if re.search(
                    r"\bpath_type\b|\bconvert(?:s|ed|ing)?\s+(?:the\s+)?incoming\s+path\s+value.{0,50}"
                    r"(?:type|pathlib\.Path|Path object)\b",
                    fact,
                    re.IGNORECASE,
                ):
                    utility += 100
                if re.search(r"\b(?:exists|file_okay|dir_okay|validat(?:e|es|ed|ing|ion)|permission)\b", fact, re.IGNORECASE):
                    utility += 90
                if re.search(r"\b(?:pathlib\.Path|Path object|Path argument|filesystem path|file[- ]system path)\b", fact, re.IGNORECASE):
                    utility += 70
                if re.search(r"\b(?:PathLike|__fspath__|os\.fspath|pure path|concrete path)\b", fact, re.IGNORECASE):
                    utility += 55
                if re.search(r"\b(?:CLI|command[- ]line|argument|option|parameter)\b", fact, re.IGNORECASE):
                    utility += 25
            ranked.append(
                (
                    utility,
                    direct_relevance,
                    relevance,
                    -candidate_index,
                    1 if factual_predicate.search(fact) else 0,
                    -fragment_index,
                    emitted_fact,
                    excerpt_key,
                )
            )
    if not ranked:
        return None
    ranked.sort(reverse=True)
    (
        _utility,
        _direct_relevance,
        _relevance,
        _candidate_order,
        _factual,
        _fragment_order,
        fact,
        excerpt_key,
    ) = ranked[0]
    normative_match = normative_pattern.search(fact)
    normative_cue = normative_match.group(0) if normative_match else ""
    if normative_match and len(normative_cue) < 3:
        normative_cue = fact[
            max(0, normative_match.start() - 6) :
            min(len(fact), normative_match.end() + 14)
        ].strip()
    return {
        "claimId": (
            f"claim_runtime_{citation_key.lower()}_"
            f"{hashlib.sha1(_normalized_evidence_text(fact).encode('utf-8')).hexdigest()[:8]}"
        ),
        "claim": fact,
        "claimType": "explicit_normative" if normative_cue else "source_fact",
        **({"normativeCue": normative_cue} if normative_cue else {}),
        "supportingSources": [citation_key],
        "evidenceExcerptKey": excerpt_key,
        "confidence": "high",
    }


def _architect_normalized_repair_outline(
    outline: Any,
    claims: list[dict[str, Any]],
    inferences: Any = None,
) -> list[dict[str, Any]]:
    claim_ids = [
        _safe_text(claim.get("claimId"))
        for claim in claims
        if _safe_text(claim.get("claimId"))
    ]
    if not claim_ids:
        return []
    raw_sections = _research_dict_list(outline, limit=6)

    # Keep the premises of a still-viable composite inference together while
    # rebuilding a malformed outline.  Without this, evenly redistributing a
    # repaired claim table can silently make every model-backed inference cross
    # section boundaries and force another full planning request.
    claim_order = {claim_id: index for index, claim_id in enumerate(claim_ids)}
    parent = {claim_id: claim_id for claim_id in claim_ids}

    def find(claim_id: str) -> str:
        root = claim_id
        while parent[root] != root:
            root = parent[root]
        while parent[claim_id] != claim_id:
            next_id = parent[claim_id]
            parent[claim_id] = root
            claim_id = next_id
        return root

    def union(left: str, right: str) -> None:
        left_root = find(left)
        right_root = find(right)
        if left_root != right_root:
            if claim_order[left_root] <= claim_order[right_root]:
                parent[right_root] = left_root
            else:
                parent[left_root] = right_root

    for inference in _research_dict_list(inferences, limit=12):
        raw_premises = [
            _safe_text(value)
            for value in list(inference.get("premiseClaimIds") or [])
            if _safe_text(value)
        ]
        if not raw_premises or any(claim_id not in parent for claim_id in raw_premises):
            continue
        roots = {find(claim_id) for claim_id in claim_ids}
        premise_roots = {find(claim_id) for claim_id in raw_premises}
        # A single inference spanning the entire claim table would make a
        # multi-section answer impossible.  Leave that inference ungrouped so
        # the normal inference validator can reject it instead.
        if len(roots) - max(0, len(premise_roots) - 1) < 2:
            continue
        first = raw_premises[0]
        for claim_id in raw_premises[1:]:
            union(first, claim_id)

    components_by_root: dict[str, list[str]] = {}
    for claim_id in claim_ids:
        components_by_root.setdefault(find(claim_id), []).append(claim_id)
    components = sorted(
        components_by_root.values(),
        key=lambda component: min(claim_order[claim_id] for claim_id in component),
    )
    section_count = min(4, len(components))
    if section_count < 2:
        return []
    buckets: list[list[str]] = [[] for _ in range(section_count)]
    bucket_index = 0
    target_size = max(1, (len(claim_ids) + section_count - 1) // section_count)
    for component_index, component in enumerate(components):
        remaining_components = len(components) - component_index
        remaining_buckets = section_count - bucket_index
        if (
            bucket_index < section_count - 1
            and buckets[bucket_index]
            and (
                len(buckets[bucket_index]) >= target_size
                or remaining_components == remaining_buckets - 1
            )
        ):
            bucket_index += 1
        buckets[bucket_index].extend(component)

    normalized: list[dict[str, Any]] = []
    used_section_ids: set[str] = set()
    for index in range(section_count):
        raw = raw_sections[index] if index < len(raw_sections) else {}
        raw_claim_ids = {
            _safe_text(value)
            for value in list(raw.get("claimIds") or [])
            if _safe_text(value)
        }
        repaired_claim_ids = set(buckets[index])
        preserves_original_claim_group = bool(
            raw_claim_ids and raw_claim_ids == repaired_claim_ids
        )
        section_id = _safe_text(raw.get("sectionId")) or f"section_{index + 1}"
        if section_id in used_section_ids:
            section_id = f"section_{index + 1}"
        used_section_ids.add(section_id)
        normalized.append(
            {
                "sectionId": section_id,
                "title": (
                    _safe_text(raw.get("title"))
                    if preserves_original_claim_group
                    else f"Evidence-backed findings {index + 1}"
                ),
                "objective": (
                    _safe_text(raw.get("objective"))
                    if preserves_original_claim_group
                    else "Explain only the assigned verified claims and their applicability boundaries."
                ),
                "claimIds": buckets[index],
                "facetIds": list(
                    dict.fromkeys(
                        facet_id
                        for claim in claims
                        if _safe_text(claim.get("claimId")) in repaired_claim_ids
                        for facet_id in _architect_claim_facet_ids(claim)
                    )
                ),
            }
        )
    return normalized


_ARCHITECT_NEGATIVE_CAPABILITY_RE = re.compile(
    r"\b(?:does\s+not|doesn't|do\s+not|not\s+(?:have|provide|support)|no\s+built[ -]in|"
    r"not\s+part\s+of|lacks?|without\s+support|cannot|can't|unsupported|unavailable)\b|"
    r"(?:不支持|不具备|没有|不存在|未提供|无法|不可用)",
    re.IGNORECASE,
)
_ARCHITECT_PERFORMANCE_DIRECTION_RE = re.compile(
    r"\b(?:improv(?:e|es|ed|ement)?|reduc(?:e|es|ed|tion)?|faster|accelerat(?:e|es|ed|ion)?|"
    r"speed(?:s|ed|ing)?\s+up|lower(?:s|ed|ing)?|decreas(?:e|es|ed|ing)?)\b|"
    r"(?:提升|改善|降低|加速|减少|缩短)",
    re.IGNORECASE,
)
_ARCHITECT_STRONG_MAGNITUDE_RE = re.compile(
    r"\b(?:significantly|substantially|dramatically|materially|considerably)\b|(?:显著|大幅|明显)",
    re.IGNORECASE,
)
_ARCHITECT_NORMATIVE_ASSERTION_RE = re.compile(
    r"\b(?:recommended|required|recommends?|requires?|prefer(?:s|red)?|best practice|should|must|ought|"
    r"canonical way|standard approach|official guidance)\b|"
    r"(?:官方.{0,8}(?:推荐|要求|指南)|推荐采用|建议(?:使用|将)|首选|最佳实践|应当|必须|标准做法)",
    re.IGNORECASE,
)
_ARCHITECT_SYNTHESIS_LABEL_RE = re.compile(
    r"\b(?:this report(?:'s)? (?:synthesis|recommendation)|practical synthesis|our synthesis|"
    r"evidence-based synthesis|synthesized recommendation|inference from)\b|"
    r"(?:本报告(?:的)?(?:综合判断|建议)|综合判断|基于证据的综合建议|跨来源推论)",
    re.IGNORECASE,
)
_ARCHITECT_EXPLICIT_SYNTHESIS_LABEL_RE = re.compile(
    r"(?:^|[\n\r])\s*#{1,6}\s*(?:this report(?:'s)? (?:synthesis|recommendation)|practical synthesis|"
    r"our synthesis|evidence-based synthesis|synthesized recommendation|inference from|"
    r"本报告(?:的)?(?:综合判断|建议)|综合判断|基于证据的综合建议|跨来源推论)[^\r\n]{0,100}(?=\r?$)|"
    r"(?:^|[\n\r>\-*#]\s*)(?:this report(?:'s)? (?:synthesis|recommendation)|practical synthesis|"
    r"our synthesis|evidence-based synthesis|synthesized recommendation|inference from|"
    r"本报告(?:的)?(?:综合判断|建议)|综合判断|基于证据的综合建议|跨来源推论)\s*[:：—-]",
    re.IGNORECASE | re.MULTILINE,
)
_ARCHITECT_CROSS_SOURCE_INFERENCE_CUE_RE = re.compile(
    r"\b(?:combined\s+with|in\s+combination\s+with|taken\s+together|together\s+with|"
    r"therefore|thus|across\s+these\s+sources|these\s+findings\s+(?:show|imply|mean)|"
    r"(?:as|because|since)\s+(?:it|they|these|this)\s+(?:implement|provide|support|enable))\b|"
    r"(?:结合.{0,30}(?:可以|能够|确保|意味着)|综合上述|共同表明|因此|据此)",
    re.IGNORECASE,
)
_ARCHITECT_CURRENT_OR_OFFICIAL_RE = re.compile(
    r"\b(?:currently|latest|today|official|authoritative)\b|"
    r"\bcurrent\b(?!\s+(?:system|platform|operating\s+system|directory|working\s+directory|"
    r"path|user|process|environment|command|session|file)\b)|"
    r"(?:目前|最新|官方|权威|当前(?!系统|平台|操作系统|目录|工作目录|路径|用户|进程|环境|命令|会话|文件))",
    re.IGNORECASE,
)
_ARCHITECT_SECONDARY_CONTEXT_RE = re.compile(
    r"\b(?:secondary|historical|older commentary|community example|editorial opinion|illustrative experience|"
    r"tutorial|guide|blog|article|case study|illustrative example)\b|"
    r"(?:二手来源|历史材料|旧文章|社区示例|评论性观点|经验性示例|教程|指南|博客|文章|案例)",
    re.IGNORECASE,
)
_ARCHITECT_SOURCE_ROLE_ATTRIBUTION_RE = re.compile(
    r"^(?P<headings>(?:(?:#{1,6}\s+[^\r\n]+|\*\*[^\r\n]+\*\*)\s*\r?\n)*)\s*"
    r"(?:[-*+]\s+|\d+[.)]\s+)?"
    r"(?:Secondary source(?:\s+[\"“][^\"”]{1,180}[\"”])?(?:\s*\(S\d+\))? states|"
    r"Mixed-source synthesis with secondary context(?:\s+from\s+[\"“][^\"”]{1,180}[\"”])?|"
    r"二手来源(?:《[^》]{1,180}》)?的表述|含二手来源(?:《[^》]{1,180}》)?的综合判断)"
    r"\s*[:：]\s*",
    re.IGNORECASE,
)
_ARCHITECT_TEMPORAL_BOUNDARY_NOTE_RE = re.compile(
    r"^\s*(?:[-*+]\s+)?(?:Temporal|Date|Applicability)\s+boundary\s*[:：]|"
    r"^\s*(?:[-*+]\s+)?(?:边界说明|时效边界|日期边界|适用性边界)\s*[:：]",
    re.IGNORECASE,
)
_ARCHITECT_QUANTIFIED_EFFECT_RE = re.compile(
    r"\b\d+(?:\.\d+)?\s*(?:%|ms|milliseconds?|seconds?|x\b|times?\b|tokens?\b|[kmgt]?b\b)",
    re.IGNORECASE,
)
_ARCHITECT_DURATION_ANCHOR_RE = re.compile(
    r"\b\d+(?:\.\d+)?[- ](?:days?|weeks?|months?|years?|hours?|minutes?)\b",
    re.IGNORECASE,
)
_ARCHITECT_QUALIFIED_VERSION_RE = re.compile(
    r"\b(?:PowerShell|Node(?:\.js)?|Python|Java|Go|Rust|npm|Homebrew|WinGet)\s+"
    r"v?\d+(?:\.\d+){0,2}(?:\s*(?:\+|or\s+higher|and\s+above))?(?=$|[^A-Za-z0-9])",
    re.IGNORECASE,
)
_ARCHITECT_UNDATED_MONTH_DAY_RE = re.compile(
    r"\b(?:January|February|March|April|May|June|July|August|September|October|"
    r"November|December)\s+\d{1,2}(?!\s*,?\s*\d{4})\b|"
    r"\b\d{1,2}\s+(?:January|February|March|April|May|June|July|August|September|"
    r"October|November|December)(?!\s+\d{4})\b|"
    r"\b\d{1,2}月\d{1,2}日\b",
    re.IGNORECASE,
)


def _architect_is_temporal_boundary_note(value: Any) -> bool:
    """Recognize Runtime-facing date caveats, not product assertions."""

    text = _safe_text(value)
    if not _ARCHITECT_TEMPORAL_BOUNDARY_NOTE_RE.search(text):
        return False
    has_temporal_scope = bool(
        re.search(
            r"\b(?:undated|date|year|publication|retrieval|as[- ]of|timestamp)\b|"
            r"(?:未标年份|未注明年份|无文档日期|检索时间|发布时间|更新时间|截至|时点|年份)",
            text,
            re.IGNORECASE,
        )
    )
    has_limitation = bool(
        re.search(
            r"\b(?:cannot|does not|do not|must not|not equivalent|requires? verification|only)\b|"
            r"(?:不能|不得|不可|不等于|不代表|仅|需.{0,12}(?:核验|确认|复查)|"
            r"适用性.{0,12}(?:判断|核验))",
            text,
            re.IGNORECASE,
        )
    )
    return has_temporal_scope and has_limitation


def _architect_assertion_text(value: Any) -> str:
    """Return prose assertions without navigation or governance labels.

    A secondary-source title can itself contain words such as ``should`` or
    ``official``.  Those title words describe the document, not the factual
    sentence that follows the Runtime-added attribution label.  Generic
    headings such as ``Best practices for path objects`` are navigation labels.
    Strong headings such as ``Current official guidance`` or ``You should
    always ...`` remain governed. The paragraph below every heading remains
    fully governed.
    """

    text = _safe_text(value)
    match = _ARCHITECT_SOURCE_ROLE_ATTRIBUTION_RE.match(text)
    if match:
        text = "\n".join(
            part
            for part in (
                _safe_text(match.group("headings")),
                text[match.end() :].lstrip(),
            )
            if part
        )

    def heading_assertion(match_value: re.Match[str]) -> str:
        heading = re.sub(
            r"^\s*(?:#{1,6}\s+|\*\*)|\*\*\s*$",
            "",
            match_value.group(0),
        ).strip()
        if re.fullmatch(
            r"(?:best practices?(?:\s+for\s+.+)?|recommendations?(?:\s+and\s+.+)?|"
            r"practical considerations?|limitations?(?:\s+and\s+.+)?|"
            r"implementation notes?|examples?|summary|conclusion)",
            heading,
            re.IGNORECASE,
        ):
            return " "
        if (
            _ARCHITECT_CURRENT_OR_OFFICIAL_RE.search(heading)
            or _ARCHITECT_NORMATIVE_ASSERTION_RE.search(heading)
        ):
            return heading
        return " "

    return re.sub(
        r"(?m)^\s*(?:#{1,6}\s+[^\r\n]+|\*\*[^\r\n]+\*\*)\s*$",
        heading_assertion,
        text,
    ).strip()
_ARCHITECT_PERFORMANCE_METRICS = (
    ("startup", re.compile(r"\bstartup\b|启动", re.IGNORECASE)),
    ("latency", re.compile(r"\blatenc(?:y|ies)\b|延迟", re.IGNORECASE)),
    ("performance", re.compile(r"\bperformance\b|性能", re.IGNORECASE)),
    ("throughput", re.compile(r"\bthroughput\b|吞吐", re.IGNORECASE)),
    ("memory", re.compile(r"\bmemory\b|内存", re.IGNORECASE)),
    ("speed", re.compile(r"\bspeed\b|速度", re.IGNORECASE)),
)
_ARCHITECT_BEHAVIOR_RELATIONS = (
    (
        "continuity",
        re.compile(
            r"\b(?:remain(?:s|ed)?\s+(?:current|valid|supported)|still\s+(?:current|valid|supported)|"
            r"fully\s+supported|continued\s+applicability|continues?\s+to\s+apply)\b|"
            r"(?:仍然|依然|继续)(?:有效|适用|受支持|是当前)|完全受支持",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:remain(?:s|ed)?\s+(?:current|valid|supported)|still\s+(?:current|valid|supported)|"
            r"fully\s+supported|continued\s+applicability|continues?\s+to\s+apply)\b|"
            r"(?:仍然|依然|继续)(?:有效|适用|受支持|是当前)|完全受支持",
            re.IGNORECASE,
        ),
    ),
    (
        "readability",
        re.compile(
            r"\b(?:more\s+readable|easier\s+to\s+read|clearer|more\s+explicit|improves?\s+readability)\b|"
            r"(?:更易读|更可读|更清晰|更明确|提升可读性)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:more\s+readable|easier\s+to\s+read|clearer|more\s+explicit|improves?\s+readability)\b|"
            r"(?:更易读|更可读|更清晰|更明确|提升可读性)",
            re.IGNORECASE,
        ),
    ),
    (
        "conversion",
        re.compile(
            r"\b(?:convert(?:s|ed|ing)?|coerce(?:s|d)?|cast(?:s|ed|ing)?|return(?:s|ed|ing)?|"
            r"yield(?:s|ed|ing)?|produce(?:s|d|ing)?|string[- ]to[- ][a-z_]+)\b|"
            r"(?:转换|转为|强制转换|返回|产出|生成).{0,40}(?:对象|类型|path|路径)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:convert(?:s|ed|ing)?|coerce(?:s|d)?|cast(?:s|ed|ing)?|return(?:s|ed|ing)?|"
            r"yield(?:s|ed|ing)?|produce(?:s|d|ing)?|result(?:s|ing)?\s+(?:is|in)|object\s+of\s+type|"
            r"instance\s+of|passed\s+to)\b|(?:转换|转为|强制转换|返回|产出|生成|得到).{0,40}(?:对象|类型|path|路径)",
            re.IGNORECASE,
        ),
    ),
    (
        "interoperability",
        re.compile(
            r"\b(?:can\s+be\s+(?:used|passed)\s+with|works?\s+with|is\s+accepted\s+by|"
            r"can\s+be\s+passed\s+to)\b|(?:可(?:与|被).{0,30}(?:使用|接受)|可传给)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:can\s+be\s+(?:used|passed)\s+with|works?\s+with|is\s+accepted\s+by|"
            r"can\s+be\s+passed\s+to)\b|"
            r"\b[a-z_][a-z0-9_]*\s*\(\s*(?:pathlib\s*\.\s*)?Path\s*\(|"
            r"(?:可(?:与|被).{0,30}(?:使用|接受)|可传给)",
            re.IGNORECASE,
        ),
    ),
    (
        "validation",
        re.compile(
            r"\b(?:validat(?:e|es|ed|ing|ion)|enforc(?:e|es|ed|ing)|reject(?:s|ed|ing)?|"
            r"check(?:s|ed|ing)?|ensure(?:s|d)?|verify|verifies|verified)\b|"
            r"(?:验证|校验|强制|拒绝|检查|确保)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:validat(?:e|es|ed|ing|ion)|enforc(?:e|es|ed|ing)|reject(?:s|ed|ing)?|"
            r"check(?:s|ed|ing)?|ensure(?:s|d)?|verif(?:y|ies|ied)|must|required|needs?\s+to|"
            r"only\s+if|not\s+allowed|invalid)\b|(?:验证|校验|强制|拒绝|检查|确保|必须|需要|无效)",
            re.IGNORECASE,
        ),
    ),
    (
        "configuration_scope",
        re.compile(
            r"(?is)(?=.*(?:telemetry|settings?\.json|config(?:uration)?\.json|"
            r"配置|遥测))(?=.*(?:\bscope\b|scoped|user[- ]level|project[- ]level|"
            r"system[- ]level|作用域|用户级|项目级|系统级))",
        ),
        re.compile(
            r"(?is)(?=.*(?:telemetry|settings?\.json|config(?:uration)?\.json|"
            r"配置|遥测))(?=.*(?:\bscope\b|scoped|user[- ]level|project[- ]level|"
            r"system[- ]level|作用域|用户级|项目级|系统级))",
        ),
    ),
    (
        "sharing",
        re.compile(
            r"\b(?:share(?:d|s)?\s+(?:usage|credits?|data|context)|"
            r"usage\s+and\s+credits\s+(?:are|is)\s+shared)\b|"
            r"(?:共享(?:用量|额度|数据|上下文))",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:share(?:d|s)?\s+(?:usage|credits?|data|context)|"
            r"usage\s+and\s+credits\s+(?:are|is)\s+shared)\b|"
            r"(?:共享(?:用量|额度|数据|上下文))",
            re.IGNORECASE,
        ),
    ),
    (
        "cross_context_association",
        re.compile(
            r"(?:\bWindows\b|Windows上).{0,180}(?:first\s+run|sign(?:ing)?\s+in|"
            r"首(?:次|启动).{0,20}(?:登录|运行)|登录)|"
            r"(?:first\s+run|sign(?:ing)?\s+in|首(?:次|启动).{0,20}(?:登录|运行)|登录)"
            r".{0,180}(?:\bWindows\b|Windows上)",
            re.IGNORECASE | re.DOTALL,
        ),
        re.compile(
            r"(?:\bWindows\b|Windows上).{0,220}(?:first\s+run|sign(?:ing)?\s+in|"
            r"首(?:次|启动).{0,20}(?:登录|运行)|登录)|"
            r"(?:first\s+run|sign(?:ing)?\s+in|首(?:次|启动).{0,20}(?:登录|运行)|登录)"
            r".{0,220}(?:\bWindows\b|Windows上)",
            re.IGNORECASE | re.DOTALL,
        ),
    ),
    (
        "stability",
        re.compile(
            r"\b(?:version[- ]agnostic|future[- ]proof|stable\s+(?:api|interface|protocol|contract|abstraction))\b|"
            r"(?:版本无关|面向未来|稳定的?(?:接口|协议|契约|抽象))",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:version[- ]agnostic|future[- ]proof|stable\s+(?:api|interface|protocol|contract|abstraction))\b|"
            r"(?:版本无关|面向未来|稳定的?(?:接口|协议|契约|抽象))",
            re.IGNORECASE,
        ),
    ),
    (
        "portability",
        re.compile(
            r"\b(?:cross[- ]platform|platform[- ]independent|portable\s+(?:across|between))\b|"
            r"(?:跨平台|平台无关|可移植)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:cross[- ]platform|platform[- ]independent|portable\s+(?:across|between))\b|"
            r"(?:跨平台|平台无关|可移植)",
            re.IGNORECASE,
        ),
    ),
    (
        "introduction",
        re.compile(
            r"\b(?:introduc(?:e|es|ed|ing)|add(?:s|ed|ing)|debut(?:s|ed)?|first\s+appeared)\b|"
            r"(?:引入|新增|首次出现)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:introduc(?:e|es|ed|ing)|add(?:s|ed|ing)|debut(?:s|ed)?|first\s+appeared)\b|"
            r"(?:引入|新增|首次出现)",
            re.IGNORECASE,
        ),
    ),
    (
        "path_separator",
        re.compile(
            r"\b(?:path\s+separators?|separator\s+issues?|forward\s+(?:and|or)\s+(?:backward\s+)?slashes?|"
            r"backward\s+(?:and|or)\s+(?:forward\s+)?slashes?|slash\s+direction)\b|"
            r"(?:路径分隔符|正斜杠|反斜杠|斜杠方向)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:path\s+separators?|separator\s+issues?|forward\s+(?:and|or)\s+(?:backward\s+)?slashes?|"
            r"backward\s+(?:and|or)\s+(?:forward\s+)?slashes?|slash\s+direction)\b|"
            r"(?:路径分隔符|正斜杠|反斜杠|斜杠方向)",
            re.IGNORECASE,
        ),
    ),
    (
        "path_join_operator",
        re.compile(
            r"(?:\b(?:slash|division)\s+operator\b|(?:^|\s)/\s+operator\b|(?:^|\s)/(?:\s|$).{0,35}\bjoin(?:s|ed|ing)?\b|"
            r"\bjoin(?:s|ed|ing)?\b.{0,35}(?:^|\s)/(?:\s|$))|"
            r"(?:/\s*运算符|斜杠运算符).{0,30}(?:拼接|连接)",
            re.IGNORECASE,
        ),
        re.compile(
            r"(?:\b(?:slash|division)\s+operator\b|(?:^|\s)/\s+operator\b|(?:^|\s)/(?:\s|$).{0,35}\bjoin(?:s|ed|ing)?\b|"
            r"\bjoin(?:s|ed|ing)?\b.{0,35}(?:^|\s)/(?:\s|$)|"
            r"(?:Path|PurePath|PurePosixPath|PureWindowsPath)\s*\([^\r\n)]*\)\s*/\s*[^\r\n]+)|"
            r"(?:/\s*运算符|斜杠运算符).{0,30}(?:拼接|连接)",
            re.IGNORECASE,
        ),
    ),
    (
        "definition",
        re.compile(
            r"\b(?:defines?|is\s+defined\s+as|formally\s+specifies?)\b|(?:定义为?|正式规定)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:defines?|is\s+defined\s+as|formally\s+specifies?)\b|(?:定义为?|正式规定)",
            re.IGNORECASE,
        ),
    ),
    (
        "replacement",
        re.compile(
            r"\b(?:replace(?:s|d|ment)?|supersede(?:s|d)?|consolidat(?:e|es|ed|ing)\s+(?:the\s+)?"
            r"(?:functionality|functions?|apis?|modules?))\b|"
            r"(?:替代|取代|整合).{0,40}(?:功能|模块|接口)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:replace(?:s|d|ment)?|supersede(?:s|d)?|consolidat(?:e|es|ed|ing)\s+(?:the\s+)?"
            r"(?:functionality|functions?|apis?|modules?))\b|"
            r"(?:替代|取代|整合).{0,40}(?:功能|模块|接口)",
            re.IGNORECASE,
        ),
    ),
    (
        "centralization",
        re.compile(
            r"\b(?:centrali[sz](?:e|es|ed|ing|ation)|single\s+coherent\s+(?:api|interface)|"
            r"unified\s+(?:api|interface|namespace))\b|(?:集中|统一).{0,30}(?:API|接口|命名空间|路径操作)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:centrali[sz](?:e|es|ed|ing|ation)|single\s+coherent\s+(?:api|interface)|"
            r"unified\s+(?:api|interface|namespace))\b|(?:集中|统一).{0,30}(?:API|接口|命名空间|路径操作)",
            re.IGNORECASE,
        ),
    ),
    (
        "priority",
        re.compile(
            r"\b(?:highest[- ]leverage|most important|top priority|primary discipline|"
            r"foundational\s+(?:design\s+)?(?:decision|principle|choice)|critical\s+design\s+decision)\b|"
            r"(?:最高杠杆|最重要|首要优先|基础性(?:设计)?(?:决策|原则|选择)|关键设计决策)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:highest[- ]leverage|most important|top priority|primary discipline|"
            r"foundational\s+(?:design\s+)?(?:decision|principle|choice)|critical\s+design\s+decision)\b|"
            r"(?:最高杠杆|最重要|首要优先|基础性(?:设计)?(?:决策|原则|选择)|关键设计决策)",
            re.IGNORECASE,
        ),
    ),
    (
        "equivalence",
        re.compile(
            r"\b(?:the\s+same\s+(?:call|api|method|interface)\s+surfaces?|"
            r"regardless\s+of\s+whether|identical\s+(?:behavior|interface|semantics?))\b|"
            r"(?:相同的?(?:调用|API|方法|接口)(?:表面|界面)?|无论是否|完全相同的?(?:行为|接口|语义))",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:the\s+same\s+(?:call|api|method|interface)\s+surfaces?|"
            r"regardless\s+of\s+whether|identical\s+(?:behavior|interface|semantics?))\b|"
            r"(?:相同的?(?:调用|API|方法|接口)(?:表面|界面)?|无论是否|完全相同的?(?:行为|接口|语义))",
            re.IGNORECASE,
        ),
    ),
    (
        "categorization",
        re.compile(
            r"\b(?:occup(?:y|ies)\s+(?:its\s+own|a\s+separate|a\s+distinct)\s+category|"
            r"belongs?\s+in\s+(?:its\s+own|a\s+separate|a\s+distinct)\s+category)\b|"
            r"(?:自成一类|属于独立类别|单独的类别)",
            re.IGNORECASE,
        ),
        re.compile(
            r"\b(?:occup(?:y|ies)\s+(?:its\s+own|a\s+separate|a\s+distinct)\s+category|"
            r"belongs?\s+in\s+(?:its\s+own|a\s+separate|a\s+distinct)\s+category)\b|"
            r"(?:自成一类|属于独立类别|单独的类别)",
            re.IGNORECASE,
        ),
    ),
)
_ARCHITECT_EVIDENCE_VERIFICATION_META_RE = re.compile(
    r"\b(?:verified|verification(?:-backed)?)\s+"
    r"(?:answer|claim|conclusion|evidence|excerpt|fact|finding|material|plan|record|recommendation|"
    r"section|source|synthesis)\b|"
    r"\b(?:answer|claim|conclusion|evidence|excerpt|fact|finding|material|plan|record|recommendation|"
    r"section|source|synthesis)\s+(?:has\s+been|is|was|were)\s+verified\b|"
    r"(?:已|经|经过|由\s*Runtime\s*)?(?:核验|验证)(?:过|的)?"
    r"(?:答案|声明|结论|证据|摘录|事实|发现|材料|计划|记录|建议|章节|来源|综合判断)|"
    r"(?:答案|声明|结论|证据|摘录|事实|发现|材料|计划|记录|建议|章节|来源|综合判断)"
    r"(?:已|经|经过)?(?:核验|验证)",
    re.IGNORECASE,
)
_ARCHITECT_BARE_TECHNICAL_IDENTIFIERS = {
    "argparse",
    "click",
    "glob",
    "os",
    "pathlib",
    "shutil",
    "typer",
}
_ARCHITECT_NON_API_DOTTED_SUFFIXES = {
    "ai",
    "cn",
    "com",
    "csv",
    "de",
    "doc",
    "docx",
    "edu",
    "eu",
    "fr",
    "gov",
    "htm",
    "html",
    "io",
    "json",
    "md",
    "net",
    "org",
    "pdf",
    "ppt",
    "pptx",
    "py",
    "txt",
    "uk",
    "xls",
    "xlsx",
    "xml",
    "yaml",
    "yml",
}


def _architect_dotted_identifier_is_api(value: Any) -> bool:
    compact = _safe_text(value).strip("`'\".,;:()[]{}")
    if "." not in compact:
        return True
    return compact.rsplit(".", 1)[-1].lower() not in _ARCHITECT_NON_API_DOTTED_SUFFIXES


def _architect_hard_assertion_anchors(statement: Any) -> list[tuple[str, str]]:
    raw = _URL_IN_TEXT_RE.sub(" ", _safe_text(statement))
    raw = re.sub(r"\[(?:S\d+)\]", " ", raw, flags=re.IGNORECASE)
    anchors: list[tuple[str, str]] = []

    def add(kind: str, value: Any) -> None:
        normalized = _normalized_evidence_text(value).strip("`'\".,;:()[]{}")
        if len(normalized) < 2:
            return
        item = (kind, normalized)
        if item not in anchors:
            anchors.append(item)

    for match in re.finditer(r"```(?:[a-z0-9_+.-]+)?\s*([\s\S]*?)```", raw, re.IGNORECASE):
        code = match.group(1).strip()
        if code:
            add("code", code)
    without_blocks = re.sub(r"```[\s\S]*?```", " ", raw)
    inline_prose_tokens = {
        "and",
        "but",
        "for",
        "from",
        "into",
        "not",
        "only",
        "operator",
        "or",
        "the",
        "this",
        "to",
        "with",
    }
    for value in re.findall(r"`([^`\r\n]{2,160})`", without_blocks):
        compact_value = value.strip()
        normalized_value = _normalized_evidence_text(compact_value)
        if not normalized_value or normalized_value in inline_prose_tokens:
            continue
        if re.search(r"\s", compact_value):
            if re.search(r"[=()\[\]{}]", compact_value) or re.match(
                r"^(?:with|for|if|return|import|from|class|def)\b",
                compact_value,
                re.IGNORECASE,
            ):
                add("code", compact_value)
            continue
        if re.fullmatch(
            r"(?:--[A-Za-z0-9][A-Za-z0-9_-]*|[A-Za-z_][A-Za-z0-9_]*(?:\.[A-Za-z_][A-Za-z0-9_]*)*"
            r"(?:\([^`]*\))?|[A-Za-z_][A-Za-z0-9_]*\s*=\s*[^`]+)",
            compact_value,
        ) and _architect_dotted_identifier_is_api(compact_value):
            add("api", compact_value)
    without_inline = re.sub(r"`[^`\r\n]*`", " ", without_blocks)
    for value in re.findall(r"(?<![\w-])--[a-z0-9][a-z0-9_-]*", without_inline, re.IGNORECASE):
        add("api", value)
    # Keep an assignment as one atomic API assertion even when the model did
    # not use backticks. Checking only the left-hand option and right-hand
    # class separately lets `path_type=Path` pass against an excerpt that only
    # documents the default `path_type=None` signature.
    for value in re.findall(
        r"\b[A-Za-z_][A-Za-z0-9_]*\s*=\s*[A-Za-z_][A-Za-z0-9_]*(?:\.[A-Za-z_][A-Za-z0-9_]*)*\b",
        without_inline,
    ):
        add("api", value)
    for value in re.findall(r"\b[A-Za-z_][A-Za-z0-9_]*\s*\(\s*\)", without_inline):
        add("api", value)
    for value in re.findall(
        r"\b(?:built[- ]in\s+)?([A-Za-z_][A-Za-z0-9_]*)\s+function\b",
        without_inline,
        re.IGNORECASE,
    ):
        add("api", value)
    for value in re.findall(r"\b[A-Za-z_][A-Za-z0-9_]*(?:\.[A-Za-z_][A-Za-z0-9_]*)+\b", without_inline):
        if _architect_dotted_identifier_is_api(value):
            add("api", value)
    bare_identifiers = {
        value.lower()
        for value in re.findall(r"\b[A-Za-z_][A-Za-z0-9_]*\b", without_inline)
        if value.lower() in _ARCHITECT_BARE_TECHNICAL_IDENTIFIERS
    }
    if len(bare_identifiers) >= 2:
        for value in sorted(bare_identifiers):
            add("api", value)
    for value in re.findall(r"\b[A-Za-z][A-Za-z0-9]*_[A-Za-z0-9_]+\b", without_inline):
        add("api", value)
    for value in re.findall(r"\b[A-Z][a-z0-9]+(?:[A-Z][A-Za-z0-9]*)+\b", without_inline):
        add("api", value)
    without_headings = re.sub(
        r"(?m)^\s*#{1,6}\s+[^\r\n]*$",
        " ",
        without_inline,
    )
    for value in re.findall(
        r"\b(?:article|annex|chapter)\s+"
        r"(?:\d+[a-z]?(?:\s*[-–—]\s*\d+[a-z]?)?|[ivxlcdm]+)"
        r"(?=$|[^a-z0-9])",
        without_headings,
        re.IGNORECASE,
    ):
        add("structured", value)
    for value in re.findall(
        r"\b\d{1,2}\s+(?:january|february|march|april|may|june|july|august|"
        r"september|october|november|december)\s+\d{4}\b",
        without_headings,
        re.IGNORECASE,
    ):
        add("date", value)
    for value in re.findall(
        r"\b(?:python\s+|v)?\d+\.\d+(?:\.\d+|\.x)?\b",
        without_headings,
        re.IGNORECASE,
    ):
        add("version", value)
    for value in _ARCHITECT_QUALIFIED_VERSION_RE.findall(without_headings):
        add("version", value)
    for value in re.findall(
        r"\bv\d+(?:\.\d+){0,2}(?:\s*(?:\+|or\s+higher|and\s+above))?(?=$|[^A-Za-z0-9])",
        without_headings,
        re.IGNORECASE,
    ):
        add("version", value)
    for value in _ARCHITECT_UNDATED_MONTH_DAY_RE.findall(without_headings):
        add("date", value)
    for value in _ARCHITECT_QUANTIFIED_EFFECT_RE.findall(without_headings):
        add("quantity", value)
    for value in _ARCHITECT_DURATION_ANCHOR_RE.findall(without_headings):
        add("duration", value)
    return anchors


def _architect_temporal_boundary_note(value: Any) -> str:
    """Describe a date precision limit without inventing a publication year."""

    text = _safe_text(value)
    if not _ARCHITECT_UNDATED_MONTH_DAY_RE.search(text):
        return ""
    if re.search(r"\b(?:19|20)\d{2}\b", text):
        return ""
    return "source_month_day_without_year; do_not_infer_current_year"


def _architect_hard_assertion_issues(
    statement: Any,
    excerpt: Any,
    *,
    source_metadata: dict[str, Any] | None = None,
) -> list[str]:
    statement_text = _safe_text(statement)
    excerpt_text = _safe_text(excerpt)
    normalized_excerpt = _normalized_evidence_text(_URL_IN_TEXT_RE.sub(" ", excerpt_text))
    normalized_qualified_excerpt = re.sub(r"\s*\.\s*", ".", normalized_excerpt)
    normalized_qualified_excerpt = re.sub(r"\s*=\s*", "=", normalized_qualified_excerpt)
    normalized_code_excerpt = re.sub(
        r"\s*([.=(),:\[\]{}])\s*",
        r"\1",
        normalized_excerpt,
    )
    metadata = source_metadata if isinstance(source_metadata, dict) else {}
    normalized_metadata = _normalized_evidence_text(
        " ".join(
            _safe_text(metadata.get(key))
            for key in ("title", "url", "version", "publishedAt", "updatedAt", "sourceDate")
        )
    )
    issues: list[str] = []
    if excerpt_text and _architect_evidence_fragment_is_truncated(excerpt_text):
        issues.append("evidence_excerpt_truncated")
    for kind, anchor in _architect_hard_assertion_anchors(statement_text):
        supported = (
            re.sub(r"\s*([.=(),:\[\]{}])\s*", r"\1", anchor) in normalized_code_excerpt
            if kind == "code"
            else anchor in normalized_qualified_excerpt
        )
        if kind == "api" and "." in anchor and not supported:
            parts = [part for part in anchor.split(".") if part]
            namespace = parts[0] if len(parts) >= 2 else ""
            terminal = parts[-1] if len(parts) >= 2 else ""
            supported = bool(
                namespace
                and terminal
                and (
                    (
                        re.search(rf"\b{re.escape(namespace)}\b", normalized_metadata)
                        and re.search(rf"\b{re.escape(terminal)}\b", normalized_excerpt)
                    )
                    or re.search(
                        rf"\bfrom\s+{re.escape(namespace)}\s+import\s+[^;]{{0,160}}\b{re.escape(terminal)}\b",
                        normalized_excerpt,
                    )
                )
            )
        if kind == "api" and re.fullmatch(r"[a-z_][a-z0-9_]*", anchor):
            supported = bool(
                re.search(rf"\b{re.escape(anchor)}\b", normalized_qualified_excerpt)
                or re.search(rf"\b{re.escape(anchor)}\b", normalized_metadata)
            )
        if kind == "version":
            supported = supported or anchor in normalized_metadata
        if kind in {"structured", "date", "duration"}:
            supported = supported or anchor in normalized_metadata
        if not supported:
            issues.append(f"{kind}_anchor_not_in_evidence:{anchor[:80]}")

    for qualifier, statement_pattern, evidence_pattern in (
        (
            "non_binding",
            r"\b(?:non[- ]binding|not legally binding)\b|(?:不具约束力|非约束性)",
            r"\b(?:non[- ]binding|not legally binding)\b|(?:不具约束力|非约束性)",
        ),
        (
            "exclusive_authority",
            r"\b(?:sole|exclusive)\s+(?:authority|competence|jurisdiction)\b|(?:唯一|排他).{0,8}(?:权限|管辖|执法权)",
            r"\b(?:sole|exclusive)\s+(?:authority|competence|jurisdiction)\b|(?:唯一|排他).{0,8}(?:权限|管辖|执法权)",
        ),
        (
            "exception",
            r"\b(?:exception|exemption|exempt(?:ed|ion)?)\b|(?:例外|豁免)",
            r"\b(?:exception|exemption|exempt(?:ed|ion)?)\b|(?:例外|豁免)",
        ),
    ):
        if re.search(statement_pattern, statement_text, re.IGNORECASE) and not re.search(
            evidence_pattern,
            f"{excerpt_text}\n{normalized_metadata}",
            re.IGNORECASE,
        ):
            issues.append(f"{qualifier}_not_entailed")

    if _ARCHITECT_NEGATIVE_CAPABILITY_RE.search(statement_text) and not _ARCHITECT_NEGATIVE_CAPABILITY_RE.search(
        excerpt_text
    ):
        issues.append("negative_capability_not_entailed")

    statement_metrics = {
        name for name, pattern in _ARCHITECT_PERFORMANCE_METRICS if pattern.search(statement_text)
    }
    if statement_metrics and _ARCHITECT_PERFORMANCE_DIRECTION_RE.search(statement_text):
        excerpt_metrics = {
            name for name, pattern in _ARCHITECT_PERFORMANCE_METRICS if pattern.search(excerpt_text)
        }
        if not _ARCHITECT_PERFORMANCE_DIRECTION_RE.search(excerpt_text) or not statement_metrics.issubset(excerpt_metrics):
            issues.append("performance_effect_not_entailed")
    if _ARCHITECT_STRONG_MAGNITUDE_RE.search(statement_text) and not (
        _ARCHITECT_STRONG_MAGNITUDE_RE.search(excerpt_text)
        or _ARCHITECT_QUANTIFIED_EFFECT_RE.search(excerpt_text)
    ):
        issues.append("magnitude_not_entailed")
    behavior_statement_text = re.sub(r"```[\s\S]*?```", " ", statement_text)
    behavior_statement_text = re.sub(
        r"(?m)^\s*#{1,6}\s+[^\r\n]*$",
        " ",
        behavior_statement_text,
    )
    behavior_statement_text = _ARCHITECT_EVIDENCE_VERIFICATION_META_RE.sub(
        " ", behavior_statement_text
    )
    for relation_name, statement_pattern, evidence_pattern in _ARCHITECT_BEHAVIOR_RELATIONS:
        if statement_pattern.search(behavior_statement_text) and not evidence_pattern.search(excerpt_text):
            issues.append(f"{relation_name}_behavior_not_entailed")
    if (
        re.search(
            r"\bpure\s+paths?\b.{0,140}(?:without|do(?:es)?\s+not|don['’]t|no)"
            r".{0,80}(?:filesystem|file system|I/O|input/output)|"
            r"\bpure\s+paths?\b.{0,120}\bpurely computational\b",
            behavior_statement_text,
            re.IGNORECASE | re.DOTALL,
        )
        and not re.search(
            r"\bpure\s+paths?\b.{0,180}(?:don['’]t|do(?:es)?\s+not|without|no)"
            r".{0,100}(?:access\s+(?:a\s+)?filesystem|filesystem access|file system|I/O|input/output)|"
            r"\bpurely computational operations?\b.{0,100}\bwithout\s+I/O\b",
            excerpt_text,
            re.IGNORECASE | re.DOTALL,
        )
    ):
        issues.append("pure_path_no_filesystem_access_not_entailed")
    if (
        re.search(
            r"\b(?:pathlib\.)?Path\b.{0,120}\b(?:provides?|includes?|exposes?|has)\b"
            r".{0,140}\b(?:read_text|write_text|is_file|is_dir|exists|open)\s*\(",
            behavior_statement_text,
            re.IGNORECASE | re.DOTALL,
        )
        and not re.search(
            r"docs\.python\.org/(?:3|\d+(?:\.\d+)?)/library/pathlib\.html",
            normalized_metadata,
            re.IGNORECASE,
        )
        and not re.search(
            r"\b(?:pathlib\.)?Path(?:\s+class)?\b.{0,160}"
            r"\b(?:provides?|includes?|exposes?|methods?)\b",
            excerpt_text,
            re.IGNORECASE | re.DOTALL,
        )
    ):
        issues.append("api_ownership_not_entailed")
    return list(dict.fromkeys(issues))


def _architect_normative_assertion_supported(
    statement: Any,
    supporting_claims: list[dict[str, Any]],
) -> bool:
    text = _architect_assertion_text(statement)
    if not _ARCHITECT_NORMATIVE_ASSERTION_RE.search(text):
        return True
    for claim in supporting_claims:
        if _safe_text(claim.get("claimType")).lower() != "explicit_normative":
            continue
        cue = _safe_text(claim.get("normativeCue"))
        excerpt = _safe_text(claim.get("evidenceExcerpt"))
        if cue and _normalized_evidence_text(cue) in _normalized_evidence_text(excerpt):
            return True
    return False


def _architect_support_role(support: Any) -> str:
    if not isinstance(support, dict):
        return "unknown"
    if support.get("runtimeOfficialSeed") is True:
        return "primary"
    authority_tier = _safe_text(support.get("authorityTier")).lower()
    catalog_category = _safe_text(support.get("catalogCategory")).lower()
    if authority_tier == "primary" and catalog_category in {
        "official_docs",
        "official_blog",
        "public_institution",
        "standards",
    }:
        return "primary"
    tier = _safe_text(support.get("tier") or authority_tier).lower()
    if tier in {"primary", "official", "first_party", "first-party", "a"}:
        return "primary"
    if tier in {
        "secondary",
        "tertiary",
        "community",
        "tutorial",
        "editorial",
        "b",
        "c",
    }:
        return "secondary"
    authority = _as_int(support.get("authorityScore"), 0)
    if authority >= 75:
        return "primary"
    if authority > 0:
        return "secondary"
    return "unknown"


def _architect_support_has_stable_current_route(support: Any) -> bool:
    """Recognize stable/current route context only for primary sources."""

    if not isinstance(support, dict) or _architect_support_role(support) != "primary":
        return False
    identity_text = " ".join(
        (
            _safe_text(support.get("title")),
            _safe_text(support.get("url") or support.get("sourceUrl")),
        )
    )
    return bool(
        re.search(
            r"/(?:latest|stable|current)(?:/|\b)|docs\.python\.org/3/",
            identity_text,
            re.IGNORECASE,
        )
    )


def _architect_support_has_current_applicability(support: Any) -> bool:
    """Return explicit applicability context without inferring it from age.

    A publication/update date is evidence about when a document was authored,
    not a deterministic statement that its contents are current.  The
    independent Reviewer receives that date separately and decides whether it
    is adequate for the concrete claim and question.
    """

    if not isinstance(support, dict):
        return False
    temporal = (
        support.get("temporalEvidence")
        if isinstance(support.get("temporalEvidence"), dict)
        else {}
    )
    status = _safe_text(
        support.get("temporalStatus")
        or support.get("applicabilityStatus")
        or temporal.get("status")
        or temporal.get("applicabilityStatus")
    ).lower()
    applicability_basis = _safe_text(
        support.get("applicabilityBasis") or temporal.get("applicabilityBasis")
    ).lower()
    if status in {
        "current",
        "current_applicable",
        "explicit_current_applicability",
        "stable_current",
    }:
        return True
    if (
        "stable_current" in applicability_basis
        or "current_documentation_route" in applicability_basis
        or "explicit_current" in applicability_basis
    ):
        return True
    return _architect_support_has_stable_current_route(support)


def _architect_support_has_temporal_context(support: Any) -> bool:
    """Return whether a Reviewer receives clear, non-future time context."""

    if not isinstance(support, dict):
        return False
    if _architect_support_has_current_applicability(support):
        return True
    temporal = (
        support.get("temporalEvidence")
        if isinstance(support.get("temporalEvidence"), dict)
        else {}
    )
    if _safe_text(
        support.get("version")
        or support.get("applicableVersion")
        or temporal.get("version")
        or temporal.get("applicableVersion")
    ):
        return True
    now_utc = datetime.now(timezone.utc)
    candidate_values = [
        value for _kind, value in research_document_date_candidates(support)
    ]
    read_evidence = (
        support.get("readEvidence")
        if isinstance(support.get("readEvidence"), dict)
        else {}
    )
    candidate_values.append(
        support.get("retrievedAt")
        or temporal.get("retrievedAt")
        or read_evidence.get("retrievedAt")
    )
    for raw_value in candidate_values:
        normalized = _normalize_document_date(raw_value)
        if not normalized:
            continue
        try:
            parsed = datetime.fromisoformat(normalized.replace("Z", "+00:00"))
        except ValueError:
            continue
        if parsed.tzinfo is None:
            parsed = parsed.replace(tzinfo=timezone.utc)
        if parsed.astimezone(timezone.utc) <= now_utc + timedelta(days=1):
            return True
    return False


def _architect_runtime_temporal_assessment(sources: list[dict[str, Any]]) -> dict[str, Any]:
    assessments: list[dict[str, Any]] = []
    counts = {
        "applicabilityContext": 0,
        "dated": 0,
        "versionBoundedOnly": 0,
        "undated": 0,
        "futureAnomaly": 0,
        "malformedDate": 0,
    }
    now_utc = datetime.now(timezone.utc)
    for source in sources[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]:
        temporal = source.get("temporalEvidence") if isinstance(source.get("temporalEvidence"), dict) else {}
        version = _safe_text(source.get("version") or temporal.get("version"))
        normalized_dates = [
            _normalize_document_date(value)
            for _kind, value in research_document_date_candidates(source)
        ]
        parsed_dates: list[datetime] = []
        malformed_dates: list[str] = []
        for normalized in normalized_dates:
            if not normalized:
                continue
            try:
                parsed = datetime.fromisoformat(normalized.replace("Z", "+00:00"))
            except ValueError:
                malformed_dates.append(normalized)
                continue
            if parsed.tzinfo is None:
                parsed = parsed.replace(tzinfo=timezone.utc)
            parsed_dates.append(parsed.astimezone(timezone.utc))
        non_future_dates = [
            value for value in parsed_dates if value <= now_utc + timedelta(days=1)
        ]
        future_dates = [
            value for value in parsed_dates if value > now_utc + timedelta(days=1)
        ]
        stable_current_route = _architect_support_has_stable_current_route(source)
        explicit_applicability_context = bool(
            _architect_support_has_current_applicability(source)
            and not stable_current_route
        )
        if malformed_dates:
            status = "malformed_date"
            counts["malformedDate"] += 1
        elif parsed_dates and not non_future_dates:
            status = "future_anomaly"
            counts["futureAnomaly"] += 1
        elif explicit_applicability_context:
            status = "explicit_current_applicability"
            counts["applicabilityContext"] += 1
        elif stable_current_route:
            status = "stable_route_observed"
            counts["applicabilityContext"] += 1
        elif non_future_dates:
            status = "dated_context"
            counts["dated"] += 1
        elif version:
            status = "version_bounded"
            counts["versionBoundedOnly"] += 1
        else:
            status = "undated"
            counts["undated"] += 1
        assessments.append(
            {
                "citationKey": _safe_text(source.get("citationKey")).strip("[]"),
                "sourceRole": _architect_support_role(source),
                "status": status,
                "stableCurrentRouteObserved": stable_current_route,
                "applicabilityRequiresReviewerJudgment": True,
                **(
                    {
                        "applicabilityBasis": (
                            "explicit_current_applicability"
                            if explicit_applicability_context
                            else "stable_current_primary_route"
                        )
                    }
                    if explicit_applicability_context or stable_current_route
                    else {}
                ),
                "documentDateStatus": (
                    "malformed"
                    if malformed_dates
                    else "future_anomaly"
                    if parsed_dates and not non_future_dates
                    else "present_with_future_anomaly"
                    if non_future_dates and future_dates
                    else "present"
                    if non_future_dates
                    else "absent"
                ),
                **({"version": version} if version else {}),
                **({"documentDates": normalized_dates} if normalized_dates else {}),
                **({"malformedDocumentDates": malformed_dates} if malformed_dates else {}),
            }
        )
    return {
        "asOf": now_utc.isoformat().replace("+00:00", "Z"),
        "retrievalTimeIsNotDocumentDate": True,
        "fixedDocumentAgeCutoffApplied": False,
        "applicabilityRequiresReviewerJudgment": True,
        "counts": counts,
        "sources": assessments,
    }


def _architect_source_role_issues(
    statement: Any,
    supporting_claims: list[dict[str, Any]],
) -> list[str]:
    text = _safe_text(statement)
    assertion_text = _architect_assertion_text(text)
    has_normative_claim = bool(_ARCHITECT_NORMATIVE_ASSERTION_RE.search(assertion_text))
    has_current_or_official_claim = bool(_ARCHITECT_CURRENT_OR_OFFICIAL_RE.search(assertion_text))
    explicitly_secondary_context = bool(
        _ARCHITECT_SOURCE_ROLE_ATTRIBUTION_RE.match(text)
        or re.match(
            r"^\s*(?:[-*+]\s+)?(?:Historical secondary commentary|Secondary (?:source|tutorial|guide|blog|article)|"
            r"According to (?:the )?(?:tutorial|guide|blog|article)|The (?:tutorial|guide|blog|article) (?:states|shows|reports|argues)|"
            r"历史二手材料|二手(?:来源|教程|指南|博客|文章)|据(?:该|这篇)?(?:教程|指南|博客|文章))\b",
            text,
            re.IGNORECASE,
        )
    )
    supports = [
        support
        for claim in supporting_claims
        for support in list(claim.get("supportingSources") or [])
        if isinstance(support, dict)
    ]
    issues: list[str] = []
    primary_supports = [
        support
        for support in supports
        if _architect_support_role(support) == "primary"
    ]
    secondary_supports = [
        support
        for support in supports
        if _architect_support_role(support) == "secondary"
    ]
    if secondary_supports and not explicitly_secondary_context:
        issues.append("secondary_source_role_required")
    if not has_normative_claim and not has_current_or_official_claim:
        return issues
    if not primary_supports and (
        has_current_or_official_claim or not explicitly_secondary_context
    ):
        issues.append("primary_source_role_required")
    if has_current_or_official_claim:
        temporal_context = any(
            _architect_support_has_temporal_context(support)
            for support in primary_supports
        )
        if not temporal_context:
            issues.append("current_document_evidence_required")
    return issues


def _architect_govern_claim_source_roles_for_question(
    claims: list[dict[str, Any]],
    question: Any,
) -> tuple[list[dict[str, Any]], list[str]]:
    """Keep official-source requests from inheriting tutorial prescriptions.

    Secondary sources can still contribute attributed facts and examples.  An
    explicit recommendation from a tutorial remains valuable experience, but
    it must stay attributed to that source rather than becoming an official
    rule.  The writer separately derives any general synthesis from primary
    facts.
    """

    requires_primary_normative_evidence = bool(
        re.search(
            r"\b(?:official|primary|first[- ]party|authoritative)\s+(?:sources?|documentation|docs|evidence)\b|"
            r"(?:官方|一手|权威)(?:来源|资料|文档|证据)",
            _safe_text(question),
            re.IGNORECASE,
        )
    )
    if not requires_primary_normative_evidence:
        return list(claims), []

    kept: list[dict[str, Any]] = []
    governed_secondary_ids: list[str] = []
    for claim in claims:
        supports = [
            support
            for support in list(claim.get("supportingSources") or [])
            if isinstance(support, dict)
        ]
        has_primary_support = any(
            _architect_support_role(support) == "primary" for support in supports
        )
        if has_primary_support:
            kept.append(claim)
            continue
        claim_text = _safe_text(claim.get("claim"))
        if not supports:
            kept.append(claim)
            continue
        claim_id = _safe_text(claim.get("claimId")) or "unknown"
        governed_secondary_ids.append(claim_id)
        if _ARCHITECT_SECONDARY_CONTEXT_RE.search(claim_text):
            kept.append(
                {
                    **claim,
                    "sourceClaim": _safe_text(claim.get("sourceClaim")) or claim_text,
                    "sourceRole": "secondary",
                }
            )
            continue
        source_title = _compact_research_text(supports[0].get("title"), limit=100)
        if re.search(r"[\u4e00-\u9fff]", claim_text):
            prefix = (
                f"二手来源《{source_title}》的表述："
                if source_title
                else "二手来源的表述："
            )
        else:
            prefix = (
                f"Secondary source “{source_title}” states: "
                if source_title
                else "Secondary source states: "
            )
        kept.append(
            {
                **claim,
                "claim": _compact_research_text(f"{prefix}{claim_text}", limit=420),
                "sourceClaim": claim_text,
                "sourceRole": "secondary",
            }
        )
    return kept, governed_secondary_ids


def _verify_architect_claim_excerpts(
    claims: Any,
    sources: list[dict[str, Any]],
    *,
    require_evidence_key: bool = False,
) -> tuple[list[dict[str, Any]], list[str]]:
    aliases: dict[str, dict[str, Any]] = {}
    evidence_by_key: dict[str, tuple[dict[str, Any], str, dict[str, Any]]] = {}
    for source in sources:
        if not isinstance(source, dict):
            continue
        for value in (source.get("url"), source.get("sourceId"), source.get("citationKey")):
            alias = _safe_text(value)
            if alias:
                aliases[alias] = source
                aliases[alias.strip("[]")] = source
        for candidate in list(source.get("evidenceCandidates") or []):
            if not isinstance(candidate, dict):
                continue
            key = _safe_text(candidate.get("evidenceExcerptKey"))
            text = _safe_text(candidate.get("text"))
            if key and text:
                evidence_by_key[key] = (source, text, candidate)

    verified: list[dict[str, Any]] = []
    issues: list[str] = []
    for index, raw_claim in enumerate(
        _research_dict_list(claims, limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT),
        start=1,
    ):
        excerpt_key = _safe_text(raw_claim.get("evidenceExcerptKey"))
        excerpt = _safe_text(raw_claim.get("evidenceExcerpt"))
        supporting = list(raw_claim.get("supportingSources") or [])[:4]
        matched_sources: list[dict[str, Any]] = []
        for support in supporting:
            values = (
                (support.get("url"), support.get("sourceUrl"), support.get("sourceId"), support.get("citationKey"), support.get("citation"))
                if isinstance(support, dict)
                else (support,)
            )
            for value in values:
                alias = _safe_text(value)
                source = aliases.get(alias) or aliases.get(alias.strip("[]"))
                if source and source not in matched_sources:
                    matched_sources.append(source)
        keyed_evidence = evidence_by_key.get(excerpt_key)
        keyed_candidate: dict[str, Any] = {}
        if require_evidence_key and not excerpt_key:
            issues.append(f"claim_{index}_evidence_key_required")
            continue
        if require_evidence_key and not keyed_evidence:
            issues.append(f"claim_{index}_evidence_key_unknown")
            continue
        if keyed_evidence:
            keyed_source, keyed_excerpt, keyed_candidate = keyed_evidence
            if keyed_source not in matched_sources:
                issues.append(f"claim_{index}_evidence_key_source_mismatch")
                continue
            excerpt = keyed_excerpt
            matched_sources = [keyed_source]
        normalized_excerpt = _normalized_evidence_text(excerpt)
        excerpt_sources = [
            source
            for source in matched_sources
            if normalized_excerpt
            and normalized_excerpt in _normalized_evidence_text(source.get("text") or source.get("evidenceText"))
        ]
        excerpt_matches = bool(20 <= len(excerpt) <= 600 and len(normalized_excerpt) >= 20 and excerpt_sources)
        if "query-focused excerpt" in normalized_excerpt:
            excerpt_matches = False
        if not excerpt_matches:
            issues.append(f"claim_{index}_evidence_excerpt_not_in_supporting_source")
            continue
        # One exact excerpt proves one source binding. Additional declared
        # supports need their own claims/excerpts and cannot inflate coverage.
        matched_sources = excerpt_sources[:1]
        claim = _compact_research_text(raw_claim.get("claim") or raw_claim.get("summary"), limit=320)
        # Keep accepted facts source-near when a planner substituted a stronger
        # relation word for an otherwise valid excerpt. This prevents semantic
        # drift from later becoming an endless reviewer argument over words
        # such as "defines", "unifies", or "adds".
        if (
            re.search(r"\bPath objects?.{0,80}/ operator.{0,80}(?:string|new Path)", claim, re.IGNORECASE)
            and re.search(r"\bA Path object can be used with\s*/", excerpt, re.IGNORECASE)
            and re.search(r"\bnew Path object\b", excerpt, re.IGNORECASE)
        ):
            claim = (
                "A Path object can be combined with a string using / to create "
                "a new Path object."
            )
        elif (
            re.search(r"\bpure paths?.{0,100}concrete paths?.{0,100}(?:add|provide).{0,30}I/O", claim, re.IGNORECASE)
            and re.search(r"pure paths.{0,100}purely computational operations without I/O", excerpt, re.IGNORECASE | re.DOTALL)
            and re.search(r"concrete paths.{0,100}inherit from pure paths.{0,100}also provide I/O operations", excerpt, re.IGNORECASE | re.DOTALL)
        ):
            claim = (
                "Pure paths provide purely computational operations without I/O; "
                "concrete paths inherit from pure paths and also provide I/O operations."
            )
        elif (
            re.search(r"\bpathlib\b.{0,80}\bunif(?:y|ies)\b.{0,120}\b(?:os|glob|shutil)\b", claim, re.IGNORECASE)
            and re.search(
                r"pathlib also brings together functionality previously spread across other libraries like\s+os\s*,\s*glob\s*,\s*and\s*shutil",
                excerpt,
                re.IGNORECASE,
            )
        ):
            claim = (
                "pathlib brings together functionality previously spread across "
                "os, glob, and shutil, making file operations more straightforward."
            )
        elif (
            re.search(
                r"\bargparse\b.{0,100}\b(?:supports?|uses?)\b.{0,80}\bpathlib\.Path\b.{0,80}"
                r"\b(?:type|converter|parameter)\b",
                claim,
                re.IGNORECASE,
            )
            and re.search(
                r"\btype\s*=\s*pathlib\s*\.\s*Path\b",
                excerpt,
                re.IGNORECASE,
            )
        ):
            claim = (
                "argparse's documented type example passes pathlib.Path as the "
                "converter for a CLI path argument."
            )
        elif (
            re.search(
                r"\bargparse\b.{0,100}\bdefault recommended standard library module\b.{0,100}"
                r"\bbasic (?:command[- ]line|CLI) applications?\b",
                claim,
                re.IGNORECASE,
            )
            and re.search(
                r"While argparse is the default recommended standard library module.{0,240}"
                r"authors with more exacting requirements.{0,220}doesn['’]t provide the necessary level of control",
                excerpt,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            claim = (
                "argparse is the default recommended standard library module for "
                "implementing basic command-line applications; more exacting behavior "
                "requirements may call for a parser with finer-grained control."
            )
        elif (
            re.search(
                r"\bPEP\s*519\b.{0,100}\bupdated\b.{0,120}\b(?:PurePath|Path)\b",
                claim,
                re.IGNORECASE,
            )
            and re.search(
                r"\b(?:PurePath|Path)\b.{0,180}\bwill be updated\b|"
                r"\bwill be updated\b.{0,180}\b(?:PurePath|Path)\b",
                excerpt,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            claim = (
                "PEP 519 proposed updating pathlib.PurePath and pathlib.Path "
                "constructors to accept PathLike objects."
            )
        elif (
            re.search(
                r"\bClick\b.{0,80}\breturns?\b.{0,50}\bfilename\b.{0,40}\b(?:str|string)\b",
                claim,
                re.IGNORECASE,
            )
            and re.search(
                r"\bPath\b.{0,60}\breturns the filename instead of an open file\b",
                excerpt,
                re.IGNORECASE,
            )
            and not re.search(r"\b(?:str|string)\b", excerpt, re.IGNORECASE)
        ):
            # Keep the accepted claim inside the exact excerpt. Another Click
            # excerpt may document the default ``str`` path_type, but this one
            # only establishes filename-versus-open-file behavior.
            claim = "Click's Path type returns the filename instead of an open file."
        elif (
            re.search(
                r"\bClick(?:'s|\.)\s*Path\b.{0,100}\b(?:has|documents?|supports?)\b"
                r".{0,50}\b(?:parameters?|validations?|validation options?)\b",
                claim,
                re.IGNORECASE,
            )
            and all(
                token in normalized_excerpt
                for token in (
                    "exists=false",
                    "file_okay=true",
                    "dir_okay=true",
                    "writable=false",
                    "readable=true",
                    "resolve_path=false",
                    "allow_dash=false",
                    "path_type=none",
                    "executable=false",
                )
            )
        ):
            # A signature is direct evidence for its parameter defaults. Keep
            # the useful values instead of retaining only the parameter names.
            claim = (
                "Click.Path's documented signature sets exists=False, "
                "file_okay=True, dir_okay=True, writable=False, readable=True, "
                "resolve_path=False, allow_dash=False, path_type=None, and "
                "executable=False."
            )
        elif (
            re.search(
                r"\bTyper\b.{0,140}\b(?:Path|pathlib\.Path)\b.{0,160}"
                r"\b(?:type annotation|annotat(?:e|ed|ion)|validation)\b",
                claim,
                re.IGNORECASE,
            )
            and "annotated[path" in normalized_excerpt
            and "typer.option" in normalized_excerpt
            and all(
                token in normalized_excerpt
                for token in (
                    "exists=true",
                    "file_okay=true",
                    "dir_okay=false",
                    "writable=false",
                    "readable=true",
                    "resolve_path=true",
                )
            )
        ):
            claim = (
                "Typer's example annotates the CLI parameter as Path and configures "
                "exists=True, file_okay=True, dir_okay=False, writable=False, "
                "readable=True, and resolve_path=True through typer.Option."
            )
        elif (
            re.match(
                r"^It will give you a directory for storing configurations appropriate "
                r"for your CLI program for the current user in each operating system\.?$",
                claim,
                re.IGNORECASE,
            )
            and re.search(r"typer\s*\.\s*get_app_dir", excerpt, re.IGNORECASE)
        ):
            claim = (
                "Typer's get_app_dir() provides an operating-system-appropriate directory "
                "for storing per-user CLI configuration."
            )
        elif (
            re.search(
                r"\bPath\b.{0,80}\b(?:provides?|has)\b.{0,60}\bmethods?\b",
                claim,
                re.IGNORECASE,
            )
            and all(
                re.search(rf"\b{re.escape(method)}\s*\(\s*\)", claim, re.IGNORECASE)
                for method in ("exists", "is_dir", "open")
            )
            and all(
                re.search(rf"\.\s*{re.escape(method)}\s*\(", excerpt, re.IGNORECASE)
                for method in ("exists", "is_dir", "open")
            )
        ):
            # Executable official examples establish the supported calls. Use
            # source-near wording instead of asking a reviewer to infer the
            # Python descriptor category from example syntax.
            claim = (
                "The official pathlib example calls exists(), is_dir(), and "
                "open() on a Path object."
            )
        elif (
            re.match(
                r"^And it will create a new Path object from that\.?$",
                claim,
                re.IGNORECASE,
            )
            and re.search(
                r"first element is a Path object.{0,120}after the /.+can be str.{0,120}"
                r"create a new Path object",
                excerpt,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            # The source splits the antecedent and result across two sentences.
            # Keep the verified fact self-contained when a planner selects only
            # the second sentence.
            claim = (
                "When the first element is a Path object, following str elements "
                "can be combined with / to create a new Path object."
            )
        if re.match(r"^(?:And it|Otherwise|It will)\b", claim, re.IGNORECASE):
            issues.append(f"claim_{index}_unresolved_anaphora")
            continue
        temporal_boundary = _architect_temporal_boundary_note(excerpt)
        if temporal_boundary and any(
            year not in excerpt
            for year in re.findall(r"\b(?:19|20)\d{2}\b", claim)
        ):
            issues.append(f"claim_{index}_source_month_day_year_inferred")
            continue
        claim_type = _safe_text(raw_claim.get("claimType") or raw_claim.get("claimKind")).lower()
        if claim_type not in {"source_fact", "explicit_normative"}:
            claim_type = "source_fact"
        normative_claim = bool(
            re.search(
                r"\b(?:recommended|required|recommends?|requires?|prefer(?:s|red)?|best practice|should|must|ought|"
                r"avoid|do not|never|canonical way|standard mechanism|officially endorsed)\b|"
                r"官方.{0,8}(?:推荐|要求)|推荐采用|建议(?:使用|将)|首选|最佳实践|应当|不应|必须|不得|标准机制",
                claim,
                re.IGNORECASE,
            )
        )
        normative_cue = _safe_text(raw_claim.get("normativeCue"))
        normalized_cue = _normalized_evidence_text(normative_cue)
        normative_cue_valid = bool(
            3 <= len(normative_cue) <= 160
            and normalized_cue in normalized_excerpt
            and re.search(
                r"\b(?:recommended|required|recommends?|requires?|prefer(?:s|red)?|best practice|should|must|ought|"
                r"avoid|do not|never|use|canonical way|standard mechanism|officially endorsed)\b|"
                r"官方.{0,8}(?:推荐|要求)|推荐采用|建议(?:使用|将)|首选|最佳实践|应当|不应|必须|不得|标准机制",
                normative_cue,
                re.IGNORECASE,
            )
        )
        if claim_type == "source_fact" and normative_claim:
            if normative_cue_valid:
                claim_type = "explicit_normative"
            else:
                issues.append(f"claim_{index}_source_fact_contains_normative_attribution")
                continue
        if claim_type == "explicit_normative" and not normative_cue_valid:
            issues.append(f"claim_{index}_normative_cue_not_in_evidence")
            continue
        negative_normative_pattern = (
            r"\b(?:not|never|avoid|forbid(?:s|den)?|prohibit(?:s|ed)?|must\s+not|should\s+not|"
            r"shall\s+not|may\s+not|do\s+not)\b|不(?:应|得|该|建议|推荐)|禁止|避免"
        )
        if claim_type == "explicit_normative" and bool(
            re.search(negative_normative_pattern, normative_cue, re.IGNORECASE)
        ) != bool(re.search(negative_normative_pattern, claim, re.IGNORECASE)):
            issues.append(f"claim_{index}_normative_polarity_mismatch")
            continue
        hard_assertion_issues = _architect_hard_assertion_issues(
            claim,
            excerpt,
            source_metadata=matched_sources[0] if matched_sources else None,
        )
        if hard_assertion_issues:
            issues.extend(f"claim_{index}_{issue}" for issue in hard_assertion_issues)
            continue
        canonical_supporting = []
        for source in matched_sources:
            support = {
                "sourceId": _safe_text(source.get("sourceId")),
                "title": _safe_text(source.get("title")),
                "url": _safe_text(source.get("url")),
                "citationKey": _safe_text(source.get("citationKey")),
            }
            for key in (
                "researchFacetId",
                "researchFacetIds",
                "researchFacetGoal",
                "evidenceQuery",
                "tier",
                "authorityTier",
                "authorityScore",
                "catalogCategory",
                "catalogSourceId",
                "runtimeOfficialSeed",
                "subjectFocused",
                "publishedAt",
                "updatedAt",
                "sourceDate",
                "sourceDateKind",
                "version",
                "temporalEvidence",
            ):
                value = source.get(key)
                if value not in (None, "", [], {}):
                    support[key] = value
            keyed_facet_id = _safe_text(keyed_candidate.get("researchFacetId"))
            if keyed_facet_id and source is matched_sources[0]:
                support["researchFacetId"] = keyed_facet_id
                support["researchFacetIds"] = [keyed_facet_id]
                keyed_facet_goal = _safe_text(
                    keyed_candidate.get("researchFacetGoal")
                )
                if keyed_facet_goal:
                    support["researchFacetGoal"] = keyed_facet_goal
            canonical_supporting.append(support)
        canonical_roles = {
            _architect_support_role(support) for support in canonical_supporting
        }
        canonical_source_role = (
            "secondary"
            if canonical_roles == {"secondary"}
            else "primary"
            if canonical_roles == {"primary"}
            else "mixed"
            if canonical_roles
            else "unknown"
        )
        verified.append(
            {
                **raw_claim,
                "claim": claim,
                "claimType": claim_type,
                "sourceRole": canonical_source_role,
                **(
                    {
                        "authorityBoundary": (
                            "attributed_secondary_opinion_not_official_rule"
                            if claim_type == "explicit_normative"
                            else "attributed_secondary_fact_or_experience"
                        )
                    }
                    if canonical_source_role == "secondary"
                    else {}
                ),
                **({"normativeCue": normative_cue} if normative_cue else {}),
                # S# labels are prompt-local and can change after a repair round.
                # Persist canonical source identity so final merge/review stays bound.
                "supportingSources": canonical_supporting,
                **({"evidenceExcerptKey": excerpt_key} if excerpt_key else {}),
                "evidenceExcerpt": excerpt,
                # The acceptance layer validates the stored, whitespace-
                # normalized excerpt bytes. Semantic matching above may
                # additionally unescape Markdown, but the persisted proof hash
                # must remain a digest of the exact visible excerpt contract.
                "evidenceExcerptSha256": hashlib.sha256(
                    re.sub(r"\s+", " ", excerpt).strip().lower().encode("utf-8")
                ).hexdigest(),
                **(
                    {
                        "temporalBoundary": temporal_boundary
                    }
                    if temporal_boundary
                    else {}
                ),
                "evidenceVerified": True,
            }
        )
    return verified, issues


def _independent_review_structured_boolean(
    field: str,
    value: Any,
    *,
    review: dict[str, Any],
) -> bool | None:
    """Adapt explicit rich reviewer assessments without guessing intent.

    Some otherwise valid provider responses expand the requested booleans into
    a coverage object or a per-claim decision list.  Treat only mechanically
    checkable positive/negative shapes as booleans.  Unknown prose remains
    invalid so an ``accept`` string alone can never bypass the review gate.
    """

    if isinstance(value, bool):
        return value
    decision = _safe_text(review.get("reviewDecision")).lower()
    failure_fields = (
        "unsupportedClaims",
        "criticalMissingEvidence",
        "recommendedNextQueries",
    )
    failure_free = all(
        isinstance(review.get(name), list) and not review.get(name)
        for name in failure_fields
    )

    positive_status = re.compile(
        r"^(?:supported(?:_[a-z0-9_]+)?|entailed|adequate|covered|complete|"
        r"pass(?:ed)?|accept(?:ed)?)$",
        re.IGNORECASE,
    )
    negative_status = re.compile(
        r"^(?:unsupported|not[_ -]?supported|not[_ -]?entailed|contradicted|"
        r"inadequate|uncovered|incomplete|fail(?:ed)?|reject(?:ed)?|missing)$",
        re.IGNORECASE,
    )

    def explicit_outcome(item: Any) -> bool | None:
        if isinstance(item, bool):
            return item
        if isinstance(item, str):
            status = item.strip()
            if positive_status.fullmatch(status):
                return True
            if negative_status.fullmatch(status):
                return False
            return None
        if not isinstance(item, dict):
            return None
        for key in (
            "entailed",
            "supported",
            "covered",
            "adequate",
            "sufficient",
            "complete",
            "passed",
            "accepted",
            "allEntailed",
        ):
            if isinstance(item.get(key), bool):
                return item[key]
        for key in ("decision", "status", "result"):
            status = _safe_text(item.get(key))
            if positive_status.fullmatch(status):
                return True
            if negative_status.fullmatch(status):
                return False
        return None

    if field == "claimEntailment" and isinstance(value, list):
        if not value:
            return None
        outcomes = [explicit_outcome(item) for item in value]
        if any(outcome is False for outcome in outcomes):
            return False
        if all(outcome is True for outcome in outcomes):
            return True
        return None

    if isinstance(value, dict):
        explicit_boolean_values = [
            item
            for key, item in value.items()
            if isinstance(item, bool)
            and (
                key.lower().startswith("cover")
                or key
                in {
                    "entailed",
                    "supported",
                    "adequate",
                    "sufficient",
                    "complete",
                    "passed",
                    "accepted",
                    "allEntailed",
                }
            )
        ]
        if any(item is False for item in explicit_boolean_values):
            return False
        if explicit_boolean_values:
            return True

        if field == "claimEntailment" and value:
            outcomes = [explicit_outcome(item) for item in value.values()]
            if any(outcome is False for outcome in outcomes):
                return False
            if all(outcome is True for outcome in outcomes):
                return True
            return None

        if field == "questionCoverage":
            answered = value.get("answeredAspects")
            unanswered = value.get("unansweredAspects")
            if isinstance(answered, list) and answered and isinstance(unanswered, list):
                tolerated_boundary = re.compile(
                    r"deliberately excluded|acceptable|not a gap|out of scope|"
                    r"not required|defensible|有意排除|可以接受|不构成缺口|不在范围",
                    re.IGNORECASE,
                )
                if any(
                    not tolerated_boundary.search(_safe_text(item))
                    for item in unanswered
                ):
                    return False
                if decision == "accept" and failure_free:
                    return True
        outcome = explicit_outcome(value)
        if outcome is not None:
            return outcome
    return None


def _independent_review_reason_is_explicitly_nonblocking(value: Any) -> bool:
    reason = _safe_text(value).strip()
    if not reason:
        return True
    if re.search(
        r"\b(?:not|isn't|aren't)\s+(?:correctly|properly|appropriately|fully)\b|"
        r"\b(?:incorrectly|inaccurately|not acceptable|unacceptable)\b|"
        r"(?:并非|没有|未能)(?:正确|恰当|充分)|不可接受",
        reason,
        re.IGNORECASE,
    ):
        return False
    minor_nonblocking = re.search(
        r"\bminor overstatement\b.{0,160}\bnot (?:a )?(?:critical|blocking|material) "
        r"unsupported claim\b|"
        r"\bnot (?:a )?(?:critical|blocking|material) (?:unsupported claim|issue|gap)\b|"
        r"(?:轻微夸大|轻微过度表述).{0,100}(?:不是|不构成)(?:关键|阻断性|实质性)"
        r"(?:无依据主张|问题|缺口)",
        reason,
        re.IGNORECASE | re.DOTALL,
    )
    explicitly_positive = bool(
        minor_nonblocking
        or re.search(
            r"\bno (?:issue|problem|gap|material concern)s?\b|"
            r"\bacceptable\b|"
            r"\b(?:acceptable|appropriately|correctly|properly) "
            r"(?:attributed|bounded|labeled|supported|scoped)\b|"
            r"\b(?:directly|fully|sufficiently) support(?:s|ed)?\b|"
            r"(?:没有|不存在)(?:问题|缺口|实质性疑虑)|"
            r"(?:归因|边界|标注|支撑|范围)(?:正确|恰当|充分)|可以接受",
            reason,
            re.IGNORECASE,
        )
    )
    if not explicitly_positive:
        return False
    remainder = reason
    if minor_nonblocking:
        remainder = remainder.replace(minor_nonblocking.group(0), " ")
    remainder = re.sub(
        r"\bno (?:issue|problem|gap|material concern)s?\b|"
        r"\bnot (?:a )?(?:critical|blocking|material) (?:unsupported claim|issue|gap)\b|"
        r"(?:没有|不存在)(?:问题|缺口|实质性疑虑)|"
        r"(?:不是|不构成)(?:关键|阻断性|实质性)(?:无依据主张|问题|缺口)",
        " ",
        remainder,
        flags=re.IGNORECASE,
    )
    return not bool(
        re.search(
            r"\b(?:truncat\w*|fragment\w*|mid-word|ocr|unreadable|readability|"
            r"garbled|incomplete|uncited|missing|lacks?|"
            r"contradict\w*|inconsisten\w*|incorrect|inaccurate|unsupported|"
            r"unverified|retry|reject|failed|should|needs?|revise|rewrite)\b|"
            r"(?:截断|残句|乱码|不可读|未引用|缺少|缺失|矛盾|不一致|错误|不准确|"
            r"无依据|未验证|重试|拒绝|失败|需要修订|需要重写)",
            remainder,
            re.IGNORECASE,
        )
    )


def _adapt_independent_architect_review_schema(value: Any) -> dict[str, Any]:
    if not isinstance(value, dict):
        return {}
    adapted = dict(value)
    raw_decision = _safe_text(value.get("reviewDecision")).lower().replace("-", "_")
    if raw_decision == "accept_with_warnings":
        reasons = _research_text_list(value.get("reviewReasons"), limit=12)
        blocking_warning = re.compile(
            r"\b(?:however|but|warning|unsupported|missing|incorrect|inaccurate|"
            r"incomplete|unclear|should|needs?|tighten|trim|revise|fix)\b|"
            r"(?:但是|然而|警告|不支持|缺少|错误|不准确|不完整|不清楚|应当|需要|收紧|精简|修订|修复)",
            re.IGNORECASE,
        )
        strict_positive_contract = bool(
            all(value.get(field) is True for field in (
                "questionCoverage",
                "claimEntailment",
                "freshnessAdequacy",
            ))
            and all(
                isinstance(value.get(field), list) and not value.get(field)
                for field in (
                    "unsupportedClaims",
                    "criticalMissingEvidence",
                    "recommendedNextQueries",
                )
            )
            and not any(blocking_warning.search(reason) for reason in reasons)
        )
        if strict_positive_contract:
            adapted["reviewDecision"] = "accept"
            adapted["providerReviewDecision"] = "accept_with_warnings"
    for field in ("questionCoverage", "claimEntailment", "freshnessAdequacy"):
        outcome = _independent_review_structured_boolean(
            field,
            value.get(field),
            review=value,
        )
        if outcome is not None:
            adapted[field] = outcome
    if (
        raw_decision in {"retry", "reject"}
        and all(
            value.get(field) is True
            for field in ("questionCoverage", "claimEntailment", "freshnessAdequacy")
        )
        and all(
            type(value.get(field)) is list and not value.get(field)
            for field in (
                "unsupportedClaims",
                "criticalMissingEvidence",
                "recommendedNextQueries",
            )
        )
        and type(value.get("reviewReasons")) is list
        and bool(value.get("reviewReasons"))
        and all(
            _independent_review_reason_is_explicitly_nonblocking(reason)
            for reason in value.get("reviewReasons")
        )
        and not any(
            value.get(field)
            for field in (
                "warnings",
                "blockingIssues",
                "citationIssues",
                "attributionIssues",
                "temporalIssues",
                "scopeViolations",
                "unverifiedClaims",
            )
        )
        and value.get("outputTruncated") is not True
        and _safe_text(value.get("finishReason")).lower()
        not in {"length", "max_tokens", "incomplete", "content_filter"}
    ):
        # The typed fields are the review contract. A provider occasionally
        # emits a stale retry/reject enum while every native typed outcome is
        # positive, every blocker surface is explicitly empty, and its reasons
        # are themselves explicitly non-blocking. Preserve the raw enum for
        # diagnostics without accepting unknown readability/citation failures.
        adapted["reviewDecision"] = "accept"
        adapted["providerReviewDecision"] = raw_decision
        adapted["providerReviewReasons"] = list(value.get("reviewReasons") or [])
        adapted["reviewSchemaConsistencyRepair"] = (
            "positive_fields_override_conflicting_decision"
        )
    return adapted


def _normalize_independent_architect_review(value: Any) -> dict[str, Any]:
    value = _adapt_independent_architect_review_schema(value)
    if not isinstance(value, dict):
        return {}
    decision = _safe_text(value.get("reviewDecision")).lower()
    if decision not in {"accept", "retry", "reject"}:
        return {}
    normalized = {
        "reviewDecision": decision,
        **(
            {"providerReviewDecision": _safe_text(value.get("providerReviewDecision"))}
            if _safe_text(value.get("providerReviewDecision"))
            else {}
        ),
        **(
            {
                "reviewSchemaConsistencyRepair": _safe_text(
                    value.get("reviewSchemaConsistencyRepair")
                )
            }
            if _safe_text(value.get("reviewSchemaConsistencyRepair"))
            else {}
        ),
        **(
            {
                "providerReviewReasons": [
                    _safe_text(reason)
                    for reason in list(value.get("providerReviewReasons") or [])[:12]
                    if _safe_text(reason)
                ]
            }
            if isinstance(value.get("providerReviewReasons"), list)
            else {}
        ),
        "reviewReasons": _research_text_list(value.get("reviewReasons"), limit=12),
        "questionCoverage": value.get("questionCoverage") is True,
        "claimEntailment": value.get("claimEntailment") is True,
        "freshnessAdequacy": value.get("freshnessAdequacy") is True,
        "unsupportedClaims": _research_text_list(value.get("unsupportedClaims"), limit=12),
        "criticalMissingEvidence": _research_text_list(value.get("criticalMissingEvidence"), limit=12),
        "recommendedNextQueries": _research_text_list(value.get("recommendedNextQueries"), limit=4),
        "bindingVersion": value.get("bindingVersion"),
        "questionFingerprint": _safe_text(value.get("questionFingerprint")),
        "answerSha256": _safe_text(value.get("answerSha256")),
        "claimDigest": _safe_text(value.get("claimDigest")),
        "sourceDigest": _safe_text(value.get("sourceDigest")),
        "temporalDigest": _safe_text(value.get("temporalDigest")),
        "reviewerModelId": _safe_text(value.get("reviewerModelId")),
        "reviewedAt": _safe_text(value.get("reviewedAt")),
        **({"reviewMode": _safe_text(value.get("reviewMode"))} if _safe_text(value.get("reviewMode")) else {}),
    }
    consensus_reviews = [
        _normalize_independent_architect_review(review)
        for review in list(value.get("consensusReviews") or [])[:4]
        if isinstance(review, dict)
    ]
    consensus_reviews = [review for review in consensus_reviews if review]
    if consensus_reviews or "consensusAccepted" in value:
        try:
            consensus_count = int(value.get("consensusReviewCount") or 0)
        except (TypeError, ValueError):
            consensus_count = 0
        normalized.update(
            {
                "consensusAccepted": value.get("consensusAccepted") is True,
                "consensusReviewCount": consensus_count,
                "consensusReviewerModelIds": [
                    _safe_text(model_id)
                    for model_id in list(value.get("consensusReviewerModelIds") or [])[:4]
                    if _safe_text(model_id)
                ],
                "consensusReviews": consensus_reviews,
            }
        )
    return normalized


def _architect_reclassify_review_evidence_gaps(
    review: dict[str, Any],
    *,
    question: str,
) -> dict[str, Any]:
    """Keep exact-official-wording requests from forcing unnecessary search.

    A question can ask for a synthesis that cites official sources without
    claiming that one official page states the final recommendation verbatim.
    In that case, a reviewer may still reject the writing or inference, but it
    must classify the problem as same-evidence revision rather than inventing
    a critical evidence gap. Questions explicitly asking what an official
    source recommends retain the stricter requirement.
    """

    if not isinstance(review, dict):
        return {}
    asks_for_official_normative_wording = bool(
        re.search(
            r"(?:what|which).{0,60}(?:official|documentation|docs).{0,60}"
            r"(?:recommend|require|prefer)|"
            r"(?:official(?:ly)?|official\s+(?:documentation|docs?)).{0,20}"
            r"(?:recommended|required|preferred)|"
            r"(?:官方|权威)(?:文档|资料|来源)?.{0,16}(?:明确)?(?:推荐|要求|首选)(?:什么|哪些|哪种|的)",
            _safe_text(question),
            re.IGNORECASE,
        )
    )
    if asks_for_official_normative_wording:
        return dict(review)

    exact_wording_gap = re.compile(
        r"\bexplicit[_ -]normative\b|"
        r"\bS\d+\b.{0,100}\b(?:carries?|contains?|has|provides?)\b.{0,60}"
        r"\b(?:cue|evidence|wording|phrase|premise)\b|"
        r"(?:official|first[- ]party|authoritative).{0,80}"
        r"(?:best[- ]practices?|guidance|recommend(?:ation|ed|s|ing)?|preferred|"
        r"prescript(?:ive|ion)|prescrib(?:e|es|ed|ing)|explicitly framing)|"
        r"(?:best[- ]practices?|guidance|recommend(?:ation|ed|s|ing)?|preferred|"
        r"prescript(?:ive|ion)|prescrib(?:e|es|ed|ing)).{0,80}"
        r"(?:official|first[- ]party|authoritative)|"
        r"(?:docs?|documentation|tutorial|changelog|page|passage|sources?).{0,160}"
        r"(?:best[- ]practices?|recommend(?:ation|ed|s|ing)?|preferred|"
        r"prescript(?:ive|ion)|prescrib(?:e|es|ed|ing)|explicitly framing)|"
        r"(?:best[- ]practices?|recommend(?:ation|ed|s|ing)?|preferred|"
        r"prescript(?:ive|ion)|prescrib(?:e|es|ed|ing)|explicitly framing).{0,160}"
        r"(?:docs?|documentation|tutorial|changelog|page|passage|sources?)|"
        r"\b(?:no|none|not|lacks?|without|does\s+not|do\s+not|did\s+not)\b.{0,140}"
        r"(?:normative|prescriptive|prescrib(?:e|es|ed|ing)|recommend(?:ation|ed|s|ing)?|"
        r"best[- ]practices?|prefer(?:red|s)?)|"
        r"(?:standards?[- ]body|steering\s+council|core\s+developers?|maintainers?)"
        r".{0,140}(?:guidance|recommend(?:ation|ed|s|ing)?|canonical|normative|"
        r"best[- ]practices?|endors(?:e|es|ed|ement)|contract)|"
        r"(?:normative\s+(?:standing|contract)|canonical\s+(?:contract|protocol|guidance))|"
        r"\bcurrent(?:ly supported)?\s+(?:version|release).{0,60}"
        r"(?:best[- ]practices?\s+(?:guide|guidance)|guidance|recommendation)\b|"
        r"(?:官方|一手|权威)(?:文档|资料|来源)?.{0,50}(?:最佳实践|指南|建议|推荐|首选)|"
        r"(?:最佳实践|指南|建议|推荐|首选).{0,50}(?:官方|一手|权威)|"
        r"当前版本.{0,40}最佳实践(?:指南|建议|指导)|"
        r"(?:未|没有|缺少|不含|未出现).{0,80}(?:规范性|明确)?(?:建议|推荐|首选|最佳实践|措辞)",
        re.IGNORECASE,
    )
    same_evidence_delivery_gap = re.compile(
        r"\bnot\s+(?:systematically\s+)?delivered\b|\bnot\s+consolidated\b|"
        r"\b(?:missing|absent|omitted)\s+from\s+(?:the\s+)?(?:answer|report|body|section|table)\b|"
        r"\b(?:answer|report|body|section|table|comparison|guidance|recommendations?)\b"
        r".{0,100}\b(?:missing|absent|omitted|not\s+(?:included|provided|organized|segmented|mapped))\b|"
        r"\bno\s+explicit\s+deliverable\b|"
        r"(?:答案|报告|正文|章节|表格|对比|比较|建议).{0,60}(?:缺少|未给出|未组织|未汇总|未整合|未分组|未呈现)|"
        r"(?:缺少|未给出|未呈现).{0,60}(?:答案|报告|正文|章节|表格)",
        re.IGNORECASE,
    )
    critical = _research_text_list(review.get("criticalMissingEvidence"), limit=12)
    reason_only_candidates = (
        _research_text_list(review.get("reviewReasons"), limit=12)
        if not critical
        else []
    )
    reclassified = [
        item
        for item in critical
        if exact_wording_gap.search(item) or same_evidence_delivery_gap.search(item)
    ]
    reclassified.extend(
        item
        for item in reason_only_candidates
        if (
            exact_wording_gap.search(item)
            or same_evidence_delivery_gap.search(item)
        )
        and item not in reclassified
    )
    if not reclassified:
        return dict(review)
    kept = [item for item in critical if item not in reclassified]
    normalized = {
        **review,
        "criticalMissingEvidence": kept,
        "recommendedNextQueries": (
            _research_text_list(review.get("recommendedNextQueries"), limit=4)
            if kept
            else []
        ),
        "reviewReasons": list(
            dict.fromkeys(
                [
                    *_research_text_list(review.get("reviewReasons"), limit=12),
                    "Exact official prescriptive wording is not required for a clearly labeled cross-source synthesis; revise the answer or attribution using the existing evidence.",
                ]
            )
        )[:12],
        "reclassifiedSameEvidenceIssues": reclassified,
    }
    return normalized


def _architect_partial_plan_evidence_ready(
    plan: dict[str, Any],
    *,
    required_claim_sources: list[str],
    required_facet_ids: list[str],
    prompt_sources: list[dict[str, Any]],
) -> bool:
    """Allow useful partial synthesis only after evidence is already broad.

    The Architect may label an unanswered subpoint as globally critical even
    when the read bundle can answer the rest of a multi-part question.  This
    guard only keeps planning alive; every retained claim still has to pass the
    exact-excerpt, facet-coverage, citation, writer, and independent-review
    gates below.
    """

    if not isinstance(plan, dict):
        return False
    raw_claims = _research_dict_list(
        plan.get("claimTable"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    )
    if len(raw_claims) < TARGET_RESEARCH_CLAIM_COUNT:
        return False
    if len(set(required_claim_sources)) < TARGET_RESEARCH_SOURCE_COUNT:
        return False
    available_facet_ids = {
        _safe_text(facet_id)
        for source in prompt_sources
        if isinstance(source, dict) and list(source.get("evidenceCandidates") or [])
        for facet_id in [
            *list(source.get("researchFacetIds") or []),
            source.get("researchFacetId"),
        ]
        if _safe_text(facet_id)
    }
    required = {_safe_text(facet_id) for facet_id in required_facet_ids if _safe_text(facet_id)}
    return not required or required.issubset(available_facet_ids)


def _architect_reconcile_review_with_answer_surface(
    review: dict[str, Any],
    *,
    question: str,
    candidate_answer: str,
    claim_table: list[dict[str, Any]] | None = None,
) -> dict[str, Any]:
    """Discard only reviewer absence claims disproved by the visible answer.

    This is deliberately narrower than a second semantic reviewer. It cannot
    overrule an unsupported API/fact claim or an actual freshness conflict. It
    only fences a known long-answer reviewer failure mode: reporting that an
    exact label or historical boundary is absent even though that exact surface
    is present in the candidate and the allegedly current guidance excludes the
    historical citations.
    """

    if not isinstance(review, dict):
        return {}
    normalized = dict(review)
    verified_claims = [
        claim for claim in list(claim_table or []) if isinstance(claim, dict)
    ]

    def guidance_section() -> str:
        match = re.search(
            r"(?ims)^##\s+(?:Evidence-backed practical guidance|基于证据的可执行实践指南)\s*$"
            r"(?P<body>.*?)"
            r"(?=^##\s+(?:Facts and usages directly supported by primary sources|一手资料直接支持的事实与用法)\s*$|\Z)",
            candidate_answer,
        )
        return match.group("body") if match else ""

    def named_subsection(pattern: str) -> str:
        match = re.search(
            rf"(?ims)^###\s+(?:{pattern})\s*$"
            r"(?P<body>.*?)"
            r"(?=^###\s+|^##\s+|\Z)",
            candidate_answer,
        )
        return match.group("body") if match else ""

    guidance = guidance_section()
    conversion_validation_section = named_subsection(
        r"Type conversion and input validation|类型转换与输入验证"
    )
    integration_section = named_subsection(
        r"CLI parsing and framework integration|CLI 解析与框架接入"
    )
    configuration_section = named_subsection(
        r"Application configuration locations|应用配置目录"
    )
    choice_section = named_subsection(
        r"Choose by CLI context, not by an unconditional library ranking|按 CLI 场景选择，而不是做无条件库排名"
    )
    path_semantics_section = named_subsection(
        r"Path semantics, filesystem operations, and historical boundaries|路径语义、文件系统操作与历史边界"
    )
    path_protocol_section = named_subsection(
        r"Path protocol and API compatibility boundary|路径协议与 API 兼容边界"
    )
    direct_match = re.search(
        r"(?ims)^##\s+(?:Direct answer(?: and interpretation rule)?|直接回答(?:与判断规则)?)\s*$"
        r"(?P<body>.*?)"
        r"(?=^##\s+|\Z)",
        candidate_answer,
    )
    direct_section = direct_match.group("body") if direct_match else ""
    framework_scoped_direct_synthesis_present = bool(
        (
            (
                re.search(
                    r"When that framework exposes documented validation options|"
                    r"当该框架确实提供已记录的校验选项",
                    direct_section,
                    re.IGNORECASE,
                )
                and re.search(
                    r"cross-source synthesis of separately verified contracts|"
                    r"对分别核验合同的跨来源综合",
                    direct_section,
                    re.IGNORECASE,
                )
                and re.search(
                    r"not a claim that any one source prescribes a universal workflow|"
                    r"不声称任何单一来源规定了通用工作流",
                    direct_section,
                    re.IGNORECASE,
                )
            )
            or (
                re.search(
                    r"Best-practice implementation pattern|最佳实践实施模式",
                    direct_section,
                    re.IGNORECASE,
                )
                and re.search(
                    r"action sequence above is this report's engineering recommendation|"
                    r"上述动作顺序是本报告.{0,40}工程建议",
                    direct_section,
                    re.IGNORECASE,
                )
                and re.search(
                    r"not a claim that one source published a universal cross-framework rule|"
                    r"不是把任一来源改写成跨框架统一规则",
                    direct_section,
                    re.IGNORECASE,
                )
            )
        )
        and len(set(re.findall(r"\[(S\d+)\]", direct_section, re.IGNORECASE))) >= 2
    )
    argparse_example_scope_present = bool(
        re.search(r"`?type=pathlib\.Path`?", choice_section, re.IGNORECASE)
        and (
            (
                re.search(
                    r"asserts only the parser configuration visible in that example|"
                    r"只陈述示例展示的解析器配置",
                    choice_section,
                    re.IGNORECASE,
                )
                and re.search(
                    r"downstream Path operations are supported by separate primary Path sources|"
                    r"后续 Path 操作由独立的 Path 一手资料支撑",
                    choice_section,
                    re.IGNORECASE,
                )
            )
            or re.search(
                r"argparse example shows that exact setting.{0,160}"
                r"does not rewrite the example as an official recommendation|"
                r"argparse 示例明确展示该配置.{0,160}不把示例改写成官方推荐",
                choice_section,
                re.IGNORECASE | re.DOTALL,
            )
        )
    )
    attributed_secondary_advice_visible = bool(
        re.search(
            r"Secondary source.{0,240}You should use Path objects anywhere you work with file paths|"
            r"二手来源.{0,240}应该在处理文件路径的任何地方使用 Path",
            integration_section,
            re.IGNORECASE | re.DOTALL,
        )
    )
    visible_contextual_practice_answer = bool(
        choice_section
        and all(
            re.search(rf"\b{framework}\b", choice_section, re.IGNORECASE)
            for framework in ("argparse", "Click", "Typer")
        )
        and len(set(re.findall(r"\[(S\d+)\]", choice_section, re.IGNORECASE))) >= 3
        and re.search(r"Practical synthesis|本报告的综合判断", choice_section, re.IGNORECASE)
        and re.search(
            r"Existing argparse application:.{0,500}(?:documented|use)|"
            r"已采用 argparse.{0,500}(?:官方|使用)",
            choice_section,
            re.IGNORECASE | re.DOTALL,
        )
        and re.search(
            r"Existing Click application:.{0,500}(?:use|Path)|"
            r"已采用 Click.{0,500}(?:使用|Path)",
            choice_section,
            re.IGNORECASE | re.DOTALL,
        )
        and re.search(
            r"Existing Typer application:.{0,500}(?:receive|use|Path)|"
            r"已采用 Typer.{0,500}(?:使用|接收|Path)",
            choice_section,
            re.IGNORECASE | re.DOTALL,
        )
        and re.search(
            r"convert at the CLI input boundary and keep using Path downstream|"
            r"selected framework's input boundary produces a Path.{0,180}keep using that Path|"
            r"在 CLI 输入边界完成转换.{0,80}后续处理继续使用 Path|"
            r"在所选框架的输入边界得到 Path 后.{0,180}继续使用 Path",
            integration_section,
            re.IGNORECASE | re.DOTALL,
        )
    )
    argparse_example_boundary_present = bool(
        re.search(
            r"Existing argparse application.{0,500}documented example.{0,300}"
            r"does not rewrite it as an official recommendation|"
            r"Existing argparse application.{0,500}argparse example shows that exact setting.{0,300}"
            r"does not rewrite the example as an official recommendation|"
            r"已采用 argparse.{0,500}官方示例.{0,300}不把示例改写成官方推荐",
            choice_section,
            re.IGNORECASE | re.DOTALL,
        )
        and re.search(r"\[S\d+\]", choice_section, re.IGNORECASE)
    )
    verified_argparse_normative_scope = any(
        re.search(
            r"argparse is the default recommended standard library module.{0,120}"
            r"implementing basic command[- ]line applications",
            _safe_text(claim.get("claim")),
            re.IGNORECASE,
        )
        and re.search(
            r"default recommended standard library module.{0,120}"
            r"implementing basic command line applications",
            _safe_text(claim.get("evidenceExcerpt")),
            re.IGNORECASE,
        )
        for claim in verified_claims
    )
    argparse_normative_scope_present = bool(
        verified_argparse_normative_scope
        and re.search(
            r"default recommended standard-library module for implementing basic command-line applications|"
            r"用于实现基础命令行应用的默认推荐标准库模块",
            choice_section,
            re.IGNORECASE,
        )
        and re.search(r"\[S\d+\]", choice_section, re.IGNORECASE)
    )
    click_option_application_boundary_present = bool(
        re.search(
            r"The source documents (?:a configurable option|those configurable options).{0,240}"
            r"report's engineering synthesis.{0,180}not a claim about Click's default value or an official preference|"
            r"source records those conversion and validation options.{0,240}"
            r"combining them at a concrete command boundary is this report's engineering synthesis|"
            r"来源记录的是(?:可配置选项|这些可配置选项).{0,240}本报告的工程综合.{0,180}"
            r"不表示 Click 默认(?:值|如此)或官方偏好|"
            r"来源记录这些转换与校验选项.{0,240}把它们组合到具体命令边界是本报告的工程综合",
            choice_section,
            re.IGNORECASE | re.DOTALL,
        )
        and re.search(r"\bClick\b.{0,500}\[S\d+\]", choice_section, re.IGNORECASE | re.DOTALL)
    )
    verified_click_path_setting = any(
        re.search(
            r"Allow passing\s+`?path_type\s*=\s*pathlib\.Path`?",
            _safe_text(claim.get("claim")),
            re.IGNORECASE,
        )
        for claim in verified_claims
    )
    verified_click_path_conversion = any(
        re.search(r"\bpath_type\b", _safe_text(claim.get("claim")), re.IGNORECASE)
        and re.search(
            r"Convert the incoming path value to this type|"
            r"convert(?:s|ed|ing)? an? incoming path value",
            _safe_text(claim.get("claim")),
            re.IGNORECASE,
        )
        for claim in verified_claims
    )
    verified_click_parameter_conversion_claims = [
        claim
        for claim in verified_claims
        if (
        re.search(r"\bpath_type\b", _safe_text(claim.get("claim")), re.IGNORECASE)
        and re.search(
            r"convert(?:s|ed|ing)? (?:the )?incoming path value.{0,100}(?:specified|this) type|"
            r"convert(?:s|ed|ing)?.{0,100}pathlib\.Path",
            _safe_text(claim.get("claim")),
            re.IGNORECASE,
        )
        and re.search(
            r"Convert the incoming path value to this type.{0,220}"
            r"Useful to convert to.{0,100}pathlib\.Path",
            _normalized_evidence_text(claim.get("evidenceExcerpt")),
            re.IGNORECASE | re.DOTALL,
        )
        and any(
            isinstance(support, dict)
            and _architect_support_role(support) == "primary"
            and re.search(
                r"\bClick\b|click\.palletsprojects\.com",
                " ".join(
                    (
                        _safe_text(support.get("title")),
                        _safe_text(support.get("url") or support.get("sourceUrl")),
                    )
                ),
                re.IGNORECASE,
            )
            for support in list(claim.get("supportingSources") or [])
        )
        )
    ]
    verified_click_parameter_conversion_contract = bool(
        verified_click_parameter_conversion_claims
    )
    click_conversion_contract_present = bool(
        (
            verified_click_parameter_conversion_contract
            or (verified_click_path_setting and verified_click_path_conversion)
        )
        and (
            re.search(
                    r"set\s+`?path_type=pathlib\.Path`?.{0,500}allows? this setting.{0,240}"
                r"path_type converts the incoming path value to the specified type.{0,300}"
                r"(?:makes no claim about path_type's default value|"
                r"verified signature records path_type=None)|"
                r"设置 path_type=pathlib\.Path.{0,500}允许该设置.{0,240}"
                r"path_type 会把输入路径值转换为指定类型.{0,300}"
                r"(?:不陈述 path_type 的默认值|已验证签名记录 path_type=None)",
                choice_section,
                re.IGNORECASE | re.DOTALL,
            )
            or (
                verified_click_parameter_conversion_contract
                and re.search(
                    r"(?:Source-backed finding|来源支撑的结论).{0,120}"
                    r"path_type.{0,160}convert.{0,160}pathlib\.Path.{0,80}\[S\d+\]",
                    candidate_answer,
                    re.IGNORECASE | re.DOTALL,
                )
                and re.search(
                    r"(?:configure|设置).{0,120}(?:Click(?:'s)? documented )?"
                    r"Path/?path_type|path_type\s*=\s*pathlib\.Path",
                    choice_section,
                    re.IGNORECASE | re.DOTALL,
                )
            )
        )
        and re.search(r"\bClick\b.{0,900}\[S\d+\]", choice_section, re.IGNORECASE | re.DOTALL)
    )
    choice_synthesis_boundary_present = bool(
        re.search(
            r"Practical synthesis.{0,80}actionable choice|本报告的综合判断.{0,40}可执行选择",
            choice_section,
            re.IGNORECASE,
        )
        and re.search(
            r"recommends neither a framework migration nor a universally superior library.{0,180}"
            r"organizes the separate framework contracts|"
            r"不建议迁移框架.{0,120}不声称某个框架普遍更优.{0,180}各框架独立合同",
            choice_section,
            re.IGNORECASE,
        )
        and len(set(re.findall(r"\[(S\d+)\]", choice_section, re.IGNORECASE))) >= 3
    )
    source_role_boundary_present = bool(
        re.search(
            r"Secondary material and attributed experience|二手资料与署名经验",
            candidate_answer,
            re.IGNORECASE,
        )
        and re.search(
            r"they do not establish an official rule|不承担官方规范证明",
            candidate_answer,
            re.IGNORECASE,
        )
        and re.search(
            r"Secondary source\s+[\"“][^\"”]{1,180}[\"”]\s+states:\s*"
            r"You should use Path objects anywhere you work with file paths(?:\.|。)?\s*\[S\d+\]|"
            r"二手来源《[^》]{1,180}》的表述：.{0,120}应该在处理文件路径的任何地方使用 Path.{0,20}\[S\d+\]",
            guidance,
            re.IGNORECASE | re.DOTALL,
        )
        and attributed_secondary_advice_visible
    )
    dedicated_secondary_boundary_present = bool(
        re.search(
            r"Secondary material and attributed experience|二手资料与署名经验",
            candidate_answer,
            re.IGNORECASE,
        )
        and re.search(
            r"they do not establish an official rule|不承担官方规范证明",
            candidate_answer,
            re.IGNORECASE,
        )
        and re.search(
            r"Attributed statement from secondary source|二手来源《[^》]+》的署名表述",
            candidate_answer,
            re.IGNORECASE,
        )
    )
    secondary_claim_citation_keys = {
        _safe_text(support.get("citationKey") or support.get("citation"))
        .strip("[]")
        .upper()
        for claim in verified_claims
        for support in list(claim.get("supportingSources") or [])
        if isinstance(support, dict)
        and _architect_support_role(support) == "secondary"
        and _safe_text(support.get("citationKey") or support.get("citation"))
    }
    guidance_citation_keys = {
        key.upper()
        for key in re.findall(r"\[(S\d+)\]", guidance, re.IGNORECASE)
    }
    secondary_excluded_from_actionable_guidance = bool(
        secondary_claim_citation_keys
        and not secondary_claim_citation_keys.intersection(guidance_citation_keys)
    )
    attributed_secondary_claim_present = any(
        _safe_text(claim.get("claimType")).lower() == "explicit_normative"
        and re.search(
            r"^Secondary source\s+[\"“][^\"”]{1,180}[\"”]\s+states:\s*"
            r"You should use Path objects anywhere you work with file paths|"
            r"^二手来源《[^》]{1,180}》的表述：.{0,120}应该在处理文件路径的任何地方使用 Path",
            _safe_text(claim.get("claim")),
            re.IGNORECASE | re.DOTALL,
        )
        and any(
            isinstance(support, dict)
            and _architect_support_role(support) == "secondary"
            for support in list(claim.get("supportingSources") or [])
        )
        for claim in verified_claims
    )
    has_verified_conversion_claim = any(
        re.search(
            r"\b(?:type\s*example.{0,50}(?:pathlib\.Path|converter)|path_type|"
            r"convert(?:s|ed|ing)?.{0,50}(?:pathlib\.Path|Path object)|Path[- ]typed argument)\b",
            _safe_text(claim.get("claim")),
            re.IGNORECASE,
        )
        for claim in verified_claims
    )
    has_verified_validation_claim = any(
        re.search(
            r"\b(?:exists|file_okay|dir_okay|readable|writable|resolve_path|"
            r"validat(?:e|es|ed|ing|ion)|permission)\b",
            _safe_text(claim.get("claim")),
            re.IGNORECASE,
        )
        for claim in verified_claims
    )
    conversion_validation_synthesis_present = bool(
        has_verified_conversion_claim
        and has_verified_validation_claim
        and re.search(
            r"Treat 'convert this token to Path' and 'which path values are acceptable' as separate decisions|"
            r"The conversion claim states how path_type or a Path-typed entry determines.{0,260}"
            r"The validation claim enumerates|"
            r"Recommended implementation \(report synthesis\).{0,420}"
            r"Documented basis:.{0,260}conversion claim establishes.{0,260}"
            r"validation claim enumerates|"
            r"把[‘'\"]?转换成 Path[’'\"]?和[‘'\"]?允许什么路径值[’'\"]?作为两个独立决定",
            conversion_validation_section,
            re.IGNORECASE | re.DOTALL,
        )
        and len(
            set(
                re.findall(
                    r"\[(S\d+)\]",
                    conversion_validation_section,
                    re.IGNORECASE,
                )
            )
        )
        >= 2
    )
    conversion_validation_boundary_present = bool(
        conversion_validation_synthesis_present
        and re.search(
            r"Evidence[- ]boundary inference:.{0,420}not presented as a verbatim official negative rule|"
            r"Evidence boundary:.{0,520}implementation sequence;.{0,180}"
            r"does not attribute a universal workflow rule to any one source|"
            r"Documented basis:.{0,520}sources establish those controls individually.{0,180}"
            r"report composes the implementation sequence|"
            r"证据边界.{0,520}(?:实施顺序|综合推断).{0,180}"
            r"(?:不把|并非).{0,100}(?:通用工作流|官方规则|单一来源)|"
            r"文档依据.{0,520}来源分别证明这些控制项.{0,180}本报告只负责组合实施顺序",
            conversion_validation_section,
            re.IGNORECASE | re.DOTALL,
        )
    )
    has_verified_path_operation_claim = any(
        re.search(
            r"\b(?:path(?:lib)?\.Path|Path object|pure paths?|concrete paths?|"
            r"filesystem path|exists\(\)|is_dir\(\)|open\(\)|with_suffix|operator)\b",
            _safe_text(claim.get("claim")),
            re.IGNORECASE,
        )
        for claim in verified_claims
    )
    path_layer_inference_trace_present = bool(
        has_verified_conversion_claim
        and has_verified_path_operation_claim
        and re.search(
            r"Engineering-decomposition boundary|工程分解边界",
            path_semantics_section,
            re.IGNORECASE,
        )
        and re.search(
            r"report's methodology, not a verbatim official recommendation|"
            r"本报告的方法，不冒充官方资料逐字发布的推荐",
            path_semantics_section,
            re.IGNORECASE,
        )
        and len(
            set(re.findall(r"\[(S\d+)\]", path_semantics_section, re.IGNORECASE))
        )
        >= 2
        and not re.search(
            r"\bofficial(?:ly)?\s+(?:requires?|recommends?|mandates?)\b|"
            r"\b(?:all|every)\s+CLI\b.{0,80}\bmust\b|"
            r"\buniversal(?:ly)?\s+(?:requires?|guarantees?)\b|"
            r"官方(?:明确)?(?:要求|规定|推荐).{0,80}(?:所有|任何)?\s*CLI",
            path_semantics_section,
            re.IGNORECASE,
        )
    )
    interpretation_rule_present = bool(
        re.search(
            r"^##\s+(?:Direct answer(?: and interpretation rule)?|直接回答(?:与判断规则)?)\s*$",
            candidate_answer,
            re.IGNORECASE | re.MULTILINE,
        )
        and re.search(
            r"must not be upgraded to 'the official preferred practice'|"
            r"'the official documentation shows this usage' is not the same as 'the official preferred practice'|"
            r"不能把.{0,80}官方文档展示了这种用法.{0,80}改写成.{0,80}官方首选|"
            r"官方文档展示了这种用法.{0,80}不等于.{0,80}官方首选",
            candidate_answer,
            re.IGNORECASE | re.DOTALL,
        )
    )
    synthesis_units_for_trace = [
        unit
        for unit in _architect_section_content_units(candidate_answer)
        if _ARCHITECT_SYNTHESIS_LABEL_RE.search(unit)
    ]

    def synthesis_unit_has_complete_citation_tail(unit: str) -> bool:
        all_keys = list(
            dict.fromkeys(
                key.upper()
                for key in re.findall(r"\[(S\d+)\]", unit, re.IGNORECASE)
            )
        )
        if len(all_keys) < 2:
            return False
        tail = re.search(r"((?:\[S\d+\])+)[.!。]?\s*$", unit, re.IGNORECASE)
        if not tail:
            return False
        tail_keys = {
            key.upper()
            for key in re.findall(r"\[(S\d+)\]", tail.group(1), re.IGNORECASE)
        }
        return set(all_keys).issubset(tail_keys)

    synthesis_citation_trace_complete = bool(
        len(synthesis_units_for_trace) >= 3
        and all(
            synthesis_unit_has_complete_citation_tail(unit)
            for unit in synthesis_units_for_trace
        )
    )
    claim_type_boundary_present = bool(
        interpretation_rule_present
        and attributed_secondary_claim_present
        and re.search(
            r"Unless an excerpt itself uses prescriptive language|"
            r"除非某条摘录本身含明确推荐词",
            candidate_answer,
            re.IGNORECASE,
        )
        and re.search(
            r"Source-backed finding|来源支撑的结论",
            candidate_answer,
            re.IGNORECASE,
        )
    )
    integration_inference_trace_present = bool(
        visible_contextual_practice_answer
        and has_verified_conversion_claim
        and len(
            set(re.findall(r"\[(S\d+)\]", integration_section, re.IGNORECASE))
        )
        >= 3
        and (
            not attributed_secondary_advice_visible
            or attributed_secondary_claim_present
        )
        and re.search(
            r"this report's explicitly labeled cross-source inference|"
            r"report's cross-source inference from the primary contracts|"
            r"本报告明确标注的跨来源推论|本报告从多个一手转换合同得出的跨来源推论",
            integration_section,
            re.IGNORECASE,
        )
    )
    asks_for_official_normative_wording = bool(
        re.search(
            r"(?:what|which).{0,60}(?:official|documentation|docs).{0,60}(?:recommend|require|prefer)|"
            r"(?:官方|权威)(?:文档|资料|来源)?.{0,16}(?:明确)?(?:推荐|要求|首选)(?:什么|哪些|哪种|的)",
            _safe_text(question),
            re.IGNORECASE,
        )
    )
    dated_pep_scope_present = bool(
        re.search(
            r"Dated/versioned PEP entries retain their explicit time and version scope; their date alone neither establishes nor negates current applicability|"
            r"带日期/版本的 PEP 保留其明确时间与版本范围；不能仅凭日期认定或否定当前适用性",
            candidate_answer,
            re.IGNORECASE,
        )
        and re.search(
            r"source-reported document date shown; no fixed age cutoff is applied|"
            r"来源报告的文档日期已列出；不套用固定年龄截止",
            candidate_answer,
            re.IGNORECASE,
        )
    )
    date_label_not_deprecation_present = bool(
        re.search(
            r"A date or version label does not itself mean that material is superseded or deprecated|"
            r"日期或版本标签本身不表示资料已被替代或废弃",
            candidate_answer,
            re.IGNORECASE,
        )
    )
    click_default_nonassertion_present = bool(
        re.search(
            r"makes no claim about (?:Click's|path_type's) default value|"
            r"不陈述 (?:Click|path_type) 的默认值",
            choice_section,
            re.IGNORECASE,
        )
        and re.search(r"\bClick\b.{0,900}\[S\d+\]", choice_section, re.IGNORECASE | re.DOTALL)
    )
    verified_click_none_signature = any(
        re.search(
            r"\bClick\.Path(?:'s)?\b.{0,120}\bsignature\b.{0,120}"
            r"\bpath_type\s*=\s*None\b",
            _safe_text(claim.get("claim")),
            re.IGNORECASE | re.DOTALL,
        )
        for claim in verified_claims
    )
    verified_click_none_behavior = any(
        re.search(
            r"\bClick(?:'s)?\b.{0,120}\bpath_type\b.{0,180}\bif\s+None\b"
            r".{0,160}\bdefault\b.{0,80}\bstr\b",
            _safe_text(claim.get("claim")),
            re.IGNORECASE | re.DOTALL,
        )
        for claim in verified_claims
    )
    click_default_contract_present = bool(
        verified_click_none_signature
        and verified_click_none_behavior
        and re.search(
            r"verified signature records path_type=None.{0,180}"
            r"None keeps Python's default str|"
            r"已验证签名记录 path_type=None.{0,180}None 会保留 Python 默认的 str",
            choice_section,
            re.IGNORECASE | re.DOTALL,
        )
        and re.search(r"\bClick\b.{0,1200}\[S\d+\]", choice_section, re.IGNORECASE | re.DOTALL)
    )
    click_validation_default_pairs = (
        ("exists", "false"),
        ("file_okay", "true"),
        ("dir_okay", "true"),
        ("writable", "false"),
        ("readable", "true"),
        ("resolve_path", "false"),
    )
    verified_click_path_signature_default_claims = [
        claim
        for claim in verified_claims
        if (
        all(
            re.search(
                rf"\b{re.escape(name)}\s*=\s*{value}\b",
                _normalized_evidence_text(claim.get("claim")),
                re.IGNORECASE,
            )
            and re.search(
                rf"\b{re.escape(name)}\s*=\s*{value}\b",
                _normalized_evidence_text(claim.get("evidenceExcerpt")),
                re.IGNORECASE,
            )
            for name, value in click_validation_default_pairs
        )
        and any(
            isinstance(support, dict)
            and _architect_support_role(support) == "primary"
            and re.search(
                r"\bClick\b|click\.palletsprojects\.com",
                " ".join(
                    (
                        _safe_text(support.get("title")),
                        _safe_text(support.get("url") or support.get("sourceUrl")),
                    )
                ),
                re.IGNORECASE,
            )
            for support in list(claim.get("supportingSources") or [])
        )
        )
    ]
    verified_click_path_signature_defaults = bool(
        verified_click_path_signature_default_claims
    )
    verified_path_intro_scope_claims = [
        claim
        for claim in verified_claims
        if re.search(
            r"\b(?:pathlib\.)?Path\b.{0,120}\bmost likely\b.{0,120}\b(?:need|needed)\b",
            _safe_text(claim.get("claim")),
            re.IGNORECASE | re.DOTALL,
        )
        and re.search(
            r"\bPath is most likely what you need\b",
            _safe_text(claim.get("evidenceExcerpt")),
            re.IGNORECASE,
        )
        and any(
            isinstance(support, dict)
            and _architect_support_role(support) == "primary"
            and re.search(
                r"docs\.python\.org/(?:3|\d+(?:\.\d+)?)/library/pathlib\.html|"
                r"\bpathlib\b.{0,80}\bObject-oriented filesystem paths\b",
                " ".join(
                    (
                        _safe_text(support.get("title")),
                        _safe_text(support.get("url") or support.get("sourceUrl")),
                    )
                ),
                re.IGNORECASE,
            )
            for support in list(claim.get("supportingSources") or [])
        )
    ]
    verified_concrete_path_io_claims = [
        claim
        for claim in verified_claims
        if re.search(
            r"\b(?:pathlib\.)?Path\b.{0,160}\bconcrete\b.{0,120}\bI/O operations?\b|"
            r"\bconcrete path class\b.{0,160}\bI/O operations?\b",
            _safe_text(claim.get("claim")),
            re.IGNORECASE | re.DOTALL,
        )
        and re.search(
            r"\bconcrete paths?\b.{0,180}\binherit from pure paths?\b.{0,180}"
            r"\b(?:also )?provide I/O operations?\b",
            _safe_text(claim.get("evidenceExcerpt")),
            re.IGNORECASE | re.DOTALL,
        )
        and any(
            isinstance(support, dict)
            and _architect_support_role(support) == "primary"
            and re.search(
                r"docs\.python\.org/(?:3|\d+(?:\.\d+)?)/library/pathlib\.html",
                _safe_text(support.get("url") or support.get("sourceUrl")),
                re.IGNORECASE,
            )
            for support in list(claim.get("supportingSources") or [])
        )
    ]
    inline_undated_boundary_keys: set[str] = set()
    for evidence_block in re.split(
        r"(?=^###\s+(?:(?:Evidence item|证据项)\s+\d+|(?:Source|来源)\s+\[S\d+\]|\[S\d+\]))",
        candidate_answer,
        flags=re.IGNORECASE | re.MULTILINE,
    ):
        if not re.search(
            r"Currency and applicability:\s*undated|"
            r"时效与适用边界：\s*无文档日期",
            evidence_block,
            re.IGNORECASE,
        ):
            continue
        inline_undated_boundary_keys.update(
            key.upper()
            for key in re.findall(r"\[(S\d+)\]", evidence_block, re.IGNORECASE)
        )
    retrieval_date_caveat_present = bool(
        re.search(
            r"Retrieval time proves that a source was read, not that it is still the latest version|"
            r"检索时间只证明本次读取发生过，不等于文档仍是最新版本",
            candidate_answer,
            re.IGNORECASE,
        )
    )
    path_protocol_nonimplication_present = bool(
        path_protocol_section
        and re.search(
            r"not (?:an )?actionable CLI premise|not used to claim.{0,100}"
            r"(?:argparse|Click|Typer).{0,100}accepts? PathLike|"
            r"不作为可执行 CLI 前提|不用于声称.{0,100}(?:argparse|Click|Typer).{0,100}接受 PathLike",
            path_protocol_section,
            re.IGNORECASE | re.DOTALL,
        )
        and re.search(
            r"does not claim that any unverified API (?:accepts|rejects)"
            r"(?:\s+or\s+(?:accepts|rejects))?\s+PathLike|"
            r"不声称任何未核验 API (?:接受|拒绝)(?:或(?:接受|拒绝))? PathLike",
            path_protocol_section,
            re.IGNORECASE,
        )
        and re.search(r"\[S\d+\]", path_protocol_section, re.IGNORECASE)
    )

    missing_label_cue = re.compile(
        r"(?:未(?:明确)?标注|没有标注|缺少标注)|"
        r"\b(?:not|isn['’]t|wasn['’]t|without)\b.{0,30}\b(?:label(?:led|ed)?|mark(?:ed)?)\b",
        re.IGNORECASE,
    )
    missing_attribution_cue = re.compile(
        r"(?:未(?:保持)?(?:明确)?归因|没有归因|缺少归因)|"
        r"\b(?:not|isn['’]t|wasn['’]t|without)\b.{0,40}\b(?:attribut(?:e|ed|ion)|credit(?:ed)?)\b",
        re.IGNORECASE,
    )

    def exact_surface_contradiction(issue: str) -> str:
        text = _safe_text(issue)
        if not text:
            return ""
        if (
            re.search(r"\bcorrectly\b|正确(?:地|保持|标注|归因)|符合要求", text, re.IGNORECASE)
            and re.search(r"\bacceptable\b|可接受|符合(?:审查|指令|要求)", text, re.IGNORECASE)
            and not re.search(
                r"\bhowever\b|\bbut\s+(?:not|fails?|lacks?|missing|unsupported)|"
                r"(?:但是|然而|不过|仍然).{0,80}(?:错误|缺少|不支持|未能)",
                text,
                re.IGNORECASE,
            )
        ):
            return "reviewer_positive_observation"
        mentioned_claim_ids = {
            value.lower()
            for value in re.findall(
                r"\b(?:claim_runtime_[a-z0-9_]+|C\d+)\b",
                text,
                re.IGNORECASE,
            )
        }
        mentioned_claim_ids_are_verified = bool(
            not mentioned_claim_ids
            or all(
                any(
                    _safe_text(claim.get("claimId")).lower() == claim_id
                    for claim in verified_claims
                )
                for claim_id in mentioned_claim_ids
            )
        )
        api_term_aliases = {
            "exists": ("exists", "existence"),
            "file_okay": ("file_okay",),
            "dir_okay": ("dir_okay",),
            "writable": ("writable",),
            "readable": ("readable",),
            "resolve_path": ("resolve_path",),
        }
        mentioned_api_terms = {
            canonical
            for canonical, aliases in api_term_aliases.items()
            if any(re.search(rf"\b{re.escape(alias)}\b", text, re.IGNORECASE) for alias in aliases)
        }
        verified_api_list_coverage = bool(
            len(mentioned_api_terms) >= 3
            and re.search(r"truncat(?:e|ed|ion)|截断", text, re.IGNORECASE)
            and re.search(r"full.{0,30}list|validation list|完整.{0,20}(?:列表|清单)|校验列表", text, re.IGNORECASE)
            and any(
                all(
                    any(
                        re.search(rf"\b{re.escape(alias)}\b", _safe_text(claim.get("claim")), re.IGNORECASE)
                        for alias in api_term_aliases[term]
                    )
                    and re.search(
                        rf"\b{re.escape(term)}\b",
                        _safe_text(claim.get("evidenceExcerpt")),
                        re.IGNORECASE,
                    )
                    for term in mentioned_api_terms
                )
                for claim in verified_claims
            )
        )
        exact_attributed_secondary_claim = any(
            _safe_text(claim.get("claimId")).lower() in mentioned_claim_ids
            and any(
                isinstance(support, dict)
                and _architect_support_role(support) == "secondary"
                for support in list(claim.get("supportingSources") or [])
            )
            and _ARCHITECT_SOURCE_ROLE_ATTRIBUTION_RE.match(
                _safe_text(claim.get("claim"))
            )
            and bool(_safe_text(claim.get("evidenceExcerpt")))
            and _normalized_evidence_text(
                _architect_assertion_text(claim.get("claim"))
            )
            in _normalized_evidence_text(claim.get("evidenceExcerpt"))
            for claim in verified_claims
        )
        verified_unused_evidence_claim = any(
            _safe_text(claim.get("claimId")).lower() in mentioned_claim_ids
            and bool(_safe_text(claim.get("claim")))
            and bool(_safe_text(claim.get("evidenceExcerpt")))
            for claim in verified_claims
        )
        if (
            visible_contextual_practice_answer
            and verified_unused_evidence_claim
            and re.search(
                r"factually correct|verbatim match|事实(?:正确|一致)|逐字一致",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"never use(?:d|s)|not used|isn['’]t used|没有用于|未用于",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"best[- ]practice|practice claim|recommendation|最佳实践|实践建议",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_evidence_fact_not_required_as_synthesis_premise"
        mentioned_source_keys = {
            key.upper()
            for key in re.findall(r"\b(S\d+)\b", text, re.IGNORECASE)
        }

        def issue_is_bound_to(claims: list[dict[str, Any]]) -> bool:
            if mentioned_claim_ids and not any(
                _safe_text(claim.get("claimId")).lower() in mentioned_claim_ids
                for claim in claims
            ):
                return False
            if mentioned_source_keys and not any(
                mentioned_source_keys.intersection(
                    {
                        _safe_text(support.get("citationKey") or support.get("citation"))
                        .strip("[]")
                        .upper()
                        for support in list(claim.get("supportingSources") or [])
                        if isinstance(support, dict)
                    }
                )
                for claim in claims
            ):
                return False
            return True
        exact_claim_excerpt_records = [
            claim
            for claim in verified_claims
            if _normalized_evidence_text(claim.get("claim"))
            and _normalized_evidence_text(claim.get("claim"))
            == _normalized_evidence_text(claim.get("evidenceExcerpt"))
        ]
        if (
            mentioned_claim_ids
            and issue_is_bound_to(exact_claim_excerpt_records)
            and re.search(
                r"\bclaim\b.{0,100}\bomits?\b|"
                r"\bomits?\b.{0,100}\b(?:claim|mention|detail|specificity)\b|"
                r"(?:claim|声明|结论).{0,80}(?:省略|遗漏)|"
                r"(?:省略|遗漏).{0,80}(?:细节|说明|声明|结论)",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"\bexcerpt\b|\bsource\b|\bS\d+(?::E\d+)?\b|摘录|来源",
                text,
                re.IGNORECASE,
            )
        ):
            # If the bound claim and excerpt are textually identical after
            # normalization, the claim cannot have omitted a detail that the
            # reviewer simultaneously quotes from that same excerpt.
            return "verified_exact_claim_excerpt_no_omission"
        verified_click_return_contract = any(
            _safe_text(claim.get("claimId")).lower() in mentioned_claim_ids
            and re.search(
                r"returns the filename instead of an open file",
                _safe_text(claim.get("claim")),
                re.IGNORECASE,
            )
            and re.search(
                r"returns the filename instead of an open file",
                _safe_text(claim.get("evidenceExcerpt")),
                re.IGNORECASE,
            )
            and (
                not mentioned_source_keys
                or bool(
                    mentioned_source_keys.intersection(
                        {
                            _safe_text(support.get("citationKey") or support.get("citation"))
                            .strip("[]")
                            .upper()
                            for support in list(claim.get("supportingSources") or [])
                            if isinstance(support, dict)
                        }
                    )
                )
            )
            for claim in verified_claims
        )
        if (
            verified_click_return_contract
            and re.search(r"\bClick\b|\bC\d+\b", text, re.IGNORECASE)
            and re.search(
                r"not entailed|not (?:a )?verified return-value contract|"
                r"signature.{0,80}not.{0,40}return|不(?:足以)?证明.{0,30}返回",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_click_filename_return_contract"
        if (
            path_protocol_nonimplication_present
            and re.search(r"\bPathLike\b|路径协议", text, re.IGNORECASE)
            and re.search(
                r"(?:all|every)\s+(?:CLI\s+)?framework|"
                r"actual acceptance|ungrounded implication|"
                r"所有.{0,20}(?:CLI|框架)|实际接受|未经支撑的暗示",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_path_protocol_nonimplication_boundary"
        if (
            verified_path_intro_scope_claims
            and issue_is_bound_to(verified_path_intro_scope_claims)
            and re.search(r"\bmost likely\b.{0,100}\b(?:need|needed)\b", text, re.IGNORECASE)
            and re.search(
                r"(?:adds?|added).{0,100}(?:scope|qualifier)|"
                r"without specifying.{0,100}(?:scope|module)|"
                r"does not specify.{0,100}(?:scope|module)|"
                r"未(?:说明|写明).{0,80}(?:范围|模块)",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_path_intro_page_scope"
        if (
            verified_concrete_path_io_claims
            and issue_is_bound_to(verified_concrete_path_io_claims)
            and (
                mentioned_claim_ids
                or re.search(r"\bconcrete paths?\b|\bI/O operations?\b", text, re.IGNORECASE)
            )
            and re.search(
                r"omits?.{0,100}inheritance|semantic narrowing|"
                r"does not (?:mention|include).{0,100}inherit|"
                r"省略.{0,80}继承|语义收窄",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_concrete_path_io_semantic_narrowing"
        if (
            argparse_example_boundary_present
            and not asks_for_official_normative_wording
            and re.search(
                r"\bargparse\b.{0,180}(?:documented )?type example|"
                r"argparse.{0,180}(?:type\s*=\s*pathlib\.Path|pathlib\.Path.{0,40}converter)",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"(?:does not|doesn't|not).{0,100}(?:recommend(?:ed|ation)?|best practice)|"
                r"(?:recommend(?:ed|ation)?|best practice).{0,100}(?:does not|doesn't|not)|"
                r"(?:未|没有).{0,100}(?:推荐|最佳实践)",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_argparse_example_not_upgraded_to_recommendation"
        if (
            argparse_normative_scope_present
            and re.search(r"\bargparse\b", text, re.IGNORECASE)
            and re.search(r"basic(?: command[- ]line| CLI)|基础命令行", text, re.IGNORECASE)
            and re.search(
                r"(?:does not|doesn't|not|fails? to).{0,100}(?:limit|scope|qualif)|"
                r"(?:未|没有).{0,100}(?:限定|范围|条件)",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_argparse_basic_application_scope"
        if (
            click_option_application_boundary_present
            and re.search(r"\bClick\b|\bpath_type\b", text, re.IGNORECASE)
            and re.search(
                r"only documents?.{0,160}(?:parameter )?option.{0,160}not.{0,120}(?:boundary|practice)|"
                r"does not (?:state|document).{0,160}(?:boundary|practice)|"
                r"仅(?:记录|说明).{0,120}(?:选项|参数).{0,120}(?:不是|未).{0,100}(?:边界|实践)",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_click_option_application_boundary"
        if (
            click_conversion_contract_present
            and re.search(r"\bClick\b|\bpath_type\b", text, re.IGNORECASE)
            and re.search(
                r"(?:only|merely) records?.{0,160}Allow passing.{0,80}path_type|"
                r"(?:claimTable|claim).{0,180}(?:only|merely).{0,120}Allow passing|"
                r"仅记录.{0,160}Allow passing.{0,80}path_type",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"(?:does not|doesn't|not|unverified).{0,120}(?:behavior|conversion|semantics)|"
                r"未记录.{0,120}(?:行为|转换|语义)|未经验证的语义",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_click_path_type_conversion_contract"
        if (
            verified_click_parameter_conversion_contract
            and click_conversion_contract_present
            and issue_is_bound_to(verified_click_parameter_conversion_claims)
            and re.search(r"\bClick\b|\bpath_type\b|\bC\d+\b", text, re.IGNORECASE)
            and re.search(
                r"(?:only|merely).{0,100}(?:documents?|describes?).{0,140}"
                r"(?:parameter|description)|"
                r"(?:documented\s+)?parameter description.{0,120}(?:is\s+)?not"
                r".{0,80}(?:guarantee|proof).{0,140}"
                r"(?:conversion|convert|supported type|value)|"
                r"does not (?:show|include).{0,100}(?:working|runtime) example|"
                r"does not (?:demonstrate|guarantee).{0,140}(?:conversion|succeeds?)|"
                r"lacks? evidence.{0,180}(?:returns?|return[- ]type|handler)|"
                r"unverified.{0,100}(?:Click|Path).{0,100}return[- ]type|"
                r"转换.{0,100}(?:缺少|没有).{0,100}(?:运行示例|返回类型)",
                text,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            # A primary parameter contract that explicitly says it converts
            # the incoming value to the requested type and names pathlib.Path
            # is direct source_fact evidence. Requiring a second runnable
            # example would turn ordinary API documentation into an
            # unnecessarily strict proof standard.
            return "verified_click_path_type_parameter_contract"
        if (
            verified_click_path_signature_defaults
            and issue_is_bound_to(verified_click_path_signature_default_claims)
            and re.search(r"\bClick\b|\bC\d+\b|\bpath_type\b", text, re.IGNORECASE)
            and re.search(
                r"signature line|escaped underscores?|without prose|prose confirmation|"
                r"behavioral defaults?|签名行|转义下划线|缺少.{0,30}文字说明",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"defaults?|exists|file_okay|dir_okay|writable|readable|resolve_path|默认",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_click_path_signature_defaults"
        if (
            click_default_nonassertion_present
            and re.search(r"\bClick\b|\bpath_type\b", text, re.IGNORECASE)
            and re.search(r"default value|默认值", text, re.IGNORECASE)
            and re.search(
                r"(?:lack|without|no|not).{0,120}(?:evidence|support)|"
                r"(?:缺乏|没有|未提供).{0,100}(?:证据|支持)",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_click_default_nonassertion"
        if (
            click_default_contract_present
            and re.search(r"\bClick\b|\bpath_type\b", text, re.IGNORECASE)
            and re.search(r"default value|默认值|默认.*None|None.*默认", text, re.IGNORECASE)
            and re.search(
                r"(?:not|isn['’]t|wasn['’]t|missing|omits?|fails? to).{0,140}"
                r"(?:state|show|reflect|include|mention)|"
                r"(?:未|没有|缺少).{0,120}(?:体现|呈现|说明|写明|提及)",
                text,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            return "verified_click_none_default_contract"
        verified_typer_specific_claim = any(
            _safe_text(claim.get("claimId")).lower() in mentioned_claim_ids
            and re.search(r"\bTyper(?:'s)?\b", _safe_text(claim.get("claim")), re.IGNORECASE)
            and any(
                isinstance(support, dict)
                and re.search(
                    r"\bTyper\b|typer\.tiangolo\.com",
                    " ".join(
                        (
                            _safe_text(support.get("title")),
                            _safe_text(support.get("url")),
                        )
                    ),
                    re.IGNORECASE,
                )
                for support in list(claim.get("supportingSources") or [])
            )
            for claim in verified_claims
        )
        verified_typer_prefer_normative_claim = any(
            _safe_text(claim.get("claimId")).lower() in mentioned_claim_ids
            and _safe_text(claim.get("claimType")).lower() == "explicit_normative"
            and re.search(r"\bTyper\b.{0,160}\bAnnotated\b", _safe_text(claim.get("claim")), re.IGNORECASE)
            and re.search(
                r"\bPrefer to use the Annotated version if possible\b",
                _safe_text(claim.get("evidenceExcerpt")),
                re.IGNORECASE,
            )
            and any(
                isinstance(support, dict)
                and _architect_support_role(support) == "primary"
                and re.search(
                    r"\bTyper\b|typer\.tiangolo\.com",
                    " ".join(
                        (
                            _safe_text(support.get("title")),
                            _safe_text(support.get("url")),
                        )
                    ),
                    re.IGNORECASE,
                )
                for support in list(claim.get("supportingSources") or [])
            )
            for claim in verified_claims
        )
        if (
            verified_typer_prefer_normative_claim
            and re.search(r"\bTyper\b.{0,180}\bAnnotated\b", text, re.IGNORECASE)
            and re.search(
                r"(?:excerpt|source).{0,100}(?:only )?(?:says?|labels?).{0,100}\btip\b|"
                r"\btip\b.{0,120}not an? official recommendation|"
                r"仅是.{0,40}(?:提示|tip).{0,100}不是官方推荐",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_primary_tip_normative_cue"
        if (
            verified_typer_specific_claim
            and re.search(r"\bTyper\b", text, re.IGNORECASE)
            and re.search(
                r"without clarifying.{0,80}Typer[- ]specific|"
                r"not universal pathlib|universal pathlib features?|"
                r"未(?:明确)?限定.{0,50}Typer|误写为 pathlib 通用",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_typer_specific_claim_scope"
        if (
            exact_attributed_secondary_claim
            and re.search(r"cross[- ]platform|跨平台", text, re.IGNORECASE)
            and re.search(r"guarantee|保证|担保", text, re.IGNORECASE)
        ):
            return "verified_attributed_secondary_exact_wording"
        if (
            attributed_secondary_claim_present
            and re.search(
                r"(?:claimTable|claim table|claim_table).{0,160}(?:未|没有|not|without).{0,80}(?:归因|attribut)|"
                r"(?:未|没有|not|without).{0,100}(?:claimTable|claim table|claim_table).{0,100}(?:归因|attribut)|"
                r"(?:未|没有|not|without).{0,80}(?:归因|attribut).{0,160}(?:claimTable|claim table|claim_table)",
                text,
                re.IGNORECASE,
            )
            and re.search(r"(?:secondary|二手|should use Path objects)", text, re.IGNORECASE)
        ):
            return "verified_claim_table_secondary_attribution"
        if (
            attributed_secondary_claim_present
            and re.search(
                r"correctly (?:prefixed|attributed|labeled|labelled)|"
                r"已正确(?:加前缀|归因|标注)",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"no escalation to (?:an? )?official rule|"
                r"no (?:upgrade|escalation).{0,80}official|"
                r"未升级为官方规则|没有冒充官方",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_secondary_attribution_positive_observation"
        if (
            conversion_validation_synthesis_present
            and re.search(
                r"(?:convert(?: this token)? to Path|转换成 Path).{0,200}"
                r"(?:separate decisions|separation principle|分离决策|分离原则|独立决定)|"
                r"(?:separate decisions|separation principle|分离决策|分离原则|独立决定).{0,200}"
                r"(?:convert(?: this token)? to Path|转换成 Path|validate path)",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"(?:claimTable|claim table|claim_table|对应 claim|claim 支持|"
                r"verified premise|verified claim|来源直接陈述|source directly states?|"
                r"(?:no|does not cite any) source.{0,100}explicitly states?.{0,100}separation|"
                r"(?:没有|未引用任何)来源.{0,100}明确(?:陈述|说明).{0,100}分离)",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_conversion_validation_synthesis"
        if (
            conversion_validation_synthesis_present
            and re.search(
                r"This report's synthesis.{0,120}explicit cross-source engineering recommendation|"
                r"本报告的综合判断.{0,80}明确的跨来源工程建议",
                conversion_validation_section,
                re.IGNORECASE,
            )
            and re.search(
                r"(?:not|does not|doesn't|without).{0,100}(?:label(?:led|ed|ing)?|mark(?:ed|ing)?).{0,100}"
                r"cross-source|"
                r"(?:not|does not|doesn't|without).{0,100}(?:individually|each framework).{0,100}cit|"
                r"(?:does not|doesn't|without).{0,100}cit(?:e|ing).{0,120}(?:premise|source).{0,120}separation|"
                r"(?:未|没有).{0,100}(?:明确标注|逐一引用).{0,100}(?:跨来源|每个框架|前提)|"
                r"(?:未引用任何|没有引用).{0,140}(?:支持.{0,80}分离原则|前提来源)",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_conversion_validation_synthesis"
        if (
            choice_synthesis_boundary_present
            and re.search(
                r"Keep the CLI framework already used|沿用应用已经采用的 CLI 框架",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"(?:no|without|does not).{0,120}(?:source|excerpt).{0,120}(?:support|state)|"
                r"(?:not|without).{0,100}(?:label(?:led|ed)|mark(?:ed)?).{0,80}synthesi|"
                r"(?:未|没有).{0,100}(?:来源|摘录).{0,100}支持|"
                r"(?:未|没有).{0,100}标注.{0,80}(?:综合|推论)",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_choice_synthesis_boundary"
        if (
            conversion_validation_synthesis_present
            and re.search(r"\bpath_type\b|Path annotation|Path 类型", text, re.IGNORECASE)
            and re.search(
                r"否定性(?:综合)?推断|否定结论|negative (?:claim|conclusion|inference)|"
                r"(?:未|没有).{0,80}(?:官方来源|官方资料).{0,80}(?:明确声明|明确声称|支持)|"
                r"(?:no|without).{0,80}official source.{0,80}(?:states?|supports?)",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_conversion_validation_evidence_boundary"
        if (
            conversion_validation_boundary_present
            and re.search(
                r"(?:actionable (?:practice|sequence)|implementation sequence).{0,220}"
                r"(?:combin(?:e|es|ed|ing)|independent framework options?|conversion|validation)|"
                r"(?:combin(?:e|es|ed|ing)|independent framework options?|conversion|validation).{0,220}"
                r"(?:actionable (?:practice|sequence)|implementation sequence)",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"(?:no|without) (?:a |any )?(?:single )?source.{0,180}"
                r"(?:universal (?:implementation )?workflow|states?|directly supports?)|"
                r"(?:universal (?:implementation )?workflow|通用工作流).{0,160}"
                r"(?:not stated|without (?:a |any )?source|没有来源|未由来源)|"
                r"(?:实施顺序|可执行做法).{0,180}(?:没有|缺少|未由).{0,100}(?:来源|单篇资料)",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_conversion_validation_synthesis"
        if (
            framework_scoped_direct_synthesis_present
            and re.search(
                r"Lead ['\"]?Direct answer|直接回答",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"supported by S\d+.{0,120}but not by S\d+|"
                r"attribution across argparse is an extrapolation|"
                r"跨 argparse.{0,80}(?:外推|扩大)",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"existence|file/directory|permission|validation|存在性|文件/目录|权限|校验",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_framework_scoped_validation_synthesis"
        if (
            path_layer_inference_trace_present
            and re.search(
                r"分离评估.{0,40}方法论|分离.{0,80}(?:官方来源|官方资料).{0,80}(?:支持|声明)|"
                r"separat(?:e|ion).{0,100}(?:methodology|official source|official recommendation)|"
                r"engineering[- ]layer separation.{0,180}(?:no|without).{0,40}primary source|"
                r"engineering[- ]decomposition boundary.{0,120}report(?:'s)? methodology.{0,120}"
                r"not source text|"
                r"(?:lacks?|missing|without).{0,80}engineering[- ]decomposition boundary|"
                r"(?:缺少|缺乏|没有).{0,80}工程分解边界",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_report_engineering_decomposition_boundary"
        if (
            path_layer_inference_trace_present
            and mentioned_claim_ids_are_verified
            and re.search(
                r"(?:cross[- ]source engineering (?:advice|synthesis)|engineering[- ]decomposition boundary|"
                r"report(?:'s)? methodology|跨来源工程(?:建议|综合)|工程分解边界|本报告的方法)",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"(?:CLI (?:input )?(?:conversion|parsing)).{0,180}"
                r"(?:downstream|later) path(?:-object)? operations?|"
                r"(?:downstream|later) path(?:-object)? operations?.{0,180}"
                r"CLI (?:input )?(?:conversion|parsing)|"
                r"CLI 输入转换.{0,120}后续路径对象操作",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"does not directly state|not (?:source text|stated by (?:a |any )?source)|"
                r"(?:no|without) (?:a |any )?(?:single )?(?:primary )?source|"
                r"不是来源原文|没有单一来源|未由单篇资料直接陈述",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_cli_integration_inference_trace"
        if (
            argparse_example_scope_present
            and path_layer_inference_trace_present
            and re.search(
                r"Argparse bullet implies.{0,160}handler.{0,100}(?:Path|path)|"
                r"argparse 条目.{0,160}处理函数.{0,100}Path",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"only shows?\s+`?type=pathlib\.Path`?.{0,180}(?:example|converter)|"
                r"只展示.{0,80}`?type=pathlib\.Path`?",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_argparse_example_scope"
        if (
            argparse_example_scope_present
            and re.search(
                r"argparse(?:'s)? documented type example.{0,120}"
                r"(?:passes|uses).{0,80}pathlib\.Path.{0,80}(?:converter|type)|"
                r"argparse.{0,120}官方示例.{0,100}type\s*=\s*pathlib\.Path",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"does not state.{0,100}(?:recommended|best practice)|"
                r"not (?:a )?(?:recommendation|best practice)|"
                r"未(?:说明|声明|证明).{0,80}(?:推荐|最佳实践)",
                text,
                re.IGNORECASE,
            )
        ):
            # The visible bullet deliberately limits itself to the exact
            # parser configuration shown by the official example and says it
            # is not an official recommendation. A reviewer cannot turn that
            # descriptive example fact into a missing normative premise.
            return "verified_argparse_example_not_upgraded_to_recommendation"
        if (
            visible_contextual_practice_answer
            and re.search(
                r"choose by CLI context, not by an unconditional library ranking|"
                r"按 CLI 场景选择，而不是做无条件库排名",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"(?:缺乏|缺少|未|没有|lack|missing).{0,100}(?:前提|premise|显式引用|explicit citations?)",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_choice_matrix_citation_trace"
        if (
            interpretation_rule_present
            and re.search(
                r"(?:Direct answer(?: and interpretation rule)?|直接回答(?:与判断规则)?|证据标准|evidence standard).{0,180}"
                r"(?:未|没有|not).{0,80}(?:claimTable|claim table|claim_table|可验证的 claim|verifiable claim)",
                text,
                re.IGNORECASE,
            )
        ):
            return "epistemic_interpretation_rule_not_source_fact"
        if (
            synthesis_citation_trace_complete
            and re.search(
                r"(?:Practical synthesis|Mixed-source synthesis|本报告的综合判断|综合推论).{0,180}"
                r"(?:未|没有|not|without).{0,100}(?:全部前提来源|all premise sources|全部引用|all citations)|"
                r"(?:部分推论|some inferences).{0,120}(?:未|没有|not).{0,80}(?:前提来源|premise sources|引用|citations)",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_synthesis_citation_trace"
        if (
            claim_type_boundary_present
            and re.search(
                r"(?:未|没有|not).{0,80}(?:区分|distinguish).{0,80}"
                r"(?:source_fact|source fact).{0,80}(?:explicit_normative|explicit normative)|"
                r"(?:source_fact|source fact).{0,80}(?:explicit_normative|explicit normative).{0,80}"
                r"(?:模糊|unclear|not distinguished)",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_source_fact_normative_boundary"
        if (
            integration_inference_trace_present
            and re.search(
                r"(?:CLI parsing and framework integration|CLI 解析与框架接入).{0,180}"
                r"(?:unsupported|not fully justified|未充分支撑|无证据|缺乏前提)|"
                r"(?:unsupported|not fully justified|未充分支撑|无证据|缺乏前提).{0,180}"
                r"(?:cross-source inference|跨来源推论|CLI parsing and framework integration|CLI 解析与框架接入)",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_cli_integration_inference_trace"
        if (
            integration_inference_trace_present
            and re.search(
                r"claimTable.{0,180}(?:does not contain|lacks?|missing).{0,120}"
                r"(?:synthesis claim|corresponding synthesis|all premises)|"
                r"(?:synthesis claim|corresponding synthesis).{0,160}(?:missing|absent|not in).{0,80}claimTable|"
                r"claimTable.{0,160}缺少.{0,80}(?:综合|推论|全部前提)",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_cli_integration_inference_trace"
        if (
            integration_inference_trace_present
            and re.search(
                r"convert at (?:the )?CLI (?:input )?boundary and keep using Path downstream|"
                r"在 CLI 输入边界.{0,80}继续使用 Path",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"not (?:attributed|label(?:led|ed)) as (?:a )?cross-source inference|"
                r"not explicitly label(?:led|ed) as (?:a )?report inference|"
                r"without explicit (?:synthesis|cross-source inference) label(?:ling|ing)?|"
                r"(?:no|without) (?:a )?single source (?:stating|that states)|"
                r"not supported by any single source or combination(?: of sources)?|"
                r"does not cite any source.{0,180}(?:demonstrates?|recommends?|supports?)"
                r".{0,120}(?:combined workflow|combined pattern|this workflow)|"
                r"no source supports.{0,100}downstream(?:-only)? pattern|"
                r"not stated or implied by any (?:single )?primary source|"
                r"no primary source states? or implies?|"
                r"未(?:明确)?(?:归因|标注)为跨来源推论",
                text,
                re.IGNORECASE,
            )
        ):
            return "verified_cli_integration_inference_trace"
        if verified_api_list_coverage:
            return "verified_api_list_excerpt_coverage"
        concrete_api_shape = re.compile(
            r"`|\[(?:S|SRC|SOURCE)[-_ ]?\d+\]|\b(?:path_type|file_okay|dir_okay|"
            r"resolve_path|readable|writable|exists)\b|=",
            re.IGNORECASE,
        )
        if (
            visible_contextual_practice_answer
            and not concrete_api_shape.search(text)
            and re.search(
                r"(?:does not|doesn't|never|fails? to).{0,60}(?:answer|deliver).{0,100}"
                r"(?:QUESTION|requested best practices?|best practices?)|"
                r"\bno actionable best practices? (?:are |is )?(?:presented|provided|given)|"
                r"(?:meta-analysis of evidence boundaries|describes? its own methodology).{0,160}"
                r"(?:not|rather than|instead of).{0,80}(?:a list|best practices?|usable response)|"
                r"(?:role of a reviewer|critique of its own evidence).{0,180}"
                r"(?:rather than|instead of|not).{0,80}(?:usable response|answer provider|best practices?)|"
                r"(?:no actionable practices?|only meta-commentary|without delivering actionable best practices?)|"
                r"(?:Practical synthesis|Engineering-decomposition boundary).{0,180}"
                r"(?:without delivering|do not state|does not state).{0,80}(?:actionable )?best practices?|"
                r"structure.{0,100}(?:obscures rather than delivers|cannot extract).{0,100}"
                r"(?:concrete guidance|best practices?)|"
                r"Direct answer.{0,140}meta-instruction.{0,80}not a best practice|"
                r"(?:central|core) actionable claim.{0,220}(?:reasonable|supported).{0,120}"
                r"(?:fails? to present|buried in).{0,100}(?:direct answer|hedging)|"
                r"(?:central|core) claim.{0,220}(?:supported by the cited sources|reasonable engineering synthesis).{0,180}"
                r"(?:fails? to present|unsupported in the context)|"
                r"(?:没有|未能|从未).{0,60}(?:回答|给出|提供).{0,80}(?:问题|最佳实践|可执行建议)",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_contextual_practice_answer"
        if visible_contextual_practice_answer and not concrete_api_shape.search(text):
            if text.startswith("Exact official prescriptive wording is not required"):
                return "visible_contextual_practice_answer"
            if (
                re.search(
                    r"(?:拒绝|不能|不得).{0,100}(?:官方|official).{0,80}(?:升级|改写|upgrade).{0,80}(?:推荐|首选|recommend|prefer)|"
                    r"(?:official|first[- ]party).{0,100}(?:usage|example|用法|示例).{0,100}(?:not|cannot|不得|不能).{0,80}(?:recommend|prefer|推荐|首选)",
                    text,
                    re.IGNORECASE,
                )
                and re.search(
                    r"(?:QUESTION|问题|用户要求).{0,40}(?:矛盾|冲突|contradict|conflict)|"
                    r"(?:矛盾|冲突|contradict|conflict).{0,40}(?:QUESTION|问题|用户要求)",
                    text,
                    re.IGNORECASE,
                )
                and not asks_for_official_normative_wording
            ):
                return "visible_contextual_practice_answer"
            if (
                re.search(
                    r"(?:未(?:明确)?(?:回答|提供|给出)|回避|仅(?:列出|描述)).{0,100}(?:current best practices|最佳实践|规范回答|综合推荐|推荐|优先级)|"
                    r"(?:current best practices|best practices).{0,100}(?:not answered|missing|only lists?|no recommendation)|"
                    r"(?:未引用|缺少).{0,80}(?:官方|official).{0,50}(?:recommended|preferred|推荐性语言)|"
                    r"(?:未引用|缺少).{0,80}(?:官方|official).{0,80}(?:best practices?|规范性结论|规范建议)|"
                    r"(?:未|没有).{0,80}(?:推出|综合成|形成).{0,50}(?:实践建议|最佳实践|best practice)|"
                    r"(?:Practical synthesis|Mixed-source synthesis|综合判断).{0,100}(?:空泛|没有具体|no concrete|merely repeats?|only repeats?)|"
                    r"(?:隐含|暗示|implies?).{0,80}(?:best practices?|最佳实践).{0,100}(?:无|没有|without|no).{0,80}(?:官方|official).{0,80}(?:推荐语义|recommendation|normative)",
                    text,
                    re.IGNORECASE,
                )
                and not asks_for_official_normative_wording
            ):
                return "visible_contextual_practice_answer"
        if (
            source_role_boundary_present
            and re.search(
                r"(?:未区分|没有区分|not distinguish).{0,100}(?:官方|official).{0,100}(?:二手|secondary|教程|tutorial)|"
                r"(?:二手|secondary|教程|tutorial).{0,100}(?:作为主要证据|main evidence).{0,100}(?:未区分|without distinction)",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_primary_secondary_role_boundary"
        if (
            visible_contextual_practice_answer
            and re.search(
                r"Choose by CLI context, not by an unconditional library ranking|"
                r"按 CLI 场景选择，而不是做无条件库排名",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"does not cite.{0,100}primary source.{0,120}(?:best practice|official guidance)|"
                r"未引用.{0,100}一手来源.{0,120}(?:最佳实践|官方指导)",
                text,
                re.IGNORECASE,
            )
            and not asks_for_official_normative_wording
        ):
            return "visible_choice_matrix_citation_trace"
        if (
            visible_contextual_practice_answer
            and source_role_boundary_present
            and re.search(
                r"(?:secondary|二手).{0,120}(?:用于支撑|used to support).{0,100}(?:best practice|最佳实践).{0,100}(?:官方规范|official normative|official recommendation)|"
                r"(?:best practice|最佳实践).{0,100}(?:secondary|二手).{0,100}(?:official normative|官方规范)",
                text,
                re.IGNORECASE,
            )
            and not asks_for_official_normative_wording
        ):
            return "visible_primary_secondary_role_boundary"
        if (
            dedicated_secondary_boundary_present
            and secondary_excluded_from_actionable_guidance
            and re.search(r"\bS\d+\b|secondary|tutorial|二手|教程", text, re.IGNORECASE)
            and re.search(
                r"actionable guidance|guidance section|actionable practice sections?|framework-scoped applications?|"
                r"可执行指南|实践指南|可执行实践(?:部分|章节)|框架限定",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"does not clearly attribute|without (?:clearly )?(?:attributing|distinguishing)|"
                r"未(?:清楚|明确)?(?:归因|区分)",
                text,
                re.IGNORECASE,
            )
        ):
            return "secondary_excluded_from_actionable_guidance"
        if (
            missing_attribution_cue.search(text)
            and re.search(r"(?:secondary|tier\s*=\s*secondary|二手|经验来源)", text, re.IGNORECASE)
            and re.search(
                r"Secondary source\s+[\"“][^\"”]{1,180}[\"”]\s+states:\s*"
                r"You should use Path objects anywhere you work with file paths(?:\.|。)?\s*\[S\d+\]|"
                r"二手来源《[^》]{1,180}》的表述：.{0,120}应该在处理文件路径的任何地方使用 Path.{0,20}\[S\d+\]",
                integration_section,
                re.IGNORECASE | re.DOTALL,
            )
        ):
            return "visible_attributed_secondary_normative"
        if (
            missing_label_cue.search(text)
            and re.search(r"Practical synthesis|本报告的综合判断", text, re.IGNORECASE)
            and re.search(r"\*\*Practical synthesis:\*\*|\*\*本报告的综合判断：\*\*", conversion_validation_section)
            and len(set(re.findall(r"\[(S\d+)\]", conversion_validation_section, re.IGNORECASE))) >= 2
        ):
            return "visible_practical_synthesis_label"
        if (
            missing_label_cue.search(text)
            and re.search(r"mixed[- ]source|混合来源|二手来源", text, re.IGNORECASE)
            and re.search(r"Mixed-source synthesis with secondary context|含二手来源的综合判断", integration_section, re.IGNORECASE)
            and len(set(re.findall(r"\[(S\d+)\]", integration_section, re.IGNORECASE))) >= 2
        ):
            return "visible_mixed_source_label"
        if (
            missing_label_cue.search(text)
            and re.search(r"Source-backed application pattern|来源支撑的应用模式", text, re.IGNORECASE)
            and re.search(r"Source-backed application pattern|来源支撑的应用模式", configuration_section, re.IGNORECASE)
            and re.search(r"\bTyper\b", configuration_section, re.IGNORECASE)
            and re.search(r"\[S\d+\]", configuration_section, re.IGNORECASE)
        ):
            return "visible_source_backed_application_pattern"
        if (
            re.search(r"PEP\s*(?:428|519)|\bS(?:6|11)\b", text, re.IGNORECASE)
            and re.search(
                r"current[- ]practice|current best|current applicability|current guarantee|"
                r"当前(?:实践|最佳|结论|适用性|保证)",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"未(?:充分)?(?:限制|标注|说明)|does not mark|not marked|misuse|用于支撑|used to support|"
                r"without (?:a )?clear historical(?:/version)? boundary|"
                r"presented without (?:a )?clear historical boundary",
                text,
                re.IGNORECASE,
            )
            and dated_pep_scope_present
            and guidance
        ):
            cited_keys = set(re.findall(r"\b(S\d+)\b", text, re.IGNORECASE))
            cited_keys = cited_keys or {"S6", "S11"}
            if not re.search(r"PEP\s*(?:428|519)", guidance, re.IGNORECASE) and not any(
                f"[{key.upper()}]" in guidance.upper() for key in cited_keys
            ):
                return "historical_pep_excluded_from_current_guidance"
        if (
            date_label_not_deprecation_present
            and re.search(r"PEP\s*(?:428|519)|dated|date|version[- ]bound|日期|版本限定", text, re.IGNORECASE)
            and re.search(r"supersed|deprecat|\u66ff\u4ee3|废弃", text, re.IGNORECASE)
            and re.search(
                r"(?:no|without|does not|doesn't|lack).{0,120}(?:evidence|source|proof)|"
                r"(?:未提供|没有|缺乏).{0,100}(?:证据|来源)",
                text,
                re.IGNORECASE,
            )
        ):
            return "date_version_label_not_deprecation_claim"
        mentioned_undated_keys = {
            key.upper()
            for key in re.findall(r"\b(S\d+)\b", text, re.IGNORECASE)
        }
        if (
            mentioned_undated_keys
            and mentioned_undated_keys.issubset(inline_undated_boundary_keys)
            and retrieval_date_caveat_present
            and re.search(r"\bundated\b|无文档日期|无日期", text, re.IGNORECASE)
            and re.search(
                r"recent publication|current official recommendation|current(?:ly)?|"
                r"近期(?:发布|更新)|当前官方推荐|当前性",
                text,
                re.IGNORECASE,
            )
            and re.search(
                r"does not (?:consistently )?(?:flag|mark)|not (?:consistently )?(?:flagged|marked)|"
                r"cannot establish|未(?:一致|逐项)?(?:标注|说明)|不能证明",
                text,
                re.IGNORECASE,
            )
        ):
            return "visible_undated_source_boundaries"
        return ""

    original_reasons = _research_text_list(normalized.get("reviewReasons"), limit=12)
    original_unsupported = _research_text_list(normalized.get("unsupportedClaims"), limit=12)
    removed_surface_reasons = [
        issue for issue in original_reasons if exact_surface_contradiction(issue)
    ]
    removed_surface_unsupported = [
        issue for issue in original_unsupported if exact_surface_contradiction(issue)
    ]
    if removed_surface_reasons or removed_surface_unsupported:
        kept_reasons = [
            issue for issue in original_reasons if issue not in removed_surface_reasons
        ]
        kept_unsupported = [
            issue for issue in original_unsupported if issue not in removed_surface_unsupported
        ]
        reconciliation_kinds = list(
            dict.fromkeys(
                exact_surface_contradiction(issue)
                for issue in [*removed_surface_reasons, *removed_surface_unsupported]
                if exact_surface_contradiction(issue)
            )
        )
        normalized.update(
            {
                "reviewReasons": kept_reasons,
                "unsupportedClaims": kept_unsupported,
                "reconciledVisibleAnswerIssues": list(
                    dict.fromkeys(
                        [
                            *_research_text_list(
                                normalized.get("reconciledVisibleAnswerIssues"),
                                limit=12,
                            ),
                            *removed_surface_reasons,
                            *removed_surface_unsupported,
                        ]
                    )
                )[:12],
                "reconciledVisibleAnswerKinds": reconciliation_kinds,
            }
        )
        if not kept_unsupported:
            normalized["claimEntailment"] = True
        if {
            "historical_pep_excluded_from_current_guidance",
            "date_version_label_not_deprecation_claim",
            "visible_undated_source_boundaries",
        }.intersection(reconciliation_kinds):
            normalized["freshnessAdequacy"] = True
        if {
            "visible_contextual_practice_answer",
            "visible_choice_matrix_citation_trace",
            "verified_cli_integration_inference_trace",
        }.intersection(reconciliation_kinds):
            normalized["questionCoverage"] = True

    if normalized.get("claimEntailment") is not True or normalized.get("freshnessAdequacy") is not True:
        return normalized

    unsupported_claims = _research_text_list(normalized.get("unsupportedClaims"), limit=12)
    if (
        unsupported_claims
        or _research_text_list(normalized.get("criticalMissingEvidence"), limit=12)
        or _research_text_list(normalized.get("recommendedNextQueries"), limit=4)
    ):
        return normalized
    if (
        (removed_surface_reasons or removed_surface_unsupported)
        and not _research_text_list(normalized.get("reviewReasons"), limit=12)
        and normalized.get("questionCoverage") is True
    ):
        normalized["reviewDecision"] = "accept"
        return normalized

    synthesis_units = [
        unit
        for unit in _architect_section_content_units(candidate_answer)
        if _ARCHITECT_SYNTHESIS_LABEL_RE.search(unit)
        and len(set(re.findall(r"\[(S\d+)\]", unit, re.IGNORECASE))) >= 2
    ]
    if len(synthesis_units) < 2:
        return normalized

    framework_units = [
        unit
        for unit in synthesis_units
        if len(
            {
                name
                for name in ("argparse", "click", "typer")
                if re.search(rf"\b{re.escape(name)}\b", unit, re.IGNORECASE)
            }
        )
        >= 2
    ]
    validation_units = [
        unit
        for unit in synthesis_units
        if re.search(
            r"\b(?:exists|file_okay|dir_okay|validation|validate|path_type|path conversion|"
            r"application directory|configuration)\b|(?:存在性|文件.?目录|输入验证|路径转换|应用目录|配置目录)",
            unit,
            re.IGNORECASE,
        )
    ]
    platform_units = [
        unit
        for unit in synthesis_units
        if re.search(r"\b(?:cross[- ]platform|platform semantics|application directory)\b|(?:跨平台|平台语义|应用目录)", unit, re.IGNORECASE)
    ]

    absence_cue = re.compile(
        r"\b(?:no|not|lack(?:s|ing)?|missing|omit(?:s|ted)?|fails? to|does not|doesn't|"
        r"only (?:lists?|catalogs?|summari[sz]es?))\b|(?:没有|未能?|缺少|欠缺|遗漏|仅(?:列出|罗列|汇总))",
        re.IGNORECASE,
    )
    synthesis_topic = re.compile(
        r"\b(?:cross[- ](?:source|library)|synthesis|guidance|best practices?|recommendations?)\b|"
        r"(?:跨来源|跨库|综合(?:判断|建议)?|实践指南|最佳实践|可执行建议|指导|指南)",
        re.IGNORECASE,
    )
    official_normative_topic = re.compile(
        r"(?:official|first[- ]party|authoritative).{0,80}(?:recommend|guidance|best practice|preferred)|"
        r"(?:官方|一手|权威).{0,50}(?:推荐|建议|指南|最佳实践|首选)",
        re.IGNORECASE,
    )
    asks_for_official_normative_wording = bool(
        re.search(
            r"(?:what|which).{0,60}(?:official|documentation|docs).{0,60}(?:recommend|require|prefer)|"
            r"(?:官方|权威)(?:文档|资料|来源)?.{0,16}(?:明确)?(?:推荐|要求|首选)(?:什么|哪些|哪种|的)",
            _safe_text(question),
            re.IGNORECASE,
        )
    )
    concrete_fact_dispute = re.compile(
        r"\b(?:unsupported|not supported|not entailed|overstates?|contradicts?|incorrect|inaccurate|"
        r"fabricated|hallucinated|wrong|false|misstates?|claims? that|states? that|asserts? that)\b|"
        r"(?:无证据支持|并非摘录所述|夸大|矛盾|错误|不准确|捏造|声称)",
        re.IGNORECASE,
    )
    concrete_fact_shape = re.compile(
        r"`|[\"“”]|\[(?:S|SRC|SOURCE)[-_ ]?\d+\]|\b[a-z][a-z0-9]*_[a-z0-9_]+\b|"
        r"\b\d+(?:\.\d+)+(?:\b|\.)|=",
        re.IGNORECASE,
    )

    def objectively_present(issue: str) -> bool:
        text = _safe_text(issue)
        if not text or concrete_fact_dispute.search(text) or concrete_fact_shape.search(text):
            return False
        if official_normative_topic.search(text) and not asks_for_official_normative_wording:
            return True
        if not (absence_cue.search(text) and synthesis_topic.search(text)):
            return False
        if re.search(r"\b(?:argparse|click|typer|cross[- ]library|integration)\b|(?:跨库|接入)", text, re.IGNORECASE):
            return bool(framework_units)
        if re.search(r"\b(?:validat|exists|file|directory|configuration)\b|(?:验证|存在|文件|目录|配置)", text, re.IGNORECASE):
            return bool(validation_units)
        if re.search(r"\b(?:cross[- ]platform|platform)\b|(?:跨平台|平台)", text, re.IGNORECASE):
            return bool(platform_units)
        return True

    review_reasons = _research_text_list(normalized.get("reviewReasons"), limit=12)
    removed_reasons = [issue for issue in review_reasons if objectively_present(issue)]
    if not removed_reasons:
        return normalized

    kept_reasons = [issue for issue in review_reasons if issue not in removed_reasons]
    normalized.update(
        {
            "reviewReasons": kept_reasons,
            "unsupportedClaims": unsupported_claims,
            "reconciledVisibleAnswerIssues": removed_reasons,
        }
    )
    if (
        not kept_reasons
        and not unsupported_claims
        and not _research_text_list(normalized.get("criticalMissingEvidence"), limit=12)
        and not _research_text_list(normalized.get("recommendedNextQueries"), limit=4)
    ):
        normalized.update(
            {
                "reviewDecision": "accept",
                "questionCoverage": True,
            }
        )
    return normalized


def _independent_architect_review_schema_valid(value: Any) -> bool:
    value = _adapt_independent_architect_review_schema(value)
    if not isinstance(value, dict):
        return False
    if _safe_text(value.get("reviewDecision")).lower() not in {"accept", "retry", "reject"}:
        return False
    if not all(isinstance(value.get(field), bool) for field in ("questionCoverage", "claimEntailment", "freshnessAdequacy")):
        return False
    return all(
        isinstance(value.get(field), list)
        for field in (
            "reviewReasons",
            "unsupportedClaims",
            "criticalMissingEvidence",
            "recommendedNextQueries",
        )
    )


def _independent_architect_review_accepts(value: Any) -> bool:
    review = _normalize_independent_architect_review(value)
    return bool(
        review.get("reviewDecision") == "accept"
        and review.get("questionCoverage") is True
        and review.get("claimEntailment") is True
        and review.get("freshnessAdequacy") is True
        and not review.get("unsupportedClaims")
        and not review.get("criticalMissingEvidence")
        and not review.get("recommendedNextQueries")
    )


def _independent_architect_review_consensus_accepts(value: Any) -> bool:
    review = _normalize_independent_architect_review(value)
    consensus_reviews = [
        item for item in list(review.get("consensusReviews") or []) if isinstance(item, dict)
    ]
    reviewer_ids = [_safe_text(item.get("reviewerModelId")) for item in consensus_reviews]
    reported_ids = [
        _safe_text(model_id)
        for model_id in list(review.get("consensusReviewerModelIds") or [])[:4]
        if _safe_text(model_id)
    ]
    modes = {_safe_text(item.get("reviewMode")).lower() for item in consensus_reviews}
    call_identities = {
        (
            _safe_text(item.get("reviewerModelId")),
            _safe_text(item.get("reviewMode")).lower(),
            _safe_text(item.get("reviewedAt")),
        )
        for item in consensus_reviews
    }
    return bool(
        review.get("consensusAccepted") is True
        and int(review.get("consensusReviewCount") or 0) == len(consensus_reviews)
        and len(consensus_reviews) >= 2
        and len(call_identities) == len(consensus_reviews)
        and modes.issuperset({"semantic", "adversarial"})
        and all(reviewer_ids)
        and reported_ids == reviewer_ids
        and all(_independent_architect_review_accepts(item) for item in consensus_reviews)
    )


def _research_architect_fallback_model_refs() -> list[str]:
    raw = os.environ.get("V8_RESEARCH_ARCHITECT_MODEL_FALLBACKS", "")
    refs = [item.strip() for item in re.split(r"[,;\n]+", raw) if item.strip()] if raw else []
    deduped: list[str] = []
    for ref in refs:
        if ref and ref not in deduped:
            deduped.append(ref)
    return deduped


def _create_web_research_architect_llm_candidates() -> list[tuple[Any, str, str]]:
    from core.llm_factory import llm_factory

    agent_id = "web-research-architect"
    candidates: list[tuple[Any, str, str]] = []
    seen: set[str] = set()

    def add_candidate(
        factory,
        *,
        model_id: str,
        role: str,
        resolved_model_ref: str = "",
        selection_origin: str,
    ) -> None:
        key = _safe_text(resolved_model_ref or model_id).lower()
        if key in seen:
            return
        seen.add(key)
        llm = factory()
        meta = getattr(llm, "_meta", None)
        if isinstance(meta, dict):
            meta.setdefault("research_candidate_origin", selection_origin)
        candidates.append((llm, model_id, role))

    try:
        model_id = _safe_text(storage.get_agent_model_binding(agent_id))
    except Exception:
        model_id = ""
    if model_id:
        add_candidate(
            lambda model_id=model_id: llm_factory.create_chat_model(model_id, temperature=0.1, max_tokens=7500, _role=agent_id),
            model_id=model_id,
            role=agent_id,
            resolved_model_ref=model_id,
            selection_origin="agent_binding",
        )
    last_error: Exception | None = None
    # `research` is a runtime kind, not a registered Config Broker model role.
    # Using it here silently falls through to an unrelated default model and
    # bypasses the explicit Architect binding. A generic ``summary`` role is
    # also not an Architect: letting it repair or replace a dedicated binding
    # changes the information chain and makes provider/model behavior appear
    # nondeterministic. Use the registered Subagent role only when the dedicated
    # binding is absent; additional Architect models must be explicitly listed
    # in V8_RESEARCH_ARCHITECT_MODEL_FALLBACKS.
    fallback_roles = () if model_id else ("subagent",)
    for role in fallback_roles:
        try:
            resolved_model_ref = _safe_text(storage.get_role_model_id(role))
            add_candidate(
                lambda role=role: llm_factory.create_for_role(role, temperature=0.1, max_tokens=7500),
                model_id=f"role:{role}",
                role=role,
                resolved_model_ref=resolved_model_ref or f"role:{role}",
                selection_origin=f"role_fallback:{role}",
            )
        except Exception as exc:  # noqa: BLE001 - role fallback is intentional.
            last_error = exc
    for model_ref in _research_architect_fallback_model_refs():
        try:
            add_candidate(
                lambda model_ref=model_ref: llm_factory.create_chat_model(model_ref, temperature=0.1, max_tokens=7500, _role=agent_id),
                model_id=model_ref,
                role=agent_id,
                resolved_model_ref=model_ref,
                selection_origin="configured_fallback",
            )
        except Exception as exc:  # noqa: BLE001 - try the next explicitly configured model.
            last_error = exc
    if not candidates:
        try:
            add_candidate(
                lambda: llm_factory.create_for_role("supervisor", temperature=0.1, max_tokens=7500),
                model_id="role:supervisor",
                role="supervisor",
                resolved_model_ref=_safe_text(storage.get_role_model_id("supervisor")) or "role:supervisor",
                selection_origin="role_fallback:supervisor",
            )
        except Exception as exc:  # noqa: BLE001 - final configured-role fallback.
            last_error = exc
    if candidates:
        return candidates
    if last_error:
        raise last_error
    raise ValueError("No model configured for Web Research Architect synthesis.")


def _research_agent_stage_system_prompt(
    stage_prompt: str,
    *,
    stage: str,
) -> str:
    """Assemble one Runtime-owned stage contract for the bound model.

    Agent registration still resolves the configured provider/model. Managed
    Agent Markdown is deliberately excluded so edits to that user-facing file
    cannot alter Research execution, evidence policy, or delivery semantics.
    """

    return build_research_runtime_system_prompt(
        stage=stage,
        stage_prompt=stage_prompt,
    )


def _create_web_research_reviewer_llm_candidates(
    architect_candidates: list[tuple[Any, str, str]],
) -> list[tuple[Any, str, str]]:
    """Prefer governed, model-distinct reviewers for production research.

    The deterministic renderer is not a model writer, but reusing the planning
    model for both reviews still creates a correlated blind spot.  The actual
    Supervisor model is the most relevant independent consumer for the first
    review.  A separately bound verification engineer is preferred for the
    adversarial pass, while the Research Architect remains available only after
    those independent consumers. Test fixtures and legacy callers without a
    real agent-binding origin keep their supplied candidate order.
    """

    candidates = list(architect_candidates)
    if not any(
        _architect_candidate_selection_origin(candidate) == "agent_binding"
        for candidate in candidates
    ):
        return candidates
    from core.llm_factory import llm_factory

    governed_reviewers: list[tuple[Any, str, str]] = []
    try:
        reviewer = llm_factory.create_for_role(
            "supervisor",
            temperature=0.0,
            max_tokens=_RESEARCH_ARCHITECT_REVIEW_MAX_TOKENS,
        )
        meta = getattr(reviewer, "_meta", None)
        if isinstance(meta, dict):
            meta.setdefault("research_candidate_origin", "role_reviewer:supervisor")
        governed_reviewers.append((reviewer, "role:supervisor", "supervisor"))
    except Exception:
        pass

    verification_agent_id = "verification-engineer"
    try:
        verification_model_ref = _safe_text(
            storage.get_agent_model_binding(verification_agent_id)
        )
    except Exception:
        verification_model_ref = ""
    if verification_model_ref:
        try:
            reviewer = llm_factory.create_chat_model(
                verification_model_ref,
                temperature=0.0,
                max_tokens=_RESEARCH_ARCHITECT_REVIEW_MAX_TOKENS,
                _role=verification_agent_id,
            )
            meta = getattr(reviewer, "_meta", None)
            if isinstance(meta, dict):
                meta.setdefault(
                    "research_candidate_origin",
                    "agent_reviewer:verification-engineer",
                )
            governed_reviewers.append(
                (reviewer, verification_model_ref, verification_agent_id)
            )
        except Exception:
            pass

    ordered: list[tuple[Any, str, str]] = []
    seen: set[str] = set()
    for candidate in [*governed_reviewers, *candidates]:
        identity = _architect_candidate_identity(candidate)
        if identity in seen:
            continue
        seen.add(identity)
        ordered.append(candidate)
    return ordered


def _architect_candidate_identity(candidate: tuple[Any, str, str]) -> str:
    llm, model_id, _role = candidate
    meta = getattr(llm, "_meta", None)
    if isinstance(meta, dict):
        resolved = _safe_text(meta.get("model_ref") or meta.get("modelRef"))
        if resolved:
            return resolved.lower()
        provider = _safe_text(meta.get("provider_id") or meta.get("provider_name"))
        wire_model = _safe_text(meta.get("model_id")) or _safe_text(getattr(llm, "model_id", ""))
        if provider and wire_model:
            return f"{provider}::{wire_model}".lower()
    return _safe_text(model_id).lower()


def _architect_candidate_context_model_ref(candidate: tuple[Any, str, str]) -> str:
    """Return the case-preserving model ref used by the model control plane."""

    llm, model_id, _role = candidate
    meta = getattr(llm, "_meta", None)
    if isinstance(meta, dict):
        resolved = _safe_text(meta.get("model_ref") or meta.get("modelRef"))
        if resolved:
            return resolved
        provider = _safe_text(meta.get("provider_id") or meta.get("provider_name"))
        wire_model = _safe_text(meta.get("model_id")) or _safe_text(
            getattr(llm, "model_id", "")
        )
        if provider and wire_model:
            return f"{provider}::{wire_model}"
    return _safe_text(model_id)


def _architect_candidate_selection_origin(candidate: tuple[Any, str, str]) -> str:
    meta = getattr(candidate[0], "_meta", None)
    if isinstance(meta, dict):
        origin = _safe_text(meta.get("research_candidate_origin"))
        if origin:
            return origin
    role = _safe_text(candidate[2]).lower()
    return f"role_fallback:{role}" if role else "unknown"


def _ordered_architect_candidates(
    candidates: list[tuple[Any, str, str]],
    *,
    prefer_distinct_from: str = "",
    prefer_role: str = "",
) -> list[tuple[Any, str, str]]:
    distinct_from = _safe_text(prefer_distinct_from).lower()
    role_hint = _safe_text(prefer_role).lower()
    return sorted(
        candidates,
        key=lambda candidate: (
            0 if distinct_from and _architect_candidate_identity(candidate) != distinct_from else 1,
            0 if role_hint and _safe_text(candidate[2]).lower() == role_hint else 1,
        ),
    )


def _research_answer_from_model_output(value: Any) -> tuple[str, bool]:
    raw = _safe_text(value)
    complete = _RESEARCH_ARCHITECT_ANSWER_COMPLETE_MARKER in raw
    answer = raw.replace(_RESEARCH_ARCHITECT_ANSWER_COMPLETE_MARKER, "").strip()
    fenced = re.fullmatch(r"```(?:markdown|md)?\s*(.*?)\s*```", answer, re.DOTALL | re.IGNORECASE)
    if fenced:
        answer = fenced.group(1).strip()
    return _normalize_research_citation_tokens(answer), complete


def _architect_response_finish_reason(response: Any) -> str:
    for container in (
        getattr(response, "response_metadata", None),
        getattr(response, "additional_kwargs", None),
    ):
        if not isinstance(container, dict):
            continue
        for key in ("finish_reason", "finishReason", "stop_reason", "stopReason"):
            value = _safe_text(container.get(key)).lower()
            if value:
                return value
    return ""


def _architect_response_safe_diagnostics(response: Any, sanitized: Any) -> dict[str, Any]:
    """Return bounded response metadata without retaining model text or reasoning."""

    response_metadata = getattr(response, "response_metadata", None)
    response_metadata = dict(response_metadata or {}) if isinstance(response_metadata, dict) else {}
    usage_metadata = getattr(response, "usage_metadata", None)
    usage_metadata = dict(usage_metadata or {}) if isinstance(usage_metadata, dict) else {}
    token_usage = response_metadata.get("token_usage") or response_metadata.get("usage") or {}
    token_usage = dict(token_usage) if isinstance(token_usage, dict) else {}
    completion_details = (
        token_usage.get("completion_tokens_details")
        or usage_metadata.get("output_token_details")
        or {}
    )
    completion_details = dict(completion_details) if isinstance(completion_details, dict) else {}

    def first_positive_int(*values: Any) -> int | None:
        for value in values:
            parsed = _research_positive_int(value)
            if parsed is not None:
                return parsed
        return None

    raw_content = getattr(response, "content", None)
    diagnostics = dict(sanitized.diagnostics() or {}) if hasattr(sanitized, "diagnostics") else {}
    diagnostics.update(
        {
            "rawContentType": type(raw_content).__name__ if raw_content is not None else "none",
            "rawContentPresent": raw_content not in (None, "", []),
            "rawContentChars": len(str(raw_content or "")),
            "inputTokens": first_positive_int(
                usage_metadata.get("input_tokens"),
                token_usage.get("prompt_tokens"),
                token_usage.get("input_tokens"),
            ),
            "completionTokens": first_positive_int(
                usage_metadata.get("output_tokens"),
                token_usage.get("completion_tokens"),
                token_usage.get("output_tokens"),
            ),
            "reasoningTokens": extract_reasoning_token_count(completion_details),
            "totalTokens": first_positive_int(
                usage_metadata.get("total_tokens"),
                token_usage.get("total_tokens"),
            ),
        }
    )
    return {key: value for key, value in diagnostics.items() if value not in (None, "", [], {})}


def _research_positive_int(value: Any) -> int | None:
    try:
        parsed = int(value)
    except (TypeError, ValueError):
        return None
    return parsed if parsed > 0 else None


def _architect_segmented_writer_profile(candidate: tuple[Any, str, str]) -> dict[str, Any]:
    llm = candidate[0]
    meta = getattr(llm, "_meta", None)
    metadata = dict(meta or {}) if isinstance(meta, dict) else {}
    configured_limit = _research_positive_int(
        metadata.get("global_max_tokens")
        or metadata.get("max_output_tokens")
        or metadata.get("maxOutputTokens")
    )
    # Every candidate created by the production Research router carries an
    # origin. Research targets a reusable 6.5k-11k character answer, so those
    # candidates always use bounded, independently evidenced sections. The
    # legacy capacity rule remains for direct/test callers that do not enter
    # through the Research routing contract.
    routed_research_candidate = bool(metadata.get("research_candidate_origin"))
    enabled = routed_research_candidate or (
        bool(configured_limit <= 8_192) if configured_limit is not None else True
    )
    if configured_limit is not None and configured_limit <= 1_600:
        section_count = 6
    elif configured_limit is not None and configured_limit <= 2_500:
        section_count = 5
    else:
        section_count = 4
    max_tokens = min(
        _RESEARCH_ARCHITECT_SECTION_MAX_TOKENS,
        configured_limit or _RESEARCH_ARCHITECT_SECTION_MAX_TOKENS,
    )
    return {
        "enabled": enabled,
        "configuredMaxTokens": configured_limit,
        "sectionCount": section_count,
        "sectionMaxTokens": max_tokens,
        "targetMinChars": max(
            900,
            (_RESEARCH_ARCHITECT_IDEAL_ANSWER_MIN_CHARS + section_count - 1) // section_count,
        ),
        "targetMaxChars": max(
            1_500,
            (_RESEARCH_ARCHITECT_IDEAL_ANSWER_MAX_CHARS + section_count - 1) // section_count,
        ),
    }


def _architect_outline_text(value: Any) -> str:
    if isinstance(value, dict):
        parts = [
            _safe_text(value.get(key))
            for key in ("title", "heading", "objective", "question", "summary", "description")
        ]
        return _compact_research_text(" - ".join(part for part in parts if part), limit=360)
    return _compact_research_text(value, limit=360)


def _architect_claim_facet_ids(claim: dict[str, Any]) -> list[str]:
    facet_ids: list[str] = []
    for value in (
        claim.get("facetIds"),
        claim.get("researchFacetIds"),
        [claim.get("researchFacetId")],
        [claim.get("taskBriefId")],
    ):
        for facet_id in list(value or []):
            normalized = _safe_text(facet_id)
            if normalized and normalized not in facet_ids:
                facet_ids.append(normalized)
    for support in list(claim.get("supportingSources") or []):
        if not isinstance(support, dict):
            continue
        for value in [
            *list(support.get("researchFacetIds") or []),
            support.get("researchFacetId"),
            support.get("taskBriefId"),
        ]:
            normalized = _safe_text(value)
            if normalized and normalized not in facet_ids:
                facet_ids.append(normalized)
    return facet_ids


def _architect_outline_claim_groups(value: Any, claims: list[dict[str, Any]]) -> list[dict[str, Any]]:
    claim_by_id = {
        _safe_text(claim.get("claimId")): claim
        for claim in claims
        if _safe_text(claim.get("claimId"))
    }
    if len(claim_by_id) != len(claims):
        return []
    raw_sections = value if isinstance(value, list) else []
    if not 2 <= len(raw_sections) <= 6:
        return []
    seen: set[str] = set()
    groups: list[dict[str, Any]] = []
    for raw_section in raw_sections:
        if not isinstance(raw_section, dict):
            return []
        claim_ids = [
            _safe_text(claim_id)
            for claim_id in list(raw_section.get("claimIds") or [])
            if _safe_text(claim_id)
        ]
        if not claim_ids or len(claim_ids) != len(set(claim_ids)):
            return []
        if any(claim_id not in claim_by_id or claim_id in seen for claim_id in claim_ids):
            return []
        seen.update(claim_ids)
        groups.append(
            {
                "section": raw_section,
                "claimIds": claim_ids,
                "claims": [claim_by_id[claim_id] for claim_id in claim_ids],
            }
        )
    return groups if seen == set(claim_by_id) else []


def _architect_outline_inference_issues(
    outline: Any,
    claims: list[dict[str, Any]],
    inferences: Any,
) -> list[str]:
    groups = _architect_outline_claim_groups(outline, claims)
    if not groups:
        return ["answer_outline_claim_binding_invalid"]
    group_ids = [set(group["claimIds"]) for group in groups]
    issues: list[str] = []
    known_claim_ids = set().union(*group_ids)
    for index, inference in enumerate(_research_dict_list(inferences, limit=12), start=1):
        premise_ids = {
            _safe_text(value)
            for value in list(inference.get("premiseClaimIds") or [])
            if _safe_text(value)
        }
        if not premise_ids or not premise_ids.issubset(known_claim_ids):
            issues.append(f"composite_inference_{index}_premise_invalid")
        elif not any(premise_ids.issubset(group) for group in group_ids):
            issues.append(f"composite_inference_{index}_crosses_sections")
    return issues


def _architect_filter_composite_inferences(
    outline: Any,
    claims: list[dict[str, Any]],
    inferences: Any,
    *,
    validate_semantics: bool = True,
) -> tuple[list[dict[str, Any]], list[str]]:
    groups = _architect_outline_claim_groups(outline, claims)
    if not groups:
        return [], ["answer_outline_claim_binding_invalid"]
    group_ids = [set(group["claimIds"]) for group in groups]
    known_claim_ids = set().union(*group_ids)
    claim_by_id = {
        _safe_text(claim.get("claimId")): claim
        for claim in claims
        if _safe_text(claim.get("claimId"))
    }
    accepted: list[dict[str, Any]] = []
    dropped: list[str] = []
    for index, inference in enumerate(_research_dict_list(inferences, limit=12), start=1):
        premise_ids = {
            _safe_text(value)
            for value in list(inference.get("premiseClaimIds") or [])
            if _safe_text(value)
        }
        if not premise_ids or not premise_ids.issubset(known_claim_ids):
            dropped.append(f"composite_inference_{index}_premise_invalid")
        elif not any(premise_ids.issubset(group) for group in group_ids):
            dropped.append(f"composite_inference_{index}_crosses_sections")
        else:
            inference_text = _compact_research_text(
                inference.get("inference")
                or inference.get("conclusion")
                or inference.get("recommendation")
                or inference.get("claim")
                or inference.get("summary"),
                limit=360,
            )
            if len(inference_text) < 20:
                dropped.append(f"composite_inference_{index}_text_missing")
                continue
            premise_claims = [claim_by_id[claim_id] for claim_id in premise_ids]
            evidence_text = "\n".join(
                _safe_text(claim.get("evidenceExcerpt")) for claim in premise_claims
            )
            source_metadata = {
                "title": " ".join(
                    _safe_text(support.get("title"))
                    for claim in premise_claims
                    for support in list(claim.get("supportingSources") or [])
                    if isinstance(support, dict)
                ),
                "url": " ".join(
                    _safe_text(support.get("url") or support.get("sourceUrl"))
                    for claim in premise_claims
                    for support in list(claim.get("supportingSources") or [])
                    if isinstance(support, dict)
                ),
            }
            if validate_semantics:
                inference_issues = _architect_hard_assertion_issues(
                    inference_text,
                    evidence_text,
                    source_metadata=source_metadata,
                )
                inference_issues.extend(
                    _architect_source_role_issues(inference_text, premise_claims)
                )
                if inference_issues:
                    dropped.append(
                        f"composite_inference_{index}_unsupported:{inference_issues[0]}"
                    )
                    continue
            accepted.append({**inference, "inference": inference_text})
    return accepted, dropped


def _architect_claim_citation_checklist(claims: list[dict[str, Any]]) -> list[dict[str, Any]]:
    checklist: list[dict[str, Any]] = []
    for claim in claims:
        citation_keys: list[str] = []
        for support in list(claim.get("supportingSources") or []):
            value = (
                support.get("citationKey") or support.get("citation")
                if isinstance(support, dict)
                else support
            )
            citation_key = _safe_text(value).strip("[]")
            if citation_key and citation_key not in citation_keys:
                citation_keys.append(citation_key)
        checklist.append(
            {
                "claimId": _safe_text(claim.get("claimId")),
                "citationKeys": citation_keys,
            }
        )
    return checklist


def _section_minimum_acceptable_chars(target_min_chars: int, *, floor: int) -> int:
    target = max(0, int(target_min_chars or 0))
    if target <= 0:
        return 0
    # Local sections are evidence-density dependent. The whole-answer gate
    # still enforces the 3k hard floor and 5k normal target; a 50% local
    # allowance prevents a valid, concise section from being rejected merely
    # because filtering removed a few unsupported model sentences.
    return min(target, max(floor, int(target * 0.50)))


def _claim_bounded_section_minimum_chars(
    target_min_chars: int,
    claims: list[dict[str, Any]],
    *,
    floor: int,
) -> int:
    minimum = _section_minimum_acceptable_chars(target_min_chars, floor=floor)
    if len(claims) != 1:
        return minimum
    claim = claims[0]
    claim_density = len(re.sub(r"\s+", "", _safe_text(claim.get("claim"))))
    # A single narrow claim cannot honestly support a long local quota. Base
    # the leaf allowance on the verified claim itself, not a long raw excerpt
    # that would invite padding; the whole-answer gate remains authoritative.
    evidence_cap = max(80, min(180, claim_density or 80))
    return min(minimum, evidence_cap)


def _architect_segment_tasks(
    verified_plan: dict[str, Any],
    *,
    section_count: int,
    target_min_chars: int,
    target_max_chars: int,
) -> list[dict[str, Any]]:
    claims = _research_dict_list(
        verified_plan.get("claimTable"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    )
    if not claims:
        return []
    outline_groups = _architect_outline_claim_groups(verified_plan.get("answerOutline"), claims)
    count = len(outline_groups) if outline_groups else max(1, min(int(section_count or 1), len(claims)))
    balanced_outline_claims: list[list[dict[str, Any]]] | None = None
    if (
        outline_groups
        and len(claims) >= count * 2
        and any(len(group["claims"]) < 2 for group in outline_groups)
        and not _research_dict_list(
            verified_plan.get("compositeInferences"),
            limit=12,
        )
    ):
        ordered_claims = [
            claim
            for group in outline_groups
            for claim in list(group.get("claims") or [])
        ]
        base_count, remainder = divmod(len(ordered_claims), count)
        balanced_outline_claims = []
        cursor = 0
        for index in range(count):
            group_size = base_count + (1 if index < remainder else 0)
            balanced_outline_claims.append(ordered_claims[cursor : cursor + group_size])
            cursor += group_size
    outline = list(verified_plan.get("answerOutline") or [])
    question = _safe_text(verified_plan.get("question"))
    required_facet_goals = {
        _safe_text(item.get("facetId")): _safe_text(item.get("goal"))
        for item in _research_dict_list(verified_plan.get("requiredFacets"), limit=24)
        if _safe_text(item.get("facetId"))
    }
    for claim in claims:
        for support in list(claim.get("supportingSources") or []):
            if not isinstance(support, dict):
                continue
            support_goal = _safe_text(
                support.get("researchFacetGoal") or support.get("evidenceQuery")
            )
            if not support_goal:
                continue
            for facet_id in [
                *list(support.get("researchFacetIds") or []),
                support.get("researchFacetId"),
            ]:
                normalized_facet_id = _safe_text(facet_id)
                if normalized_facet_id:
                    required_facet_goals.setdefault(
                        normalized_facet_id,
                        support_goal,
                    )
    required_deliverables = _research_dict_list(
        verified_plan.get("requiredDeliverables"),
        limit=8,
    )
    requires_synthesis_conclusion = bool(
        re.search(
            r"\b(?:best practices?|recommend(?:ation|ed|s|ing)?|should)\b|(?:最佳实践|建议|应该|应当)",
            question,
            re.IGNORECASE,
        )
    )
    # The profile target is derived from its configured section count. A
    # valid planner outline may contain a different 4-6 section count; do not
    # accidentally inflate the whole-answer target when that happens.
    profile_section_count = max(1, int(section_count or count))
    total_target_min_chars = target_min_chars * profile_section_count
    total_target_max_chars = target_max_chars * profile_section_count
    allocated_target_min_chars = 0
    allocated_target_max_chars = 0
    tasks: list[dict[str, Any]] = []
    for index in range(count):
        if outline_groups:
            assigned_claims = (
                balanced_outline_claims[index]
                if balanced_outline_claims is not None
                else outline_groups[index]["claims"]
            )
        else:
            start = (index * len(claims)) // count
            end = ((index + 1) * len(claims)) // count
            assigned_claims = claims[start:end]
        citation_keys: list[str] = []
        for claim in assigned_claims:
            for support in list(claim.get("supportingSources") or []):
                value = (
                    support.get("citationKey") or support.get("citation")
                    if isinstance(support, dict)
                    else support
                )
                citation_key = _safe_text(value).strip("[]")
                if citation_key and citation_key not in citation_keys:
                    citation_keys.append(citation_key)
        outline_hint = _architect_outline_text(outline[index]) if index < len(outline) else ""
        assigned_claim_ids = {
            _safe_text(claim.get("claimId"))
            for claim in assigned_claims
            if _safe_text(claim.get("claimId"))
        }
        assigned_facet_ids = list(
            dict.fromkeys(
                facet_id
                for claim in assigned_claims
                for facet_id in _architect_claim_facet_ids(claim)
            )
        )
        relevant_inferences = [
            inference
            for inference in _research_dict_list(verified_plan.get("compositeInferences"), limit=12)
            if (
                premise_ids := {
                    _safe_text(value)
                    for value in list(inference.get("premiseClaimIds") or [])
                    if _safe_text(value)
                }
            )
            and premise_ids.issubset(assigned_claim_ids)
        ]
        claim_weight = len(assigned_claims) / max(1, len(claims))
        section_target_min_chars = (
            max(700, int(round(total_target_min_chars * claim_weight)))
            if total_target_min_chars >= MIN_RESEARCH_ANSWER_CHARS
            else target_min_chars
        )
        section_target_max_chars = (
            max(1_100, int(round(total_target_max_chars * claim_weight)))
            if total_target_max_chars >= MIN_RESEARCH_ANSWER_CHARS
            else target_max_chars
        )
        if index == count - 1:
            section_target_min_chars = max(
                section_target_min_chars,
                total_target_min_chars - allocated_target_min_chars,
            )
            section_target_max_chars = max(
                section_target_max_chars,
                total_target_max_chars - allocated_target_max_chars,
            )
        allocated_target_min_chars += section_target_min_chars
        allocated_target_max_chars += section_target_max_chars
        acceptable_floor = 350 if len(assigned_claims) <= 2 else 450
        permitted_hard_anchors: list[dict[str, str]] = []
        verbatim_code_blocks: list[str] = []
        for claim in assigned_claims:
            claim_text = _safe_text(claim.get("claim"))
            claim_hard_anchors = _architect_hard_assertion_anchors(claim_text)
            # The excerpt may contain adjacent model names, directories, limits,
            # or versions that are not part of the verified claim.  Let it prove
            # the claim, but do not turn those incidental details into Writer
            # permissions.
            for kind, value in claim_hard_anchors:
                item = {"kind": kind, "value": value}
                if item not in permitted_hard_anchors:
                    permitted_hard_anchors.append(item)
            for match in re.finditer(
                r"```(?:[a-z0-9_+.-]+)?\s*([\s\S]*?)```",
                _safe_text(claim.get("evidenceExcerpt")),
                re.IGNORECASE,
            ):
                code = match.group(1).strip()
                if (
                    code
                    and claim_hard_anchors
                    and not _architect_hard_assertion_issues(claim_text, code)
                    and code not in verbatim_code_blocks
                ):
                    verbatim_code_blocks.append(code)
        tasks.append(
            {
                "sectionId": f"section_{index + 1}",
                "sequence": index + 1,
                "sectionCount": count,
                "outlineHint": outline_hint,
                "facetIds": assigned_facet_ids,
                "facetGoals": [
                    {
                        "facetId": facet_id,
                        "goal": required_facet_goals.get(facet_id) or facet_id,
                    }
                    for facet_id in assigned_facet_ids
                ],
                "deliverableGoals": required_deliverables if index == count - 1 else [],
                "assignedClaims": assigned_claims,
                "compositeInferences": relevant_inferences,
                "conflictMatrix": verified_plan.get("conflictMatrix") or [] if index == count - 1 else [],
                "temporalAssessment": verified_plan.get("temporalAssessment") or {},
                "requiredCitationKeys": citation_keys,
                "claimCitationChecklist": _architect_claim_citation_checklist(assigned_claims),
                "permittedHardAnchors": permitted_hard_anchors,
                "verbatimCodeBlocks": verbatim_code_blocks,
                "targetMinChars": section_target_min_chars,
                "targetMaxChars": section_target_max_chars,
                "minimumAcceptableChars": _claim_bounded_section_minimum_chars(
                    section_target_min_chars,
                    assigned_claims,
                    floor=acceptable_floor,
                ),
                "directConclusionSection": index == 0,
                "requiresSynthesisConclusion": bool(
                    requires_synthesis_conclusion and relevant_inferences
                ),
                "limitationsAndActionSection": index == count - 1,
            }
        )
    return tasks


def _normalize_research_citation_tokens(value: Any) -> str:
    text = _safe_text(value)
    text = re.sub(
        r"(?:\[|【)\s*(S\d+)\s*:\s*E\d+\s*(?:\]|】)",
        lambda match: f"[{match.group(1).upper()}]",
        text,
        flags=re.IGNORECASE,
    )
    pattern = re.compile(
        r"(?:\[|【)\s*(S\d+(?:\s*[,，;；、/]\s*S\d+)*)\s*(?:\]|】)",
        re.IGNORECASE,
    )

    def replace_group(match: re.Match[str]) -> str:
        return "".join(f"[{key.upper()}]" for key in re.findall(r"S\d+", match.group(1), re.IGNORECASE))

    return pattern.sub(replace_group, text)


def _research_answer_section_from_model_output(value: Any, *, section_id: str) -> tuple[str, bool]:
    raw = _safe_text(value)
    marker = f"<!-- {_RESEARCH_ARCHITECT_SECTION_COMPLETE_MARKER_PREFIX}:{section_id} -->"
    complete = marker in raw
    answer = raw.replace(marker, "").strip()
    fenced = re.fullmatch(r"```(?:markdown|md)?\s*(.*?)\s*```", answer, re.DOTALL | re.IGNORECASE)
    if fenced:
        answer = fenced.group(1).strip()
    return _normalize_research_citation_tokens(answer), complete


def _architect_strip_section_source_urls(section: Any) -> tuple[str, int]:
    """Keep link labels while reserving raw source URLs for the final appendix."""

    text = _safe_text(section)
    text, markdown_link_count = re.subn(
        r"\[([^\]\r\n]+)\]\(\s*https?://[^\s)]+(?:\s+[\"'][^)]*[\"'])?\s*\)",
        lambda match: match.group(1).strip(),
        text,
        flags=re.IGNORECASE,
    )
    text, autolink_count = re.subn(
        r"<https?://[^>\s]+>",
        "",
        text,
        flags=re.IGNORECASE,
    )
    text, bare_url_count = re.subn(
        r"https?://[^\s)\]>]+",
        "",
        text,
        flags=re.IGNORECASE,
    )
    if markdown_link_count or autolink_count or bare_url_count:
        text = re.sub(r"[ \t]+(?=[,.;:!?，。；：！？])", "", text)
        text = re.sub(r"(?m)[ \t]+$", "", text)
        text = re.sub(r"\n{3,}", "\n\n", text)
    return text.strip(), markdown_link_count + autolink_count + bare_url_count


def _architect_strip_unverified_section_code_blocks(
    section: Any,
    task: dict[str, Any],
) -> tuple[str, int]:
    """Drop optional model-written code unless it exactly matches a verified block."""

    allowed_code_blocks = {
        _safe_text(value).replace("\r\n", "\n").strip()
        for value in list(task.get("verbatimCodeBlocks") or [])
        if _safe_text(value).strip()
    }
    kept_units: list[str] = []
    dropped = 0
    for unit in _architect_section_content_units(section):
        if "```" not in unit:
            kept_units.append(unit)
            continue
        fence_count = len(re.findall(r"```", unit))
        code_blocks = [
            match.group(1).replace("\r\n", "\n").strip()
            for match in re.finditer(
                r"```(?:[a-z0-9_+.-]+)?\s*\r?\n([\s\S]*?)```",
                unit,
                re.IGNORECASE,
            )
        ]
        if (
            fence_count % 2
            or not code_blocks
            or any(code not in allowed_code_blocks for code in code_blocks)
        ):
            dropped += 1
            continue
        kept_units.append(unit)
    return "\n\n".join(kept_units).strip(), dropped


def _architect_section_content_units(section: Any) -> list[str]:
    units: list[str] = []
    pending_headings: list[str] = []
    for paragraph in re.split(r"\n\s*\n", _safe_text(section)):
        paragraph = paragraph.strip()
        if not paragraph:
            continue
        if re.fullmatch(r"(?:#{1,6}\s+[^\r\n]+|\*\*[^\r\n]+\*\*)", paragraph):
            pending_headings.append(paragraph)
            continue
        if re.fullmatch(
            r"(?:(?:source|evidence|来源|依据)\s*[:：]?\s*)?(?:\[S\d+\]\s*)+",
            paragraph,
            re.IGNORECASE,
        ) and units and "```" in units[-1]:
            units[-1] = f"{units[-1]}\n\n{paragraph}"
            continue
        if pending_headings:
            paragraph = "\n".join([*pending_headings, paragraph])
            pending_headings = []
        if "```" in paragraph:
            units.append(paragraph)
            continue
        lines = [line.strip() for line in paragraph.splitlines() if line.strip()]
        if len(lines) > 1 and all(
            re.match(r"^(?:[-*+]\s+|\d+[.)]\s+|\|)", line) for line in lines
        ):
            units.extend(lines)
        else:
            units.append(paragraph)
    units.extend(pending_headings)
    return units


def _architect_statement_has_high_risk_assertion(statement: Any) -> bool:
    text = _safe_text(statement)
    return bool(
        _architect_hard_assertion_anchors(text)
        or _ARCHITECT_NEGATIVE_CAPABILITY_RE.search(text)
        or (
            _ARCHITECT_PERFORMANCE_DIRECTION_RE.search(text)
            and any(pattern.search(text) for _name, pattern in _ARCHITECT_PERFORMANCE_METRICS)
        )
        or _ARCHITECT_STRONG_MAGNITUDE_RE.search(text)
    )


def _architect_synthesis_unit_supported(
    unit: str,
    task: dict[str, Any],
    *,
    required_inference_id: str = "",
) -> bool:
    if not _ARCHITECT_EXPLICIT_SYNTHESIS_LABEL_RE.search(unit):
        return not required_inference_id
    claims = _research_dict_list(
        task.get("assignedClaims"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    )
    claim_by_id = {
        _safe_text(claim.get("claimId")): claim
        for claim in claims
        if _safe_text(claim.get("claimId"))
    }
    unit_citations = {
        value.upper() for value in re.findall(r"\[(S\d+)\]", unit, re.IGNORECASE)
    }
    for inference in _research_dict_list(task.get("compositeInferences"), limit=12):
        if (
            required_inference_id
            and _safe_text(inference.get("inferenceId")) != required_inference_id
        ):
            continue
        inference_text = _safe_text(
            inference.get("inference")
            or inference.get("conclusion")
            or inference.get("recommendation")
            or inference.get("claim")
            or inference.get("summary")
        )
        premise_ids = {
            _safe_text(value)
            for value in list(inference.get("premiseClaimIds") or [])
            if _safe_text(value)
        }
        if not inference_text or not premise_ids or not premise_ids.issubset(claim_by_id):
            continue
        premise_claims = [claim_by_id[claim_id] for claim_id in premise_ids]
        premise_citations = {
            _safe_text(
                support.get("citationKey") or support.get("citation")
                if isinstance(support, dict)
                else support
            ).strip("[]").upper()
            for claim in premise_claims
            for support in list(claim.get("supportingSources") or [])
            if _safe_text(
                support.get("citationKey") or support.get("citation")
                if isinstance(support, dict)
                else support
            )
        }
        if not premise_citations or not premise_citations.issubset(unit_citations):
            continue
        normalized_inference = _normalized_evidence_text(inference_text)
        normalized_unit = _normalized_evidence_text(unit)
        if normalized_inference not in normalized_unit and _source_relevance_score(
            inference_text,
            text=unit,
        ) < 40:
            continue
        evidence_text = "\n".join(
            _safe_text(claim.get("evidenceExcerpt")) for claim in premise_claims
        )
        source_metadata = {
            "title": " ".join(
                _safe_text(support.get("title"))
                for claim in premise_claims
                for support in list(claim.get("supportingSources") or [])
                if isinstance(support, dict)
            ),
            "url": " ".join(
                _safe_text(support.get("url") or support.get("sourceUrl"))
                for claim in premise_claims
                for support in list(claim.get("supportingSources") or [])
                if isinstance(support, dict)
            ),
        }
        if _architect_hard_assertion_issues(
            unit,
            evidence_text,
            source_metadata=source_metadata,
        ):
            continue
        if _architect_source_role_issues(unit, premise_claims):
            continue
        return True
    return False


def _architect_cross_source_inference_requires_label(
    unit: str,
    supporting_claims: list[dict[str, Any]],
) -> bool:
    """Require a synthesis label only when the unit actually spans sources.

    Lexical cues such as ``combined with`` also occur in direct single-source
    facts (for example combining a Path with ``/``). Treating the phrase alone
    as proof of cross-source inference caused verified claims to be restored
    and rejected repeatedly. Two distinct cited source identities retain the
    stricter synthesis-label gate.
    """

    if not _ARCHITECT_CROSS_SOURCE_INFERENCE_CUE_RE.search(unit):
        return False
    cited_source_keys = {
        _safe_text(
            support.get("citationKey") or support.get("citation")
            if isinstance(support, dict)
            else support
        ).strip("[]").upper()
        for claim in supporting_claims
        for support in list(claim.get("supportingSources") or [])
        if _safe_text(
            support.get("citationKey") or support.get("citation")
            if isinstance(support, dict)
            else support
        )
    }
    return len(cited_source_keys) >= 2


def _architect_section_issues(section: str, task: dict[str, Any], *, complete: bool) -> list[str]:
    issues: list[str] = []
    if not complete:
        issues.append("section_incomplete")
    effective_chars = research_effective_answer_chars({"answer": section})
    minimum_chars = int(
        task.get("minimumAcceptableChars")
        if "minimumAcceptableChars" in task
        else task.get("targetMinChars") or 0
    )
    depth_tolerance = min(64, max(16, (minimum_chars * 3 + 99) // 100)) if minimum_chars else 0
    if effective_chars + depth_tolerance < minimum_chars:
        issues.append(f"section_depth_not_met:{minimum_chars}:actual={effective_chars}")
    if len(section) > int(task.get("targetMaxChars") or 0) * 2:
        issues.append("section_exceeds_size_limit")
    for citation_key in list(task.get("requiredCitationKeys") or []):
        if f"[{citation_key}]" not in section:
            issues.append(f"section_citation_missing:{citation_key}")
    allowed_citations = {str(value).upper() for value in list(task.get("requiredCitationKeys") or [])}
    cited = {value.upper() for value in re.findall(r"\[(S\d+)\]", section, re.IGNORECASE)}
    for citation_key in sorted(cited - allowed_citations):
        issues.append(f"section_citation_out_of_scope:{citation_key}")
    if re.search(r"https?://", section, re.IGNORECASE):
        issues.append("section_contains_source_url")
    if re.search(r"(?m)^\s*>\s?", section):
        issues.append("section_contains_blockquote")
    fence_count = len(re.findall(r"```", section))
    if fence_count % 2:
        issues.append("section_code_block_incomplete")
    allowed_code_blocks = {
        _safe_text(value).replace("\r\n", "\n").strip()
        for value in list(task.get("verbatimCodeBlocks") or [])
        if _safe_text(value).strip()
    }
    for match in re.finditer(
        r"```(?:[a-z0-9_+.-]+)?\s*\r?\n([\s\S]*?)```",
        section,
        re.IGNORECASE,
    ):
        code = match.group(1).replace("\r\n", "\n").strip()
        if code not in allowed_code_blocks:
            issues.append("section_code_block_not_verbatim")
    claims = _research_dict_list(
        task.get("assignedClaims"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    )
    content_units = _architect_section_content_units(section)
    # Missing registered inferences are restored verbatim by
    # ``_assemble_architect_sections`` with every premise citation attached.
    # Keep rejecting unsupported synthesis that the Writer does emit below,
    # but do not split or regenerate an otherwise valid evidence section just
    # because the Writer omitted a Runtime-registered conclusion.
    for unit in content_units:
        unit_citations = {
            value.upper() for value in re.findall(r"\[(S\d+)\]", unit, re.IGNORECASE)
        }
        supporting_claims: list[dict[str, Any]] = []
        for claim in claims:
            claim_citations = {
                _safe_text(
                    support.get("citationKey") or support.get("citation")
                    if isinstance(support, dict)
                    else support
                ).strip("[]").upper()
                for support in list(claim.get("supportingSources") or [])
            }
            if unit_citations.intersection(claim_citations):
                supporting_claims.append(claim)
        has_hard_assertion = _architect_statement_has_high_risk_assertion(unit)
        has_normative_assertion = bool(
            _ARCHITECT_NORMATIVE_ASSERTION_RE.search(_architect_assertion_text(unit))
        )
        has_synthesis_assertion = bool(_ARCHITECT_EXPLICIT_SYNTHESIS_LABEL_RE.search(unit))
        if _architect_is_temporal_boundary_note(unit):
            if not unit_citations:
                issues.append("section_temporal_boundary_without_citation")
            continue
        for role_issue in _architect_source_role_issues(unit, supporting_claims):
            issues.append(f"section_{role_issue}")
        if _architect_cross_source_inference_requires_label(
            unit,
            supporting_claims,
        ) and not has_synthesis_assertion:
            issues.append("section_synthesis_label_required")
        if not has_hard_assertion and not has_normative_assertion and not has_synthesis_assertion:
            continue
        if not unit_citations:
            preview = re.sub(r"\s+", " ", unit).strip()[:100]
            issues.append(
                f"section_{'normative_assertion' if has_normative_assertion else 'high_risk_fact'}"
                f"_without_citation:{preview}"
            )
            continue
        evidence_text = "\n".join(
            _safe_text(claim.get("evidenceExcerpt")) for claim in supporting_claims
        )
        source_metadata = {
            "title": " ".join(
                _safe_text(support.get("title"))
                for claim in supporting_claims
                for support in list(claim.get("supportingSources") or [])
                if isinstance(support, dict)
            ),
            "url": " ".join(
                _safe_text(support.get("url") or support.get("sourceUrl"))
                for claim in supporting_claims
                for support in list(claim.get("supportingSources") or [])
                if isinstance(support, dict)
            ),
        }
        synthesis_supported = bool(
            has_synthesis_assertion and _architect_synthesis_unit_supported(unit, task)
        )
        if has_normative_assertion and not (
            _architect_normative_assertion_supported(unit, supporting_claims)
            or synthesis_supported
        ):
            issues.append("section_normative_attribution_not_entailed")
        if has_synthesis_assertion and not synthesis_supported:
            issues.append("section_synthesis_inference_not_bound")
        if has_hard_assertion:
            for assertion_issue in _architect_hard_assertion_issues(
                unit,
                evidence_text,
                source_metadata=source_metadata,
            ):
                issues.append(f"section_unsupported_hard_fact:{assertion_issue}")

    for claim in claims:
        claim_anchors = _architect_hard_assertion_anchors(claim.get("claim"))
        if not claim_anchors:
            continue
        claim_citations = {
            _safe_text(
                support.get("citationKey") or support.get("citation")
                if isinstance(support, dict)
                else support
            ).strip("[]").upper()
            for support in list(claim.get("supportingSources") or [])
        }
        cited_claim_text = _normalized_evidence_text(
            "\n".join(
                unit
                for unit in content_units
                if claim_citations.intersection(
                    value.upper() for value in re.findall(r"\[(S\d+)\]", unit, re.IGNORECASE)
                )
            )
        )
        missing_anchors = [anchor for _kind, anchor in claim_anchors if anchor not in cited_claim_text]
        if missing_anchors:
            claim_id = _safe_text(claim.get("claimId")) or "unknown"
            issues.append(f"section_claim_anchor_missing:{claim_id}")
    return issues


def _architect_repair_unambiguous_section_citations(
    section: str,
    task: dict[str, Any],
) -> tuple[str, int]:
    repaired_units: list[str] = []
    repair_count = 0
    claims = _research_dict_list(
        task.get("assignedClaims"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    )
    for unit in _architect_section_content_units(section):
        if re.search(r"\[S\d+\]", unit, re.IGNORECASE) or not (
            _architect_statement_has_high_risk_assertion(unit)
            or _ARCHITECT_NORMATIVE_ASSERTION_RE.search(_architect_assertion_text(unit))
        ):
            repaired_units.append(unit)
            continue
        matching_citations: set[str] = set()
        for claim in claims:
            supporting = [
                support
                for support in list(claim.get("supportingSources") or [])
                if isinstance(support, dict)
            ]
            citation_keys = {
                _safe_text(support.get("citationKey") or support.get("citation")).strip("[]").upper()
                for support in supporting
                if _safe_text(support.get("citationKey") or support.get("citation"))
            }
            if len(citation_keys) != 1:
                continue
            source_metadata = {
                "title": " ".join(_safe_text(support.get("title")) for support in supporting),
                "url": " ".join(
                    _safe_text(support.get("url") or support.get("sourceUrl"))
                    for support in supporting
                ),
            }
            has_synthesis_assertion = bool(_ARCHITECT_EXPLICIT_SYNTHESIS_LABEL_RE.search(unit))
            if (
                (
                    _architect_normative_assertion_supported(unit, [claim])
                    or (
                        has_synthesis_assertion
                        and _architect_synthesis_unit_supported(unit, task)
                    )
                )
                and not _architect_source_role_issues(unit, [claim])
                and not _architect_hard_assertion_issues(
                    unit,
                    claim.get("evidenceExcerpt"),
                    source_metadata=source_metadata,
                )
            ):
                matching_citations.update(citation_keys)
        if len(matching_citations) == 1:
            citation_key = next(iter(matching_citations))
            repaired_units.append(f"{unit.rstrip()} [{citation_key}]")
            repair_count += 1
        else:
            repaired_units.append(unit)
    return "\n\n".join(repaired_units).strip(), repair_count


def _architect_repair_source_role_labels(
    section: str,
    task: dict[str, Any],
) -> tuple[str, int]:
    claims = _research_dict_list(
        task.get("assignedClaims"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    )
    repaired_units: list[str] = []
    repair_count = 0
    for unit in _architect_section_content_units(section):
        lines = unit.splitlines()
        heading_count = 0
        while heading_count < len(lines) and re.match(r"^\s*#{1,6}\s+", lines[heading_count]):
            heading_count += 1
        existing_body = "\n".join(lines[heading_count:]).lstrip()
        language_sample = re.sub(
            r"^(?:Secondary source(?:\s+[\"“][^\"”]{1,120}[\"”])?(?:\s*\(S\d+\))? states|"
            r"Mixed-source synthesis with secondary context(?:\s+from\s+[\"“][^\"”]{1,120}[\"”])?|"
            r"二手来源(?:《[^》]{1,120}》)?的表述|含二手来源(?:《[^》]{1,120}》)?的综合判断)"
            r"\s*[:：]\s*",
            "",
            existing_body,
            flags=re.IGNORECASE,
        )
        language_sample = re.sub(
            r"^(?:\*\*)?(?:本报告(?:的)?综合判断|综合判断|Practical synthesis)(?:\*\*)?\s*[:：]\s*",
            "",
            language_sample,
            flags=re.IGNORECASE,
        )
        cjk_count = len(re.findall(r"[\u4e00-\u9fff]", language_sample))
        latin_count = len(re.findall(r"[A-Za-z]", language_sample))
        use_chinese_prefix = cjk_count >= 4 and cjk_count * 3 >= max(1, latin_count)
        if not use_chinese_prefix:
            normalized_body = re.sub(
                r"^含二手来源《([^》]{1,120})》的综合判断\s*[:：]\s*",
                lambda match: (
                    'Mixed-source synthesis with secondary context from '
                    f'“{match.group(1)}”: '
                ),
                existing_body,
            )
            normalized_body = re.sub(
                r"^含二手来源的综合判断\s*[:：]\s*",
                "Mixed-source synthesis with secondary context: ",
                normalized_body,
            )
            normalized_body = re.sub(
                r"^二手来源《([^》]{1,120})》的表述\s*[:：]\s*",
                lambda match: f'Secondary source “{match.group(1)}” states: ',
                normalized_body,
            )
            normalized_body = re.sub(
                r"^二手来源的表述\s*[:：]\s*",
                "Secondary source states: ",
                normalized_body,
            )
            normalized_body = re.sub(
                r"^(?P<prefix>(?:Secondary source(?:\s+[\"“][^\"”]{1,120}[\"”])?(?:\s*\(S\d+\))? states|"
                r"Mixed-source synthesis with secondary context(?:\s+from\s+[\"“][^\"”]{1,120}[\"”])?)"
                r"\s*:\s*)(?:\*\*)?(?:本报告(?:的)?综合判断|综合判断)(?:\*\*)?\s*[:：]\s*",
                r"\g<prefix>",
                normalized_body,
                flags=re.IGNORECASE,
            )
            normalized_body = re.sub(
                r"^(?:\*\*)?(?:本报告(?:的)?综合判断|综合判断)(?:\*\*)?\s*[:：]\s*",
                "Practical synthesis: ",
                normalized_body,
            )
            if normalized_body != existing_body:
                unit = "\n".join([*lines[:heading_count], normalized_body]).strip()
                existing_body = normalized_body
                repair_count += 1
        unit_citations = {
            value.upper() for value in re.findall(r"\[(S\d+)\]", unit, re.IGNORECASE)
        }
        supporting_claims = [
            claim
            for claim in claims
            if unit_citations.intersection(
                {
                    _safe_text(
                        support.get("citationKey") or support.get("citation")
                        if isinstance(support, dict)
                        else support
                    ).strip("[]").upper()
                    for support in list(claim.get("supportingSources") or [])
                }
            )
        ]
        role_issues = _architect_source_role_issues(
            unit,
            supporting_claims,
        )
        if "secondary_source_role_required" not in role_issues:
            repaired_units.append(unit)
            continue
        supports = [
            support
            for claim in supporting_claims
            for support in list(claim.get("supportingSources") or [])
            if isinstance(support, dict)
        ]
        secondary_supports = [
            support
            for support in supports
            if _architect_support_role(support) == "secondary"
        ]
        primary_supports = [
            support for support in supports if _architect_support_role(support) == "primary"
        ]
        secondary_title = " / ".join(
            dict.fromkeys(
                _compact_research_text(support.get("title"), limit=80)
                for support in secondary_supports
                if _compact_research_text(support.get("title"), limit=80)
            )
        )[:120]
        if use_chinese_prefix:
            if primary_supports:
                prefix = (
                    f"含二手来源《{secondary_title}》的综合判断："
                    if secondary_title
                    else "含二手来源的综合判断："
                )
            else:
                prefix = f"二手来源《{secondary_title}》的表述：" if secondary_title else "二手来源的表述："
        elif primary_supports:
            prefix = (
                f"Mixed-source synthesis with secondary context from “{secondary_title}”: "
                if secondary_title
                else "Mixed-source synthesis with secondary context: "
            )
        else:
            prefix = (
                f"Secondary source “{secondary_title}” states: "
                if secondary_title
                else "Secondary source states: "
            )
        lines = unit.splitlines()
        heading_count = 0
        while heading_count < len(lines) and re.match(r"^\s*#{1,6}\s+", lines[heading_count]):
            heading_count += 1
        existing_body = "\n".join(lines[heading_count:]).lstrip()
        if re.match(
            r"^(?:Secondary source|Mixed-source synthesis with secondary context|"
            r"二手来源|含二手来源(?:《[^》]{1,120}》)?的综合判断)",
            existing_body,
            re.IGNORECASE,
        ):
            repaired_units.append(unit)
            continue
        if heading_count:
            unit = "\n".join(
                [*lines[:heading_count], f"{prefix}{' '.join(lines[heading_count:]).strip()}"]
            ).strip()
        else:
            unit = f"{prefix}{unit}"
        repaired_units.append(unit)
        repair_count += 1
    return "\n\n".join(repaired_units).strip(), repair_count


def _architect_split_unit_for_validation(unit: str) -> tuple[list[str], list[str]]:
    """Split prose for surgical evidence checks while preserving its semantic labels."""

    text = _safe_text(unit).strip()
    if not text or "```" in text or re.search(r"(?m)^\s*>\s?", text):
        return [], [text] if text else []

    lines = text.splitlines()
    headings: list[str] = []
    while lines and re.fullmatch(r"(?:\s*#{1,6}\s+[^\r\n]+|\s*\*\*[^\r\n]+\*\*)", lines[0]):
        headings.append(lines.pop(0).strip())
    body = "\n".join(lines).strip()
    if not body:
        return headings, []

    prefix_match = re.match(
        r"^(?P<prefix>(?:"
        r"Secondary source(?:\s+[\"“][^\"”]{1,120}[\"”])?(?:\s*\(S\d+\))? states|"
        r"Mixed-source synthesis with secondary context(?:\s+from\s+[\"“][^\"”]{1,120}[\"”])?|"
        r"Practical synthesis|This report(?:'s)? synthesis|Our synthesis|"
        r"二手来源(?:《[^》]{1,120}》)?的表述|含二手来源(?:《[^》]{1,120}》)?的综合判断|"
        r"本报告(?:的)?综合判断|综合判断"
        r")\s*[:：]\s*)",
        body,
        re.IGNORECASE,
    )
    semantic_prefix = prefix_match.group("prefix") if prefix_match else ""
    if not semantic_prefix and headings:
        heading_text = " ".join(re.sub(r"^(?:#{1,6}\s+|\*\*)|\*\*$", "", item) for item in headings)
        if _ARCHITECT_SYNTHESIS_LABEL_RE.search(heading_text):
            semantic_prefix = "本报告的综合判断：" if re.search(r"[\u4e00-\u9fff]", heading_text) else "Practical synthesis: "

    fragments = [
        item.strip()
        for item in re.split(
            r"(?<=[。！？!?])\s+|(?<=\.)\s+(?=(?:[`\"“A-Z\u4e00-\u9fff]))",
            body,
        )
        if item.strip()
    ]
    if not fragments:
        fragments = [body]
    citation_keys = list(
        dict.fromkeys(value.upper() for value in re.findall(r"\[(S\d+)\]", text, re.IGNORECASE))
    )
    normalized: list[str] = []
    for index, fragment in enumerate(fragments):
        candidate = fragment
        if index and semantic_prefix and not candidate.lower().startswith(semantic_prefix.lower()):
            candidate = f"{semantic_prefix}{candidate}"
        if citation_keys and not re.search(r"\[S\d+\]", candidate, re.IGNORECASE):
            candidate = f"{candidate.rstrip()} " + " ".join(f"[{key}]" for key in citation_keys)
        normalized.append(candidate.strip())
    return headings, normalized


def _architect_drop_unsupported_section_units(
    section: str,
    task: dict[str, Any],
    *,
    diagnostics: list[str] | None = None,
    reason_counts: dict[str, int] | None = None,
) -> tuple[str, int]:
    claims = _research_dict_list(
        task.get("assignedClaims"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    )
    kept_units: list[str] = []
    dropped = 0

    def unsupported_reason(unit: str) -> str:
        if re.search(r"(?m)^\s*>\s?", unit):
            return "blockquote_not_allowed"
        unit_citations = {
            value.upper() for value in re.findall(r"\[(S\d+)\]", unit, re.IGNORECASE)
        }
        supporting_claims = [
            claim
            for claim in claims
            if unit_citations.intersection(
                {
                    _safe_text(
                        support.get("citationKey") or support.get("citation")
                        if isinstance(support, dict)
                        else support
                    ).strip("[]").upper()
                    for support in list(claim.get("supportingSources") or [])
                }
            )
        ]
        if _architect_is_temporal_boundary_note(unit):
            return "" if unit_citations else "missing_citation"
        has_hard_assertion = _architect_statement_has_high_risk_assertion(unit)
        has_normative_assertion = bool(
            _ARCHITECT_NORMATIVE_ASSERTION_RE.search(_architect_assertion_text(unit))
        )
        has_synthesis_assertion = bool(_ARCHITECT_EXPLICIT_SYNTHESIS_LABEL_RE.search(unit))
        source_role_issues = _architect_source_role_issues(unit, supporting_claims)
        if source_role_issues:
            return source_role_issues[0]
        if _architect_cross_source_inference_requires_label(
            unit,
            supporting_claims,
        ) and not has_synthesis_assertion:
            return "synthesis_label_required"
        if not has_hard_assertion and not has_normative_assertion and not has_synthesis_assertion:
            return ""
        if not unit_citations:
            return "missing_citation"
        if not supporting_claims:
            return "citation_not_bound"
        evidence_text = "\n".join(_safe_text(claim.get("evidenceExcerpt")) for claim in supporting_claims)
        source_metadata = {
            "title": " ".join(
                _safe_text(support.get("title"))
                for claim in supporting_claims
                for support in list(claim.get("supportingSources") or [])
                if isinstance(support, dict)
            ),
            "url": " ".join(
                _safe_text(support.get("url") or support.get("sourceUrl"))
                for claim in supporting_claims
                for support in list(claim.get("supportingSources") or [])
                if isinstance(support, dict)
            ),
        }
        synthesis_supported = bool(
            has_synthesis_assertion and _architect_synthesis_unit_supported(unit, task)
        )
        if has_normative_assertion and not (
            _architect_normative_assertion_supported(unit, supporting_claims)
            or synthesis_supported
        ):
            return "normative_attribution_not_entailed"
        if has_synthesis_assertion and not synthesis_supported:
            return "synthesis_inference_not_bound"
        if has_hard_assertion:
            hard_assertion_issues = _architect_hard_assertion_issues(
                unit,
                evidence_text,
                source_metadata=source_metadata,
            )
            if hard_assertion_issues:
                return hard_assertion_issues[0]
        return ""

    def record_drop(reason: str, unit: str) -> None:
        if reason_counts is not None:
            reason_counts[reason] = int(reason_counts.get(reason) or 0) + 1
        if diagnostics is None or len(diagnostics) >= 3:
            return
        preview = re.sub(r"\s+", " ", _safe_text(unit)).strip()[:120]
        diagnostics.append(f"{reason}:{preview}")

    for unit in _architect_section_content_units(section):
        reason = unsupported_reason(unit)
        if not reason:
            kept_units.append(unit)
            continue

        headings, fragments = _architect_split_unit_for_validation(unit)
        if not fragments or (not headings and len(fragments) == 1 and fragments[0] == unit):
            dropped += 1
            record_drop(reason, unit)
            continue

        kept_fragments: list[str] = []
        for fragment in fragments:
            fragment_reason = unsupported_reason(fragment)
            if fragment_reason:
                dropped += 1
                record_drop(fragment_reason, fragment)
                continue
            kept_fragments.append(fragment)
        if not kept_fragments:
            continue
        if headings:
            headed_fragment = "\n".join([*headings, kept_fragments[0]])
            heading_reason = unsupported_reason(headed_fragment)
            if heading_reason:
                dropped += 1
                record_drop(heading_reason, "\n".join(headings))
            else:
                kept_fragments[0] = headed_fragment
        kept_units.extend(kept_fragments)
    return "\n\n".join(kept_units).strip(), dropped


def _architect_restore_unrepresented_claims(
    section: str,
    task: dict[str, Any],
) -> tuple[str, int]:
    units = _architect_section_content_units(section)
    restored: list[str] = []
    for claim in _research_dict_list(
        task.get("assignedClaims"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    ):
        citation_keys = [
            _safe_text(
                support.get("citationKey") or support.get("citation")
                if isinstance(support, dict)
                else support
            ).strip("[]").upper()
            for support in list(claim.get("supportingSources") or [])
            if _safe_text(
                support.get("citationKey") or support.get("citation")
                if isinstance(support, dict)
                else support
            )
        ]
        if not citation_keys:
            continue
        cited_units = [
            unit
            for unit in units
            if any(f"[{citation_key}]" in unit.upper() for citation_key in citation_keys)
        ]
        cited_text = _normalized_evidence_text("\n".join(cited_units))
        normalized_claim = _normalized_evidence_text(claim.get("claim"))
        # A shared [S#] token is not proof that every plain-language claim from
        # that source was expressed. Preserve the exact Runtime-verified claim
        # unless the corresponding cited content already contains it verbatim.
        claim_text = _safe_text(claim.get("claim"))
        claim_anchors = _architect_hard_assertion_anchors(claim_text)
        represented = bool(normalized_claim and normalized_claim in cited_text)
        if not represented and claim_text:
            for unit in cited_units:
                normalized_unit = _normalized_evidence_text(unit)
                anchors_present = all(anchor in normalized_unit for _kind, anchor in claim_anchors)
                threshold = 40 if claim_anchors else 60
                if anchors_present and _source_relevance_score(claim_text, text=unit) >= threshold:
                    represented = True
                    break
        if represented:
            continue
        if not claim_text:
            continue
        restored.append(
            f"- {claim_text} " + "".join(f"[{citation_key}]" for citation_key in citation_keys)
        )
    if not restored:
        return section, 0
    restored_text = "\n".join(restored)
    cjk_count = len(re.findall(r"[\u4e00-\u9fff]", restored_text))
    latin_count = len(re.findall(r"[A-Za-z]", restored_text))
    heading = "### 来源支撑的细节" if cjk_count >= 4 and cjk_count * 3 >= max(1, latin_count) else "### Source-backed details"
    block = heading + "\n\n" + restored_text
    return f"{section.rstrip()}\n\n{block}".strip(), len(restored)


def _architect_dedupe_repeated_content_units(value: str) -> tuple[str, int]:
    units = _architect_section_content_units(value)
    if not units:
        return _safe_text(value), 0
    kept: list[str] = []
    seen: set[str] = set()
    dropped = 0
    for unit in units:
        signature = _normalized_evidence_text(unit)
        if len(signature) >= 60 and signature in seen:
            dropped += 1
            continue
        if signature:
            seen.add(signature)
        kept.append(unit)
    return "\n\n".join(kept).strip(), dropped


def _split_architect_section_task(task: dict[str, Any]) -> list[dict[str, Any]]:
    claims = _research_dict_list(
        task.get("assignedClaims"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    )
    if len(claims) < 2:
        return []
    midpoint = max(1, len(claims) // 2)
    claim_groups = [claims[:midpoint], claims[midpoint:]]
    if not all(claim_groups):
        return []
    children: list[dict[str, Any]] = []
    for child_index, assigned_claims in enumerate(claim_groups, start=1):
        assigned_claim_ids = {
            _safe_text(claim.get("claimId"))
            for claim in assigned_claims
            if _safe_text(claim.get("claimId"))
        }
        citation_keys: list[str] = []
        for claim in assigned_claims:
            for support in list(claim.get("supportingSources") or []):
                value = (
                    support.get("citationKey") or support.get("citation")
                    if isinstance(support, dict)
                    else support
                )
                citation_key = _safe_text(value).strip("[]")
                if citation_key and citation_key not in citation_keys:
                    citation_keys.append(citation_key)
        inferences = [
            inference
            for inference in _research_dict_list(task.get("compositeInferences"), limit=12)
            if (
                premise_ids := {
                    _safe_text(value)
                    for value in list(inference.get("premiseClaimIds") or [])
                    if _safe_text(value)
                }
            )
            and premise_ids.issubset(assigned_claim_ids)
        ]
        suffix = "a" if child_index == 1 else "b"
        child_target_min_chars = max(700, (int(task.get("targetMinChars") or 0) + 1) // 2)
        children.append(
            {
                **task,
                "sectionId": f"{_safe_text(task.get('sectionId'))}_{suffix}",
                "subsectionOf": _safe_text(task.get("sectionId")),
                "assignedClaims": assigned_claims,
                "compositeInferences": inferences,
                "conflictMatrix": (task.get("conflictMatrix") or []) if child_index == 2 else [],
                "requiredCitationKeys": citation_keys,
                "claimCitationChecklist": _architect_claim_citation_checklist(assigned_claims),
                "targetMinChars": child_target_min_chars,
                "targetMaxChars": max(1_300, (int(task.get("targetMaxChars") or 0) + 1) // 2),
                "minimumAcceptableChars": _claim_bounded_section_minimum_chars(
                    child_target_min_chars,
                    assigned_claims,
                    floor=350,
                ),
                "directConclusionSection": bool(task.get("directConclusionSection") and child_index == 1),
                "requiresSynthesisConclusion": bool(
                    task.get("requiresSynthesisConclusion") and inferences
                ),
                "limitationsAndActionSection": bool(task.get("limitationsAndActionSection") and child_index == 2),
            }
        )
    return children


def _architect_missing_composite_inference_section(
    *,
    question: str,
    verified_plan: dict[str, Any],
    sections: list[str],
) -> str:
    """Restore only verified syntheses lost by a section retry/split.

    A section may be split after a Writer failure.  An inference whose premises
    straddle the two child claim groups cannot safely belong to either child and
    used to disappear from the answer.  The Runtime owns the verified plan, so
    final assembly may render the exact registered inference with all premise
    citations, but it must not create or rewrite a recommendation here.
    """

    claims = _research_dict_list(
        verified_plan.get("claimTable"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    )
    inferences = _research_dict_list(verified_plan.get("compositeInferences"), limit=12)
    if not claims or not inferences:
        return ""

    existing_units = [
        unit
        for section in sections
        for unit in _architect_section_content_units(section)
    ]
    claim_by_id = {
        _safe_text(claim.get("claimId")): claim
        for claim in claims
        if _safe_text(claim.get("claimId"))
    }
    chinese = bool(re.search(r"[\u4e00-\u9fff]", question))
    blocks: list[str] = []
    has_audience = False
    for inference in inferences:
        inference_id = _safe_text(inference.get("inferenceId"))
        inference_text = _safe_text(
            inference.get("inference")
            or inference.get("conclusion")
            or inference.get("recommendation")
        )
        if not inference_id or not inference_text:
            continue
        premise_claims = [
            claim_by_id.get(_safe_text(claim_id))
            for claim_id in list(inference.get("premiseClaimIds") or [])
        ]
        if not premise_claims or not all(isinstance(claim, dict) for claim in premise_claims):
            continue
        premise_keys = list(
            dict.fromkeys(
                _safe_text(support.get("citationKey") or support.get("citation"))
                .strip("[]")
                .upper()
                for claim in premise_claims
                for support in list(claim.get("supportingSources") or [])
                if isinstance(support, dict)
                and _safe_text(support.get("citationKey") or support.get("citation"))
            )
        )
        if not premise_keys:
            continue
        normalized_inference = _normalized_evidence_text(inference_text)
        if any(
            _ARCHITECT_EXPLICIT_SYNTHESIS_LABEL_RE.search(unit)
            and normalized_inference in _normalized_evidence_text(unit)
            and set(premise_keys).issubset(
                {
                    value.upper()
                    for value in re.findall(r"\[(S\d+)\]", unit, re.IGNORECASE)
                }
            )
            for unit in existing_units
        ):
            continue
        prefix = "**本报告的综合判断：** " if chinese else "**Practical synthesis:** "
        unit = prefix + inference_text + " " + "".join(f"[{key}]" for key in premise_keys)
        audience = _compact_research_text(inference.get("audience"), limit=80)
        has_audience = has_audience or bool(audience)
        blocks.append((f"### {audience}\n\n" if audience else "") + unit)

    if not blocks:
        return ""
    if has_audience:
        heading = (
            "## 按使用者类型的可执行选型建议"
            if chinese
            else "## Actionable guidance by audience"
        )
    else:
        heading = "## 已登记的跨来源综合判断" if chinese else "## Registered cross-source syntheses"
    return heading + "\n\n" + "\n\n".join(blocks)


def _assemble_architect_sections(
    *,
    question: str,
    verified_plan: dict[str, Any],
    sections: list[str],
    sources: list[dict[str, Any]],
) -> str:
    chinese = bool(re.search(r"[\u4e00-\u9fff]", question))
    headline = _compact_research_text(verified_plan.get("headline"), limit=180)
    if not headline:
        headline = "调研结论" if chinese else "Research findings"
    as_of = _safe_text(verified_plan.get("asOf"))
    lines = [f"# {headline}"]
    if as_of:
        lines.append(f"**{'截至' if chinese else 'As of'}：** {as_of}" if chinese else f"**As of:** {as_of}")
        lines.append(
            "**时点说明：** 这是本次调研的观察时点，不是任一来源的发布日期或更新时间。"
            if chinese
            else "**Temporal note:** This is the research observation time, not a publication or update date for any source."
        )
    lines.append(
        "**证据解释：** “二手来源《…》的表述”保留作者/教程经验归属；“本报告的综合判断”才表示跨来源建议。"
        "某项事实或做法被来源记录，不等于来源把它明确列为首选，除非摘录本身含相应推荐语义。"
        if chinese
        else "**Evidence interpretation:** `Secondary source “…” states` preserves attributed tutorial/author experience; "
        "`Practical synthesis` marks this report's cross-source recommendation. A fact or practice being documented "
        "does not make it an official preference unless the excerpt itself contains that recommendation."
    )
    assembled_sections = [section.strip() for section in sections if section.strip()]
    missing_inference_section = _architect_missing_composite_inference_section(
        question=question,
        verified_plan=verified_plan,
        sections=assembled_sections,
    )
    if missing_inference_section:
        assembled_sections.insert(0, missing_inference_section)
    lines.extend(assembled_sections)
    cited_source_keys = {
        value.upper()
        for section in assembled_sections
        for value in re.findall(r"\[(S\d+)\]", section, re.IGNORECASE)
    }
    cited_sources = [
        source
        for source in sources[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]
        if _safe_text(source.get("citationKey")).strip("[]").upper()
        in cited_source_keys
    ]
    ordered_sources = sorted(
        cited_sources,
        key=lambda source: (
            int(match.group(1))
            if (match := re.fullmatch(r"S(\d+)", _safe_text(source.get("citationKey")).strip("[]"), re.IGNORECASE))
            else 10_000,
            _safe_text(source.get("citationKey")),
        ),
    )
    currency_lines: list[str] = []
    for source in ordered_sources:
        temporal = source.get("temporalEvidence") if isinstance(source.get("temporalEvidence"), dict) else {}
        version = _safe_text(source.get("version") or temporal.get("version"))
        document_dates = [
            (kind, _normalize_document_date(value))
            for kind, value in research_document_date_candidates(source)
        ]
        document_dates = [(kind, value) for kind, value in document_dates if value]
        updated_at = next((value for kind, value in document_dates if kind == "updated"), "")
        published_at = next((value for kind, value in document_dates if kind == "published"), "")
        source_date = next((value for kind, value in document_dates if kind == "document"), "")
        facts: list[str] = []
        if version:
            facts.append(("适用版本" if chinese else "applicable version") + f" {version}")
        if updated_at:
            facts.append(("更新时间" if chinese else "updated") + f" {updated_at}")
        if published_at and published_at[:10] != updated_at[:10]:
            facts.append(("发布时间" if chinese else "published") + f" {published_at}")
        if (
            source_date
            and source_date[:10] not in {updated_at[:10], published_at[:10]}
        ):
            date_label = "来源日期" if chinese else "source date"
            facts.append(f"{date_label} {source_date}")
        parsed_dates: list[datetime] = []
        unparseable_document_dates: list[str] = []
        for raw_date in (updated_at, published_at, source_date):
            if not raw_date:
                continue
            try:
                parsed_date = datetime.fromisoformat(raw_date.replace("Z", "+00:00"))
            except ValueError:
                unparseable_document_dates.append(raw_date)
                continue
            if parsed_date.tzinfo is None:
                parsed_date = parsed_date.replace(tzinfo=timezone.utc)
            parsed_dates.append(parsed_date.astimezone(timezone.utc))
        stable_route_observed = _architect_support_has_stable_current_route(source)
        status_parts: list[str] = []
        if version:
            status_parts.append("版本边界明确" if chinese else "version-bounded")
        now_utc = datetime.now(timezone.utc)
        future_dates = [
            value for value in parsed_dates if value > now_utc + timedelta(days=1)
        ]
        if future_dates:
            status_parts.append(
                "日期晚于当前时点，视为异常且不能证明当前性"
                if chinese
                else "future-dated anomaly; cannot establish current applicability"
            )
        elif unparseable_document_dates:
            status_parts.append(
                "来源提供了无法规范解析的文档日期；不能据此证明当前性，需由 Reviewer 结合其他清晰时间/版本上下文判断"
                if chinese
                else "the source supplied an unparseable document date; it cannot establish current applicability, and the Reviewer must rely on other clear time/version context"
            )
        elif stable_route_observed:
            status_parts.append(
                "检索时观察到一手 stable/current 文档入口；这是适用性上下文，不证明页面近期发布或更新，具体充分性由独立 Reviewer 按问题判断"
                if chinese
                else "a primary stable/current documentation route was observed at retrieval time; this is applicability context, not evidence of recent publication or update, and the independent Reviewer judges adequacy for the question"
            )
        elif parsed_dates:
            status_parts.append(
                "来源报告的文档日期已显式列出；不套用固定年龄截止，适用性由独立 Reviewer 结合主张范围、版本和领域变化速度判断"
                if chinese
                else "source-reported document dates are shown explicitly; no fixed age cutoff is applied, and the independent Reviewer judges applicability against claim scope, version context, and domain change rate"
            )
        elif version:
            status_parts.append(
                "版本号仅界定适用范围；更广或当前适用性由独立 Reviewer 判断"
                if chinese
                else "the version bounds the evidence scope; broader or current applicability is left to the independent Reviewer"
            )
        else:
            tier = _architect_support_role(source)
            status_parts.append(
                (
                    "未提供明确日期或版本；一手/第一方页面只支撑其中记录的非时效 API 事实，不宣称近期发布"
                    if tier == "primary"
                    else "未提供明确日期或版本；二手材料仅用于署名经验或非时效案例"
                    if tier == "secondary"
                    else "未提供明确日期或版本；仅用于非时效事实或案例"
                )
                if chinese
                else (
                    "undated primary/first-party page; supports its recorded non-temporal API facts but does not establish recent publication"
                    if tier == "primary"
                    else "undated secondary material; used only for attributed experience or non-temporal examples"
                    if tier == "secondary"
                    else "undated; used only for non-temporal facts or examples"
                )
            )
        read_evidence = (
            source.get("readEvidence")
            if isinstance(source.get("readEvidence"), dict)
            else {}
        )
        retrieved_at = _safe_text(
            source.get("retrievedAt")
            or temporal.get("retrievedAt")
            or read_evidence.get("retrievedAt")
        )
        if retrieved_at:
            try:
                parsed_retrieved_at = datetime.fromisoformat(
                    retrieved_at.replace("Z", "+00:00")
                )
                if parsed_retrieved_at.tzinfo is None:
                    parsed_retrieved_at = parsed_retrieved_at.replace(tzinfo=timezone.utc)
                parsed_retrieved_at = parsed_retrieved_at.astimezone(timezone.utc)
            except ValueError:
                parsed_retrieved_at = None
            if parsed_retrieved_at is not None and parsed_retrieved_at <= now_utc + timedelta(days=1):
                facts.append(
                    ("检索时间" if chinese else "retrieved") + f" {retrieved_at}"
                )
        facts.append(
            ("证据状态" if chinese else "evidence status")
            + " "
            + ("；" if chinese else "; ").join(status_parts)
        )
        citation_key = _safe_text(source.get("citationKey")).strip("[]")
        title = _compact_research_text(source.get("title"), limit=120) or _safe_text(source.get("url"))
        tier = _architect_support_role(source)
        if chinese:
            role = "一手来源" if tier == "primary" else "二手/经验来源" if tier == "secondary" else ""
        else:
            role = "primary source" if tier == "primary" else "secondary/experience source" if tier == "secondary" else ""
        role_suffix = f"（{role}）" if role and chinese else f" ({role})" if role else ""
        separator = "；" if chinese else "; "
        currency_lines.append(
            f"- [{citation_key}] {title}{role_suffix}：{separator.join(facts)}。"
            if chinese
            else f"- [{citation_key}] {title}{role_suffix}: {separator.join(facts)}."
        )
    if currency_lines:
        lines.append("## 证据时效与适用范围" if chinese else "## Evidence currency and applicability")
        lines.extend(currency_lines)
        lines.append(
            "本报告不套用固定文档年龄截止。来源报告的发布/更新时间、版本范围、文档入口状态和检索时间分别显示；"
            "检索时间只证明本次读取发生过，不是发布日期或更新时间。更新缓慢、有日期、版本限定、二手或未标日期的相关资料都可保留，"
            "其对每条主张和本问题是否充分由独立 Reviewer 结合风险与领域变化速度判断。日期或版本标签本身不表示资料已被替代或废弃。"
            if chinese
            else "No fixed document-age cutoff is applied. Source-reported publication or update dates, version scope, documentation-route status, and retrieval time are shown separately. "
            "Retrieval time records when the evidence was fetched; it is not a publication or update date. Slowly updated, dated, version-bounded, secondary, or undated material is retained when relevant, "
            "and the independent Reviewer judges its adequacy for each claim and for the question as asked. A date or version label does not itself mean that material is superseded or deprecated."
        )
    lines.append("## 来源" if chinese else "## Sources")
    for source in ordered_sources:
        citation_key = _safe_text(source.get("citationKey")).strip("[]")
        title = _compact_research_text(source.get("title"), limit=180) or _safe_text(source.get("url"))
        url = _safe_text(source.get("url"))
        temporal = source.get("temporalEvidence") if isinstance(source.get("temporalEvidence"), dict) else {}
        source_metadata: list[str] = []
        version = _safe_text(source.get("version") or temporal.get("version"))
        if version:
            source_metadata.append(("版本 " if chinese else "version ") + version)
        seen_document_dates: set[str] = set()
        for kind, value in research_document_date_candidates(source):
            normalized_date = _normalize_document_date(value)
            if not normalized_date or normalized_date in seen_document_dates:
                continue
            seen_document_dates.add(normalized_date)
            date_label = {
                "updated": "更新" if chinese else "updated",
                "published": "发布" if chinese else "published",
            }.get(kind, "文档日期" if chinese else "document date")
            source_metadata.append(f"{date_label} {normalized_date}")
        suffix = f" ({'; '.join(source_metadata)})" if source_metadata else ""
        lines.append(f"- [{citation_key}] {title} - {url}{suffix}")
    return "\n\n".join(lines).strip()


def _assemble_architect_claim_report(
    *,
    question: str,
    verified_plan: dict[str, Any],
    sources: list[dict[str, Any]],
) -> str:
    """Build a detailed non-generative report from already verified claims."""

    chinese = bool(re.search(r"[\u4e00-\u9fff]", question))
    verified_plan = dict(verified_plan)
    claims = _research_dict_list(
        verified_plan.get("claimTable"),
        limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
    )
    has_primary_normative_claim = any(
        _safe_text(claim.get("claimType")).lower() == "explicit_normative"
        and any(
            _architect_support_role(support) == "primary"
            for support in list(claim.get("supportingSources") or [])
            if isinstance(support, dict)
        )
        for claim in claims
    )
    headline = _safe_text(verified_plan.get("headline"))
    unsafe_official_headline = bool(
        re.search(r"\b(?:official|authoritative)\b|(?:官方|权威)", headline, re.IGNORECASE)
        and re.search(
            r"\b(?:best practices?|recommendations?|guidance|integration patterns?|"
            r"implementation patterns?|recommended approaches?|practices?)\b|"
            r"(?:最佳实践|建议|指南|指导|接入模式|集成模式|实施模式|实践)",
            headline,
            re.IGNORECASE,
        )
    )
    if unsafe_official_headline and not has_primary_normative_claim:
        verified_plan["headline"] = (
            "基于证据的实践判断与适用边界"
            if chinese
            else "Evidence-backed practices and applicability boundaries"
        )
    source_by_key = {
        _safe_text(source.get("citationKey")).strip("[]").upper(): source
        for source in sources
        if _safe_text(source.get("citationKey"))
    }
    temporal = _architect_runtime_temporal_assessment(sources)
    temporal_by_key = {
        _safe_text(item.get("citationKey")).upper(): item
        for item in list(temporal.get("sources") or [])
        if isinstance(item, dict) and _safe_text(item.get("citationKey"))
    }
    excerpt_char_limit = min(
        320,
        max(140, 2_400 // max(1, len(claims))),
    )
    primary_blocks: list[str] = []
    secondary_blocks: list[str] = []
    claim_records: list[dict[str, Any]] = []
    used_keys: list[str] = []

    for index, claim in enumerate(claims, start=1):
        supports = [
            support
            for support in list(claim.get("supportingSources") or [])
            if isinstance(support, dict)
        ]
        citation_keys = list(
            dict.fromkeys(
                _safe_text(support.get("citationKey") or support.get("citation"))
                .strip("[]")
                .upper()
                for support in supports
                if _safe_text(support.get("citationKey") or support.get("citation"))
            )
        )
        if not citation_keys:
            continue
        for key in citation_keys:
            if key not in used_keys:
                used_keys.append(key)
        citation_text = "".join(f"[{key}]" for key in citation_keys)
        source = source_by_key.get(citation_keys[0]) or supports[0]
        role = _architect_support_role(supports[0] if supports else source)
        title = _compact_research_text(
            source.get("title") if isinstance(source, dict) else "",
            limit=140,
        ) or citation_keys[0]
        claim_text = _safe_text(claim.get("claim"))
        excerpt = _compact_research_text(
            _URL_IN_TEXT_RE.sub("", _safe_text(claim.get("evidenceExcerpt"))),
            limit=excerpt_char_limit,
        )
        normalized_claim_detail = _normalized_evidence_text(
            _architect_assertion_text(claim_text)
        )
        normalized_excerpt_detail = _normalized_evidence_text(excerpt)
        include_excerpt_detail = bool(
            excerpt
            and not (
                len(normalized_claim_detail) >= 20
                and (
                    normalized_claim_detail in normalized_excerpt_detail
                    or normalized_excerpt_detail in normalized_claim_detail
                )
            )
        )
        assessment = temporal_by_key.get(citation_keys[0]) or {}
        status = _safe_text(assessment.get("status"))
        document_date_status = _safe_text(assessment.get("documentDateStatus"))
        version = _safe_text(assessment.get("version"))
        dates = [
            _safe_text(value)
            for value in list(assessment.get("documentDates") or [])
            if _safe_text(value)
        ]
        if status == "stable_route_observed":
            time_boundary = (
                "检索时观察到一手 stable/current 文档入口；这是适用性上下文，不证明近期发布/更新，充分性由独立 Reviewer 按本主张判断"
                if chinese
                else "primary stable/current documentation route observed at retrieval time; this is applicability context, not proof of recent publication/update, and the independent Reviewer judges adequacy for this claim"
            )
        elif status == "explicit_current_applicability":
            time_boundary = (
                "来源显式提供当前适用性上下文；发布日期、更新时间和版本仍分别列出，充分性由独立 Reviewer 按本主张判断"
                if chinese
                else "the source supplies explicit current-applicability context; publication/update dates and version remain separate, and the independent Reviewer judges adequacy for this claim"
            )
        elif status == "dated_context":
            time_boundary = (
                "来源报告的文档日期已列出；不套用固定年龄截止，独立 Reviewer 按本主张范围与版本判断适用性"
                if chinese
                else "source-reported document date shown; no fixed age cutoff is applied, and the independent Reviewer judges applicability against this claim and its version scope"
            )
        elif status == "version_bounded":
            time_boundary = (
                "版本明确但无可解析文档日期；证据用于所标版本范围，更广或当前适用性由独立 Reviewer 判断"
                if chinese
                else "version-bounded without a parseable document date; evidence is used within that stated scope, and the independent Reviewer judges broader or current applicability"
            )
        elif status == "future_anomaly":
            time_boundary = (
                "来源报告的文档日期晚于本轮观察时点，标为异常且不能据此证明当前性"
                if chinese
                else "source-reported document date is later than the research observation time; it is flagged as an anomaly and cannot establish current applicability"
            )
        elif status == "malformed_date":
            time_boundary = (
                "来源报告的文档日期无法规范解析，标为异常；不能据此证明当前性，Reviewer 只能使用其他清晰时间/版本上下文"
                if chinese
                else "source-reported document date is unparseable and flagged as an anomaly; it cannot establish current applicability, so the Reviewer may rely only on other clear time/version context"
            )
        else:
            time_boundary = (
                (
                    "无文档日期的一手/第一方页面；只支撑所记录的非时效 API 事实"
                    if role == "primary"
                    else "无文档日期的二手材料；只支撑署名经验或非时效案例"
                )
                if chinese
                else (
                    "undated primary/first-party page; recorded non-temporal API fact only"
                    if role == "primary"
                    else "undated secondary material; attributed experience or non-temporal example only"
                )
            )
        metadata_bits = [
            bit
            for bit in (
                ("版本 " if chinese else "version ") + version if version else "",
                ("文档日期 " if chinese else "document date ") + ", ".join(dates) if dates else "",
            )
            if bit
        ]
        claim_records.append(
            {
                "claim": claim_text,
                "citationKeys": citation_keys,
                "citations": citation_text,
                "role": role,
                "sourceKey": citation_keys[0],
                "sourceTitle": title,
                "sourceUrl": _safe_text(source.get("url") or source.get("sourceUrl")),
                "temporalStatus": status,
                "documentDateStatus": document_date_status,
                "documentDates": dates,
                "version": version,
                "claimType": _safe_text(claim.get("claimType")).lower(),
                "claimSearchText": claim_text.lower(),
                "searchText": " ".join((claim_text, title)).lower(),
            }
        )
        if chinese:
            finding_label = (
                f"**二手来源《{title}》的署名表述：**"
                if role == "secondary"
                else "**来源支撑的结论：**"
            )
            detail_label = (
                "**该二手来源的绑定证据细节：**"
                if role == "secondary"
                else "**绑定证据细节：**"
            )
            block = (
                f"### {citation_text} {title} — claim {index}\n\n"
                f"{finding_label} {claim_text} {citation_text}\n\n"
                + (
                    f"{detail_label} {excerpt} {citation_text}\n\n"
                    if include_excerpt_detail
                    else ""
                )
                + f"**时效与适用边界：** {time_boundary}"
                + (f"（{'；'.join(metadata_bits)}）" if metadata_bits else "")
                + "。"
            )
        else:
            finding_label = (
                f'**Attributed statement from secondary source "{title}":**'
                if role == "secondary"
                else "**Source-backed finding:**"
            )
            detail_label = (
                "**Bound evidence detail from that secondary source:**"
                if role == "secondary"
                else "**Bound evidence detail:**"
            )
            block = (
                f"### {citation_text} {title} — claim {index}\n\n"
                f"{finding_label} {claim_text} {citation_text}\n\n"
                + (
                    f"{detail_label} {excerpt} {citation_text}\n\n"
                    if include_excerpt_detail
                    else ""
                )
                + f"**Currency and applicability:** {time_boundary}"
                + (f" ({'; '.join(metadata_bits)})" if metadata_bits else "")
                + "."
            )
        (secondary_blocks if role == "secondary" else primary_blocks).append(block)

    primary_citations = list(
        dict.fromkeys(
            key
            for claim in claims
            for support in list(claim.get("supportingSources") or [])
            if isinstance(support, dict) and _architect_support_role(support) == "primary"
            if (key := _safe_text(support.get("citationKey") or support.get("citation")).strip("[]").upper())
        )
    )
    primary_source_summary = "; ".join(
        f"{_compact_research_text(source_by_key[key].get('title'), limit=64)} [{key}]"
        for key in primary_citations[:8]
        if key in source_by_key
    )
    direct_records: list[dict[str, Any]] = []
    direct_source_keys: set[str] = set()
    for record in claim_records:
        source_key = _safe_text(record.get("sourceKey"))
        if not source_key or source_key in direct_source_keys:
            continue
        direct_records.append(record)
        direct_source_keys.add(source_key)
        if len(direct_records) >= 8:
            break
    direct_findings = "\n".join(
        f"- {_safe_text(record.get('claim')).rstrip()} {_safe_text(record.get('citations'))}"
        for record in direct_records
    )
    if chinese:
        primary_source_note = (
            f"**本报告点名使用的一手/第一方证据：** {primary_source_summary}。"
            "带日期或版本的资料保留其明确时间与适用范围；不能仅凭日期认定或否定当前适用性。\n\n"
            if primary_source_summary
            else ""
        )
        direct = (
            "## 直接回答\n\n"
            "以下是已通过逐条摘录核验、可直接用于回答本问题的核心事实；每一项都保留其来源绑定，"
            "不把单篇来源改写成跨来源统一规则。\n\n"
            + direct_findings
            + "\n\n"
        )
        sections = [direct]
        if primary_blocks:
            sections.append(
                "## 一手资料直接支持的事实与用法\n\n"
                + primary_source_note
                + "\n\n".join(primary_blocks)
            )
        if secondary_blocks:
            sections.append(
                "## 二手资料与署名经验\n\n"
                "**来源角色边界：** 本节仅保留署名经验、案例与权衡；它们不是上方框架限定动作的前提，不构成官方规则，也不证明当前官方保证。\n\n"
                + "\n\n".join(secondary_blocks)
            )
    else:
        primary_source_note = (
            f"**Primary/first-party evidence explicitly used here:** {primary_source_summary}. "
            "Dated or versioned material retains its explicit time and applicability scope; its date alone neither establishes nor negates current applicability.\n\n"
            if primary_source_summary
            else ""
        )
        direct = (
            "## Direct answer\n\n"
            "The following core findings were each verified against a bound source excerpt and directly address the question. "
            "They are source-bound facts, not a claim that any one source published a universal cross-source rule.\n\n"
            + direct_findings
            + "\n\n"
        )
        sections = [direct]
        if primary_blocks:
            sections.append(
                "## Facts and usages directly supported by primary sources\n\n"
                + primary_source_note
                + "\n\n".join(primary_blocks)
            )
        if secondary_blocks:
            sections.append(
                "## Secondary material and attributed experience\n\n"
                "**Source-role boundary:** This section retains attributed experience, examples, and trade-offs only. These items are not premises for the framework-scoped actions above; they do not establish an official rule or current official guarantees.\n\n"
                + "\n\n".join(secondary_blocks)
            )

    def select_records(
        include_pattern: str,
        *,
        exclude_pattern: str = "",
        limit: int = 4,
        claim_only: bool = False,
        roles: tuple[str, ...] = ("primary",),
    ) -> list[dict[str, Any]]:
        selected: list[dict[str, Any]] = []
        seen_sources: set[str] = set()
        for record in claim_records:
            if record.get("role") not in roles:
                continue
            search_text = _safe_text(
                record.get("claimSearchText") if claim_only else record.get("searchText")
            )
            if not re.search(include_pattern, search_text, re.IGNORECASE):
                continue
            if exclude_pattern and re.search(exclude_pattern, search_text, re.IGNORECASE):
                continue
            source_key = _safe_text(record.get("sourceKey"))
            if source_key in seen_sources:
                continue
            selected.append(record)
            seen_sources.add(source_key)
            if len(selected) >= max(1, limit):
                break
        return selected

    def premise_text(records: list[dict[str, Any]]) -> str:
        separator = "；" if chinese else "; "
        return separator.join(
            f"{_safe_text(record.get('claim')).rstrip('。.!')} {_safe_text(record.get('citations'))}"
            for record in records
        )

    def record_citations(records: list[dict[str, Any]]) -> str:
        keys = list(
            dict.fromkeys(
                key
                for record in records
                for key in list(record.get("citationKeys") or [])
                if _safe_text(key)
            )
        )
        return "".join(f"[{key}]" for key in keys)

    def distinct_source_records(
        records: list[dict[str, Any]],
        *,
        limit: int,
    ) -> list[dict[str, Any]]:
        selected: list[dict[str, Any]] = []
        seen: set[str] = set()
        for record in records:
            source_key = _safe_text(record.get("sourceKey"))
            if not source_key or source_key in seen:
                continue
            selected.append(record)
            seen.add(source_key)
            if len(selected) >= max(1, limit):
                break
        return selected

    framework_pattern = r"\b(?:argparse|click|typer|command[- ]line|cli|argument|option|parser|parameter|get_app_dir)\b"
    # Only later, concrete Path operations may support the "keep using Path
    # downstream" synthesis.  Protocol/representation facts such as
    # os.PathLike, __fspath__, or NotImplementedError are useful evidence in
    # their own sections, but they do not establish that implementation step.
    core_path_pattern = (
        r"(?:\b(?:exists|is_dir|open|read_text|write_text|iterdir|glob|with_suffix)\s*\(|"
        r"\bfilesystem\s+path\s+operations?\b|"
        r"\bconcrete\s+paths?\b.{0,80}\bI/O(?:\s+operations?)?\b|"
        r"\bPath\s+object\b.{0,100}(?:\bused\s+with\s*/|"
        r"\bcombined\b.{0,40}\bstring\b|\bnew\s+Path\s+object\b))"
    )
    validation_pattern = r"\b(?:exists|file_okay|dir_okay|resolve_path|validat(?:e|es|ion)|permission|path_type)\b"
    configuration_pattern = r"\b(?:get_app_dir|application\s+directory|configuration|config\s+file|user\s+data)\b"
    framework_records = select_records(framework_pattern, limit=4)
    conversion_records = select_records(
        r"\b(?:type\s+converter|documented\s+type\s+example|type\s*=\s*pathlib|"
        r"path_type.{0,80}(?:convert|incoming\s+path\s+value|returned?\s+type)|"
        r"passes?\s+pathlib\.Path.{0,30}converter|convert(?:s|ed|ing)?\s+(?:an?\s+)?"
        r"(?:incoming\s+)?path\s+value|supports?\s+pathlib\.Path|Path[- ]typed\s+argument)\b",
        limit=4,
    )
    core_path_records = select_records(
        core_path_pattern,
        # Select on the claim's concrete operation, not on the source title.
        # A framework page can legitimately document a later Path operation
        # (for example joining an app directory with ``/``); the strict
        # positive pattern already rejects validation and protocol-only facts.
        limit=4,
    )
    core_path_records = [
        record
        for record in core_path_records
        if (
            re.search(
                r"docs\.python\.org/(?:3|\d+(?:\.\d+)?)/library/pathlib\.html",
                _safe_text(record.get("sourceUrl")),
                re.IGNORECASE,
            )
            or re.search(
                r"\b(?:pathlib\.Path|Path object|pure paths?|concrete paths?)\b",
                _safe_text(record.get("claimSearchText")),
                re.IGNORECASE,
            )
        )
        and not re.search(
            r"\bif you (?:just )?want to read or write a file\b.{0,80}\bsee\s+open\s*\(",
            _safe_text(record.get("claimSearchText")),
            re.IGNORECASE | re.DOTALL,
        )
    ]
    current_core_path_records = [
        record
        for record in core_path_records
        if _safe_text(record.get("temporalStatus"))
        not in {"future_anomaly", "malformed_date"}
    ]
    canonical_current_core_path_records = [
        record
        for record in current_core_path_records
        if re.search(
            r"docs\.python\.org/(?:3|\d+(?:\.\d+)?)/library/pathlib\.html",
            _safe_text(record.get("sourceUrl")),
            re.IGNORECASE,
        )
    ]
    # Prefer the canonical pathlib reference for a generalized downstream Path
    # operation. A framework tutorial may still document a valid example and
    # remains visible in its own framework/configuration section, but it should
    # not displace a stronger language-level source when one is already read.
    if canonical_current_core_path_records:
        current_core_path_records = canonical_current_core_path_records
    path_protocol_records = select_records(
        r"\b(?:os\.PathLike|PathLike|__fspath__|os\.fspath)\b",
        limit=3,
        claim_only=True,
    )
    validation_records = select_records(validation_pattern, limit=4)
    boundary_validation_records = select_records(
        r"\b(?:exists|file_okay|dir_okay|resolve_path|validat(?:e|es|ion)|permission)\b",
        limit=8,
    )
    boundary_validation_records = [
        record
        for record in boundary_validation_records
        if re.search(framework_pattern, _safe_text(record.get("searchText")), re.IGNORECASE)
    ][:4]
    path_conversion_records = select_records(
        r"\bpath_type\b.{0,80}\b(?:convert(?:s|ed|ing)?|incoming\s+path\s+value|returned?\s+type)\b|"
        r"\b(?:convert(?:s|ed|ing)?|converter).{0,50}(?:pathlib|path)|"
        r"(?:pathlib|path).{0,50}(?:convert(?:s|ed|ing)?|converter)\b",
        limit=3,
    )
    configuration_records = select_records(configuration_pattern, limit=3, claim_only=True)
    configuration_path_records = select_records(
        r"\b(?:Path objects? can be joined|joined with\s*/|construct(?:s|ed|ing)?\s+(?:the\s+)?(?:concrete\s+)?config(?:uration)?\s+path)\b",
        limit=3,
    )
    path_cli_guidance_requested = bool(
        re.search(
            r"\b(?:pathlib|argparse|click|typer|pathlike|__fspath__|path_type)\b|"
            r"(?:python\s+path(?:s|lib)?|路径对象|命令行路径|CLI\s+path)",
            question,
            re.IGNORECASE,
        )
    )
    if not path_cli_guidance_requested:
        # The specialized Path/CLI synthesis below is useful for an explicit
        # Python path question only. A generic product/configuration claim
        # must never activate a leftover framework-specific recommendation.
        framework_records = []
        conversion_records = []
        core_path_records = []
        current_core_path_records = []
        canonical_current_core_path_records = []
        path_protocol_records = []
        validation_records = []
        boundary_validation_records = []
        path_conversion_records = []
        configuration_records = []
        configuration_path_records = []
    decision_blocks: list[str] = []
    argparse_choice_records = [
        record for record in claim_records
        if record.get("role") == "primary" and "argparse" in _safe_text(record.get("searchText"))
    ][:3]
    click_choice_records = [
        record for record in claim_records
        if record.get("role") == "primary"
        and "click" in _safe_text(record.get("searchText"))
        and re.search(r"\b(?:path_type|exists|file_okay|dir_okay|permission|Path type)\b", _safe_text(record.get("claim")), re.IGNORECASE)
    ][:4]
    typer_choice_records = distinct_source_records(
        [
            record for record in claim_records
            if record.get("role") == "primary"
            and "typer" in _safe_text(record.get("searchText"))
            and re.search(r"\b(?:Path type|Path argument|exists|get_app_dir|configuration)\b", _safe_text(record.get("claim")), re.IGNORECASE)
        ],
        limit=3,
    )
    typer_integration_records = [
        record
        for record in typer_choice_records
        if re.search(
            r"\b(?:Path[- ]typed|Path\s+(?:type|argument|parameter)|exists|file_okay|"
            r"dir_okay|resolve_path|validat(?:e|es|ion))\b",
            _safe_text(record.get("claim")),
            re.IGNORECASE,
        )
    ]
    if not path_cli_guidance_requested:
        argparse_choice_records = []
        click_choice_records = []
        typer_choice_records = []
        typer_integration_records = []
    if argparse_choice_records or click_choice_records or typer_choice_records:
        choice_lines: list[str] = []
        choice_matrix_citations = record_citations(
            [
                *argparse_choice_records,
                *click_choice_records,
                *typer_choice_records,
            ]
        )
        if argparse_choice_records:
            argparse_normative_records = [
                record
                for record in argparse_choice_records
                if record.get("claimType") == "explicit_normative"
                or "default recommended" in _safe_text(record.get("claim")).lower()
            ]
            citations = record_citations(
                argparse_normative_records or argparse_choice_records
            )
            argparse_choice_text = (
                (
                    "- 基础且希望只用标准库的 CLI：argparse 原文范围是‘用于实现基础命令行应用的默认推荐标准库模块’；"
                    "行为要求更精细的作者可能需要控制粒度更高的解析器。该限定范围不能扩大成所有 CLI 的总排名。"
                    if chinese
                    else "- Basic command-line application that should remain standard-library-only: argparse is documented as the default recommended standard-library module for implementing basic command-line applications; authors with more exacting behavior requirements may need a parser with finer-grained control. Do not expand that scoped statement into an all-CLI ranking."
                )
                if argparse_normative_records
                else (
                    "- 已采用 argparse：**推荐实施：** 按官方示例设置 `type=pathlib.Path`，让路径参数在解析边界进入 Path 处理流程。"
                    "**文档依据：** argparse 示例明确展示该配置；本报告不把示例改写成官方推荐。"
                    if chinese
                    else "- Existing argparse application: **Recommended implementation:** set `type=pathlib.Path` so the path argument enters the handler through the documented parser configuration. **Documented basis:** the argparse example shows that exact setting; this report does not rewrite the example as an official recommendation."
                )
            )
            choice_lines.append(argparse_choice_text + f" {citations}")
        if click_choice_records:
            citations = record_citations(click_choice_records)
            click_has_conversion = any(
                re.search(
                    r"\bconvert(?:s|ed|ing)?\b|\bincoming\s+path\s+value\b|"
                    r"\breturned?\s+type\b",
                    _safe_text(record.get("claim")),
                    re.IGNORECASE,
                )
                for record in click_choice_records
            )
            click_has_validation = any(
                re.search(r"\b(?:exists|file_okay|dir_okay|permission|validat)\b", _safe_text(record.get("claim")), re.IGNORECASE)
                for record in click_choice_records
            )
            click_signature_defaults_none = any(
                re.search(
                    r"\bpath_type\s*=\s*None\b|"
                    r"\bsignature\b.{0,120}\bpath_type\b.{0,40}\bNone\b",
                    _safe_text(record.get("claim")),
                    re.IGNORECASE | re.DOTALL,
                )
                for record in click_choice_records
            )
            click_none_keeps_str = any(
                re.search(
                    r"\bpath_type\b.{0,180}\bif\s+None\b.{0,160}"
                    r"\b(?:Python(?:'s|’s)\s+default|default)\b.{0,80}\bstr\b",
                    _safe_text(record.get("claim")),
                    re.IGNORECASE | re.DOTALL,
                )
                for record in click_choice_records
            )
            click_default_boundary = (
                (
                    "已验证签名记录 path_type=None，配套参数合同说明 None 会保留 Python 默认的 str；因此这里显式设置 pathlib.Path。"
                    if chinese
                    else "The verified signature records path_type=None, and the parameter contract states that None keeps Python's default str; this is why the action here sets pathlib.Path explicitly."
                )
                if click_signature_defaults_none and click_none_keeps_str
                else (
                    (
                        "已验证签名记录 path_type=None；本段不推定当前证据未记录的 None 分支返回类型。"
                        if chinese
                        else "The verified signature records path_type=None; this paragraph does not infer a return type for the None branch that the current claims do not record."
                    )
                    if click_signature_defaults_none
                    else (
                        (
                            "已验证参数合同说明 path_type 为 None 时保留 Python 默认的 str；本段不把该条件分支改写成未经签名证据支持的参数默认值。"
                            if chinese
                            else "The verified parameter contract states that when path_type is None, Click keeps Python's default str; this paragraph does not rewrite that conditional behavior as a parameter default without signature evidence."
                        )
                        if click_none_keeps_str
                        else (
                            "本句不陈述 path_type 的默认值。"
                            if chinese
                            else "This sentence makes no claim about path_type's default value."
                        )
                    )
                )
            )
            if click_has_conversion and click_has_validation:
                click_choice_text = (
                    "- 已采用 Click：**推荐实施：** 显式配置 Click 的 Path/path_type，并只启用参数合同需要的存在性、文件/目录类型或权限检查。"
                    "**文档依据：** 来源记录这些转换与校验选项；把它们组合到具体命令边界是本报告的工程综合。"
                    + click_default_boundary
                    if chinese
                    else "- Existing Click application: **Recommended implementation:** explicitly configure Click's Path/path_type and enable only the existence, file/directory-type, or permission checks required by the argument contract. **Documented basis:** the source records those conversion and validation options; combining them at a concrete command boundary is this report's engineering synthesis. "
                    + click_default_boundary
                )
            elif click_has_conversion:
                click_choice_text = (
                    "- 已采用 Click：**推荐实施：** 需要处理函数接收 Path 时，设置 `path_type=pathlib.Path`。"
                    "**文档依据：** 引用前提分别记录 Click 允许该设置，以及 path_type 会把输入路径值转换为指定类型。"
                    + click_default_boundary
                    + " 若命令还需要存在性、类型或权限检查，再按已验证校验合同配置。"
                    if chinese
                    else "- Existing Click application: **Recommended implementation:** when the handler should receive a Path, set `path_type=pathlib.Path`. **Documented basis:** the cited premises record both that Click allows this setting and that path_type converts the incoming path value to the specified type. "
                    + click_default_boundary
                    + " If the command also needs existence, type, or permission checks, configure those documented checks according to the argument contract."
                )
            else:
                click_choice_text = (
                    "- 已采用 Click：只实施当前 verified claim 逐字记录的 Path 能力；本报告不从一个未说明转换行为或校验行为的摘录推定另一项能力。"
                    if chinese
                    else "- Existing Click application: implement only the Path capability stated by the current verified claim; this report does not infer conversion or validation behavior that the excerpt does not record."
                )
            choice_lines.append(click_choice_text + f" {citations}")
        if typer_choice_records:
            typer_path_records = [
                record
                for record in typer_choice_records
                if re.search(r"\b(?:Path type|Path argument|exists|file_okay)\b", _safe_text(record.get("claim")), re.IGNORECASE)
            ]
            typer_config_records = [
                record
                for record in typer_choice_records
                if re.search(r"\b(?:get_app_dir|configuration)\b", _safe_text(record.get("claim")), re.IGNORECASE)
            ]
            citations = record_citations(typer_choice_records)
            typer_parts: list[str] = []
            if typer_path_records:
                typer_parts.append(
                    "**推荐实施：** 用 Path 类型参数接收路径；命令需要校验时，按文档列出的 exists/file_okay 等参数设置所需检查"
                    if chinese
                    else "**Recommended implementation:** receive the value as a Path-typed argument; when the command needs validation, set the required checks through documented parameters such as exists/file_okay"
                )
            if typer_config_records:
                typer_parts.append(
                    "用户级配置位置可使用 get_app_dir，再由 Path 构造具体文件"
                    if chinese
                    else "for per-user configuration, use the documented get_app_dir entry point and then construct the concrete file with Path"
                )
            choice_lines.append(
                ("- 已采用 Typer：" if chinese else "- Existing Typer application: ")
                + ("；" if chinese else "; ").join(typer_parts)
                + ("。" if chinese else ".")
                + f" {citations}"
            )
        decision_blocks.append(
            (
                "### 按 CLI 场景选择，而不是做无条件库排名\n\n"
                "**本报告的综合判断—可执行选择：** 根据应用实际使用的解析器，直接采用下方对应的框架限定动作。"
                "本节不建议迁移框架，也不声称某个框架普遍更优；它只把各框架独立合同组织成可执行分支。"
                f" {choice_matrix_citations}\n\n"
                "下面每条加粗建议都是本报告的框架限定建议，随后紧跟文档依据；引用不表示某个来源逐字发布了整条组合建议。\n\n"
                if chinese
                else "### Choose by CLI context, not by an unconditional library ranking\n\n"
                "**Practical synthesis — actionable choice:** Select the framework-specific branch below that matches the parser actually used by the application. "
                "This section recommends neither a framework migration nor a universally superior library; it organizes the separate framework contracts into implementable branches."
                f" {choice_matrix_citations}\n\n"
                "Each bold recommendation below is report-level guidance followed by its documented framework basis; the citations do not claim that one source wrote the combined recommendation.\n\n"
            )
            + "\n\n".join(choice_lines)
        )
    primary_cli_integration_records = distinct_source_records(
        [
            *path_conversion_records,
            *conversion_records,
            *typer_integration_records,
            *current_core_path_records,
        ],
        limit=5,
    )
    # Keep attributed secondary experience visible in its dedicated section,
    # but do not make it a premise for the primary cross-framework pattern.
    # This preserves the experience without silently upgrading its authority.
    cli_integration_records = list(primary_cli_integration_records)
    if len(cli_integration_records) >= 2 and current_core_path_records:
        premises = premise_text(cli_integration_records)
        decision_citations = record_citations(cli_integration_records)
        if chinese:
            decision_blocks.append(
                "### CLI 解析与框架接入\n\n"
                "**推荐实施模式（本报告综合）：** 在所选框架的输入边界得到 Path 后，让业务代码继续使用 Path 完成下文已引用的路径操作，避免立即退回字符串再重复解析。"
                f"**文档依据：** {premises}。"
                "各引用分别证明框架输入合同和 Path 操作；把它们串成这一实施模式，是本报告从一手合同得出的跨来源推论。"
                "argparse、Click、Typer 的具体入口仍按各自分支使用。"
                + f" {decision_citations}"
            )
        else:
            decision_blocks.append(
                "### CLI parsing and framework integration\n\n"
                "**Recommended implementation pattern (report synthesis):** once the selected framework's input boundary produces a Path, keep using that Path in application code for the cited downstream operations instead of immediately converting it back to a string and reparsing it. "
                f"**Documented basis:** {premises}. "
                "The citations separately establish the framework input contracts and Path operations; connecting them into this implementation pattern is the report's cross-source inference from the primary contracts. "
                "Use the specific argparse, Click, or Typer entry point shown in its own branch."
                + f" {decision_citations}"
            )
    if len(current_core_path_records) >= 2:
        premises = premise_text(current_core_path_records)
        decision_citations = record_citations(current_core_path_records)
        if chinese:
            decision_blocks.append(
                "### 路径语义、文件系统操作与历史边界\n\n"
                f"**本报告的综合判断—明确的跨来源工程建议：** 以下前提已分别绑定引用：{premises}。"
                "这些来源分别记录后续路径对象操作与 CLI 框架的输入合同。"
                "**工程分解边界：** 因此本报告把 CLI 输入转换与后续路径对象操作作为两个工程层分别核对；"
                "这种分层是本报告的方法，不冒充官方资料逐字发布的推荐；本段只采用段末明确列出的引用作为前提。"
                f" {decision_citations}"
            )
        else:
            decision_blocks.append(
                "### Path semantics, filesystem operations, and historical boundaries\n\n"
                f"**This report's synthesis — explicit cross-source engineering recommendation:** The following premises are individually citation-bound: {premises}. "
                "These sources document downstream path-object operations separately from the CLI frameworks' input contracts. "
                "**Engineering-decomposition boundary:** this report therefore treats CLI input conversion and later path-object operations as separate engineering layers; "
                "that separation is the report's methodology, not a verbatim official recommendation. Only the citations displayed in this paragraph are its premises."
                f" {decision_citations}"
            )
    if path_protocol_records:
        premises = premise_text(path_protocol_records)
        decision_citations = record_citations(path_protocol_records)
        if chinese:
            decision_blocks.append(
                "### 路径协议与 API 兼容边界\n\n"
                f"**来源直接支持的协议事实：** {premises}。"
                "**非可执行 CLI 前提：** 本段只提供路径协议背景，不用于声称 argparse、Click 或 Typer 接受 PathLike。"
                "当具体 CLI 或库调用是否接受 PathLike 会影响实现时，"
                "仍以该调用自身已引用的参数合同为准。本句不声称任何未核验 API 会拒绝 PathLike。"
                f" {decision_citations}"
            )
        else:
            decision_blocks.append(
                "### Path protocol and API compatibility boundary\n\n"
                f"**Source-backed protocol facts:** {premises}. "
                "**Not an actionable CLI premise:** this section is path-protocol background only; it is not used to claim that argparse, Click, or Typer accepts PathLike. "
                "When a concrete CLI or library call's PathLike acceptance affects the implementation, rely on that call's own cited parameter contract. "
                "This section does not claim that any unverified API accepts or rejects PathLike."
                f" {decision_citations}"
            )
    validation_scenario_records = list(
        {
            _safe_text(record.get("sourceKey")): record
            for record in [*path_conversion_records, *boundary_validation_records]
            if _safe_text(record.get("sourceKey"))
        }.values()
    )[:5]
    if validation_scenario_records:
        premises = premise_text(validation_scenario_records)
        decision_citations = record_citations(validation_scenario_records)
        if chinese:
            decision_blocks.append(
                "### 类型转换与输入验证\n\n"
                f"**本报告的综合判断：** 已验证前提包括：{premises}。"
                + (
                    "**推荐实施（本报告综合）：** 处理函数需要 Path 时，采用该框架记录的转换器设置或 Path 类型参数；"
                    "再依据命令的真实输入合同，只启用文档列出的必要校验项。"
                    "**文档依据：** 转换 claim 说明进入处理函数的值，校验 claim 列出存在性、文件/目录类型或权限检查；来源分别证明这些控制项，本报告只负责组合实施顺序。"
                    if boundary_validation_records and path_conversion_records
                    else "这只是资料明确记录的路径转换或验证能力；在 CLI 输入边界不得据此推定额外检查。"
                )
                + f" {decision_citations}"
            )
        else:
            decision_blocks.append(
                "### Type conversion and input validation\n\n"
                f"**Practical synthesis:** The verified premises are: {premises}. "
                + (
                    "**Recommended implementation (report synthesis):** when the handler should receive a Path, use the framework's documented converter setting or Path-typed parameter, then enable only the documented validation checks required by the command's actual input contract. "
                    "**Documented basis:** the conversion claim establishes the value delivered to the handler and the validation claim enumerates existence, file/directory-type, or permission controls; the sources establish those controls individually and this report composes the implementation sequence."
                    if boundary_validation_records and path_conversion_records
                    else "This is only the path conversion or validation capability explicitly documented in these premises; do not infer additional checks at the CLI boundary."
                )
                + f" {decision_citations}"
            )
    if configuration_records:
        configuration_scenario_records = [
            *configuration_records,
            *configuration_path_records,
        ][:4]
        premises = premise_text(configuration_scenario_records)
        decision_citations = record_citations(configuration_scenario_records)
        if chinese:
            decision_blocks.append(
                "### 应用配置目录\n\n"
                f"**来源支撑的应用模式：** 已验证事实包括：{premises}。"
                "在 Typer 内，对于按用户保存的 CLI 配置，优先使用该框架已经记录的应用目录入口，再用 Path 构造具体配置文件路径；"
                "只有当该入口的平台语义符合产品需求时才采用，且不要把应用目录发现误写成 pathlib 自身的能力。"
                f" {decision_citations}"
            )
        else:
            decision_blocks.append(
                "### Application configuration locations\n\n"
                f"**Source-backed application pattern:** The verified facts are: {premises}. "
                "Within Typer, for per-user CLI configuration, use that framework's documented application-directory entry point and then construct the concrete config path with Path, "
                "but only when that entry point's platform semantics fit the product. Do not attribute application-directory discovery to pathlib itself."
                f" {decision_citations}"
            )
    if decision_blocks:
        guidance_intro = (
            "**可执行答案入口：** 下列条目就是本报告给出的具体实践；每条都说明适用框架、要采取的动作、引用前提和推论边界。"
            "后面的证据目录用于复核这些条目，不替代这里的直接答案。\n\n"
            if chinese
            else "**Actionable-answer entry point:** The items below are the report's concrete practices. Each states the applicable framework, the action to take, its cited premises, and the inference boundary. "
            "The later evidence catalog supports these items; it does not replace the direct answer.\n\n"
        )
        sections.insert(
            1,
            ("## 基于证据的可执行实践指南\n\n" if chinese else "## Evidence-backed practical guidance\n\n")
            + guidance_intro
            + "\n\n".join(decision_blocks),
        )

    inference_blocks: list[str] = []
    claim_by_id = {
        _safe_text(claim.get("claimId")): claim
        for claim in claims
        if _safe_text(claim.get("claimId"))
    }
    for inference in _research_dict_list(verified_plan.get("compositeInferences"), limit=12):
        inference_text = _safe_text(
            inference.get("inference")
            or inference.get("conclusion")
            or inference.get("recommendation")
        )
        premise_claims = [
            claim_by_id.get(_safe_text(claim_id))
            for claim_id in list(inference.get("premiseClaimIds") or [])
        ]
        premise_claims = [claim for claim in premise_claims if isinstance(claim, dict)]
        premise_keys = list(
            dict.fromkeys(
                _safe_text(support.get("citationKey") or support.get("citation")).strip("[]").upper()
                for claim in premise_claims
                for support in list(claim.get("supportingSources") or [])
                if isinstance(support, dict)
                and _safe_text(support.get("citationKey") or support.get("citation"))
            )
        )
        if inference_text and premise_keys:
            prefix = "**本报告的综合判断：** " if chinese else "**Practical synthesis:** "
            inference_blocks.append(prefix + inference_text + " " + "".join(f"[{key}]" for key in premise_keys))
    if inference_blocks and not decision_blocks:
        sections.insert(
            2 if decision_blocks else 1,
            ("## 已登记的跨来源综合判断\n\n" if chinese else "## Registered cross-source syntheses\n\n")
            + "\n\n".join(inference_blocks),
        )

    dated_or_version_scoped_citations = record_citations(
        distinct_source_records(
            [
                record
                for record in claim_records
                if record.get("documentDates") or record.get("version")
            ],
            limit=8,
        )
    )
    undated_secondary_citations = record_citations(
        distinct_source_records(
            [
                record
                for record in claim_records
                if record.get("temporalStatus") == "undated"
                and record.get("role") == "secondary"
            ],
            limit=8,
        )
    )
    if chinese:
        conclusion_boundary = (
            "## 结论边界\n\n本报告只保留与已读取摘录绑定的事实和已登记综合判断。"
        )
        if dated_or_version_scoped_citations:
            conclusion_boundary += (
                f" **本报告已实际应用的日期/版本边界：** {dated_or_version_scoped_citations} 的时间戳与版本范围均已显式列出；"
                "没有按固定年龄自动判定其失效，具体适用性由独立 Reviewer 按主张和问题判断。"
            )
        if undated_secondary_citations:
            conclusion_boundary += (
                f" **本报告已实际应用的无日期边界：** {undated_secondary_citations} 只保留为明确署名的经验或非时效案例。"
            )
    else:
        conclusion_boundary = (
            "## Conclusion boundaries\n\nThis report retains only facts bound to read excerpts and registered syntheses."
        )
        if dated_or_version_scoped_citations:
            conclusion_boundary += (
                f" **Date/version boundaries applied in this report:** {dated_or_version_scoped_citations} expose their timestamps and version scope;"
                " none is classified as invalid by a fixed age rule, and the independent Reviewer judges applicability to each claim and the question."
            )
        if undated_secondary_citations:
            conclusion_boundary += (
                f" **Undated boundary applied in this report:** {undated_secondary_citations} remain explicitly attributed experience or non-temporal examples only."
            )
    sections.append(conclusion_boundary)
    return _assemble_architect_sections(
        question=question,
        verified_plan=verified_plan,
        sections=sections,
        sources=sources,
    )


def _invoke_architect_candidate_with_deadline(
    candidate: tuple[Any, str, str],
    messages: list[Any],
    *,
    seconds: float,
    max_tokens: int,
    disable_thinking: bool = False,
) -> Any:
    llm = candidate[0]
    request_timeout = max(0.25, float(seconds) - 0.25)
    request_kwargs: dict[str, Any] = {
        "timeout": request_timeout,
        "max_tokens": max_tokens,
    }
    if disable_thinking:
        meta = getattr(llm, "_meta", None)
        control = dict(meta.get("thinking_control") or {}) if isinstance(meta, dict) else {}
        if control.get("supportsNoThink"):
            control["disabled"] = True
            request_kwargs.update(no_think_request_patch(control))
    ainvoke = getattr(llm, "ainvoke", None)
    if callable(ainvoke) and threading.current_thread() is threading.main_thread():
        async def invoke_async() -> Any:
            return await asyncio.wait_for(
                ainvoke(
                    messages,
                    config={"callbacks": []},
                    **request_kwargs,
                ),
                timeout=request_timeout,
            )

        try:
            asyncio.get_running_loop()
        except RuntimeError:
            return asyncio.run(invoke_async())
        executor = ThreadPoolExecutor(max_workers=1)
        future = executor.submit(lambda: asyncio.run(invoke_async()))
        try:
            return future.result(timeout=max(0.5, float(seconds)))
        finally:
            executor.shutdown(wait=False, cancel_futures=True)

    executor = ThreadPoolExecutor(max_workers=1)
    future = executor.submit(
        lambda: llm.invoke(
            messages,
            config={"callbacks": []},
            **request_kwargs,
        )
    )
    try:
        return future.result(timeout=max(0.5, float(seconds)))
    finally:
        executor.shutdown(wait=False, cancel_futures=True)


def _research_architect_mode(
    question: str,
    sources: list[dict[str, Any]],
    *,
    freshness: str,
) -> str:
    stats = _research_architect_structural_stats(question, sources, freshness=freshness)
    return "full_synthesis" if stats.get("structuralTargetMet") is True else "gap_review"


def _architect_review_claim_ledger(
    claims: list[dict[str, Any]],
    sources: list[dict[str, Any]],
) -> dict[str, Any]:
    """Build the canonical, lossless evidence surface for final review.

    Planning evidence candidates are intentionally excluded.  Once a claim is
    Runtime-verified, showing the reviewer both the old candidates and the
    locked excerpt creates two competing meanings for the same ``[S#]`` key.
    The review stage needs only the immutable citation index and exact claim
    excerpts that the writer was allowed to use.
    """

    source_by_key = {
        _safe_text(source.get("citationKey")).strip("[]").upper(): source
        for source in sources
        if isinstance(source, dict) and _safe_text(source.get("citationKey"))
    }
    claim_rows: list[dict[str, Any]] = []
    cited_keys: list[str] = []
    facet_claims: dict[str, list[str]] = {}
    facet_citations: dict[str, list[str]] = {}
    for claim in claims:
        if not isinstance(claim, dict):
            continue
        claim_id = _safe_text(claim.get("claimId"))
        citation_keys = list(
            dict.fromkeys(
                _safe_text(
                    support.get("citationKey") or support.get("citation")
                    if isinstance(support, dict)
                    else support
                )
                .strip("[]")
                .upper()
                for support in list(claim.get("supportingSources") or [])
                if _safe_text(
                    support.get("citationKey") or support.get("citation")
                    if isinstance(support, dict)
                    else support
                ).strip("[]")
            )
        )
        if not claim_id or not citation_keys:
            continue
        for citation_key in citation_keys:
            if citation_key not in cited_keys:
                cited_keys.append(citation_key)
        facet_ids = _architect_claim_facet_ids(claim)
        for facet_id in facet_ids:
            if claim_id not in facet_claims.setdefault(facet_id, []):
                facet_claims[facet_id].append(claim_id)
            for citation_key in citation_keys:
                if citation_key not in facet_citations.setdefault(facet_id, []):
                    facet_citations[facet_id].append(citation_key)
        claim_rows.append(
            {
                "claimId": claim_id,
                "claim": _safe_text(claim.get("claim")),
                "claimType": _safe_text(claim.get("claimType")) or "source_fact",
                **(
                    {"normativeCue": _safe_text(claim.get("normativeCue"))}
                    if _safe_text(claim.get("normativeCue"))
                    else {}
                ),
                "citationKeys": citation_keys,
                "evidenceExcerptKey": _safe_text(claim.get("evidenceExcerptKey")),
                "exactEvidenceExcerpt": _safe_text(claim.get("evidenceExcerpt")),
                "evidenceExcerptSha256": _safe_text(claim.get("evidenceExcerptSha256")),
                **(
                    {"temporalBoundary": _safe_text(claim.get("temporalBoundary"))}
                    if _safe_text(claim.get("temporalBoundary"))
                    else {}
                ),
                **({"facetIds": facet_ids} if facet_ids else {}),
            }
        )

    def citation_sort_key(value: str) -> tuple[int, str]:
        match = re.fullmatch(r"S(\d+)", value, re.IGNORECASE)
        return (int(match.group(1)) if match else 10_000, value)

    citation_index: list[dict[str, Any]] = []
    for citation_key in sorted(cited_keys, key=citation_sort_key):
        source = source_by_key.get(citation_key) or {}
        citation_index.append(
            {
                "citationKey": citation_key,
                "sourceId": _safe_text(source.get("sourceId")),
                "title": _safe_text(source.get("title")),
                "url": _safe_text(source.get("url")),
                "tier": source.get("tier"),
                "authorityTier": source.get("authorityTier"),
                "runtimeOfficialSeed": source.get("runtimeOfficialSeed") is True,
                "retrievedAt": source.get("retrievedAt"),
                "publishedAt": source.get("publishedAt"),
                "updatedAt": source.get("updatedAt"),
                "sourceDate": source.get("sourceDate"),
                "sourceDateKind": source.get("sourceDateKind"),
                "version": source.get("version"),
                "temporalEvidence": source.get("temporalEvidence") or {},
            }
        )
    return {
        "bindingRule": (
            "citationIndex and claims are the only canonical [S#] bindings for review; "
            "source position and planning candidates carry no evidentiary meaning"
        ),
        "citationIndex": citation_index,
        "claims": claim_rows,
        "facetCoverage": [
            {
                "facetId": facet_id,
                "claimIds": facet_claims[facet_id],
                "citationKeys": facet_citations.get(facet_id, []),
            }
            for facet_id in sorted(facet_claims)
        ],
    }


def _invoke_web_research_architect_staged(
    *,
    question: str,
    sources: list[dict[str, Any]],
    freshness: str,
    timeout_seconds: int,
    per_call_timeout_seconds: int | None = None,
) -> dict[str, Any]:
    started_at = time.perf_counter()
    total_budget = max(5, int(timeout_seconds or 0))
    configured_call_timeout = (
        _as_int(per_call_timeout_seconds, 0)
        if per_call_timeout_seconds is not None
        else _as_int(_research_config().get("architectAgentTimeoutSeconds"), 60)
    )
    call_timeout_cap_seconds = max(
        5.0,
        min(float(configured_call_timeout or 60), 90.0, float(total_budget)),
    )
    delivery_reserve = min(120.0, max(4.0, total_budget * 0.5))
    review_reserve = min(80.0, max(8.0, total_budget * 0.3))
    candidate_errors: list[str] = []
    raw_previews: list[str] = []
    plan_attempts: list[dict[str, Any]] = []
    context_preparations: list[dict[str, Any]] = []
    context_preparations_lock = threading.Lock()

    def remaining_seconds() -> float:
        return max(0.0, total_budget - (time.perf_counter() - started_at))

    def prepared_messages(
        candidate: tuple[Any, str, str],
        *,
        system_prompt: str,
        instruction: str,
        materials: list[dict[str, str]],
        target_role: str,
        node: str,
    ) -> list[Any]:
        prepared = prepare_background_model_messages(
            system_prompt=system_prompt,
            instruction=instruction,
            materials=materials,
            runtime_kind="research",
            target_role=target_role,
            resolved_model_id=_architect_candidate_context_model_ref(candidate),
            component="research",
            node=node,
        )
        audit = dict(prepared.audit or {})
        material_stats = [
            {
                "title": _safe_text(material.get("title")),
                "kind": _safe_text(material.get("kind")),
                "chars": len(_safe_text(material.get("content"))),
                "sha256": hashlib.sha256(
                    _safe_text(material.get("content")).encode("utf-8", errors="ignore")
                ).hexdigest(),
            }
            for material in materials
        ]
        preparation = {
            "node": node,
            "modelId": _architect_candidate_identity(candidate),
            "resolvedModelRef": _architect_candidate_context_model_ref(candidate),
            "modelRole": candidate[2],
            "selectionOrigin": _architect_candidate_selection_origin(candidate),
            "originalMessageCount": int(audit.get("original_message_count") or 0),
            "preparedMessageCount": len(prepared.messages),
            "preparedMessageChars": sum(
                len(_safe_text(getattr(message, "content", "")))
                for message in prepared.messages
            ),
            "estimatedInputTokens": int(audit.get("estimated_input_tokens") or 0),
            "effectiveContextWindowTokens": int(
                audit.get("effective_context_window_tokens") or 0
            ),
            "compactionApplied": audit.get("compaction_applied") is True,
            "compactionMode": _safe_text(audit.get("compaction_mode")),
            "triggerReason": _safe_text(audit.get("trigger_reason")),
            "materials": material_stats,
        }
        if "[RESEARCH-RUNTIME-CONTRACT " in system_prompt:
            preparation.update(
                {
                    "researchPromptContractVersion": RESEARCH_PROMPT_CONTRACT_VERSION,
                    "researchPromptContractDigest": research_runtime_prompt_digest(system_prompt),
                }
            )
        with context_preparations_lock:
            context_preparations.append(preparation)
        return prepared.messages

    compact_sources: list[dict[str, Any]] = []
    for source in sources[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]:
        fallback_evidence_query = _safe_text(source.get("evidenceQuery")) or question
        evidence_queries = list(
            dict.fromkeys(
                _safe_text(value)
                for value in list(source.get("evidenceQueries") or [])
                if _safe_text(value)
            )
        ) or [fallback_evidence_query]
        source_evidence_query = min(evidence_queries, key=len)
        facet_by_query = {
            _safe_text(view.get("evidenceQuery")): _safe_text(
                view.get("researchFacetId")
            )
            for view in list(source.get("evidenceViews") or [])
            if isinstance(view, dict) and _safe_text(view.get("evidenceQuery"))
        }
        source_text = _safe_text(
            _safe_text(source.get("text"))[:_RESEARCH_ARCHITECT_SOURCE_TEXT_CHARS]
        )
        retrieved_at = _safe_text(source.get("retrievedAt"))
        original_content_chars = _as_int(
            source.get("originalContentChars")
            or source.get("contentChars")
            or len(_safe_text(source.get("text"))),
            len(_safe_text(source.get("text"))),
        )
        compact_source = {
            "sourceId": _safe_text(source.get("sourceId")),
            "citationKey": _safe_text(source.get("citationKey")),
            "title": _safe_text(source.get("title")),
            "url": _safe_text(source.get("url")),
            "tier": source.get("tier"),
            "authorityTier": source.get("authorityTier"),
            "authorityScore": source.get("authorityScore"),
            "catalogCategory": source.get("catalogCategory"),
            "catalogSourceId": source.get("catalogSourceId"),
            "runtimeOfficialSeed": source.get("runtimeOfficialSeed") is True,
            "qualityDimensions": source.get("qualityDimensions") or {},
            "retrievedAt": retrieved_at,
            "publishedAt": source.get("publishedAt"),
            "updatedAt": source.get("updatedAt"),
            "sourceDate": source.get("sourceDate"),
            "sourceDateKind": source.get("sourceDateKind"),
            "version": source.get("version"),
            "temporalEvidence": source.get("temporalEvidence") or {},
            "selectedForEvidence": True,
            "contentChars": len(source_text),
            "originalContentChars": original_content_chars,
            "omittedChars": max(0, original_content_chars - len(source_text)),
            "evidenceSelection": source.get("evidenceSelection") or "read_body",
            "readEvidence": _read_evidence_receipt(
                source_text,
                retrieved_at=retrieved_at or None,
            ),
            "researchFacetId": source.get("researchFacetId"),
            "researchFacetIds": list(source.get("researchFacetIds") or []),
            "researchFacetGoal": source.get("researchFacetGoal"),
            "evidenceQueries": evidence_queries,
            "evidenceQuery": source_evidence_query,
            "text": source_text,
        }
        evidence_candidates = _architect_multi_query_evidence_candidates(
            compact_source,
            evidence_queries,
            facet_by_query=facet_by_query,
        )
        citation_key = _safe_text(compact_source.get("citationKey")).strip("[]")
        compact_source["evidenceCandidates"] = [
            {
                **candidate,
                "evidenceExcerptKey": f"{citation_key}:E{index}",
            }
            for index, candidate in enumerate(evidence_candidates, start=1)
        ]
        compact_source["answerabilityScore"] = max(
            [
                int(candidate.get("relevanceScore") or 0)
                for candidate in compact_source["evidenceCandidates"]
                if isinstance(candidate, dict)
            ]
            or [0]
        )
        compact_source["subjectFocused"] = _architect_source_subject_focused(
            compact_source,
            source_evidence_query,
            evidence_candidates=compact_source["evidenceCandidates"],
        )
        compact_sources.append(compact_source)
    prompt_sources = [
        {
            key: source.get(key)
            for key in (
                "sourceId",
                "citationKey",
                "title",
                "url",
                "tier",
                "authorityTier",
                "authorityScore",
                "catalogCategory",
                "catalogSourceId",
                "runtimeOfficialSeed",
                "retrievedAt",
                "publishedAt",
                "updatedAt",
                "sourceDate",
                "sourceDateKind",
                "version",
                "researchFacetIds",
                "researchFacetGoal",
                "evidenceCandidates",
                "answerabilityScore",
                "subjectFocused",
            )
            if source.get(key) not in (None, "", [], {})
        }
        for source in compact_sources
    ]
    required_plan_facet_ids = [
        facet_id
        for facet_id in list(
            dict.fromkeys(
                [
                    *[
                        _research_facet_id(kind)
                        for _goal, kind in _build_explicit_question_facets(question)
                        if _research_facet_id(kind)
                    ],
                    *[
                        _safe_text(facet_id)
                        for source in prompt_sources
                        for facet_id in [
                            *list(source.get("researchFacetIds") or []),
                            source.get("researchFacetId"),
                        ]
                        if _safe_text(facet_id)
                    ],
                ]
            )
        )
        if _research_facet_requires_atomic_claim(facet_id)
    ]
    prompt_sources = _architect_facet_focused_prompt_sources(
        prompt_sources,
        required_plan_facet_ids,
    )
    plan_claim_upper = min(
        _RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
        max(
            _RESEARCH_ARCHITECT_PLAN_MAX_CLAIM_COUNT,
            len(required_plan_facet_ids),
        ),
    )
    plan_claim_lower = min(
        plan_claim_upper,
        max(
            TARGET_RESEARCH_CLAIM_COUNT,
            len(required_plan_facet_ids),
        ),
    )
    required_claim_sources = _architect_required_claim_source_keys(
        prompt_sources,
        limit=TARGET_RESEARCH_SOURCE_COUNT,
        question=question,
    )
    if len(required_claim_sources) < TARGET_RESEARCH_SOURCE_COUNT:
        return {
            "_agentError": "architect_evidence_candidates_insufficient",
            "_architectMode": "full_synthesis",
            "_modelFallbackAttempts": [
                f"evidence_candidate_source_coverage:{len(required_claim_sources)}/{TARGET_RESEARCH_SOURCE_COUNT}"
            ],
        }
    plan_claim_source_floor = MIN_RESEARCH_SOURCE_COUNT
    named_decision_audiences = _research_named_decision_audiences(question)
    runtime_named_decision_fallback_allowed = (
        _architect_runtime_named_decision_fallback_allowed(question)
    )
    question_requires_synthesis = bool(
        re.search(
            r"\b(?:best practices?|recommend(?:ation|ed|s|ing)?|should|selection|trade-?offs?)\b|"
            r"(?:最佳实践|建议|应该|应当|选型|取舍)",
            question,
            re.IGNORECASE,
        )
    )
    source_material = json.dumps(prompt_sources, ensure_ascii=False)
    compact_retry_source_material = json.dumps(
        _architect_facet_focused_prompt_sources(
            prompt_sources,
            required_plan_facet_ids,
            candidates_per_facet=1,
            extra_source_count=0,
        ),
        ensure_ascii=False,
    )
    candidates = _create_web_research_architect_llm_candidates()
    # The explicit Web Research Architect binding is the routing truth. Role
    # models are ordered fallbacks and must not silently pre-empt it.
    plan_candidates = list(candidates)

    plan_system_prompt = (
        "你是 Web Research Architect 的证据架构师。只能使用 SOURCES 中的来源元数据和 evidenceCandidates，"
        "先把问题拆成可逐项核验的原子事实，再规划答案；不要在本阶段写长答案。"
        "每个 claim 必须从其 supporting source 选择一个 evidenceExcerptKey，禁止改写或拼接摘录。"
        "claimType 只能是 source_fact 或 explicit_normative：source_fact 只陈述摘录直接蕴含的事实；"
        "只有摘录明确出现推荐、要求或首选语义时才可使用 explicit_normative。"
        "复合建议只能写入 compositeInferences，由多项已验证前提推出，不得冒充任何单一来源或官方机构的直接推荐。"
        "不得把普通事实升级为最高优先级、基础性设计决策、独立类别、等价接口或无条件保证；这些关系也必须由前提明确支持。"
        "若 QUESTION 含多个 [facet-id]，claimTable 与 answerOutline 必须让每个 facet 至少有一条可追溯 claim；"
        "不得用总来源数或总字数掩盖漏答的 facet。"
        "若 QUESTION 另含 Deliverable requirements/交付要求，它们不是新搜索 facet，但 answerOutline 必须分配明确章节，"
        "并只用已验证 claims 与 compositeInferences 完成交付目标。"
        "用户询问 best practices/实践建议并要求引用官方来源，通常是要求以官方事实作为综合前提，不等于要求官方页面逐字写出最终建议；"
        "只有用户明确询问‘官方文档具体推荐/要求什么’时，才把缺少官方规范性措辞视为必要证据缺口。"
        "没有单篇资料覆盖完整组合问题本身不是证据缺口；只有必要原子前提缺失才可 retry。"
        "证据可来自任一已验证 SOURCES，不得因为某个事实来自另一官方页面、host 或同产品的不同文档页，就要求它必须在你臆定的特定页面重复出现；"
        "若 SOURCES 中已有来源携带所需事实或规范性措辞，该前提就不属于 missingEvidence。"
    )
    if named_decision_audiences:
        plan_system_prompt += (
            "QUESTION 已明确列出多个决策受众。每个受众都必须在 compositeInferences 中得到一条有实质内容的"
            "选型/行动结论，并在 answerOutline 中有明确落点；每条结论仍只能由同节的已验证 claims 推出。"
            "受众标签是交付结构，不是需要官方来源逐字写出的事实，也不得因此虚构产品能力。"
        )
    plan_system_prompt = _research_agent_stage_system_prompt(
        plan_system_prompt,
        stage="evidence_plan",
    )
    plan_prompts = [
        (
            "只输出严格 JSON，不要 Markdown。字段：reviewDecision, reviewReasons, headline, claimTable, "
            "answerOutline, compositeInferences, conflictMatrix, missingEvidence, criticalMissingEvidence, "
            "recommendedNextQueries, assumptions, temporalAssessment。reviewDecision 只能是 accept、retry、reject。"
            f"保持紧凑：reviewReasons 最多2项且每项不超过100字符；accept 时 claimTable 为 "
            f"{plan_claim_lower}-{plan_claim_upper} 项、"
            "区间前值是最低目标、后值只是上限；达到最低目标并覆盖问题后就停止，禁止把上限误读成必需数量或为上限凑 claim。"
            f"覆盖至少 {TARGET_RESEARCH_SOURCE_COUNT} 个不同 SOURCES；"
            f"必须为这些来源各写至少一个独立 claim：{', '.join(required_claim_sources)}。"
            "每项含唯一 claimId、claim、claimType、supportingSources、evidenceExcerptKey、confidence，claim 为20-180个有效字符；"
            "explicit_normative 还必须包含 normativeCue，且该短语必须逐字出现在所选 evidenceExcerptKey 的摘录中。"
            "answerOutline 必须是4-6个对象，每项仅含 sectionId、title、objective、claimIds；所有 claimId 必须恰好出现一次，"
            "并覆盖用户问题的子问题、冲突/限制、时效判断和可执行结论。"
            "compositeInferences 每项只含 inferenceId、inference、premiseClaimIds；inference 是20-360字符的明确综合结论，"
            "premiseClaimIds 只能引用 claimTable，且同一 inference 的全部 premise 必须位于同一个 outline section。"
            "若 QUESTION 明确列出多个决策受众，必须恰好为每个受众保留一条 inference；不要为同一受众输出多条重叠建议。"
            "建议 inference 的 premiseClaimIds 应彼此尽量不重叠；这些 premise claim 只能分配到该建议 section，"
            "不得在事实 section 和建议 section 重复出现同一 claimId。"
            "若少数子问题证据不足，但已有证据仍能实质回答核心问题，不得把整个计划标成 retry；"
            "删除不受支持的候选 claim，把未决子问题放入 missingEvidence，并在答案中明确限制。"
            "只有删除或限定不受支持的子结论后仍无法给出有用核心答案时，才使用 criticalMissingEvidence；"
            "此时 research answer 才不存在，并在最多4项 recommendedNextQueries 中精确说明缺失前提。\n"
            "recommendedNextQueries 必须直接写搜索关键词或 site: 查询，不得以 Fetch、Retrieve、Read、Open 或同义祈使词开头。\n"
            f"NAMED_DECISION_AUDIENCES: {json.dumps(named_decision_audiences, ensure_ascii=False)}\n"
            f"QUESTION: {question}"
        ),
        (
            "只根据 SOURCES 返回一个紧凑、完整、可解析的 JSON 对象。不要输出长答案。"
            f"accept 必须有 {plan_claim_lower}-{plan_claim_upper} 个紧凑原子 claim、"
            "区间后值只是上限而非必达数量；不得因无法填满上限而 retry/reject。"
            f"覆盖 {TARGET_RESEARCH_SOURCE_COUNT} 个不同来源，"
            f"且必须逐一覆盖 {', '.join(required_claim_sources)}；每项使用 supporting source 自己的 evidenceExcerptKey。"
            "answerOutline 使用4-6个 sectionId/title/objective/claimIds 对象并恰好覆盖全部 claimId。"
            "严格保持首个提示定义的字段名和 JSON schema；如果客户端习惯布尔 accept，可额外输出 accept，但仍应保留 reviewDecision。\n"
            f"NAMED_DECISION_AUDIENCES: {json.dumps(named_decision_audiences, ensure_ascii=False)}\n"
            f"QUESTION: {question}"
        ),
    ]

    def near_duplicate_claim(candidate_text: str, existing_text: str) -> bool:
        candidate_terms = {
            term
            for term in re.findall(r"[a-z0-9_]+|[\u4e00-\u9fff]", candidate_text.lower())
            if term not in _RELEVANCE_STOPWORDS
        }
        existing_terms = {
            term
            for term in re.findall(r"[a-z0-9_]+|[\u4e00-\u9fff]", existing_text.lower())
            if term not in _RELEVANCE_STOPWORDS
        }
        if not candidate_terms or not existing_terms:
            return False
        return len(candidate_terms.intersection(existing_terms)) / len(
            candidate_terms.union(existing_terms)
        ) >= 0.55

    def missing_verified_claim_facets(
        claims: list[dict[str, Any]],
    ) -> list[str]:
        covered_facet_ids = {
            _safe_text(facet_id)
            for claim in claims
            for support in list(claim.get("supportingSources") or [])
            if isinstance(support, dict)
            for facet_id in [
                *list(support.get("researchFacetIds") or []),
                support.get("researchFacetId"),
            ]
            if _safe_text(facet_id)
        }
        return [
            facet_id
            for facet_id in required_plan_facet_ids
            if facet_id not in covered_facet_ids
        ]

    def supplement_verified_claims(
        base_claims: list[dict[str, Any]],
    ) -> tuple[list[dict[str, Any]], list[str]]:
        supplemented = list(
            _architect_balance_base_claims_for_supplement(base_claims)
        )
        added_claim_ids: list[str] = []
        excluded_facts = {
            _normalized_evidence_text(claim.get("claim"))
            for claim in supplemented
            if _normalized_evidence_text(claim.get("claim"))
        }
        ordered_sources = _architect_supplement_source_order(
            compact_sources,
            required_claim_sources,
        )

        def practice_specificity(value: Any) -> int:
            text = _safe_text(value).lower()
            score = 0
            for pattern, weight in (
                (r"should use Path objects anywhere|use Path objects anywhere", 130),
                (r"\bpath_type\b", 100),
                (r"\btype\s*=\s*pathlib|passes?\s+pathlib\.path|pathlib\.path\s+as\s+(?:a\s+)?(?:type|converter)", 100),
                (r"\bfile_okay\b|\bdir_okay\b", 90),
                (r"\bget_app_dir\b|application directory|configuration directory", 85),
                (r"\bexists\b|\bresolve_path\b|\breadable\b|\bwritable\b", 65),
                (r"\bpathlike\b|\b__fspath__\b|\bos\.fspath\b", 60),
                (r"\bpurepath\b|\bpure paths?\b|\bconcrete paths?\b", 50),
                (r"\bpathlib\.path\b|\bpath objects?\b", 35),
            ):
                if re.search(pattern, text, re.IGNORECASE):
                    score += weight
            return score

        # The planner can spend both per-source slots on generic introduction
        # prose while a more actionable exact contract (for example argparse's
        # callable ``type`` plus ``pathlib.Path`` example) is already present in
        # the read evidence.  Replace only a weaker duplicate-source claim; do
        # not increase the finite claim budget or displace another source.
        for source in ordered_sources:
            source_citation = _safe_text(source.get("citationKey")).strip("[]")
            existing_indexes = [
                index
                for index, claim in enumerate(supplemented)
                if source_citation
                in {
                    _safe_text(
                        support.get("citationKey") or support.get("citation")
                        if isinstance(support, dict)
                        else support
                    ).strip("[]")
                    for support in list(claim.get("supportingSources") or [])
                }
            ]
            if not existing_indexes:
                continue
            preferred_raw = _architect_exact_excerpt_source_fact(
                source,
                question,
                excluded_facts=excluded_facts,
            )
            if not preferred_raw:
                continue
            preferred_text = _safe_text(preferred_raw.get("claim"))
            if any(
                near_duplicate_claim(preferred_text, _safe_text(supplemented[index].get("claim")))
                for index in existing_indexes
            ):
                continue
            preferred_verified, preferred_issues = _verify_architect_claim_excerpts(
                [preferred_raw],
                compact_sources,
                require_evidence_key=True,
            )
            if preferred_issues or len(preferred_verified) != 1:
                continue
            weakest_index = min(
                existing_indexes,
                key=lambda index: practice_specificity(supplemented[index].get("claim")),
            )
            if practice_specificity(preferred_text) <= practice_specificity(
                supplemented[weakest_index].get("claim")
            ) + 30:
                continue
            supplemented[weakest_index] = preferred_verified[0]
            excluded_facts.add(_normalized_evidence_text(preferred_text))
            added_claim_ids.append(_safe_text(preferred_verified[0].get("claimId")))

        def covered_citations() -> set[str]:
            return {
                _safe_text(
                    support.get("citationKey") or support.get("citation")
                    if isinstance(support, dict)
                    else support
                ).strip("[]")
                for claim in supplemented
                for support in list(claim.get("supportingSources") or [])
                if _safe_text(
                    support.get("citationKey") or support.get("citation")
                    if isinstance(support, dict)
                    else support
                ).strip("[]")
            }

        def add_exact_claim(
            source: dict[str, Any],
            *,
            required_facet_id: str | None = None,
            prefer_semantic_unique: bool = True,
        ) -> bool:
            source_citation = _safe_text(source.get("citationKey")).strip("[]")
            rejected_semantic_duplicates: set[str] = set()
            for _attempt in range(_RESEARCH_ARCHITECT_EVIDENCE_CANDIDATE_COUNT):
                supplemental_raw = _architect_exact_excerpt_source_fact(
                    source,
                    question,
                    excluded_facts=excluded_facts,
                    required_facet_id=required_facet_id,
                )
                if not supplemental_raw:
                    break
                supplemental_text = _safe_text(supplemental_raw.get("claim"))
                excluded_facts.add(_normalized_evidence_text(supplemental_text))
                existing_source_claims = [
                    claim
                    for claim in supplemented
                    if source_citation
                    in {
                        _safe_text(
                            support.get("citationKey") or support.get("citation")
                            if isinstance(support, dict)
                            else support
                        ).strip("[]")
                        for support in list(claim.get("supportingSources") or [])
                    }
                ]
                if any(
                    near_duplicate_claim(
                        supplemental_text,
                        _safe_text(claim.get("claim")),
                    )
                    for claim in existing_source_claims
                ):
                    continue
                if prefer_semantic_unique and any(
                    near_duplicate_claim(
                        supplemental_text,
                        _safe_text(claim.get("claim")),
                    )
                    for claim in supplemented
                ):
                    rejected_semantic_duplicates.add(
                        _normalized_evidence_text(supplemental_text)
                    )
                    continue
                supplemental_verified, supplemental_issues = (
                    _verify_architect_claim_excerpts(
                        [supplemental_raw],
                        compact_sources,
                        require_evidence_key=True,
                    )
                )
                if supplemental_issues or len(supplemental_verified) != 1:
                    continue
                supplemented.extend(supplemental_verified)
                added_claim_ids.append(
                    _safe_text(supplemental_verified[0].get("claimId"))
                )
                return True
            # A source-coverage pass may retry without the uniqueness
            # preference. Do not permanently hide candidates rejected only
            # because another document expressed a semantically similar fact.
            excluded_facts.difference_update(rejected_semantic_duplicates)
            return False

        def claim_facet_ids(claim: dict[str, Any]) -> set[str]:
            return {
                _safe_text(facet_id)
                for support in list(claim.get("supportingSources") or [])
                if isinstance(support, dict)
                for facet_id in [
                    *list(support.get("researchFacetIds") or []),
                    support.get("researchFacetId"),
                ]
                if _safe_text(facet_id)
            }

        def claim_citation_keys(claim: dict[str, Any]) -> set[str]:
            return {
                _safe_text(
                    support.get("citationKey")
                    or support.get("citation")
                    or support.get("sourceId")
                    if isinstance(support, dict)
                    else support
                ).strip("[]")
                for support in list(claim.get("supportingSources") or [])
                if _safe_text(
                    support.get("citationKey")
                    or support.get("citation")
                    or support.get("sourceId")
                    if isinstance(support, dict)
                    else support
                ).strip("[]")
            }

        def make_room_for_missing_facet() -> tuple[int, dict[str, Any]] | None:
            if len(supplemented) < _RESEARCH_ARCHITECT_IDEAL_CLAIM_COUNT:
                return None
            facet_counts: dict[str, int] = {}
            citation_counts: dict[str, int] = {}
            for claim in supplemented:
                for facet_id in claim_facet_ids(claim):
                    facet_counts[facet_id] = facet_counts.get(facet_id, 0) + 1
                for citation_key in claim_citation_keys(claim):
                    citation_counts[citation_key] = citation_counts.get(citation_key, 0) + 1
            required_facets = set(required_plan_facet_ids)
            required_citations = set(required_claim_sources)
            replaceable: list[tuple[tuple[int, int, int, int], int]] = []
            for index, claim in enumerate(supplemented):
                facets = claim_facet_ids(claim)
                citations = claim_citation_keys(claim)
                if any(
                    facet_id in required_facets
                    and facet_counts.get(facet_id, 0) <= 1
                    for facet_id in facets
                ):
                    continue
                if any(
                    citation_key in required_citations
                    and citation_counts.get(citation_key, 0) <= 1
                    for citation_key in citations
                ):
                    continue
                roles = {
                    _architect_support_role(support)
                    for support in list(claim.get("supportingSources") or [])
                    if isinstance(support, dict)
                }
                replaceable.append(
                    (
                        (
                            1 if not facets.intersection(required_facets) else 0,
                            1 if not citations.intersection(required_citations) else 0,
                            1 if "secondary" in roles else 0,
                            -index,
                        ),
                        index,
                    )
                )
            if not replaceable:
                return None
            _rank, index = max(replaceable)
            return index, supplemented.pop(index)

        # Close every substantive answer facet before spending the finite claim
        # budget on extra document depth. If the model already filled the array,
        # replace only a redundant/non-required claim and restore it on failure.
        for facet_id in missing_verified_claim_facets(supplemented):
            facet_sources = [
                source
                for source in ordered_sources
                if any(
                    _safe_text(candidate.get("researchFacetId")) == facet_id
                    for candidate in list(source.get("evidenceCandidates") or [])
                    if isinstance(candidate, dict)
                )
            ]
            removed: tuple[int, dict[str, Any]] | None = None
            if len(supplemented) >= _RESEARCH_ARCHITECT_IDEAL_CLAIM_COUNT:
                removed = make_room_for_missing_facet()
                # The 12-claim ideal is a planning target, not a reason to
                # discard an explicit user facet. Use bounded repair headroom
                # up to the 16-claim verification ceiling when every existing
                # claim protects a required source or facet.
                if (
                    removed is None
                    and len(supplemented) >= _RESEARCH_ARCHITECT_MAX_CLAIM_COUNT
                ):
                    continue
            added = any(
                add_exact_claim(
                    source,
                    required_facet_id=facet_id,
                    prefer_semantic_unique=False,
                )
                for source in facet_sources
            )
            if not added and removed is not None:
                supplemented.insert(removed[0], removed[1])

        # Then close document coverage. Each generated fact is rebound to the
        # stored excerpt and passes the same strict claim validator.
        while len(supplemented) < _RESEARCH_ARCHITECT_IDEAL_CLAIM_COUNT:
            covered = covered_citations()
            uncovered_sources = [
                source
                for source in ordered_sources
                if _safe_text(source.get("citationKey")).strip("[]") not in covered
            ]
            if not uncovered_sources:
                break
            made_progress = False
            for source in uncovered_sources:
                if len(supplemented) >= _RESEARCH_ARCHITECT_IDEAL_CLAIM_COUNT:
                    break
                if add_exact_claim(source) or add_exact_claim(
                    source,
                    prefer_semantic_unique=False,
                ):
                    made_progress = True
            if not made_progress:
                break

        # Spend any remaining capacity on distinct verified facts for depth.
        while len(supplemented) < _RESEARCH_ARCHITECT_IDEAL_CLAIM_COUNT:
            made_progress = False
            for source in ordered_sources:
                if len(supplemented) >= _RESEARCH_ARCHITECT_IDEAL_CLAIM_COUNT:
                    break
                if add_exact_claim(source):
                    made_progress = True
            if not made_progress:
                break
        return supplemented, added_claim_ids

    plan: dict[str, Any] | None = None
    plan_candidate: tuple[Any, str, str] | None = None
    verified_claims: list[dict[str, Any]] = []
    best_repair_plan: dict[str, Any] | None = None
    best_repair_claims: list[dict[str, Any]] = []
    best_repair_missing_citations: list[str] = []
    best_repair_missing_facet_ids: list[str] = []
    best_repair_candidate: tuple[Any, str, str] | None = None
    best_repair_score = (-1, -1, -1)
    runtime_missing_facet_ids: list[str] = []
    audience_repair_prompt_added = False
    for candidate_index, candidate in enumerate(plan_candidates):
        model_label = _architect_candidate_identity(candidate) or candidate[1] or f"role:{candidate[2]}"
        candidate_repair_claims: list[dict[str, Any]] = []
        candidate_repair_inferences: list[dict[str, Any]] = []
        candidate_repair_allowed_citations: set[str] = set()
        candidate_repair_materials: list[dict[str, str]] = []
        candidate_delta_claim_ids: list[str] = []
        candidate_supplemental_claim_ids: list[str] = []
        remaining_candidates = max(1, len(plan_candidates) - candidate_index)
        candidate_total_budget = min(
            90.0 if named_decision_audiences else 60.0,
            max(2.0, (remaining_seconds() - delivery_reserve) / remaining_candidates),
        )
        candidate_started_at = time.perf_counter()
        for prompt_index, prompt in enumerate(plan_prompts):
            remaining = remaining_seconds()
            if remaining <= delivery_reserve + 1:
                candidate_errors.append("architect_delivery_budget_reserved")
                break
            candidate_remaining = candidate_total_budget - (time.perf_counter() - candidate_started_at)
            minimum_repair_seconds = min(
                (
                    min(15.0, _RESEARCH_ARCHITECT_MIN_PLAN_REPAIR_SECONDS)
                    if named_decision_audiences
                    else _RESEARCH_ARCHITECT_MIN_PLAN_REPAIR_SECONDS
                ),
                max(2.0, candidate_total_budget * 0.5),
            )
            if (
                prompt_index > 0
                and candidate_remaining < minimum_repair_seconds
            ):
                plan_attempts.append(
                    {
                        "modelId": model_label,
                        "modelRole": candidate[2],
                        "selectionOrigin": _architect_candidate_selection_origin(candidate),
                        "attempt": prompt_index + 1,
                        "allocatedTimeoutMs": int(max(0.0, candidate_remaining) * 1000),
                        "remainingMsAtStart": int(max(0.0, remaining) * 1000),
                        "status": "skipped_insufficient_budget",
                    }
                )
                candidate_errors.append(f"{model_label}: architect_plan_repair_budget_insufficient")
                break
            per_call_budget = min(call_timeout_cap_seconds, candidate_remaining)
            messages = prepared_messages(
                candidate,
                system_prompt=plan_system_prompt,
                instruction=prompt,
                materials=(
                    candidate_repair_materials
                    if prompt_index > 0 and candidate_repair_materials
                    else [
                        {
                            "title": "Research evidence candidates",
                            "kind": "research_sources",
                            "content": source_material,
                        }
                    ]
                ),
                target_role="web-research-architect",
                node="web_research_architect_plan",
            )
            attempt_started_at = time.perf_counter()
            plan_attempt = {
                "modelId": model_label,
                "modelRole": candidate[2],
                "selectionOrigin": _architect_candidate_selection_origin(candidate),
                "attempt": prompt_index + 1,
                "allocatedTimeoutMs": int(max(0.0, per_call_budget) * 1000),
                "remainingMsAtStart": int(max(0.0, remaining) * 1000),
            }
            try:
                response = _invoke_architect_candidate_with_deadline(
                    candidate,
                    messages,
                    seconds=per_call_budget,
                    max_tokens=_RESEARCH_ARCHITECT_PLAN_MAX_TOKENS,
                    disable_thinking=True,
                )
            except concurrent.futures.TimeoutError:
                plan_attempt.update(
                    {
                        "status": "deadline_timeout",
                        "elapsedMs": int((time.perf_counter() - attempt_started_at) * 1000),
                    }
                )
                plan_attempts.append(plan_attempt)
                candidate_errors.append(f"{model_label}: architect_plan_timeout")
                if prompt_index == 0 and len(plan_prompts) > 1:
                    candidate_repair_materials = [
                        {
                            "title": "Facet-focused evidence candidates",
                            "kind": "research_sources",
                            "content": compact_retry_source_material,
                        }
                    ]
                    continue
                break
            except Exception as exc:  # noqa: BLE001 - try the next configured Architect model.
                plan_attempt.update(
                    {
                        "status": "provider_error",
                        "elapsedMs": int((time.perf_counter() - attempt_started_at) * 1000),
                        "failureCode": type(exc).__name__,
                    }
                )
                plan_attempts.append(plan_attempt)
                candidate_errors.append(f"{model_label}: architect_plan_{type(exc).__name__}: {_safe_text(exc)[:220]}")
                break
            sanitized_output = sanitize_background_model_output(response)
            raw_content = sanitized_output.text
            finish_reason = _architect_response_finish_reason(response)
            plan_attempt.update(
                {
                    "status": "completed",
                    "elapsedMs": int((time.perf_counter() - attempt_started_at) * 1000),
                    "responseChars": len(raw_content),
                    "finishReason": finish_reason,
                    "requestedMaxTokens": _RESEARCH_ARCHITECT_PLAN_MAX_TOKENS,
                    "configuredMaxTokens": _architect_segmented_writer_profile(candidate).get(
                        "configuredMaxTokens"
                    ),
                    **_architect_response_safe_diagnostics(response, sanitized_output),
                }
            )
            plan_attempts.append(plan_attempt)
            if raw_content:
                raw_previews.append(f"plan:{model_label}: {raw_content[:400]}")
            if not raw_content and finish_reason in {"length", "max_tokens", "max_output_tokens"}:
                output_failure = (
                    "reasoning_budget_exhausted"
                    if int(plan_attempt.get("reasoningChars") or 0) > 0
                    or int(plan_attempt.get("reasoningTokens") or 0) > 0
                    else "empty_visible_output_at_length"
                )
                plan_attempt["outputFailureClass"] = output_failure
                candidate_errors.append(f"{model_label}: architect_plan_{output_failure}")
                break
            parsed = _extract_json_object(raw_content)
            if not isinstance(parsed, dict):
                if prompt_index == len(plan_prompts) - 1:
                    candidate_errors.append(f"{model_label}: architect_plan_no_json")
                continue
            decision = _architect_plan_decision(
                parsed,
                allow_runtime_validation_candidate=prompt_index > 0,
            )
            if (
                not decision
                and prompt_index > 0
                and candidate_repair_claims
                and isinstance(parsed.get("answerOutline"), list)
            ):
                # A repair response may intentionally omit the locked claim
                # table.  Its outline/inference bindings still face all normal
                # deterministic gates below.
                decision = "accept"
            if decision not in {"accept", "retry", "reject"}:
                candidate_errors.append(
                    f"{model_label}: architect_plan_invalid_decision:{_safe_text(parsed.get('reviewDecision'))[:40]}"
                )
                continue
            parsed["reviewDecision"] = decision
            parsed["_modelRole"] = candidate[2]
            parsed["_modelId"] = model_label
            parsed["_modelParseMode"] = "staged_json_markdown"
            parsed["_architectMode"] = "full_synthesis"
            if decision != "accept":
                original_decision = decision
                reclassified_plan = _architect_reclassify_review_evidence_gaps(
                    parsed,
                    question=question,
                )
                reclassified_issues = _research_text_list(
                    reclassified_plan.get("reclassifiedSameEvidenceIssues"),
                    limit=12,
                )
                remaining_critical = _research_text_list(
                    reclassified_plan.get("criticalMissingEvidence"),
                    limit=12,
                )
                remaining_queries = _research_text_list(
                    reclassified_plan.get("recommendedNextQueries"),
                    limit=4,
                )
                retry_without_evidence_gap = bool(
                    original_decision == "retry"
                    and not remaining_critical
                    and not remaining_queries
                )
                partial_plan_evidence_ready = bool(
                    original_decision == "retry"
                    and _architect_partial_plan_evidence_ready(
                        parsed,
                        required_claim_sources=required_claim_sources,
                        required_facet_ids=required_plan_facet_ids,
                        prompt_sources=prompt_sources,
                    )
                )
                if (
                    (reclassified_issues or retry_without_evidence_gap)
                    and not remaining_critical
                    and not remaining_queries
                ):
                    parsed = reclassified_plan
                    decision = "accept"
                    parsed["reviewDecision"] = "accept"
                    parsed["_originalPlanDecision"] = original_decision
                    parsed["_reclassifiedPlanEvidenceGaps"] = reclassified_issues
                    if retry_without_evidence_gap and not reclassified_issues:
                        parsed["_planDecisionRepair"] = "retry_without_evidence_gap"
                        candidate_errors.append(
                            f"{model_label}: architect_plan_retry_without_evidence_gap"
                        )
                    else:
                        candidate_errors.append(
                            f"{model_label}: architect_plan_same_evidence_gap_reclassified:"
                            f"{len(reclassified_issues)}"
                        )
                elif partial_plan_evidence_ready:
                    non_blocking_gaps = list(
                        dict.fromkeys(
                            [
                                *_research_text_list(
                                    reclassified_plan.get("missingEvidence"),
                                    limit=12,
                                ),
                                *reclassified_issues,
                                *remaining_critical,
                            ]
                        )
                    )[:12]
                    parsed = {
                        **reclassified_plan,
                        "reviewDecision": "accept",
                        "missingEvidence": non_blocking_gaps,
                        "criticalMissingEvidence": [],
                        "recommendedNextQueries": [],
                        "_originalPlanDecision": original_decision,
                        "_planDecisionRepair": "partial_evidence_answerable",
                        "_nonBlockingPlanEvidenceGaps": non_blocking_gaps,
                        "_deferredPlanQueries": remaining_queries,
                    }
                    decision = "accept"
                    candidate_errors.append(
                        f"{model_label}: architect_plan_partial_evidence_answerable:"
                        f"{len(non_blocking_gaps)}"
                    )
                else:
                    parsed = reclassified_plan
                    if prompt_index < len(plan_prompts) - 1:
                        # A capable model can occasionally misread a bounded
                        # claim range as an exact quota and refuse before
                        # emitting any verifiable plan. Use the already-budgeted
                        # compact retry once; only the final attempt may turn a
                        # genuine evidence gap into a terminal plan decision.
                        candidate_errors.append(
                            f"{model_label}: architect_plan_nonaccept_retry:{original_decision}"
                        )
                        candidate_repair_materials = [
                            {
                                "title": "Facet-focused evidence candidates",
                                "kind": "research_sources",
                                "content": compact_retry_source_material,
                            }
                        ]
                        continue
                    parsed["researchResult"] = ""
                    parsed["answer"] = ""
                    parsed["claimTable"] = []
                    parsed["criticalMissingEvidence"] = remaining_critical
                    parsed["recommendedNextQueries"] = remaining_queries
                    parsed["_modelFallbackAttempts"] = candidate_errors
                    parsed["_planAttempts"] = plan_attempts[-6:]
                    return parsed
            raw_claims = _research_dict_list(
                parsed.get("claimTable"),
                limit=plan_claim_upper,
            )
            if prompt_index > 0 and candidate_repair_claims:
                candidate_claims = list(candidate_repair_claims)
                excerpt_issues: list[str] = []
                locked_claim_ids = {
                    _safe_text(claim.get("claimId"))
                    for claim in candidate_claims
                    if _safe_text(claim.get("claimId"))
                }
                locked_claim_texts = {
                    _normalized_evidence_text(claim.get("claim"))
                    for claim in candidate_claims
                    if _normalized_evidence_text(claim.get("claim"))
                }
                raw_delta_claims = _research_dict_list(
                    parsed.get("claimDelta") or parsed.get("claimTable"),
                    limit=plan_claim_upper,
                )
                raw_delta_claims = [
                    claim
                    for claim in raw_delta_claims
                    if _safe_text(claim.get("claimId")) not in locked_claim_ids
                ][:3]
                if raw_delta_claims and candidate_repair_allowed_citations:
                    verified_delta_claims, delta_issues = (
                        _verify_architect_claim_excerpts(
                            raw_delta_claims,
                            compact_sources,
                            require_evidence_key=True,
                        )
                    )
                    excerpt_issues.extend(
                        f"repair_delta_{issue}" for issue in delta_issues
                    )
                    for delta_claim in verified_delta_claims:
                        delta_citations = {
                            _safe_text(
                                support.get("citationKey")
                                if isinstance(support, dict)
                                else support
                            ).strip("[]")
                            for support in list(
                                delta_claim.get("supportingSources") or []
                            )
                            if _safe_text(
                                support.get("citationKey")
                                if isinstance(support, dict)
                                else support
                            ).strip("[]")
                        }
                        normalized_delta_claim = _normalized_evidence_text(
                            delta_claim.get("claim")
                        )
                        if not delta_citations.intersection(
                            candidate_repair_allowed_citations
                        ):
                            excerpt_issues.append(
                                "repair_delta_source_outside_missing_evidence"
                            )
                            continue
                        if (
                            _safe_text(delta_claim.get("claimId"))
                            in locked_claim_ids
                            or normalized_delta_claim in locked_claim_texts
                        ):
                            continue
                        candidate_claims.append(delta_claim)
                        delta_claim_id = _safe_text(
                            delta_claim.get("claimId")
                        )
                        locked_claim_ids.add(delta_claim_id)
                        locked_claim_texts.add(normalized_delta_claim)
                        candidate_delta_claim_ids.append(delta_claim_id)
                candidate_claims, delta_supplemental_ids = (
                    supplement_verified_claims(candidate_claims)
                )
                candidate_claims, delta_secondary_claim_ids = (
                    _architect_govern_claim_source_roles_for_question(
                        candidate_claims,
                        question,
                    )
                )
                if delta_secondary_claim_ids:
                    parsed["_attributedSecondaryClaimIds"] = (
                        delta_secondary_claim_ids
                    )
                candidate_supplemental_claim_ids.extend(
                    claim_id
                    for claim_id in delta_supplemental_ids
                    if claim_id not in candidate_supplemental_claim_ids
                )
            else:
                candidate_claims, excerpt_issues = _verify_architect_claim_excerpts(
                    raw_claims,
                    compact_sources,
                    require_evidence_key=True,
                )
                candidate_claims, candidate_supplemental_claim_ids = supplement_verified_claims(
                    candidate_claims
                )
                dropped_unfocused_claim_ids = [
                    _safe_text(claim.get("claimId")) or "unknown"
                    for claim in candidate_claims
                    if list(claim.get("supportingSources") or [])
                    and all(
                        isinstance(support, dict)
                        and support.get("subjectFocused") is False
                        for support in list(claim.get("supportingSources") or [])
                    )
                ]
                if dropped_unfocused_claim_ids:
                    candidate_claims = [
                        claim
                        for claim in candidate_claims
                        if (_safe_text(claim.get("claimId")) or "unknown")
                        not in set(dropped_unfocused_claim_ids)
                    ]
                    parsed["_droppedUnfocusedClaimIds"] = dropped_unfocused_claim_ids
                    candidate_errors.append(
                        f"{model_label}: unfocused_claims_excluded:"
                        + ",".join(dropped_unfocused_claim_ids)
                    )
                candidate_claims, attributed_secondary_claim_ids = (
                    _architect_govern_claim_source_roles_for_question(
                        candidate_claims,
                        question,
                    )
                )
                if attributed_secondary_claim_ids:
                    parsed["_attributedSecondaryClaimIds"] = attributed_secondary_claim_ids
                    # The runtime has already converted these claims into an
                    # explicitly attributed secondary-source form.  Keep the
                    # audit trail, but do not reject an otherwise complete
                    # evidence plan merely because the model omitted the
                    # sourceRole label on its first pass.
                    parsed.setdefault("_nonBlockingPlanWarnings", []).append(
                        "secondary_claims_attributed:"
                        + ",".join(attributed_secondary_claim_ids)
                    )
            # Each repair starts from the newest Runtime-verified claim set.
            # Otherwise a valid claimDelta disappears when a later inference-
            # only repair is needed.
            candidate_repair_claims = list(candidate_claims)
            parsed["claimTable"] = candidate_claims
            if (
                candidate_supplemental_claim_ids
                or len(candidate_claims) != len(raw_claims)
                or not _architect_outline_claim_groups(parsed.get("answerOutline"), candidate_claims)
            ):
                parsed["answerOutline"] = _architect_normalized_repair_outline(
                    parsed.get("answerOutline"),
                    candidate_claims,
                    parsed.get("compositeInferences"),
                )
            plan_metrics = research_acceptance_metrics(
                {
                    "question": question,
                    "freshness": freshness,
                    "reviewDecision": "accept",
                    "answer": "",
                    "sourceUrls": compact_sources,
                    "claimTable": candidate_claims,
                    "criticalMissingEvidence": parsed.get("criticalMissingEvidence") or [],
                    "asOf": _utc_now_iso(),
                }
            )
            plan_issues = list(excerpt_issues)
            if plan_metrics["claimCount"] < TARGET_RESEARCH_CLAIM_COUNT:
                plan_issues.append(f"claim_target_not_met:{TARGET_RESEARCH_CLAIM_COUNT}")
            if plan_metrics["uniqueClaimCount"] < TARGET_RESEARCH_CLAIM_COUNT:
                plan_issues.append(f"distinct_claim_target_not_met:{TARGET_RESEARCH_CLAIM_COUNT}")
            if plan_metrics["evidenceVerifiedClaimCount"] != plan_metrics["claimCount"]:
                plan_issues.append("unverified_claim_excerpt_present")
            missing_citations: list[str] = []
            if plan_metrics["claimSupportedSourceCount"] < TARGET_RESEARCH_SOURCE_COUNT:
                covered_citations = {
                    _safe_text(support.get("citationKey")).strip("[]")
                    for claim in candidate_claims
                    for support in list(claim.get("supportingSources") or [])
                    if isinstance(support, dict) and _safe_text(support.get("citationKey"))
                }
                missing_citations = [
                    citation
                    for citation in required_claim_sources
                    if citation not in covered_citations
                ]
                source_coverage_gap = (
                    "claim_source_coverage_target_not_met:"
                    f"{plan_metrics['claimSupportedSourceCount']}/{TARGET_RESEARCH_SOURCE_COUNT}"
                    + (f":missing={','.join(missing_citations)}" if missing_citations else "")
                )
                if plan_metrics["claimSupportedSourceCount"] < plan_claim_source_floor:
                    plan_issues.append(source_coverage_gap)
                else:
                    # The target remains visible without turning one less
                    # claim-bearing source into an all-answer veto.
                    parsed["_softClaimSourceCoverageGap"] = source_coverage_gap
            missing_claim_facet_ids = missing_verified_claim_facets(
                candidate_claims
            )
            if missing_claim_facet_ids:
                plan_issues.append(
                    "claim_facet_coverage_target_not_met:missing="
                    + ",".join(missing_claim_facet_ids)
                )
            critical_missing_evidence = _research_text_list(
                parsed.get("criticalMissingEvidence"),
                limit=12,
            )
            if critical_missing_evidence:
                plan_issues.append("critical_evidence_gap")
            proposed_inferences = _research_dict_list(
                parsed.get("compositeInferences"),
                limit=12,
            )
            if candidate_repair_inferences:
                locked_audiences = {
                    audience
                    for audience in named_decision_audiences
                    if any(
                        audience.casefold()
                        in _safe_text(
                            inference.get("audience")
                            or inference.get("inference")
                            or inference.get("conclusion")
                            or inference.get("recommendation")
                        ).casefold()
                        for inference in candidate_repair_inferences
                    )
                }
                proposed_inferences = [
                    inference
                    for inference in proposed_inferences
                    if not any(
                        audience.casefold()
                        in _safe_text(
                            inference.get("audience")
                            or inference.get("inference")
                            or inference.get("conclusion")
                            or inference.get("recommendation")
                        ).casefold()
                        for audience in locked_audiences
                    )
                ]
            normalized_inferences = _research_normalize_named_decision_inferences(
                [*candidate_repair_inferences, *proposed_inferences],
                named_decision_audiences,
            )
            parsed["compositeInferences"] = normalized_inferences
            if candidate_repair_inferences:
                parsed["answerOutline"] = _architect_normalized_repair_outline(
                    parsed.get("answerOutline"),
                    candidate_claims,
                    normalized_inferences,
                )
            filtered_inferences, dropped_inferences = _architect_filter_composite_inferences(
                parsed.get("answerOutline"),
                candidate_claims,
                normalized_inferences,
                validate_semantics=False,
            )
            parsed["compositeInferences"] = filtered_inferences
            if len(filtered_inferences) > len(candidate_repair_inferences):
                candidate_repair_inferences = list(filtered_inferences)
            if dropped_inferences:
                parsed["_droppedCompositeInferenceIssues"] = dropped_inferences
            has_primary_explicit_normative_claim = any(
                _safe_text(claim.get("claimType")).lower() == "explicit_normative"
                and any(
                    _architect_support_role(support) == "primary"
                    for support in list(claim.get("supportingSources") or [])
                    if isinstance(support, dict)
                )
                for claim in candidate_claims
            )
            required_inference_count = (
                len(named_decision_audiences)
                if named_decision_audiences
                else (
                    1
                    if question_requires_synthesis
                    and not has_primary_explicit_normative_claim
                    else 0
                )
            )
            if len(filtered_inferences) < required_inference_count:
                parsed.setdefault("_nonBlockingPlanWarnings", []).append(
                    "composite_inference_recommended_for_audiences:"
                    f"{len(filtered_inferences)}/{required_inference_count}"
                    + (
                        ":dropped=" + ",".join(dropped_inferences[:3])
                        if dropped_inferences
                        else ""
                    )
                )
            plan_issues.extend(
                _architect_outline_inference_issues(
                    parsed.get("answerOutline"),
                    candidate_claims,
                    filtered_inferences,
                )
            )
            repair_score = (
                int(plan_metrics.get("claimSupportedSourceCount") or 0),
                len(required_plan_facet_ids) - len(missing_claim_facet_ids),
                int(plan_metrics.get("evidenceVerifiedClaimCount") or 0),
            )
            repair_gap_count = len(
                set([*missing_citations, *missing_claim_facet_ids])
            )
            repair_candidate_eligible = bool(
                repair_gap_count <= 3
                and len(candidate_claims)
                >= TARGET_RESEARCH_CLAIM_COUNT - repair_gap_count
                and int(plan_metrics.get("uniqueClaimCount") or 0)
                >= TARGET_RESEARCH_CLAIM_COUNT - repair_gap_count
                and int(plan_metrics.get("evidenceVerifiedClaimCount") or 0)
                == int(plan_metrics.get("claimCount") or 0)
                and not critical_missing_evidence
            )
            # Rank only candidates that the deterministic repair path can
            # actually finish. Prefer the newer candidate on an equal score so
            # a corrected retry is not shadowed by its stale first draft.
            if repair_candidate_eligible and repair_score >= best_repair_score:
                best_repair_score = repair_score
                best_repair_plan = dict(parsed)
                best_repair_claims = list(candidate_claims)
                best_repair_missing_citations = list(missing_citations)
                best_repair_missing_facet_ids = list(
                    missing_claim_facet_ids
                )
                best_repair_candidate = candidate
            if plan_issues:
                candidate_errors.append(f"{model_label}: " + ",".join(plan_issues[:8]))
                if prompt_index == 0:
                    candidate_repair_allowed_citations = set(
                        missing_citations
                    )
                    for source in compact_sources:
                        source_facets = {
                            _safe_text(value)
                            for value in [
                                *list(source.get("researchFacetIds") or []),
                                source.get("researchFacetId"),
                            ]
                            if _safe_text(value)
                        }
                        if source_facets.intersection(
                            missing_claim_facet_ids
                        ):
                            citation_key = _safe_text(
                                source.get("citationKey")
                            ).strip("[]")
                            if citation_key:
                                candidate_repair_allowed_citations.add(
                                    citation_key
                                )
                    repair_claim_material = [
                        {
                            "claimId": _safe_text(claim.get("claimId")),
                            "claim": _safe_text(claim.get("claim")),
                            "claimType": _safe_text(claim.get("claimType")),
                            "sourceRole": _safe_text(claim.get("sourceRole")),
                            "primarySupported": any(
                                _architect_support_role(support) == "primary"
                                for support in list(claim.get("supportingSources") or [])
                                if isinstance(support, dict)
                            ),
                            "citations": [
                                _safe_text(
                                    support.get("citationKey") or support.get("citation")
                                    if isinstance(support, dict)
                                    else support
                                ).strip("[]")
                                for support in list(claim.get("supportingSources") or [])
                            ],
                            "supportingSources": [
                                {
                                    "citationKey": _safe_text(
                                        support.get("citationKey")
                                        or support.get("citation")
                                    ).strip("[]"),
                                    "title": _safe_text(support.get("title")),
                                    "sourceRole": _architect_support_role(support),
                                    "updatedAt": _safe_text(
                                        support.get("updatedAt")
                                        or support.get("sourceDate")
                                        or support.get("retrievedAt")
                                    ),
                                }
                                for support in list(claim.get("supportingSources") or [])
                                if isinstance(support, dict)
                            ],
                        }
                        for claim in candidate_claims
                    ]
                    repair_sources = [
                        source
                        for source in prompt_sources
                        if _safe_text(source.get("citationKey")).strip("[]")
                        in candidate_repair_allowed_citations
                    ]
                    candidate_repair_materials = [
                        {
                            "title": "Runtime-verified locked claims",
                            "kind": "research_verified_claims",
                            "content": json.dumps(
                                repair_claim_material,
                                ensure_ascii=False,
                            ),
                        }
                    ]
                    if repair_sources:
                        candidate_repair_materials.append(
                            {
                                "title": "Missing evidence candidates",
                                "kind": "research_sources",
                                "content": json.dumps(
                                    repair_sources,
                                    ensure_ascii=False,
                                ),
                            }
                        )
                    plan_prompts[1] = (
                        "修正上一份计划，只输出一个短小严格 JSON。RUNTIME_VERIFIED_CLAIMS 已由 Runtime 锁定，"
                        "不得重写或删减。只返回字段 reviewDecision, reviewReasons, headline, claimDelta, "
                        "answerOutline, compositeInferences, conflictMatrix, missingEvidence, "
                        "criticalMissingEvidence, recommendedNextQueries, assumptions, temporalAssessment。"
                        f"上一份计划未通过 Runtime 门禁：{', '.join(plan_issues[:8])}。"
                        "claimDelta 只能为缺失证据新增0-3条 claim，禁止重复或替换锁定 claim；每条仍须包含唯一 "
                        "claimId、claim、claimType、supportingSources、evidenceExcerptKey、confidence，并且只能引用 "
                        f"MISSING_EVIDENCE_CITATIONS 中的来源：{', '.join(sorted(candidate_repair_allowed_citations)) or 'none'}。"
                        "本轮若已完成修正，reviewDecision 必须精确写 accept，不得写 revise 或 revised。"
                        "answerOutline 必须是4-6个 sectionId/title/objective/claimIds 对象，全部锁定 claimId 与新增 "
                        "claimDelta 的 claimId 都恰好出现一次；"
                        "每个 compositeInference 只含 inferenceId、inference、premiseClaimIds，premiseClaimIds 必须全部"
                        "落在同一 section；综合结论只可由列出的 claim 直接推出，禁止新增 API、版本、数量、性能、"
                        "跨平台、替代、优先级、最高杠杆、基础性决策、独立类别、等价接口或无条件保证。"
                        "若 QUESTION 明确列出多个决策受众，修复稿必须恰好保留每个受众一条 inference；不要复制 claimId 到多个 section，"
                        "把每条 inference 的 premise claim 只放进承载该 inference 的建议 section。"
                        "若问题询问建议，且没有 primarySupported=true 的 explicit_normative claim（包括只有二手"
                        " explicit_normative 的情况），必须提供1-3个有实质内容的 compositeInference；其 premises "
                        "优先使用 primarySupported=true 的 source_fact。二手经验继续保留为已归属的教程/作者建议，"
                        "但不得成为通用 API 保证或官方结论的唯一前提。accept 时 conflictMatrix、missingEvidence、criticalMissingEvidence、"
                        "recommendedNextQueries、assumptions 均返回空数组。不要输出 researchResult，不要改字段名，"
                        "整个响应尽量控制在1200 tokens 内。\n"
                        + (
                            f"必须为这些明确受众各保留一条 compositeInference（共 {len(named_decision_audiences)} 条）："
                            + json.dumps(named_decision_audiences, ensure_ascii=False)
                            + "。受众建议是跨来源综合结论，不得伪装为官方逐字建议。\n"
                            if named_decision_audiences
                            else ""
                        )
                        + f"QUESTION: {question}\nMISSING_EVIDENCE_CITATIONS: "
                        + json.dumps(
                            sorted(candidate_repair_allowed_citations),
                            ensure_ascii=False,
                        )
                        + "\nRUNTIME_VERIFIED_CLAIMS: "
                        + json.dumps(repair_claim_material, ensure_ascii=False)
                    )
                if (
                    named_decision_audiences
                    and prompt_index == len(plan_prompts) - 1
                    and not audience_repair_prompt_added
                ):
                    locked_inference_material = [
                        {
                            "inferenceId": _safe_text(inference.get("inferenceId")),
                            "inference": _safe_text(inference.get("inference")),
                            "premiseClaimIds": list(
                                inference.get("premiseClaimIds") or []
                            ),
                        }
                        for inference in candidate_repair_inferences
                    ]
                    candidate_repair_materials = [
                        material
                        for material in candidate_repair_materials
                        if material.get("kind") != "research_verified_inferences"
                    ]
                    if locked_inference_material:
                        candidate_repair_materials.append(
                            {
                                "title": "Runtime-verified locked inferences",
                                "kind": "research_verified_inferences",
                                "content": json.dumps(
                                    locked_inference_material,
                                    ensure_ascii=False,
                                ),
                            }
                        )
                    locked_inference_text = " ".join(
                        _safe_text(inference.get("inference"))
                        for inference in candidate_repair_inferences
                    ).casefold()
                    missing_decision_audiences = [
                        audience
                        for audience in named_decision_audiences
                        if audience.casefold() not in locked_inference_text
                    ]
                    plan_prompts.append(
                        plan_prompts[1]
                        + "\n这是受众结构的最后一次短修复。RUNTIME_VERIFIED_INFERENCES 已由 Runtime 锁定，"
                        "不得改写；compositeInferences 只返回 MISSING_DECISION_AUDIENCES 对应的新 inference，"
                        "Runtime 会与锁定项合并。不要复制 claimId，不要添加任何未在 evidenceExcerpt 中出现的"
                        "产品名、API、版本或数字。\nMISSING_DECISION_AUDIENCES: "
                        + json.dumps(
                            missing_decision_audiences,
                            ensure_ascii=False,
                        )
                        + "\nRUNTIME_VERIFIED_INFERENCES: "
                        + json.dumps(
                            locked_inference_material,
                            ensure_ascii=False,
                        )
                    )
                    audience_repair_prompt_added = True
                continue
            if candidate_supplemental_claim_ids:
                parsed["_runtimeExactExcerptClaimSupplements"] = candidate_supplemental_claim_ids
                candidate_errors.append(
                    f"{model_label}: runtime_exact_excerpt_claim_supplements:"
                    f"{len(candidate_supplemental_claim_ids)}"
                )
            if candidate_delta_claim_ids:
                parsed["_runtimeVerifiedClaimDeltaIds"] = list(
                    dict.fromkeys(candidate_delta_claim_ids)
                )
            plan = parsed
            plan["_planAttempts"] = plan_attempts[-6:]
            plan_candidate = candidate
            verified_claims = candidate_claims
            break
        if (
            plan is None
            and best_repair_candidate is candidate
            and best_repair_plan is not None
            and len(
                set(
                    [
                        *best_repair_missing_citations,
                        *best_repair_missing_facet_ids,
                    ]
                )
            )
            <= 3
            and len(best_repair_claims)
            >= TARGET_RESEARCH_CLAIM_COUNT
            - len(
                set(
                    [
                        *best_repair_missing_citations,
                        *best_repair_missing_facet_ids,
                    ]
                )
            )
            and not _research_text_list(best_repair_plan.get("criticalMissingEvidence"), limit=12)
        ):
            repaired_claims: list[dict[str, Any]] = []
            repair_failed = False
            for citation_key in best_repair_missing_citations:
                source = next(
                    (
                        item
                        for item in compact_sources
                        if _safe_text(item.get("citationKey")).strip("[]") == citation_key
                    ),
                    None,
                )
                raw_claim = (
                    _architect_exact_excerpt_source_fact(source, question)
                    if isinstance(source, dict)
                    else None
                )
                if not raw_claim:
                    repair_failed = True
                    break
                verified_repair, repair_issues = _verify_architect_claim_excerpts(
                    [raw_claim],
                    compact_sources,
                    require_evidence_key=True,
                )
                if repair_issues or len(verified_repair) != 1:
                    repair_failed = True
                    break
                repaired_claims.extend(verified_repair)
            combined_claims, supplemental_claim_ids = supplement_verified_claims(
                [*best_repair_claims, *repaired_claims]
            )
            repair_metrics = research_acceptance_metrics(
                {
                    "question": question,
                    "freshness": freshness,
                    "reviewDecision": "accept",
                    "answer": "",
                    "sourceUrls": compact_sources,
                    "claimTable": combined_claims,
                    "criticalMissingEvidence": [],
                    "asOf": _utc_now_iso(),
                }
            )
            repaired_outline = _architect_normalized_repair_outline(
                best_repair_plan.get("answerOutline"),
                combined_claims,
                best_repair_plan.get("compositeInferences"),
            )
            repaired_inferences, _repaired_inference_drops = (
                _architect_filter_composite_inferences(
                    repaired_outline,
                    combined_claims,
                    best_repair_plan.get("compositeInferences"),
                    validate_semantics=False,
                )
            )
            if (
                named_decision_audiences
                and runtime_named_decision_fallback_allowed
                and len(repaired_inferences) < len(named_decision_audiences)
            ):
                runtime_inferences = _architect_runtime_named_decision_inferences(
                    combined_claims,
                    named_decision_audiences,
                    question=question,
                )
                runtime_outline = _architect_normalized_repair_outline(
                    best_repair_plan.get("answerOutline"),
                    combined_claims,
                    runtime_inferences,
                )
                runtime_inferences, _runtime_inference_drops = (
                    _architect_filter_composite_inferences(
                        runtime_outline,
                        combined_claims,
                        runtime_inferences,
                        validate_semantics=False,
                    )
                )
                if len(runtime_inferences) == len(named_decision_audiences):
                    repaired_outline = runtime_outline
                    repaired_inferences = runtime_inferences
            repair_failed = bool(
                repair_failed
                or repair_metrics["claimCount"] < TARGET_RESEARCH_CLAIM_COUNT
                or repair_metrics["uniqueClaimCount"] < TARGET_RESEARCH_CLAIM_COUNT
                or repair_metrics["evidenceVerifiedClaimCount"] != repair_metrics["claimCount"]
                or repair_metrics["claimSupportedSourceCount"] < plan_claim_source_floor
                or missing_verified_claim_facets(combined_claims)
                or _architect_outline_inference_issues(
                    repaired_outline,
                    combined_claims,
                    repaired_inferences,
                )
            )
            if not repair_failed:
                plan = {
                    **best_repair_plan,
                    "reviewDecision": "accept",
                    "claimTable": combined_claims,
                    "answerOutline": repaired_outline,
                    "compositeInferences": repaired_inferences,
                    "conflictMatrix": [],
                    "missingEvidence": [],
                    "assumptions": [],
                    "temporalAssessment": {},
                    "criticalMissingEvidence": [],
                    "_runtimeExactExcerptClaimRepairs": list(best_repair_missing_citations),
                    "_runtimeClaimFacetRepairs": list(
                        best_repair_missing_facet_ids
                    ),
                    "_runtimeExactExcerptClaimSupplements": supplemental_claim_ids,
                }
                plan_candidate = best_repair_candidate
                verified_claims = combined_claims
                candidate_errors.append(
                    (
                        f"{model_label}: runtime_exact_excerpt_claim_repair:"
                        + ",".join(best_repair_missing_citations)
                    )
                    if best_repair_missing_citations
                    else f"{model_label}: runtime_verified_claim_outline_repair"
                )
        if plan is not None:
            break

    if plan is None and candidates:
        # A dedicated Architect can return malformed or provider-specific JSON
        # or an internally contradictory ``accept`` plus critical-gap payload
        # even though the already-read evidence is structurally sufficient.
        # Do not hand planning to an unrelated generic Summary model. Build a
        # strict claim plan from exact stored excerpts, then require the normal
        # independent Supervisor + adversarial review chain before delivery.
        # A genuine ``retry``/``reject`` decision returns earlier; this path only
        # recovers an accepted plan that failed Runtime validation.
        runtime_claims, runtime_supplement_ids = supplement_verified_claims(
            list(best_repair_claims)
        )
        runtime_claims, runtime_secondary_ids = (
            _architect_govern_claim_source_roles_for_question(
                runtime_claims,
                question,
            )
        )
        runtime_metrics = research_acceptance_metrics(
            {
                "question": question,
                "freshness": freshness,
                "reviewDecision": "accept",
                "answer": "",
                "sourceUrls": compact_sources,
                "claimTable": runtime_claims,
                "criticalMissingEvidence": [],
                "asOf": _utc_now_iso(),
            }
        )
        runtime_inferences = (
            _architect_runtime_named_decision_inferences(
                runtime_claims,
                named_decision_audiences,
                question=question,
            )
            if runtime_named_decision_fallback_allowed
            else []
        )
        runtime_outline = _architect_normalized_repair_outline(
            [],
            runtime_claims,
            runtime_inferences,
        )
        runtime_inferences, runtime_inference_issues = (
            _architect_filter_composite_inferences(
                runtime_outline,
                runtime_claims,
                runtime_inferences,
                validate_semantics=False,
            )
        )
        runtime_missing_facet_ids = missing_verified_claim_facets(runtime_claims)
        runtime_outline_issues = _architect_outline_inference_issues(
            runtime_outline,
            runtime_claims,
            runtime_inferences,
        )
        runtime_plan_ready = bool(
            runtime_metrics["claimCount"] >= TARGET_RESEARCH_CLAIM_COUNT
            and runtime_metrics["uniqueClaimCount"] >= TARGET_RESEARCH_CLAIM_COUNT
            and runtime_metrics["evidenceVerifiedClaimCount"]
            == runtime_metrics["claimCount"]
            and runtime_metrics["claimSupportedSourceCount"]
            >= plan_claim_source_floor
            and not runtime_missing_facet_ids
            and not runtime_outline_issues
            and not runtime_inference_issues
        )
        if runtime_plan_ready:
            plan_candidate = candidates[0]
            verified_claims = runtime_claims
            plan = {
                "reviewDecision": "accept",
                "reviewReasons": [],
                "headline": "Runtime-verified evidence plan",
                "claimTable": runtime_claims,
                "answerOutline": runtime_outline,
                "compositeInferences": runtime_inferences,
                "conflictMatrix": [],
                "missingEvidence": [],
                "criticalMissingEvidence": [],
                "recommendedNextQueries": [],
                "assumptions": [],
                "temporalAssessment": _architect_runtime_temporal_assessment(
                    compact_sources
                ),
                "_runtimeDeterministicEvidencePlan": True,
                "_runtimeExactExcerptClaimSupplements": runtime_supplement_ids,
                "_attributedSecondaryClaimIds": runtime_secondary_ids,
                "_nonBlockingPlanWarnings": (
                    [
                        "composite_inference_recommended_for_audiences:"
                        f"{len(runtime_inferences)}/{len(named_decision_audiences)}"
                    ]
                    if len(runtime_inferences) < len(named_decision_audiences)
                    else []
                ),
            }
            candidate_errors.append(
                "runtime_deterministic_evidence_plan_after_architect_protocol_failure"
            )
        else:
            candidate_errors.append(
                "runtime_deterministic_evidence_plan_not_ready:"
                f"claims={runtime_metrics['claimCount']},"
                f"unique={runtime_metrics['uniqueClaimCount']},"
                f"verified={runtime_metrics['evidenceVerifiedClaimCount']},"
                f"sources={runtime_metrics['claimSupportedSourceCount']},"
                "missingFacets=" + ",".join(runtime_missing_facet_ids[:8]) + ","
                "outline=" + ",".join(runtime_outline_issues[:4]) + ","
                "inferences=" + ",".join(runtime_inference_issues[:4]) + ","
                f"audiences={len(runtime_inferences)}/{len(named_decision_audiences)}"
            )

    if plan is None or plan_candidate is None:
        return {
            "_agentError": "architect_evidence_plan_unavailable",
            "_architectMode": "full_synthesis",
            "_missingFacetIds": runtime_missing_facet_ids[:8],
            "_rawPreview": "\n--- retry ---\n".join(raw_previews)[:800],
            "_modelFallbackAttempts": candidate_errors[:16],
            "_planAttempts": plan_attempts[-6:],
            "_contextPreparations": context_preparations[-24:],
        }

    plan["_planAttempts"] = plan_attempts[-6:]

    # A product comparison with explicitly named decision audiences needs a
    # compact synthesis pass after claims are locked.  The normal planning
    # prompt carries full evidence candidates and can spend its budget on
    # search-plan repair; asking that same prompt to also invent three
    # audience conclusions is what previously left a valid 20-claim plan with
    # no usable decision layer.  This pass sees only Runtime-verified claims,
    # and its output still goes through the existing outline/inference gates,
    # segmented writer, and two independent reviews.
    if (
        named_decision_audiences
        and not runtime_named_decision_fallback_allowed
        and plan_candidate is not None
    ):
        existing_inferences = _research_dict_list(
            plan.get("compositeInferences"),
            limit=12,
        )
        existing_inference_text = " ".join(
            _safe_text(
                inference.get("audience")
                or inference.get("inference")
                or inference.get("conclusion")
                or inference.get("recommendation")
            )
            for inference in existing_inferences
        ).casefold()
        missing_audiences = [
            audience
            for audience in named_decision_audiences
            if _safe_text(audience).casefold() not in existing_inference_text
        ]
        decision_audit: dict[str, Any] = {
            "status": "not_needed" if not missing_audiences else "pending",
            "requestedAudiences": list(missing_audiences),
            "acceptedCount": 0,
        }
        if missing_audiences and remaining_seconds() > delivery_reserve + 10:
            decision_claim_material = [
                {
                    "claimId": _safe_text(claim.get("claimId")),
                    "claim": _safe_text(claim.get("claim")),
                    "sourceClaim": _safe_text(claim.get("sourceClaim")),
                    "claimType": _safe_text(claim.get("claimType")),
                    "citationKeys": [
                        _safe_text(
                            support.get("citationKey") or support.get("citation")
                            if isinstance(support, dict)
                            else support
                        ).strip("[]")
                        for support in list(claim.get("supportingSources") or [])
                        if _safe_text(
                            support.get("citationKey") or support.get("citation")
                            if isinstance(support, dict)
                            else support
                        )
                    ],
                    "sourceTitles": [
                        _compact_research_text(support.get("title"), limit=100)
                        for support in list(claim.get("supportingSources") or [])
                        if isinstance(support, dict) and _safe_text(support.get("title"))
                    ],
                    "evidenceExcerpt": _compact_research_text(
                        claim.get("evidenceExcerpt"),
                        limit=320,
                    ),
                }
                for claim in verified_claims
                if _safe_text(claim.get("claimId"))
            ]
            allowed_decision_claim_ids = [
                _safe_text(claim.get("claimId"))
                for claim in verified_claims
                if _safe_text(claim.get("claimId"))
            ]
            decision_system_prompt = _research_agent_stage_system_prompt(
                "你是 Research Runtime 的受众决策综合节点。只使用 RUNTIME_VERIFIED_CLAIMS 中的事实，"
                "不要搜索、不要新增产品能力、版本、命令、价格或数字。对 QUESTION 明确列出的每个受众各返回一条"
                "条件性综合判断；结论必须标明受众、只引用已列出的 premiseClaimIds，并把不确定性保留为条件，"
                "不能冒充任何官方来源的原话。输出严格 JSON。",
                stage="evidence_plan",
            )
            decision_instruction = (
                "只输出 {\"compositeInferences\":[...]}。每项只含 inferenceId、audience、inference、"
                "premiseClaimIds；每个受众一项，inference 40-260 个字符，至少引用一个最相关的已验证 claim，"
                "优先使用不同且直接相关的前提，禁止复用同一 claim；不要填入不存在的 claimId。"
                "推荐必须是条件性的‘当这些已验证条件符合时优先考虑……；否则保留比较分支’，"
                "不要把 source_fact 写成官方推荐。\nQUESTION: "
                + question
                + "\nMISSING_AUDIENCES: "
                + json.dumps(missing_audiences, ensure_ascii=False)
                + "\nALLOWED_PREMISE_CLAIM_IDS: "
                + json.dumps(allowed_decision_claim_ids, ensure_ascii=False)
                + "\nEXISTING_OUTLINE: "
                + json.dumps(
                    [
                        {
                            "sectionId": _safe_text(item.get("sectionId")),
                            "claimIds": list(item.get("claimIds") or []),
                        }
                        for item in _research_dict_list(plan.get("answerOutline"), limit=6)
                    ],
                    ensure_ascii=False,
                )
            )
            decision_messages = prepared_messages(
                plan_candidate,
                system_prompt=decision_system_prompt,
                instruction=decision_instruction,
                materials=[
                    {
                        "title": "Runtime-verified decision claims",
                        "kind": "research_verified_claims",
                        "content": json.dumps(
                            decision_claim_material,
                            ensure_ascii=False,
                        ),
                    }
                ],
                target_role="web-research-architect",
                node="web_research_architect_decision_completion",
            )
            decision_seconds = min(
                float(_RESEARCH_ARCHITECT_DECISION_TIMEOUT_SECONDS),
                max(8.0, remaining_seconds() - delivery_reserve - 3.0),
            )
            decision_audit.update(
                {
                    "status": "started",
                    "modelId": _architect_candidate_identity(plan_candidate),
                    "timeoutSeconds": decision_seconds,
                    "requestChars": len(decision_instruction),
                }
            )
            decision_started_at = time.perf_counter()
            try:
                decision_response = _invoke_architect_candidate_with_deadline(
                    plan_candidate,
                    decision_messages,
                    seconds=decision_seconds,
                    max_tokens=_RESEARCH_ARCHITECT_DECISION_MAX_TOKENS,
                    disable_thinking=True,
                )
            except concurrent.futures.TimeoutError:
                decision_audit.update(
                    {
                        "status": "deadline_timeout",
                        "elapsedMs": int((time.perf_counter() - decision_started_at) * 1000),
                    }
                )
                candidate_errors.append(
                    f"{_architect_candidate_identity(plan_candidate)}: audience_decision_timeout"
                )
            except Exception as exc:  # noqa: BLE001 - decision completion is non-blocking.
                decision_audit.update(
                    {
                        "status": "provider_error",
                        "failureCode": type(exc).__name__,
                        "elapsedMs": int((time.perf_counter() - decision_started_at) * 1000),
                    }
                )
                candidate_errors.append(
                    f"{_architect_candidate_identity(plan_candidate)}: audience_decision_"
                    f"{type(exc).__name__}"
                )
            else:
                decision_text = sanitize_background_model_output(decision_response).text
                decision_payload = _extract_json_object(decision_text)
                raw_rows = (
                    _research_dict_list(decision_payload.get("compositeInferences"), limit=12)
                    if isinstance(decision_payload, dict)
                    else []
                )
                new_rows: list[dict[str, Any]] = []
                allowed_decision_claim_id_set = set(allowed_decision_claim_ids)
                normalized_premise_id_count = 0
                for row_index, row in enumerate(raw_rows, start=1):
                    row_text = _safe_text(
                        row.get("audience")
                        or row.get("inference")
                        or row.get("conclusion")
                        or row.get("recommendation")
                    )
                    if not any(
                        _safe_text(audience).casefold() in row_text.casefold()
                        for audience in missing_audiences
                    ):
                        continue
                    raw_premise_ids = [
                        _safe_text(value)
                        for value in list(row.get("premiseClaimIds") or [])
                        if _safe_text(value)
                    ]
                    premise_ids = list(
                        dict.fromkeys(
                            claim_id
                            for claim_id in raw_premise_ids
                            if claim_id in allowed_decision_claim_id_set
                        )
                    )
                    if not premise_ids:
                        continue
                    normalized_premise_id_count += max(
                        0,
                        len(raw_premise_ids) - len(premise_ids),
                    )
                    new_rows.append(
                        {
                            **row,
                            "inferenceId": _safe_text(row.get("inferenceId"))
                            or f"inference_runtime_audience_completion_{row_index}",
                            "premiseClaimIds": premise_ids,
                        }
                    )
                combined_inferences = _research_normalize_named_decision_inferences(
                    [*existing_inferences, *new_rows],
                    named_decision_audiences,
                )
                completion_outline = _architect_normalized_repair_outline(
                    plan.get("answerOutline"),
                    verified_claims,
                    combined_inferences,
                )
                filtered_inferences, dropped_inferences = (
                    _architect_filter_composite_inferences(
                        completion_outline,
                        verified_claims,
                        combined_inferences,
                        validate_semantics=False,
                    )
                )
                existing_ids = {
                    _safe_text(row.get("inferenceId"))
                    for row in existing_inferences
                    if _safe_text(row.get("inferenceId"))
                }
                filtered_ids = {
                    _safe_text(row.get("inferenceId"))
                    for row in filtered_inferences
                    if _safe_text(row.get("inferenceId"))
                }
                accepted_audiences = {
                    audience.casefold()
                    for audience in named_decision_audiences
                    if any(
                        audience.casefold()
                        in _safe_text(
                            row.get("audience")
                            or row.get("inference")
                            or row.get("conclusion")
                            or row.get("recommendation")
                        ).casefold()
                        for row in filtered_inferences
                    )
                }
                if existing_ids.issubset(filtered_ids) and filtered_inferences:
                    plan["answerOutline"] = completion_outline
                    plan["compositeInferences"] = filtered_inferences
                    warnings = _research_text_list(
                        plan.get("_nonBlockingPlanWarnings"),
                        limit=12,
                    )
                    if len(accepted_audiences) == len(named_decision_audiences):
                        warnings = [
                            warning
                            for warning in warnings
                            if not warning.startswith(
                                "composite_inference_recommended_for_audiences:"
                            )
                        ]
                    plan["_nonBlockingPlanWarnings"] = warnings
                    decision_audit.update(
                        {
                            "status": "accepted" if accepted_audiences else "no_valid_inferences",
                            "elapsedMs": int((time.perf_counter() - decision_started_at) * 1000),
                            "acceptedCount": len(accepted_audiences),
                            "normalizedPremiseIdCount": normalized_premise_id_count,
                            "dropped": dropped_inferences[:4],
                            "missingAudiencesAfter": [
                                audience
                                for audience in named_decision_audiences
                                if audience.casefold() not in accepted_audiences
                            ],
                        }
                    )
                else:
                    decision_audit.update(
                        {
                            "status": "invalid_or_unbound",
                            "elapsedMs": int((time.perf_counter() - decision_started_at) * 1000),
                            "acceptedCount": 0,
                            "dropped": dropped_inferences[:4],
                        }
                    )
                    candidate_errors.append(
                        f"{_architect_candidate_identity(plan_candidate)}: audience_decision_unbound"
                    )
        elif missing_audiences:
            decision_audit["status"] = "skipped_delivery_budget_reserved"
        plan["_audienceDecisionCompletion"] = decision_audit

    ordered_writer_candidates = list(candidates)
    # The candidate that produced a Runtime-verified plan has already proved it can
    # serve this request. Start writing with it instead of spending the delivery
    # budget retrying a candidate that just failed planning.
    writer_candidates = [plan_candidate] + [
        candidate
        for candidate in ordered_writer_candidates
        if candidate[0] is not plan_candidate[0]
    ]
    answer_language = "简体中文" if re.search(r"[\u4e00-\u9fff]", question) else "English"
    writer_system_prompt = (
        "你是 Research Runtime 的最终答案撰写器。只使用 VERIFIED PLAN 和 SOURCES 中已经读取并由 Runtime 验真的事实，"
        "不要添加常识性补写、未经证据支持的数据或虚构的官方立场。直接回答用户问题，不要描述调研流程。"
        "source_fact 只能写成来源直接事实；explicit_normative 才能归因为来源明确推荐或要求。"
        "组合建议只能来自 VERIFIED PLAN 已登记的 compositeInferences，必须明确写成“综合判断”或“本报告建议”，"
        "同一内容单元引用全部 premiseClaimIds 的来源；禁止自行新增综合建议或写成官方推荐。"
        "问题即使询问 best practices，也不能把 source_fact 改写成官方最佳实践；应把可执行建议明确标成"
        "“本报告的综合判断”或“practical synthesis”，并让旧的、未定年的或 secondary 来源只承担历史示例、"
        "使用经验或边界说明，不得作为当前官方立场。"
        "不要输出逐字大段引文或 Markdown blockquote；应在不改变含义的前提下简洁转述已验证 claim。"
        "不要自行声称某做法是最高杠杆、基础性设计决策、独立类别、等价接口或无条件适用；除非 VERIFIED PLAN "
        "已登记且其全部 premise 明确支持这一关系。"
        "来源摘录只有月日而没有年份时，必须明确写‘来源未标明年份’，不得补成当前年份、2026 或其他年份，"
        "也不得把这条未定年事实单独当作当前选型依据。"
        "五个来源和三千字只是拒收线；正常交付必须有足够深度、差异、限制、时效证据和可执行结论，禁止重复凑字。"
        "QUESTION 含多个 [facet-id] 时，正文必须逐项实质回答每个 facet，并在对应内容单元引用支持该 facet 的来源。"
        "QUESTION 含 Deliverable requirements/交付要求时，必须在正文中完成这些输出目标；它们只综合已验证证据，"
        "不得被误当成需要另行搜索的新事实，也不得凭空增加行动建议。"
        "VERIFIED PLAN 含 namedDecisionAudiences 时，必须逐一给出对应的本报告综合判断；不得用通用总结替代，"
        "也不得把这些跨来源结论冒充为某一官方来源的原话。"
        f"答案正文必须只使用 {answer_language}，来源原标题除外。"
    )
    writer_system_prompt = _research_agent_stage_system_prompt(
        writer_system_prompt,
        stage="answer_writer",
    )
    prompt_contracts = {
        "evidencePlan": {
            "stage": "evidence_plan",
            "version": RESEARCH_PROMPT_CONTRACT_VERSION,
            "digest": research_runtime_prompt_digest(plan_system_prompt),
        },
        "answerWriter": {
            "stage": "answer_writer",
            "version": RESEARCH_PROMPT_CONTRACT_VERSION,
            "digest": research_runtime_prompt_digest(writer_system_prompt),
        },
    }
    writer_prompt = (
        f"用与 QUESTION 相同的主要语言撰写详细 Markdown 答案。正常目标是去空白后 "
        f"{_RESEARCH_ARCHITECT_IDEAL_ANSWER_MIN_CHARS}-{_RESEARCH_ARCHITECT_IDEAL_ANSWER_MAX_CHARS} 个有效 Unicode 字符；"
        f"{TARGET_RESEARCH_ANSWER_CHARS} 只是硬拒收线，不得把达到拒收线当作完成，也不得重复凑字。"
        f"答案不得超过 {_MAX_RESEARCH_VISIBLE_ANSWER_CHARS} 字。先给直接结论，再逐层回答问题；至少形成 "
        f"{TARGET_RESEARCH_CLAIM_COUNT} 个有实质内容的证据单元，并让至少 {TARGET_RESEARCH_SOURCE_COUNT} 个不同来源分别出现在相关段落的 [S#] 引用中。"
        "必须明确 as-of 时点，区分来源直接事实、跨来源综合推论、冲突或限制；结尾列出实际使用的来源标题、URL、日期/版本。"
        "只输出答案正文，不要 JSON、不要代码围栏。完成全部正文和来源列表后，最后单独输出完成标记 "
        f"{_RESEARCH_ARCHITECT_ANSWER_COMPLETE_MARKER}。\nQUESTION: {question}"
    )
    verified_plan = {
        "question": question,
        "requiredFacets": [
            {
                "facetId": _research_facet_id(kind),
                "goal": _safe_text(goal),
            }
            for goal, kind in _build_explicit_question_facets(question)
            if _research_facet_id(kind)
        ],
        "requiredDeliverables": _build_explicit_question_deliverables(question),
        "namedDecisionAudiences": named_decision_audiences,
        "headline": plan.get("headline"),
        "claimTable": verified_claims,
        "answerOutline": plan.get("answerOutline") or [],
        "compositeInferences": plan.get("compositeInferences") or [],
        "conflictMatrix": plan.get("conflictMatrix") or [],
        "missingEvidence": plan.get("missingEvidence") or [],
        "assumptions": plan.get("assumptions") or [],
        "temporalAssessment": _architect_runtime_temporal_assessment(prompt_sources),
        "asOf": _utc_now_iso(),
    }
    answer = ""
    writer_candidate: tuple[Any, str, str] | None = None
    writer_revision_count = 0
    writer_section_diagnostics: list[dict[str, Any]] = []
    writer_section_diagnostics_lock = threading.Lock()

    def answer_structural_issues(candidate_answer: str) -> list[str]:
        candidate_payload = {
            "question": question,
            "freshness": freshness,
            "reviewDecision": "accept",
            "answer": candidate_answer,
            "sourceUrls": compact_sources,
            "claimTable": verified_claims,
            "criticalMissingEvidence": [],
            "asOf": verified_plan["asOf"],
        }
        issues = [
            issue
            for issue in research_high_quality_issues(candidate_payload)
            if issue != "independent_semantic_review_not_accepted"
        ]
        brief_coverage = _research_brief_coverage(
            question=question,
            source_matrix=compact_sources,
            claim_table=verified_claims,
            answer=candidate_answer,
        )
        issues.extend(
            f"required_facet_not_answered:{item.get('taskBriefId')}:{item.get('status')}"
            for item in list(brief_coverage.get("items") or [])
            if isinstance(item, dict) and item.get("status") != "supported"
        )
        return issues

    def write_segmented_answer(
        candidate: tuple[Any, str, str],
        profile: dict[str, Any],
        *,
        review_feedback: list[str] | None = None,
        accepted_sections: dict[int, str] | None = None,
        previous_sections: dict[int, str] | None = None,
        alternate_writer_reserve: float = 0.0,
    ) -> tuple[str, int, int, list[str], dict[int, str]]:
        tasks = _architect_segment_tasks(
            verified_plan,
            section_count=int(profile.get("sectionCount") or 4),
            target_min_chars=int(profile.get("targetMinChars") or 0),
            target_max_chars=int(profile.get("targetMaxChars") or 0),
        )
        if len(tasks) < 2:
            return "", 0, len(tasks), ["segmented_writer_requires_multiple_sections"], {}
        model_label = _architect_candidate_identity(candidate) or candidate[1] or f"role:{candidate[2]}"
        valid_citations = {
            _safe_text(source.get("citationKey")).strip("[]")
            for source in prompt_sources
            if _safe_text(source.get("citationKey"))
        }
        split_slots = max(0, 6 - len(tasks))
        split_slots_lock = threading.Lock()

        def generate_section(
            task: dict[str, Any],
            *,
            split_depth: int = 0,
        ) -> tuple[int, str, int, list[str], int]:
            nonlocal split_slots
            section_id = _safe_text(task.get("sectionId"))
            section_sequence = int(task.get("sequence") or 0)
            previous_accepted_section = (
                _safe_text(dict(previous_sections or {}).get(section_sequence))
                if split_depth == 0
                else ""
            )
            required_citations = {
                _safe_text(value).strip("[]")
                for value in list(task.get("requiredCitationKeys") or [])
                if _safe_text(value)
            }
            if not required_citations or not required_citations.issubset(valid_citations):
                return int(task.get("sequence") or 0), "", 0, [f"{section_id}: invalid_assigned_citations"], 0
            assigned_claims = _research_dict_list(
                task.get("assignedClaims"),
                limit=_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT,
            )
            relevant_sources: list[dict[str, Any]] = []
            for source in prompt_sources:
                citation_key = _safe_text(source.get("citationKey")).strip("[]")
                if citation_key not in required_citations:
                    continue
                source_claims = [
                    {
                        "claimId": _safe_text(claim.get("claimId")),
                        "claim": _safe_text(claim.get("claim")),
                        "claimType": _safe_text(claim.get("claimType")),
                        "normativeCue": _safe_text(claim.get("normativeCue")),
                        "evidenceExcerptKey": _safe_text(claim.get("evidenceExcerptKey")),
                        "exactEvidenceExcerpt": _safe_text(claim.get("evidenceExcerpt")),
                        "temporalBoundary": _safe_text(claim.get("temporalBoundary")),
                    }
                    for claim in assigned_claims
                    if citation_key
                    in {
                        _safe_text(
                            support.get("citationKey") or support.get("citation")
                            if isinstance(support, dict)
                            else support
                        ).strip("[]")
                        for support in list(claim.get("supportingSources") or [])
                    }
                ]
                relevant_sources.append(
                    {
                        "citationKey": citation_key,
                        "title": source.get("title"),
                        "publishedAt": source.get("publishedAt"),
                        "updatedAt": source.get("updatedAt"),
                        "version": source.get("version"),
                        "temporalEvidence": source.get("temporalEvidence") or {},
                        "verifiedClaims": source_claims,
                    }
                )
            claim_evidence_digest = hashlib.sha256(
                json.dumps(
                    [
                        {
                            "claimId": _safe_text(claim.get("claimId")),
                            "citationKeys": checklist.get("citationKeys") or [],
                            "evidenceExcerptSha256": _safe_text(
                                claim.get("evidenceExcerptSha256")
                            )
                            or hashlib.sha256(
                                _safe_text(claim.get("evidenceExcerpt")).encode(
                                    "utf-8", errors="ignore"
                                )
                            ).hexdigest(),
                        }
                        for claim, checklist in zip(
                            assigned_claims,
                            _architect_claim_citation_checklist(assigned_claims),
                            strict=False,
                        )
                    ],
                    ensure_ascii=False,
                    sort_keys=True,
                ).encode("utf-8", errors="ignore")
            ).hexdigest()
            section_source_payload_chars = len(
                json.dumps(relevant_sources, ensure_ascii=False)
            )
            marker = f"<!-- {_RESEARCH_ARCHITECT_SECTION_COMPLETE_MARKER_PREFIX}:{section_id} -->"
            previous_section = ""
            last_section_issues: list[str] = []
            local_errors: list[str] = []
            for attempt_index in range(3):
                continuation_only = bool(
                    attempt_index == 2
                    and last_section_issues
                    and all(issue.startswith("section_depth_not_met:") for issue in last_section_issues)
                )
                if attempt_index == 2 and not continuation_only:
                    break
                remaining = remaining_seconds()
                if remaining <= review_reserve + alternate_writer_reserve + 5:
                    local_errors.append(f"{section_id}: review_budget_reserved")
                    break
                per_call_budget = min(
                    call_timeout_cap_seconds,
                    75.0,
                    max(5.0, remaining - review_reserve - alternate_writer_reserve - 3.0),
                )
                instruction = (
                    "只撰写 SECTION_CONTRACT 指定的一个 Markdown 章节，不要写总标题、总来源列表或其他章节。"
                    f"正文只使用 {answer_language}。"
                    "只能使用 assignedClaims、compositeInferences 与 VERIFIED SOURCES 中逐项列出的 exactEvidenceExcerpt；"
                    "同一网页没有被列入本节的其他内容也不得使用，不得增加常识性事实或猜测。"
                    "严格按 claimCitationChecklist 逐项覆盖每个 assigned claim，并在该 claim 的内容单元内原样写出其 [S#]；"
                    "每个 assignedClaims.claim 至少一次保持原意逐字写入正文（包括 Runtime 已加的 secondary 署名归因），"
                    "只允许调整标点和 Markdown，不得用更宽的同义改写替代；"
                    "每个事实句必须在同句放置支持它的引用，不得把引用集中堆在段末，也不得引用范围外编号。"
                    "主题标题应由紧随正文的引用支撑；若输出代码块，必须在代码块后紧接一行独立 [S#] 引用。"
                    "API/类名/参数/配置键/版本/数值/代码示例、否定能力、性能效果与强度描述，只有在该引用绑定的 exact evidenceExcerpt "
                    "逐字出现或明确蕴含时才能写；禁止用一个相关引用替新增事实背书。若摘录没有代码，就不要生成代码。"
                    "若 assigned claim 的 temporalBoundary 标记为 source_month_day_without_year，正文必须保留‘年份未标明’边界，"
                    "不得把该月日与当前年份、当前版本或当前选型建议绑定。"
                    "SECTION_CONTRACT.permittedHardAnchors 是本节可出现的高风险事实锚点白名单；不在其中的锚点一律删除。"
                    "只有 SECTION_CONTRACT.verbatimCodeBlocks 非空时才可输出代码块，而且必须逐字复制其中一个完整代码块，"
                    "不得改变量名、参数或示例值；该列表为空时禁止输出任何代码块。"
                    "来源直接事实、跨来源综合判断、冲突/限制与时效边界必须明确区分；不要输出 URL。"
                    + (
                        "本节承担以下用户问题 facet，必须逐项实质回答，不能只写共同背景："
                        + json.dumps(task.get("facetGoals") or [], ensure_ascii=False)
                        + "。"
                        if task.get("facetGoals")
                        else ""
                    )
                    + (
                        "本节还承担以下交付目标；它们不是新增搜索事实，必须只用 assignedClaims 和已登记的 "
                        "compositeInferences 完成，并让每个清单项或结论就近引用其证据："
                        + json.dumps(task.get("deliverableGoals") or [], ensure_ascii=False)
                        + "。"
                        if task.get("deliverableGoals")
                        else ""
                    )
                    + "禁止 Markdown blockquote 或长篇逐字引文；只转述 assignedClaims。secondary 教程、文章或社区来源"
                    "只能明确标成 secondary/教程示例/历史材料，不能写入‘官方建议’或‘当前标准’标题。"
                    "任何 best practice、should、recommended、最佳实践、应当或建议措辞，若不是 assignedClaims 中"
                    "带 normativeCue 的 explicit_normative，就必须在同一内容单元明确写 practical synthesis / 本报告的综合判断，"
                    "且内容必须对应 SECTION_CONTRACT.compositeInferences 中已登记的 inference，并引用所有 premise 前提；"
                    "未登记的行动建议必须删除。secondary 或旧来源必须标明其二手/历史角色。"
                    f"去空白有效字符建议目标为 {task['targetMinChars']}-{task['targetMaxChars']}；"
                    f"本节可按证据密度自然长短，但不得少于 {task['minimumAcceptableChars']}，禁止重复凑字。"
                    + ("本节开头直接给出与所分配证据相符的结论。" if task.get("directConclusionSection") else "")
                    + (
                        "本节前两个内容单元内必须明确使用 Practical synthesis: / 本报告的综合判断：标注综合结论，"
                        "并在同一单元引用全部必要的一手、当前前提。"
                        if task.get("requiresSynthesisConclusion")
                        else ""
                    )
                    + (
                        "本节收束 assignedClaims 直接支持的限制、风险和适用边界。显式 deliverableGoals 可把来源已经规定的"
                        "义务改写成逐项执行清单，但必须标明这是操作化重述并就近引用；其他跨来源行动仍只有存在 "
                        "SECTION_CONTRACT.compositeInferences 时才能写，不得为了凑成建议而使用 should/recommended/建议/应当等规范措辞。"
                        if task.get("limitationsAndActionSection")
                        else ""
                    )
                    + "提交前逐字核对必须出现的引用："
                    + " ".join(f"[{key}]" for key in sorted(required_citations))
                    + f"。完成本节后最后单独输出 {marker}。\nQUESTION: {question}"
                )
                if review_feedback:
                    instruction += (
                        "\n同一独立 Reviewer 对上一版完整答案给出以下意见。只修复与本节 assignedClaims 相关的部分，"
                        "不要新增事实或扩大引用范围："
                        + json.dumps(review_feedback, ensure_ascii=False)
                    )
                    if previous_accepted_section:
                        instruction += (
                            "\n上一版该章节已经通过段级事实、引用和长度门禁。请以它为受控草稿完整重写："
                            "保留其中正确的 assignedClaims、引用、来源角色和 synthesis 标记，只修改 Reviewer 指出的"
                            "问题；不要退化成更短摘要，也不要复制任何已被 Reviewer 指为不支持的句子。"
                            "\nPREVIOUS_ACCEPTED_SECTION:\n"
                            + previous_accepted_section[:12_000]
                        )
                if attempt_index:
                    missing_citations = sorted(
                        {
                            issue.rsplit(":", 1)[-1]
                            for issue in local_errors
                            if "section_citation_missing:" in issue
                        }
                    )
                    unsupported_anchors = sorted(
                        {
                            match.group(1)
                            for issue in local_errors
                            if (
                                match := re.search(
                                    r"(?:api|version|quantity|code)_anchor_not_in_evidence:([^,\s]+)",
                                    issue,
                                )
                            )
                        }
                    )
                    if continuation_only:
                        instruction += (
                            "\n上一稿已通过事实、引用与范围门禁，只是有效内容不足。仅输出不重复的续写段落，"
                            "不要重写标题或已有内容；继续只解释 assignedClaims 的条件、边界、差异和相互关系，"
                            "每个事实单元同处放引用。Runtime 会把续写拼接到上一稿后再整体复核。"
                            f"\nPREVIOUS_SECTION:\n{previous_section}"
                        )
                    else:
                        instruction += (
                            "\n上一稿仅本节未通过 Runtime 门禁。请完整重写本节，不要改写其他章节。"
                            f"失败原因：{', '.join(local_errors[-8:])}。\nPREVIOUS_SECTION:\n{previous_section}"
                        )
                    if missing_citations:
                        instruction += "\n缺失的精确引用 token：" + " ".join(
                            f"[{key}]" for key in missing_citations
                        )
                    if unsupported_anchors:
                        instruction += (
                            "\n以下锚点没有被 assignedClaims 的 exact excerpt 支持，必须删除其每一次出现，"
                            "不得换成同义 API 或常识补写："
                            + ", ".join(unsupported_anchors)
                        )
                    if any("section_depth_not_met:" in issue for issue in local_errors):
                        instruction += (
                            "\n本节有效内容不足。只能通过解释 assignedClaims 已给出的适用条件、边界、差异和"
                            "相互关系来增加深度；每个事实单元同处放引用，禁止重复同一句或增加新 API/数据。"
                        )
                messages = prepared_messages(
                    candidate,
                    system_prompt=writer_system_prompt,
                    instruction=instruction,
                    materials=[
                        {
                            "title": "Section contract",
                            "kind": "research_section_contract",
                            "content": json.dumps(task, ensure_ascii=False),
                        },
                        {
                            "title": "Verified sources for this section",
                            "kind": "research_section_sources",
                            "content": json.dumps(relevant_sources, ensure_ascii=False),
                        },
                    ],
                    target_role="web-research-answer-writer",
                    node=(
                        "web_research_architect_answer_section_revision"
                        if attempt_index
                        else "web_research_architect_answer_section"
                    ),
                )
                try:
                    response = _invoke_architect_candidate_with_deadline(
                        candidate,
                        messages,
                        seconds=per_call_budget,
                        max_tokens=int(profile.get("sectionMaxTokens") or _RESEARCH_ARCHITECT_SECTION_MAX_TOKENS),
                        disable_thinking=True,
                    )
                except concurrent.futures.TimeoutError:
                    local_errors.append(f"{section_id}: timeout")
                    with writer_section_diagnostics_lock:
                        writer_section_diagnostics.append(
                            {
                                "modelId": model_label,
                                "sectionId": section_id,
                                "attempt": attempt_index + 1,
                                "status": "timeout",
                                "assignedClaimIds": [
                                    _safe_text(claim.get("claimId")) for claim in assigned_claims
                                ],
                                "requiredCitationKeys": sorted(required_citations),
                                "claimEvidenceDigest": claim_evidence_digest,
                                "sectionSourcePayloadChars": section_source_payload_chars,
                            }
                        )
                    continue
                except Exception as exc:  # noqa: BLE001 - retry only the failed section.
                    local_errors.append(f"{section_id}: {type(exc).__name__}: {_safe_text(exc)[:180]}")
                    with writer_section_diagnostics_lock:
                        writer_section_diagnostics.append(
                            {
                                "modelId": model_label,
                                "sectionId": section_id,
                                "attempt": attempt_index + 1,
                                "status": "provider_error",
                                "failureCode": type(exc).__name__,
                                "assignedClaimIds": [
                                    _safe_text(claim.get("claimId")) for claim in assigned_claims
                                ],
                                "requiredCitationKeys": sorted(required_citations),
                                "claimEvidenceDigest": claim_evidence_digest,
                                "sectionSourcePayloadChars": section_source_payload_chars,
                            }
                        )
                    continue
                sanitized_output = sanitize_background_model_output(response)
                raw_section = sanitized_output.text
                raw_response_chars = len(raw_section)
                prior_section = previous_section
                section, complete = _research_answer_section_from_model_output(
                    raw_section,
                    section_id=section_id,
                )
                if continuation_only and prior_section:
                    section = f"{prior_section}\n\n{section}".strip()
                    section, continuation_duplicate_drops = _architect_dedupe_repeated_content_units(
                        section
                    )
                    if continuation_duplicate_drops:
                        local_errors.append(
                            f"{section_id}: runtime_continuation_duplicate_drops:"
                            f"{continuation_duplicate_drops}"
                        )
                parsed_section_chars = len(section)
                section, unverified_code_block_drops = _architect_strip_unverified_section_code_blocks(
                    section,
                    task,
                )
                post_code_block_chars = len(section)
                if unverified_code_block_drops:
                    local_errors.append(
                        f"{section_id}: runtime_unverified_code_block_drops:"
                        f"{unverified_code_block_drops}"
                    )
                section, citation_repairs = _architect_repair_unambiguous_section_citations(
                    section,
                    task,
                )
                post_citation_chars = len(section)
                if citation_repairs:
                    local_errors.append(
                        f"{section_id}: runtime_unambiguous_citation_repairs:{citation_repairs}"
                    )
                section, source_role_repairs = _architect_repair_source_role_labels(
                    section,
                    task,
                )
                post_source_role_chars = len(section)
                if source_role_repairs:
                    local_errors.append(
                        f"{section_id}: runtime_source_role_repairs:{source_role_repairs}"
                    )
                unsupported_unit_diagnostics: list[str] = []
                unsupported_unit_reason_counts: dict[str, int] = {}
                section, unsupported_unit_drops = _architect_drop_unsupported_section_units(
                    section,
                    task,
                    diagnostics=unsupported_unit_diagnostics,
                    reason_counts=unsupported_unit_reason_counts,
                )
                post_unsupported_drop_chars = len(section)
                if unsupported_unit_drops:
                    local_errors.append(
                        f"{section_id}: runtime_unsupported_unit_drops:{unsupported_unit_drops}"
                    )
                    local_errors.extend(
                        f"{section_id}: unsupported_unit_preview:{item}"
                        for item in unsupported_unit_diagnostics
                    )
                section, restored_claims = _architect_restore_unrepresented_claims(
                    section,
                    task,
                )
                if restored_claims:
                    local_errors.append(
                        f"{section_id}: runtime_verified_claim_restorations:{restored_claims}"
                    )
                    section, restored_source_role_repairs = _architect_repair_source_role_labels(
                        section,
                        task,
                    )
                    if restored_source_role_repairs:
                        local_errors.append(
                            f"{section_id}: runtime_source_role_repairs:{restored_source_role_repairs}"
                        )
                section, source_url_strips = _architect_strip_section_source_urls(section)
                if source_url_strips:
                    local_errors.append(
                        f"{section_id}: runtime_source_url_strips:{source_url_strips}"
                    )
                section, repeated_unit_drops = _architect_dedupe_repeated_content_units(section)
                if repeated_unit_drops:
                    local_errors.append(
                        f"{section_id}: runtime_repeated_unit_drops:{repeated_unit_drops}"
                    )
                previous_section = section
                section_issues = _architect_section_issues(section, task, complete=complete)
                diagnostic_status = "accepted" if not section_issues else "rejected"
                with writer_section_diagnostics_lock:
                    writer_section_diagnostics.append(
                        {
                            "modelId": model_label,
                            "sectionId": section_id,
                            "attempt": attempt_index + 1,
                            "status": diagnostic_status,
                            "continuationOnly": continuation_only,
                            "finishReason": _architect_response_finish_reason(response),
                            "requestedMaxTokens": int(
                                profile.get("sectionMaxTokens")
                                or _RESEARCH_ARCHITECT_SECTION_MAX_TOKENS
                            ),
                            "configuredMaxTokens": profile.get("configuredMaxTokens"),
                            **_architect_response_safe_diagnostics(response, sanitized_output),
                            "assignedClaimIds": [
                                _safe_text(claim.get("claimId")) for claim in assigned_claims
                            ],
                            "requiredCitationKeys": sorted(required_citations),
                            "claimEvidenceDigest": claim_evidence_digest,
                            "sectionSourcePayloadChars": section_source_payload_chars,
                            "rawResponseChars": raw_response_chars,
                            "parsedSectionChars": parsed_section_chars,
                            "postCodeBlockChars": post_code_block_chars,
                            "postCitationChars": post_citation_chars,
                            "postSourceRoleChars": post_source_role_chars,
                            "postUnsupportedDropChars": post_unsupported_drop_chars,
                            "postSourceUrlStripChars": len(section),
                            "postRestoreChars": len(section),
                            "citationRepairCount": citation_repairs,
                            "unverifiedCodeBlockDropCount": unverified_code_block_drops,
                            "sourceRoleRepairCount": source_role_repairs,
                            "unsupportedUnitDropCount": unsupported_unit_drops,
                            "unsupportedUnitReasons": dict(
                                sorted(unsupported_unit_reason_counts.items())
                            ),
                            "restoredClaimCount": restored_claims,
                            "sourceUrlStripCount": source_url_strips,
                            "effectiveChars": research_effective_answer_chars(
                                {"answer": section}
                            ),
                            "minimumAcceptableChars": int(
                                task.get("minimumAcceptableChars") or 0
                            ),
                            "issues": list(section_issues)[:8],
                        }
                    )
                last_section_issues = list(section_issues)
                if not complete:
                    finish_reason = _architect_response_finish_reason(response)
                    if finish_reason:
                        section_issues[0] = f"section_incomplete:{finish_reason}"
                if not section_issues:
                    return int(task.get("sequence") or 0), section, attempt_index, local_errors, 1
                local_errors.extend(f"{section_id}: {issue}" for issue in section_issues)
            children: list[dict[str, Any]] = []
            splittable_failure = bool(
                len(assigned_claims) > 1
                and any(
                    marker in issue
                    for issue in local_errors
                    for marker in (
                        "section_incomplete",
                        "section_depth_not_met",
                        "section_unsupported_hard_fact",
                        "runtime_unsupported_unit_drops",
                        ": timeout",
                    )
                )
            )
            if split_depth < 2 and splittable_failure:
                with split_slots_lock:
                    if split_slots > 0:
                        children = _split_architect_section_task(task)
                        if children:
                            split_slots -= 1
            if children and remaining_seconds() > review_reserve + alternate_writer_reserve + 8:
                child_sections: list[str] = []
                child_revisions = 1
                child_count = 0
                for child in children:
                    _sequence, child_section, revisions, child_errors, produced_count = generate_section(
                        child,
                        split_depth=split_depth + 1,
                    )
                    local_errors.extend(child_errors)
                    if not child_section:
                        return int(task.get("sequence") or 0), "", child_revisions, local_errors, 0
                    child_sections.append(child_section)
                    child_revisions += revisions
                    child_count += produced_count
                return (
                    int(task.get("sequence") or 0),
                    "\n\n".join(child_sections),
                    child_revisions,
                    local_errors,
                    child_count,
                )
            return int(task.get("sequence") or 0), "", 0, local_errors, 0

        section_results: dict[int, str] = {
            int(sequence): _safe_text(section)
            for sequence, section in dict(accepted_sections or {}).items()
            if 1 <= int(sequence) <= len(tasks) and _safe_text(section)
        }
        pending_tasks = [
            task for task in tasks if int(task.get("sequence") or 0) not in section_results
        ]
        revision_count = 0
        produced_section_count = len(section_results)
        errors: list[str] = []
        warm_base_model = getattr(candidate[0], "_get_base_model", None)
        if pending_tasks and callable(warm_base_model):
            try:
                warm_base_model()
            except Exception as exc:  # noqa: BLE001 - expose provider setup failure before parallel calls.
                errors.append(
                    f"{model_label}: segmented_writer_prewarm_{type(exc).__name__}: {_safe_text(exc)[:180]}"
                )
                return "", revision_count, produced_section_count, errors, section_results
        with ThreadPoolExecutor(max_workers=min(2, len(pending_tasks) or 1)) as executor:
            futures = [executor.submit(generate_section, task) for task in pending_tasks]
            for future in as_completed(futures):
                try:
                    sequence, section, revisions, section_errors, produced_count = future.result()
                except Exception as exc:  # noqa: BLE001 - never deliver a partial segmented answer.
                    errors.append(f"{model_label}: segmented_section_{type(exc).__name__}: {_safe_text(exc)[:180]}")
                    continue
                errors.extend(section_errors)
                if section:
                    section_results[sequence] = section
                    revision_count += revisions
                    produced_section_count += produced_count
        if len(section_results) != len(tasks):
            errors.append(f"{model_label}: segmented_section_coverage:{len(section_results)}/{len(tasks)}")
            return "", revision_count, produced_section_count, errors, section_results
        ordered_sections = [section_results[index] for index in range(1, len(tasks) + 1)]
        assembled = _assemble_architect_sections(
            question=question,
            verified_plan=verified_plan,
            sections=ordered_sections,
            sources=compact_sources,
        )
        assembled, repeated_unit_drops = _architect_dedupe_repeated_content_units(assembled)
        if repeated_unit_drops:
            errors.append(f"{model_label}: runtime_repeated_unit_drops:{repeated_unit_drops}")
        if len(assembled) > _MAX_RESEARCH_VISIBLE_ANSWER_CHARS:
            errors.append(f"{model_label}: answer_exceeds_delivery_surface_limit")
            return "", revision_count, produced_section_count, errors, section_results
        structural_issues = answer_structural_issues(assembled)
        if structural_issues:
            errors.append(f"{model_label}: " + ",".join(structural_issues[:8]))
            return "", revision_count, produced_section_count, errors, section_results
        return assembled, revision_count, produced_section_count, errors, section_results

    writer_mode = "single"
    writer_runtime_fallback = False
    writer_section_count = 0
    writer_attempts: list[dict[str, Any]] = []
    accepted_segmented_sections: dict[int, str] = {}
    segmented_contributor_count = 0
    segmented_contract_profile: dict[str, Any] | None = None
    productive_writer_candidates: list[tuple[float, int, int, tuple[Any, str, str]]] = []
    deterministic_answer = ""

    def stable_segmented_profile(profile: dict[str, Any]) -> dict[str, Any]:
        nonlocal segmented_contract_profile
        if segmented_contract_profile is None:
            segmented_contract_profile = {
                "sectionCount": int(profile.get("sectionCount") or 4),
                "targetMinChars": int(profile.get("targetMinChars") or 0),
                "targetMaxChars": int(profile.get("targetMaxChars") or 0),
            }
        return {
            **profile,
            **segmented_contract_profile,
            "enabled": True,
        }

    # Plan provenance must not choose the writer policy. A Runtime-recovered,
    # exact-excerpt plan is just as safe to hand to the normal model writer as
    # an Architect-authored plan. This preserves provider-adaptive segmented
    # writing; the deterministic claim report remains a fail-safe only after
    # every configured writer has actually failed.
    for writer_index, candidate in enumerate(writer_candidates):
        model_label = _architect_candidate_identity(candidate) or candidate[1] or f"role:{candidate[2]}"
        segmented_profile = _architect_segmented_writer_profile(candidate)
        if segmented_profile.get("enabled") or accepted_segmented_sections:
            effective_segmented_profile = stable_segmented_profile(segmented_profile)
            attempt_started_at = time.perf_counter()
            starting_remaining = remaining_seconds()
            prior_accepted_count = len(accepted_segmented_sections)
            future_writer_count = max(0, len(writer_candidates) - writer_index - 1)
            target_task_count = len(
                _architect_segment_tasks(
                    verified_plan,
                    section_count=int(effective_segmented_profile.get("sectionCount") or 4),
                    target_min_chars=int(effective_segmented_profile.get("targetMinChars") or 0),
                    target_max_chars=int(effective_segmented_profile.get("targetMaxChars") or 0),
                )
            )
            alternate_reserve = (
                min(45.0, max(0.0, starting_remaining - review_reserve - 10.0))
                if future_writer_count
                else 0.0
            )
            segmented_answer, revisions, section_count, segmented_errors, accepted_sections = write_segmented_answer(
                candidate,
                effective_segmented_profile,
                accepted_sections=accepted_segmented_sections,
                alternate_writer_reserve=alternate_reserve,
            )
            accepted_segmented_sections = accepted_sections
            new_accepted_count = max(0, len(accepted_sections) - prior_accepted_count)
            elapsed_ms = int((time.perf_counter() - attempt_started_at) * 1000)
            if new_accepted_count:
                segmented_contributor_count += 1
                productive_writer_candidates.append(
                    (
                        new_accepted_count / max(1, elapsed_ms),
                        new_accepted_count,
                        -writer_index,
                        candidate,
                    )
                )
            writer_attempts.append(
                {
                    "modelId": model_label,
                    "selectionOrigin": _architect_candidate_selection_origin(candidate),
                    "mode": "segmented",
                    "startedWithRemainingSeconds": round(starting_remaining, 1),
                    "elapsedMs": elapsed_ms,
                    "acceptedSectionCount": len(accepted_sections),
                    "newAcceptedSectionCount": new_accepted_count,
                    "targetSectionCount": target_task_count,
                    "failureCodes": list(
                        dict.fromkeys(
                            error[:180]
                            for error in segmented_errors
                            if "unsupported_unit_preview" not in error
                        )
                    )[-8:],
                }
            )
            candidate_errors.extend(segmented_errors)
            if segmented_answer:
                answer = segmented_answer
                writer_candidate = candidate
                writer_revision_count += revisions
                writer_mode = (
                    "segmented_candidate_handoff"
                    if segmented_contributor_count > 1
                    else "segmented"
                )
                writer_section_count = section_count
                break
            continue
        writer_instruction = writer_prompt
        observed_truncation = False
        for attempt_index in range(2):
            remaining = remaining_seconds()
            if remaining <= review_reserve + 1:
                candidate_errors.append("architect_review_budget_reserved")
                break
            remaining_writers = max(1, len(writer_candidates) - writer_index)
            per_call_budget = min(
                call_timeout_cap_seconds,
                105.0,
                max(2.0, (remaining - review_reserve) / min(2, remaining_writers)),
            )
            messages = prepared_messages(
                candidate,
                system_prompt=writer_system_prompt,
                instruction=writer_instruction,
                materials=[
                    {"title": "Verified evidence plan", "kind": "research_plan", "content": json.dumps(verified_plan, ensure_ascii=False)},
                    {"title": "Research evidence candidates", "kind": "research_sources", "content": source_material},
                ],
                target_role="web-research-answer-writer",
                node="web_research_architect_answer_revision" if attempt_index else "web_research_architect_answer",
            )
            try:
                response = _invoke_architect_candidate_with_deadline(
                    candidate,
                    messages,
                    seconds=per_call_budget,
                    max_tokens=_RESEARCH_ARCHITECT_ANSWER_MAX_TOKENS,
                    disable_thinking=True,
                )
            except concurrent.futures.TimeoutError:
                candidate_errors.append(f"{model_label}: architect_answer_timeout")
                if remaining_seconds() > review_reserve + 12:
                    fallback_profile = stable_segmented_profile(segmented_profile)
                    segmented_answer, revisions, section_count, segmented_errors, _accepted_sections = write_segmented_answer(
                        candidate,
                        fallback_profile,
                    )
                    candidate_errors.extend(segmented_errors)
                    if segmented_answer:
                        answer = segmented_answer
                        writer_candidate = candidate
                        writer_revision_count += revisions
                        writer_mode = "segmented_after_timeout"
                        writer_section_count = section_count
                break
            except Exception as exc:  # noqa: BLE001 - try the next configured writer model.
                candidate_errors.append(f"{model_label}: architect_answer_{type(exc).__name__}: {_safe_text(exc)[:220]}")
                if remaining_seconds() > review_reserve + 12:
                    fallback_profile = stable_segmented_profile(segmented_profile)
                    segmented_answer, revisions, section_count, segmented_errors, _accepted_sections = write_segmented_answer(
                        candidate,
                        fallback_profile,
                    )
                    candidate_errors.extend(segmented_errors)
                    if segmented_answer:
                        answer = segmented_answer
                        writer_candidate = candidate
                        writer_revision_count += revisions
                        writer_mode = "segmented_after_error"
                        writer_section_count = section_count
                break
            raw_answer = sanitize_background_model_output(response).text
            candidate_answer, complete = _research_answer_from_model_output(raw_answer)
            finish_reason = _architect_response_finish_reason(response)
            structural_issues: list[str] = []
            if not complete:
                structural_issues.append(
                    "architect_answer_incomplete" + (f":{finish_reason}" if finish_reason else "")
                )
                observed_truncation = finish_reason in {"length", "max_tokens", "max_output_tokens"} or bool(candidate_answer)
            if len(candidate_answer) > _MAX_RESEARCH_VISIBLE_ANSWER_CHARS:
                structural_issues.append("answer_exceeds_delivery_surface_limit")
            if complete and len(candidate_answer) <= _MAX_RESEARCH_VISIBLE_ANSWER_CHARS:
                structural_issues.extend(answer_structural_issues(candidate_answer))
            if not structural_issues:
                answer = candidate_answer
                writer_candidate = candidate
                writer_revision_count += attempt_index
                break
            candidate_errors.append(f"{model_label}: " + ",".join(structural_issues[:8]))
            if observed_truncation and remaining_seconds() > review_reserve + 12:
                fallback_profile = stable_segmented_profile(segmented_profile)
                segmented_answer, revisions, section_count, segmented_errors, _accepted_sections = write_segmented_answer(
                    candidate,
                    fallback_profile,
                )
                candidate_errors.extend(segmented_errors)
                if segmented_answer:
                    answer = segmented_answer
                    writer_candidate = candidate
                    writer_revision_count += revisions
                    writer_mode = "segmented_after_truncation"
                    writer_section_count = section_count
                break
            if attempt_index or not candidate_answer or remaining_seconds() <= review_reserve + 8:
                break
            writer_instruction = (
                "完整重写上一份候选答案，不要补写附录，也不要解释门禁。保留所有正确事实和引用，修复以下 Runtime 门禁："
                + ", ".join(structural_issues[:8])
                + f"。正常目标仍是 {_RESEARCH_ARCHITECT_IDEAL_ANSWER_MIN_CHARS}-{_RESEARCH_ARCHITECT_IDEAL_ANSWER_MAX_CHARS} 个去空白有效字符，"
                + "每个重要结论在对应内容单元内放置正确 [S#]；完成来源列表后必须输出完成标记。\n"
                + f"QUESTION: {question}\nPREVIOUS_DRAFT:\n{candidate_answer[:_MAX_RESEARCH_VISIBLE_ANSWER_CHARS]}"
            )
        if answer:
            break

    # Candidate handoff can leave one valid section missing after preserving
    # all other work. Give the most productive writer one bounded final pass
    # over that section only; the existing helper keeps every evidence and
    # section gate in force and stops at the independent-review reserve.
    if (
        not answer
        and accepted_segmented_sections
        and segmented_contract_profile is not None
        and productive_writer_candidates
    ):
        tail_tasks = _architect_segment_tasks(
            verified_plan,
            section_count=int(segmented_contract_profile.get("sectionCount") or 4),
            target_min_chars=int(segmented_contract_profile.get("targetMinChars") or 0),
            target_max_chars=int(segmented_contract_profile.get("targetMaxChars") or 0),
        )
        missing_sequences = [
            int(task.get("sequence") or 0)
            for task in tail_tasks
            if int(task.get("sequence") or 0) not in accepted_segmented_sections
        ]
        if len(missing_sequences) == 1 and remaining_seconds() > review_reserve + 8:
            tail_candidate = max(productive_writer_candidates, key=lambda item: item[:3])[3]
            tail_label = (
                _architect_candidate_identity(tail_candidate)
                or tail_candidate[1]
                or f"role:{tail_candidate[2]}"
            )
            tail_profile = stable_segmented_profile(
                _architect_segmented_writer_profile(tail_candidate)
            )
            tail_started_at = time.perf_counter()
            tail_starting_remaining = remaining_seconds()
            prior_tail_count = len(accepted_segmented_sections)
            (
                tail_answer,
                tail_revisions,
                tail_section_count,
                tail_errors,
                tail_sections,
            ) = write_segmented_answer(
                tail_candidate,
                tail_profile,
                accepted_sections=accepted_segmented_sections,
            )
            accepted_segmented_sections = tail_sections
            writer_attempts.append(
                {
                    "modelId": tail_label,
                    "selectionOrigin": _architect_candidate_selection_origin(tail_candidate),
                    "mode": "segmented_tail_recovery",
                    "startedWithRemainingSeconds": round(tail_starting_remaining, 1),
                    "elapsedMs": int((time.perf_counter() - tail_started_at) * 1000),
                    "missingSectionSequencesBefore": missing_sequences,
                    "acceptedSectionCount": len(tail_sections),
                    "newAcceptedSectionCount": max(0, len(tail_sections) - prior_tail_count),
                    "targetSectionCount": len(tail_tasks),
                    "accepted": bool(tail_answer),
                    "failureCodes": list(
                        dict.fromkeys(
                            error[:180]
                            for error in tail_errors
                            if "unsupported_unit_preview" not in error
                        )
                    )[-8:],
                }
            )
            candidate_errors.extend(tail_errors)
            if tail_answer:
                answer = tail_answer
                writer_candidate = tail_candidate
                writer_revision_count += tail_revisions
                writer_mode = "segmented_tail_recovered"
                writer_section_count = tail_section_count

    if not answer or writer_candidate is None:
        deterministic_started_at = time.perf_counter()
        deterministic_answer = _assemble_architect_claim_report(
            question=question,
            verified_plan=verified_plan,
            sources=compact_sources,
        )
        deterministic_answer, deterministic_repeat_drops = _architect_dedupe_repeated_content_units(
            deterministic_answer
        )
        deterministic_issues: list[str] = []
        if deterministic_repeat_drops:
            candidate_errors.append(
                f"runtime_deterministic_claim_report_repeated_unit_drops:{deterministic_repeat_drops}"
            )
        if len(deterministic_answer) > _MAX_RESEARCH_VISIBLE_ANSWER_CHARS:
            deterministic_issues.append("answer_exceeds_delivery_surface_limit")
        else:
            deterministic_issues.extend(answer_structural_issues(deterministic_answer))
        writer_attempts.append(
            {
                "modelId": "runtime:verified-claim-report",
                "selectionOrigin": "runtime_deterministic_fallback",
                "mode": "deterministic_claim_report_after_writer",
                "startedWithRemainingSeconds": round(remaining_seconds(), 1),
                "elapsedMs": int((time.perf_counter() - deterministic_started_at) * 1000),
                "accepted": not deterministic_issues,
                "failureCodes": list(dict.fromkeys(deterministic_issues))[-8:],
            }
        )
        if not deterministic_issues:
            answer = deterministic_answer
            writer_candidate = plan_candidate
            writer_mode = "deterministic_claim_report_after_writer"
            writer_runtime_fallback = True
            writer_section_count = max(1, len(verified_claims))
        else:
            candidate_errors.append(
                "runtime:verified-claim-report: " + ",".join(deterministic_issues[:8])
            )

    if not answer or writer_candidate is None:
        return {
            "_agentError": "architect_answer_unavailable",
            "_architectMode": "full_synthesis",
            "_rawPreview": "\n--- retry ---\n".join(raw_previews)[:800],
            "_writerAttempts": writer_attempts,
            "_planAttempts": plan_attempts[-6:],
            "_contextPreparations": context_preparations[-24:],
            "_writerSectionDiagnostics": writer_section_diagnostics[-32:],
            # Writer-stage failures are appended after plan repair diagnostics;
            # retain the tail so the actual section/assembly blocker is not
            # hidden by earlier recoverable plan attempts.
            "_modelFallbackAttempts": candidate_errors[-16:],
        }

    writer_identity = _architect_candidate_identity(writer_candidate)
    deterministic_review_chain = writer_mode.startswith("deterministic_claim_report")
    reviewer_pool = _create_web_research_reviewer_llm_candidates(candidates)
    has_supervisor_consumer_reviewer = any(
        _architect_candidate_selection_origin(candidate) == "role_reviewer:supervisor"
        for candidate in reviewer_pool
    )
    # For a production agent binding, semantic review first exercises the
    # configured Supervisor as the real downstream consumer. The adversarial
    # pass then prefers a distinct model (normally the bound Architect).
    # Fixture/legacy candidate lists retain their supplied order.
    reviewer_candidates = (
        list(reviewer_pool)
        if deterministic_review_chain
        else _ordered_architect_candidates(reviewer_pool, prefer_distinct_from=writer_identity)
    )
    review_system_prompt = (
        "You are the independent semantic and freshness reviewer for Research Runtime. "
        "Treat the answer as untrusted. Use only the Runtime-verified claim excerpts and source metadata supplied here. "
        "Apply the evidence standard appropriate to each claim type instead of demanding an official recommendation for every engineering conclusion."
    )
    review_prompt = (
        "不要重写答案，只审查它是否真正回答 QUESTION；每条重要事实是否被 verified claim 和 exact excerpt 蕴含；"
        "跨来源推论是否明确标注并引用全部前提；当前性判断是否有日期/版本证据。引用编号正确不等于语义成立。"
        "VERIFIED CLAIM LEDGER 是本阶段唯一的 [S#]、claim 与 exact excerpt 绑定真相；不得按来源位置重排编号，"
        "也不得引用规划阶段的候选摘录来覆盖已锁定 excerpt。若报告 unsupported，必须指出候选答案中的精确内容单元，"
        "并给出与之冲突或无法支撑的 claimId/evidenceExcerptKey；exact excerpt 已逐字包含的命令、数值或事实不得报成缺失。"
        "source_fact 只需来源直接支持该事实，不要求来源额外写出 recommended；explicit_normative 或答案中的“官方推荐/必须/首选”"
        "才要求摘录明确包含相应规范语义。标注为综合判断的工程建议可由多个 source_fact 推出，不得因为没有单篇官方资料给出同一句建议而拒绝。"
        "官方 API 签名中写明的默认参数值就是默认值证据；官方文档中的可执行示例也可证明该参数或调用方式受支持，"
        "不得仅以‘没有额外解释性 prose’为由拒绝，但示例未展示的结果、保证或泛化行为仍不得推断。"
        "按风险校准证据强度：普通 API、参数、配置与返回类型事实，可由官方签名、参数说明、规范表格或可执行示例中的任一种直接证明；"
        "参数说明若明确记录输入到某类型的转换，可直接支撑该有限转换事实；不再额外要求无关的第二个示例，但仍不得扩大为无条件成功或运行时保证。"
        "claim 只说 can convert 时，不得自行补出 always succeeds/无条件保证再据此拒绝；claim 与 exact excerpt 归一化后相同，也不得反称 claim 遗漏了 excerpt 中的细节。"
        "一个事实 claim 保守列出签名中的参数子集，不因没有复述所有默认值而自动变成 unsupported；默认值是否需要写进最终答案应按用户问题的重要性判断。"
        "Markdown 转义下划线不使签名失效。显著标注的跨来源工程综合，只需每个前提分别核验、引用齐全且推论保守，不要求单篇来源逐字给出组合结论。"
        "来源标题、页面身份与段落上下文可以为保守改写提供范围；不要求摘录在每句里重复页面主题。"
        "从来源句中省略一个仍为真的附加从句属于语义收窄；只要保留的结论被原句直接蕴含，就不是 unsupported。"
        "只有最新/全部/绝不/不存在能力、经验性性能数字、安全、法律或无条件保证等高风险主张，才要求更强的逐项证据；"
        "API 签名中的布尔值、默认值、版本号和普通配置数字仍是普通接口事实，可由官方签名或参数表直接证明。"
        "面对范围模糊的问题，应检查答案是否显式列出合理假设、适用分支和边界；不得仅因来源没有逐字复述用户的模糊总问题而拒绝。"
        "官方文档的 Tip 若逐字写明 Prefer/推荐，也属于该来源自身的 explicit_normative 语义；不得仅因视觉标签叫 Tip 就否认推荐词，"
        "但仍须保持其框架、版本和原句范围。"
        "tier=secondary 不能单独证明官方规范、API 保证或弃用状态，但可证明该来源自身的实现、案例或明确署名经验；"
        "二手来源的 explicit_normative 可以保留为该教程/作者的明确署名建议；只要答案和 claimTable 都保持该归属，"
        "就不能仅因它不是官方建议而拒绝，但也不得把它升级成官方规则或让它单独支撑通用 API 保证。"
        "`Secondary source “标题” states:`、`Mixed-source synthesis with secondary context:`、"
        "`二手来源《标题》的表述：` 都是明确归因；不得把带这些前缀的内容误报为无归因。若仍发现遗漏，必须指出"
        "不带归因前缀的精确内容单元，不能只罗列来源编号。证据时效段若已明确列出日期、版本、stable/current 路由或 undated 状态，"
        "不得按固定年龄自动拒绝；只有正文超出这些边界、把上下文升级为无证据的当前保证时才拒绝。"
        "时效充分性由你结合问题风险、领域变化速度、发布/更新/版本/适用状态和本轮检索时间做语义判断，"
        "不得套用固定年份或固定 dated-source 数量门槛。retrievedAt 只证明 Runtime 何时读取该页面，不能把旧正文变成最新事实；"
        "但来源已清楚标出 undated/version-bound/dated/stable-route 状态和检索时间时，也不得仅因缺少发布日期自动拒绝。"
        "使用中等强度而非法律/合规尽调强度：宽泛或模糊的比较请求只要求核心差异、适用条件、明确边界和可执行选择足以回答用户，"
        "不能把“比较安装/价格/隐私”擅自升级成每款产品都必须给出全部安装命令、最低 OS/Node 版本、每档精确金额或穷尽式功能清单。"
        "若答案已清楚标注某个次要细节本轮未核实，同时核心结论仍由已验证证据成立，应把它当限制而不是 criticalMissingEvidence。"
        "只有缺失事实会实质改变核心选型、使答案误导，或让用户问题无法负责任地回答时，才要求新增证据；"
        "答案实际写入未被 ledger 支撑的命令、版本、价格、能力或保证仍必须拒绝。"
        "与问题相关的背景/互操作事实若被清楚标为非可执行前提，可以保留以解释边界；不得仅因它没有参与每一条行动建议而拒绝，"
        "也不得在答案已明确否认某种跨框架含义时反向推定该含义存在。"
        "只输出严格 JSON：reviewDecision, reviewReasons, questionCoverage, claimEntailment, freshnessAdequacy, "
        "unsupportedClaims, criticalMissingEvidence, recommendedNextQueries。若不是 accept，先区分写作问题与证据缺口。"
        "questionCoverage、claimEntailment、freshnessAdequacy 必须各自直接输出 JSON 布尔值 true/false，"
        "不得改成对象、逐 claim 数组或说明文字；逐 claim 说明应放进 reviewReasons/unsupportedClaims。"
        "只有确实缺少新证据时才填写 criticalMissingEvidence 和 recommendedNextQueries；若问题可仅靠同一证据重写、修正归因或补清晰标注，"
        "这两项必须保持空数组，并在 reviewReasons/unsupportedClaims 中说明。需要新证据时，recommendedNextQueries 最多4项且必须是可直接交给搜索工具的具体查询，优先官方一手资料。"
        "查询必须直接写关键词或 site: 条件，不得以 Fetch、Retrieve、Read、Open 或同义祈使词开头。"
        "保持紧凑：reviewReasons 最多4项、unsupportedClaims 最多6项、criticalMissingEvidence 最多4项，每项不超过160字符。"
        "只有三个布尔值均为 true，且 unsupportedClaims、criticalMissingEvidence、recommendedNextQueries 均为空时才能 accept。\n"
        f"QUESTION: {question}"
    )
    adversarial_review_prompt = (
        "\n这是第二道对抗性审查。逐句、逐表格单元和逐代码块寻找反例，不得沿用第一位 Reviewer 的结论。"
        "重点拒绝：claimTable/exact excerpt 中没有出现的 API、类名、参数、配置、版本、数值或代码；"
        "把相关引用当作新增事实背书；把未找到证据写成不存在能力；把机制描述扩大成性能提升；"
        "以及同一答案中互相矛盾的陈述。蕴含判断是语义判断，不是逐字匹配：不得仅因保守改写使用同义词就拒绝；"
        "语义等价、范围更窄的改写可保守接受；不得把 claim 没有说的实体、范围、保证或行为补回答案再据此扩展。"
        "只有改写新增了实体、范围、保证或行为时才算 unsupported。"
        "只要存在一项真实新增，就必须在 unsupportedClaims 中精确指出，不能 accept。"
    )
    def invoke_independent_review(
        candidate: tuple[Any, str, str],
        candidate_answer: str,
        *,
        seconds: float,
        node: str,
        review_mode: str,
        retry_reason: str = "",
    ) -> tuple[dict[str, Any], str]:
        model_label = _architect_candidate_identity(candidate) or candidate[1] or f"role:{candidate[2]}"
        review_candidate_payload = {
            "answer": candidate_answer,
            "asOf": verified_plan["asOf"],
            "temporalAssessment": verified_plan.get("temporalAssessment") or {},
            "namedDecisionAudiences": verified_plan.get("namedDecisionAudiences") or [],
            "requiredDeliverables": verified_plan.get("requiredDeliverables") or [],
        }
        review_claim_ledger = _architect_review_claim_ledger(
            verified_claims,
            compact_sources,
        )
        retry_instruction = ""
        if retry_reason:
            retry_instruction = (
                "\n上一次 Reviewer 调用没有产生可验证的结构化结果，这不是对答案的实质性否决。"
                "请重新独立审查同一候选答案，并严格满足输出 schema：reviewReasons、unsupportedClaims、"
                "criticalMissingEvidence、recommendedNextQueries 必须是 JSON 数组，空值写 []，绝不能写 null；"
                "questionCoverage、claimEntailment、freshnessAdequacy 必须是 JSON 布尔值。只输出一个 JSON 对象。"
            )
        messages = prepared_messages(
            candidate,
            system_prompt=review_system_prompt,
            instruction=(
                review_prompt
                + (adversarial_review_prompt if review_mode == "adversarial" else "")
                + retry_instruction
            ),
            materials=[
                {
                    "title": "Canonical verified claim ledger",
                    "kind": "research_review_claim_ledger",
                    "content": json.dumps(review_claim_ledger, ensure_ascii=False),
                },
                {"title": "Candidate answer", "kind": "research_review_candidate", "content": json.dumps(review_candidate_payload, ensure_ascii=False)},
            ],
            target_role="web-research-independent-reviewer",
            node=node,
        )
        try:
            response = _invoke_architect_candidate_with_deadline(
                candidate,
                messages,
                seconds=seconds,
                max_tokens=_RESEARCH_ARCHITECT_REVIEW_MAX_TOKENS,
                disable_thinking=True,
            )
        except concurrent.futures.TimeoutError:
            return {}, f"{model_label}: independent_review_timeout"
        except Exception as exc:  # noqa: BLE001 - caller may try another independent reviewer.
            return {}, f"{model_label}: independent_review_{type(exc).__name__}: {_safe_text(exc)[:220]}"
        sanitized_review_text = sanitize_background_model_output(response).text
        parsed_review = _extract_json_object(sanitized_review_text)
        if not _independent_architect_review_schema_valid(parsed_review):
            compact_preview = re.sub(r"\s+", " ", sanitized_review_text).strip()[:180]
            return {}, (
                f"{model_label}: independent_review_invalid_schema"
                + (f":{compact_preview}" if compact_preview else "")
            )
        normalized_review = {
            **_normalize_independent_architect_review(parsed_review),
            "reviewMode": review_mode,
        }
        reclassified_review = _architect_reclassify_review_evidence_gaps(
            normalized_review,
            question=question,
        )
        return _architect_reconcile_review_with_answer_surface(
            reclassified_review,
            question=question,
            candidate_answer=candidate_answer,
            claim_table=verified_claims,
        ), ""

    def run_review_consensus(
        candidate_answer: str,
        *,
        node_prefix: str,
    ) -> tuple[
        list[tuple[dict[str, Any], tuple[Any, str, str], str]],
        dict[str, Any],
        tuple[Any, str, str] | None,
    ]:
        accepted: list[tuple[dict[str, Any], tuple[Any, str, str], str]] = []
        for review_mode in ("semantic", "adversarial"):
            ordered = list(reviewer_candidates)
            if accepted and (
                has_supervisor_consumer_reviewer or not deterministic_review_chain
            ):
                ordered = _ordered_architect_candidates(
                    reviewer_candidates,
                    prefer_distinct_from=_architect_candidate_identity(accepted[-1][1]),
                )
            mode_accepted = False
            for candidate in ordered:
                retry_reason = ""
                for review_attempt in range(2):
                    remaining = remaining_seconds()
                    remaining_modes = 2 - len(accepted)
                    if remaining <= max(4.0, remaining_modes * 2.0):
                        candidate_errors.append("architect_total_timeout")
                        break
                    per_call_budget = min(
                        call_timeout_cap_seconds,
                        32.0,
                        max(3.0, (remaining - 2.0) / max(1, remaining_modes)),
                    )
                    review, review_error = invoke_independent_review(
                        candidate,
                        candidate_answer,
                        seconds=per_call_budget,
                        node=(
                            f"{node_prefix}_{review_mode}"
                            if review_attempt == 0
                            else f"{node_prefix}_{review_mode}_retry"
                        ),
                        review_mode=review_mode,
                        retry_reason=retry_reason,
                    )
                    if review_error:
                        candidate_errors.append(review_error)
                        retry_reason = review_error
                        retry_budget_available = remaining_seconds() > max(
                            6.0,
                            remaining_modes * 3.0 + 3.0,
                        )
                        if review_attempt == 0 and retry_budget_available:
                            continue
                        break
                    if not _independent_architect_review_accepts(review):
                        model_label = _architect_candidate_identity(candidate) or candidate[1] or f"role:{candidate[2]}"
                        reasons = review.get("reviewReasons") or ["independent_semantic_review_rejected"]
                        candidate_errors.append(f"{model_label}: " + ",".join(str(value) for value in reasons[:4]))
                        # A substantive rejection vetoes the candidate answer. It
                        # must be revised or sent back for more evidence; another
                        # reviewer cannot override it.
                        return [], review, candidate
                    accepted.append((review, candidate, review_mode))
                    mode_accepted = True
                    break
                if mode_accepted:
                    break
            if not mode_accepted:
                return [], {}, None
        return accepted, {}, None

    independent_review: dict[str, Any] = {}
    reviewer_candidate: tuple[Any, str, str] | None = None
    accepted_review_passes, best_failed_review, failed_reviewer_candidate = run_review_consensus(
        answer,
        node_prefix="web_research_independent_review",
    )
    if accepted_review_passes:
        independent_review = dict(accepted_review_passes[0][0])
        reviewer_candidate = accepted_review_passes[0][1]

    review_critical_missing = _research_text_list(best_failed_review.get("criticalMissingEvidence"), limit=12)
    review_queries = _research_text_list(best_failed_review.get("recommendedNextQueries"), limit=4)
    if (
        not independent_review
        and best_failed_review
        and writer_candidate is not None
        and failed_reviewer_candidate is not None
        and not review_critical_missing
        and not review_queries
        and remaining_seconds() > review_reserve + 16
    ):
        revision_feedback = _research_text_list(
            [
                *list(best_failed_review.get("reviewReasons") or []),
                *list(best_failed_review.get("unsupportedClaims") or []),
            ],
            limit=10,
        )
        writer_label = _architect_candidate_identity(writer_candidate) or writer_candidate[1]
        revised_answer = ""
        revision_issues: list[str] = []
        revision_prompt = (
            "同一独立 Reviewer 已拒绝上一稿，但没有要求新增证据。请只用同一 VERIFIED PLAN 与 SOURCES 完整重写答案，"
            "逐项修复 Reviewer 意见；不得添加新事实、不得写附录式补丁。二手来源的每个事实必须明确写成该教程/文章"
            "自己的表述并点名来源；跨来源建议必须明确标成‘本报告的综合判断’或‘practical synthesis’，且在同一内容"
            "单元引用全部 premise 来源。删除没有证据支持的优先级、最高杠杆、基础性决策、独立类别、等价接口、"
            "无条件保证与泛化跨平台断言。Reviewer 指出的 unsupported claim 必须整句删除或收窄到对应 claim，"
            "不得只换同义词保留原关系；未标年份的月日必须写明‘年份未标明’，不得套用当前年份。完成来源列表后输出完成标记。\n"
            + "REVIEW_FEEDBACK: "
            + json.dumps(revision_feedback, ensure_ascii=False)
            + f"\nQUESTION: {question}\nPREVIOUS_DRAFT:\n{answer[:_MAX_RESEARCH_VISIBLE_ANSWER_CHARS]}"
        )
        try:
            if writer_mode.startswith("segmented"):
                revision_candidates = sorted(
                    writer_candidates,
                    key=lambda candidate: int(
                        _architect_segmented_writer_profile(candidate).get("configuredMaxTokens") or 0
                    ),
                    reverse=True,
                )
                accepted_revision_sections: dict[int, str] = {}
                section_revisions = 0
                section_count = 0
                revision_contributor_count = 0
                # A high-output alternate can revise the assembled answer more
                # coherently and cheaply than regenerating every accepted
                # section.  Small-output providers keep the segmented path.
                for full_revision_candidate in list(revision_candidates):
                    full_profile = _architect_segmented_writer_profile(full_revision_candidate)
                    configured_max_tokens = int(full_profile.get("configuredMaxTokens") or 0)
                    if configured_max_tokens < _RESEARCH_ARCHITECT_COHERENT_REVISION_MIN_TOKENS:
                        continue
                    full_revision_remaining = remaining_seconds()
                    review_budget_floor = max(
                        45.0,
                        min(review_reserve, full_revision_remaining * 0.55),
                    )
                    full_revision_budget = min(
                        call_timeout_cap_seconds,
                        45.0,
                        max(0.0, full_revision_remaining - review_budget_floor - 4.0),
                    )
                    if full_revision_budget < 8.0:
                        break
                    full_revision_label = (
                        _architect_candidate_identity(full_revision_candidate)
                        or full_revision_candidate[1]
                        or f"role:{full_revision_candidate[2]}"
                    )
                    full_revision_started_at = time.perf_counter()
                    full_revision_errors: list[str] = []
                    messages = prepared_messages(
                        full_revision_candidate,
                        system_prompt=writer_system_prompt,
                        instruction=revision_prompt,
                        materials=[
                            {
                                "title": "Verified evidence plan",
                                "kind": "research_plan",
                                "content": json.dumps(verified_plan, ensure_ascii=False),
                            },
                            {
                                "title": "Research evidence candidates",
                                "kind": "research_sources",
                                "content": source_material,
                            },
                        ],
                        target_role="web-research-answer-writer",
                        node="web_research_reviewer_guided_full_revision",
                    )
                    try:
                        response = _invoke_architect_candidate_with_deadline(
                            full_revision_candidate,
                            messages,
                            seconds=full_revision_budget,
                            max_tokens=_RESEARCH_ARCHITECT_ANSWER_MAX_TOKENS,
                            disable_thinking=True,
                        )
                        full_answer, complete = _research_answer_from_model_output(
                            sanitize_background_model_output(response).text
                        )
                        if not complete:
                            finish_reason = _architect_response_finish_reason(response)
                            full_revision_errors.append(
                                "architect_answer_incomplete"
                                + (f":{finish_reason}" if finish_reason else "")
                            )
                        global_revision_task = {
                            "assignedClaims": verified_claims,
                            "compositeInferences": verified_plan.get("compositeInferences") or [],
                        }
                        full_answer, citation_repairs = _architect_repair_unambiguous_section_citations(
                            full_answer,
                            global_revision_task,
                        )
                        if citation_repairs:
                            full_revision_errors.append(
                                f"runtime_citation_repairs:{citation_repairs}"
                            )
                        full_answer, source_role_repairs = _architect_repair_source_role_labels(
                            full_answer,
                            global_revision_task,
                        )
                        if source_role_repairs:
                            full_revision_errors.append(
                                f"runtime_source_role_repairs:{source_role_repairs}"
                            )
                        unsupported_diagnostics: list[str] = []
                        full_answer, unsupported_drops = _architect_drop_unsupported_section_units(
                            full_answer,
                            global_revision_task,
                            diagnostics=unsupported_diagnostics,
                        )
                        if unsupported_drops:
                            full_revision_errors.append(
                                f"runtime_unsupported_unit_drops:{unsupported_drops}"
                            )
                        full_answer, repeated_unit_drops = _architect_dedupe_repeated_content_units(
                            full_answer
                        )
                        if repeated_unit_drops:
                            full_revision_errors.append(
                                f"runtime_repeated_unit_drops:{repeated_unit_drops}"
                            )
                        if len(full_answer) > _MAX_RESEARCH_VISIBLE_ANSWER_CHARS:
                            full_revision_errors.append("answer_exceeds_delivery_surface_limit")
                        if complete and len(full_answer) <= _MAX_RESEARCH_VISIBLE_ANSWER_CHARS:
                            full_revision_errors.extend(answer_structural_issues(full_answer))
                    except concurrent.futures.TimeoutError:
                        full_answer = ""
                        full_revision_errors.append("reviewer_guided_full_revision_timeout")
                    except Exception as exc:  # noqa: BLE001 - fall back to segmented revision.
                        full_answer = ""
                        full_revision_errors.append(
                            f"reviewer_guided_full_revision_{type(exc).__name__}: {_safe_text(exc)[:180]}"
                        )
                    hard_full_revision_errors = [
                        issue
                        for issue in full_revision_errors
                        if not issue.startswith("runtime_source_role_repairs:")
                        and not issue.startswith("runtime_unsupported_unit_drops:")
                        and not issue.startswith("runtime_citation_repairs:")
                        and not issue.startswith("runtime_repeated_unit_drops:")
                    ]
                    writer_attempts.append(
                        {
                            "modelId": full_revision_label,
                            "selectionOrigin": _architect_candidate_selection_origin(
                                full_revision_candidate
                            ),
                            "mode": "single_reviewer_revision",
                            "startedWithRemainingSeconds": round(full_revision_remaining, 1),
                            "elapsedMs": int((time.perf_counter() - full_revision_started_at) * 1000),
                            "accepted": bool(full_answer and not hard_full_revision_errors),
                            "failureCodes": list(dict.fromkeys(full_revision_errors))[-8:],
                        }
                    )
                    candidate_errors.extend(
                        f"{full_revision_label}: {issue}" for issue in hard_full_revision_errors
                    )
                    if full_answer and not hard_full_revision_errors:
                        revised_answer = full_answer
                        writer_candidate = full_revision_candidate
                        writer_label = full_revision_label
                        writer_identity = _architect_candidate_identity(writer_candidate)
                        reviewer_candidates = _ordered_architect_candidates(
                            candidates,
                            prefer_distinct_from=writer_identity,
                        )
                        writer_mode = "single_reviewer_revised"
                        section_count = 1
                        revision_candidates = []
                        break
                for revision_index, revision_candidate in enumerate(revision_candidates):
                    revision_label = (
                        _architect_candidate_identity(revision_candidate)
                        or revision_candidate[1]
                        or f"role:{revision_candidate[2]}"
                    )
                    revision_started_at = time.perf_counter()
                    revision_starting_remaining = remaining_seconds()
                    prior_revision_count = len(accepted_revision_sections)
                    future_revision_count = max(0, len(revision_candidates) - revision_index - 1)
                    revision_reserve = (
                        min(35.0, max(0.0, revision_starting_remaining - review_reserve - 8.0))
                        if future_revision_count
                        else 0.0
                    )
                    (
                        candidate_revised_answer,
                        candidate_section_revisions,
                        candidate_section_count,
                        segmented_errors,
                        accepted_sections,
                    ) = write_segmented_answer(
                        revision_candidate,
                        stable_segmented_profile(
                            _architect_segmented_writer_profile(revision_candidate)
                        ),
                        review_feedback=revision_feedback,
                        accepted_sections=accepted_revision_sections,
                        previous_sections=accepted_segmented_sections,
                        alternate_writer_reserve=revision_reserve,
                    )
                    accepted_revision_sections = accepted_sections
                    section_revisions += candidate_section_revisions
                    section_count = max(section_count, candidate_section_count)
                    if len(accepted_sections) > prior_revision_count:
                        revision_contributor_count += 1
                    writer_attempts.append(
                        {
                            "modelId": revision_label,
                            "selectionOrigin": _architect_candidate_selection_origin(
                                revision_candidate
                            ),
                            "mode": "segmented_reviewer_revision",
                            "startedWithRemainingSeconds": round(revision_starting_remaining, 1),
                            "elapsedMs": int((time.perf_counter() - revision_started_at) * 1000),
                            "acceptedSectionCount": len(accepted_sections),
                            "newAcceptedSectionCount": max(
                                0,
                                len(accepted_sections) - prior_revision_count,
                            ),
                            "failureCodes": list(
                                dict.fromkeys(
                                    error[:180]
                                    for error in segmented_errors
                                    if "unsupported_unit_preview" not in error
                                )
                            )[-8:],
                        }
                    )
                    candidate_errors.extend(segmented_errors)
                    if candidate_revised_answer:
                        revised_answer = candidate_revised_answer
                        writer_candidate = revision_candidate
                        writer_label = revision_label
                        writer_identity = _architect_candidate_identity(writer_candidate)
                        reviewer_candidates = _ordered_architect_candidates(
                            candidates,
                            prefer_distinct_from=writer_identity,
                        )
                        writer_mode = (
                            "segmented_reviewer_revised_handoff"
                            if revision_contributor_count > 1
                            else "segmented_reviewer_revised"
                        )
                        break
                if not revised_answer:
                    revision_issues.append("segmented_reviewer_guided_revision_failed")
                else:
                    writer_revision_count += 1 + section_revisions
                    writer_section_count = section_count
            else:
                revision_prompt = (
                    "同一独立 Reviewer 已拒绝上一稿，但没有要求新增证据。请只用同一 VERIFIED PLAN 与 SOURCES 完整重写答案，"
                    "逐项修复 Reviewer 意见；不得添加新事实、不得写附录式补丁。完成来源列表后输出完成标记。\n"
                    + "REVIEW_FEEDBACK: "
                    + json.dumps(revision_feedback, ensure_ascii=False)
                    + f"\nQUESTION: {question}\nPREVIOUS_DRAFT:\n{answer[:_MAX_RESEARCH_VISIBLE_ANSWER_CHARS]}"
                )
                remaining = remaining_seconds()
                revision_budget = min(
                    call_timeout_cap_seconds,
                    75.0,
                    max(5.0, (remaining - 8.0) * 0.7),
                )
                messages = prepared_messages(
                    writer_candidate,
                    system_prompt=writer_system_prompt,
                    instruction=revision_prompt,
                    materials=[
                        {
                            "title": "Verified evidence plan",
                            "kind": "research_plan",
                            "content": json.dumps(verified_plan, ensure_ascii=False),
                        },
                        {
                            "title": "Research evidence candidates",
                            "kind": "research_sources",
                            "content": source_material,
                        },
                    ],
                    target_role="web-research-answer-writer",
                    node="web_research_reviewer_guided_revision",
                )
                response = _invoke_architect_candidate_with_deadline(
                    writer_candidate,
                    messages,
                    seconds=revision_budget,
                    max_tokens=_RESEARCH_ARCHITECT_ANSWER_MAX_TOKENS,
                    disable_thinking=True,
                )
                revised_answer, complete = _research_answer_from_model_output(
                    sanitize_background_model_output(response).text
                )
                revision_issues = [] if complete else ["architect_answer_incomplete"]
                if len(revised_answer) > _MAX_RESEARCH_VISIBLE_ANSWER_CHARS:
                    revision_issues.append("answer_exceeds_delivery_surface_limit")
                if complete and len(revised_answer) <= _MAX_RESEARCH_VISIBLE_ANSWER_CHARS:
                    revision_issues.extend(answer_structural_issues(revised_answer))
                if not revision_issues:
                    writer_revision_count += 1
            if revision_issues:
                candidate_errors.append(f"{writer_label}: " + ",".join(revision_issues[:8]))
            elif revised_answer:
                revised_passes, revised_failed_review, revised_failed_candidate = run_review_consensus(
                    revised_answer,
                    node_prefix="web_research_independent_review_revision",
                )
                if revised_passes:
                    answer = revised_answer
                    accepted_review_passes = revised_passes
                    independent_review = dict(revised_passes[0][0])
                    reviewer_candidate = revised_passes[0][1]
                elif revised_failed_review:
                    answer = revised_answer
                    best_failed_review = revised_failed_review
                    failed_reviewer_candidate = revised_failed_candidate
                    review_critical_missing = _research_text_list(
                        revised_failed_review.get("criticalMissingEvidence"), limit=12
                    )
                    review_queries = _research_text_list(
                        revised_failed_review.get("recommendedNextQueries"), limit=4
                    )
        except concurrent.futures.TimeoutError:
            candidate_errors.append(f"{writer_label}: reviewer_guided_revision_timeout")
        except Exception as exc:  # noqa: BLE001 - preserve the substantive rejection.
            candidate_errors.append(
                f"{writer_label}: reviewer_guided_revision_{type(exc).__name__}: {_safe_text(exc)[:220]}"
            )

    # A structured same-evidence rejection is a repair request, not a reason
    # to discard a complete model-written answer. Preserve the Reviewer
    # feedback through the bounded rewrite/re-review above. The deterministic
    # claim report is the final same-evidence fallback only when review could
    # not produce structured feedback or the guided revision still failed.
    fallback_critical_missing = _research_text_list(
        best_failed_review.get("criticalMissingEvidence"), limit=12
    )
    fallback_queries = _research_text_list(
        best_failed_review.get("recommendedNextQueries"), limit=4
    )
    if (
        not independent_review
        and not writer_mode.startswith("deterministic_claim_report")
        and (
            not best_failed_review
            or (not fallback_critical_missing and not fallback_queries)
        )
    ):
        deterministic_started_at = time.perf_counter()
        deterministic_answer = _assemble_architect_claim_report(
            question=question,
            verified_plan=verified_plan,
            sources=compact_sources,
        )
        deterministic_answer, deterministic_repeat_drops = _architect_dedupe_repeated_content_units(
            deterministic_answer
        )
        deterministic_issues: list[str] = []
        if deterministic_repeat_drops:
            candidate_errors.append(
                f"runtime_deterministic_claim_report_repeated_unit_drops:{deterministic_repeat_drops}"
            )
        if len(deterministic_answer) > _MAX_RESEARCH_VISIBLE_ANSWER_CHARS:
            deterministic_issues.append("answer_exceeds_delivery_surface_limit")
        else:
            deterministic_issues.extend(answer_structural_issues(deterministic_answer))
        deterministic_attempt = {
            "modelId": "runtime:verified-claim-report",
            "selectionOrigin": "runtime_deterministic_fallback",
            "mode": "deterministic_claim_report_after_review",
            "startedWithRemainingSeconds": round(remaining_seconds(), 1),
            "elapsedMs": int((time.perf_counter() - deterministic_started_at) * 1000),
            "accepted": False,
            "failureCodes": list(dict.fromkeys(deterministic_issues))[-8:],
        }
        writer_attempts.append(deterministic_attempt)
        if not deterministic_issues and remaining_seconds() > 12.0:
            (
                deterministic_passes,
                deterministic_failed_review,
                deterministic_failed_candidate,
            ) = run_review_consensus(
                deterministic_answer,
                node_prefix="web_research_independent_review_claim_report",
            )
            deterministic_attempt["reviewed"] = True
            deterministic_attempt["accepted"] = bool(deterministic_passes)
            if deterministic_passes:
                answer = deterministic_answer
                accepted_review_passes = deterministic_passes
                independent_review = dict(deterministic_passes[0][0])
                reviewer_candidate = deterministic_passes[0][1]
                best_failed_review = {}
                failed_reviewer_candidate = None
                writer_revision_count += 1
                writer_mode = "deterministic_claim_report_after_review"
                writer_runtime_fallback = True
                writer_section_count = max(1, len(verified_claims))
            elif deterministic_failed_review:
                answer = deterministic_answer
                best_failed_review = deterministic_failed_review
                failed_reviewer_candidate = deterministic_failed_candidate
                writer_revision_count += 1
                writer_mode = "deterministic_claim_report_review_rejected"
                writer_runtime_fallback = True
                writer_section_count = max(1, len(verified_claims))
        elif not deterministic_issues:
            deterministic_attempt["failureCodes"] = ["architect_review_budget_reserved"]
            candidate_errors.append("runtime:verified-claim-report: architect_review_budget_reserved")
        else:
            candidate_errors.append(
                "runtime:verified-claim-report: " + ",".join(deterministic_issues[:8])
            )

    if not independent_review or reviewer_candidate is None:
        review_reasons = _research_text_list(best_failed_review.get("reviewReasons"), limit=12)
        critical_missing = _research_text_list(best_failed_review.get("criticalMissingEvidence"), limit=12)
        recommended_queries = _research_text_list(best_failed_review.get("recommendedNextQueries"), limit=4)
        same_evidence_rejected = bool(best_failed_review and not critical_missing and not recommended_queries)
        rejected_answer = answer or deterministic_answer
        rejected_units = _architect_section_content_units(rejected_answer)
        rejected_diagnostics = {
            "rawChars": len(rejected_answer),
            "effectiveChars": research_effective_answer_chars({"answer": rejected_answer}),
            "citationKeys": sorted(
                set(re.findall(r"\[(S\d+)\]", rejected_answer, re.IGNORECASE)),
                key=lambda value: int(value[1:]),
            ),
            "synthesisUnitCount": sum(
                1 for unit in rejected_units if _ARCHITECT_SYNTHESIS_LABEL_RE.search(unit)
            ),
            "headings": re.findall(r"(?m)^#{1,6}\s+(.+)$", rejected_answer)[:24],
            "answerSha256": hashlib.sha256(rejected_answer.encode("utf-8")).hexdigest()
            if rejected_answer
            else "",
            "preview": rejected_answer[:4_000],
            "claimDigest": [
                {
                    "claimId": _safe_text(claim.get("claimId")),
                    "claim": _compact_research_text(claim.get("claim"), limit=240),
                    "citationKeys": [
                        _safe_text(
                            support.get("citationKey") or support.get("citation")
                        ).strip("[]")
                        for support in list(claim.get("supportingSources") or [])
                        if isinstance(support, dict)
                    ][:2],
                    "evidenceExcerpt": _compact_research_text(
                        claim.get("evidenceExcerpt"),
                        limit=360,
                    ),
                }
                for claim in verified_claims[:_RESEARCH_ARCHITECT_MAX_CLAIM_COUNT]
            ],
        }
        return {
            **plan,
            "temporalAssessment": verified_plan.get("temporalAssessment") or {},
            "reviewDecision": "reject" if same_evidence_rejected else "retry",
            "reviewReasons": review_reasons or ["Independent semantic review rejected the candidate answer."],
            "researchResult": "",
            "answer": "",
            "claimTable": verified_claims,
            "criticalMissingEvidence": critical_missing,
            "recommendedNextQueries": recommended_queries,
            "asOf": verified_plan["asOf"],
            "_independentReview": best_failed_review,
            "_modelRole": plan_candidate[2],
            "_modelId": _architect_candidate_identity(plan_candidate),
            "_writerModelRole": writer_candidate[2],
            "_writerModelId": _architect_candidate_identity(writer_candidate),
            "_reviewerModelRole": failed_reviewer_candidate[2] if failed_reviewer_candidate else "",
            "_reviewerModelId": (
                _architect_candidate_identity(failed_reviewer_candidate) if failed_reviewer_candidate else ""
            ),
            "_modelParseMode": "staged_json_markdown",
            "_architectMode": "full_synthesis",
            "_architectPerCallTimeoutSeconds": call_timeout_cap_seconds,
            "_sameEvidenceReviewRejected": same_evidence_rejected,
            "_writerRevisionCount": writer_revision_count,
            "_writerMode": writer_mode,
            "_writerRuntimeFallback": writer_runtime_fallback,
            "_writerSectionCount": writer_section_count,
            "_modelFallbackAttempts": candidate_errors[:16],
            "_writerAttempts": writer_attempts,
            "_contextPreparations": context_preparations[-24:],
            "_writerSectionDiagnostics": writer_section_diagnostics[-32:],
            "_rejectedAnswerDiagnostics": rejected_diagnostics,
            "_researchPromptContracts": prompt_contracts,
        }

    reviewed_source_urls = [
        {
            key: source.get(key)
            for key in (
                "sourceId",
                "citationKey",
                "title",
                "url",
                "tier",
                "authorityTier",
                "authorityScore",
                "catalogCategory",
                "catalogSourceId",
                "runtimeOfficialSeed",
                "selectedForEvidence",
                "retrievedAt",
                "publishedAt",
                "updatedAt",
                "sourceDate",
                "sourceDateKind",
                "version",
                "temporalEvidence",
                "contentChars",
                "originalContentChars",
                "omittedChars",
                "evidenceSelection",
                "readEvidence",
                "researchFacetId",
                "researchFacetIds",
                "researchFacetGoal",
                "evidenceQuery",
                "subjectFocused",
            )
            if source.get(key) not in (None, "", [], {})
        }
        for source in compact_sources
    ]
    validation_payload = {
        "question": question,
        "freshness": freshness,
        "reviewDecision": "accept",
        "answer": answer,
        "sourceUrls": reviewed_source_urls,
        "claimTable": verified_claims,
        "criticalMissingEvidence": [],
        "asOf": verified_plan["asOf"],
    }
    bound_consensus_reviews: list[dict[str, Any]] = []
    for review, candidate, review_mode in accepted_review_passes:
        reviewer_model_id = _architect_candidate_identity(candidate)
        reviewed_at = _utc_now_iso()
        bound_review = {
            **review,
            "reviewMode": review_mode,
        }
        bound_review.update(
            build_research_review_binding(
                validation_payload,
                reviewer_model_id=reviewer_model_id,
                reviewed_at=reviewed_at,
            )
        )
        bound_consensus_reviews.append(bound_review)
    independent_review = {
        **bound_consensus_reviews[0],
        "consensusAccepted": len(bound_consensus_reviews) >= 2,
        "consensusReviewCount": len(bound_consensus_reviews),
        "consensusReviewerModelIds": [
            _safe_text(review.get("reviewerModelId")) for review in bound_consensus_reviews
        ],
        "consensusReviews": bound_consensus_reviews,
    }
    validation_payload["independentReview"] = independent_review
    quality_issues = research_high_quality_issues(validation_payload)
    if quality_issues:
        return {
            "_agentError": "architect_post_review_quality_gate_failed",
            "_architectMode": "full_synthesis",
            "_modelFallbackAttempts": [*candidate_errors, *quality_issues][:8],
            "_writerAttempts": writer_attempts,
            "_planAttempts": plan_attempts[-6:],
            "_contextPreparations": context_preparations[-24:],
            "_writerSectionDiagnostics": writer_section_diagnostics[-32:],
            "_researchPromptContracts": prompt_contracts,
        }

    return {
        **plan,
        "temporalAssessment": verified_plan.get("temporalAssessment") or {},
        "reviewDecision": "accept",
        "researchResult": answer,
        "answer": answer,
        "claimTable": verified_claims,
        "criticalMissingEvidence": [],
        "asOf": verified_plan["asOf"],
        "_independentReview": independent_review,
        "_modelRole": plan_candidate[2],
        "_modelId": _architect_candidate_identity(plan_candidate),
        "_writerModelRole": writer_candidate[2],
        "_writerModelId": _architect_candidate_identity(writer_candidate),
        "_reviewerModelRole": reviewer_candidate[2],
        "_reviewerModelId": _architect_candidate_identity(reviewer_candidate),
        "_reviewerConsensusCount": len(bound_consensus_reviews),
        "_reviewerConsensusModelIds": [
            _safe_text(review.get("reviewerModelId")) for review in bound_consensus_reviews
        ],
        "_modelParseMode": "staged_json_markdown",
        "_architectMode": "full_synthesis",
        "_architectPerCallTimeoutSeconds": call_timeout_cap_seconds,
        "_writerRevisionCount": writer_revision_count,
        "_writerMode": writer_mode,
        "_writerRuntimeFallback": writer_runtime_fallback,
        "_writerSectionCount": writer_section_count,
        "_modelFallbackAttempts": candidate_errors,
        "_writerAttempts": writer_attempts,
        "_contextPreparations": context_preparations[-24:],
        "_writerSectionDiagnostics": writer_section_diagnostics[-32:],
        "_researchPromptContracts": prompt_contracts,
        # The delivery merge must retain the exact read receipts and temporal
        # projection reviewed here.  The deterministic base pack may carry a
        # full-body receipt while Architect intentionally reviewed a bounded
        # 32K body; swapping those receipts would invalidate an accepted review.
        "_reviewedSourceUrls": reviewed_source_urls,
    }


def _invoke_web_research_architect_agent(
    *,
    question: str,
    source_matrix: list[dict[str, Any]],
    shards: list[dict[str, Any]],
    confidence: str,
    average_authority: float,
    freshness: str,
    timeout_seconds: int,
    per_call_timeout_seconds: int | None = None,
    architect_mode: str = "",
) -> dict[str, Any] | None:
    sources = _research_architect_sources_for_prompt(
        source_matrix,
        shards,
        question=question,
        freshness=freshness,
    )
    if not sources:
        return None
    mode = architect_mode or _research_architect_mode(question, sources, freshness=freshness)
    if mode == "full_synthesis" and _research_architect_mode(question, sources, freshness=freshness) != "full_synthesis":
        mode = "gap_review"
    if mode == "full_synthesis":
        try:
            return _invoke_web_research_architect_staged(
                question=question,
                sources=sources,
                freshness=freshness,
                timeout_seconds=timeout_seconds,
                per_call_timeout_seconds=per_call_timeout_seconds,
            )
        except Exception as exc:  # noqa: BLE001 - deterministic fallback must keep Research Runtime alive.
            return {"_agentError": f"{type(exc).__name__}: {exc}", "_architectMode": mode}

    def _call() -> dict[str, Any] | None:
        started_at = time.perf_counter()
        total_budget = max(5, int(timeout_seconds or 0))
        configured_call_timeout = (
            _as_int(per_call_timeout_seconds, 0)
            if per_call_timeout_seconds is not None
            else _as_int(_research_config().get("architectAgentTimeoutSeconds"), 60)
        )
        call_timeout_cap_seconds = max(
            5.0,
            min(float(configured_call_timeout or 60), 90.0, float(total_budget)),
        )
        candidates = _create_web_research_architect_llm_candidates()
        prompt_sources: list[dict[str, Any]] = []
        for source in sources[:_RESEARCH_ARCHITECT_MAX_SOURCE_COUNT]:
            compact_source = {
                "sourceId": _safe_text(source.get("sourceId")),
                "citationKey": _safe_text(source.get("citationKey")),
                "title": _safe_text(source.get("title")),
                "url": _safe_text(source.get("url")),
                "tier": source.get("tier"),
                "authorityScore": source.get("authorityScore"),
                "retrievedAt": source.get("retrievedAt"),
                "publishedAt": source.get("publishedAt"),
                "updatedAt": source.get("updatedAt"),
                "version": source.get("version"),
                "selectedForEvidence": True,
            }
            source_with_text = {
                **compact_source,
                "text": _safe_text(source.get("text"))[:_RESEARCH_ARCHITECT_SOURCE_TEXT_CHARS],
            }
            compact_source["evidenceCandidates"] = _architect_evidence_candidates(
                source_with_text,
                question,
                limit=3,
            )
            prompt_sources.append(compact_source)

        system_prompt = (
            "你是 Web Research Architect 的证据缺口审阅器。只判断已读取 SOURCES 还缺哪些必要原子前提，"
            "不要写答案、扩写背景或引入 SOURCES 外事实。多份来源可以分别支持组合问题的原子前提；"
            "没有单篇资料覆盖完整问题本身不是缺口。"
        )
        system_prompt = _research_agent_stage_system_prompt(
            system_prompt,
            stage="evidence_gap",
        )
        prompt = (
            "只输出严格 JSON：reviewDecision, reviewReasons, criticalMissingEvidence, recommendedNextQueries。"
            "reviewDecision 必须是 retry 或 reject；recommendedNextQueries 最多4项，每项应能直接交给搜索工具。"
            f"最低结构目标：{TARGET_RESEARCH_SOURCE_COUNT} 个逻辑来源、{TARGET_RESEARCH_DISTINCT_HOST_COUNT} 个独立主机；"
            "时效充分性不是固定发布日期数量门槛：根据每个来源明确标注的发布/更新/版本/检索时间与问题风险判断，"
            "只有确实无法判断关键当前性时才建议补搜。检索时间只表示本轮读取时间，不等于文档发布时间。"
            f"\nQUESTION: {question}"
        )
        candidate_errors: list[str] = []
        raw_previews: list[str] = []
        for candidate_index, candidate in enumerate(candidates):
            remaining = max(0.0, total_budget - (time.perf_counter() - started_at))
            if remaining <= 1:
                candidate_errors.append("architect_total_timeout")
                break
            remaining_candidates = max(1, len(candidates) - candidate_index)
            per_call_budget = min(
                call_timeout_cap_seconds,
                60.0,
                max(2.0, remaining / remaining_candidates),
            )
            model_label = _architect_candidate_identity(candidate) or candidate[1] or f"role:{candidate[2]}"
            prepared = prepare_background_model_messages(
                system_prompt=system_prompt,
                instruction=prompt,
                materials=[
                    {
                        "title": "Research evidence candidates",
                        "kind": "research_sources",
                        "content": json.dumps(prompt_sources, ensure_ascii=False),
                    }
                ],
                runtime_kind="research",
                target_role="web-research-architect",
                resolved_model_id=_architect_candidate_context_model_ref(candidate),
                component="research",
                node="web_research_architect_gap_review",
            )
            try:
                response = _invoke_architect_candidate_with_deadline(
                    candidate,
                    prepared.messages,
                    seconds=per_call_budget,
                    max_tokens=_RESEARCH_ARCHITECT_REVIEW_MAX_TOKENS,
                    disable_thinking=True,
                )
            except concurrent.futures.TimeoutError:
                candidate_errors.append(f"{model_label}: architect_gap_review_timeout")
                continue
            except Exception as exc:  # noqa: BLE001 - try the next configured Architect model.
                candidate_errors.append(
                    f"{model_label}: architect_gap_review_{type(exc).__name__}: {_safe_text(exc)[:220]}"
                )
                continue
            raw_content = sanitize_background_model_output(response).text
            if raw_content:
                raw_previews.append(f"{model_label}: {raw_content[:400]}")
            parsed = _extract_json_object(raw_content)
            if not isinstance(parsed, dict):
                candidate_errors.append(f"{model_label}: architect_gap_review_no_json")
                continue
            decision = _safe_text(parsed.get("reviewDecision")).lower()
            if decision not in {"retry", "reject"}:
                candidate_errors.append(f"{model_label}: architect_gap_review_invalid_decision")
                continue
            parsed.update(
                {
                    "reviewDecision": decision,
                    "researchResult": "",
                    "answer": "",
                    "claimTable": [],
                    "criticalMissingEvidence": _research_text_list(
                        parsed.get("criticalMissingEvidence"), limit=12
                    ),
                    "recommendedNextQueries": _research_text_list(
                        parsed.get("recommendedNextQueries"), limit=4
                    ),
                    "_modelRole": candidate[2],
                    "_modelId": model_label,
                    "_modelParseMode": "gap_review_json",
                    "_architectMode": "gap_review",
                    "_modelFallbackAttempts": candidate_errors,
                    "_researchPromptContractVersion": RESEARCH_PROMPT_CONTRACT_VERSION,
                    "_researchPromptContractDigest": research_runtime_prompt_digest(system_prompt),
                }
            )
            return parsed
        return {
            "_agentError": "architect_gap_review_no_json",
            "_architectMode": "gap_review",
            "_rawPreview": "\n--- retry ---\n".join(raw_previews)[:800],
            "_modelFallbackAttempts": candidate_errors[:16],
            "_researchPromptContractVersion": RESEARCH_PROMPT_CONTRACT_VERSION,
            "_researchPromptContractDigest": research_runtime_prompt_digest(system_prompt),
        }

    try:
        return _call()
    except Exception as exc:  # noqa: BLE001 - deterministic fallback must keep Research Runtime alive.
        return {"_agentError": f"{type(exc).__name__}: {exc}", "_architectMode": mode}

def _merge_web_research_architect_agent_pack(
    base_pack: dict[str, Any],
    agent_pack: dict[str, Any] | None,
    *,
    question: str,
    freshness: str = "auto",
) -> dict[str, Any]:
    source_texts = dict(base_pack.get("_sourceTexts") or {}) if isinstance(base_pack.get("_sourceTexts"), dict) else {}
    public_base_pack = {key: value for key, value in base_pack.items() if key != "_sourceTexts"}
    architect_mode = _safe_text((agent_pack or {}).get("_architectMode")) if isinstance(agent_pack, dict) else ""
    if not isinstance(agent_pack, dict) or agent_pack.get("_agentError"):
        fallback_reason = "architect_agent_no_result"
        raw_preview = ""
        if isinstance(agent_pack, dict) and agent_pack.get("_agentError"):
            fallback_reason = _safe_text(agent_pack.get("_agentError")) or fallback_reason
            raw_preview = _safe_text(agent_pack.get("_rawPreview"))[:500]
        return {
            **public_base_pack,
            "synthesisMode": "deterministic_fallback",
            "modelSynthesis": {
                "used": False,
                "agentId": "web-research-architect",
                "mode": architect_mode or "unknown",
                "fallbackReason": fallback_reason,
                **(
                    {"missingFacetIds": _as_list(agent_pack.get("_missingFacetIds"))[:8]}
                    if isinstance(agent_pack, dict) and agent_pack.get("_missingFacetIds")
                    else {}
                ),
                **({"rawPreview": raw_preview} if raw_preview else {}),
                **({"fallbackAttempts": list(agent_pack.get("_modelFallbackAttempts") or [])[-10:]} if isinstance(agent_pack, dict) and agent_pack.get("_modelFallbackAttempts") else {}),
                **({"planAttempts": list(agent_pack.get("_planAttempts") or [])[-6:]} if isinstance(agent_pack, dict) and agent_pack.get("_planAttempts") else {}),
                **({"writerAttempts": list(agent_pack.get("_writerAttempts") or [])[-4:]} if isinstance(agent_pack, dict) and agent_pack.get("_writerAttempts") else {}),
                **({"contextPreparations": list(agent_pack.get("_contextPreparations") or [])[-24:]} if isinstance(agent_pack, dict) and agent_pack.get("_contextPreparations") else {}),
                **({"writerSectionDiagnostics": list(agent_pack.get("_writerSectionDiagnostics") or [])[-32:]} if isinstance(agent_pack, dict) and agent_pack.get("_writerSectionDiagnostics") else {}),
                **({"researchPromptContracts": dict(agent_pack.get("_researchPromptContracts") or {})} if isinstance(agent_pack, dict) and agent_pack.get("_researchPromptContracts") else {}),
                **({"researchPromptContractVersion": agent_pack.get("_researchPromptContractVersion")} if isinstance(agent_pack, dict) and agent_pack.get("_researchPromptContractVersion") else {}),
                **({"researchPromptContractDigest": agent_pack.get("_researchPromptContractDigest")} if isinstance(agent_pack, dict) and agent_pack.get("_researchPromptContractDigest") else {}),
            },
        }
    decision = _safe_text(agent_pack.get("reviewDecision")).lower()
    if decision not in {"accept", "retry", "reject"}:
        return {
            **public_base_pack,
            "modelSynthesis": {
                "used": False,
                "agentId": "web-research-architect",
                "mode": architect_mode or "unknown",
                "fallbackReason": "architect_agent_invalid_review_decision",
            },
        }
    independent_review = _normalize_independent_architect_review(
        agent_pack.get("_independentReview") or agent_pack.get("independentReview")
    )
    if decision == "accept" and not _independent_architect_review_consensus_accepts(independent_review):
        return {
            **public_base_pack,
            "reviewDecision": "retry",
            "reviewReasons": list(
                dict.fromkeys(
                    [
                        *list(agent_pack.get("reviewReasons") or []),
                        *list(independent_review.get("reviewReasons") or []),
                        "Independent semantic and freshness review did not accept the proposed answer.",
                    ]
                )
            ),
            "answer": "",
            "researchResult": "",
            "criticalMissingEvidence": ["A separate semantic and freshness review must accept the answer before delivery."],
            "synthesisMode": "model_agent",
            "modelSynthesis": {
                "used": True,
                "agentId": "web-research-architect",
                "mode": architect_mode or "full_synthesis",
                "fallbackReason": "independent_semantic_review_not_accepted",
            },
        }
    research_result = _safe_text(agent_pack.get("researchResult") or agent_pack.get("answer"))
    if decision == "accept" and not research_result:
        return {
            **public_base_pack,
            "reviewReasons": ["Architect returned accept without a detailed answer."],
            "modelSynthesis": {
                "used": False,
                "agentId": "web-research-architect",
                "mode": architect_mode or "full_synthesis",
                "fallbackReason": "architect_agent_missing_research_result",
            },
        }
    reviewed_sources = [
        dict(item)
        for item in list(agent_pack.get("_reviewedSourceUrls") or [])
        if isinstance(item, dict)
    ]
    known_sources = reviewed_sources or [
        item for item in list(base_pack.get("sourceUrls") or []) if isinstance(item, dict)
    ]
    evidence_sources = []
    for item in known_sources:
        evidence_text = next(
            (
                _safe_text(source_texts.get(alias) or source_texts.get(alias.strip("[]")))
                for alias in (
                    _safe_text(item.get("url")),
                    _safe_text(item.get("sourceId")),
                    _safe_text(item.get("citationKey")),
                )
                if alias and _safe_text(source_texts.get(alias) or source_texts.get(alias.strip("[]")))
            ),
            "",
        )
        evidence_sources.append({**item, "evidenceText": evidence_text})
    verified_raw_claims, excerpt_issues = _verify_architect_claim_excerpts(agent_pack.get("claimTable"), evidence_sources)
    if decision == "accept" and excerpt_issues:
        return {
            **public_base_pack,
            "reviewDecision": "retry",
            "reviewReasons": list(dict.fromkeys([*list(agent_pack.get("reviewReasons") or []), *excerpt_issues])),
            "answer": "",
            "researchResult": "",
            "criticalMissingEvidence": ["Architect claims did not retain exact excerpts from their supporting source bodies."],
            "synthesisMode": "model_agent",
            "modelSynthesis": {
                "used": True,
                "agentId": "web-research-architect",
                "mode": architect_mode or "full_synthesis",
                "fallbackReason": "claim_evidence_excerpt_verification_failed",
            },
        }
    known_urls = {item.get("url") for item in known_sources if item.get("url")}
    known_by_url = {
        _safe_text(item.get("url")): item
        for item in known_sources
        if _safe_text(item.get("url"))
    }
    known_by_id = {_safe_text(item.get("sourceId")): item for item in known_sources if _safe_text(item.get("sourceId"))}
    known_by_citation: dict[str, dict[str, Any]] = {}
    for item in known_sources:
        citation_key = _safe_text(item.get("citationKey"))
        if citation_key:
            known_by_citation[citation_key] = item
            known_by_citation[citation_key.strip("[]")] = item

    def enrich_supporting_source(source: dict[str, Any]) -> dict[str, Any]:
        known = (
            known_by_url.get(_safe_text(source.get("url") or source.get("sourceUrl")))
            or known_by_id.get(_safe_text(source.get("sourceId")))
            or known_by_citation.get(
                _safe_text(source.get("citationKey") or source.get("citation"))
            )
            or known_by_citation.get(
                _safe_text(source.get("citationKey") or source.get("citation")).strip("[]")
            )
            or {}
        )
        enriched = dict(source)
        for key in (
            "host",
            "tier",
            "authorityScore",
            "subjectFocused",
            "retrievedAt",
            "publishedAt",
            "updatedAt",
            "sourceDate",
            "sourceDateKind",
            "version",
            "temporalEvidence",
        ):
            value = known.get(key)
            if value not in (None, "", [], {}):
                enriched[key] = value
        return {key: value for key, value in enriched.items() if value not in (None, "", [], {})}

    claim_table: list[dict[str, Any]] = []
    for raw_item in verified_raw_claims:
        if not isinstance(raw_item, dict):
            continue
        supporting: list[dict[str, Any]] = []
        for source in list(raw_item.get("supportingSources") or [])[:4]:
            if isinstance(source, str):
                if source in known_urls:
                    source = {"url": source}
                elif source in known_by_id:
                    source = {"sourceId": source}
                else:
                    source = {"citationKey": source}
            if not isinstance(source, dict):
                continue
            url = _safe_text(source.get("url") or source.get("sourceUrl"))
            if url and url in known_urls:
                known = next((item for item in known_sources if item.get("url") == url), {})
                supporting.append(
                    {
                        "title": _safe_text(known.get("title") or source.get("title")) or url,
                        "url": url,
                        "sourceId": known.get("sourceId"),
                        "citationKey": known.get("citationKey"),
                    }
                )
                continue
            source_id = _safe_text(source.get("sourceId"))
            if source_id and source_id in known_by_id:
                known = known_by_id[source_id]
                known_url = _safe_text(known.get("url"))
                if known_url:
                    supporting.append(
                        {
                            "title": _safe_text(known.get("title")) or known_url,
                            "url": known_url,
                            "sourceId": source_id,
                            "citationKey": known.get("citationKey"),
                        }
                    )
                    continue
            citation_key = _safe_text(source.get("citationKey") or source.get("citation"))
            known = known_by_citation.get(citation_key) or known_by_citation.get(citation_key.strip("[]"))
            if known:
                known_url = _safe_text(known.get("url"))
                if known_url:
                    supporting.append(
                        {
                            "title": _safe_text(known.get("title")) or known_url,
                            "url": known_url,
                            "sourceId": known.get("sourceId"),
                            "citationKey": known.get("citationKey"),
                        }
                    )
        if not supporting:
            candidate_urls: list[str] = []
            candidate_source_ids: list[str] = []
            candidate_citation_keys: list[str] = []
            for field in ("sourceURL", "sourceUrl", "url"):
                value = _safe_text(raw_item.get(field))
                if value:
                    candidate_urls.append(value)
            for field in ("sourceId", "sourceID"):
                value = _safe_text(raw_item.get(field))
                if value:
                    candidate_source_ids.append(value)
            for field in ("citationKey", "citation"):
                value = _safe_text(raw_item.get(field))
                if value:
                    candidate_citation_keys.append(value)
            for value in list(raw_item.get("sourceUrls") or [])[:4]:
                if isinstance(value, str):
                    candidate_urls.append(value)
                elif isinstance(value, dict):
                    candidate_urls.append(_safe_text(value.get("url")))
                    candidate_source_ids.append(_safe_text(value.get("sourceId")))
                    candidate_citation_keys.append(_safe_text(value.get("citationKey") or value.get("citation")))
            for url in candidate_urls[:4]:
                if url and url in known_urls:
                    known = next((item for item in known_sources if item.get("url") == url), {})
                    supporting.append(
                        {
                            "title": _safe_text(known.get("title")) or url,
                            "url": url,
                            "sourceId": known.get("sourceId"),
                            "citationKey": known.get("citationKey"),
                        }
                    )
            for source_id in candidate_source_ids[:4]:
                if source_id and source_id in known_by_id:
                    known = known_by_id[source_id]
                    known_url = _safe_text(known.get("url"))
                    if known_url:
                        supporting.append(
                            {
                                "title": _safe_text(known.get("title")) or known_url,
                                "url": known_url,
                                "sourceId": source_id,
                                "citationKey": known.get("citationKey"),
                            }
                        )
            for citation_key in candidate_citation_keys[:4]:
                known = known_by_citation.get(citation_key) or known_by_citation.get(citation_key.strip("[]"))
                if known:
                    known_url = _safe_text(known.get("url"))
                    if known_url:
                        supporting.append(
                            {
                                "title": _safe_text(known.get("title")) or known_url,
                                "url": known_url,
                                "sourceId": known.get("sourceId"),
                                "citationKey": known.get("citationKey"),
                            }
                        )
        if not supporting:
            continue
        supporting = [enrich_supporting_source(source) for source in supporting]
        claim = _safe_text(raw_item.get("claim"))
        if claim:
            claim_table.append(
                {
                    **(
                        {"claimId": _safe_text(raw_item.get("claimId"))}
                        if _safe_text(raw_item.get("claimId"))
                        else {}
                    ),
                    "claim": claim,
                    "claimType": _safe_text(raw_item.get("claimType") or raw_item.get("claimKind")) or "source_fact",
                    **(
                        {"normativeCue": _safe_text(raw_item.get("normativeCue"))}
                        if _safe_text(raw_item.get("normativeCue"))
                        else {}
                    ),
                    **(
                        {"sourceRole": _safe_text(raw_item.get("sourceRole"))}
                        if _safe_text(raw_item.get("sourceRole"))
                        else {}
                    ),
                    **(
                        {"sourceClaim": _safe_text(raw_item.get("sourceClaim"))}
                        if _safe_text(raw_item.get("sourceClaim"))
                        else {}
                    ),
                    "supportingSources": supporting,
                    "refutingSources": list(raw_item.get("refutingSources") or [])[:4],
                    "confidence": _safe_text(raw_item.get("confidence")) or base_pack.get("confidence"),
                    "evidenceExcerptKey": raw_item.get("evidenceExcerptKey"),
                    "evidenceExcerpt": raw_item.get("evidenceExcerpt"),
                    "evidenceExcerptSha256": raw_item.get("evidenceExcerptSha256"),
                    "evidenceVerified": raw_item.get("evidenceVerified") is True,
                }
            )
    headline = _safe_text(agent_pack.get("headline")) or f"Web Research Architect synthesized final evidence for: {question}"
    candidate = {
        **public_base_pack,
        "question": question,
        "freshness": freshness,
        "reviewDecision": decision,
        "reviewReasons": _research_text_list(agent_pack.get("reviewReasons"), limit=12),
        "headline": headline,
        "answer": research_result,
        "researchResult": research_result,
        "sourceUrls": known_sources,
        "claimTable": claim_table,
        "independentReview": independent_review,
        "conflictMatrix": _research_text_list(agent_pack.get("conflictMatrix") or base_pack.get("conflictMatrix"), limit=12),
        "missingEvidence": _research_text_list(agent_pack.get("missingEvidence") or base_pack.get("missingEvidence"), limit=12),
        "criticalMissingEvidence": _research_text_list(agent_pack.get("criticalMissingEvidence"), limit=12),
        "recommendedNextQueries": _research_text_list(agent_pack.get("recommendedNextQueries"), limit=12),
        "assumptions": _research_text_list(agent_pack.get("assumptions") or base_pack.get("assumptions"), limit=12),
        "asOf": _safe_text(agent_pack.get("asOf") or base_pack.get("asOf")) or _utc_now_iso(),
        "temporalAssessment": agent_pack.get("temporalAssessment") if isinstance(agent_pack.get("temporalAssessment"), dict) else {},
        "synthesisMode": "model_agent",
        "modelSynthesis": {
            "used": True,
            "agentId": "web-research-architect",
            "agentName": "Web Research Architect",
            "mode": architect_mode or "full_synthesis",
            "modelRole": agent_pack.get("_modelRole"),
            "modelId": agent_pack.get("_modelId"),
            **({"writerModelRole": agent_pack.get("_writerModelRole")} if agent_pack.get("_writerModelRole") else {}),
            **({"writerModelId": agent_pack.get("_writerModelId")} if agent_pack.get("_writerModelId") else {}),
            **({"reviewerModelRole": agent_pack.get("_reviewerModelRole")} if agent_pack.get("_reviewerModelRole") else {}),
            **({"reviewerModelId": agent_pack.get("_reviewerModelId")} if agent_pack.get("_reviewerModelId") else {}),
            "reviewerConsensusCount": int(agent_pack.get("_reviewerConsensusCount") or 0),
            "reviewerConsensusModelIds": [
                _safe_text(model_id)
                for model_id in list(agent_pack.get("_reviewerConsensusModelIds") or [])[:4]
                if _safe_text(model_id)
            ],
            "parseMode": agent_pack.get("_modelParseMode") or "json",
            "sameEvidenceReviewRejected": agent_pack.get("_sameEvidenceReviewRejected") is True,
            "writerRevisionCount": int(agent_pack.get("_writerRevisionCount") or 0),
            "writerMode": _safe_text(agent_pack.get("_writerMode")) or "unknown",
            "writerSectionCount": int(agent_pack.get("_writerSectionCount") or 0),
            **({"fallbackAttempts": list(agent_pack.get("_modelFallbackAttempts") or [])[-10:]} if agent_pack.get("_modelFallbackAttempts") else {}),
            **({"planAttempts": list(agent_pack.get("_planAttempts") or [])[-6:]} if agent_pack.get("_planAttempts") else {}),
            **({"writerAttempts": list(agent_pack.get("_writerAttempts") or [])[-4:]} if agent_pack.get("_writerAttempts") else {}),
            **({"contextPreparations": list(agent_pack.get("_contextPreparations") or [])[-24:]} if agent_pack.get("_contextPreparations") else {}),
            **({"writerSectionDiagnostics": list(agent_pack.get("_writerSectionDiagnostics") or [])[-32:]} if agent_pack.get("_writerSectionDiagnostics") else {}),
            **(
                {"audienceDecisionCompletion": dict(agent_pack.get("_audienceDecisionCompletion") or {})}
                if isinstance(agent_pack.get("_audienceDecisionCompletion"), dict)
                and agent_pack.get("_audienceDecisionCompletion")
                else {}
            ),
            **({"researchPromptContracts": dict(agent_pack.get("_researchPromptContracts") or {})} if agent_pack.get("_researchPromptContracts") else {}),
            **({"researchPromptContractVersion": agent_pack.get("_researchPromptContractVersion")} if agent_pack.get("_researchPromptContractVersion") else {}),
            **({"researchPromptContractDigest": agent_pack.get("_researchPromptContractDigest")} if agent_pack.get("_researchPromptContractDigest") else {}),
            **(
                {"rejectedAnswerDiagnostics": dict(agent_pack.get("_rejectedAnswerDiagnostics") or {})}
                if isinstance(agent_pack.get("_rejectedAnswerDiagnostics"), dict)
                and agent_pack.get("_rejectedAnswerDiagnostics")
                else {}
            ),
        },
    }
    issues = research_high_quality_issues(candidate)
    if decision == "accept" and issues:
        return {
            **candidate,
            "reviewDecision": "retry",
            "reviewReasons": list(dict.fromkeys([*candidate.get("reviewReasons", []), *issues])),
            "answer": "",
            "researchResult": "",
            "criticalMissingEvidence": list(
                dict.fromkeys(
                    [
                        *candidate.get("criticalMissingEvidence", []),
                        "The proposed answer did not pass the independent Research quality gate.",
                    ]
                )
            ),
        }
    return candidate


def _web_research_architect_pack(
    *,
    question: str,
    source_matrix: list[dict[str, Any]],
    shards: list[dict[str, Any]],
    confidence: str,
    average_authority: float,
    freshness: str,
    architect_call_state: dict[str, Any] | None = None,
) -> dict[str, Any]:
    prompt_sources = _research_architect_sources_for_prompt(
        source_matrix,
        shards,
        question=question,
        freshness=freshness,
    )
    base_pack = _deterministic_web_research_architect_pack(
        question=question,
        source_matrix=prompt_sources or source_matrix,
        shards=shards,
        confidence=confidence,
        average_authority=average_authority,
    )
    config = _research_config()
    if not config.get("architectAgentSynthesisEnabled", True):
        return {
            **{key: value for key, value in base_pack.items() if key != "_sourceTexts"},
            "modelSynthesis": {
                "used": False,
                "agentId": "web-research-architect",
                "fallbackReason": "architect_agent_synthesis_disabled",
            },
        }
    structural_stats = _research_architect_structural_stats(
        question,
        prompt_sources,
        freshness=freshness,
    )
    mode = "full_synthesis" if structural_stats.get("structuralTargetMet") is True else "gap_review"
    call_state = architect_call_state if isinstance(architect_call_state, dict) else {}
    if not prompt_sources:
        return {
            **{key: value for key, value in base_pack.items() if key != "_sourceTexts"},
            "modelSynthesis": {
                "used": False,
                "agentId": "web-research-architect",
                "mode": mode,
                "fallbackReason": "architect_gap_review_no_readable_sources",
            },
        }
    if mode == "gap_review":
        structural_gaps: list[str] = []
        if int(structural_stats.get("selectedSourceCount") or 0) < TARGET_RESEARCH_SOURCE_COUNT:
            structural_gaps.append(
                "Architect-answerable source target not met: "
                f"{structural_stats.get('selectedSourceCount', 0)}/{TARGET_RESEARCH_SOURCE_COUNT}."
            )
        if int(structural_stats.get("distinctHostCount") or 0) < TARGET_RESEARCH_DISTINCT_HOST_COUNT:
            structural_gaps.append(
                "Architect-answerable independent host target not met: "
                f"{structural_stats.get('distinctHostCount', 0)}/{TARGET_RESEARCH_DISTINCT_HOST_COUNT}."
            )
        missing_facet_ids = [
            _safe_text(value)
            for value in list(structural_stats.get("missingFacetIds") or [])
            if _safe_text(value)
        ]
        if missing_facet_ids:
            structural_gaps.append(
                "Architect-answerable evidence is missing for explicit Research facets: "
                + ", ".join(missing_facet_ids)
                + "."
            )
        minimum_reviewable = bool(
            int(structural_stats.get("selectedSourceCount") or 0) >= MIN_RESEARCH_SOURCE_COUNT
            and int(structural_stats.get("distinctHostCount") or 0)
            >= MIN_RESEARCH_DISTINCT_HOST_COUNT
        )
        if (
            minimum_reviewable
            and int(call_state.get("gapReviewAttempts") or 0) < 1
        ):
            call_state["gapReviewAttempts"] = int(call_state.get("gapReviewAttempts") or 0) + 1
            agent_pack = _invoke_web_research_architect_agent(
                question=question,
                source_matrix=prompt_sources or source_matrix,
                shards=shards,
                confidence=confidence,
                average_authority=average_authority,
                freshness=freshness,
                timeout_seconds=min(
                    90,
                    int(_RESEARCH_ARCHITECT_SYNTHESIS_DEADLINE_MS / 1000),
                ),
                per_call_timeout_seconds=int(config.get("architectAgentTimeoutSeconds") or 60),
                architect_mode="gap_review",
            )
            merged = _merge_web_research_architect_agent_pack(
                base_pack,
                agent_pack,
                question=question,
                freshness=freshness,
            )
            model_synthesis = (
                dict(merged.get("modelSynthesis") or {})
                if isinstance(merged.get("modelSynthesis"), dict)
                else {}
            )
            merged["modelSynthesis"] = {
                **model_synthesis,
                "mode": "gap_review",
                "structuralTargetMet": False,
                "structuralStats": structural_stats,
            }
            merged["criticalMissingEvidence"] = list(
                dict.fromkeys(
                    [
                        *list(merged.get("criticalMissingEvidence") or []),
                        *structural_gaps,
                    ]
                )
            )
            return merged
        return {
            **{key: value for key, value in base_pack.items() if key != "_sourceTexts"},
            "criticalMissingEvidence": list(
                dict.fromkeys(
                    [
                        *list(base_pack.get("criticalMissingEvidence") or []),
                        *structural_gaps,
                    ]
                )
            ),
            "modelSynthesis": {
                "used": False,
                "agentId": "web-research-architect",
                "mode": mode,
                "fallbackReason": (
                    "architect_gap_review_attempt_budget_exhausted"
                    if minimum_reviewable
                    else "architect_gap_review_deferred_until_minimum_floor"
                ),
                "structuralTargetMet": False,
                "structuralStats": structural_stats,
            },
        }
    if int(call_state.get("fullSynthesisAttempts") or 0) >= _MAX_RESEARCH_ARCHITECT_FULL_SYNTHESIS_ATTEMPTS:
        return {
            **{key: value for key, value in base_pack.items() if key != "_sourceTexts"},
            "modelSynthesis": {
                "used": False,
                "agentId": "web-research-architect",
                "mode": mode,
                "fallbackReason": "architect_full_synthesis_attempt_budget_exhausted",
                "attemptBudgetExhausted": True,
            },
        }
    call_state["fullSynthesisAttempts"] = int(call_state.get("fullSynthesisAttempts") or 0) + 1
    agent_pack = _invoke_web_research_architect_agent(
        question=question,
        # The deterministic base pack and the model/reviewer chain must see
        # the same ordered evidence set.  Otherwise citation keys can be bound
        # to one order while the final pack publishes another after relevance
        # ranking, making an otherwise valid independent review unverifiable.
        source_matrix=prompt_sources or source_matrix,
        shards=shards,
        confidence=confidence,
        average_authority=average_authority,
        freshness=freshness,
        timeout_seconds=int(_RESEARCH_ARCHITECT_SYNTHESIS_DEADLINE_MS / 1000),
        per_call_timeout_seconds=int(config.get("architectAgentTimeoutSeconds") or 60),
        architect_mode=mode,
    )
    merged = _merge_web_research_architect_agent_pack(
        base_pack,
        agent_pack,
        question=question,
        freshness=freshness,
    )
    model_synthesis = (
        dict(merged.get("modelSynthesis") or {})
        if isinstance(merged.get("modelSynthesis"), dict)
        else {}
    )
    merged["modelSynthesis"] = {
        **model_synthesis,
        "structuralTargetMet": True,
        "structuralStats": structural_stats,
    }
    return merged


def _deadline_failure(
    *,
    tool_name: str,
    family: str,
    deadline_ms: int,
    summary: str,
    failure_class: str,
    error: str,
    recommended_next_action: str,
) -> dict[str, Any]:
    with ToolExecutionEnvelope(tool_name=tool_name, family=family, deadline_ms=deadline_ms, retry_limit=1) as envelope:
        return envelope.failure_payload(
            summary=summary,
            failure_class=failure_class,
            error=error,
            retryable=False,
            recommended_next_action=recommended_next_action,
        )


def _call_with_deadline(
    func,
    *,
    deadline_ms: int,
    tool_name: str,
    family: str,
    recommended_next_action: str,
) -> dict[str, Any]:
    with ToolExecutionEnvelope(tool_name=tool_name, family=family, deadline_ms=deadline_ms, retry_limit=1) as envelope:
        executor = concurrent.futures.ThreadPoolExecutor(max_workers=1, thread_name_prefix=f"v8-{tool_name}")
        future = executor.submit(func)
        try:
            return _parse_tool_json(future.result(timeout=max(deadline_ms / 1000.0, 0.1)))
        except concurrent.futures.TimeoutError:
            future.cancel()
            return envelope.failure_payload(
                summary=f"{tool_name} exceeded its deadline.",
                failure_class="deadline_exceeded",
                error=f"{tool_name} exceeded {deadline_ms}ms deadline",
                retryable=False,
                recommended_next_action=recommended_next_action,
            )
        except Exception as exc:
            failure_class = classify_failure(exc)
            return envelope.failure_payload(
                summary=f"{tool_name} failed.",
                failure_class=failure_class,
                error=str(exc),
                retryable=False if failure_class in {"network_timeout", "provider_error", "auth_failed", "blocked_by_safety", "policy_reject", "unsupported_operation"} else True,
                recommended_next_action=recommended_next_action,
            )
        finally:
            executor.shutdown(wait=False, cancel_futures=True)


def _research_read_failure_is_retryable(payload: dict[str, Any]) -> bool:
    if not isinstance(payload, dict) or payload.get("ok") is True:
        return False
    tool_execution = (
        payload.get("toolExecution")
        if isinstance(payload.get("toolExecution"), dict)
        else {}
    )
    status_value = payload.get("status") or tool_execution.get("statusCode")
    try:
        status_code = int(status_value)
    except (TypeError, ValueError):
        status_code = 0
    if status_code in {408, 425, 429} or status_code >= 500:
        return True
    failure_class = _safe_text(
        payload.get("failureClass") or tool_execution.get("failureClass")
    ).lower()
    if failure_class in {
        "deadline_exceeded",
        "network_timeout",
        "connection_error",
        "connection_reset",
        "tls_error",
        "rate_limited",
        "service_unavailable",
    }:
        return True
    failure_text = " ".join(
        _safe_text(value)
        for value in (
            payload.get("error"),
            payload.get("reason"),
            payload.get("missingContentReason"),
            tool_execution.get("error"),
            tool_execution.get("summary"),
        )
        if _safe_text(value)
    ).lower()
    return bool(
        re.search(
            r"\b(?:timed?\s*out|timeout|connection\s+(?:reset|refused|closed)|"
            r"tls|ssl|handshake|temporar(?:y|ily) unavailable|rate\s*limit|too many requests)\b|"
            r"\b(?:408|425|429|5\d\d)\b",
            failure_text,
        )
    )


def _source_router_search(**kwargs: Any) -> str:
    # Keep legacy tests monkeypatchable with a SimpleNamespace(func=...), while
    # production routes through the Source Router rather than the raw web_search
    # primitive.
    if web_search is not None and getattr(web_search, "func", None) and not getattr(web_search, "name", None):
        return web_search.func(**kwargs)
    return source_router_search(**kwargs)


def _should_try_context7_source(question: str, source_policy: str) -> bool:
    policy = _safe_text(source_policy).lower()
    text = f"{question}\n{source_policy}".lower()
    if any(marker in policy for marker in ("official_docs_first", "context7", "technical_docs")):
        return True
    # Context7 is a software/library documentation source, not a generic
    # "official or primary" source.  A legal or policy question mentioning
    # documentation obligations must stay on the normal Source Router.
    return bool(
        re.search(
            r"\b(?:api|sdk|framework|library|package|module|next\.js|react|expo|electron|python|typescript)\b|"
            r"(?:软件|开发框架|代码库|程序库|依赖包|接口文档|技术文档)",
            text,
            re.IGNORECASE,
        )
    )


def _run_coro_blocking(coro: Any, *, timeout_seconds: float) -> Any:
    async def _bounded() -> Any:
        return await asyncio.wait_for(coro, timeout=max(0.1, timeout_seconds))

    try:
        asyncio.get_running_loop()
    except RuntimeError:
        return asyncio.run(_bounded())
    with concurrent.futures.ThreadPoolExecutor(max_workers=1) as executor:
        future = executor.submit(lambda: asyncio.run(_bounded()))
        return future.result(timeout=max(timeout_seconds + 1.0, 1.0))


def _compact_mcp_text(payload: Any, *, limit: int = 6000) -> str:
    chunks: list[str] = []

    def visit(value: Any, depth: int = 0) -> None:
        if len("\n".join(chunks)) >= limit or depth > 5:
            return
        if isinstance(value, str):
            text = _safe_text(value)
            if text:
                chunks.append(text)
            return
        if isinstance(value, list):
            for item in value[:20]:
                visit(item, depth + 1)
            return
        if not isinstance(value, dict):
            return
        for key in ("text", "markdown", "content", "contents", "result", "docs", "document", "output"):
            if key in value:
                visit(value.get(key), depth + 1)

    visit(payload)
    text = "\n\n".join(dict.fromkeys(chunks))
    return text[:limit]


def _mcp_payload_is_error(payload: Any) -> bool:
    if isinstance(payload, dict):
        if payload.get("isError") is True:
            return True
        return any(_mcp_payload_is_error(value) for value in payload.values())
    if isinstance(payload, list):
        return any(_mcp_payload_is_error(item) for item in payload[:20])
    if isinstance(payload, str):
        lowered = payload.lower()
        return "mcp error" in lowered or "input validation error" in lowered
    return False


def _extract_context7_library_id(payload: Any) -> str:
    text = _compact_mcp_text(payload, limit=3000)
    for pattern in (
        r"(?im)^\s*(/[^/\s]+/[^/\s]+)\s*$",
        r"(?im)Context7-compatible library ID:\s*(`?)(/[^`\s]+)\1",
        r"(?im)\b(/[^/\s]+/[^/\s]+)\b",
    ):
        match = re.search(pattern, text)
        if match:
            return str(match.group(match.lastindex or 1) or "").strip("` ")
    return ""


def _context7_tool_names() -> tuple[str, str, str]:
    for server_name, tools in list((getattr(mcp_manager, "_server_tools", {}) or {}).items()):
        tool_names = [str(getattr(tool, "name", "") or "").strip() for tool in list(tools or [])]
        lowered = {name.lower(): name for name in tool_names}
        server_key = str(server_name or "").lower()
        looks_like_context7 = "context7" in server_key or any("context7" in name.lower() for name in tool_names)
        if not looks_like_context7 and not any(name in lowered for name in ("resolve-library-id", "get-library-docs", "query-docs")):
            continue
        resolve_tool = (
            lowered.get("resolve-library-id")
            or lowered.get("resolve_library_id")
            or next((name for name in tool_names if "resolve" in name.lower() and "library" in name.lower()), "")
        )
        docs_tool = (
            lowered.get("get-library-docs")
            or lowered.get("get_library_docs")
            or lowered.get("query-docs")
            or lowered.get("query_docs")
            or next((name for name in tool_names if ("docs" in name.lower() or "document" in name.lower())), "")
        )
        if docs_tool:
            return str(server_name), resolve_tool, docs_tool
    return "", "", ""


async def _call_context7_source_async(question: str) -> dict[str, Any]:
    await mcp_manager.initialize()
    server_name, resolve_tool, docs_tool = _context7_tool_names()
    if not server_name or not docs_tool:
        return {"ok": False, "error": "context7_mcp_not_available", "attempts": []}
    attempts: list[dict[str, Any]] = []
    library_id = ""
    if resolve_tool:
        for arguments in ({"query": question[:120]}, {"libraryName": question[:120]}):
            try:
                resolved = await mcp_manager.call_tool(
                    server_name=server_name,
                    tool_name=resolve_tool,
                    arguments=arguments,
                )
                is_error = _mcp_payload_is_error(resolved)
                attempts.append({"tool": resolve_tool, "ok": not is_error, "argumentsShape": sorted(arguments)})
                if is_error:
                    continue
                library_id = _extract_context7_library_id(resolved)
                if library_id:
                    break
            except Exception as exc:
                attempts.append({"tool": resolve_tool, "ok": False, "argumentsShape": sorted(arguments), "error": str(exc)[:300]})
    docs_attempts: list[dict[str, Any]] = []
    if library_id:
        docs_attempts.extend(
            [
                {"libraryId": library_id, "topic": question[:240], "tokens": 3000},
                {"context7CompatibleLibraryID": library_id, "topic": question[:240], "tokens": 3000},
                {"libraryId": library_id, "query": question[:240]},
            ]
        )
    else:
        return {"ok": False, "error": "context7_library_id_not_resolved", "serverName": server_name, "attempts": attempts}
    last_error = ""
    for arguments in docs_attempts:
        try:
            docs = await mcp_manager.call_tool(server_name=server_name, tool_name=docs_tool, arguments=arguments)
            text = _compact_mcp_text(docs, limit=_RESEARCH_SOURCE_CAPTURE_CHARS)
            is_error = _mcp_payload_is_error(docs) or "mcp error" in text.lower()
            attempts.append({"tool": docs_tool, "ok": bool(text) and not is_error, "argumentsShape": sorted(arguments)})
            if text and not is_error:
                return {
                    "ok": True,
                    "serverName": server_name,
                    "toolName": docs_tool,
                    "libraryId": library_id,
                    "text": text,
                    "attempts": attempts,
                }
            if is_error:
                last_error = _compact_research_text(text, limit=300) or "context7_docs_error"
        except Exception as exc:
            last_error = str(exc)
            attempts.append({"tool": docs_tool, "ok": False, "argumentsShape": sorted(arguments), "error": last_error[:300]})
    return {"ok": False, "error": last_error or "context7_docs_empty", "serverName": server_name, "attempts": attempts}


def _run_context7_source(question: str, *, tool_call_id: str) -> dict[str, Any]:
    try:
        payload = _run_coro_blocking(_call_context7_source_async(question), timeout_seconds=20)
    except (asyncio.CancelledError, Exception) as exc:
        return {
            "shardId": "context7_docs",
            "kind": "technical_docs",
            "query": question,
            "ok": False,
            "provider": "context7",
            "resultCount": 0,
            "results": [],
            "fetchedTopSources": [],
            "errors": [str(exc)[:500] or "context7_failed"],
            "sourceRouter": {"selectedProvider": "context7", "fallbackReason": "context7_exception"},
        }
    if not payload.get("ok"):
        return {
            "shardId": "context7_docs",
            "kind": "technical_docs",
            "query": question,
            "ok": False,
            "provider": "context7",
            "resultCount": 0,
            "results": [],
            "fetchedTopSources": [],
            "errors": [_safe_text(payload.get("error")) or "context7_unavailable"],
            "providerAttemptMatrix": payload.get("attempts") or [],
            "sourceRouter": {"selectedProvider": "context7", "fallbackReason": "context7_unavailable"},
        }
    original_text = _safe_text(payload.get("text"))
    text = _research_source_excerpt(original_text, question, limit=_RESEARCH_SOURCE_CAPTURE_CHARS)
    library_id = _safe_text(payload.get("libraryId"))
    url = f"mcp://context7/{library_id.lstrip('/')}" if library_id else "mcp://context7/docs"
    quality = {
        "host": "context7",
        "authorityScore": 92,
        "tier": "primary",
        "authorityTier": "official_docs_mcp",
        "reasons": ["context7_mcp", "official_docs_first"],
    }
    return {
        "shardId": "context7_docs",
        "kind": "technical_docs",
        "query": question,
        "ok": True,
        "provider": "context7",
        "networkRoute": "mcp",
        "sourceCapability": "official_technical_docs",
        "sourceRouter": {"selectedProvider": "context7", "networkRoute": "mcp", "sourcePolicy": "official_docs_first"},
        "providerAttemptMatrix": payload.get("attempts") or [],
        "resultCount": 1,
        "results": [
            {
                "resultRank": 1,
                "title": f"Context7 technical docs{f' {library_id}' if library_id else ''}",
                "url": url,
                "finalUrl": url,
                "snippet": text[:600],
                "sourceQualityHints": quality,
            }
        ],
        "fetchedTopSources": [
            {
                "url": url,
                "ok": True,
                "title": f"Context7 technical docs{f' {library_id}' if library_id else ''}",
                "status": "mcp",
                "text": text,
                "textPreview": text[:1200],
                "contentChars": len(text),
                "originalContentChars": len(original_text),
                "retrievedAt": _utc_now_iso(),
                "omittedChars": max(0, len(original_text) - len(text)),
                "evidenceSelection": "query_focused_excerpt",
                "extractionQuality": "context7_mcp_docs",
                "sourceCapability": "official_technical_docs",
            }
        ],
        "errors": [],
    }


def _run_seed_url_shard(
    shard: dict[str, Any],
    *,
    allowed_domains: list[str],
    blocked_domains: list[str],
    source_policy: str,
    use_agent_browser_profile: bool,
    tool_call_id: str,
    read_attempt_ledger: _ResearchReadAttemptLedger | None = None,
    read_round: int = 1,
    cancel_event: threading.Event | None = None,
) -> dict[str, Any]:
    """Read an explicit seed URL before depending on a search provider."""

    url = _safe_text(shard.get("seedUrl") or shard.get("query"))
    if cancel_event is not None and cancel_event.is_set():
        return {
            **shard,
            "ok": False,
            "provider": "explicit_seed_url",
            "resultCount": 0,
            "results": [],
            "fetchedTopSources": [],
            "errors": ["research_shard_cancelled"],
        }
    host = _host(url)
    if blocked_domains and any(host == domain or host.endswith(f".{domain}") for domain in blocked_domains):
        return {
            **shard,
            "ok": False,
            "provider": "explicit_seed_url",
            "resultCount": 0,
            "results": [],
            "fetchedTopSources": [],
            "errors": ["seed_url_blocked_by_domain_policy"],
        }
    if allowed_domains and not any(host == domain or host.endswith(f".{domain}") for domain in allowed_domains):
        return {
            **shard,
            "ok": False,
            "provider": "explicit_seed_url",
            "resultCount": 0,
            "results": [],
            "fetchedTopSources": [],
            "errors": ["seed_url_outside_allowed_domains"],
        }

    read_claim = (
        read_attempt_ledger.claim(url, round_index=read_round)
        if read_attempt_ledger is not None
        else ""
    )
    cached_read: dict[str, Any] | None = None
    if read_attempt_ledger is not None and not read_claim:
        cached_read = read_attempt_ledger.cached_payload(
            url,
            wait_seconds=_RESEARCH_SOURCE_READ_DEADLINE_MS / 1000.0,
        )
        if cached_read is None:
            return {
                **shard,
                "ok": False,
                "provider": "explicit_seed_url",
                "resultCount": 0,
                "results": [],
                "fetchedTopSources": [],
                "errors": ["source_read_deduplicated"],
            }

    if cached_read is not None:
        read_payload = dict(cached_read.get("readPayload") or {})
        original_text = _safe_text(cached_read.get("originalText"))
    else:
        read_payload = _call_with_deadline(
            lambda: web_read.func(
                url=url,
                mode="auto",
                headless=True,
                referer_mode="none",
                referer_url="",
                maxTextChars=_RESEARCH_SOURCE_READ_CHARS,
                useAgentBrowserProfile=bool(use_agent_browser_profile),
                tool_call_id=tool_call_id,
            ),
            deadline_ms=_RESEARCH_SOURCE_READ_DEADLINE_MS,
            tool_name="web_read",
            family="research",
            recommended_next_action="保留该 seed 为 unavailable，或在同一 Research brief 内补充另一条官方来源。",
        )
        original_text = _safe_text(
            read_payload.get("text")
            or read_payload.get("markdown")
            or read_payload.get("textPreview")
        )
    if cancel_event is not None and cancel_event.is_set():
        if read_attempt_ledger is not None:
            read_attempt_ledger.finish(read_claim, retryable=False)
        return {
            **shard,
            "ok": False,
            "provider": "explicit_seed_url",
            "resultCount": 0,
            "results": [],
            "fetchedTopSources": [],
            "errors": ["research_shard_cancelled"],
        }
    evidence_query = _safe_text(shard.get("evidenceQuery") or shard.get("reason") or shard.get("query"))
    original_content_chars = _research_original_content_chars(original_text, read_payload)
    text = _research_source_excerpt(original_text, evidence_query, limit=_RESEARCH_SOURCE_CAPTURE_CHARS)
    title = _safe_text(read_payload.get("title"))[:300] or url
    quality = _source_quality(
        url,
        allowed_domains=allowed_domains,
        source_policy=source_policy,
        title=title,
        snippet=text[:600],
    )
    result = {
        "resultRank": 1,
        "title": title,
        "url": url,
        "finalUrl": _safe_text(read_payload.get("finalUrl")) or url,
        "snippet": text[:600],
        "sourceQualityHints": quality,
        "publishedAt": read_payload.get("publishedAt"),
        "updatedAt": read_payload.get("updatedAt"),
        "version": read_payload.get("version"),
    }
    temporal = _source_temporal_evidence(read_payload, result)
    fetched = {
        "url": url,
        "ok": bool(read_payload.get("ok") and text),
        "title": title,
        "status": read_payload.get("status"),
        "finalUrl": _safe_text(read_payload.get("finalUrl")) or url,
        "text": text,
        "textPreview": text[:1200],
        "contentChars": len(text),
        "originalContentChars": original_content_chars,
        "metadata": read_payload.get("metadata") if isinstance(read_payload.get("metadata"), dict) else {},
        "retrievedAt": temporal.get("retrievedAt"),
        "publishedAt": temporal.get("publishedAt"),
        "updatedAt": temporal.get("updatedAt"),
        "version": temporal.get("version"),
        "temporalEvidence": temporal,
        "omittedChars": max(0, original_content_chars - len(text)),
        "evidenceSelection": "query_focused_excerpt",
        "extractionQuality": read_payload.get("extractionQuality") or ("readable" if text else "unreadable"),
        "sourceCapability": read_payload.get("sourceCapability"),
        "providerAttemptMatrix": read_payload.get("providerAttemptMatrix") or read_payload.get("attemptedProviders") or [],
        "rawRef": read_payload.get("rawRef") or read_payload.get("detailRawRef"),
        "missingContentReason": read_payload.get("missingContentReason"),
        "warnings": read_payload.get("warnings") if isinstance(read_payload.get("warnings"), list) else [],
        "failureClass": read_payload.get("failureClass") or (read_payload.get("toolExecution") or {}).get("failureClass"),
        "toolExecution": read_payload.get("toolExecution"),
        "readReuse": "shared_document_cache" if cached_read is not None else None,
    }
    ok = bool(read_payload.get("ok") and text)
    if read_attempt_ledger is not None:
        read_attempt_ledger.finish(
            read_claim,
            final_url=read_payload.get("finalUrl") or url,
            retryable=bool(
                not ok and _research_read_failure_is_retryable(read_payload)
            ),
            cache_payload=(
                {
                    "readPayload": read_payload,
                    "originalText": original_text,
                }
                if ok and cached_read is None
                else None
            ),
        )
    return {
        **shard,
        "ok": ok,
        "provider": "explicit_seed_url",
        "networkRoute": "direct_read",
        "sourceCapability": read_payload.get("sourceCapability") or "seed_url_read",
        "sourceRouter": {"selectedProvider": "explicit_seed_url", "networkRoute": "direct_read", "sourcePolicy": source_policy},
        "providerAttemptMatrix": read_payload.get("providerAttemptMatrix") or read_payload.get("attemptedProviders") or [],
        "resultCount": 1,
        "results": [result],
        "fetchedTopSources": [fetched],
        "errors": [] if ok else [_safe_text(read_payload.get("error")) or "seed_url_unreadable"],
    }


def _run_search_shard(
    shard: dict[str, Any],
    *,
    allowed_domains: list[str],
    blocked_domains: list[str],
    source_policy: str,
    max_rounds: int,
    use_agent_browser_profile: bool,
    tool_call_id: str,
    claimed_read_urls: set[str] | None = None,
    claimed_read_urls_lock: threading.Lock | None = None,
    read_attempt_ledger: _ResearchReadAttemptLedger | None = None,
    read_round: int = 1,
    cancel_event: threading.Event | None = None,
) -> dict[str, Any]:
    if cancel_event is not None and cancel_event.is_set():
        return {
            **shard,
            "ok": False,
            "provider": None,
            "resultCount": 0,
            "results": [],
            "fetchedTopSources": [],
            "errors": ["research_shard_cancelled"],
        }
    seed_url = _safe_text(shard.get("seedUrl"))
    if str(shard.get("kind") or "").strip() == "seed_url" and seed_url:
        return _run_seed_url_shard(
            shard,
            allowed_domains=allowed_domains,
            blocked_domains=blocked_domains,
            source_policy=source_policy,
            use_agent_browser_profile=use_agent_browser_profile,
            tool_call_id=tool_call_id,
            read_attempt_ledger=read_attempt_ledger,
            read_round=read_round,
            cancel_event=cancel_event,
        )
    query = _safe_text(shard.get("query"))
    source_intent = _safe_text(shard.get("sourceIntent")).lower()
    site_domains = _query_site_domains(query)
    video_research = _is_video_research(query, source_policy, shard.get("kind"))
    search_payload = _call_with_deadline(
        lambda: _source_router_search(
            query=query,
            limit=8,
            search_engine="auto",
            mode="auto",
            referer_mode="none",
            referer_url="",
            useAgentBrowserProfile=bool(use_agent_browser_profile),
            tool_call_id=tool_call_id,
        ),
        deadline_ms=min(_RESEARCH_SHARD_DEADLINE_MS, 45_000),
        tool_name="source_router_search",
        family="research",
        recommended_next_action="换关键词、限定权威域名，或保留该 shard 为 failed_source。",
    )
    if cancel_event is not None and cancel_event.is_set():
        return {
            **shard,
            "ok": False,
            "provider": None,
            "resultCount": 0,
            "results": [],
            "fetchedTopSources": [],
            "errors": ["research_shard_cancelled"],
        }
    if search_payload.get("kind") == "tool_deadline_envelope":
        return {
            **shard,
            "ok": False,
            "provider": None,
            "failureClass": search_payload.get("failureClass") or "deadline_exceeded",
            "retryable": bool(search_payload.get("retryable")),
            "elapsedMs": search_payload.get("elapsedMs"),
            "resultCount": 0,
            "results": [],
            "fetchedTopSources": [],
            "errors": [_safe_text(search_payload.get("error")) or "search_deadline_exceeded"],
            "toolExecution": search_payload.get("toolExecution"),
        }
    raw_results = search_payload.get("results") if isinstance(search_payload.get("results"), list) else []
    results: list[dict[str, Any]] = []
    read_eligible_urls: set[str] = set()
    for index, result in enumerate(raw_results, start=1):
        url = _safe_text((result or {}).get("url"))
        host = _host(url)
        if blocked_domains and any(host == domain or host.endswith(f".{domain}") for domain in blocked_domains):
            continue
        if allowed_domains and not any(host == domain or host.endswith(f".{domain}") for domain in allowed_domains):
            continue
        if site_domains and not _host_matches_domains(host, site_domains):
            continue
        title = _safe_text((result or {}).get("title"))[:300]
        snippet = _safe_text((result or {}).get("snippet"))[:600]
        quality = _source_quality(
            url,
            allowed_domains=allowed_domains,
            source_policy=source_policy,
            title=title,
            snippet=snippet,
            video_research=video_research,
            question=query,
        )
        discovery_relevance = _source_relevance_score(
            query,
            title=title,
            snippet=snippet,
        )
        if (site_domains or _source_matches_intent(quality, source_intent)) and (
            discovery_relevance >= 10 or int(quality.get("authorityScore") or 0) >= 70
        ):
            read_eligible_urls.add(url)
        results.append(
            {
                "resultRank": index,
                "title": title,
                "url": url,
                "finalUrl": url,
                "snippet": snippet,
                "discoveryRelevanceScore": discovery_relevance,
                "sourceQualityHints": quality,
                "publishedAt": (result or {}).get("publishedAt") or (result or {}).get("publishedDate"),
                "updatedAt": (result or {}).get("updatedAt") or (result or {}).get("updatedDate"),
                "version": (result or {}).get("version"),
                "sourceIntent": source_intent or None,
            }
        )
    top_results = sorted(
        results,
        key=lambda item: (
            int(item.get("discoveryRelevanceScore") or 0),
            int((item.get("sourceQualityHints") or {}).get("authorityScore") or 0),
            _research_document_priority(_safe_text(item.get("url")), question=query),
        ),
        reverse=True,
    )
    fetched: list[dict[str, Any]] = []
    accepted_read_count = 0
    for result in top_results:
            if cancel_event is not None and cancel_event.is_set():
                break
            if len(fetched) >= 4 or accepted_read_count >= 2:
                break
            url = _safe_text(result.get("url"))
            if not url:
                continue
            if url not in read_eligible_urls:
                continue
            source_identity = _research_document_identity(url, question=query)
            read_claim = ""
            cached_read: dict[str, Any] | None = None
            if read_attempt_ledger is not None:
                read_claim = read_attempt_ledger.claim(url, round_index=read_round)
                if not read_claim:
                    cached_read = read_attempt_ledger.cached_payload(
                        url,
                        wait_seconds=_RESEARCH_SOURCE_READ_DEADLINE_MS / 1000.0,
                    )
                    if cached_read is None:
                        continue
            elif claimed_read_urls is not None and claimed_read_urls_lock is not None:
                with claimed_read_urls_lock:
                    if source_identity in claimed_read_urls:
                        continue
                    claimed_read_urls.add(source_identity)
            if cached_read is not None:
                read_payload = dict(cached_read.get("readPayload") or {})
                original_text = _safe_text(cached_read.get("originalText"))
            else:
                read_payload = _call_with_deadline(
                    lambda: web_read.func(
                        url=url,
                        mode="auto",
                        headless=True,
                        referer_mode="none",
                        referer_url="",
                        maxTextChars=_RESEARCH_SOURCE_READ_CHARS,
                        useAgentBrowserProfile=bool(use_agent_browser_profile),
                        tool_call_id=tool_call_id,
                    ),
                    deadline_ms=_RESEARCH_SOURCE_READ_DEADLINE_MS,
                    tool_name="web_read",
                    family="research",
                    recommended_next_action="标记该 source unavailable，换可访问来源或降低置信度。",
                )
                original_text = _safe_text(
                    read_payload.get("text")
                    or read_payload.get("markdown")
                    or read_payload.get("textPreview")
                )
            if cancel_event is not None and cancel_event.is_set():
                if read_attempt_ledger is not None:
                    read_attempt_ledger.finish(read_claim, retryable=False)
                break
            text = original_text
            read_attempts: list[dict[str, Any]] = []
            if read_payload.get("providerAttemptMatrix"):
                for item in list(read_payload.get("providerAttemptMatrix") or [])[:6]:
                    if isinstance(item, dict):
                        read_attempts.append(item)
            elif read_payload.get("toolExecution"):
                read_attempts.append({"provider": "builtin_scrapling", "status": "error", "failureClass": (read_payload.get("toolExecution") or {}).get("failureClass")})
            else:
                read_attempts.append({"provider": "builtin_scrapling", "status": "success" if read_payload.get("ok") else "error"})
            needs_jina_fallback = bool(not read_payload.get("ok") or not text or _source_noise_reasons(text))
            if needs_jina_fallback and _jina_api_key():
                jina_payload = _read_with_jina(url)
                read_attempts.append(
                    {
                        "provider": "jina",
                        "status": "success" if jina_payload.get("ok") else "error",
                        "failureClass": jina_payload.get("failureClass"),
                        "reason": jina_payload.get("reason"),
                    }
                )
                jina_text = _safe_text(jina_payload.get("text") or jina_payload.get("textPreview"))
                if jina_payload.get("ok") and jina_text and not _source_noise_reasons(jina_text):
                    read_payload = {
                        **read_payload,
                        **jina_payload,
                        "title": _safe_text(read_payload.get("title")) or _safe_text(jina_payload.get("title")),
                        "providerAttemptMatrix": read_attempts,
                    }
                    original_text = jina_text
                    text = jina_text
            elif needs_jina_fallback:
                read_attempts.append(
                    {
                        "provider": "jina",
                        "status": "skipped",
                        "failureClass": "credential_missing",
                        "reason": "missing_env:JINA_API_KEY",
                    }
                )
            if cancel_event is not None and cancel_event.is_set():
                if read_attempt_ledger is not None:
                    read_attempt_ledger.finish(read_claim, retryable=False)
                break
            extraction_quality = read_payload.get("extractionQuality")
            if not extraction_quality:
                extraction_quality = "readable" if read_payload.get("ok") and text else "unreadable"
            temporal = _source_temporal_evidence(read_payload, result)
            original_content_chars = _research_original_content_chars(original_text, read_payload)
            text = _research_source_excerpt(original_text, query, limit=_RESEARCH_SOURCE_CAPTURE_CHARS)
            final_url = _safe_text(read_payload.get("finalUrl")) or url
            if read_attempt_ledger is not None:
                read_attempt_ledger.finish(
                    read_claim,
                    final_url=final_url,
                    retryable=bool(
                        not (read_payload.get("ok") and text)
                        and _research_read_failure_is_retryable(read_payload)
                    ),
                    cache_payload=(
                        {
                            "readPayload": read_payload,
                            "originalText": original_text,
                        }
                        if read_payload.get("ok") and original_text and cached_read is None
                        else None
                    ),
                )
            final_host = _host(final_url)
            if site_domains and not _host_matches_domains(final_host, site_domains):
                continue
            final_quality = _source_quality(
                final_url,
                allowed_domains=allowed_domains,
                source_policy=source_policy,
                title=_safe_text(read_payload.get("title") or result.get("title"))[:300],
                snippet="\n".join(
                    part
                    for part in (_safe_text(result.get("snippet")), text[:600])
                    if part
                ),
                video_research=video_research,
                question=query,
            )
            result["sourceQualityHints"] = final_quality
            if not _source_matches_intent(final_quality, source_intent):
                continue
            result["finalUrl"] = final_url
            fetched.append(
                {
                    "url": url,
                    "finalUrl": final_url,
                    "ok": bool(read_payload.get("ok")),
                    "title": _safe_text(read_payload.get("title"))[:300],
                    "status": read_payload.get("status"),
                    "text": text,
                    "textPreview": text[:1200],
                    "contentChars": len(text),
                    "originalContentChars": original_content_chars,
                    "metadata": read_payload.get("metadata") if isinstance(read_payload.get("metadata"), dict) else {},
                    "retrievedAt": temporal.get("retrievedAt"),
                    "publishedAt": temporal.get("publishedAt"),
                    "updatedAt": temporal.get("updatedAt"),
                    "version": temporal.get("version"),
                    "temporalEvidence": temporal,
                    "omittedChars": max(0, original_content_chars - len(text)),
                    "evidenceSelection": "query_focused_excerpt",
                    "extractionQuality": extraction_quality,
                    "sourceCapability": read_payload.get("sourceCapability"),
                    "providerAttemptMatrix": read_payload.get("providerAttemptMatrix") or read_payload.get("attemptedProviders") or read_attempts,
                    "rawRef": read_payload.get("rawRef") or read_payload.get("detailRawRef"),
                    "missingContentReason": read_payload.get("missingContentReason"),
                    "warnings": read_payload.get("warnings") if isinstance(read_payload.get("warnings"), list) else [],
                    "failureClass": read_payload.get("failureClass") or (read_payload.get("toolExecution") or {}).get("failureClass"),
                    "toolExecution": read_payload.get("toolExecution"),
                    "readReuse": "shared_document_cache" if cached_read is not None else None,
                }
            )
            preflight_gate = _source_quality_gate(
                question=query,
                result=result,
                read_payload={**read_payload, "text": text},
                source_policy=source_policy,
            )
            fetched[-1]["preflightSourceQualityGate"] = preflight_gate
            # A cached projection gives this facet evidence but does not add a
            # new independent document. Keep the shard's two-source network
            # budget available for additional corroboration.
            if preflight_gate.get("selectedForEvidence") and cached_read is None:
                accepted_read_count += 1
    return {
        **shard,
        "ok": bool(search_payload.get("ok")),
        "provider": search_payload.get("provider"),
        "failureClass": search_payload.get("failureClass"),
        "retryable": search_payload.get("retryable"),
        "elapsedMs": search_payload.get("elapsedMs"),
        "networkRoute": search_payload.get("networkRoute"),
        "sourceCapability": search_payload.get("sourceCapability"),
        "sourceRouter": search_payload.get("sourceRouter"),
        "sourceIntent": source_intent or None,
        "siteDomains": site_domains,
        "providerAttemptMatrix": search_payload.get("providerAttemptMatrix") or search_payload.get("attemptedProviders"),
        "resultCount": len(results),
        "results": top_results,
        "fetchedTopSources": fetched,
        "errors": [] if search_payload.get("ok") else [_safe_text(search_payload.get("error")) or "search_failed"],
    }


def _run_search_shards(
    shards: list[dict[str, Any]],
    *,
    allowed_domains: list[str],
    blocked_domains: list[str],
    source_policy: str,
    max_rounds: int,
    use_agent_browser_profile: bool,
    tool_call_id: str,
    already_read_urls: set[str] | None = None,
    read_attempt_ledger: _ResearchReadAttemptLedger | None = None,
    read_round: int = 1,
) -> list[dict[str, Any]]:
    if not shards:
        return []
    completed: list[dict[str, Any] | None] = [None] * len(shards)
    claimed_read_urls = already_read_urls if already_read_urls is not None else set()
    claimed_read_urls_lock = threading.Lock()
    attempt_ledger = read_attempt_ledger or _ResearchReadAttemptLedger(
        terminal_identities=set(claimed_read_urls),
    )
    if read_attempt_ledger is not None:
        for identity in set(claimed_read_urls):
            attempt_ledger.mark_terminal(identity, already_identity=True)
    cancel_event = threading.Event()
    timed_out = False
    executor = ThreadPoolExecutor(
        max_workers=max(1, min(len(shards), _RESEARCH_MAX_PARALLEL_SEARCH_SHARDS))
    )
    try:
        futures = {
            executor.submit(
                _run_search_shard,
                shard,
                allowed_domains=allowed_domains,
                blocked_domains=blocked_domains,
                source_policy=source_policy,
                max_rounds=max_rounds,
                use_agent_browser_profile=bool(use_agent_browser_profile),
                tool_call_id=tool_call_id,
                claimed_read_urls=claimed_read_urls,
                claimed_read_urls_lock=claimed_read_urls_lock,
                read_attempt_ledger=attempt_ledger,
                read_round=read_round,
                cancel_event=cancel_event,
            ): index
            for index, shard in enumerate(shards)
        }
        try:
            for future in as_completed(futures, timeout=max(_RESEARCH_TOOL_DEADLINE_MS / 1000.0, 0.1)):
                index = futures[future]
                shard = shards[index]
                try:
                    completed[index] = future.result()
                except Exception as exc:
                    completed[index] = {
                        **shard,
                        "ok": False,
                        "provider": None,
                        "resultCount": 0,
                        "results": [],
                        "fetchedTopSources": [],
                        "errors": [str(exc) or "research_shard_failed"],
                    }
        except TimeoutError:
            timed_out = True
            cancel_event.set()
            for future, index in futures.items():
                if future.done():
                    continue
                future.cancel()
                shard = shards[index]
                completed[index] = {
                    **shard,
                    "ok": False,
                    "provider": None,
                    "resultCount": 0,
                    "results": [],
                    "fetchedTopSources": [],
                    "errors": ["research_shard_deadline_exceeded"],
                    "toolExecution": _deadline_failure(
                        tool_name="research_broker",
                        family="research",
                        deadline_ms=_RESEARCH_TOOL_DEADLINE_MS,
                        summary="Research shard exceeded the parallel search-batch deadline.",
                        failure_class="deadline_exceeded",
                        error="research_broker parallel search-batch deadline exceeded",
                        recommended_next_action="Use partial evidence, narrow the query, or run another focused research pass.",
                    ).get("toolExecution"),
                }
    finally:
        if timed_out:
            cancel_event.set()
        executor.shutdown(wait=not timed_out, cancel_futures=True)
    return [item for item in completed if item is not None]


def _read_source_count(shards: list[dict[str, Any]], *, question: str = "") -> int:
    urls: set[str] = set()
    for shard in shards:
        for item in list(shard.get("fetchedTopSources") or []):
            if isinstance(item, dict) and item.get("ok") and _safe_text(item.get("text") or item.get("textPreview")):
                url = _safe_text(item.get("url"))
                if url:
                    urls.add(
                        _research_document_identity(
                            _safe_text(item.get("finalUrl") or url),
                            question=question,
                        )
                    )
    return len(urls)


def _research_source_candidates(
    question: str,
    shards: list[dict[str, Any]],
    *,
    source_policy: str,
) -> list[dict[str, Any]]:
    selected_by_document: dict[str, dict[str, Any]] = {}
    entity_hints = _catalog_official_entity_hints(question)
    if len(entity_hints) < 2:
        entity_hints = []
    explicit_facets = [
        (goal, _research_facet_id(kind))
        for goal, kind in _build_explicit_question_facets(question)
        if _research_facet_id(kind)
    ]
    for shard in shards:
        evidence_query = _safe_text(shard.get("evidenceQuery") or shard.get("query")) or question
        query_entity_indexes = _research_entity_indexes_in_text(
            entity_hints,
            evidence_query,
        )
        fetched = _fetched_source_map([shard], question=evidence_query)
        for result in list(shard.get("results") or []):
            if not isinstance(result, dict):
                continue
            requested_url = _safe_text(result.get("url"))
            if not requested_url:
                continue
            read_payload = fetched.get(requested_url) or fetched.get(_safe_text(result.get("finalUrl"))) or {}
            resolved_url = _safe_text(read_payload.get("finalUrl") or result.get("finalUrl")) or requested_url
            normalized_result = {
                **result,
                "url": resolved_url,
                "requestedUrl": requested_url,
                "sourceIntent": _safe_text(shard.get("sourceIntent")),
            }
            gate = _source_quality_gate(
                question=evidence_query,
                result=normalized_result,
                read_payload=read_payload,
                source_policy=source_policy,
            )
            document_id = _research_document_identity(resolved_url, question=question)
            if not document_id:
                continue
            quality = dict(result.get("sourceQualityHints") or {})
            read_text = _safe_text(read_payload.get("text") or read_payload.get("markdown") or read_payload.get("textPreview"))
            if gate.get("selectedForEvidence") is True and not _research_source_matches_root_subject(
                question,
                title=_safe_text(read_payload.get("title") or result.get("title")),
                url=resolved_url,
                snippet=_safe_text(result.get("snippet")),
                text=read_text,
            ):
                gate = {
                    **gate,
                    "selectedForEvidence": False,
                    "rejectedReason": "research_root_subject_mismatch",
                }
            if gate.get("selectedForEvidence") is True and query_entity_indexes:
                source_entity_indexes = _research_source_entity_indexes(
                    entity_hints,
                    host=_host(resolved_url),
                    url=resolved_url,
                    title=_safe_text(read_payload.get("title") or result.get("title")),
                    text=read_text,
                    evidence_query=evidence_query,
                )
                if not query_entity_indexes.intersection(source_entity_indexes):
                    gate = {
                        **gate,
                        "selectedForEvidence": False,
                        "rejectedReason": "research_query_entity_mismatch",
                    }
            body_relevance = _source_relevance_score(
                evidence_query,
                title=_safe_text(read_payload.get("title") or result.get("title")),
                snippet=_safe_text(result.get("snippet")),
                text=read_text,
            )
            rank = (
                1 if gate.get("selectedForEvidence") else 0,
                _research_document_priority(resolved_url, question=question),
                int(quality.get("authorityScore") or 0),
                body_relevance,
                len(read_text),
            )
            facet_id = (
                _safe_text(shard.get("researchFacetId"))
                or _research_facet_id(shard.get("kind"))
            )
            evidence_view = {
                "shardId": _safe_text(shard.get("shardId")),
                "researchFacetId": facet_id,
                "evidenceQuery": evidence_query,
                "selectedForEvidence": gate.get("selectedForEvidence") is True,
                "bodyRelevance": body_relevance,
                "readPayload": read_payload,
            }
            evidence_views = [evidence_view]
            if gate.get("selectedForEvidence") is True and read_text:
                for facet_goal, explicit_facet_id in explicit_facets:
                    if explicit_facet_id == facet_id:
                        continue
                    facet_relevance = _source_relevance_score(
                        facet_goal,
                        title=_safe_text(read_payload.get("title") or result.get("title")),
                        text=read_text,
                    )
                    if facet_relevance < _source_minimum_body_relevance(facet_goal):
                        continue
                    evidence_views.append(
                        {
                            "shardId": _safe_text(shard.get("shardId")),
                            "researchFacetId": explicit_facet_id,
                            "evidenceQuery": facet_goal,
                            "selectedForEvidence": True,
                            "bodyRelevance": facet_relevance,
                            "readPayload": read_payload,
                        }
                    )
            facet_ids = list(
                dict.fromkeys(
                    _safe_text(view.get("researchFacetId"))
                    for view in evidence_views
                    if view.get("selectedForEvidence") is True
                    and _safe_text(view.get("researchFacetId"))
                )
            )
            candidate = {
                "documentId": document_id,
                "documentPriority": rank[1],
                "requestedUrl": requested_url,
                "url": resolved_url,
                "result": normalized_result,
                "readPayload": read_payload,
                "quality": quality,
                "gate": gate,
                "shard": shard,
                "evidenceQuery": evidence_query,
                "bodyRelevance": body_relevance,
                "rank": rank,
                "evidenceViews": evidence_views,
                "researchFacetIds": facet_ids,
            }
            existing = selected_by_document.get(document_id)
            if existing is None:
                selected_by_document[document_id] = candidate
                continue
            merged_views = [
                *list(existing.get("evidenceViews") or []),
                *evidence_views,
            ]
            deduped_views: list[dict[str, Any]] = []
            seen_views: set[tuple[str, str, str]] = set()
            for view in merged_views:
                key = (
                    _safe_text(view.get("shardId")),
                    _safe_text(view.get("researchFacetId")),
                    _safe_text(view.get("evidenceQuery")),
                )
                if key in seen_views:
                    continue
                seen_views.add(key)
                deduped_views.append(view)
            facet_ids = list(
                dict.fromkeys(
                    _safe_text(view.get("researchFacetId"))
                    for view in deduped_views
                    if view.get("selectedForEvidence") is True
                    and _safe_text(view.get("researchFacetId"))
                )
            )
            if rank > existing["rank"]:
                candidate["evidenceViews"] = deduped_views
                candidate["researchFacetIds"] = facet_ids
                selected_by_document[document_id] = candidate
            else:
                existing["evidenceViews"] = deduped_views
                existing["researchFacetIds"] = facet_ids
    return list(selected_by_document.values())


def _required_research_facet_queries(
    question: str,
    shards: list[dict[str, Any]],
) -> list[tuple[str, str]]:
    explicit = _build_explicit_question_facets(question)
    if explicit:
        return explicit
    derived: list[tuple[str, str]] = []
    seen: set[str] = set()
    for shard in shards:
        facet_id = _safe_text(shard.get("researchFacetId")) or _research_facet_id(
            shard.get("kind")
        )
        goal = _safe_text(
            shard.get("facetGoal")
            or shard.get("evidenceQuery")
            or shard.get("query")
        )
        if not facet_id or not goal or facet_id in seen:
            continue
        seen.add(facet_id)
        derived.append((goal, f"facet:{facet_id}"))
    return derived


def _selected_facet_evidence_stats(
    question: str,
    shards: list[dict[str, Any]],
    *,
    source_policy: str,
) -> dict[str, Any]:
    required = [
        _research_facet_id(kind)
        for _goal, kind in _required_research_facet_queries(question, shards)
        if _research_facet_id(kind)
    ]
    covered: list[str] = []
    source_counts: dict[str, int] = {facet_id: 0 for facet_id in required}
    for candidate in _research_source_candidates(
        question,
        shards,
        source_policy=source_policy,
    ):
        for facet_id in list(candidate.get("researchFacetIds") or []):
            normalized = _safe_text(facet_id)
            if normalized not in source_counts:
                continue
            source_counts[normalized] += 1
            if normalized not in covered:
                covered.append(normalized)
    missing = [facet_id for facet_id in required if facet_id not in covered]
    return {
        "required": bool(required),
        "complete": not missing,
        "requiredFacetIds": required,
        "coveredFacetIds": covered,
        "missingFacetIds": missing,
        "sourceCountByFacet": source_counts,
    }


def _research_evidence_projection_identities(
    question: str,
    shards: list[dict[str, Any]],
    *,
    source_policy: str,
) -> set[str]:
    projections: set[str] = set()
    for shard in shards:
        facet_id = (
            _safe_text(shard.get("researchFacetId"))
            or _research_facet_id(shard.get("kind"))
        )
        for candidate in _research_source_candidates(
            question,
            [shard],
            source_policy=source_policy,
        ):
            if candidate.get("gate", {}).get("selectedForEvidence") is not True:
                continue
            identity = _safe_text(candidate.get("documentId"))
            if identity:
                evidence_query = _safe_text(
                    shard.get("evidenceQuery") or shard.get("query")
                )
                projections.add(
                    f"{identity}#{facet_id}#{_query_slug(evidence_query)}"
                )
    return projections


def _selected_source_stats(question: str, shards: list[dict[str, Any]], *, source_policy: str) -> dict[str, Any]:
    selected: dict[str, dict[str, Any]] = {}
    for candidate in _research_source_candidates(question, shards, source_policy=source_policy):
        gate = candidate["gate"]
        if not gate.get("selectedForEvidence"):
            continue
        url = candidate["url"]
        result = candidate["result"]
        read_payload = candidate["readPayload"]
        temporal = _source_temporal_evidence(read_payload, result)
        selected[candidate["documentId"]] = {
            "url": url,
            "host": _safe_text((result.get("sourceQualityHints") or {}).get("host")) or _host(url),
            "retrievedAt": temporal.get("retrievedAt"),
            "publishedAt": temporal.get("publishedAt"),
            "updatedAt": temporal.get("updatedAt"),
            "version": temporal.get("version"),
        }
    hosts = {_safe_text(item.get("host")).lower().removeprefix("www.") for item in selected.values() if _safe_text(item.get("host"))}
    dated = sum(research_source_has_dated_evidence(item) for item in selected.values())
    return {
        "selectedSourceCount": len(selected),
        "distinctHostCount": len(hosts),
        "datedSourceCount": dated,
        "sourceUrls": [item.get("url") for item in selected.values() if item.get("url")],
    }


def _build_refinement_shards(
    question: str,
    shards: list[dict[str, Any]],
    *,
    source_policy: str,
    limit: int = 3,
    round_index: int = 2,
    only_facet_ids: set[str] | None = None,
    force_primary_facet_ids: set[str] | None = None,
) -> list[dict[str, Any]]:
    seen = {_safe_text(shard.get("query")).lower() for shard in shards if _safe_text(shard.get("query"))}
    year = datetime.now(timezone.utc).year
    facet_queries = _build_question_facet_queries(question)
    if not facet_queries:
        facet_queries = _required_research_facet_queries(question, shards)
    requested_facet_ids = {
        _safe_text(facet_id) for facet_id in set(only_facet_ids or set()) if _safe_text(facet_id)
    }
    if requested_facet_ids:
        facet_queries = [
            item
            for item in facet_queries
            if _research_facet_id(item[1]) in requested_facet_ids
        ]
    forced_primary = {
        _safe_text(facet_id)
        for facet_id in set(force_primary_facet_ids or set())
        if _safe_text(facet_id)
    }
    facet_documents: dict[str, set[str]] = {
        _research_facet_id(kind): set()
        for _query, kind in facet_queries
        if _research_facet_id(kind)
    }
    facet_existing_queries: dict[str, list[str]] = {
        facet_id: [] for facet_id in facet_documents
    }
    for shard in shards:
        facet_id = _safe_text(shard.get("researchFacetId")) or _research_facet_id(
            shard.get("kind")
        )
        if facet_id not in facet_documents:
            continue
        shard_query = _safe_text(shard.get("query"))
        if shard_query and shard_query not in facet_existing_queries[facet_id]:
            facet_existing_queries[facet_id].append(shard_query)
        for candidate in _research_source_candidates(
            question,
            [shard],
            source_policy=source_policy,
        ):
            if candidate.get("gate", {}).get("selectedForEvidence"):
                facet_documents[facet_id].add(_safe_text(candidate.get("documentId")))

    ordered_facets = sorted(
        facet_queries,
        key=lambda item: (
            len(facet_documents.get(_research_facet_id(item[1]), set())),
            facet_queries.index(item),
        ),
    )
    candidates: list[tuple[str, str, str, str, str]] = []
    for goal, kind in ordered_facets:
        facet_id = _research_facet_id(kind)
        existing = facet_existing_queries.get(facet_id) or []
        # Prefer the Architect's concise primary query over the original long
        # brief when planning a repair.  This keeps refinement atomic even if
        # the source gap was detected after the full combined question.
        base_query = (
            min(existing, key=len)
            if existing
            else _deterministic_facet_search_query(goal)
        )
        selected_count = len(facet_documents.get(facet_id, set()))
        if facet_id in forced_primary:
            query = f"{base_query} official primary source {year}"
            source_intent = "official_primary"
            reason = "补齐精确 claim 计划确认缺失的一手证据"
        elif selected_count <= 0:
            query = f"{base_query} official primary source {year}"
            source_intent = "official_primary"
            reason = "补齐该事实域尚缺的官方或一手证据"
        else:
            query = f"{base_query} independent expert analysis evidence {year}"
            source_intent = "independent_secondary"
            reason = "补齐该事实域的独立交叉证据"
        candidates.append((query, kind, reason, facet_id, source_intent))
    if not facet_queries:
        candidates.extend(
            [
                (f"{question} official source primary evidence {year}", "gap_primary_source", "补读官方或一手来源", "", "official_primary"),
                (f"{question} independent expert analysis evidence", "gap_independent_source", "补充独立来源", "", "independent_secondary"),
                (f"{question} limitations risks disagreement counterexample", "gap_conflict_check", "补查限制、冲突和反例", "", "mixed"),
                (f"{question} latest update release date {year}", "gap_freshness_check", "补查可定年的最新变化", "", "official_primary"),
                (f"{question} study report dataset methodology", "gap_data_check", "补查研究、数据与方法", "", "mixed"),
                (f"{question} case study implementation outcomes", "gap_detail_check", "补充案例与落地细节", "", "independent_secondary"),
            ]
        )
    refined: list[dict[str, Any]] = []
    for query, kind, reason, facet_id, source_intent in candidates:
        query = _normalize_research_search_query(query)
        if not query:
            continue
        normalized = query.lower()
        if normalized in seen:
            continue
        refined.append(
            {
                "shardId": f"shard_refine_{round_index}_{len(refined)+1}",
                "kind": kind,
                "query": query,
                "evidenceQuery": query,
                **({"researchFacetId": facet_id} if facet_id else {}),
                "sourceIntent": source_intent,
                "verification": True,
                "reason": reason,
            }
        )
        if len(refined) >= max(1, limit):
            break
    return refined


_RESEARCH_RUNTIME_DIAGNOSTIC_GAP_PATTERNS = (
    re.compile(r"research should continue until at least \d+ readable sources", re.IGNORECASE),
    re.compile(r"no source-backed claims were extracted from readable page bodies", re.IGNORECASE),
    re.compile(r"(?:answer|evidence|source|host|claim|coverage)_floor_not_met", re.IGNORECASE),
    re.compile(
        r"(?:architect|independent)_review_not_accepted|research_(?:brief_coverage_incomplete|delivery_gate_not_ready|source_transport_exhausted)",
        re.IGNORECASE,
    ),
)


def _research_gap_is_runtime_diagnostic(value: Any) -> bool:
    text = re.sub(r"\s+", " ", _safe_text(value)).strip()
    return bool(text) and any(pattern.search(text) for pattern in _RESEARCH_RUNTIME_DIAGNOSTIC_GAP_PATTERNS)


def _research_critical_gap_queries(
    question: str,
    critical_gaps: list[Any],
    *,
    limit: int = 4,
) -> list[str]:
    """Turn reviewer gaps into bounded, entity-aware live search queries.

    Facet repair answers the question "does this dimension have a source?";
    reviewer gaps answer the more useful question "does every named product
    have the detail needed for the requested comparison?".  Keep this bridge
    deterministic and small: one query per distinct gap, with the entities
    explicitly named in that gap when possible, otherwise the entities named
    by the user.  This preserves ordinary search fallback while preventing a
    broad, already-covered facet from starving a concrete missing detail.
    """

    hints = _catalog_official_entity_hints(question)
    if not hints:
        return []
    labels = [
        _safe_text(item.get("label"))
        for item in hints
        if _safe_text(item.get("label"))
    ]
    if not labels:
        return []
    year = datetime.now(timezone.utc).year
    queries: list[str] = []
    seen: set[str] = set()
    for raw_gap in list(critical_gaps or []):
        gap = re.sub(r"\s+", " ", _safe_text(raw_gap)).strip()
        if len(gap) < 8:
            continue
        if _research_gap_is_runtime_diagnostic(gap):
            continue
        indexes = sorted(_research_entity_indexes_in_text(hints, gap))
        scoped_labels = [labels[index] for index in indexes if index < len(labels)]
        if not scoped_labels:
            scoped_labels = labels
        # Keep the search phrase focused.  The reviewer wording is retained
        # for traceability, but bounded before it can become a new mega-query.
        gap = _deterministic_facet_search_query(gap, max_chars=180)
        query = (
            f"{' '.join(scoped_labels)} {gap} official documentation "
            f"primary source {year}"
        )
        normalized = _normalize_research_search_query(query)
        if not normalized or normalized.casefold() in seen:
            continue
        seen.add(normalized.casefold())
        queries.append(normalized)
        if len(queries) >= max(1, int(limit)):
            break
    return queries


def _research_architect_plan_failure_gaps(
    question: str,
    model_synthesis: dict[str, Any],
) -> list[str]:
    """Describe evidence to collect after a strict Architect plan failure.

    A malformed/overclaiming plan is a model-quality signal, not proof that
    the web search was complete.  For the common full-synthesis failure,
    produce paired entity queries so the next live round can add missing
    operational and governance facts without weakening claim verification.
    """

    if _safe_text(model_synthesis.get("fallbackReason")) != "architect_evidence_plan_unavailable":
        return []
    hints = _catalog_official_entity_hints(question)
    labels = [
        _safe_text(item.get("label"))
        for item in hints
        if _safe_text(item.get("label"))
    ]
    if len(labels) >= 2:
        pairs = [
            labels[index : index + 2]
            for index in range(0, len(labels), 2)
        ]
        pair_text = [" and ".join(pair) for pair in pairs if pair]
        if len(pair_text) == 1:
            pair_text = [pair_text[0], pair_text[0]]
        gaps: list[str] = []
        for pair in pair_text[:2]:
            gaps.append(
                f"{pair} Windows installation runtime, model selection, and tool calling "
                "official documentation with exact commands and current boundaries"
            )
        for pair in pair_text[:2]:
            gaps.append(
                f"{pair} MCP extensions, repository workflow, account pricing, privacy, "
                "telemetry, and limitations official documentation with dated boundaries"
            )
        return gaps
    subject = _research_query_subject(question)
    return [
        f"{subject} Windows installation runtime model tool calling MCP extensions official documentation current",
        f"{subject} account pricing privacy telemetry limitations official documentation dated current",
    ]


def _merge_research_repair_queries(
    architect_queries: list[Any],
    deterministic_shards: list[dict[str, Any]],
    *,
    limit: int = 8,
) -> list[str]:
    """Preserve Architect gap queries alongside deterministic facet repair.

    Structured repair previously replaced the Architect's recommended queries,
    so official URLs/site queries identified during gap review were never run.
    """

    merged: list[str] = []
    seen: set[str] = set()
    for value in [
        *architect_queries,
        *[_safe_text(shard.get("query")) for shard in deterministic_shards],
    ]:
        query = _normalize_research_search_query(value)
        normalized = query.casefold()
        if not query or normalized in seen:
            continue
        seen.add(normalized)
        merged.append(query)
        if len(merged) >= max(1, int(limit)):
            break
    return merged


def _normalize_research_search_query(value: Any) -> str:
    query = re.sub(r"\s+", " ", _safe_text(value)).strip()
    if not query:
        return ""
    if _research_gap_is_runtime_diagnostic(query):
        return ""
    # A combined runtime brief is useful planning material, but it is not one
    # literal search-engine query.  When a model echoes the whole multi-facet
    # brief as a repair query, drop it so the existing per-facet deterministic
    # repair rows can take over.
    if len(re.findall(r"\[[a-z0-9][a-z0-9_-]{1,79}\]", query, re.IGNORECASE)) >= 2:
        return ""
    if re.search(
        r"\bresearch every item below\b|\bcover each item explicitly\b|"
        r"\bdeliverable requirements?\b",
        query,
        re.IGNORECASE,
    ):
        return ""
    query = re.sub(
        r"^(?:please\s+)?(?:fetch|retrieve|read|open|visit|look\s+up|search(?:\s+for)?|find|locate|obtain|consult)\s*[:：-]?\s*",
        "",
        query,
        flags=re.IGNORECASE,
    )
    query = re.sub(r"^(?:请)?(?:获取|抓取|读取|打开|访问|查找|搜索|检索|查询)\s*[:：-]?\s*", "", query)
    for stale_host, current_host in dict(
        _source_catalog().get("hostQueryRewrites") or {}
    ).items():
        query = re.sub(
            rf"(?<![a-z0-9.-])site:{re.escape(stale_host)}(?=\s|$)",
            f"site:{current_host}",
            query,
            flags=re.IGNORECASE,
        )
    return _deterministic_facet_search_query(query, max_chars=280).strip(" \t\r\n-:：")


def _build_question_facet_queries(question: str) -> list[tuple[str, str]]:
    structured_facets = _build_explicit_question_facets(question)
    if structured_facets:
        return structured_facets

    lowered = _safe_text(question).lower()
    subject = _research_query_subject(question)
    cli_request = bool(
        re.search(r"\bcli\b", lowered)
        or "command-line" in lowered
        or "command line" in lowered
        or "命令行" in lowered
    )
    pathlib_request = "pathlib" in lowered or "path object" in lowered or "文件路径" in lowered
    if cli_request and pathlib_request:
        return [
            (
                f"site:docs.python.org/3/library/argparse.html {subject} argparse type pathlib.Path",
                "facet_cli_parser",
            ),
            (
                f"site:click.palletsprojects.com/en/stable/parameter-types {subject} click.Path path_type resolve_path",
                "facet_cli_framework",
            ),
            (
                f"site:typer.tiangolo.com pathlib Path command line parameter type",
                "facet_cli_typer",
            ),
            (
                f"site:docs.python.org/3/library/pathlib.html {subject} Path filesystem semantics",
                "facet_pathlib_api",
            ),
        ]
    return []


def _build_explicit_question_facets(question: str) -> list[tuple[str, str]]:
    """Return only user/runtime-declared ``[facet-id]`` brief lines.

    ``_build_question_facet_queries`` also knows a few deterministic discovery
    expansions for ordinary questions.  Those are useful search hints, but they
    are not an explicit multi-brief contract and therefore must not trigger the
    Architect query planner, per-brief delivery gate, or structured repair path.
    """
    structured_facets: list[tuple[str, str]] = []
    seen_facet_queries: set[str] = set()
    for line in _safe_text(question).splitlines():
        match = re.match(
            r"^\s*(?:\d+[.)]|[-*])\s*\[([^\]\r\n]{1,80})\]\s*(.+?)\s*$",
            line,
        )
        if not match:
            continue
        facet_id = re.sub(r"[^a-z0-9_-]+", "-", match.group(1).strip().lower()).strip("-")
        facet_query = _safe_text(match.group(2))
        normalized = facet_query.casefold()
        if not facet_id or len(facet_query) < 8 or normalized in seen_facet_queries:
            continue
        seen_facet_queries.add(normalized)
        structured_facets.append((facet_query, f"facet:{facet_id[:64]}"))
    return structured_facets


def _research_should_decompose_root_question(question: str) -> bool:
    """Use the query Architect only when an ordinary brief has real substructure.

    Short, single-topic questions already have a deterministic search and repair
    path.  Sending every such question through a model planner adds latency and
    can replace a sound literal query with unrelated facets.  Longer briefs and
    medium-sized briefs that state several distinct asks still benefit from
    semantic decomposition.
    """

    text = re.sub(r"\s+", " ", _safe_text(question)).strip()
    if len(text) >= 96:
        return True
    if len(text) < 48:
        return False
    clause_markers = re.findall(
        r"[?？;；\n]|(?:分别|区分|核对|比较|对比|同时|以及|并给出|还要|另外)|"
        r"\b(?:compare|distinguish|separately|including|alongside|as well as)\b",
        _safe_text(question),
        re.IGNORECASE,
    )
    return len(clause_markers) >= 2


def _research_entity_query_focus(question: str) -> str:
    """Project only the comparison dimensions actually requested by the user."""

    lowered = _safe_text(question).lower()
    focus_terms: list[str] = []
    year_match = re.search(r"\b20\d{2}\b", lowered)
    if year_match or any(marker in lowered for marker in ("截至", "当前", "最新", "current", "latest")):
        focus_terms.append(year_match.group(0) if year_match else "current")
    focus_groups = (
        (("windows", "powershell", "wsl"), "Windows PowerShell WSL"),
        (("安装", "运行", "install", "setup", "runtime"), "installation runtime"),
        (("模型", "工具调用", "model", "tool use", "tool calling"), "models tools"),
        (("mcp", "扩展", "extension"), "MCP extensions"),
        (("代码库", "仓库", "工作流", "repository", "codebase", "workflow"), "repository workflow"),
        (("账号", "订阅", "价格", "account", "subscription", "pricing"), "account pricing"),
        (("隐私", "数据边界", "privacy", "data boundary"), "privacy data"),
        (("局限", "限制", "limitation", "constraint"), "limitations"),
    )
    for markers, terms in focus_groups:
        if any(marker in lowered for marker in markers):
            focus_terms.append(terms)
    return " ".join(focus_terms) or "current official capabilities limitations"


_RESEARCH_ENTITY_EVIDENCE_DIMENSIONS = (
    "setup",
    "models_tools",
    "extensions_workflow",
    "accounts_pricing",
    "privacy_limits",
)
_RESEARCH_OPERATIONAL_DIMENSIONS = frozenset(
    {"setup", "models_tools", "extensions_workflow"}
)
_RESEARCH_GOVERNANCE_DIMENSIONS = frozenset(
    {"accounts_pricing", "privacy_limits"}
)


def _research_query_dimension_classes(value: Any) -> set[str]:
    text = re.sub(r"[_-]+", " ", _safe_text(value).lower())
    classes: set[str] = set()
    if re.search(
        r"\b(?:windows|powershell|wsl|install(?:ation|er|s|ed|ing)?|setup|runtime|"
        r"prerequisites?|node\.js|npm|npx|winget|choco|scoop)\b|"
        r"(?:安装|运行|前置条件|系统要求)",
        text,
        re.IGNORECASE,
    ):
        classes.add("setup")
    if re.search(
        r"\b(?:models?|model selection|tool use|tool calling|function calling|"
        r"gpt(?:\s|-)?\d|sonnet|opus|haiku|gemini\s+\d)\b|"
        r"(?:模型|工具调用|函数调用)",
        text,
        re.IGNORECASE,
    ):
        classes.add("models_tools")
    if re.search(
        r"\b(?:mcp|extensions?|plugins?|skills?|hooks?|custom agents?|"
        r"repositor(?:y|ies)|codebases?|workflows?)\b|"
        r"(?:扩展|插件|技能|钩子|子代理|代码库|仓库|工作流)",
        text,
        re.IGNORECASE,
    ):
        classes.add("extensions_workflow")
    if re.search(
        r"\b(?:accounts?|subscriptions?|plans?|pricing|prices?|costs?|billing|"
        r"quotas?|licenses?|seats?|free tiers?|paid tiers?)\b|"
        r"(?:账号|账户|订阅|套餐|价格|定价|成本|计费|配额|许可|席位|免费层|付费层)",
        text,
        re.IGNORECASE,
    ):
        classes.add("accounts_pricing")
    if re.search(
        r"\b(?:privacy|telemetry|retention|data boundaries|data training|"
        r"model training|data collection|limitations?|constraints?|known issues?|security)\b|"
        r"\b(?:customer|user|personal) data\b.{0,80}\btrain(?:s|ed|ing)?\b|"
        r"(?:隐私|遥测|数据边界|数据训练|数据收集|保留期|局限|限制|安全边界)",
        text,
        re.IGNORECASE,
    ):
        classes.add("privacy_limits")
    if re.search(r"\b(?:operations?|capabilit(?:y|ies))\b|(?:运行能力|操作能力|能力边界)", text):
        classes.update(_RESEARCH_OPERATIONAL_DIMENSIONS)
    if re.search(r"\bgovernance\b|(?:治理边界)", text):
        classes.update(_RESEARCH_GOVERNANCE_DIMENSIONS)
    return classes


def _research_query_dimension_families(value: Any) -> set[str]:
    dimensions = _research_query_dimension_classes(value)
    families: set[str] = set()
    if dimensions.intersection(_RESEARCH_OPERATIONAL_DIMENSIONS):
        families.add("operations")
    if dimensions.intersection(_RESEARCH_GOVERNANCE_DIMENSIONS):
        families.add("governance")
    return families


def _apply_catalog_official_entity_seeds(
    question: str,
    planned_shards: list[dict[str, Any]],
) -> tuple[list[dict[str, Any]], dict[str, Any]]:
    """Replace matching entity-dimension queries with trusted catalog URL reads."""

    entity_hints = _catalog_official_entity_hints(question)
    seeds = list(_source_catalog().get("officialEntitySeeds") or [])
    if len(entity_hints) < 2 or not seeds or not planned_shards:
        return planned_shards, {"replacedCount": 0, "seedIds": []}

    replacements: dict[int, dict[str, Any]] = {}
    used_rows: set[int] = set()
    seed_ids: list[str] = []
    for seed in seeds:
        aliases = " ".join(
            _safe_text(alias)
            for alias in list(seed.get("aliases") or [])
            if _safe_text(alias)
        )
        entity_indexes = _research_entity_indexes_in_text(entity_hints, aliases)
        if len(entity_indexes) != 1:
            continue
        entity_index = next(iter(entity_indexes))
        dimension = _safe_text(seed.get("dimension")).lower() or "setup"
        seed_key = f"{_safe_text(seed.get('entityId'))}:{dimension}"
        candidates: list[tuple[tuple[int, int, int], int, dict[str, Any]]] = []
        for index, shard in enumerate(planned_shards):
            if index in used_rows or _safe_text(shard.get("kind")) == "seed_url":
                continue
            query_text = " ".join(
                (
                    _safe_text(shard.get("query")),
                    _safe_text(shard.get("evidenceQuery")),
                    _safe_text(shard.get("facetGoal")),
                )
            )
            if _research_entity_indexes_in_text(entity_hints, query_text) != {entity_index}:
                continue
            dimensions = _research_query_dimension_classes(query_text)
            candidates.append(
                (
                    (
                        1 if dimension in dimensions else 0,
                        1 if shard.get("verification") is False else 0,
                        1 if _safe_text(shard.get("sourceIntent")) == "official_primary" else 0,
                    ),
                    index,
                    shard,
                )
            )
        if not candidates:
            continue
        _score, row_index, original = max(candidates, key=lambda item: item[0])
        seed_url = _safe_text(seed.get("url"))
        if not seed_url:
            continue
        replacement = {
            **original,
            "shardId": (
                "shard_catalog_seed_"
                f"{_safe_text(seed.get('entityId'))}-{dimension}"
            ),
            "kind": "seed_url",
            "query": seed_url,
            # Keep the original facet contract on a direct-read shard.  Some
            # planner responses omit one of these optional fields; falling
            # back to the planned query prevents the URL itself from becoming
            # the evidence query and losing entity/dimension attribution.
            "researchFacetId": (
                _safe_text(original.get("researchFacetId"))
                or _research_facet_id(original.get("kind"))
                or None
            ),
            "facetGoal": (
                _safe_text(original.get("facetGoal"))
                or _safe_text(original.get("evidenceQuery"))
                or _safe_text(original.get("query"))
                or None
            ),
            "evidenceQuery": (
                _safe_text(original.get("evidenceQuery"))
                or _safe_text(original.get("facetGoal"))
                or _safe_text(original.get("query"))
                or seed_url
            ),
            "seedUrl": seed_url,
            "plannedQuery": _safe_text(original.get("query")),
            "reason": "runtime_catalog_official_entity_seed",
            "catalogSeedId": _safe_text(seed.get("entityId")),
            "catalogSeedDimension": dimension,
            "sourceIntent": "official_primary",
            "verification": True,
        }
        replacements[row_index] = replacement
        used_rows.add(row_index)
        seed_ids.append(seed_key)
    return [replacements.get(index, shard) for index, shard in enumerate(planned_shards)], {
        "replacedCount": len(replacements),
        "seedIds": seed_ids,
    }


def _research_entity_query_focus_parts(question: str) -> list[tuple[str, str]]:
    lowered = _safe_text(question).lower()
    year_match = re.search(r"\b20\d{2}\b", lowered)
    time_term = year_match.group(0) if year_match else (
        "current"
        if any(marker in lowered for marker in ("截至", "当前", "最新", "current", "latest"))
        else ""
    )
    parts: list[tuple[str, str]] = []
    dimension_groups = (
        (
            "setup",
            (
                ("windows", "powershell", "wsl", "安装", "运行", "install", "setup", "runtime"),
                "Windows PowerShell WSL installation runtime",
            ),
        ),
        (
            "models_tools",
            (("模型", "工具调用", "model", "tool use", "tool calling"), "models and tool calling"),
        ),
        (
            "extensions_workflow",
            (
                ("mcp", "扩展", "extension", "代码库", "仓库", "工作流", "repository", "codebase", "workflow"),
                "MCP extensions repository workflow",
            ),
        ),
        (
            "accounts_pricing",
            (
                ("账号", "账户", "订阅", "价格", "account", "subscription", "pricing"),
                "account subscription pricing",
            ),
        ),
        (
            "privacy_limits",
            (
                ("隐私", "数据边界", "局限", "限制", "privacy", "telemetry", "data boundary", "limitation", "constraint"),
                "privacy data boundaries limitations",
            ),
        ),
    )
    for dimension, (markers, terms) in dimension_groups:
        if any(marker in lowered for marker in markers):
            parts.append(
                (
                    dimension,
                    " ".join(value for value in (time_term, terms) if value),
                )
            )
    if not parts:
        parts.append(("focus", _research_entity_query_focus(question)))
    return parts


def _build_explicit_question_deliverables(question: str) -> list[dict[str, str]]:
    """Parse declared output goals without turning them into search facets."""

    deliverables: list[dict[str, str]] = []
    in_deliverables = False
    for line in _safe_text(question).splitlines():
        if re.match(
            r"^\s*(?:deliverable requirements?|output requirements?|交付要求|输出要求)\s*[:：]?\s*$",
            line,
            re.IGNORECASE,
        ):
            in_deliverables = True
            continue
        if not in_deliverables:
            continue
        bullet = re.match(r"^\s*[-*]\s+(.+?)\s*$", line)
        if not bullet:
            if line.strip() and deliverables:
                break
            continue
        raw_goal = _safe_text(bullet.group(1))
        identified = re.match(
            r"(?:\[([^\]\r\n]{1,80})\]|([a-z0-9][a-z0-9_-]{1,79}))\s*[:：]\s*(.+)$",
            raw_goal,
            re.IGNORECASE,
        )
        if identified:
            raw_id = _safe_text(identified.group(1) or identified.group(2))
            goal = _safe_text(identified.group(3))
        else:
            raw_id = f"deliverable-{len(deliverables) + 1}"
            goal = raw_goal
        deliverable_id = re.sub(r"[^a-z0-9_-]+", "-", raw_id.lower()).strip("-")
        if deliverable_id and len(goal) >= 8:
            deliverables.append(
                {
                    "deliverableId": deliverable_id[:80],
                    "goal": goal,
                }
            )
    return deliverables[:8]


def _research_named_decision_audiences(question: str) -> list[str]:
    """Extract only audiences the user explicitly asks to separate.

    This is a delivery hint, not a new evidence facet. Keep the parser narrow so
    ordinary fuzzy research questions remain model-judged instead of acquiring
    brittle deterministic labels.
    """

    text = re.sub(r"\s+", " ", _safe_text(question)).strip()
    if not text:
        return []
    audience_text = ""
    for pattern in (
        r"按(.{2,120}?)(?:划分|分类)(?:的)?(?:可执行)?(?:选型|选择|行动)?建议",
        r"按(.{2,120}?)(?:分别)?(?:给出|提供|形成)(?:可执行)?(?:选型|选择|行动)?建议",
        r"(?:recommendations?|guidance)\s+(?:organized|grouped|split)\s+by\s+(.{2,120}?)(?:[.?!]|$)",
    ):
        match = re.search(pattern, text, re.IGNORECASE)
        if match:
            audience_text = _safe_text(match.group(1))
            break
    if not audience_text:
        return []
    parts = [
        re.sub(r"^(?:按|面向|针对)\s*", "", value).strip(" \t,，、;；:.：")
        for value in re.split(
            r"\s*(?:、|，|,|；|;|/|\b(?:and|or)\b|和|及|与)\s*",
            audience_text,
            flags=re.IGNORECASE,
        )
    ]
    audiences = list(
        dict.fromkeys(
            value
            for value in parts
            if 1 < len(value) <= 48
        )
    )
    return audiences[:6] if 2 <= len(audiences) <= 6 else []


def _research_normalize_named_decision_inferences(
    inferences: Any,
    audiences: list[str],
) -> list[dict[str, Any]]:
    """Keep one non-overlapping model inference for each explicit audience."""

    rows = [
        item
        for item in _research_dict_list(inferences, limit=12)
        if isinstance(item, dict)
    ]
    if not audiences or not rows:
        return rows

    candidates_by_audience: list[list[dict[str, Any]]] = []
    for audience in audiences:
        normalized_audience = _safe_text(audience).casefold()
        candidates = [
            row
            for row in rows
            if normalized_audience
            in _safe_text(
                row.get("audience")
                or row.get("inference")
                or row.get("conclusion")
                or row.get("recommendation")
                or row.get("claim")
            ).casefold()
        ]
        if not candidates:
            # Do not reinterpret unlabeled model output as a named audience;
            # the normal evidence/review path should decide whether it is usable.
            return rows
        candidates_by_audience.append(candidates[:8])

    best: tuple[tuple[int, int, int], list[dict[str, Any]]] | None = None

    def visit(index: int, chosen: list[dict[str, Any]]) -> None:
        nonlocal best
        if index >= len(candidates_by_audience):
            premise_sets = [
                {
                    _safe_text(value)
                    for value in list(row.get("premiseClaimIds") or [])
                    if _safe_text(value)
                }
                for row in chosen
            ]
            overlap = sum(
                len(left.intersection(right))
                for position, left in enumerate(premise_sets)
                for right in premise_sets[position + 1 :]
            )
            premise_count = sum(len(values) for values in premise_sets)
            text_length = sum(
                len(
                    _safe_text(
                        row.get("inference")
                        or row.get("conclusion")
                        or row.get("recommendation")
                        or row.get("claim")
                    )
                )
                for row in chosen
            )
            score = (overlap, -premise_count, -text_length)
            if best is None or score < best[0]:
                best = (score, list(chosen))
            return
        for candidate in candidates_by_audience[index]:
            visit(index + 1, [*chosen, candidate])

    visit(0, [])
    return best[1] if best else rows


def _architect_runtime_named_decision_fallback_allowed(question: str) -> bool:
    """Keep deterministic audience grouping out of explicit product comparisons."""

    return len(_catalog_official_entity_hints(question)) < 2


def _architect_runtime_named_decision_inferences(
    claims: list[dict[str, Any]],
    audiences: list[str],
    *,
    question: str,
) -> list[dict[str, Any]]:
    """Build concrete audience choices from disjoint verified premises.

    This is used only after every Architect planning attempt failed protocol or
    entailment validation. Every branch names the product already present in a
    verified premise and repeats the premise as its selection condition. If the
    Runtime cannot bind facts to distinct named products, it returns no plan
    rather than emitting a generic recommendation-shaped placeholder.
    """

    if not audiences:
        return []

    entity_hints = _catalog_official_entity_hints(question)

    def citation_keys(claim: dict[str, Any]) -> set[str]:
        return {
            _safe_text(
                support.get("citationKey") or support.get("citation")
                if isinstance(support, dict)
                else support
            ).strip("[]")
            for support in list(claim.get("supportingSources") or [])
            if _safe_text(
                support.get("citationKey") or support.get("citation")
                if isinstance(support, dict)
                else support
            ).strip("[]")
        }

    if len(entity_hints) < 2:
        generic_claims = [
            claim
            for claim in claims
            if _safe_text(claim.get("claimId"))
            and citation_keys(claim)
            and all(
                isinstance(support, dict)
                and _architect_support_role(support) == "primary"
                for support in list(claim.get("supportingSources") or [])
            )
        ]
        required_claim_count = len(audiences) * 2
        if len(generic_claims) < required_claim_count:
            return []
        chinese_question = bool(re.search(r"[\u4e00-\u9fff]", question))
        rows: list[dict[str, Any]] = []
        for index, audience in enumerate(audiences, start=1):
            chosen = generic_claims[(index - 1) * 2 : index * 2]
            if citation_keys(chosen[0]).intersection(citation_keys(chosen[1])):
                return []
            facts = [
                _compact_research_text(claim.get("claim"), limit=90)
                for claim in chosen
            ]
            if chinese_question:
                inference = (
                    f"面向{audience}，本报告建议仅在“{facts[0]}”与“{facts[1]}”两项来源绑定事实均符合任务时"
                    "采用该方案；否则保留为未决分支。"
                )
            else:
                inference = (
                    f"For {audience}, select the option when both source-bound facts fit the task: "
                    f"\"{facts[0]}\" and \"{facts[1]}\"; otherwise leave the choice open."
                )
            rows.append(
                {
                    "inferenceId": f"inference_runtime_audience_{index}",
                    "inference": inference,
                    "premiseClaimIds": [
                        _safe_text(claim.get("claimId")) for claim in chosen
                    ],
                }
            )
        return rows

    def claim_entity_indexes(claim: dict[str, Any]) -> set[int]:
        surface = " ".join(
            [
                _safe_text(claim.get("claim")),
                *[
                    " ".join(
                        (
                            _safe_text(support.get("title")),
                            _safe_text(support.get("url") or support.get("sourceUrl")),
                        )
                    )
                    for support in list(claim.get("supportingSources") or [])
                    if isinstance(support, dict)
                ],
            ]
        )
        return _research_entity_indexes_in_text(entity_hints, surface)

    def claim_source_role(claim: dict[str, Any]) -> str:
        roles = {
            _architect_support_role(support)
            for support in list(claim.get("supportingSources") or [])
            if isinstance(support, dict)
        }
        if roles == {"primary"}:
            return "primary"
        if roles == {"secondary"} and _ARCHITECT_SECONDARY_CONTEXT_RE.search(
            _safe_text(claim.get("claim"))
        ):
            return "secondary"
        return "unknown"

    eligible = [
        claim
        for claim in claims
        if _safe_text(claim.get("claimId"))
        and citation_keys(claim)
        and len(claim_entity_indexes(claim)) == 1
        and claim_source_role(claim) in {"primary", "secondary"}
    ]
    if len(eligible) < len(audiences):
        return []

    chinese = bool(re.search(r"[\u4e00-\u9fff]", question))
    same_language_claims = [
        claim
        for claim in eligible
        if bool(re.search(r"[\u4e00-\u9fff]", _safe_text(claim.get("claim")))) == chinese
    ]
    if len(same_language_claims) >= len(audiences):
        eligible = same_language_claims

    entity_index_by_claim_id = {
        _safe_text(claim.get("claimId")): next(iter(claim_entity_indexes(claim)))
        for claim in eligible
    }

    def audience_keywords(audience: str) -> tuple[str, ...]:
        normalized = _safe_text(audience).casefold()
        if re.search(r"(?:个人|独立|solo|individual|personal)", normalized):
            return (
                "install",
                "setup",
                "terminal",
                "local",
                "free",
                "price",
                "pricing",
                "account",
                "sandbox",
                "安装",
                "终端",
                "本地",
                "免费",
                "价格",
                "账号",
            )
        if re.search(r"(?:小团队|团队|team|group|organization)", normalized):
            return (
                "team",
                "organization",
                "repository",
                "workflow",
                "review",
                "merge",
                "agent",
                "permission",
                "security",
                "sandbox",
                "restricted",
                "signed",
                "signature",
                "governance",
                "仓库",
                "协作",
                "审查",
                "权限",
                "安全",
                "沙箱",
                "受限",
                "签名",
                "治理",
            )
        if re.search(r"(?:订阅|subscriber|subscription|platform)", normalized):
            return (
                "subscriber",
                "subscription",
                "plan",
                "account",
                "pricing",
                "price",
                "seat",
                "license",
                "billing",
                "free",
                "pro",
                "business",
                "enterprise",
                "订阅",
                "套餐",
                "账号",
                "平台",
                "价格",
                "席位",
                "许可",
                "计费",
                "免费",
                "企业",
            )
        return tuple(
            term
            for term in re.findall(r"[a-z0-9_]+|[\u4e00-\u9fff]", normalized)
            if term not in _RELEVANCE_STOPWORDS
        )

    def premise_rank(claim: dict[str, Any], audience: str) -> tuple[int, int, int, int]:
        searchable = " ".join(
            [
                _safe_text(claim.get("claim")),
                *[
                    _safe_text(support.get("title"))
                    for support in list(claim.get("supportingSources") or [])
                    if isinstance(support, dict)
                ],
            ]
        ).casefold()

        def contains_keyword(keyword: str) -> bool:
            if re.fullmatch(r"[a-z0-9_]+", keyword):
                return bool(
                    re.search(
                        rf"(?<![a-z0-9]){re.escape(keyword)}(?![a-z0-9])",
                        searchable,
                    )
                )
            return keyword in searchable

        keyword_hits = sum(
            1 for keyword in audience_keywords(audience) if contains_keyword(keyword)
        )
        normative = int(
            _safe_text(claim.get("claimType")).lower() == "explicit_normative"
        )
        primary = int(claim_source_role(claim) == "primary")
        claim_length = len(_safe_text(claim.get("claim")))
        clarity = max(0, 120 - abs(min(claim_length, 180) - 90))
        return keyword_hits, primary, normative, clarity

    candidates_by_audience: list[list[dict[str, Any]]] = []
    for audience in audiences:
        candidates: list[dict[str, Any]] = []
        for claim in eligible:
            claim_id = _safe_text(claim.get("claimId"))
            rank = premise_rank(claim, audience)
            if rank[0] <= 0:
                continue
            candidates.append(
                {
                    "claims": [claim],
                    "claimIds": frozenset((claim_id,)),
                    "score": (1, *rank),
                }
            )
        for left_index, left in enumerate(eligible):
            left_id = _safe_text(left.get("claimId"))
            for right in eligible[left_index + 1 :]:
                right_id = _safe_text(right.get("claimId"))
                if (
                    entity_index_by_claim_id[left_id]
                    == entity_index_by_claim_id[right_id]
                    or citation_keys(left).intersection(citation_keys(right))
                ):
                    continue
                left_rank = premise_rank(left, audience)
                right_rank = premise_rank(right, audience)
                if left_rank[0] <= 0 or right_rank[0] <= 0:
                    continue
                candidates.append(
                    {
                        "claims": [left, right],
                        "claimIds": frozenset((left_id, right_id)),
                        "score": (
                            2,
                            *tuple(
                                left_rank[position] + right_rank[position]
                                for position in range(len(left_rank))
                            ),
                        ),
                    }
                )
        candidates.sort(key=lambda item: item["score"], reverse=True)
        if not candidates:
            return []
        candidates_by_audience.append(candidates)

    memo: dict[
        tuple[int, frozenset[str]],
        tuple[tuple[int, int, int, int, int], list[dict[str, Any]]] | None,
    ] = {}

    def choose_pairs(
        audience_index: int,
        used_claim_ids: frozenset[str],
    ) -> tuple[tuple[int, int, int, int, int], list[dict[str, Any]]] | None:
        key = (audience_index, used_claim_ids)
        if key in memo:
            return memo[key]
        if audience_index >= len(audiences):
            return (0, 0, 0, 0, 0), []
        best: tuple[
            tuple[int, int, int, int, int],
            list[dict[str, Any]],
        ] | None = None
        for candidate in candidates_by_audience[audience_index]:
            if candidate["claimIds"].intersection(used_claim_ids):
                continue
            suffix = choose_pairs(
                audience_index + 1,
                used_claim_ids.union(candidate["claimIds"]),
            )
            if suffix is None:
                continue
            total_score = tuple(
                candidate["score"][position] + suffix[0][position]
                for position in range(len(candidate["score"]))
            )
            choice = (total_score, [candidate, *suffix[1]])
            if best is None or choice[0] > best[0]:
                best = choice
        memo[key] = best
        return best

    selected = choose_pairs(0, frozenset())
    if selected is None:
        return []

    rows: list[dict[str, Any]] = []
    for index, (audience, candidate) in enumerate(
        zip(audiences, selected[1], strict=True),
        start=1,
    ):
        chosen = list(candidate["claims"])
        premise_ids = [_safe_text(claim.get("claimId")) for claim in chosen]
        branch_fact_limit = 90 if len(chosen) > 1 else 180
        branches = [
            (
                _safe_text(entity_hints[entity_index_by_claim_id[claim_id]].get("label")),
                _compact_research_text(
                    claim.get("sourceClaim") or claim.get("claim"),
                    limit=branch_fact_limit,
                ),
            )
            for claim_id, claim in zip(premise_ids, chosen, strict=True)
        ]
        has_secondary = any(claim_source_role(claim) == "secondary" for claim in chosen)
        if chinese:
            prefix = "含二手来源的综合判断：" if has_secondary else ""
            if len(branches) > 1:
                inference = (
                    f"{prefix}面向{audience}，本报告给出条件性选型：若“{branches[0][1]}”符合你的实际约束，"
                    f"优先试用{branches[0][0]}；若“{branches[1][1]}”更符合，优先试用{branches[1][0]}。"
                    "两项均不匹配时，不据此强选。"
                )
            else:
                inference = (
                    f"{prefix}面向{audience}，若“{branches[0][1]}”符合你的实际约束，本报告建议优先试用"
                    f"{branches[0][0]}；若不符合，则把其他产品保留为待比较分支。"
                )
        else:
            prefix = "Mixed-source synthesis with secondary context: " if has_secondary else ""
            if len(branches) > 1:
                inference = (
                    f"{prefix}For {audience}, this report gives a conditional choice: prefer {branches[0][0]} when "
                    f"\"{branches[0][1]}\" matches the actual constraints; prefer {branches[1][0]} when "
                    f"\"{branches[1][1]}\" is the closer match. If neither condition holds, do not force this choice."
                )
            else:
                inference = (
                    f"{prefix}For {audience}, prefer {branches[0][0]} when \"{branches[0][1]}\" matches "
                    "the actual constraints; otherwise leave the other products as open comparison branches."
                )
        rows.append(
            {
                "inferenceId": f"inference_runtime_audience_{index}",
                "inference": inference,
                "premiseClaimIds": premise_ids,
            }
        )
    return rows


def _research_facet_id(shard_kind: Any) -> str:
    kind = _safe_text(shard_kind)
    if kind.startswith("facet:"):
        return kind.split(":", 1)[1]
    return ""


def _research_facet_requires_atomic_claim(facet_id: Any) -> bool:
    """Separate answer topics from evidence-acquisition strategy labels."""

    normalized = re.sub(r"[_\s]+", "-", _safe_text(facet_id).lower()).strip("-")
    if not normalized:
        return False
    return not bool(
        re.search(
            r"(?:^|-)(?:cross-check|crosscheck|corroboration|source-comparison|"
            r"source-authority|legal-analysis-comparison|"
            r"independent-analysis-comparison|legal-analysis)(?:$|-)",
            normalized,
        )
    )


def _research_authority_query_budget(facet_count: int) -> int:
    if facet_count < 2:
        return 0
    return max(2, min(5, (int(facet_count) + 2) // 3))


def _research_brief_coverage(
    *,
    question: str,
    source_matrix: list[dict[str, Any]],
    claim_table: list[dict[str, Any]],
    answer: str,
) -> dict[str, Any]:
    facets = _build_explicit_question_facets(question)
    if not facets:
        return {
            "required": False,
            "complete": True,
            "coveredFacetIds": [],
            "missingFacetIds": [],
            "items": [],
        }

    selected_sources = [
        source
        for source in source_matrix
        if isinstance(source, dict)
        and bool(
            source.get("selectedForEvidence")
            or (source.get("sourceQualityGate") or {}).get("selectedForEvidence")
        )
    ]
    answer_citations = {
        _safe_text(value).strip("[]")
        for value in re.findall(r"\[([^\]\r\n]{1,80})\]", _safe_text(answer))
        if _safe_text(value)
    }

    def source_aliases(source: dict[str, Any]) -> set[str]:
        aliases = {
            _safe_text(source.get("sourceId")),
            _safe_text(source.get("citationKey")),
            _safe_text(source.get("citationKey")).strip("[]"),
            _safe_text(source.get("url")),
        }
        return {alias for alias in aliases if alias}

    source_rows_by_facet: dict[str, list[dict[str, Any]]] = {}
    for source in selected_sources:
        facet_ids = list(
            dict.fromkeys(
                [
                    *list(source.get("researchFacetIds") or []),
                    source.get("researchFacetId"),
                ]
            )
        )
        for raw_facet_id in facet_ids:
            facet_id = _safe_text(raw_facet_id)
            if facet_id and source not in source_rows_by_facet.setdefault(facet_id, []):
                source_rows_by_facet[facet_id].append(source)

    coverage_items: list[dict[str, Any]] = []
    for goal, kind in facets:
        facet_id = _research_facet_id(kind)
        facet_sources = source_rows_by_facet.get(facet_id, [])
        facet_aliases = set().union(*(source_aliases(source) for source in facet_sources)) if facet_sources else set()
        cited_source_keys = sorted(
            {
                _safe_text(source.get("citationKey")).strip("[]")
                for source in facet_sources
                if _safe_text(source.get("citationKey")).strip("[]") in answer_citations
            }
        )
        supported_claim_ids: list[str] = []
        for index, claim in enumerate(claim_table, start=1):
            if not isinstance(claim, dict):
                continue
            supporting_aliases: set[str] = set()
            for support in list(claim.get("supportingSources") or []):
                if isinstance(support, str):
                    supporting_aliases.update({_safe_text(support), _safe_text(support).strip("[]")})
                elif isinstance(support, dict):
                    support_facet_ids = {
                        _safe_text(value)
                        for value in [
                            *list(support.get("researchFacetIds") or []),
                            support.get("researchFacetId"),
                            support.get("taskBriefId"),
                        ]
                        if _safe_text(value)
                    }
                    if support_facet_ids and facet_id not in support_facet_ids:
                        continue
                    supporting_aliases.update(source_aliases(support))
            if facet_aliases.intersection(supporting_aliases):
                claim_id = _safe_text(claim.get("claimId")) or f"claim-{index}"
                if claim_id not in supported_claim_ids:
                    supported_claim_ids.append(claim_id)
        if supported_claim_ids and cited_source_keys:
            status = "supported"
            explicit_gap = ""
        elif facet_sources:
            status = "evidence_only"
            explicit_gap = "Readable evidence exists, but no verified claim cited it in the accepted answer."
        else:
            status = "missing"
            explicit_gap = "No selected readable source was retained for this research facet."
        coverage_items.append(
            {
                "taskBriefId": facet_id,
                "goal": _safe_text(goal),
                "status": status,
                "supportedClaimIds": supported_claim_ids,
                "sourceIds": [
                    _safe_text(source.get("sourceId"))
                    for source in facet_sources
                    if _safe_text(source.get("sourceId"))
                ],
                "citationKeys": cited_source_keys,
                "explicitGap": explicit_gap or None,
            }
        )
    covered = [item["taskBriefId"] for item in coverage_items if item["status"] == "supported"]
    missing = [item["taskBriefId"] for item in coverage_items if item["status"] != "supported"]
    return {
        "required": True,
        "complete": not missing,
        "coveredFacetIds": covered,
        "missingFacetIds": missing,
        "items": coverage_items,
    }


def _invoke_web_research_architect_query_plan(
    *,
    question: str,
    facets: list[tuple[str, str]],
    source_policy: str,
    freshness: str,
    max_shards: int,
) -> tuple[list[dict[str, Any]], dict[str, Any]]:
    """Ask the bound Research Architect to design atomic search queries.

    The Runtime, not the model, still owns and executes all search/read tools.
    The model only converts a broad structured question into short queries and
    source intents.  A validated deterministic fallback remains available when
    a provider cannot return the strict plan schema.
    """

    normalized_facets: list[dict[str, str]] = []
    for goal, kind in facets:
        facet_id = _research_facet_id(kind)
        if not facet_id:
            continue
        normalized_facets.append({"facetId": facet_id, "goal": _safe_text(goal)})
    decompose_root_question = bool(
        len(normalized_facets) == 1
        and normalized_facets[0]["facetId"] == "root-question"
    )
    if (len(normalized_facets) < 2 and not decompose_root_question) or max_shards <= 0:
        return [], {"used": False, "reason": "structured_multi_facet_plan_not_required"}

    official_host_hints = _catalog_official_host_hints(question)
    official_entity_hints = _catalog_official_entity_hints(question)
    entity_focus_parts = _research_entity_query_focus_parts(question)
    entity_matrix_target = len(official_entity_hints) * len(entity_focus_parts)
    entity_matrix_budgeted = bool(
        decompose_root_question
        and len(official_entity_hints) >= 2
        and len(entity_focus_parts) >= 2
        and int(max_shards) >= entity_matrix_target
    )
    root_primary_target = (
        entity_matrix_target
        if entity_matrix_budgeted
        else min(8, int(max_shards))
        if len(official_entity_hints) >= 2 and len(entity_focus_parts) >= 2
        else min(6, int(max_shards))
    )
    authority_budget = (
        min(2, max(0, int(max_shards) - root_primary_target))
        if decompose_root_question
        else min(
            max(0, int(max_shards) - len(normalized_facets)),
            _research_authority_query_budget(len(normalized_facets)),
        )
    )
    stage_prompt = _research_agent_stage_system_prompt(
        "你是 Research Runtime 的查询架构阶段。你只设计查询，不执行搜索、不写答案。"
        "把每个结构化 facet 转成一个短而原子的主查询，保留法条、版本、日期、组织、产品或标准等判别词。"
        "任何缩写都必须同时带上当前语境中的完整名称；遇到同名缩写或实体歧义时，加入足以排除常见同名概念的限定词，"
        "不得把未展开的缩写单独当作主题锚点。"
        "查询语言应优先匹配最可能的一手来源语言；跨法域、国际标准和主流技术文档通常优先英文，必要时可保留原文关键词。"
        "规范、法律、处罚、适用日期、官方指南和产品当前行为优先 official_primary；行业经验、案例和争议允许 independent_secondary；"
        "不要把完整多 facet 问题复制进任何一个查询，不要编造具体页面 URL。"
        "Runtime 会提供与问题实体匹配的 officialHostHints；它们是可选的权威来源候选而非网络 allowlist，"
        "当某个 official_primary facet 明确对应其中的厂商或产品时，优先用 site:host 生成原子查询。"
        "如果 explicitEntities 含多个用户明确点名的比较对象，每个对象必须至少出现在一个 verification=false 主查询中；"
        "不得只围绕第一个对象按维度拆分而遗漏其余对象。"
        "Runtime 会提供 entityFocusParts。查询额度足够时，每个 explicit entity 必须为其中每个维度分别生成一条主查询；"
        "每条都只聚焦一个实体和一个维度，不能用罗列多个产品或多个维度的宽泛比较查询代替原子证据查询。"
        "每个主查询必须对应且仅对应一个 facetId。补充核验查询只能覆盖最高风险或一手证据最关键的 facet。"
        + (
            "当输入只有 root-question 时，先根据用户真实意图拆成互不重复的原子事实域，自行生成简短稳定的 ASCII facetId；"
            "不要把 root-question 当成输出 facetId，也不要只给同一整句问题换后缀。"
            if decompose_root_question
            else "只能使用输入已给的 facetId，不得改名或新增 facet。"
        ),
        stage="query_plan",
    )
    stage_contract_digest = research_runtime_prompt_digest(stage_prompt)
    instruction = (
        "只输出严格 JSON 对象，字段 shards。shards 每项字段：facetId, query, sourceIntent, verification。"
        "sourceIntent 只能是 official_primary、independent_secondary、mixed；verification 必须是布尔值。"
        + (
            (
                f"把 root-question 拆成恰好 {root_primary_target} 个 verification=false 的原子主查询，"
                if entity_matrix_budgeted
                else f"把 root-question 拆成 {min(4, root_primary_target)}-{root_primary_target} 个 verification=false 的原子主查询，"
            )
            + f"每个主查询使用不同 facetId；然后最多输出 {authority_budget} 个 verification=true 的补充核验查询，"
            + "其 facetId 必须复用某个主查询。"
            if decompose_root_question
            else f"先为每个 facetId 恰好输出一个 verification=false 的主查询；然后最多输出 {authority_budget} 个 verification=true 的补充核验查询。"
        )
        + f"总数不得超过 {max_shards}。每条 query 必须单行、8-280 字符、可直接交给搜索引擎；禁止解释文字。"
        f"\nSOURCE_POLICY: {_safe_text(source_policy) or 'authoritative'}"
        f"\nFRESHNESS: {_safe_text(freshness) or 'auto'}"
    )
    material = json.dumps(
        {
            "facets": normalized_facets,
            "officialHostHints": official_host_hints,
            "explicitEntities": official_entity_hints,
            "entityFocusParts": [
                {"dimension": dimension, "focus": focus}
                for dimension, focus in entity_focus_parts
            ],
        },
        ensure_ascii=False,
    )

    def runtime_entity_fallback(
        reason: str,
        *,
        errors: list[str],
        raw_preview: str = "",
    ) -> tuple[list[dict[str, Any]], dict[str, Any]]:
        if not decompose_root_question or len(official_entity_hints) < 2:
            return [], {
                "used": False,
                "reason": reason,
                "errors": errors[-8:],
                **({"rawPreview": raw_preview[:800]} if raw_preview else {}),
                "researchPromptContractVersion": RESEARCH_PROMPT_CONTRACT_VERSION,
                "researchPromptContractDigest": stage_contract_digest,
            }
        split_entity_focus = bool(
            len(entity_focus_parts) >= 2
            and int(max_shards)
            >= len(official_entity_hints) * len(entity_focus_parts)
        )
        compact_focus_parts: list[tuple[str, str]] = []
        for family, dimension_ids in (
            ("operations", _RESEARCH_OPERATIONAL_DIMENSIONS),
            ("governance", _RESEARCH_GOVERNANCE_DIMENSIONS),
        ):
            values = [
                focus
                for dimension, focus in entity_focus_parts
                if dimension in dimension_ids
            ]
            if values:
                compact_focus_parts.append((family, " ".join(values)))
        compact_entity_focus = bool(
            len(compact_focus_parts) >= 2
            and int(max_shards)
            >= len(official_entity_hints) * len(compact_focus_parts)
        )
        fallback_focus_parts = (
            entity_focus_parts
            if split_entity_focus
            else compact_focus_parts
            if compact_entity_focus
            else [("focus", _research_entity_query_focus(question))]
        )
        primary_rows: list[dict[str, Any]] = []
        primary_rows_with_hints: list[tuple[dict[str, Any], dict[str, Any]]] = []
        used_facet_ids: set[str] = set()
        seen_fallback_queries: set[str] = set()
        for hint in official_entity_hints:
            label = _safe_text(hint.get("label"))
            if not label:
                continue
            for focus_id, focus in fallback_focus_parts:
                if len(primary_rows) >= int(max_shards):
                    break
                facet_slug = re.sub(r"[^a-z0-9_-]+", "-", label.lower()).strip("-")
                facet_id = f"entity-{facet_slug or _query_slug(label)}-{focus_id}"[:64]
                if facet_id in used_facet_ids:
                    facet_id = f"{facet_id[:54]}-{_query_slug(focus)}"
                query = _normalize_research_search_query(
                    f'"{label}" {focus} official documentation'
                )[:280].strip()
                if len(query) < 8 or query.casefold() in seen_fallback_queries:
                    continue
                row = {
                    "shardId": f"shard_plan_entity_fallback_{len(primary_rows) + 1}_{_query_slug(query)}",
                    "kind": f"facet:{facet_id}",
                    "researchFacetId": facet_id,
                    "query": query,
                    "evidenceQuery": query,
                    "facetGoal": f"{label}: {focus}",
                    "sourceIntent": "official_primary",
                    "verification": False,
                    "reason": "runtime_explicit_entity_fallback",
                    "plannedBy": "research-runtime",
                }
                primary_rows.append(row)
                primary_rows_with_hints.append((row, hint))
                used_facet_ids.add(facet_id)
                seen_fallback_queries.add(query.casefold())

        verification_rows: list[dict[str, Any]] = []
        for primary_row, hint in primary_rows_with_hints:
            if len(primary_rows) + len(verification_rows) >= int(max_shards):
                break
            host = _safe_text(hint.get("host"))
            if not host:
                continue
            query = _normalize_research_search_query(
                f"site:{host} {_safe_text(primary_row.get('query'))}"
            )[:280].strip()
            if len(query) < 8 or query.casefold() in seen_fallback_queries:
                continue
            verification_rows.append(
                {
                    **primary_row,
                    "shardId": f"shard_plan_entity_fallback_verify_{len(verification_rows) + 1}_{_query_slug(query)}",
                    "query": query,
                    "evidenceQuery": query,
                    "verification": True,
                    "reason": "runtime_official_entity_fallback_verification",
                }
            )
            seen_fallback_queries.add(query.casefold())
        rows = [*primary_rows, *verification_rows]
        return rows, {
            "used": False,
            "fallbackUsed": True,
            "reason": reason,
            "errors": errors[-8:],
            **({"rawPreview": raw_preview[:800]} if raw_preview else {}),
            "decomposedRootQuestion": True,
            "plannedShardCount": len(rows),
            "facetCount": len(primary_rows),
            "explicitEntityCount": len(official_entity_hints),
            "runtimeEntityCoverageCount": len(
                {
                    _safe_text(hint.get("label"))
                    for _row, hint in primary_rows_with_hints
                    if _safe_text(hint.get("label"))
                }
            ),
            "runtimeAuthorityVerificationCount": len(verification_rows),
            "researchPromptContractVersion": RESEARCH_PROMPT_CONTRACT_VERSION,
            "researchPromptContractDigest": stage_contract_digest,
        }

    candidate_errors: list[str] = []
    raw_previews: list[str] = []
    try:
        candidates = _create_web_research_architect_llm_candidates()
    except Exception as exc:  # noqa: BLE001 - deterministic queries remain available.
        return runtime_entity_fallback(
            "query_planner_model_unavailable",
            errors=[f"{type(exc).__name__}: {_safe_text(exc)[:220]}"],
        )

    known_facets = {item["facetId"]: item for item in normalized_facets}
    for candidate in candidates:
        model_label = _architect_candidate_identity(candidate) or candidate[1] or f"role:{candidate[2]}"
        prepared = prepare_background_model_messages(
            system_prompt=stage_prompt,
            instruction=instruction,
            materials=[
                {
                    "title": "Structured research facets",
                    "kind": "research_query_facets",
                    "content": material,
                }
            ],
            runtime_kind="research",
            target_role="web-research-architect",
            resolved_model_id=_architect_candidate_context_model_ref(candidate),
            component="research",
            node="web_research_architect_query_plan",
        )
        try:
            response = _invoke_architect_candidate_with_deadline(
                candidate,
                prepared.messages,
                seconds=_RESEARCH_ARCHITECT_QUERY_PLAN_TIMEOUT_SECONDS,
                max_tokens=_RESEARCH_ARCHITECT_QUERY_PLAN_MAX_TOKENS,
                disable_thinking=True,
            )
        except concurrent.futures.TimeoutError:
            candidate_errors.append(f"{model_label}: query_plan_timeout")
            continue
        except Exception as exc:  # noqa: BLE001 - try configured fallback candidates.
            candidate_errors.append(
                f"{model_label}: query_plan_{type(exc).__name__}: {_safe_text(exc)[:220]}"
            )
            continue
        sanitized_output = sanitize_background_model_output(response)
        raw_content = sanitized_output.text
        if raw_content:
            raw_previews.append(f"{model_label}: {raw_content[:500]}")
        parsed = _extract_json_object(raw_content)
        raw_shards = list(parsed.get("shards") or []) if isinstance(parsed, dict) else []
        if not raw_shards:
            diagnostic = sanitized_output.no_visible_text_reason or "schema_or_shards_missing"
            candidate_errors.append(
                f"{model_label}: query_plan_no_shards:{diagnostic}:"
                f"visible_chars={sanitized_output.visible_chars}:"
                f"reasoning_chars={sanitized_output.reasoning_chars}"
            )
            continue

        primary_by_facet: dict[str, dict[str, Any]] = {}
        verification_rows: list[dict[str, Any]] = []
        seen_queries: set[str] = set()
        for raw_item in raw_shards:
            if not isinstance(raw_item, dict):
                continue
            facet_id = re.sub(
                r"[^a-z0-9_-]+",
                "-",
                _safe_text(raw_item.get("facetId")).lower(),
            ).strip("-")
            if (
                not facet_id
                or (decompose_root_question and facet_id == "root-question")
                or (not decompose_root_question and facet_id not in known_facets)
            ):
                continue
            query = _normalize_research_search_query(raw_item.get("query"))
            if len(query) < 8 or len(query) > 280 or query.casefold() in seen_queries:
                continue
            source_intent = _safe_text(raw_item.get("sourceIntent")).lower()
            if source_intent not in {"official_primary", "independent_secondary", "mixed"}:
                source_intent = "mixed"
            verification = raw_item.get("verification") is True
            row = {
                "shardId": f"shard_plan_{len(primary_by_facet) + len(verification_rows) + 1}_{_query_slug(query)}",
                "kind": f"facet:{facet_id}",
                "researchFacetId": facet_id,
                "query": query,
                "evidenceQuery": query,
                "facetGoal": (
                    query
                    if decompose_root_question
                    else known_facets[facet_id]["goal"]
                ),
                "sourceIntent": source_intent,
                "verification": verification,
                "reason": "web_research_architect_query_plan",
                "plannedBy": "web-research-architect",
                "queryPlanModelId": model_label,
            }
            seen_queries.add(query.casefold())
            if verification:
                verification_rows.append(row)
            elif facet_id not in primary_by_facet:
                primary_by_facet[facet_id] = row

        if decompose_root_question:
            required_primary_count = min(4, int(max_shards))
            if len(primary_by_facet) < required_primary_count:
                candidate_errors.append(
                    f"{model_label}: query_plan_decomposition_too_small:"
                    f"{len(primary_by_facet)}/{required_primary_count}"
                )
                continue
            ordered_primary = list(primary_by_facet.values())
            verification_rows = [
                row
                for row in verification_rows
                if _safe_text(row.get("researchFacetId")) in primary_by_facet
            ]
        else:
            if len(primary_by_facet) != len(known_facets):
                candidate_errors.append(
                    f"{model_label}: query_plan_facet_coverage:{len(primary_by_facet)}/{len(known_facets)}"
                )
                continue
            ordered_primary = [primary_by_facet[item["facetId"]] for item in normalized_facets]

        architect_primary_count = len(ordered_primary)
        runtime_entity_rows: list[dict[str, Any]] = []
        if decompose_root_question and len(official_entity_hints) >= 2:
            def represented_entity_indexes(rows: list[dict[str, Any]]) -> set[int]:
                represented: set[int] = set()
                for row in rows:
                    row_text = " ".join(
                        (
                            _safe_text(row.get("query")),
                            _safe_text(row.get("facetGoal")),
                        )
                    ).lower()
                    for entity_index, hint in enumerate(official_entity_hints):
                        if any(
                            _safe_text(alias).lower() in row_text
                            for alias in list(hint.get("matchedAliases") or [])
                        ):
                            represented.add(entity_index)
                return represented

            required_primary_count = min(4, int(max_shards))
            while len(ordered_primary) > required_primary_count:
                represented = represented_entity_indexes(ordered_primary)
                missing_count = len(official_entity_hints) - len(represented)
                if len(ordered_primary) + missing_count <= int(max_shards):
                    break
                coverage_counts: dict[int, int] = {}
                row_entity_indexes: list[set[int]] = []
                for row in ordered_primary:
                    indexes = represented_entity_indexes([row])
                    row_entity_indexes.append(indexes)
                    for entity_index in indexes:
                        coverage_counts[entity_index] = coverage_counts.get(entity_index, 0) + 1
                removable_index = next(
                    (
                        index
                        for index in range(len(ordered_primary) - 1, -1, -1)
                        if not row_entity_indexes[index]
                        or all(
                            coverage_counts.get(entity_index, 0) > 1
                            for entity_index in row_entity_indexes[index]
                        )
                    ),
                    None,
                )
                if removable_index is None:
                    break
                ordered_primary.pop(removable_index)

            represented = represented_entity_indexes(ordered_primary)
            query_focus = _research_entity_query_focus(question)
            used_facet_ids = {
                _safe_text(row.get("researchFacetId")) for row in ordered_primary
            }
            for entity_index, hint in enumerate(official_entity_hints):
                if entity_index in represented or len(ordered_primary) + len(runtime_entity_rows) >= int(max_shards):
                    continue
                label = _safe_text(hint.get("label"))
                host = _safe_text(hint.get("host"))
                if not label or not host:
                    continue
                facet_slug = re.sub(r"[^a-z0-9_-]+", "-", label.lower()).strip("-")
                facet_id = f"entity-{facet_slug or _query_slug(label)}"[:64]
                if facet_id in used_facet_ids:
                    facet_id = f"{facet_id[:54]}-{_query_slug(label)}"
                query = _normalize_research_search_query(
                    f'site:{host} "{label}" {query_focus}'
                )[:280].strip()
                if len(query) < 8 or query.casefold() in seen_queries:
                    continue
                runtime_entity_rows.append(
                    {
                        "shardId": f"shard_plan_entity_{len(runtime_entity_rows) + 1}_{_query_slug(query)}",
                        "kind": f"facet:{facet_id}",
                        "researchFacetId": facet_id,
                        "query": query,
                        "evidenceQuery": query,
                        "facetGoal": f"{label}: {query_focus}",
                        "sourceIntent": "official_primary",
                        "verification": False,
                        "reason": "runtime_explicit_entity_coverage",
                        "plannedBy": "research-runtime",
                        "queryPlanModelId": model_label,
                    }
                )
                used_facet_ids.add(facet_id)
                seen_queries.add(query.casefold())
            ordered_primary = [*ordered_primary, *runtime_entity_rows]

        required_dimension_classes = _research_query_dimension_classes(question)
        require_entity_dimension_matrix = bool(
            decompose_root_question
            and len(official_entity_hints) >= 2
            and len(required_dimension_classes) >= 2
            and int(max_shards)
            >= len(official_entity_hints) * len(required_dimension_classes)
        )
        if require_entity_dimension_matrix:
            missing_entity_dimensions: list[str] = []
            for entity_index, hint in enumerate(official_entity_hints):
                entity_rows = [
                    row
                    for row in ordered_primary
                    if _research_entity_indexes_in_text(
                        official_entity_hints,
                        " ".join(
                            (
                                _safe_text(row.get("query")),
                                _safe_text(row.get("facetGoal")),
                            )
                        ),
                    )
                    == {entity_index}
                ]
                covered_dimensions = set().union(
                    *(
                        _research_query_dimension_classes(
                            " ".join(
                                (
                                    _safe_text(row.get("query")),
                                    _safe_text(row.get("facetGoal")),
                                )
                            )
                        )
                        for row in entity_rows
                    )
                ) if entity_rows else set()
                missing_dimensions = sorted(
                    required_dimension_classes - covered_dimensions
                )
                if (
                    len(entity_rows) < len(required_dimension_classes)
                    or missing_dimensions
                ):
                    missing_entity_dimensions.append(
                        f"{_safe_text(hint.get('label'))}:"
                        + ",".join(missing_dimensions or ["atomic_query_count"])
                    )
            if missing_entity_dimensions:
                candidate_errors.append(
                    f"{model_label}: query_plan_entity_dimension_coverage:"
                    + ";".join(missing_entity_dimensions)
                )
                continue

        runtime_verification_rows: list[dict[str, Any]] = []
        represented_site_hosts = {
            host
            for row in [*ordered_primary, *verification_rows]
            for host in _query_site_domains(row.get("query"))
        }
        for row in ordered_primary:
            if len(runtime_verification_rows) >= authority_budget:
                break
            if row.get("sourceIntent") != "official_primary" or _query_site_domains(
                row.get("query")
            ):
                continue
            row_text = " ".join(
                (
                    _safe_text(row.get("query")),
                    _safe_text(row.get("facetGoal")),
                )
            ).lower()
            matched_hint = next(
                (
                    hint
                    for hint in official_entity_hints
                    if not any(
                        _safe_text(host) in represented_site_hosts
                        for host in list(hint.get("hosts") or [hint.get("host")])
                    )
                    and any(
                        _safe_text(alias).lower() in row_text
                        for alias in list(hint.get("matchedAliases") or [])
                    )
                ),
                None,
            )
            if not matched_hint:
                continue
            host = _safe_text(matched_hint.get("host"))
            verification_query = _normalize_research_search_query(
                f"site:{host} {_safe_text(row.get('query'))}"
            )[:280].strip()
            if len(verification_query) < 8 or verification_query.casefold() in seen_queries:
                continue
            runtime_verification_rows.append(
                {
                    **row,
                    "shardId": f"shard_plan_verify_{len(runtime_verification_rows) + 1}_{_query_slug(verification_query)}",
                    "query": verification_query,
                    "evidenceQuery": verification_query,
                    "verification": True,
                    "reason": "runtime_official_host_verification",
                    "plannedBy": "research-runtime",
                }
            )
            represented_site_hosts.add(host)
            seen_queries.add(verification_query.casefold())
        verification_capacity = max(0, authority_budget - len(runtime_verification_rows))
        accepted = [
            *ordered_primary,
            *runtime_verification_rows,
            *verification_rows[:verification_capacity],
        ][:max_shards]
        if len(accepted) < len(normalized_facets):
            candidate_errors.append(f"{model_label}: query_plan_effective_cap_too_small")
            continue
        return accepted, {
            "used": True,
            "agentId": "web-research-architect",
            "modelId": model_label,
            "facetCount": len(ordered_primary),
            "architectFacetCount": architect_primary_count,
            "decomposedRootQuestion": decompose_root_question,
            "plannedShardCount": len(accepted),
            "authorityVerificationCount": sum(item.get("verification") is True for item in accepted),
            "runtimeAuthorityVerificationCount": sum(
                item.get("reason") == "runtime_official_host_verification" for item in accepted
            ),
            "explicitEntityCount": len(official_entity_hints),
            "runtimeEntityCoverageCount": sum(
                item.get("reason") == "runtime_explicit_entity_coverage" for item in accepted
            ),
            "errorsBeforeSuccess": candidate_errors[-6:],
            "researchPromptContractVersion": RESEARCH_PROMPT_CONTRACT_VERSION,
            "researchPromptContractDigest": stage_contract_digest,
        }

    return runtime_entity_fallback(
        "query_planner_protocol_failure",
        errors=candidate_errors,
        raw_preview="\n--- retry ---\n".join(raw_previews),
    )


def _research_query_subject(question: str) -> str:
    generic_terms = {
        "about",
        "are",
        "best",
        "cite",
        "current",
        "documentation",
        "evidence",
        "for",
        "latest",
        "official",
        "practice",
        "practices",
        "source",
        "sources",
        "the",
        "tools",
        "using",
        "what",
        "with",
    }
    terms: list[str] = []
    for term in re.split(r"[^a-z0-9_.+-]+", _safe_text(question).lower()):
        normalized = term.strip("._+-")
        if len(normalized) >= 3 and normalized not in generic_terms and normalized not in terms:
            terms.append(normalized)
    cjk_runs = re.findall(r"[\u4e00-\u9fff]{2,}", _safe_text(question))
    for run in cjk_runs[:2]:
        if run not in terms:
            terms.append(run[:40])
    return " ".join(terms[:8]) or _safe_text(question)[:160]


def _build_temporal_refinement_queries(
    question: str,
    source_matrix: list[dict[str, Any]],
    *,
    seen_queries: set[str],
    limit: int = 2,
) -> list[str]:
    subject = _research_query_subject(question)
    year = datetime.now(timezone.utc).year
    lowered_question = _safe_text(question).lower()
    asks_for_changes = bool(
        re.search(
            r"\b(?:change|changes|changed|changelog|release notes?|version history|what'?s new)\b",
            _safe_text(question).lower(),
        )
        or any(marker in _safe_text(question) for marker in ("变更", "更新日志", "发布说明", "版本历史"))
    )
    host_scores: dict[str, float] = {}
    for source in source_matrix:
        if not isinstance(source, dict):
            continue
        if not bool(source.get("selectedForEvidence") or (source.get("sourceQualityGate") or {}).get("selectedForEvidence")):
            continue
        host = (_safe_text(source.get("host")) or _host(_safe_text(source.get("url")))).lower().removeprefix("www.")
        authority = float(source.get("authorityScore") or 0)
        if host and _architect_support_role(source) == "primary":
            host_scores[host] = max(authority, host_scores.get(host, 0))

    candidates: list[str] = []
    if (
        ("pathlib" in lowered_question or "path object" in lowered_question)
        and ("cli" in lowered_question or "command-line" in lowered_question or "command line" in lowered_question)
    ):
        candidates.extend(
            (
                "https://peps.python.org/pep-0428/ pathlib object-oriented filesystem paths PEP 428",
                "https://peps.python.org/pep-0519/ pathlib PathLike filesystem path protocol PEP 519",
                "https://docs.python.org/3/library/os.html pathlib PathLike fspath current Python documentation",
                "https://click.palletsprojects.com/en/stable/parameter-types path_type pathlib.Path version",
                "https://typer.tiangolo.com/tutorial/parameter-types/path/ pathlib.Path command-line parameter current documentation",
            )
        )
    authoritative_hosts = sorted(host_scores.items(), key=lambda item: item[1], reverse=True)[:2]
    if asks_for_changes:
        for host, _score in authoritative_hosts:
            candidates.append(f"site:{host} {subject} release notes changelog version history")
    for host, _score in authoritative_hosts:
        candidates.append(f"site:{host} {subject} current official documentation version {year}")
    for host, _score in authoritative_hosts:
        candidates.append(f"site:{host} {subject} official reference last updated {year}")
    if asks_for_changes:
        candidates.append(f"{subject} official release notes version history")
    candidates.extend(
        (
            f"{subject} current official documentation version {year}",
            f"{subject} dated independent analysis updated {year}",
        )
    )

    queries: list[str] = []
    for query in candidates:
        normalized = query.lower()
        if normalized in seen_queries or normalized in {item.lower() for item in queries}:
            continue
        queries.append(query)
        if len(queries) >= max(1, limit):
            break
    return queries


def _research_unread_direct_urls(
    query: str,
    *,
    question: str,
    read_document_identities: set[str],
) -> tuple[list[str], list[str]]:
    direct_urls = [match.rstrip(".,;:") for match in _URL_IN_TEXT_RE.findall(query)]
    unread = [
        url
        for url in direct_urls
        if _research_document_identity(url, question=question) not in read_document_identities
    ]
    return direct_urls, unread


def _architect_existing_document_projection_shards(
    question: str,
    requested_queries: list[str],
    completed_shards: list[dict[str, Any]],
    *,
    source_policy: str,
    round_index: int,
    limit: int = 8,
) -> list[dict[str, Any]]:
    """Project precise Architect gaps onto authoritative bodies read this run."""

    existing_projections: set[tuple[str, str]] = set()
    for candidate in _research_source_candidates(
        question,
        completed_shards,
        source_policy=source_policy,
    ):
        document_id = _safe_text(candidate.get("documentId"))
        for view in list(candidate.get("evidenceViews") or []):
            evidence_query = _safe_text(view.get("evidenceQuery")).lower()
            if document_id and evidence_query:
                existing_projections.add((document_id, evidence_query))

    long_documents: dict[str, dict[str, Any]] = {}
    for candidate in _research_source_candidates(
        question,
        completed_shards,
        source_policy=source_policy,
    ):
        if candidate.get("gate", {}).get("selectedForEvidence") is not True:
            continue
        read_payload = candidate.get("readPayload") or {}
        quality = candidate.get("quality") or {}
        original_content_chars = _as_int(
            read_payload.get("originalContentChars"),
            len(
                _safe_text(
                    read_payload.get("text")
                    or read_payload.get("markdown")
                    or read_payload.get("textPreview")
                )
            ),
        )
        captured_text = _safe_text(
            read_payload.get("text")
            or read_payload.get("markdown")
            or read_payload.get("textPreview")
        )
        fully_captured = bool(
            captured_text
            and len(captured_text) >= max(MIN_RESEARCH_SOURCE_BODY_CHARS, original_content_chars)
        )
        if (
            (original_content_chars < 50_000 and not fully_captured)
            or int(quality.get("authorityScore") or 0) < 70
        ):
            continue
        document_id = _safe_text(candidate.get("documentId"))
        url = _safe_text(candidate.get("url"))
        if not document_id or not url:
            continue
        current = long_documents.get(document_id)
        document = {
            "documentId": document_id,
            "url": url,
            "title": _safe_text(
                read_payload.get("title")
                or (candidate.get("result") or {}).get("title")
            ),
            "snippet": _safe_text((candidate.get("result") or {}).get("snippet")),
            "text": captured_text,
            "authorityScore": int(quality.get("authorityScore") or 0),
            "tier": _safe_text(quality.get("tier")),
            "authorityTier": _safe_text(quality.get("authorityTier")),
            "originalContentChars": original_content_chars,
        }
        if current is None or (
            document["authorityScore"], document["originalContentChars"]
        ) > (
            int(current.get("authorityScore") or 0),
            int(current.get("originalContentChars") or 0),
        ):
            long_documents[document_id] = document

    if not long_documents:
        return []

    facets = [
        {
            "facetId": _research_facet_id(kind),
            "query": _safe_text(goal),
        }
        for goal, kind in _required_research_facet_queries(question, completed_shards)
        if _research_facet_id(kind) and _safe_text(goal)
    ]
    projections: list[dict[str, Any]] = []
    seen_queries: set[str] = set()
    for query in requested_queries:
        normalized_query = _safe_text(query)
        lowered_query = normalized_query.lower()
        if not normalized_query or lowered_query in seen_queries:
            continue
        seen_queries.add(lowered_query)
        if _URL_IN_TEXT_RE.search(normalized_query):
            continue
        structured_gap = bool(
            re.search(
                r"\b(?:article|annex|chapter|section|recital)\s+"
                r"(?:\d+[a-z]?(?:\s*\(\s*[a-z0-9]+\s*\))*|[ivxlcdm]+)"
                r"(?=$|[^a-z0-9])|"
                r"(?:第\s*\d+\s*条|附录\s*[A-Za-z0-9一二三四五六七八九十]+)",
                normalized_query,
                re.IGNORECASE,
            )
        )
        ranked_documents = sorted(
            long_documents.values(),
            key=lambda document: (
                _source_relevance_score(
                    normalized_query,
                    title=_safe_text(document.get("title")),
                    snippet=_safe_text(document.get("snippet")),
                    text=_safe_text(document.get("text")),
                ),
                int(document.get("authorityScore") or 0),
                int(document.get("originalContentChars") or 0),
            ),
            reverse=True,
        )
        document = next(
            (
                candidate_document
                for candidate_document in ranked_documents
                if (
                    structured_gap
                    or _source_relevance_score(
                        normalized_query,
                        title=_safe_text(candidate_document.get("title")),
                        snippet=_safe_text(candidate_document.get("snippet")),
                        text=_safe_text(candidate_document.get("text")),
                    )
                    >= 15
                )
                and (candidate_document["documentId"], lowered_query)
                not in existing_projections
            ),
            None,
        )
        if document is None:
            continue
        facet = (
            max(
                facets,
                key=lambda item: _source_relevance_score(
                    _safe_text(item.get("query")),
                    text=normalized_query,
                ),
            )
            if facets
            else None
        )
        facet_id = _safe_text((facet or {}).get("facetId")) or (
            f"architect-gap-{_query_slug(normalized_query)}"
        )
        source_intent = (
            "official_primary"
            if (
                _safe_text(document.get("authorityTier")).lower() == "primary"
                or _safe_text(document.get("tier")).lower() == "primary"
            )
            else "independent_secondary"
        )
        projections.append(
            {
                "shardId": (
                    f"shard_architect_projection_{round_index}_"
                    f"{len(projections) + 1}_{_query_slug(normalized_query)}"
                ),
                "kind": "seed_url",
                "seedUrl": document.get("url"),
                "query": normalized_query,
                "evidenceQuery": normalized_query,
                "researchFacetId": facet_id,
                "facetGoal": _safe_text((facet or {}).get("query")) or normalized_query,
                "sourceIntent": source_intent,
                "verification": True,
                "reason": "architect_existing_document_projection",
                "existingDocumentProjection": {
                    "documentId": document.get("documentId"),
                    "contentReused": True,
                    "reuseScope": "same_run_original_body",
                    "networkReadRequired": False,
                },
            }
        )
        if len(projections) >= max(1, int(limit or 0)):
            break
    return projections


def _research_shard_has_readable_fetch(shard: dict[str, Any]) -> bool:
    return any(
        isinstance(item, dict)
        and item.get("ok") is True
        and bool(
            _safe_text(
                item.get("text")
                or item.get("markdown")
                or item.get("body")
                or item.get("textPreview")
            )
        )
        for item in list(shard.get("fetchedTopSources") or [])
    )


def _architect_model_failure_without_evidence_gap(model_synthesis: dict[str, Any]) -> bool:
    if model_synthesis.get("used") is not False:
        return False
    if _safe_text(model_synthesis.get("mode")).lower() != "full_synthesis":
        return False
    reason = _safe_text(model_synthesis.get("fallbackReason"))
    if not reason:
        return False
    structural_stats = (
        model_synthesis.get("structuralStats")
        if isinstance(model_synthesis.get("structuralStats"), dict)
        else {}
    )
    structurally_complete = bool(
        model_synthesis.get("structuralTargetMet") is True
        or structural_stats.get("structuralTargetMet") is True
    )
    if _as_list(model_synthesis.get("missingFacetIds")) and not structurally_complete:
        return False
    return reason != "architect_evidence_candidates_insufficient"


def _research_loop_report(question: str, shards: list[dict[str, Any]], *, source_policy: str) -> dict[str, Any]:
    covered_claims: list[str] = []
    rejected_sources: list[dict[str, Any]] = []
    read_sources: list[str] = []
    for candidate in _research_source_candidates(question, shards, source_policy=source_policy):
        result = candidate["result"]
        read_payload = candidate["readPayload"]
        gate = candidate["gate"]
        url = candidate["url"]
        if gate.get("selectedForEvidence"):
            read_sources.append(url)
            text = _safe_text(read_payload.get("text") or read_payload.get("markdown") or read_payload.get("textPreview"))
            for sentence in _research_sentences(text, limit=2):
                if sentence not in covered_claims:
                    covered_claims.append(sentence)
                if len(covered_claims) >= 8:
                    break
        else:
            rejected_sources.append(
                {
                    "url": url,
                    "title": _safe_text(result.get("title"))[:180],
                    "reason": gate.get("rejectedReason") or "source_quality_below_gate",
                }
            )
    uncovered: list[str] = []
    if not read_sources:
        uncovered.append("No source passed quality/readability gates.")
    elif len(read_sources) < TARGET_RESEARCH_SOURCE_COUNT:
        uncovered.append(
            f"Need {TARGET_RESEARCH_SOURCE_COUNT - len(read_sources)} more readable source(s) to reach the normal Research target; "
            f"{MIN_RESEARCH_SOURCE_COUNT} is only the rejection floor."
        )
    return {
        "outline": [
            {"sectionId": "question", "title": "Research question", "status": "covered" if question else "missing"},
            {"sectionId": "evidence", "title": "Source-backed evidence", "status": "covered" if covered_claims else "missing"},
            {"sectionId": "limitations", "title": "Gaps and limitations", "status": "covered" if uncovered or rejected_sources else "pending"},
        ],
        "coveredClaims": covered_claims[:8],
        "uncoveredClaims": uncovered,
        "rejectedSources": rejected_sources[:12],
        "researchLoopReport": {
            "summary": "quality_gated_evidence_ready" if len(read_sources) >= TARGET_RESEARCH_SOURCE_COUNT else "quality_gated_evidence_incomplete",
            "selectedSourceCount": len(read_sources),
            "targetSourceCount": TARGET_RESEARCH_SOURCE_COUNT,
            "rejectedSourceCount": len(rejected_sources),
            "claimCount": len(covered_claims),
        },
    }


def _recent_research_discovery_seed_shards(
    question: str,
    planned_shards: list[dict[str, Any]],
    *,
    scope: str,
    source_limit: int = _RESEARCH_ARCHITECT_MAX_SOURCE_COUNT,
) -> list[dict[str, Any]]:
    """Reuse recent verified URLs as discovery hints, never as evidence.

    Evidence bundles already expire after six hours. Only an exact topic match
    is eligible, and every returned URL still goes through a new live read and
    the complete source, claim, temporal, and independent-review gates.
    """

    topic_fingerprint = _topic_fingerprint(question)
    entity_hints = _catalog_official_entity_hints(question)
    if len(entity_hints) < 2:
        entity_hints = []
    facets: list[dict[str, Any]] = []
    seen_facets: set[str] = set()
    for shard in planned_shards:
        facet_id = _safe_text(shard.get("researchFacetId")) or _research_facet_id(
            shard.get("kind")
        )
        query = _safe_text(
            shard.get("facetGoal")
            or shard.get("evidenceQuery")
            or shard.get("query")
        )
        if not facet_id or not query or facet_id in seen_facets:
            continue
        seen_facets.add(facet_id)
        facets.append(
            {
                "facetId": facet_id,
                "query": query,
                "sourceIntent": _safe_text(shard.get("sourceIntent")),
            }
        )

    discovered: dict[str, dict[str, Any]] = {}
    for bundle in list_evidence_bundles(scope=scope, limit=50):
        if _safe_text(bundle.get("topicFingerprint")) != topic_fingerprint:
            continue
        bundle_id = _safe_text(bundle.get("evidenceBundleId"))
        for source in list(bundle.get("sourceMatrix") or []):
            if not isinstance(source, dict):
                continue
            prior_rejection = _safe_text(source.get("rejectedReason"))
            if (
                source.get("selectedForEvidence") is not True
                and prior_rejection != "authority_too_low"
            ):
                continue
            read_evidence = (
                source.get("readEvidence")
                if isinstance(source.get("readEvidence"), dict)
                else {}
            )
            url = _safe_text(source.get("url") or source.get("sourceUrl"))
            if not url or read_evidence.get("verified") is not True:
                continue
            identity = _research_document_identity(url, question=question)
            if not identity or identity in discovered:
                continue
            historical_queries = " ".join(
                _safe_text(view.get("evidenceQuery"))
                for view in list(source.get("evidenceViews") or [])
                if isinstance(view, dict) and _safe_text(view.get("evidenceQuery"))
            )
            discovered[identity] = {
                "url": url,
                "host": _host(url),
                "title": _safe_text(source.get("title")),
                "snippet": _safe_text(source.get("snippet")),
                "historicalQueries": historical_queries,
                "authorityScore": _as_int(source.get("authorityScore"), 0),
                "tier": _safe_text(source.get("tier")),
                "authorityTier": _safe_text(source.get("authorityTier")),
                "contentChars": _as_int(source.get("contentChars"), 0),
                "originalContentChars": _as_int(
                    source.get("originalContentChars"),
                    _as_int(source.get("contentChars"), 0),
                ),
                "originBundleId": bundle_id,
                "originRetrievedAt": _safe_text(source.get("retrievedAt")),
            }

    ranked = sorted(
        discovered.values(),
        key=lambda item: (
            int(item.get("authorityScore") or 0),
            int(item.get("originalContentChars") or 0),
            int(item.get("contentChars") or 0),
        ),
        reverse=True,
    )

    def facet_score(source: dict[str, Any], facet: dict[str, Any]) -> int:
        return _source_relevance_score(
            _safe_text(facet.get("query")),
            title=_safe_text(source.get("title")),
            snippet=_safe_text(source.get("snippet")),
            text=_safe_text(source.get("historicalQueries")),
        )

    def facet_identity_score(source: dict[str, Any], facet: dict[str, Any]) -> int:
        return _source_relevance_score(
            _safe_text(facet.get("query")),
            title=_safe_text(source.get("title")),
            snippet=_safe_text(source.get("snippet")),
        )

    def facet_source_entity_compatible(
        source: dict[str, Any],
        facet: dict[str, Any],
    ) -> bool:
        if not entity_hints:
            return True
        evidence_query = _safe_text(facet.get("query"))
        query_entity_indexes = _research_entity_indexes_in_text(
            entity_hints,
            evidence_query,
        )
        if not query_entity_indexes:
            return True
        source_entity_indexes = _research_source_entity_indexes(
            entity_hints,
            host=source.get("host") or _host(_safe_text(source.get("url"))),
            url=source.get("url"),
            title=source.get("title"),
            text=source.get("snippet"),
            evidence_query=evidence_query,
        )
        return bool(query_entity_indexes.intersection(source_entity_indexes))

    def facet_rank(
        source: dict[str, Any],
        facet: dict[str, Any],
    ) -> tuple[int, int]:
        return (
            facet_identity_score(source, facet),
            facet_score(source, facet),
        )

    selection_limit = max(1, min(int(source_limit or 0), 16))
    selected: list[dict[str, Any]] = []
    selected_urls: set[str] = set()
    selected_hosts: set[str] = set()

    def select_source(source: dict[str, Any]) -> None:
        url = _safe_text(source.get("url"))
        host = _safe_text(source.get("host"))
        if not url or url in selected_urls or len(selected) >= selection_limit:
            return
        selected.append(source)
        selected_urls.add(url)
        if host:
            selected_hosts.add(host)

    # Preserve one verified URL that actually matches each current facet
    # before generic authority/body-size ranking. Otherwise short official
    # Code, template, or article pages are displaced by a few omnibus texts,
    # and the live re-read repeats the wrong documents for every facet.
    for facet in facets:
        if len(selected) >= selection_limit or not ranked:
            break
        compatible_sources = [
            source
            for source in ranked
            if facet_source_entity_compatible(source, facet)
        ]
        if not compatible_sources:
            continue
        best_source = max(
            compatible_sources,
            key=lambda source: (
                *facet_rank(source, facet),
                int(source.get("authorityScore") or 0),
                int(source.get("originalContentChars") or 0),
            ),
        )
        if max(facet_rank(best_source, facet)) > 0:
            select_source(best_source)

    for prefer_new_host in (True, False):
        for source in ranked:
            if len(selected) >= selection_limit:
                break
            url = _safe_text(source.get("url"))
            host = _safe_text(source.get("host"))
            if not url or url in selected_urls:
                continue
            if prefer_new_host and host in selected_hosts:
                continue
            select_source(source)

    seeds: list[dict[str, Any]] = []
    assigned_facets: set[str] = set()
    seeded_pairs: set[tuple[str, str]] = set()

    def append_seed(source: dict[str, Any], facet: dict[str, Any] | None) -> None:
        facet_id = _safe_text((facet or {}).get("facetId"))
        evidence_query = _safe_text((facet or {}).get("query")) or question
        pair = (_safe_text(source.get("url")), facet_id)
        if pair in seeded_pairs:
            return
        seeded_pairs.add(pair)
        source_intent = _safe_text((facet or {}).get("sourceIntent"))
        if source_intent == "official_primary" and (
            _safe_text(source.get("tier")).lower() in {"secondary", "weak"}
            or (
                _safe_text(source.get("authorityTier")).lower() != "primary"
                and int(source.get("authorityScore") or 0) < 70
            )
        ):
            source_intent = "independent_secondary"
        seed_key = hashlib.sha1(
            f"{source.get('url')}|{facet_id}|{evidence_query}".encode("utf-8", errors="ignore")
        ).hexdigest()[:10]
        seeds.append(
            {
                "shardId": f"shard_recent_discovery_{seed_key}",
                "kind": "seed_url",
                "seedUrl": source.get("url"),
                "query": evidence_query,
                "evidenceQuery": evidence_query,
                **({"researchFacetId": facet_id} if facet_id else {}),
                **({"facetGoal": evidence_query} if facet_id else {}),
                **({"sourceIntent": source_intent} if source_intent else {}),
                "verification": True,
                "reason": "recent_verified_discovery_live_reread",
                "discoveryReuse": {
                    "kind": "recent_verified_url",
                    "originBundleId": source.get("originBundleId"),
                    "originRetrievedAt": source.get("originRetrievedAt"),
                    "contentReused": False,
                    "requiresLiveRead": True,
                },
            }
        )
        if facet_id:
            assigned_facets.add(facet_id)

    for source in selected:
        compatible_facets = [
            facet
            for facet in facets
            if facet_source_entity_compatible(source, facet)
        ]
        best_facet = (
            max(compatible_facets, key=lambda facet: facet_rank(source, facet))
            if compatible_facets
            else None
        )
        append_seed(source, best_facet)

    for facet in facets:
        facet_id = _safe_text(facet.get("facetId"))
        if facet_id in assigned_facets or not selected:
            continue
        compatible_sources = [
            source
            for source in selected
            if facet_source_entity_compatible(source, facet)
        ]
        if compatible_sources:
            append_seed(
                max(compatible_sources, key=lambda source: facet_rank(source, facet)),
                facet,
            )

    # One long primary document can contain many independently addressable
    # clauses (for example a statute, standard, or specification). Re-read it
    # once, then project the cached original body against every current facet;
    # this does not create extra sources and does not reuse historical content.
    omnibus_sources = [
        source
        for source in selected
        if int(source.get("originalContentChars") or 0) >= 100_000
        and int(source.get("authorityScore") or 0) >= 70
        and _safe_text(source.get("tier")).lower() != "secondary"
    ]
    if omnibus_sources:
        omnibus = max(
            omnibus_sources,
            key=lambda source: (
                facet_score(source, {"query": question}),
                int(source.get("authorityScore") or 0),
                int(source.get("originalContentChars") or 0),
            ),
        )
        for facet in facets:
            if facet_source_entity_compatible(omnibus, facet):
                append_seed(omnibus, facet)

    return seeds[:32]


_RESEARCH_PROVIDER_TERMINAL_FAILURES = {
    "credential_missing",
    "provider_adapter_unavailable",
    "provider_disabled",
    "provider_unconfigured",
    "provider_unknown",
    "needs_agent_browser_login",
    "runtime_dependency_missing",
    "blocked_by_safety",
}
_RESEARCH_PROVIDER_BOUNDED_RETRY_FAILURES = {
    "deadline_exceeded",
    "network_timeout",
    "provider_challenge",
    "rate_limited",
    "search_failed",
    "service_unavailable",
}
_RESEARCH_PROVIDER_RELEVANCE_FAILURES = {
    "irrelevant_results",
    "no_results",
}


def _research_transport_failure_summary(shards: list[dict[str, Any]]) -> dict[str, Any]:
    """Summarize whether another query round can use a different transport.

    A strict-network host can otherwise spend every Research round retrying the
    exact same missing credentials, login-only providers, and timed-out public
    endpoints.  This is a per-run circuit breaker: configuration is re-read on
    the next Research run, so logging into Agent Browser immediately restores
    MetaSo/Baidu eligibility without a process restart.
    """

    attempts = [
        dict(item)
        for item in _flatten_provider_attempts(shards)
        if isinstance(item, dict) and _safe_text(item.get("provider"))
    ]
    if not attempts:
        return {}
    by_provider: dict[str, list[dict[str, Any]]] = {}
    for attempt in attempts:
        by_provider.setdefault(_safe_text(attempt.get("provider")).lower(), []).append(attempt)

    provider_states: list[dict[str, Any]] = []
    has_usable_provider = False
    reachable_but_irrelevant = False
    exhausted_for_run = True
    for provider, provider_attempts in sorted(by_provider.items()):
        statuses = {
            _safe_text(item.get("status")).lower()
            for item in provider_attempts
            if _safe_text(item.get("status"))
        }
        failure_classes = {
            _safe_text(item.get("failureClass") or item.get("reason")).lower()
            for item in provider_attempts
            if _safe_text(item.get("failureClass") or item.get("reason"))
        }
        if statuses & {"ok", "success"}:
            state = "usable"
            has_usable_provider = True
            exhausted_for_run = False
        elif failure_classes and failure_classes <= _RESEARCH_PROVIDER_TERMINAL_FAILURES:
            state = "unavailable_until_configuration_changes"
        elif failure_classes and failure_classes <= _RESEARCH_PROVIDER_RELEVANCE_FAILURES:
            state = "reachable_but_irrelevant"
            reachable_but_irrelevant = True
        elif (
            failure_classes
            and failure_classes <= _RESEARCH_PROVIDER_BOUNDED_RETRY_FAILURES
            and len(provider_attempts) >= 2
        ):
            state = "repeated_transport_failure"
        else:
            state = "retryable_or_unknown"
            exhausted_for_run = False
        provider_states.append(
            {
                "provider": provider,
                "state": state,
                "attemptCount": len(provider_attempts),
                "failureClasses": sorted(failure_classes)[:6],
            }
        )

    return {
        "exhaustedForRun": bool(exhausted_for_run and not has_usable_provider),
        "reachableButIrrelevant": reachable_but_irrelevant,
        "providerCount": len(provider_states),
        "providers": provider_states,
        "recommendedNextAction": (
            "Open the governed Agent Browser login surface or configure one search provider, then start a new Research run."
            if exhausted_for_run and not has_usable_provider
            else "Refine the query or continue with the remaining usable provider."
        ),
    }


def _run_research_loop(
    *,
    question: str,
    initial_shards: list[dict[str, Any]],
    allowed_domains: list[str],
    blocked_domains: list[str],
    source_policy: str,
    freshness: str,
    max_rounds: int,
    use_agent_browser_profile: bool,
    tool_call_id: str,
    read_attempt_ledger: _ResearchReadAttemptLedger | None = None,
    recent_discovery_seed_shards: list[dict[str, Any]] | None = None,
) -> tuple[list[dict[str, Any]], dict[str, Any]]:
    loop_state: dict[str, Any] = {
        "phase": "research_loop",
        "questions": [question],
        "rounds": [],
        "readSources": [],
        "outline": [],
        "coveredClaims": [],
        "uncoveredClaims": [],
        "conflictClaims": [],
        "sourceGaps": [],
        "rejectedSources": [],
        "nextQueries": [],
        "researchLoopReport": {},
        "stopReason": "",
    }
    all_shards: list[dict[str, Any]] = []
    attempt_ledger = read_attempt_ledger or _ResearchReadAttemptLedger(
        question=question,
    )
    if _should_try_context7_source(question, source_policy):
        context7_shard = _run_context7_source(question, tool_call_id=tool_call_id)
        all_shards.append(context7_shard)
        for item in list(context7_shard.get("fetchedTopSources") or []):
            if isinstance(item, dict) and _safe_text(item.get("finalUrl") or item.get("url")):
                attempt_ledger.mark_terminal(item.get("finalUrl") or item.get("url"))
        if not context7_shard.get("ok"):
            loop_state["sourceGaps"].append(
                {
                    "provider": "context7",
                    "reason": _safe_text((context7_shard.get("errors") or ["context7_unavailable"])[0]),
                    "policy": "official_docs_first",
                }
            )
    pending_shards = list(initial_shards)
    pending_discovery_seeds = list(recent_discovery_seed_shards or [])
    final_stats: dict[str, Any] = {
        "selectedSourceCount": 0,
        "distinctHostCount": 0,
        "datedSourceCount": 0,
        "sourceUrls": [],
    }
    # Keep one bounded round for an Architect-discovered gap, but let the
    # deterministic discovery loop use the remaining budget. The previous
    # ``max_rounds - (max_rounds - 1)`` formula made this cap permanently one.
    architect_repair_reserve = 1 if max_rounds > 1 else 0
    discovery_round_cap = max(1, max_rounds - architect_repair_reserve)
    for round_index in range(1, discovery_round_cap + 1):
        if not pending_shards:
            loop_state["stopReason"] = "no_refinement_queries_available"
            break
        completed = _run_search_shards(
            pending_shards,
            allowed_domains=allowed_domains,
            blocked_domains=blocked_domains,
            source_policy=source_policy,
            max_rounds=max_rounds,
            use_agent_browser_profile=bool(use_agent_browser_profile),
            tool_call_id=tool_call_id,
            read_attempt_ledger=attempt_ledger,
            read_round=round_index,
        )
        all_shards.extend(completed)
        final_stats = _selected_source_stats(question, all_shards, source_policy=source_policy)
        facet_stats = _selected_facet_evidence_stats(
            question,
            all_shards,
            source_policy=source_policy,
        )
        loop_state["rounds"].append(
            {
                "round": round_index,
                "queries": [shard.get("query") for shard in pending_shards],
                "resultCount": sum(int(shard.get("resultCount") or 0) for shard in completed),
                "readSourceCount": _read_source_count(all_shards, question=question),
                **final_stats,
                "facetEvidence": facet_stats,
            }
        )
        transport_summary = _research_transport_failure_summary(completed)
        if transport_summary:
            loop_state["rounds"][-1]["transportSummary"] = transport_summary
        source_target_met = final_stats["selectedSourceCount"] >= TARGET_RESEARCH_SOURCE_COUNT
        host_target_met = final_stats["distinctHostCount"] >= TARGET_RESEARCH_DISTINCT_HOST_COUNT
        facet_target_met = facet_stats.get("complete") is True
        if source_target_met and host_target_met and facet_target_met:
            loop_state["stopReason"] = "high_quality_evidence_target_met"
            break
        if round_index >= discovery_round_cap:
            loop_state["stopReason"] = "discovery_budget_reached_before_architect_review"
            break
        if pending_discovery_seeds:
            pending_shards = pending_discovery_seeds
            pending_discovery_seeds = []
            loop_state["discoveryReuse"] = {
                "kind": "recent_verified_url_live_reread",
                "seedShardCount": len(pending_shards),
                "originBundleIds": list(
                    dict.fromkeys(
                        _safe_text((shard.get("discoveryReuse") or {}).get("originBundleId"))
                        for shard in pending_shards
                        if _safe_text((shard.get("discoveryReuse") or {}).get("originBundleId"))
                    )
                ),
                "contentReused": False,
                "liveReadRequired": True,
            }
            loop_state["nextQueries"] = [shard.get("query") for shard in pending_shards]
            continue
        if (
            int(final_stats.get("selectedSourceCount") or 0) == 0
            and transport_summary.get("exhaustedForRun") is True
            and (
                transport_summary.get("reachableButIrrelevant") is not True
                or round_index >= min(2, discovery_round_cap)
            )
        ):
            loop_state["stopReason"] = "source_transport_exhausted"
            loop_state["transportSummary"] = transport_summary
            loop_state["nextQueries"] = []
            break
        missing_sources = max(0, TARGET_RESEARCH_SOURCE_COUNT - int(final_stats["selectedSourceCount"]))
        missing_facets = list(facet_stats.get("missingFacetIds") or [])
        refinement_limit = max(
            2,
            min(
                8,
                len(missing_facets),
                ((missing_sources + 1) // 2) + 1,
            )
            if missing_facets and missing_sources
            else min(8, len(missing_facets))
            if missing_facets
            else min(4, ((missing_sources + 1) // 2) + 1),
        )
        pending_shards = _build_refinement_shards(
            question,
            all_shards,
            source_policy=source_policy,
            limit=refinement_limit,
            round_index=round_index + 1,
        )
        loop_state["nextQueries"] = [shard.get("query") for shard in pending_shards]

    loop_state["readSources"] = list(final_stats.get("sourceUrls") or [])
    if int(final_stats.get("selectedSourceCount") or 0) < TARGET_RESEARCH_SOURCE_COUNT:
        loop_state["uncoveredClaims"].append(
            f"Readable source target not met: {final_stats.get('selectedSourceCount', 0)}/{TARGET_RESEARCH_SOURCE_COUNT}."
        )
    if int(final_stats.get("distinctHostCount") or 0) < TARGET_RESEARCH_DISTINCT_HOST_COUNT:
        loop_state["uncoveredClaims"].append(
            f"Independent host target not met: {final_stats.get('distinctHostCount', 0)}/{TARGET_RESEARCH_DISTINCT_HOST_COUNT}."
        )
    final_facet_stats = _selected_facet_evidence_stats(
        question,
        all_shards,
        source_policy=source_policy,
    )
    if final_facet_stats.get("missingFacetIds"):
        loop_state["uncoveredClaims"].append(
            "Readable evidence missing for explicit Research facets: "
            + ", ".join(final_facet_stats.get("missingFacetIds") or [])
            + "."
        )
    loop_state["facetEvidence"] = final_facet_stats
    report = _research_loop_report(question, all_shards, source_policy=source_policy)
    if loop_state["uncoveredClaims"]:
        report["uncoveredClaims"] = list(dict.fromkeys([*report.get("uncoveredClaims", []), *loop_state["uncoveredClaims"]]))
    loop_state.update(report)
    loop_state["readAttemptStats"] = attempt_ledger.snapshot()
    return all_shards, loop_state


def _research_shards_for_bundle(
    question: str,
    shards: list[dict[str, Any]],
    selected_sources: list[dict[str, Any]],
    *,
    limit: int = _RESEARCH_ARCHITECT_MAX_SOURCE_COUNT * 2,
) -> list[dict[str, Any]]:
    bounded_limit = max(1, int(limit or 0))
    shard_rows: list[
        tuple[int, dict[str, Any], set[str], bool, set[tuple[str, str, str]]]
    ] = []
    for index, shard in enumerate(shards):
        identities = {
            _research_document_identity(
                _safe_text(item.get("finalUrl") or item.get("url")),
                question=question,
            )
            for item in list(shard.get("fetchedTopSources") or [])
            if isinstance(item, dict)
            and item.get("ok")
            and _safe_text(item.get("text") or item.get("markdown") or item.get("textPreview"))
            and _safe_text(item.get("finalUrl") or item.get("url"))
        }
        facet_id = _safe_text(shard.get("researchFacetId")) or _research_facet_id(
            shard.get("kind")
        )
        evidence_query = _safe_text(shard.get("evidenceQuery") or shard.get("query"))
        view_identities = {
            (identity, facet_id, evidence_query.casefold())
            for identity in identities
            if evidence_query
        }
        shard_rows.append(
            (index, shard, identities, bool(identities), view_identities)
        )

    required_sources = [
        (
            _research_document_identity(_safe_text(source.get("url")), question=question),
            _safe_text(source.get("shardId")),
        )
        for source in selected_sources
        if _safe_text(source.get("url"))
    ]
    selected_indexes: set[int] = set()
    covered_identities: set[str] = set()
    covered_views: set[tuple[str, str, str]] = set()
    selected_shards: list[dict[str, Any]] = []
    for identity, preferred_shard_id in required_sources:
        if not identity or identity in covered_identities or len(selected_shards) >= bounded_limit:
            continue
        match = next(
            (
                row
                for row in shard_rows
                if row[0] not in selected_indexes
                and identity in row[2]
                and preferred_shard_id
                and _safe_text(row[1].get("shardId")) == preferred_shard_id
            ),
            None,
        )
        if match is None:
            match = next(
                (
                    row
                    for row in shard_rows
                    if row[0] not in selected_indexes and identity in row[2]
                ),
                None,
            )
        if match is None:
            continue
        index, shard, identities, _has_readable_fetch, view_identities = match
        selected_indexes.add(index)
        covered_identities.update(identities)
        covered_views.update(view_identities)
        selected_shards.append(shard)

    # One long document may be read once and projected against several exact
    # facets. Preserve each distinct focused view needed for deterministic
    # replay; document-level de-duplication alone loses the evidence chain.
    for index, shard, _identities, has_readable_fetch, view_identities in shard_rows:
        if len(selected_shards) >= bounded_limit:
            break
        if (
            index in selected_indexes
            or not has_readable_fetch
            or not (view_identities - covered_views)
        ):
            continue
        selected_indexes.add(index)
        covered_views.update(view_identities)
        selected_shards.append(shard)

    for index, shard, _identities, has_readable_fetch, view_identities in shard_rows:
        if len(selected_shards) >= bounded_limit:
            break
        if index in selected_indexes or not has_readable_fetch:
            continue
        selected_indexes.add(index)
        covered_views.update(view_identities)
        selected_shards.append(shard)
    return selected_shards


def _research_source_matrix_for_bundle(
    question: str,
    source_matrix: list[dict[str, Any]],
    architect_sources: list[dict[str, Any]],
    *,
    limit: int = _RESEARCH_ARCHITECT_MAX_SOURCE_COUNT,
) -> list[dict[str, Any]]:
    bounded_limit = max(1, int(limit or 0))
    sources_by_identity: dict[str, list[dict[str, Any]]] = {}
    for source in source_matrix:
        identity = _research_document_identity(
            _safe_text(source.get("url")),
            question=question,
        )
        if identity:
            sources_by_identity.setdefault(identity, []).append(source)

    persisted: list[dict[str, Any]] = []
    seen_identities: set[str] = set()

    def add_source(source: dict[str, Any], *, citation_key: Any = "") -> None:
        if len(persisted) >= bounded_limit:
            return
        identity = _research_document_identity(
            _safe_text(source.get("url")),
            question=question,
        )
        if not identity or identity in seen_identities:
            return
        row = dict(source)
        if _safe_text(citation_key):
            row["citationKey"] = _safe_text(citation_key)
        persisted.append(row)
        seen_identities.add(identity)

    for architect_source in architect_sources:
        url = _safe_text(architect_source.get("url"))
        identity = _research_document_identity(url, question=question)
        candidates = sources_by_identity.get(identity) or []
        source = next(
            (
                candidate
                for candidate in candidates
                if _safe_text(candidate.get("url")) == url
            ),
            candidates[0] if candidates else None,
        )
        if source is not None:
            add_source(
                source,
                citation_key=architect_source.get("citationKey"),
            )
        if len(persisted) >= bounded_limit:
            break

    for source in source_matrix:
        add_source(source)
        if len(persisted) >= bounded_limit:
            break
    return persisted


def _synthesize_bundle(
    *,
    question: str,
    research_intent: str,
    source_policy: str,
    freshness: str,
    shards: list[dict[str, Any]],
    deliverable: str,
    research_loop_state: dict[str, Any] | None = None,
    experience_reuse: dict[str, Any] | None = None,
    architect_call_state: dict[str, Any] | None = None,
) -> dict[str, Any]:
    source_matrix: list[dict[str, Any]] = []
    citations: list[dict[str, Any]] = []
    conflicts: list[dict[str, Any]] = []
    as_of = _utc_now_iso()
    for candidate in _research_source_candidates(question, shards, source_policy=source_policy):
        result = candidate["result"]
        read_payload = candidate["readPayload"]
        quality = candidate["quality"]
        gate = candidate["gate"]
        shard = candidate["shard"]
        url = candidate["url"]
        temporal = _source_temporal_evidence(read_payload, result)
        read_text = _safe_text(read_payload.get("text") or read_payload.get("markdown") or read_payload.get("textPreview"))
        read_evidence = _read_evidence_receipt(read_text, retrieved_at=temporal.get("retrievedAt"))
        source_matrix.append(
            {
                "sourceId": _source_id(url),
                "documentId": candidate["documentId"],
                "documentPriority": candidate["documentPriority"],
                "requestedUrl": candidate["requestedUrl"] if candidate["requestedUrl"] != url else None,
                "title": result.get("title"),
                "url": url,
                "host": _host(url) or quality.get("host"),
                "authorityScore": quality.get("authorityScore"),
                "tier": quality.get("tier"),
                "authorityTier": quality.get("authorityTier"),
                "catalogSourceId": quality.get("catalogSourceId"),
                "catalogCategory": quality.get("catalogCategory"),
                "popularitySignals": quality.get("popularitySignals") or [],
                "matchedSignals": quality.get("reasons") or [],
                "snippet": _safe_text(result.get("snippet"))[:240],
                "provider": shard.get("provider"),
                "networkRoute": shard.get("networkRoute"),
                "shardId": shard.get("shardId"),
                "researchFacetId": (
                    _safe_text(shard.get("researchFacetId"))
                    or _research_facet_id(shard.get("kind"))
                    or None
                ),
                "researchFacetIds": list(candidate.get("researchFacetIds") or []),
                "researchFacetGoal": shard.get("facetGoal"),
                "evidenceViews": [
                    {
                        "shardId": _safe_text(view.get("shardId")),
                        "researchFacetId": _safe_text(view.get("researchFacetId")),
                        "evidenceQuery": _safe_text(view.get("evidenceQuery")),
                    }
                    for view in list(candidate.get("evidenceViews") or [])
                    if view.get("selectedForEvidence") is True
                ],
                "evidenceQuery": candidate.get("evidenceQuery"),
                "sourceRouterLocale": ((shard.get("sourceRouter") or {}).get("locale") if isinstance(shard.get("sourceRouter"), dict) else None),
                "sourceQualityGate": gate,
                "selectedForEvidence": gate.get("selectedForEvidence"),
                "rejectedReason": gate.get("rejectedReason") or None,
                "retrievedAt": temporal.get("retrievedAt"),
                "publishedAt": temporal.get("publishedAt"),
                "updatedAt": temporal.get("updatedAt"),
                "version": temporal.get("version"),
                "temporalEvidence": temporal,
                "contentChars": len(read_text),
                "originalContentChars": read_payload.get("originalContentChars") or len(read_text),
                "omittedChars": read_payload.get("omittedChars") or 0,
                "evidenceSelection": read_payload.get("evidenceSelection") or "read_body",
                "readEvidence": read_evidence,
            }
        )
        if gate.get("selectedForEvidence"):
            citations.append({"title": result.get("title"), "url": url})
    source_matrix.sort(
        key=lambda item: (
            1 if (item.get("sourceQualityGate") or {}).get("selectedForEvidence") else 0,
            int(item.get("authorityScore") or 0),
            int(item.get("documentPriority") or 0),
            int(item.get("contentChars") or 0),
        ),
        reverse=True,
    )
    for index, source in enumerate(source_matrix, start=1):
        source["citationKey"] = f"S{index}"
    selected_source_matrix = [item for item in source_matrix if (item.get("sourceQualityGate") or {}).get("selectedForEvidence")]
    evidence_bank = _build_evidence_bank(question=question, source_matrix=source_matrix, shards=shards)
    authority_scores = [int(item.get("authorityScore") or 0) for item in (selected_source_matrix or source_matrix)[:TARGET_RESEARCH_SOURCE_COUNT]]
    average_authority = round(sum(authority_scores) / len(authority_scores), 1) if authority_scores else 0
    confidence = (
        "high"
        if average_authority >= 75 and len(selected_source_matrix) >= TARGET_RESEARCH_SOURCE_COUNT
        else ("medium" if len(selected_source_matrix) >= MIN_RESEARCH_SOURCE_COUNT else "low")
    )
    if not source_matrix:
        conflicts.append({"kind": "no_sources", "summary": "No usable source result was collected."})
    elif not selected_source_matrix:
        conflicts.append({"kind": "no_quality_sources", "summary": "Search returned candidates, but none passed source quality/readability gates."})
    provider_attempt_matrix = _flatten_provider_attempts(shards)
    architect_pack = _web_research_architect_pack(
        question=question,
        source_matrix=selected_source_matrix,
        shards=shards,
        confidence=confidence,
        average_authority=average_authority,
        freshness=freshness,
        architect_call_state=architect_call_state,
    )
    claim_table = list(architect_pack.get("claimTable") or [])
    conflict_matrix = list(architect_pack.get("conflictMatrix") or [])
    if conflicts:
        conflict_matrix.extend(conflicts)
    architect_sources = [
        dict(item)
        for item in list(architect_pack.get("sourceUrls") or [])
        if isinstance(item, dict) and item.get("url")
    ]
    persisted_source_matrix = _research_source_matrix_for_bundle(
        question,
        source_matrix,
        architect_sources,
    )
    visible_shards = _research_shards_for_bundle(
        question,
        shards,
        persisted_source_matrix,
    )
    source_urls = [item.get("url") for item in architect_sources]
    brief_coverage = _research_brief_coverage(
        question=question,
        # Architect-facing source refs are intentionally compact and may omit
        # Runtime routing metadata such as researchFacetId. Coverage is a
        # Runtime gate, so evaluate it against the canonical selected matrix.
        source_matrix=selected_source_matrix,
        claim_table=claim_table,
        answer=_safe_text(architect_pack.get("answer") or architect_pack.get("researchResult")),
    )
    bundle = {
        "ok": False,
        "kind": "research_evidence_bundle",
        "summary": architect_pack.get("headline") or f"Collected {len(source_matrix)} ranked source(s) across {len(shards)} research shard(s).",
        "question": question,
        "topicFingerprint": _topic_fingerprint(question),
        "researchIntent": research_intent,
        "sourcePolicy": source_policy,
        "freshness": freshness,
        "deliverable": deliverable,
        "answer": architect_pack.get("answer") or "",
        "resultPreview": architect_pack.get("answer") or "",
        "researchResult": architect_pack,
        "finalExperiencePack": architect_pack,
        "claimTable": claim_table,
        "briefCoverageRequired": brief_coverage.get("required") is True,
        "briefCoverageComplete": brief_coverage.get("complete") is True,
        "briefCoverage": brief_coverage.get("items") or [],
        "coveredTaskBriefIds": brief_coverage.get("coveredFacetIds") or [],
        "missingTaskBriefIds": brief_coverage.get("missingFacetIds") or [],
        "conflictMatrix": conflict_matrix,
        "missingEvidence": list(architect_pack.get("missingEvidence") or []),
        "criticalMissingEvidence": list(architect_pack.get("criticalMissingEvidence") or []),
        "reviewDecision": architect_pack.get("reviewDecision"),
        "reviewReasons": list(architect_pack.get("reviewReasons") or []),
        "independentReview": architect_pack.get("independentReview") or {},
        "recommendedNextQueries": list(architect_pack.get("recommendedNextQueries") or []),
        "asOf": architect_pack.get("asOf") or as_of,
        "assumptions": list(architect_pack.get("assumptions") or []),
        "sourceUrls": source_urls,
        "providerAttemptMatrix": provider_attempt_matrix,
        "researchLoopState": research_loop_state
        or {
            "phase": "single_pass",
            "rounds": [],
            "stopReason": "single_pass_synthesis",
            "questions": [question],
            "readSources": source_urls,
            "uncoveredClaims": [],
            "conflictClaims": [],
            "nextQueries": [],
        },
        "researchEvidenceBank": evidence_bank,
        "rejectedSources": evidence_bank.get("rejectedSources") or [],
        "experienceReuse": experience_reuse
        or {
            "reuseDecision": "ignore",
            "reason": "not_checked",
            "candidatePackId": None,
            "topicFingerprint": _topic_fingerprint(question),
        },
        "refinedBy": {
            "agentId": "web-research-architect",
            "agentName": "Web Research Architect",
            "role": "research_runtime_synthesis",
        },
        "confidence": confidence,
        "authorityScore": average_authority,
        "sourceMatrix": persisted_source_matrix,
        "conflicts": conflict_matrix,
        "citations": citations[:8],
        "shards": [
            {
                "shardId": shard.get("shardId"),
                "kind": shard.get("kind"),
                "query": shard.get("query"),
                "researchFacetId": shard.get("researchFacetId"),
                "facetGoal": shard.get("facetGoal"),
                "evidenceQuery": shard.get("evidenceQuery"),
                "catalogSeedDimension": shard.get("catalogSeedDimension"),
                "ok": shard.get("ok"),
                "resultCount": shard.get("resultCount"),
                "provider": shard.get("provider"),
                "networkRoute": shard.get("networkRoute"),
                "sourceCapability": shard.get("sourceCapability"),
                "fetchedTopSources": shard.get("fetchedTopSources") or [],
                "errors": shard.get("errors") or [],
            }
            for shard in visible_shards
        ],
        "rawRefs": [{"kind": "web_search_query", "query": shard.get("query"), "shardId": shard.get("shardId")} for shard in visible_shards],
        "omitted": {
            "rawHtml": "stored in web fetch cache when available; not returned to agent-visible output",
            "fullSearchPages": "omitted",
            "shardsOmitted": max(0, len(shards) - len(visible_shards)),
            "rawRefsOmitted": max(0, len(shards) - len(visible_shards)),
            "sourceCatalogRef": "research_source_quality_catalog:v1",
            "popularityMetrics": "best-effort extraction from search title/snippet; verify on-page metrics for final claims",
            "providerAttemptsOmitted": max(0, len(provider_attempt_matrix) - 40),
        },
        "recommendedNextAction": "continue_research",
    }
    bundle["researchAnswerPack"] = _research_answer_pack(bundle)
    delivery_ready = bool(((bundle["researchAnswerPack"].get("score") or {}).get("deliveryReady")))
    bundle["ok"] = delivery_ready
    bundle["answer"] = bundle["researchAnswerPack"].get("answer") or ""
    bundle["resultPreview"] = bundle["answer"]
    bundle["qualityTier"] = (bundle["researchAnswerPack"].get("score") or {}).get("qualityTier")
    bundle["qualityMetrics"] = (bundle["researchAnswerPack"].get("score") or {}).get("acceptanceMetrics") or {}
    bundle["deliveryReady"] = delivery_ready
    bundle["recommendedNextAction"] = "use_research_answer_pack" if delivery_ready else "continue_research"
    return bundle


@tool
def research_broker(
    mode: str = "plan",
    question: str = "",
    query: str = "",
    researchIntent: str = "",
    freshness: str = "auto",
    sourcePolicy: str = "authoritative",
    seedUrls: list[str] | None = None,
    allowedDomains: list[str] | None = None,
    blockedDomains: list[str] | None = None,
    maxShards: int | None = None,
    maxRounds: int | None = None,
    forceRefresh: bool = False,
    deliverable: str = "evidence_bundle",
    evidenceBundleId: str = "",
    experiencePackId: str = "",
    title: str = "",
    tags: list[str] | str | None = None,
    minConfidence: str = "",
    limit: int = 20,
    includeArchived: bool = False,
    confirm: bool = False,
    useAgentBrowserProfile: bool = False,
    tool_call_id: Annotated[str, InjectedToolCallId] = "",
    state: Annotated[dict[str, Any], InjectedState] = None,
) -> str:
    """L2 聚焦证据工具：一个可独立验真的多源问题；返回当前回合 evidence pack，不提供受管进度、恢复或跨阶段 handoff。

    Use this for exactly one focused question needing source comparison, freshness/conflict checks, or a compact
    answer/evidence pack. Use `web_broker` for one URL or narrow lookup. Use one Research episode when there are several
    independent fact domains, managed recovery/progress, or downstream evidence handoff; put every known domain in its
    initial researchBriefIds/researchBriefGoals arrays. A brief already owned by that episode must be repaired there, not through this direct tool.
    Reuse a suitable current experience pack; refresh stale, low-confidence, or conflicting evidence.
    forceRefresh=true is reserved for explicit live validation that must prove a fresh search/read/synthesis pass.
    useAgentBrowserProfile=true directly uses the allowlisted login-backed Agent browser profile.
    """
    config = _research_config()
    normalized_mode = _safe_text(mode).lower() or "plan"
    supported_modes = {
        "plan",
        "run",
        "observe",
        "get_evidence",
        "search_experience",
        "get_experience",
        "promote_experience",
        "archive_experience",
        "restore_experience",
        "delete_experience",
    }
    if normalized_mode not in supported_modes:
        return _render_payload(
            {
                "ok": False,
                "mode": normalized_mode,
                "summary": f"Unsupported research_broker mode: {normalized_mode}",
                "recommendedNextAction": "use plan, search_experience, get_experience, run, observe, get_evidence, promote_experience, archive_experience, restore_experience, or delete_experience",
            }
        )
    if not config["enabled"] and normalized_mode in {"plan", "run"}:
        return _render_payload(
            {
                "ok": False,
                "mode": normalized_mode,
                "summary": "Research Runtime is disabled by supervisor.research.enabled=false.",
                "recommendedNextAction": "enable_research_runtime",
            }
        )

    scope = _ledger_scope(state)
    if normalized_mode == "observe":
        items = list_evidence_bundles(scope=scope, limit=limit)
        summary = research_ledger_summary(scope=scope, include_archived=includeArchived)
        return _render_payload(
            {
                "ok": True,
                "mode": normalized_mode,
                "summary": f"{len(items)} research evidence bundle(s) are available in persistent scope {scope}.",
                "counts": summary.get("counts") or {},
                "items": [
                    {
                        "evidenceBundleId": item.get("evidenceBundleId"),
                        "question": item.get("question"),
                        "confidence": item.get("confidence"),
                        "authorityScore": item.get("authorityScore"),
                        "createdAt": item.get("createdAt"),
                    }
                    for item in items
                ],
                "detailTool": "research_broker(mode='get_evidence', evidenceBundleId=...)",
                "recommendedNextAction": "get_evidence" if items else "search_experience_then_run",
            }
        )

    if normalized_mode == "get_evidence":
        bundle = get_evidence_bundle(_safe_text(evidenceBundleId))
        if bundle:
            answer_pack = _compact_visible_answer_pack(_research_answer_pack(bundle))
            return _render_payload(
                {
                    "ok": True,
                    "mode": normalized_mode,
                    "kind": "research_evidence_bundle",
                    "summary": "Evidence bundle found in persistent research ledger.",
                    "question": bundle.get("question"),
                    "freshness": bundle.get("freshness"),
                    "asOf": answer_pack.get("asOf") or bundle.get("asOf"),
                    "reviewDecision": answer_pack.get("reviewDecision") or bundle.get("reviewDecision"),
                    "independentReview": answer_pack.get("independentReview") or bundle.get("independentReview") or {},
                    "evidenceBundleId": bundle.get("evidenceBundleId"),
                    "answer": answer_pack.get("answer") or "",
                    "researchAnswerPack": answer_pack,
                    "deliveryReady": bool(((answer_pack.get("score") or {}).get("deliveryReady"))),
                    "qualityTier": (answer_pack.get("score") or {}).get("qualityTier"),
                    "qualityMetrics": (answer_pack.get("score") or {}).get("acceptanceMetrics") or {},
                    "detailTool": "research_broker(mode='get_experience', experiencePackId=...)",
                    "recommendedNextAction": "use_evidence_bundle",
                },
                max_chars=60_000,
            )
        return _render_payload(
            {
                "ok": False,
                "mode": normalized_mode,
                "summary": "Evidence bundle not found or expired.",
                "recommendedNextAction": "search_experience_then_run",
            }
        )

    if normalized_mode == "search_experience":
        clean_query = _safe_text(query) or _safe_text(question)
        if not clean_query:
            return _render_payload(
                {
                    "ok": False,
                    "mode": normalized_mode,
                    "summary": "search_experience requires query or question.",
                    "recommendedNextAction": "provide_query",
                }
            )
        packs = search_experience_packs_with_options(
            query=clean_query,
            scope=scope,
            tags=_as_list(tags),
            min_confidence=minConfidence,
            limit=limit,
            include_archived=includeArchived,
        )
        reuse_decision = _experience_reuse_decision(
            packs,
            question=clean_query,
            source_policy=sourcePolicy,
            freshness=freshness,
            min_confidence=minConfidence,
        )
        return _render_payload(
            {
                "ok": True,
                "mode": normalized_mode,
                "kind": "research_experience_search",
                "summary": f"Found {len(packs)} reusable research experience pack(s) for scope {scope}.",
                "query": clean_query,
                "reuseDecision": reuse_decision,
                "items": [
                    {
                        "experiencePackId": item.get("experiencePackId"),
                        "title": item.get("title"),
                        "status": item.get("status"),
                        "confidence": item.get("confidence"),
                        "authorityScore": item.get("authorityScore"),
                        "topicFingerprint": item.get("topicFingerprint"),
                        "sourcePolicy": item.get("sourcePolicy"),
                        "freshnessWindow": item.get("freshnessWindow"),
                        "freshnessState": item.get("freshnessState"),
                        "ageDays": item.get("ageDays"),
                        "staleAt": item.get("staleAt"),
                        "expiresAt": item.get("expiresAt"),
                        "qualityStatus": item.get("qualityStatus"),
                        "invalidationReason": item.get("invalidationReason"),
                        "missingEvidence": list(item.get("missingEvidence") or [])[:3],
                        "answerAvailable": bool(_safe_text(item.get("researchResult"))),
                        "qualityTier": item.get("qualityTier"),
                        "qualityMetrics": item.get("qualityMetrics") or {},
                        "sourceUrls": list(item.get("sourceUrls") or [])[:4],
                        "usageCount": item.get("usageCount"),
                        "lastUsedAt": item.get("lastUsedAt"),
                        "tags": item.get("tags") or [],
                        "sourceMatrixDigest": list(item.get("sourceMatrixDigest") or [])[:4],
                    }
                    for item in packs
                ],
                "detailTool": "research_broker(mode='get_experience', experiencePackId=...)",
                "omitted": {"fullExperiencePack": "use get_experience for the selected pack"},
                "recommendedNextAction": "get_experience" if packs else "run",
            }
        )

    if normalized_mode == "get_experience":
        pack = get_experience_pack(_safe_text(experiencePackId), include_archived=includeArchived)
        return _render_payload(
            {
                "ok": bool(pack),
                "mode": normalized_mode,
                "kind": "research_experience_pack",
                "summary": "Experience pack found." if pack else "Experience pack not found.",
                **({"item": pack} if pack else {}),
                "detailTool": "research_broker(mode='get_evidence', evidenceBundleId=item.createdFromBundleId)",
                "recommendedNextAction": "reuse_experience" if pack else "search_experience_then_run",
            },
            max_chars=8000,
        )

    if normalized_mode == "archive_experience":
        pack = archive_experience_pack(_safe_text(experiencePackId), initiated_by="research_broker")
        return _render_payload(
            {
                "ok": bool(pack),
                "mode": normalized_mode,
                "kind": "research_experience_archive",
                "summary": "Experience pack archived." if pack else "Experience pack not found.",
                **({"item": pack} if pack else {}),
                "recommendedNextAction": "search_experience",
            },
            max_chars=8000,
        )

    if normalized_mode == "restore_experience":
        pack = restore_experience_pack(_safe_text(experiencePackId), initiated_by="research_broker")
        return _render_payload(
            {
                "ok": bool(pack),
                "mode": normalized_mode,
                "kind": "research_experience_restore",
                "summary": "Experience pack restored." if pack else "Experience pack not found.",
                **({"item": pack} if pack else {}),
                "recommendedNextAction": "get_experience" if pack else "search_experience",
            },
            max_chars=8000,
        )

    if normalized_mode == "delete_experience":
        deleted = delete_experience_pack(_safe_text(experiencePackId), confirm=confirm)
        return _render_payload(
            {
                "ok": bool(deleted),
                "mode": normalized_mode,
                "kind": "research_experience_delete",
                "summary": "Experience pack permanently deleted." if deleted else "Experience pack not deleted. Set confirm=true and provide an existing experiencePackId.",
                "experiencePackId": _safe_text(experiencePackId),
                "recommendedNextAction": "search_experience",
            },
            max_chars=4000,
        )

    if normalized_mode == "promote_experience":
        pack = promote_experience_pack(_safe_text(evidenceBundleId), title=title, tags=_as_list(tags))
        return _render_payload(
            {
                "ok": bool(pack),
                "mode": normalized_mode,
                "kind": "research_experience_promotion",
                "summary": "Evidence bundle promoted to reusable experience pack." if pack else "Evidence bundle not found; no experience pack promoted.",
                **({"item": pack} if pack else {}),
                "recommendedNextAction": "get_experience" if pack else "observe_or_run",
            },
            max_chars=8000,
        )

    clean_question = _safe_text(question)
    if not clean_question:
        return _render_payload(
            {
                "ok": False,
                "mode": normalized_mode,
                "summary": "research_broker requires question.",
                "recommendedNextAction": "provide_question",
            }
        )

    seed_urls = _as_list(seedUrls)
    # A URL embedded in the user's brief is an explicit read request even
    # when the caller did not populate the structured ``seedUrls`` field.
    # Keep caller-provided seeds first and append only new URLs from the text;
    # normal domain policy and the shard cap still apply in ``_build_shards``.
    seen_seed_urls = {_safe_text(value).lower() for value in seed_urls if _safe_text(value)}
    for embedded_url in _URL_IN_TEXT_RE.findall(clean_question):
        normalized_url = embedded_url.rstrip(".,;:")
        if normalized_url and normalized_url.lower() not in seen_seed_urls:
            seed_urls.append(normalized_url)
            seen_seed_urls.add(normalized_url.lower())
    allowed_domains = _as_list(allowedDomains)
    blocked_domains = _as_list(blockedDomains)
    structured_facets = _build_explicit_question_facets(clean_question)
    requested_shard_cap = maxShards is not None
    shard_cap = max(1, min(_as_int(maxShards, config["defaultShardCount"]), config["maxShardCount"]))
    if not requested_shard_cap and len(structured_facets) >= 2:
        shard_cap = min(
            config["maxShardCount"],
            max(
                shard_cap,
                len(seed_urls)
                + len(structured_facets)
                + _research_authority_query_budget(len(structured_facets)),
            ),
        )
    round_cap = max(1, min(_as_int(maxRounds, config["maxRounds"]), config["maxRounds"]))
    shards = _build_shards(
        question=clean_question,
        research_intent=researchIntent,
        source_policy=sourcePolicy,
        seed_urls=seed_urls,
        allowed_domains=allowed_domains,
        max_shards=shard_cap,
    )

    if normalized_mode == "plan":
        plan = {
            "ok": True,
            "mode": normalized_mode,
            "kind": "research_plan",
            "summary": f"Planned {len(shards)} read-only research shard(s).",
            "question": clean_question,
            "researchIntent": researchIntent,
            "freshness": freshness,
            "sourcePolicy": sourcePolicy,
            "sourceCatalogRef": "research_source_quality_catalog:v1",
            "experienceFirstPolicy": {
                "summary": "Before running new research, search reusable experience packs for repeat topics.",
                "searchTool": "research_broker(mode='search_experience', query=question)",
                "reuseWhen": ["confidence is medium/high", "scope and freshness still fit", "no material conflict"],
            },
            "shardDefaults": {
                "contextIsolation": "atomic_brief_only",
                "allowedTools": ["source_router_search", "web_read"],
                "sideEffects": "read_only",
                "deadlineMs": _RESEARCH_SHARD_DEADLINE_MS,
                "sourceReadDeadlineMs": _RESEARCH_SOURCE_READ_DEADLINE_MS,
                "useAgentBrowserProfile": bool(useAgentBrowserProfile),
            },
            "limits": {
                "defaultShardCount": config["defaultShardCount"],
                "requestedMaxShards": maxShards,
                "effectiveMaxShards": shard_cap,
                "hardMaxShardCount": config["maxShardCount"],
                "effectiveMaxRounds": round_cap,
                "toolDeadlineMs": _RESEARCH_TOOL_DEADLINE_MS,
                "architectSynthesisDeadlineMs": _RESEARCH_ARCHITECT_SYNTHESIS_DEADLINE_MS,
                "architectAgentTimeoutSeconds": config["architectAgentTimeoutSeconds"],
            },
            "shards": shards,
            "recommendedNextAction": "search_experience_then_run",
        }
        return _render_payload(plan)

    experience_candidates = search_experience_packs_with_options(
        query=clean_question,
        scope=scope,
        tags=_as_list(tags),
        min_confidence=minConfidence,
        limit=3,
        include_archived=False,
    )
    experience_reuse = _experience_reuse_decision(
        experience_candidates,
        question=clean_question,
        source_policy=sourcePolicy,
        freshness=freshness,
        min_confidence=minConfidence,
    )
    if forceRefresh:
        experience_reuse = {
            **experience_reuse,
            "reuseDecision": "refresh",
            "reason": "explicit_force_refresh",
            "candidatePackId": experience_reuse.get("candidatePackId"),
            "skippedSearches": False,
        }
    if experience_reuse.get("reuseDecision") == "reuse" and experience_candidates:
        pack_id = _safe_text(experience_reuse.get("candidatePackId"))
        pack = get_experience_pack(pack_id, record_usage=True) if pack_id else experience_candidates[0]
        if pack:
            reused_bundle = _bundle_from_reused_pack(
                pack,
                question=clean_question,
                reuse=experience_reuse,
                deliverable=deliverable,
            )
            if reused_bundle.get("deliveryReady"):
                return _render_payload(reused_bundle, max_chars=36000)
            experience_reuse = {
                **experience_reuse,
                "reuseDecision": "refresh",
                "reason": "reused_pack_revalidation_failed",
                "skippedSearches": False,
                "revalidationIssues": list(
                    (reused_bundle.get("researchAnswerPack") or {}).get("missingOrStaleReasons")
                    or []
                )[:12],
            }

    query_plan_state: dict[str, Any] = {
        "used": False,
        "reason": "deterministic_query_plan",
        "facetCount": len(structured_facets),
        "plannedShardCount": len(shards),
    }
    if len(structured_facets) >= 2 or (
        not structured_facets and _research_should_decompose_root_question(clean_question)
    ):
        seed_shards = [shard for shard in shards if _safe_text(shard.get("kind")) == "seed_url"]
        planning_facets = structured_facets or [
            (clean_question, "facet:root-question")
        ]
        planned_shards, query_plan_state = _invoke_web_research_architect_query_plan(
            question=clean_question,
            facets=planning_facets,
            source_policy=sourcePolicy,
            freshness=freshness,
            max_shards=max(0, shard_cap - len(seed_shards)),
        )
        if planned_shards:
            planned_shards, catalog_seed_audit = _apply_catalog_official_entity_seeds(
                clean_question,
                planned_shards,
            )
            query_plan_state["catalogOfficialEntitySeeds"] = catalog_seed_audit
            shards = [*seed_shards, *planned_shards][:shard_cap]

    recent_discovery_seeds = _recent_research_discovery_seed_shards(
        clean_question,
        shards,
        scope=scope,
    )
    query_plan_state["recentDiscoverySeedCount"] = len(recent_discovery_seeds)
    read_attempt_ledger = _ResearchReadAttemptLedger(question=clean_question)
    completed_shards, research_loop_state = _run_research_loop(
        question=clean_question,
        initial_shards=shards,
        allowed_domains=allowed_domains,
        blocked_domains=blocked_domains,
        source_policy=sourcePolicy,
        freshness=freshness,
        max_rounds=round_cap,
        use_agent_browser_profile=bool(useAgentBrowserProfile),
        tool_call_id=tool_call_id,
        read_attempt_ledger=read_attempt_ledger,
        recent_discovery_seed_shards=recent_discovery_seeds,
    )
    research_loop_state["queryPlan"] = query_plan_state
    architect_call_state: dict[str, Any] = {
        "fullSynthesisAttempts": 0,
        "gapReviewAttempts": 0,
    }
    bundle = _synthesize_bundle(
        question=clean_question,
        research_intent=researchIntent,
        source_policy=sourcePolicy,
        freshness=freshness,
        shards=completed_shards,
        deliverable=deliverable,
        research_loop_state=research_loop_state,
        experience_reuse=experience_reuse,
        architect_call_state=architect_call_state,
    )
    initial_architect_pack = (
        bundle.get("finalExperiencePack")
        if isinstance(bundle.get("finalExperiencePack"), dict)
        else {}
    )
    initial_model_synthesis = (
        initial_architect_pack.get("modelSynthesis")
        if isinstance(initial_architect_pack.get("modelSynthesis"), dict)
        else {}
    )
    if initial_model_synthesis.get("sameEvidenceReviewRejected") is True:
        research_loop_state["stopReason"] = "same_evidence_review_rejected_after_revision"
    while (
        not bundle.get("deliveryReady")
        and research_loop_state.get("stopReason") != "source_transport_exhausted"
        and len(list(research_loop_state.get("rounds") or [])) < round_cap
        and int(architect_call_state.get("fullSynthesisAttempts") or 0)
        < _MAX_RESEARCH_ARCHITECT_FULL_SYNTHESIS_ATTEMPTS
    ):
        architect_pack = bundle.get("finalExperiencePack") if isinstance(bundle.get("finalExperiencePack"), dict) else {}
        requested_queries = [
            query
            for query in (
                _normalize_research_search_query(value)
                for value in _research_text_list(
                    architect_pack.get("recommendedNextQueries"), limit=4
                )
            )
            if query
        ]
        model_synthesis = architect_pack.get("modelSynthesis") if isinstance(architect_pack.get("modelSynthesis"), dict) else {}
        precise_missing_facet_ids = {
            _safe_text(facet_id)
            for facet_id in _as_list(model_synthesis.get("missingFacetIds"))
            if _safe_text(facet_id)
        }
        if model_synthesis.get("sameEvidenceReviewRejected") is True and not requested_queries:
            research_loop_state["stopReason"] = "same_evidence_review_rejected_after_revision"
            break
        structured_repair = len(
            _required_research_facet_queries(clean_question, completed_shards)
        ) >= 2
        critical_gaps = _research_text_list(
            architect_pack.get("criticalMissingEvidence"),
            limit=4,
        )
        if (
            not requested_queries
            and not critical_gaps
            and _architect_model_failure_without_evidence_gap(model_synthesis)
        ):
            research_loop_state["stopReason"] = (
                "architect_model_failure_without_evidence_gap"
            )
            break
        if not critical_gaps:
            critical_gaps = _research_architect_plan_failure_gaps(
                clean_question,
                model_synthesis,
            )
        targeted_gap_queries = _research_critical_gap_queries(
            clean_question,
            critical_gaps,
            limit=6,
        )
        if targeted_gap_queries:
            # Reviewer-identified omissions have priority over generic
            # "one more source for this facet" repairs.  Keep Architect
            # queries after them so explicit URLs and model-suggested sources
            # remain eligible in the same round.
            requested_queries = [*targeted_gap_queries, *requested_queries]
        if not requested_queries and not structured_repair:
            requested_queries = [
                " ".join((clean_question, gap, "official primary source evidence")).strip()
                for gap in critical_gaps
                if gap
            ][:4]
        seen_queries = {
            _safe_text(shard.get("query")).lower()
            for shard in completed_shards
            if _safe_text(shard.get("query"))
        }
        requested_queries = [query for query in requested_queries if query.lower() not in seen_queries]
        architect_requested_queries = list(requested_queries)
        repair_round = len(list(research_loop_state.get("rounds") or [])) + 1
        repair_shards: list[dict[str, Any]] = []
        existing_document_projection_count = 0
        direct_url_fallbacks: list[dict[str, str]] = []
        seen_repair_urls: set[str] = {
            _research_document_identity(
                _safe_text(item.get("finalUrl") or item.get("url")),
                question=clean_question,
            )
            for shard in completed_shards
            for item in list(shard.get("fetchedTopSources") or [])
            if isinstance(item, dict)
            and item.get("ok")
            and _safe_text(item.get("finalUrl") or item.get("url"))
        }
        skipped_seen_direct_queries: list[str] = []
        if structured_repair:
            repair_facet_stats = _selected_facet_evidence_stats(
                clean_question,
                completed_shards,
                source_policy=sourcePolicy,
            )
            repair_shards = _build_refinement_shards(
                clean_question,
                completed_shards,
                source_policy=sourcePolicy,
                limit=max(
                    4,
                    min(8, len(list(repair_facet_stats.get("missingFacetIds") or []))),
                ),
                round_index=repair_round,
                only_facet_ids=(precise_missing_facet_ids or None),
                force_primary_facet_ids=(precise_missing_facet_ids or None),
            )
            requested_queries = _merge_research_repair_queries(
                architect_requested_queries,
                repair_shards,
            )
        elif not requested_queries:
            repair_shards = _build_refinement_shards(
                clean_question,
                completed_shards,
                source_policy=sourcePolicy,
                limit=4,
                round_index=repair_round,
            )
            requested_queries = [
                _safe_text(shard.get("query"))
                for shard in repair_shards
                if _safe_text(shard.get("query"))
            ]
        repair_shard_index = 0
        for index, query in enumerate(requested_queries, start=1):
            if repair_shards and any(_safe_text(shard.get("query")) == query for shard in repair_shards):
                continue
            direct_urls, unread_direct_urls = _research_unread_direct_urls(
                query,
                question=clean_question,
                read_document_identities=seen_repair_urls,
            )
            if direct_urls and not unread_direct_urls:
                # An Architect URL request means "read this source", not
                # "search the web again for this already-read URL plus its
                # description". Reissuing it was the source of repeated PEP
                # searches in live audits.
                skipped_seen_direct_queries.append(query)
                continue
            direct_url = unread_direct_urls[0] if unread_direct_urls else ""
            if direct_url:
                seen_repair_urls.add(_research_document_identity(direct_url, question=clean_question))
                repair_shard_index += 1
                seed_shard_id = f"shard_architect_repair_{repair_round}_{repair_shard_index}"
                repair_shards.append(
                    {
                        "shardId": seed_shard_id,
                        "kind": "seed_url",
                        "query": direct_url,
                        "seedUrl": direct_url,
                        "evidenceQuery": query,
                        "reason": "web_research_architect_requested_direct_source",
                    }
                )
                repair_query = _safe_text(query.replace(direct_url, " "))
                repair_host = _host(direct_url)
                site_query = " ".join(
                    part
                    for part in (
                        f"site:{repair_host}" if repair_host else "",
                        clean_question,
                        repair_query,
                    )
                    if part
                )
                if (
                    site_query.lower() not in seen_queries
                    and not any(
                        _safe_text(item.get("query")).lower() == site_query.lower()
                        for item in direct_url_fallbacks
                    )
                ):
                    direct_url_fallbacks.append(
                        {
                            "seedShardId": seed_shard_id,
                            "query": site_query,
                            "reason": "verify_or_correct_architect_requested_url",
                        }
                    )
            else:
                repair_shard_index += 1
                repair_shards.append(
                    {
                        "shardId": f"shard_architect_repair_{repair_round}_{repair_shard_index}",
                        "kind": "architect_evidence_repair",
                        "query": query,
                        "reason": "web_research_architect_requested_evidence",
                    }
                )
        projection_shards = _architect_existing_document_projection_shards(
            clean_question,
            requested_queries,
            completed_shards,
            source_policy=sourcePolicy,
            round_index=repair_round,
            limit=8,
        )
        if projection_shards:
            existing_document_projection_count = len(projection_shards)
            repair_shards = [*projection_shards, *repair_shards]
        if not repair_shards:
            break
        already_read_urls = {
            _research_document_identity(
                _safe_text(item.get("finalUrl") or item.get("url")),
                question=clean_question,
            )
            for shard in completed_shards
            for item in list(shard.get("fetchedTopSources") or [])
            if isinstance(item, dict)
            and item.get("ok")
            and _safe_text(item.get("finalUrl") or item.get("url"))
        }
        existing_read_identities = set(already_read_urls)
        existing_projection_identities = _research_evidence_projection_identities(
            clean_question,
            completed_shards,
            source_policy=sourcePolicy,
        )
        repaired = _run_search_shards(
            repair_shards,
            allowed_domains=allowed_domains,
            blocked_domains=blocked_domains,
            source_policy=sourcePolicy,
            max_rounds=round_cap,
            use_agent_browser_profile=bool(useAgentBrowserProfile),
            tool_call_id=tool_call_id,
            already_read_urls=already_read_urls,
            read_attempt_ledger=read_attempt_ledger,
            read_round=repair_round,
        )
        for shard in repaired:
            for item in list(shard.get("fetchedTopSources") or []):
                if not isinstance(item, dict) or not item.get("ok"):
                    continue
                resolved_url = _safe_text(item.get("finalUrl") or item.get("url"))
                if resolved_url:
                    already_read_urls.add(
                        _research_document_identity(resolved_url, question=clean_question)
                    )
        fallback_shards: list[dict[str, Any]] = []
        repaired_by_shard_id = {
            _safe_text(shard.get("shardId")): shard
            for shard in repaired
            if _safe_text(shard.get("shardId"))
        }
        for fallback in direct_url_fallbacks:
            seed_result = repaired_by_shard_id.get(_safe_text(fallback.get("seedShardId")))
            if seed_result and _research_shard_has_readable_fetch(seed_result):
                continue
            repair_shard_index += 1
            fallback_shards.append(
                {
                    "shardId": f"shard_architect_repair_{repair_round}_{repair_shard_index}",
                    "kind": "architect_url_search_fallback",
                    "query": _safe_text(fallback.get("query")),
                    "reason": _safe_text(fallback.get("reason")),
                }
            )
        if fallback_shards:
            repaired.extend(
                _run_search_shards(
                    fallback_shards,
                    allowed_domains=allowed_domains,
                    blocked_domains=blocked_domains,
                    source_policy=sourcePolicy,
                    max_rounds=round_cap,
                    use_agent_browser_profile=bool(useAgentBrowserProfile),
                    tool_call_id=tool_call_id,
                    already_read_urls=already_read_urls,
                    read_attempt_ledger=read_attempt_ledger,
                    read_round=repair_round,
                )
            )
        completed_shards.extend(repaired)
        repaired_read_identities = {
            _research_document_identity(
                _safe_text(item.get("finalUrl") or item.get("url")),
                question=clean_question,
            )
            for shard in repaired
            for item in list(shard.get("fetchedTopSources") or [])
            if isinstance(item, dict) and item.get("ok") and _safe_text(item.get("finalUrl") or item.get("url"))
        }
        materially_new_identities = repaired_read_identities - existing_read_identities
        repaired_projection_identities = _research_evidence_projection_identities(
            clean_question,
            repaired,
            source_policy=sourcePolicy,
        )
        materially_new_projection_identities = (
            repaired_projection_identities - existing_projection_identities
        )
        repair_stats = _selected_source_stats(clean_question, completed_shards, source_policy=sourcePolicy)
        repair_facet_stats = _selected_facet_evidence_stats(
            clean_question,
            completed_shards,
            source_policy=sourcePolicy,
        )
        research_loop_state.setdefault("rounds", []).append(
            {
                "round": repair_round,
                "kind": "architect_evidence_repair",
                "queries": requested_queries,
                "skippedAlreadyReadDirectQueries": skipped_seen_direct_queries,
                "existingDocumentProjectionCount": existing_document_projection_count,
                "directUrlFallbackCount": len(fallback_shards),
                "resultCount": sum(int(shard.get("resultCount") or 0) for shard in repaired),
                "materialEvidenceChanged": bool(
                    materially_new_identities or materially_new_projection_identities
                ),
                "newReadSourceCount": len(materially_new_identities),
                "newEvidenceProjectionCount": len(materially_new_projection_identities),
                "readSourceCount": _read_source_count(completed_shards, question=clean_question),
                **repair_stats,
                "facetEvidence": repair_facet_stats,
            }
        )
        research_loop_state["readSources"] = repair_stats.get("sourceUrls") or []
        research_loop_state["readAttemptStats"] = read_attempt_ledger.snapshot()
        research_loop_state["facetEvidence"] = repair_facet_stats
        research_loop_state["nextQueries"] = requested_queries
        research_loop_state["stopReason"] = (
            "architect_evidence_repair_completed"
            if materially_new_identities or materially_new_projection_identities
            else "architect_evidence_repair_no_new_evidence"
        )
        refreshed_gaps: list[str] = []
        if int(repair_stats.get("selectedSourceCount") or 0) < TARGET_RESEARCH_SOURCE_COUNT:
            refreshed_gaps.append(
                f"Readable source target not met: {repair_stats.get('selectedSourceCount', 0)}/{TARGET_RESEARCH_SOURCE_COUNT}."
            )
        if int(repair_stats.get("distinctHostCount") or 0) < TARGET_RESEARCH_DISTINCT_HOST_COUNT:
            refreshed_gaps.append(
                f"Independent host target not met: {repair_stats.get('distinctHostCount', 0)}/{TARGET_RESEARCH_DISTINCT_HOST_COUNT}."
            )
        if repair_facet_stats.get("missingFacetIds"):
            refreshed_gaps.append(
                "Readable evidence missing for explicit Research facets: "
                + ", ".join(repair_facet_stats.get("missingFacetIds") or [])
                + "."
            )
        refreshed_report = _research_loop_report(clean_question, completed_shards, source_policy=sourcePolicy)
        research_loop_state.update(refreshed_report)
        research_loop_state["uncoveredClaims"] = list(
            dict.fromkeys([*list(refreshed_report.get("uncoveredClaims") or []), *refreshed_gaps])
        )
        if not materially_new_identities and not materially_new_projection_identities:
            research_loop_state["stopReason"] = "architect_evidence_repair_no_new_evidence"
            break
        bundle = _synthesize_bundle(
            question=clean_question,
            research_intent=researchIntent,
            source_policy=sourcePolicy,
            freshness=freshness,
            shards=completed_shards,
            deliverable=deliverable,
            research_loop_state=research_loop_state,
            experience_reuse=experience_reuse,
            architect_call_state=architect_call_state,
        )
        refreshed_architect_pack = (
            bundle.get("finalExperiencePack")
            if isinstance(bundle.get("finalExperiencePack"), dict)
            else {}
        )
        refreshed_model_synthesis = (
            refreshed_architect_pack.get("modelSynthesis")
            if isinstance(refreshed_architect_pack.get("modelSynthesis"), dict)
            else {}
        )
        refreshed_structural_stats = refreshed_model_synthesis.get("structuralStats")
        if isinstance(refreshed_structural_stats, dict) and research_loop_state.get("rounds"):
            research_loop_state["rounds"][-1]["architectStructuralStats"] = dict(
                refreshed_structural_stats
            )
    research_loop_state["architectCallState"] = dict(architect_call_state)
    stored = _store_evidence(bundle, state=state)
    return _render_payload(_visible_bundle(stored), max_chars=36000)
